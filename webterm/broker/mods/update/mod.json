{
  "id": "update",
  "version": "1.0.0",
  "ctxVersion": 1,
  "defaultEnabled": false,
  "title": "Update check",
  "description": "Taskbar chip + window reporting whether this build is current with upstream, via the broker's GET /update/check. Ships disabled; turning it on asks the serving broker to open its own update-check switch, which an operator can override with update_check_enabled in the broker config.",
  "entry": "update.js",
  "author": "JohnConnorNPC",
  "styles": ["update.css"],
  "help": { "slug": "update", "label": "Update check", "order": 2085, "icon": "⬆" }
}
