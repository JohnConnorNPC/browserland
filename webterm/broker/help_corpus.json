{"sections":[{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland is a web-based terminal desktop: you point a browser at the "},{"t":"strong","v":"broker"},{"t":"text","v":" and get a full windowed desktop of live terminals and app windows. This page walks you through your first session."}],"t":"p"}],"search":"overview browserland is a web-based terminal desktop: you point a browser at the broker and get a full windowed desktop of live terminals and app windows. this page walks you through your first session.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Open the broker URL in your browser. By default that is:"}],"t":"p"},{"spans":[{"t":"code","v":"http://127.0.0.1:4445/"}],"t":"pre"},{"spans":[{"t":"text","v":"When the page loads you get the desktop: an empty work area with a "},{"t":"strong","v":"taskbar"},{"t":"text","v":" along the bottom. The shells that Browserland runs keep running even when no browser is attached, so if terminals were started earlier they are still there waiting for you — reopen the tab and the screen heals from a snapshot."}],"t":"p"}],"search":"open the broker open the broker url in your browser. by default that is: http://127.0.0.1:4445/ when the page loads you get the desktop: an empty work area with a taskbar along the bottom. the shells that browserland runs keep running even when no browser is attached, so if terminals were started earlier they are still there waiting for you — reopen the tab and the screen heals from a snapshot.","title":"Open the broker"},{"body":[{"spans":[{"t":"text","v":"You have three quick ways to get a terminal on screen:"}],"t":"p"},{"spans":[{"t":"strong","v":"Click an existing session"},{"t":"text","v":" — if any terminals are already running, their buttons appear in the taskbar. Click one to focus and raise it (or restore it if it was minimized)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The launch button (+)"},{"t":"text","v":" — by default, left-click the "},{"t":"strong","v":"+"},{"t":"text","v":" at the left of the taskbar to launch a default terminal on your default host (the local broker unless you pick another under "},{"t":"strong","v":"Control Panel → Hosts"},{"t":"text","v":" — see Hosts-and-Multi-Browser), and right-click it for the full profile / app menu (other profiles, app windows, and \"Open in folder…\"). If you have added remote hosts, each broker gets a live status row with its profiles below it — and the taskbar's broker badge opens this same menu. A Control Panel toggle can swap these two gestures (see Taskbar)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The \"New terminal\" shortcut"},{"t":"text","v":" — press "},{"t":"kbd","v":"Ctrl+Alt+Enter"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"The shortcut always starts on the "},{"t":"strong","v":"local broker"},{"t":"text","v":"; the quick-launch gesture follows the default-host setting. To start a terminal on any other host, pick its profile from the launch menu. Each terminal is a real shell on its broker's host. Type in it like any terminal; close the window later and the shell keeps running so you can reattach."}],"t":"p"},{"spans":[{"t":"text","v":"From the "},{"t":"strong","v":"+"},{"t":"text","v":" menu you can also open other window types — a sticky note, a text editor, a file manager, a task manager, a scratchpad, and the session recorder. If you enable the "},{"t":"strong","v":"AI status"},{"t":"text","v":" mod (Control Panel → Mods; it ships off by default), an "},{"t":"strong","v":"AI-provider status"},{"t":"text","v":" monitor joins the menu too — enabling it lets the broker check each provider's public status page, so the broker's egress IP becomes visible to those hosts. The "},{"t":"strong","v":"Update check"},{"t":"text","v":" mod adds an "},{"t":"strong","v":"Update check"},{"t":"text","v":" window reporting whether every configured broker is current with upstream; it ships off by default, and turning it on asks the broker serving the page to start checking — that click is the opt-in, and it takes effect without a restart. See Window-Types for what each one does, and Taskbar for the rest of the launch menu."}],"t":"p"}],"search":"launch your first terminal you have three quick ways to get a terminal on screen: click an existing session — if any terminals are already running, their buttons appear in the taskbar. click one to focus and raise it (or restore it if it was minimized). the launch button (+) — by default, left-click the + at the left of the taskbar to launch a default terminal on your default host (the local broker unless you pick another under control panel → hosts — see hosts-and-multi-browser), and right-click it for the full profile / app menu (other profiles, app windows, and \"open in folder…\"). if you have added remote hosts, each broker gets a live status row with its profiles below it — and the taskbar's broker badge opens this same menu. a control panel toggle can swap these two gestures (see taskbar). the \"new terminal\" shortcut — press ctrl+alt+enter . the shortcut always starts on the local broker ; the quick-launch gesture follows the default-host setting. to start a terminal on any other host, pick its profile from the launch menu. each terminal is a real shell on its broker's host. type in it like any terminal; close the window later and the shell keeps running so you can reattach. from the + menu you can also open other window types — a sticky note, a text editor, a file manager, a task manager, a scratchpad, and the session recorder. if you enable the ai status mod (control panel → mods; it ships off by default), an ai-provider status monitor joins the menu too — enabling it lets the broker check each provider's public status page, so the broker's egress ip becomes visible to those hosts. the update check mod adds an update check window reporting whether every configured broker is current with upstream; it ships off by default, and turning it on asks the broker serving the page to start checking — that click is the opt-in, and it takes effect without a restart. see window-types for what each one does, and taskbar for the rest of the launch menu. hosts and multi browser taskbar window types taskbar","title":"Launch your first terminal"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Control Panel"},{"t":"text","v":" is where you configure the desktop. Open it any of these ways:"}],"t":"p"},{"spans":[{"t":"strong","v":"Right-click the launch (+) button"},{"t":"text","v":" and choose "},{"t":"strong","v":"Control panel"},{"t":"text","v":" (that menu uses SVG icons, not emoji)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Right-click the empty desktop or the taskbar"},{"t":"text","v":" and choose "},{"t":"strong","v":"🎛 Control panel"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"Press the "},{"t":"strong","v":"Open control panel"},{"t":"text","v":" shortcut, "},{"t":"kbd","v":"Ctrl+Alt+p"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"It opens as a moveable floating window — drag its title bar, resize it, or minimize it like any other window — with a tab per connected broker."}],"t":"p"},{"spans":[{"t":"text","v":"Applets"}],"t":"sub"},{"spans":[{"t":"text","v":"Inside a broker's tab the settings are grouped into "},{"t":"strong","v":"applets"},{"t":"text","v":": the panel opens on a grid of captioned icons, and you pick a topic first. Clicking one (or pressing Enter on a focused icon) swaps the grid for that applet's settings plus a "},{"t":"strong","v":"Back"},{"t":"text","v":" button. It is one level deep — there is no nesting. The line under the grid describes whichever icon you are hovering or have tabbed to."}],"t":"p"},{"spans":[{"t":"text","v":"Applet · What lives there"}],"t":"sub"},{"spans":[{"t":"strong","v":"Desktop"},{"t":"text","v":" — Theme, background pattern, the clock chip's time zone, the start button's label and gestures, the broker status chip — and the terminal font, via an opt-in mod (off by default; enable it under "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Windows"},{"t":"text","v":" — Window size, window mode (floating vs. tiling — see Window-Modes), taskbar / title labels, drag hold delay (see Snapping-and-Pop-out), window slide speed, the workspace scrollbar, the terminal close button, and the Workspaces settings (see Workspaces)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Input"},{"t":"text","v":" — Keybindings (see Keyboard-Shortcuts) and Clipboard (OSC 52) — whether programs on that host may set your clipboard, off by default"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminals"},{"t":"text","v":" — The default start path, the default terminal profile, and the launch profiles themselves"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Startup"},{"t":"text","v":" — Restore-on-refresh"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Access"},{"t":"text","v":" — MCP — AI-agent access to your terminals (see MCP-and-AI-Agents)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Mods"},{"t":"text","v":" — Which mods this browser runs — and, since it is no longer enable/disable only, "},{"t":"strong","v":"Install a mod…"},{"t":"text","v":" and "},{"t":"strong","v":"Uninstall"},{"t":"text","v":", a shipped-vs-installed badge, and an install preview with advisory warnings (see Installing-Mods). Plus the settings each enabled mod contributes. A mod may also place its control in one of the applets above — the Color scheme radio sits in "},{"t":"strong","v":"Desktop"},{"t":"text","v":", for instance — and a mod-owned setting carries a small badge naming its mod wherever it lands"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Advanced"},{"t":"text","v":" — "},{"t":"strong","v":"Mods on this broker"},{"t":"text","v":": which mods that broker pins on or off for every browser that loads its page — including a remote broker's, from its own tab (see Hosts-and-Multi-Browser)"}],"t":"bullet"},{"spans":[{"t":"text","v":"An applet only appears when it has something to show on the tab you are on. A remote broker's tab has no "},{"t":"strong","v":"Desktop"},{"t":"text","v":" or "},{"t":"strong","v":"Startup"},{"t":"text","v":" icon, for example, because everything in them belongs to the browser you are sitting at rather than to that broker (see Per-browser vs. per-host settings below)."}],"t":"p"},{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Browser"},{"t":"text","v":" tab is not an applet grid — it is short already, and holds the Hosts list, "},{"t":"strong","v":"Broker registry"},{"t":"text","v":", "},{"t":"strong","v":"Sync mods"},{"t":"text","v":" (copy this broker's mod setup to the other brokers you have configured, instead of repeating it on each machine — see Hosts-and-Multi-Browser), and Troubleshooting."}],"t":"p"},{"spans":[{"t":"text","v":"Finding a setting"}],"t":"sub"},{"spans":[{"t":"text","v":"Two ways in, and you do not have to guess which applet something is under:"}],"t":"p"},{"spans":[{"t":"strong","v":"The box above the grid"},{"t":"text","v":" filters as you type. It matches section headings, control labels, hints and the values you have entered, and icons with no match drop out of the grid. When what you typed narrows to a single applet, press "},{"t":"strong","v":"Enter"},{"t":"text","v":" to open it with the matching setting scrolled into view; "},{"t":"strong","v":"Esc"},{"t":"text","v":" clears the box."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Show everything"},{"t":"text","v":" turns the applets off and gives you the whole flat list in one scroll — the browse path, the "},{"t":"kbd","v":"Ctrl+F"},{"t":"text","v":" path, and the escape hatch for when the grouping puts something where you did not look. Filtering while it is on filters that flat list instead of the grid. The toggle is remembered in the browser you set it from and is never shared with anyone else viewing the same broker."}],"t":"bullet"},{"spans":[{"t":"text","v":"Per-browser vs. per-host settings"}],"t":"sub"},{"spans":[{"t":"strong","v":"Browser-global settings"},{"t":"text","v":" follow the browser you are sitting at — theme and background, the terminal font (an opt-in mod, off by default — turn it on under "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":"), the start-button label, restore-on-refresh, when the broker status chip shows, the workspace pager's label mode, visibility and taskbar filter, and the clock chip's time zone."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Per-host settings"},{"t":"text","v":" live on a tab per broker — window mode, drag hold delay, MCP, keyboard shortcuts, the default terminal profile, and the default start path — so each broker remembers its own. "},{"t":"strong","v":"Mods on this broker"},{"t":"text","v":" sits there too, and is the one setting that governs other browsers: it pins mods on or off for everyone who loads that broker's page, so it overrides their per-browser Mods list rather than yours."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Clipboard (OSC 52)"},{"t":"text","v":" is per host like the settings above it, but is the one setting that is also "},{"t":"strong","v":"browser-local"},{"t":"text","v":": it never travels to a broker, in either direction. That is deliberate. It is the switch that decides whether a broker may write to your clipboard, so storing it on that broker would let the broker grant itself the permission. Turning it on for one host has no effect on any other, and it does not follow you to another browser."}],"t":"bullet"}],"search":"the control panel the control panel is where you configure the desktop. open it any of these ways: right-click the launch (+) button and choose control panel (that menu uses svg icons, not emoji). right-click the empty desktop or the taskbar and choose 🎛 control panel . press the open control panel shortcut, ctrl+alt+p . it opens as a moveable floating window — drag its title bar, resize it, or minimize it like any other window — with a tab per connected broker. applets inside a broker's tab the settings are grouped into applets : the panel opens on a grid of captioned icons, and you pick a topic first. clicking one (or pressing enter on a focused icon) swaps the grid for that applet's settings plus a back button. it is one level deep — there is no nesting. the line under the grid describes whichever icon you are hovering or have tabbed to. applet · what lives there desktop — theme, background pattern, the clock chip's time zone, the start button's label and gestures, the broker status chip — and the terminal font, via an opt-in mod (off by default; enable it under control panel → mods ) windows — window size, window mode (floating vs. tiling — see window-modes), taskbar / title labels, drag hold delay (see snapping-and-pop-out), window slide speed, the workspace scrollbar, the terminal close button, and the workspaces settings (see workspaces) input — keybindings (see keyboard-shortcuts) and clipboard (osc 52) — whether programs on that host may set your clipboard, off by default terminals — the default start path, the default terminal profile, and the launch profiles themselves startup — restore-on-refresh access — mcp — ai-agent access to your terminals (see mcp-and-ai-agents) mods — which mods this browser runs — and, since it is no longer enable/disable only, install a mod… and uninstall , a shipped-vs-installed badge, and an install preview with advisory warnings (see installing-mods). plus the settings each enabled mod contributes. a mod may also place its control in one of the applets above — the color scheme radio sits in desktop , for instance — and a mod-owned setting carries a small badge naming its mod wherever it lands advanced — mods on this broker : which mods that broker pins on or off for every browser that loads its page — including a remote broker's, from its own tab (see hosts-and-multi-browser) an applet only appears when it has something to show on the tab you are on. a remote broker's tab has no desktop or startup icon, for example, because everything in them belongs to the browser you are sitting at rather than to that broker (see per-browser vs. per-host settings below). the browser tab is not an applet grid — it is short already, and holds the hosts list, broker registry , sync mods (copy this broker's mod setup to the other brokers you have configured, instead of repeating it on each machine — see hosts-and-multi-browser), and troubleshooting. finding a setting two ways in, and you do not have to guess which applet something is under: the box above the grid filters as you type. it matches section headings, control labels, hints and the values you have entered, and icons with no match drop out of the grid. when what you typed narrows to a single applet, press enter to open it with the matching setting scrolled into view; esc clears the box. show everything turns the applets off and gives you the whole flat list in one scroll — the browse path, the ctrl+f path, and the escape hatch for when the grouping puts something where you did not look. filtering while it is on filters that flat list instead of the grid. the toggle is remembered in the browser you set it from and is never shared with anyone else viewing the same broker. per-browser vs. per-host settings browser-global settings follow the browser you are sitting at — theme and background, the terminal font (an opt-in mod, off by default — turn it on under control panel → mods ), the start-button label, restore-on-refresh, when the broker status chip shows, the workspace pager's label mode, visibility and taskbar filter, and the clock chip's time zone. per-host settings live on a tab per broker — window mode, drag hold delay, mcp, keyboard shortcuts, the default terminal profile, and the default start path — so each broker remembers its own. mods on this broker sits there too, and is the one setting that governs other browsers: it pins mods on or off for everyone who loads that broker's page, so it overrides their per-browser mods list rather than yours. clipboard (osc 52) is per host like the settings above it, but is the one setting that is also browser-local : it never travels to a broker, in either direction. that is deliberate. it is the switch that decides whether a broker may write to your clipboard, so storing it on that broker would let the broker grant itself the permission. turning it on for one host has no effect on any other, and it does not follow you to another browser. window modes snapping and pop out workspaces keyboard shortcuts mcp and ai agents installing mods hosts and multi browser hosts and multi browser","title":"The Control Panel"}],"label":"Getting Started","order":0,"slug":"getting-started"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland ships a set of default keyboard shortcuts for the actions you use most: moving between columns, switching workspaces, launching terminals, and toggling window modes. Every shortcut is rebindable, so you can make the desktop match your muscle memory."}],"t":"p"}],"search":"overview browserland ships a set of default keyboard shortcuts for the actions you use most: moving between columns, switching workspaces, launching terminals, and toggling window modes. every shortcut is rebindable, so you can make the desktop match your muscle memory.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"All shortcuts are user-rebindable in "},{"t":"strong","v":"Control Panel → Input → Keybindings"},{"t":"text","v":". Open the Control Panel (right-click the + button and choose "},{"t":"strong","v":"Control panel"},{"t":"text","v":", or the desktop → "},{"t":"strong","v":"🎛 Control panel"},{"t":"text","v":", or press "},{"t":"kbd","v":"Ctrl+Alt+p"},{"t":"text","v":"), find the action you want, and record a new combo for it."}],"t":"p"},{"spans":[{"t":"text","v":"To change a binding, press that action's "},{"t":"strong","v":"Set"},{"t":"text","v":" button and then the combination you want; "},{"t":"strong","v":"Clear"},{"t":"text","v":" removes the binding. Only "},{"t":"strong","v":"Ctrl / Alt / Meta"},{"t":"text","v":" combinations are accepted, so ordinary typing still reaches the terminal."}],"t":"bullet"},{"spans":[{"t":"text","v":"Keyboard shortcuts are part of the per-host settings (a tab per broker), so a host can carry its own bindings."}],"t":"bullet"},{"spans":[{"t":"text","v":"The in-app guide (the \"?\" chip at the bottom-right of the taskbar) also injects one "},{"t":"strong","v":"Keyboard shortcuts"},{"t":"text","v":" entry per bindable action, showing its current combo or \"Unbound\" — handy for checking what is mapped right now."}],"t":"p"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"strong","v":"Toggle help is unbound by default."},{"t":"text","v":" The \"Toggle help\" action has no default key. Assign your own combo under "},{"t":"strong","v":"Control Panel → Input → Keybindings"},{"t":"text","v":" if you want to open the in-app guide from the keyboard. (You can always open it by clicking the \"?\" chip at the bottom-right of the taskbar.)"}],"t":"p"}],"search":"how to rebind a shortcut all shortcuts are user-rebindable in control panel → input → keybindings . open the control panel (right-click the + button and choose control panel , or the desktop → 🎛 control panel , or press ctrl+alt+p ), find the action you want, and record a new combo for it. to change a binding, press that action's set button and then the combination you want; clear removes the binding. only ctrl / alt / meta combinations are accepted, so ordinary typing still reaches the terminal. keyboard shortcuts are part of the per-host settings (a tab per broker), so a host can carry its own bindings. the in-app guide (the \"?\" chip at the bottom-right of the taskbar) also injects one keyboard shortcuts entry per bindable action, showing its current combo or \"unbound\" — handy for checking what is mapped right now. tip: toggle help is unbound by default. the \"toggle help\" action has no default key. assign your own combo under control panel → input → keybindings if you want to open the in-app guide from the keyboard. (you can always open it by clicking the \"?\" chip at the bottom-right of the taskbar.) getting started","title":"How to rebind a shortcut"},{"body":[{"spans":[{"t":"text","v":"A few keys inside a terminal are fixed rather than rebindable: "},{"t":"kbd","v":"Ctrl+Shift+C"},{"t":"text","v":" copies the current selection, "},{"t":"kbd","v":"Ctrl+Shift+V"},{"t":"text","v":" pastes, and on an https (or localhost) page "},{"t":"kbd","v":"Alt+V"},{"t":"text","v":" checks the clipboard for an image and pastes it into the terminal as an uploaded file path — with no image on the clipboard the keypress reaches the app unchanged, so Claude Code keeps its own "},{"t":"kbd","v":"Alt+V"},{"t":"text","v":" hotkey. See Window Types for how image paste works."}],"t":"p"},{"spans":[{"t":"strong","v":"Copy and paste into a terminal."},{"t":"text","v":" Selecting text with the mouse copies it the moment you release the button — there is no copy key to press. "},{"t":"kbd","v":"Ctrl+Shift+C"},{"t":"text","v":" is the explicit chord for the same thing, and it exists because plain "},{"t":"kbd","v":"Ctrl+C"},{"t":"text","v":" is left alone so it can still interrupt whatever is running. To paste, use your browser's paste shortcut ("},{"t":"kbd","v":"Ctrl+V"},{"t":"text","v":", or "},{"t":"kbd","v":"Cmd+V"},{"t":"text","v":" on macOS), "},{"t":"kbd","v":"Ctrl+Shift+V"},{"t":"text","v":", or right-click. Right-click pastes directly on an https or "},{"t":"code","v":"localhost"},{"t":"text","v":" page, though a browser may ask you to confirm the first time; on a plain-http LAN origin ("},{"t":"code","v":"http://192.168.1.10:4445"},{"t":"text","v":") the browser refuses to hand the page your clipboard at all, so right-click is left alone and opens the browser's own menu, with its "},{"t":"strong","v":"Paste"},{"t":"text","v":" entry, instead. The keyboard paste works on both, because there the text arrives with the keypress rather than being read out of the clipboard — as does any other browser paste action, including paste-as-plain-text, since terminal input is plain text either way. A multi-line paste goes in as one block rather than as separate lines, so a pasted command is not submitted at its first newline."}],"t":"p"},{"spans":[{"t":"strong","v":"Copying from inside a full-screen program."},{"t":"text","v":" A program with its own copy key — tmux copy-mode, Neovim, "},{"t":"code","v":"lazygit"},{"t":"text","v":", a pager — cannot reach your laptop's clipboard directly, so it asks the terminal to do it for it. Browserland can honour that request, but it is "},{"t":"strong","v":"off by default"},{"t":"text","v":" and you turn it on "},{"t":"strong","v":"per host"},{"t":"text","v":", under "},{"t":"strong","v":"Control Panel → that host's tab → Input → Clipboard (OSC 52)"},{"t":"text","v":". The reason it is off is that everything downstream of the terminal can send this request: not just the program you are using, but a "},{"t":"code","v":"cat"},{"t":"text","v":" of a hostile file, a dependency's build output, or an SSH session to a machine you don't own — and the clipboard it would write is on the computer you are sitting at, not on the remote host. Enabling it for your own broker is a small step; enabling it for a box you don't control is a real one, which is why each host is a separate switch. The switch is stored in "},{"t":"strong","v":"this browser only"},{"t":"text","v":" and never travels to a broker, so a broker cannot turn it on for itself."}],"t":"p"},{"spans":[{"t":"text","v":"Once it is on, a copy still needs the page to be on https or "},{"t":"code","v":"localhost"},{"t":"text","v":", the window focused, and the copy to have followed something you actually did in the front terminal — a copy request that arrives while you are in another tab is dropped. Every refusal says so, and so does every accepted copy, naming the terminal it came from. Copies are capped at 1 MiB and are refused whole rather than truncated. A few deliberate limits worth knowing: text that is not valid UTF-8, or that contains control characters, is refused rather than silently mangled; in Neovim "},{"t":"code","v":"\"+y"},{"t":"text","v":" works but "},{"t":"code","v":"\"*y"},{"t":"text","v":" does not, because the browser has one clipboard and no separate X11 primary selection to write; and a program can never read your clipboard this way — that request is refused outright and is not behind any setting. tmux emits the request out of the box; Neovim needs "},{"t":"code","v":"set clipboard+=unnamedplus"},{"t":"text","v":" with its OSC 52 provider, or an explicit "},{"t":"code","v":"vim.g.clipboard"},{"t":"text","v":" OSC 52 block."}],"t":"p"},{"spans":[{"t":"text","v":"There is also a rate limit: at most "},{"t":"strong","v":"5 accepted copies in any 10 seconds"},{"t":"text","v":", so a program stuck in a loop cannot hold your clipboard hostage. A host you have switched "},{"t":"strong","v":"off"},{"t":"text","v":" may ask again after "},{"t":"strong","v":"60 seconds"},{"t":"text","v":" — the offer is not a once-per-page-load one, or a program emitting OSC 52 at startup would consume it and your own copy ten minutes later would fail in exactly the silence this feature exists to remove."}],"t":"p"},{"spans":[{"t":"strong","v":"When a full-screen app grabs the mouse."},{"t":"text","v":" Apps like "},{"t":"code","v":"lazygit"},{"t":"text","v":", "},{"t":"code","v":"btop"},{"t":"text","v":" and "},{"t":"code","v":"mc"},{"t":"text","v":" switch mouse reporting on, and from that moment the terminal forwards clicks, drags and the wheel to the app instead of the browser: drag-select stops highlighting anything, and the wheel scrolls the app's own pane. Hold "},{"t":"code","v":"Shift"},{"t":"text","v":" and drag to force a selection anyway — on macOS hold "},{"t":"code","v":"Option"},{"t":"text","v":" instead, because "},{"t":"code","v":"Shift"},{"t":"text","v":" is not the Mac gesture — and "},{"t":"code","v":"Shift+scroll"},{"t":"text","v":" scrolls the terminal rather than the app. Both are taken before the app ever sees them, so they work no matter what it has enabled. ("},{"t":"code","v":"Shift+scroll"},{"t":"text","v":" only has somewhere to go if there is scrollback: an app drawing on the alternate screen, which most full-screen ones do, has none, so there its own scroll keys are what you want.) Plain "},{"t":"kbd","v":"Ctrl+C"},{"t":"text","v":" keeps its usual meaning and goes to the app — that is the point, it interrupts — which is exactly why the copy chord is "},{"t":"kbd","v":"Ctrl+Shift+C"},{"t":"text","v":"; pasting is unaffected, because that is the browser's own shortcut rather than a key the app ever sees. The "},{"t":"strong","v":"Mouse mode chip"},{"t":"text","v":" mod is "},{"t":"strong","v":"on by default"},{"t":"text","v":", so a 🖱 appears in the window's title bar for exactly as long as mouse reporting is on, with the escape gesture in its tooltip. (You can turn it off under Control Panel → Mods.)"}],"t":"p"}],"search":"fixed terminal keys a few keys inside a terminal are fixed rather than rebindable: ctrl+shift+c copies the current selection, ctrl+shift+v pastes, and on an https (or localhost) page alt+v checks the clipboard for an image and pastes it into the terminal as an uploaded file path — with no image on the clipboard the keypress reaches the app unchanged, so claude code keeps its own alt+v hotkey. see window types for how image paste works. copy and paste into a terminal. selecting text with the mouse copies it the moment you release the button — there is no copy key to press. ctrl+shift+c is the explicit chord for the same thing, and it exists because plain ctrl+c is left alone so it can still interrupt whatever is running. to paste, use your browser's paste shortcut ( ctrl+v , or cmd+v on macos), ctrl+shift+v , or right-click. right-click pastes directly on an https or localhost page, though a browser may ask you to confirm the first time; on a plain-http lan origin ( http://192.168.1.10:4445 ) the browser refuses to hand the page your clipboard at all, so right-click is left alone and opens the browser's own menu, with its paste entry, instead. the keyboard paste works on both, because there the text arrives with the keypress rather than being read out of the clipboard — as does any other browser paste action, including paste-as-plain-text, since terminal input is plain text either way. a multi-line paste goes in as one block rather than as separate lines, so a pasted command is not submitted at its first newline. copying from inside a full-screen program. a program with its own copy key — tmux copy-mode, neovim, lazygit , a pager — cannot reach your laptop's clipboard directly, so it asks the terminal to do it for it. browserland can honour that request, but it is off by default and you turn it on per host , under control panel → that host's tab → input → clipboard (osc 52) . the reason it is off is that everything downstream of the terminal can send this request: not just the program you are using, but a cat of a hostile file, a dependency's build output, or an ssh session to a machine you don't own — and the clipboard it would write is on the computer you are sitting at, not on the remote host. enabling it for your own broker is a small step; enabling it for a box you don't control is a real one, which is why each host is a separate switch. the switch is stored in this browser only and never travels to a broker, so a broker cannot turn it on for itself. once it is on, a copy still needs the page to be on https or localhost , the window focused, and the copy to have followed something you actually did in the front terminal — a copy request that arrives while you are in another tab is dropped. every refusal says so, and so does every accepted copy, naming the terminal it came from. copies are capped at 1 mib and are refused whole rather than truncated. a few deliberate limits worth knowing: text that is not valid utf-8, or that contains control characters, is refused rather than silently mangled; in neovim \"+y works but \"*y does not, because the browser has one clipboard and no separate x11 primary selection to write; and a program can never read your clipboard this way — that request is refused outright and is not behind any setting. tmux emits the request out of the box; neovim needs set clipboard+=unnamedplus with its osc 52 provider, or an explicit vim.g.clipboard osc 52 block. there is also a rate limit: at most 5 accepted copies in any 10 seconds , so a program stuck in a loop cannot hold your clipboard hostage. a host you have switched off may ask again after 60 seconds — the offer is not a once-per-page-load one, or a program emitting osc 52 at startup would consume it and your own copy ten minutes later would fail in exactly the silence this feature exists to remove. when a full-screen app grabs the mouse. apps like lazygit , btop and mc switch mouse reporting on, and from that moment the terminal forwards clicks, drags and the wheel to the app instead of the browser: drag-select stops highlighting anything, and the wheel scrolls the app's own pane. hold shift and drag to force a selection anyway — on macos hold option instead, because shift is not the mac gesture — and shift+scroll scrolls the terminal rather than the app. both are taken before the app ever sees them, so they work no matter what it has enabled. ( shift+scroll only has somewhere to go if there is scrollback: an app drawing on the alternate screen, which most full-screen ones do, has none, so there its own scroll keys are what you want.) plain ctrl+c keeps its usual meaning and goes to the app — that is the point, it interrupts — which is exactly why the copy chord is ctrl+shift+c ; pasting is unaffected, because that is the browser's own shortcut rather than a key the app ever sees. the mouse mode chip mod is on by default , so a 🖱 appears in the window's title bar for exactly as long as mouse reporting is on, with the escape gesture in its tooltip. (you can turn it off under control panel → mods.) window types","title":"Fixed terminal keys"},{"body":[{"spans":[{"t":"text","v":"The table above is authoritative. The notes below group the actions so you know where each one applies."}],"t":"p"},{"spans":[{"t":"text","v":"Columns (tiling mode)"}],"t":"sub"},{"spans":[{"t":"text","v":"Focus column left/right and move column left/right shift your focus and your columns along the tiling strip. They apply when you are in tiling mode. See Columns and Widths for width presets and how columns work, and Window Modes for switching into tiling mode."}],"t":"p"},{"spans":[{"t":"text","v":"Workspaces"}],"t":"sub"},{"spans":[{"t":"text","v":"Previous/next workspace step through your virtual desktops; the \"Go to workspace 1–5\" actions jump straight to one. See Workspaces for the pager and sending windows between desktops."}],"t":"p"},{"spans":[{"t":"text","v":"These seven come from the "},{"t":"strong","v":"Workspaces"},{"t":"text","v":" mod (enabled by default), so they leave this list if you turn it off — and your bindings for them come back untouched when you turn it back on."}],"t":"p"},{"spans":[{"t":"text","v":"Windows"}],"t":"sub"},{"spans":[{"t":"text","v":"New terminal launches the "},{"t":"strong","v":"local broker's"},{"t":"text","v":" default profile. Close focused window and Minimize focused window act on the front window."}],"t":"p"},{"spans":[{"t":"text","v":"App-wide"}],"t":"sub"},{"spans":[{"t":"text","v":"Toggle tiling mode switches between floating and tiling (see Window Modes). Toggle fullscreen, Open control panel, and Toggle help round out the global actions — Toggle help is unbound until you assign it a combo."}],"t":"p"}],"search":"what the actions do the table above is authoritative. the notes below group the actions so you know where each one applies. columns (tiling mode) focus column left/right and move column left/right shift your focus and your columns along the tiling strip. they apply when you are in tiling mode. see columns and widths for width presets and how columns work, and window modes for switching into tiling mode. workspaces previous/next workspace step through your virtual desktops; the \"go to workspace 1–5\" actions jump straight to one. see workspaces for the pager and sending windows between desktops. these seven come from the workspaces mod (enabled by default), so they leave this list if you turn it off — and your bindings for them come back untouched when you turn it back on. windows new terminal launches the local broker's default profile. close focused window and minimize focused window act on the front window. app-wide toggle tiling mode switches between floating and tiling (see window modes). toggle fullscreen, open control panel, and toggle help round out the global actions — toggle help is unbound until you assign it a combo. columns and widths window modes workspaces window modes","title":"What the actions do"}],"label":"Keyboard Shortcuts","order":1,"slug":"keyboard-shortcuts"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland has two window modes that change how your windows are placed on the desktop: "},{"t":"strong","v":"floating"},{"t":"text","v":" and "},{"t":"strong","v":"tiling"},{"t":"text","v":". This page explains the difference, how to switch, and how the tiling strip works."}],"t":"p"}],"search":"overview browserland has two window modes that change how your windows are placed on the desktop: floating and tiling . this page explains the difference, how to switch, and how the tiling strip works.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Mode · What it does"}],"t":"sub"},{"spans":[{"t":"strong","v":"Floating"},{"t":"text","v":" — Free-moving, overlapping windows. You place each window anywhere, drag its title bar to move it, and drag its edges or corners to resize it. Windows can sit on top of one another."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tiling"},{"t":"text","v":" — A niri-style horizontal strip of "},{"t":"strong","v":"non-overlapping columns"},{"t":"text","v":". Windows live side by side in columns; nothing overlaps, and the strip scrolls left and right when there are more columns than fit on screen."}],"t":"bullet"},{"spans":[{"t":"text","v":"The window mode is a per-host setting (one tab per broker), so each host can be in its own mode."}],"t":"p"}],"search":"the two modes mode · what it does floating — free-moving, overlapping windows. you place each window anywhere, drag its title bar to move it, and drag its edges or corners to resize it. windows can sit on top of one another. tiling — a niri-style horizontal strip of non-overlapping columns . windows live side by side in columns; nothing overlaps, and the strip scrolls left and right when there are more columns than fit on screen. the window mode is a per-host setting (one tab per broker), so each host can be in its own mode.","title":"The two modes"},{"body":[{"spans":[{"t":"text","v":"You can flip between floating and tiling two ways:"}],"t":"p"},{"spans":[{"t":"strong","v":"Control Panel → Windows → Window mode."},{"t":"text","v":" Open the Control Panel (right-click the + button and choose "},{"t":"strong","v":"Control panel"},{"t":"text","v":", or the desktop → "},{"t":"strong","v":"🎛 Control panel"},{"t":"text","v":", or press "},{"t":"kbd","v":"Ctrl+Alt+p"},{"t":"text","v":"), then pick the window mode."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The \"Toggle tiling mode\" shortcut"},{"t":"text","v":" — default "},{"t":"kbd","v":"Ctrl+Alt+t"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"The shortcut is rebindable like every other keyboard action. See Keyboard-Shortcuts to change it."}],"t":"p"}],"search":"switching modes you can flip between floating and tiling two ways: control panel → windows → window mode. open the control panel (right-click the + button and choose control panel , or the desktop → 🎛 control panel , or press ctrl+alt+p ), then pick the window mode. the \"toggle tiling mode\" shortcut — default ctrl+alt+t . the shortcut is rebindable like every other keyboard action. see keyboard-shortcuts to change it. getting started keyboard shortcuts","title":"Switching modes"},{"body":[{"spans":[{"t":"text","v":"In tiling mode, windows live in "},{"t":"strong","v":"columns"},{"t":"text","v":" arranged along a horizontal strip. The strip can be wider than your screen, so only some columns are visible at once and the rest scroll into view."}],"t":"p"},{"spans":[{"t":"text","v":"Scrolling the strip"}],"t":"sub"},{"spans":[{"t":"text","v":"There are three ways to move the strip left and right:"}],"t":"p"},{"spans":[{"t":"text","v":"Use the "},{"t":"strong","v":"strip scrollbar"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Drag a window near the strip edge"},{"t":"text","v":" — dragging toward the left or right edge scrolls the strip in that direction."}],"t":"bullet"},{"spans":[{"t":"text","v":"Use the "},{"t":"strong","v":"focus-column"},{"t":"text","v":" and "},{"t":"strong","v":"move-column"},{"t":"text","v":" shortcuts to bring off-screen columns into view as you focus or move them."}],"t":"bullet"},{"spans":[{"t":"text","v":"Action · Default binding"}],"t":"sub"},{"spans":[{"t":"text","v":"Focus column left — "},{"t":"kbd","v":"Ctrl+Alt+ArrowLeft"}],"t":"bullet"},{"spans":[{"t":"text","v":"Focus column right — "},{"t":"kbd","v":"Ctrl+Alt+ArrowRight"}],"t":"bullet"},{"spans":[{"t":"text","v":"Move column left — "},{"t":"kbd","v":"Ctrl+Alt+Shift+ArrowLeft"}],"t":"bullet"},{"spans":[{"t":"text","v":"Move column right — "},{"t":"kbd","v":"Ctrl+Alt+Shift+ArrowRight"}],"t":"bullet"},{"spans":[{"t":"text","v":"For column widths, moving and focusing columns, and ejecting a window into its own column, see Columns and Widths."}],"t":"p"}],"search":"the tiling strip in tiling mode, windows live in columns arranged along a horizontal strip. the strip can be wider than your screen, so only some columns are visible at once and the rest scroll into view. scrolling the strip there are three ways to move the strip left and right: use the strip scrollbar . drag a window near the strip edge — dragging toward the left or right edge scrolls the strip in that direction. use the focus-column and move-column shortcuts to bring off-screen columns into view as you focus or move them. action · default binding focus column left — ctrl+alt+arrowleft focus column right — ctrl+alt+arrowright move column left — ctrl+alt+shift+arrowleft move column right — ctrl+alt+shift+arrowright for column widths, moving and focusing columns, and ejecting a window into its own column, see columns and widths. columns and widths","title":"The tiling strip"},{"body":[{"spans":[{"t":"text","v":"You don't have to switch the whole desktop to move a single window between styles:"}],"t":"p"},{"spans":[{"t":"strong","v":"Snap a floating window into the strip"},{"t":"text","v":", or "},{"t":"strong","v":"pop a tiled window out to a float"},{"t":"text","v":", using the hold-to-snap and pop-out drag gestures — see Snapping and Pop-out."}],"t":"bullet"},{"spans":[{"t":"text","v":"Inside tiling mode, organize windows into "},{"t":"strong","v":"tabs, splits, and rows"},{"t":"text","v":" by dragging them onto each other — see Arranging Windows."}],"t":"bullet"}],"search":"moving windows between the two modes you don't have to switch the whole desktop to move a single window between styles: snap a floating window into the strip , or pop a tiled window out to a float , using the hold-to-snap and pop-out drag gestures — see snapping and pop-out. inside tiling mode, organize windows into tabs, splits, and rows by dragging them onto each other — see arranging windows. snapping and pop out arranging windows","title":"Moving windows between the two modes"}],"label":"Window Modes","order":2,"slug":"window-modes"},{"cards":[{"body":[{"spans":[{"t":"text","v":"In tiling mode you build your layout by "},{"t":"strong","v":"dragging windows onto each other"},{"t":"text","v":". Where you drop decides what happens: stack windows as tabs, split a tile side-by-side, stack panes as rows, or spin off a brand-new column. This page covers all three combine gestures plus the drop-zone overlay that previews every landing spot."}],"t":"p"},{"spans":[{"t":"text","v":"These gestures apply to the tiling strip. For the float/tile hold gestures (snapping a floating window in, popping a tiled one out), see Snapping-and-Pop-out. For sizing and moving whole columns, see Columns-and-Widths. Most of these actions also have a right-click menu equivalent — see Context-Menus."}],"t":"p"}],"search":"overview in tiling mode you build your layout by dragging windows onto each other . where you drop decides what happens: stack windows as tabs, split a tile side-by-side, stack panes as rows, or spin off a brand-new column. this page covers all three combine gestures plus the drop-zone overlay that previews every landing spot. these gestures apply to the tiling strip. for the float/tile hold gestures (snapping a floating window in, popping a tiled one out), see snapping-and-pop-out. for sizing and moving whole columns, see columns-and-widths. most of these actions also have a right-click menu equivalent — see context-menus. snapping and pop out columns and widths context menus","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Tabbed windows share a single tile and show a "},{"t":"strong","v":"tab strip"},{"t":"text","v":"; only one is visible at a time."}],"t":"p"},{"spans":[{"t":"text","v":"Drag a window onto another window's "},{"t":"strong","v":"top band"},{"t":"text","v":" to stack them as tabs sharing one tile. (A floating window can also be Alt-dragged onto a target to dock as a tab in its column — see Snapping-and-Pop-out.)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Click a tab in the tile's tab strip to switch between the tabbed windows."}],"t":"bullet"},{"spans":[{"t":"text","v":"The title-bar menu offers the same thing without dragging:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · What it does"}],"t":"sub"},{"spans":[{"t":"code","v":"Tab into left column"},{"t":"text","v":" — Tab this window into the neighbor column's live tile (to the left)"}],"t":"bullet"},{"spans":[{"t":"code","v":"Tab into right column"},{"t":"text","v":" — Tab this window into the neighbor column's live tile (to the right)"}],"t":"bullet"},{"spans":[{"t":"code","v":"Tab this window"},{"t":"text","v":" — Seed or keep a tab strip in place"}],"t":"bullet"},{"spans":[{"t":"text","v":"Untabbing"}],"t":"sub"},{"spans":[{"t":"text","v":"To pull a window back out of a tab strip, use the title-bar / tab context menu:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · What it does"}],"t":"sub"},{"spans":[{"t":"code","v":"Untab tile (split to rows)"},{"t":"text","v":" — Breaks the tab strip into stacked rows"}],"t":"bullet"},{"spans":[{"t":"code","v":"Untab cell (side by side)"},{"t":"text","v":" — Drops a nested split-group's tabs out as adjacent side-by-side cells"}],"t":"bullet"}],"search":"tabs: stack windows in one tile tabbed windows share a single tile and show a tab strip ; only one is visible at a time. drag a window onto another window's top band to stack them as tabs sharing one tile. (a floating window can also be alt-dragged onto a target to dock as a tab in its column — see snapping-and-pop-out.) click a tab in the tile's tab strip to switch between the tabbed windows. the title-bar menu offers the same thing without dragging: menu item · what it does tab into left column — tab this window into the neighbor column's live tile (to the left) tab into right column — tab this window into the neighbor column's live tile (to the right) tab this window — seed or keep a tab strip in place untabbing to pull a window back out of a tab strip, use the title-bar / tab context menu: menu item · what it does untab tile (split to rows) — breaks the tab strip into stacked rows untab cell (side by side) — drops a nested split-group's tabs out as adjacent side-by-side cells snapping and pop out","title":"Tabs: stack windows in one tile"},{"body":[{"spans":[{"t":"text","v":"A split places two windows next to each other inside one tile."}],"t":"p"},{"spans":[{"t":"text","v":"Drag a window onto the "},{"t":"strong","v":"LEFT or RIGHT interior"},{"t":"text","v":" of another window to split that tile horizontally, placing the two side-by-side."}],"t":"bullet"},{"spans":[{"t":"text","v":"Drag the "},{"t":"strong","v":"gutter"},{"t":"text","v":" between the panes to resize them."}],"t":"bullet"},{"spans":[{"t":"text","v":"Un-splitting"}],"t":"sub"},{"spans":[{"t":"code","v":"Un-split row (split to rows)"},{"t":"text","v":" in the window context menu explodes a split row into stacked rows."}],"t":"bullet"},{"spans":[{"t":"code","v":"Untab cell (side by side)"},{"t":"text","v":" appears only when the pane is a nested tab-group cell, dropping its tabs out side by side."}],"t":"bullet"},{"spans":[{"t":"text","v":"There is no menu item that merges a split back into one pane — to dissolve a split differently, just drag a pane somewhere else."}],"t":"bullet"}],"search":"splits: side-by-side panes a split places two windows next to each other inside one tile. drag a window onto the left or right interior of another window to split that tile horizontally, placing the two side-by-side. drag the gutter between the panes to resize them. un-splitting un-split row (split to rows) in the window context menu explodes a split row into stacked rows. untab cell (side by side) appears only when the pane is a nested tab-group cell, dropping its tabs out side by side. there is no menu item that merges a split back into one pane — to dissolve a split differently, just drag a pane somewhere else.","title":"Splits: side-by-side panes"},{"body":[{"spans":[{"t":"text","v":"Drag a window onto another window's "},{"t":"strong","v":"TOP or BOTTOM edge"},{"t":"text","v":" to stack them as rows within the same column."}],"t":"bullet"},{"spans":[{"t":"text","v":"Drag the "},{"t":"strong","v":"horizontal gutter"},{"t":"text","v":" to resize the rows."}],"t":"bullet"}],"search":"stacks: rows in a column drag a window onto another window's top or bottom edge to stack them as rows within the same column. drag the horizontal gutter to resize the rows.","title":"Stacks: rows in a column"},{"body":[{"spans":[{"t":"text","v":"While you drag, a highlighted overlay previews exactly where the window will land. Aim for the zone that matches the layout you want:"}],"t":"p"},{"spans":[{"t":"text","v":"Drop on… · Result"}],"t":"sub"},{"spans":[{"t":"text","v":"A column's far "},{"t":"strong","v":"left / right edge"},{"t":"text","v":" — A "},{"t":"strong","v":"NEW column"}],"t":"bullet"},{"spans":[{"t":"text","v":"A window's "},{"t":"strong","v":"top band"},{"t":"text","v":" — "},{"t":"strong","v":"TAB"},{"t":"text","v":" into that tile"}],"t":"bullet"},{"spans":[{"t":"text","v":"A window's interior "},{"t":"strong","v":"left / right quadrant"},{"t":"text","v":" — A "},{"t":"strong","v":"SPLIT"},{"t":"text","v":" (side by side)"}],"t":"bullet"},{"spans":[{"t":"text","v":"A window's interior "},{"t":"strong","v":"top / bottom quadrant"},{"t":"text","v":" — A "},{"t":"strong","v":"SPLIT"},{"t":"text","v":" into rows"}],"t":"bullet"},{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"bottom band"},{"t":"text","v":" of the desktop — "},{"t":"strong","v":"FLOAT"},{"t":"text","v":" the window"}],"t":"bullet"},{"spans":[{"t":"text","v":"Watch the overlay before you release: if the highlight doesn't show the layout you want, move the cursor to a different zone and the preview updates. Dropping on a column edge is the drag equivalent of the menu's \"Move to new column\" — see Columns-and-Widths for column moves and width presets."}],"t":"p"}],"search":"the drop-zone cheat sheet while you drag, a highlighted overlay previews exactly where the window will land. aim for the zone that matches the layout you want: drop on… · result a column's far left / right edge — a new column a window's top band — tab into that tile a window's interior left / right quadrant — a split (side by side) a window's interior top / bottom quadrant — a split into rows the bottom band of the desktop — float the window watch the overlay before you release: if the highlight doesn't show the layout you want, move the cursor to a different zone and the preview updates. dropping on a column edge is the drag equivalent of the menu's \"move to new column\" — see columns-and-widths for column moves and width presets. columns and widths","title":"The drop-zone cheat sheet"}],"label":"Arranging Windows","order":3,"slug":"arranging-windows"},{"cards":[{"body":[{"spans":[{"t":"text","v":"In tiling mode, every window lives in a "},{"t":"strong","v":"column"},{"t":"text","v":" on the horizontal strip. This page covers how to resize those columns with width presets, move and focus columns from the keyboard, and split a shared column out into its own."}],"t":"p"},{"spans":[{"t":"text","v":"These controls only apply in "},{"t":"strong","v":"tiling mode"},{"t":"text","v":". For an overview of floating vs. tiling and the strip itself, see Window Modes. For tabs, splits, and the drag-to-merge drop zones, see Arranging Windows."}],"t":"p"}],"search":"overview in tiling mode, every window lives in a column on the horizontal strip. this page covers how to resize those columns with width presets, move and focus columns from the keyboard, and split a shared column out into its own. these controls only apply in tiling mode . for an overview of floating vs. tiling and the strip itself, see window modes. for tabs, splits, and the drag-to-merge drop zones, see arranging windows. window modes arranging windows","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"A tiled column can be sized to one of four widths from the window context menu (right-click the title bar) or the width controls. The current width is marked with a check."}],"t":"p"},{"spans":[{"t":"text","v":"Menu label · Width"}],"t":"sub"},{"spans":[{"t":"code","v":"⅓"},{"t":"text","v":" — One third of the strip"}],"t":"bullet"},{"spans":[{"t":"code","v":"½"},{"t":"text","v":" — Half of the strip"}],"t":"bullet"},{"spans":[{"t":"code","v":"⅔"},{"t":"text","v":" — Two thirds of the strip"}],"t":"bullet"},{"spans":[{"t":"code","v":"max"},{"t":"text","v":" — Maximum width"}],"t":"bullet"},{"spans":[{"t":"text","v":"To set a preset, right-click the window's title bar and pick a width under the "},{"t":"strong","v":"Column width"},{"t":"text","v":" heading."}],"t":"p"},{"spans":[{"t":"text","v":"Fine-tune by dragging"}],"t":"sub"},{"spans":[{"t":"text","v":"For a width between the presets, drag a column's "},{"t":"strong","v":"side gutter"},{"t":"text","v":" — the seam between two columns — left or right. This resizes the column to a custom width."}],"t":"p"}],"search":"column width presets a tiled column can be sized to one of four widths from the window context menu (right-click the title bar) or the width controls. the current width is marked with a check. menu label · width ⅓ — one third of the strip ½ — half of the strip ⅔ — two thirds of the strip max — maximum width to set a preset, right-click the window's title bar and pick a width under the column width heading. fine-tune by dragging for a width between the presets, drag a column's side gutter — the seam between two columns — left or right. this resizes the column to a custom width.","title":"Column width presets"},{"body":[{"spans":[{"t":"text","v":"You can shuffle columns and jump focus between them without touching the mouse. These are the defaults — all keys are rebindable in Control Panel → Input → Keybindings."}],"t":"p"},{"spans":[{"t":"text","v":"Action · Default binding"}],"t":"sub"},{"spans":[{"t":"text","v":"Focus column left — "},{"t":"kbd","v":"Ctrl+Alt+ArrowLeft"}],"t":"bullet"},{"spans":[{"t":"text","v":"Focus column right — "},{"t":"kbd","v":"Ctrl+Alt+ArrowRight"}],"t":"bullet"},{"spans":[{"t":"text","v":"Move column left — "},{"t":"kbd","v":"Ctrl+Alt+Shift+ArrowLeft"}],"t":"bullet"},{"spans":[{"t":"text","v":"Move column right — "},{"t":"kbd","v":"Ctrl+Alt+Shift+ArrowRight"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Focus"},{"t":"text","v":" jumps your selection to the window in the neighboring column without moving or rearranging anything."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Move"},{"t":"text","v":" picks up the focused column and swaps it with its neighbor, so you can reorder the strip."}],"t":"bullet"}],"search":"move & focus columns you can shuffle columns and jump focus between them without touching the mouse. these are the defaults — all keys are rebindable in control panel → input → keybindings. action · default binding focus column left — ctrl+alt+arrowleft focus column right — ctrl+alt+arrowright move column left — ctrl+alt+shift+arrowleft move column right — ctrl+alt+shift+arrowright focus jumps your selection to the window in the neighboring column without moving or rearranging anything. move picks up the focused column and swaps it with its neighbor, so you can reorder the strip. keyboard shortcuts","title":"Move & focus columns"},{"body":[{"spans":[{"t":"text","v":"When two or more windows share a column (stacked as rows), the window context menu can pull one out:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · What it does"}],"t":"sub"},{"spans":[{"t":"strong","v":"Move to own column"},{"t":"text","v":" — Ejects a window that shares a column into its own column. Available only when the window is sharing."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Move to new column"},{"t":"text","v":" — Spawns a fresh column immediately to its right and drops the window there — the click equivalent of dragging a window to a column edge."}],"t":"bullet"},{"spans":[{"t":"code","v":"Move to new column"},{"t":"text","v":" is the menu shortcut for the same result you get by dragging a window onto a column's far left or right edge (the "},{"t":"strong","v":"new column"},{"t":"text","v":" drop zone). See the drop-zone cheat sheet in Arranging Windows for the drag gestures."}],"t":"p"}],"search":"eject a window into its own column when two or more windows share a column (stacked as rows), the window context menu can pull one out: menu item · what it does move to own column — ejects a window that shares a column into its own column. available only when the window is sharing. move to new column — spawns a fresh column immediately to its right and drops the window there — the click equivalent of dragging a window to a column edge. move to new column is the menu shortcut for the same result you get by dragging a window onto a column's far left or right edge (the new column drop zone). see the drop-zone cheat sheet in arranging windows for the drag gestures. arranging windows","title":"Eject a window into its own column"}],"label":"Columns & Widths","order":4,"slug":"columns-and-widths"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland has two \"hold-to-snap\" gestures that move a window between the floating and tiling layouts just by dragging and pausing: you can "},{"t":"strong","v":"snap"},{"t":"text","v":" a floating window into the tiling grid, or "},{"t":"strong","v":"pop"},{"t":"text","v":" a tiled window back out to a float. Each gesture also has a one-click shortcut on the title-bar right-click menu."}],"t":"p"},{"spans":[{"t":"text","v":"Both gestures share a single timer — the "},{"t":"strong","v":"drag hold delay"},{"t":"text","v":" (the dwell). You start a drag, hold the window still long enough, and the window arms the gesture."}],"t":"p"}],"search":"overview browserland has two \"hold-to-snap\" gestures that move a window between the floating and tiling layouts just by dragging and pausing: you can snap a floating window into the tiling grid, or pop a tiled window back out to a float. each gesture also has a one-click shortcut on the title-bar right-click menu. both gestures share a single timer — the drag hold delay (the dwell). you start a drag, hold the window still long enough, and the window arms the gesture. window modes context menus","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Use this to drop a free-floating window into the tiling strip."}],"t":"p"},{"spans":[{"t":"text","v":"Start dragging the window's title bar."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Hold it still"},{"t":"text","v":" for the dwell (default ~3s) to enter snap mode."}],"t":"bullet"},{"spans":[{"t":"text","v":"The drop zones light up — the highlighted overlay previews where the window will land."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Release"},{"t":"text","v":" to tile the window where the overlay shows."}],"t":"bullet"},{"spans":[{"t":"text","v":"To cancel without tiling, drop the window on the top-left "},{"t":"strong","v":"\"return to floating\""},{"t":"text","v":" zone, or press "},{"t":"kbd","v":"Escape"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"For the full map of edges, bands, and quadrants you can drop onto, see the drop-zone cheat sheet."}],"t":"p"},{"spans":[{"t":"text","v":"Skip the drag"}],"t":"sub"},{"spans":[{"t":"text","v":"If you just want the window tiled and don't need to choose a spot, "},{"t":"strong","v":"right-click the title bar"},{"t":"text","v":" and choose "},{"t":"strong","v":"\"Tile this window\""},{"t":"text","v":" to drop it straight into the strip."}],"t":"p"}],"search":"snap a floating window into the grid use this to drop a free-floating window into the tiling strip. start dragging the window's title bar. hold it still for the dwell (default ~3s) to enter snap mode. the drop zones light up — the highlighted overlay previews where the window will land. release to tile the window where the overlay shows. to cancel without tiling, drop the window on the top-left \"return to floating\" zone, or press escape . for the full map of edges, bands, and quadrants you can drop onto, see the drop-zone cheat sheet. skip the drag if you just want the window tiled and don't need to choose a spot, right-click the title bar and choose \"tile this window\" to drop it straight into the strip. arranging windows","title":"Snap a floating window into the grid"},{"body":[{"spans":[{"t":"text","v":"Use this to detach a window from the tiling strip back into a free-floating window."}],"t":"p"},{"spans":[{"t":"text","v":"Start dragging the tiled window."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Hold it still"},{"t":"text","v":" for the dwell to arm "},{"t":"strong","v":"\"release to float\""},{"t":"text","v":" mode — a dashed outline appears and the bottom band lights up."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Release"},{"t":"text","v":" to detach the window as a floating window."}],"t":"bullet"},{"spans":[{"t":"text","v":"To cancel, "},{"t":"strong","v":"move the window again"},{"t":"text","v":" before releasing — that keeps it tiling. Pressing "},{"t":"kbd","v":"Escape"},{"t":"text","v":" aborts the drag entirely."}],"t":"p"},{"spans":[{"t":"text","v":"Skip the gesture"}],"t":"sub"},{"spans":[{"t":"text","v":"To detach a window directly without the hold, "},{"t":"strong","v":"right-click the title bar"},{"t":"text","v":" and choose "},{"t":"strong","v":"\"Float this window\""},{"t":"text","v":"."}],"t":"p"}],"search":"pop a tiled window out to a float use this to detach a window from the tiling strip back into a free-floating window. start dragging the tiled window. hold it still for the dwell to arm \"release to float\" mode — a dashed outline appears and the bottom band lights up. release to detach the window as a floating window. to cancel, move the window again before releasing — that keeps it tiling. pressing escape aborts the drag entirely. skip the gesture to detach a window directly without the hold, right-click the title bar and choose \"float this window\" .","title":"Pop a tiled window out to a float"},{"body":[{"spans":[{"t":"text","v":"The hold time for "},{"t":"strong","v":"both"},{"t":"text","v":" gestures is set per host in "},{"t":"strong","v":"Control Panel → Windows → Drag hold delay"},{"t":"text","v":", measured in milliseconds."}],"t":"p"},{"spans":[{"t":"text","v":"Setting · Behavior"}],"t":"sub"},{"spans":[{"t":"code","v":"0"},{"t":"text","v":" — "},{"t":"strong","v":"Disables both"},{"t":"text","v":" the snap and the pop-out gestures entirely"}],"t":"bullet"},{"spans":[{"t":"code","v":"250"},{"t":"text","v":"–"},{"t":"code","v":"20000"},{"t":"text","v":" — Hold time before a gesture arms (default "},{"t":"code","v":"3000"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"text","v":"Because this is a per-host setting, each broker you connect to keeps its own delay."}],"t":"p"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"text","v":"Set the delay to "},{"t":"code","v":"0"},{"t":"text","v":" if you find windows snapping or floating by accident, and use the "},{"t":"strong","v":"\"Tile this window\""},{"t":"text","v":" / "},{"t":"strong","v":"\"Float this window\""},{"t":"text","v":" title-bar menu items instead."}],"t":"p"}],"search":"hold delay (configurable) the hold time for both gestures is set per host in control panel → windows → drag hold delay , measured in milliseconds. setting · behavior 0 — disables both the snap and the pop-out gestures entirely 250 – 20000 — hold time before a gesture arms (default 3000 ) because this is a per-host setting, each broker you connect to keeps its own delay. tip: set the delay to 0 if you find windows snapping or floating by accident, and use the \"tile this window\" / \"float this window\" title-bar menu items instead.","title":"Hold delay (configurable)"}],"label":"Snapping & Pop-out","order":5,"slug":"snapping-and-pop-out"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Floating windows are free-moving, overlapping windows you can place anywhere on the desktop. This page covers how to move, resize, minimize, close, pin, recolor, and arrange them. For the snap-to-tile and pop-out gestures, see Snapping-and-Pop-out."}],"t":"p"}],"search":"overview floating windows are free-moving, overlapping windows you can place anywhere on the desktop. this page covers how to move, resize, minimize, close, pin, recolor, and arrange them. for the snap-to-tile and pop-out gestures, see snapping-and-pop-out. snapping and pop out","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Most of the controls on this page live on a window's "},{"t":"strong","v":"title-bar right-click menu"},{"t":"text","v":" (or its color button / a keyboard shortcut), so they work the same whether the window is floating or "},{"t":"strong","v":"snapped into the tiling strip"},{"t":"text","v":" — see Snapping-and-Pop-out. The exceptions are the ones that only make sense for a free-floating window: dragging to move or resize, pinning, and the whole-desktop arrange and size-lock actions."}],"t":"p"},{"spans":[{"t":"text","v":"Control · Works on a snapped (tiled) window?"}],"t":"sub"},{"spans":[{"t":"strong","v":"Minimize"},{"t":"text","v":" / "},{"t":"strong","v":"Restore"},{"t":"text","v":" — Yes"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" / "},{"t":"strong","v":"Terminate"},{"t":"text","v":" / "},{"t":"strong","v":"Delete"},{"t":"text","v":" — Yes"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Recolor"},{"t":"text","v":" (color button) — Yes"}],"t":"bullet"},{"spans":[{"t":"text","v":"Minimize / close shortcuts ("},{"t":"kbd","v":"Ctrl+Alt+m"},{"t":"text","v":" / "},{"t":"kbd","v":"Ctrl+Alt+w"},{"t":"text","v":") — Yes — they act on the focused window in either mode"}],"t":"bullet"},{"spans":[{"t":"text","v":"Move / resize by dragging — Floating only — a snapped window is sized by its column width and arranged in the strip (see Columns-and-Widths)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Lock to screen (pin)"},{"t":"text","v":" — Floating only — a snapped window is part of the strip and always scrolls with it; the pin exempts a floating window from following the strip's scroll"}],"t":"bullet"},{"spans":[{"t":"text","v":"Cascade / Tile / Minimize All / Lock Size — Floating only — these arrange the floating layer"}],"t":"bullet"}],"search":"floating vs. snapped: what carries over most of the controls on this page live on a window's title-bar right-click menu (or its color button / a keyboard shortcut), so they work the same whether the window is floating or snapped into the tiling strip — see snapping-and-pop-out. the exceptions are the ones that only make sense for a free-floating window: dragging to move or resize, pinning, and the whole-desktop arrange and size-lock actions. control · works on a snapped (tiled) window? minimize / restore — yes close / terminate / delete — yes recolor (color button) — yes minimize / close shortcuts ( ctrl+alt+m / ctrl+alt+w ) — yes — they act on the focused window in either mode move / resize by dragging — floating only — a snapped window is sized by its column width and arranged in the strip (see columns-and-widths) lock to screen (pin) — floating only — a snapped window is part of the strip and always scrolls with it; the pin exempts a floating window from following the strip's scroll cascade / tile / minimize all / lock size — floating only — these arrange the floating layer snapping and pop out columns and widths","title":"Floating vs. snapped: what carries over"},{"body":[{"spans":[{"t":"strong","v":"Floating only."},{"t":"text","v":" Dragging applies to free-floating windows. A window that's "},{"t":"strong","v":"snapped into the tiling strip"},{"t":"text","v":" is moved and sized through the strip instead — set its width with the column presets (Columns-and-Widths) and rearrange it with the strip actions (Arranging-Windows)."}],"t":"p"},{"spans":[{"t":"strong","v":"Move"},{"t":"text","v":" — Drag a floating window's title bar."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Resize"},{"t":"text","v":" — Drag its edges or corners."}],"t":"bullet"},{"spans":[{"t":"text","v":"Most of the remaining controls live on the "},{"t":"strong","v":"title-bar right-click menu"},{"t":"text","v":" — right-click a window's title bar to open it. See Context-Menus for the full right-click reference across the desktop."}],"t":"p"}],"search":"move and resize floating only. dragging applies to free-floating windows. a window that's snapped into the tiling strip is moved and sized through the strip instead — set its width with the column presets (columns-and-widths) and rearrange it with the strip actions (arranging-windows). move — drag a floating window's title bar. resize — drag its edges or corners. most of the remaining controls live on the title-bar right-click menu — right-click a window's title bar to open it. see context-menus for the full right-click reference across the desktop. columns and widths arranging windows context menus","title":"Move and resize"},{"body":[{"spans":[{"t":"text","v":"The title bar itself carries two buttons — "},{"t":"strong","v":"minimize"},{"t":"text","v":" and "},{"t":"strong","v":"×"},{"t":"text","v":" — and × is worth understanding: by default it "},{"t":"strong","v":"soft-closes"},{"t":"text","v":" (the view detaches, a terminal's shell keeps running). Turn on "},{"t":"strong","v":"Control Panel → Windows → \"Close (×) terminates terminal sessions\""},{"t":"text","v":" and × instead hard-kills the session, optionally behind "},{"t":"strong","v":"\"Confirm before terminating\""},{"t":"text","v":". The right-click "},{"t":"strong","v":"Close"},{"t":"text","v":" stays the soft-close path either way."}],"t":"p"},{"spans":[{"t":"text","v":"The rest of these actions are on "},{"t":"strong","v":"every"},{"t":"text","v":" window's title-bar right-click menu, so they work the same whether the window is floating or "},{"t":"strong","v":"snapped into the tiling strip"},{"t":"text","v":" — see Snapping-and-Pop-out."}],"t":"p"},{"spans":[{"t":"text","v":"A window's title-bar menu lets you get a window out of the way or close it. It's important to know that "},{"t":"strong","v":"closing is soft"},{"t":"text","v":" — what actually happens depends on the window type."}],"t":"p"},{"spans":[{"t":"text","v":"Action · What it does"}],"t":"sub"},{"spans":[{"t":"strong","v":"Minimize"},{"t":"text","v":" — Hides the window to the taskbar."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Restore"},{"t":"text","v":" — Brings a minimized window back (same menu item, when the window is minimized)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" — Soft close. A "},{"t":"strong","v":"terminal"},{"t":"text","v":"'s shell keeps running so you can reattach later. A "},{"t":"strong","v":"non-empty sticky note"},{"t":"text","v":" is retained and reopens from Closed notes in the "},{"t":"strong","v":"+"},{"t":"text","v":" menu (an empty note is discarded). A "},{"t":"strong","v":"text editor"},{"t":"text","v":"'s content is a file on the host, so closing it (after a save prompt for unsaved changes) leaves your file intact. A "},{"t":"strong","v":"file manager"},{"t":"text","v":", the "},{"t":"strong","v":"task manager"},{"t":"text","v":", and the Control Panel/help are ephemeral — Close just dismisses them."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminate"},{"t":"text","v":" — Terminals only. Hard-kills the shell process tree. You're asked to confirm first."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Delete note"},{"t":"text","v":" / "},{"t":"strong","v":"Delete file"},{"t":"text","v":" — Intended for windows that own a saved document (notes and the text editor). The guard is a denylist of four window kinds, so some ephemeral windows show the item too — tracked as issue #186. Permanently discards that window and its stored document. You're asked to confirm first."}],"t":"bullet"},{"spans":[{"t":"text","v":"Because a closed terminal keeps its shell alive, "},{"t":"strong","v":"Terminate"},{"t":"text","v":" is the only way to actually kill the process tree. For a sticky note or an editor file, "},{"t":"strong","v":"Close"},{"t":"text","v":" preserves the content (a note in Closed notes, a file on disk), while "},{"t":"strong","v":"Delete"},{"t":"text","v":" is the one path that throws it away."}],"t":"p"},{"spans":[{"t":"text","v":"You can also minimize or close the focused window from the keyboard:"}],"t":"p"},{"spans":[{"t":"text","v":"Action · Default binding"}],"t":"sub"},{"spans":[{"t":"text","v":"Minimize focused window — "},{"t":"kbd","v":"Ctrl+Alt+m"}],"t":"bullet"},{"spans":[{"t":"text","v":"Close focused window — "},{"t":"kbd","v":"Ctrl+Alt+w"}],"t":"bullet"},{"spans":[{"t":"text","v":"These act on the focused window in either mode (floating or snapped), and they're rebindable — see Keyboard-Shortcuts."}],"t":"p"}],"search":"minimize, close, terminate, and delete the title bar itself carries two buttons — minimize and × — and × is worth understanding: by default it soft-closes (the view detaches, a terminal's shell keeps running). turn on control panel → windows → \"close (×) terminates terminal sessions\" and × instead hard-kills the session, optionally behind \"confirm before terminating\" . the right-click close stays the soft-close path either way. the rest of these actions are on every window's title-bar right-click menu, so they work the same whether the window is floating or snapped into the tiling strip — see snapping-and-pop-out. a window's title-bar menu lets you get a window out of the way or close it. it's important to know that closing is soft — what actually happens depends on the window type. action · what it does minimize — hides the window to the taskbar. restore — brings a minimized window back (same menu item, when the window is minimized). close — soft close. a terminal 's shell keeps running so you can reattach later. a non-empty sticky note is retained and reopens from closed notes in the + menu (an empty note is discarded). a text editor 's content is a file on the host, so closing it (after a save prompt for unsaved changes) leaves your file intact. a file manager , the task manager , and the control panel/help are ephemeral — close just dismisses them. terminate — terminals only. hard-kills the shell process tree. you're asked to confirm first. delete note / delete file — intended for windows that own a saved document (notes and the text editor). the guard is a denylist of four window kinds, so some ephemeral windows show the item too — tracked as issue #186. permanently discards that window and its stored document. you're asked to confirm first. because a closed terminal keeps its shell alive, terminate is the only way to actually kill the process tree. for a sticky note or an editor file, close preserves the content (a note in closed notes, a file on disk), while delete is the one path that throws it away. you can also minimize or close the focused window from the keyboard: action · default binding minimize focused window — ctrl+alt+m close focused window — ctrl+alt+w these act on the focused window in either mode (floating or snapped), and they're rebindable — see keyboard-shortcuts. snapping and pop out keyboard shortcuts","title":"Minimize, close, terminate, and delete"},{"body":[{"spans":[{"t":"strong","v":"Floating only."},{"t":"text","v":" Pinning applies to floating windows. A window that's snapped into the tiling strip is part of the strip and always scrolls with it; the pin exists to exempt a floating window from following the strip's scroll — so the "},{"t":"strong","v":"Lock to screen"},{"t":"text","v":" item only appears on a floating window's menu."}],"t":"p"},{"spans":[{"t":"text","v":"By default a floating window scrolls away with the tiling strip. To keep it put, use the title-bar right-click menu:"}],"t":"p"},{"spans":[{"t":"strong","v":"Lock to screen (pin)"},{"t":"text","v":" — The window stays in place and does "},{"t":"strong","v":"not"},{"t":"text","v":" scroll away with the strip. This pins its position, not always-on-top."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Unlock (scroll with strip)"},{"t":"text","v":" — Reverses it, so the window scrolls with the strip again."}],"t":"bullet"}],"search":"pin a window (lock to screen) floating only. pinning applies to floating windows. a window that's snapped into the tiling strip is part of the strip and always scrolls with it; the pin exists to exempt a floating window from following the strip's scroll — so the lock to screen item only appears on a floating window's menu. by default a floating window scrolls away with the tiling strip. to keep it put, use the title-bar right-click menu: lock to screen (pin) — the window stays in place and does not scroll away with the strip. this pins its position, not always-on-top. unlock (scroll with strip) — reverses it, so the window scrolls with the strip again.","title":"Pin a window (lock to screen)"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"color button"},{"t":"text","v":" is on every window's title bar, so "},{"t":"strong","v":"recolor works the same whether the window is floating or snapped"},{"t":"text","v":" into the tiling strip."}],"t":"p"},{"spans":[{"t":"strong","v":"Recolor"},{"t":"text","v":" — Use the "},{"t":"strong","v":"color button"},{"t":"text","v":" in the window's title bar. You can pick from the palette, choose a custom color, or reuse a recent one."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Rename"},{"t":"text","v":" — Double-click an "},{"t":"strong","v":"app window's"},{"t":"text","v":" title to rename it."}],"t":"bullet"}],"search":"color and rename the color button is on every window's title bar, so recolor works the same whether the window is floating or snapped into the tiling strip. recolor — use the color button in the window's title bar. you can pick from the palette, choose a custom color, or reuse a recent one. rename — double-click an app window's title to rename it.","title":"Color and rename"},{"body":[{"spans":[{"t":"text","v":"Right-click the "},{"t":"strong","v":"empty desktop"},{"t":"text","v":" in floating mode for one-shot arrangements that act on all floating windows:"}],"t":"p"},{"spans":[{"t":"text","v":"Item · Effect"}],"t":"sub"},{"spans":[{"t":"strong","v":"Cascade"},{"t":"text","v":" — Stacks the windows in an overlapping cascade."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile Horizontally"},{"t":"text","v":" — Gives each window a full-width row, stacked top to bottom."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile Vertically"},{"t":"text","v":" — Gives each window a full-height column, placed side by side."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile H + V"},{"t":"text","v":" — Tiles the windows in a grid."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Minimize All Windows"},{"t":"text","v":" — Minimizes every floating window to the taskbar."}],"t":"bullet"},{"spans":[{"t":"text","v":"After you run one, an "},{"t":"strong","v":"\"Undo <action>\""},{"t":"text","v":" item appears so you can reverse the most recent arrange. Undo is "},{"t":"strong","v":"single-level"},{"t":"text","v":" — only the last arrangement can be undone."}],"t":"p"}],"search":"arrange all floating windows right-click the empty desktop in floating mode for one-shot arrangements that act on all floating windows: item · effect cascade — stacks the windows in an overlapping cascade. tile horizontally — gives each window a full-width row, stacked top to bottom. tile vertically — gives each window a full-height column, placed side by side. tile h + v — tiles the windows in a grid. minimize all windows — minimizes every floating window to the taskbar. after you run one, an \"undo <action>\" item appears so you can reverse the most recent arrange. undo is single-level — only the last arrangement can be undone.","title":"Arrange all floating windows"},{"body":[{"spans":[{"t":"text","v":"The empty-desktop right-click menu also has a global "},{"t":"strong","v":"Lock Size"},{"t":"text","v":" toggle:"}],"t":"p"},{"spans":[{"t":"strong","v":"Lock Size"},{"t":"text","v":" — Snaps every floating window to the default size and hides the resize handles, so windows can't be resized."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Unlock Size"},{"t":"text","v":" — Restores free resizing."}],"t":"bullet"}],"search":"lock size (global) the empty-desktop right-click menu also has a global lock size toggle: lock size — snaps every floating window to the default size and hides the resize handles, so windows can't be resized. unlock size — restores free resizing.","title":"Lock Size (global)"}],"label":"Floating Window Controls","order":6,"slug":"floating-window-controls"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Workspaces are virtual desktops. Each workspace is its own desktop of windows, so you can keep one set of terminals, notes, and editors on workspace 1 and a completely different set on workspace 2. Only one workspace is shown at a time."}],"t":"p"},{"spans":[{"t":"text","v":"Workspaces are a "},{"t":"strong","v":"mod"},{"t":"text","v":", enabled by default — see Hosts-and-Multi-Browser for how mods are turned on and off. Everything on this page works out of the box; Turning workspaces off at the bottom covers what happens if you disable it."}],"t":"p"}],"search":"overview workspaces are virtual desktops. each workspace is its own desktop of windows, so you can keep one set of terminals, notes, and editors on workspace 1 and a completely different set on workspace 2. only one workspace is shown at a time. workspaces are a mod , enabled by default — see hosts-and-multi-browser for how mods are turned on and off. everything on this page works out of the box; turning workspaces off at the bottom covers what happens if you disable it. hosts and multi browser","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"You can move between workspaces a few ways:"}],"t":"p"},{"spans":[{"t":"strong","v":"Pager dots"},{"t":"text","v":" — Click a dot in the pager at the bottom of the taskbar. Each dot is one workspace; the active one is highlighted."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The workspace menu"},{"t":"text","v":" — Right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar, and pick a workspace from the list. It works in both window modes, and because the taskbar is always on screen it stays reachable even when windows cover the desktop — so it still works with the pager hidden."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Previous / Next shortcuts"},{"t":"text","v":" — Step to the workspace before or after the current one."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Go to workspace 1–5"},{"t":"text","v":" — Jump straight to a numbered workspace."}],"t":"bullet"},{"spans":[{"t":"text","v":"Action · Default binding"}],"t":"sub"},{"spans":[{"t":"text","v":"Previous workspace — "},{"t":"kbd","v":"Ctrl+Alt+ArrowUp"}],"t":"bullet"},{"spans":[{"t":"text","v":"Next workspace — "},{"t":"kbd","v":"Ctrl+Alt+ArrowDown"}],"t":"bullet"},{"spans":[{"t":"text","v":"Go to workspace 1 — "},{"t":"kbd","v":"Ctrl+Alt+1"}],"t":"bullet"},{"spans":[{"t":"text","v":"Go to workspace 2 — "},{"t":"kbd","v":"Ctrl+Alt+2"}],"t":"bullet"},{"spans":[{"t":"text","v":"Go to workspace 3 — "},{"t":"kbd","v":"Ctrl+Alt+3"}],"t":"bullet"},{"spans":[{"t":"text","v":"Go to workspace 4 — "},{"t":"kbd","v":"Ctrl+Alt+4"}],"t":"bullet"},{"spans":[{"t":"text","v":"Go to workspace 5 — "},{"t":"kbd","v":"Ctrl+Alt+5"}],"t":"bullet"},{"spans":[{"t":"text","v":"All of these are rebindable — see Keyboard-Shortcuts."}],"t":"p"},{"spans":[{"t":"text","v":"Each row of the workspace menu shows that workspace's name and a count of what it holds — its columns in tiling mode, its floating windows in floating mode — with a "},{"t":"code","v":"✓"},{"t":"text","v":" on the one you are on. Below the list the menu adds, renames, and removes workspaces too; see the sections below. For more on the tiling strip, see Window-Modes."}],"t":"p"}],"search":"switching workspaces you can move between workspaces a few ways: pager dots — click a dot in the pager at the bottom of the taskbar. each dot is one workspace; the active one is highlighted. the workspace menu — right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar, and pick a workspace from the list. it works in both window modes, and because the taskbar is always on screen it stays reachable even when windows cover the desktop — so it still works with the pager hidden. previous / next shortcuts — step to the workspace before or after the current one. go to workspace 1–5 — jump straight to a numbered workspace. action · default binding previous workspace — ctrl+alt+arrowup next workspace — ctrl+alt+arrowdown go to workspace 1 — ctrl+alt+1 go to workspace 2 — ctrl+alt+2 go to workspace 3 — ctrl+alt+3 go to workspace 4 — ctrl+alt+4 go to workspace 5 — ctrl+alt+5 all of these are rebindable — see keyboard-shortcuts. each row of the workspace menu shows that workspace's name and a count of what it holds — its columns in tiling mode, its floating windows in floating mode — with a ✓ on the one you are on. below the list the menu adds, renames, and removes workspaces too; see the sections below. for more on the tiling strip, see window-modes. keyboard shortcuts window modes","title":"Switching workspaces"},{"body":[{"spans":[{"t":"text","v":"There are a few ways to append a fresh, empty workspace:"}],"t":"p"},{"spans":[{"t":"text","v":"Click the "},{"t":"strong","v":"+"},{"t":"text","v":" dot at the end of the pager."}],"t":"bullet"},{"spans":[{"t":"text","v":"Choose "},{"t":"strong","v":"New workspace"},{"t":"text","v":" from the pager-dot right-click menu."}],"t":"bullet"},{"spans":[{"t":"text","v":"Choose "},{"t":"strong","v":"New workspace"},{"t":"text","v":" from the workspace menu (right-click the empty desktop, strip, or taskbar)."}],"t":"bullet"}],"search":"adding a workspace there are a few ways to append a fresh, empty workspace: click the + dot at the end of the pager. choose new workspace from the pager-dot right-click menu. choose new workspace from the workspace menu (right-click the empty desktop, strip, or taskbar).","title":"Adding a workspace"},{"body":[{"spans":[{"t":"text","v":"Right-click a window's "},{"t":"strong","v":"title bar"},{"t":"text","v":" to move it elsewhere (the Taskbar-item menu does not have these — it is only Focus / Minimize / Close / Terminate):"}],"t":"p"},{"spans":[{"t":"strong","v":"Send to workspace N"},{"t":"text","v":" (or its name) — moves the window to that existing workspace."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Send to new workspace"},{"t":"text","v":" — appends a new workspace and moves the window there."}],"t":"bullet"},{"spans":[{"t":"strong","v":"On all workspaces"},{"t":"text","v":" — keeps a "},{"t":"strong","v":"floating"},{"t":"text","v":" window visible on every workspace. (The menu shows it as "},{"t":"code","v":"Show on all workspaces"},{"t":"text","v":", with a "},{"t":"code","v":"✓ On all workspaces"},{"t":"text","v":" once it is enabled.)"}],"t":"bullet"},{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Send to"},{"t":"text","v":" items appear only on "},{"t":"strong","v":"tiled"},{"t":"text","v":" windows' title-bar menus. A floating window's menu offers "},{"t":"strong","v":"Tile this window"},{"t":"text","v":" instead — tile it first, then send it — or use "},{"t":"strong","v":"Show on all workspaces"},{"t":"text","v":" to make it visible everywhere."}],"t":"p"}],"search":"sending a window to another workspace right-click a window's title bar to move it elsewhere (the taskbar-item menu does not have these — it is only focus / minimize / close / terminate): send to workspace n (or its name) — moves the window to that existing workspace. send to new workspace — appends a new workspace and moves the window there. on all workspaces — keeps a floating window visible on every workspace. (the menu shows it as show on all workspaces , with a ✓ on all workspaces once it is enabled.) the send to items appear only on tiled windows' title-bar menus. a floating window's menu offers tile this window instead — tile it first, then send it — or use show on all workspaces to make it visible everywhere. taskbar","title":"Sending a window to another workspace"},{"body":[{"spans":[{"t":"text","v":"A window you open lands on the workspace you are "},{"t":"strong","v":"currently on"},{"t":"text","v":", and it stays there — it never appears for a moment and then disappears."}],"t":"p"},{"spans":[{"t":"text","v":"Launching something that is "},{"t":"strong","v":"already open"},{"t":"text","v":" does not open a second copy. What happens next depends on how that window is arranged:"}],"t":"p"},{"spans":[{"t":"text","v":"The open window is… · Launching it again…"}],"t":"sub"},{"spans":[{"t":"strong","v":"Floating"},{"t":"text","v":", on another workspace — Moves it to the workspace you are on, and focuses it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Floating"},{"t":"text","v":", set to On all workspaces — Focuses it where it is — it is already visible everywhere, so it is never moved."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tiled"},{"t":"text","v":", on another workspace — Takes "},{"t":"strong","v":"you"},{"t":"text","v":" to its workspace and focuses it there."}],"t":"bullet"},{"spans":[{"t":"text","v":"Minimized — Restores it, applying the same rule."}],"t":"bullet"},{"spans":[{"t":"text","v":"The split is deliberate. A floating window can be picked up and moved anywhere, so it comes to you. A tiled window's place is its column in that workspace's strip, so moving it would rearrange the layout you built — instead you are taken to it. To move a tiled window for real, use "},{"t":"strong","v":"Send to workspace N"},{"t":"text","v":" from its title-bar menu."}],"t":"p"},{"spans":[{"t":"text","v":"This applies to every way of launching: the "},{"t":"strong","v":"+"},{"t":"text","v":" menu, the keyboard shortcut, and the app's own button or chip."}],"t":"p"},{"spans":[{"t":"text","v":"Clicking a window's "},{"t":"strong","v":"taskbar"},{"t":"text","v":" item behaves the same way, with one addition — it also un-minimizes, and clicking the item of the window you are already focused on minimizes it."}],"t":"p"},{"spans":[{"t":"strong","v":"Closing a window forgets its workspace."},{"t":"text","v":" Reopen it later and it opens on whichever workspace you are on, like anything else newly opened. Reloading the page is different: nothing is closed, so every window comes back on the workspace it was on."}],"t":"p"}],"search":"where a window opens, and what happens when you launch it again a window you open lands on the workspace you are currently on , and it stays there — it never appears for a moment and then disappears. launching something that is already open does not open a second copy. what happens next depends on how that window is arranged: the open window is… · launching it again… floating , on another workspace — moves it to the workspace you are on, and focuses it. floating , set to on all workspaces — focuses it where it is — it is already visible everywhere, so it is never moved. tiled , on another workspace — takes you to its workspace and focuses it there. minimized — restores it, applying the same rule. the split is deliberate. a floating window can be picked up and moved anywhere, so it comes to you. a tiled window's place is its column in that workspace's strip, so moving it would rearrange the layout you built — instead you are taken to it. to move a tiled window for real, use send to workspace n from its title-bar menu. this applies to every way of launching: the + menu, the keyboard shortcut, and the app's own button or chip. clicking a window's taskbar item behaves the same way, with one addition — it also un-minimizes, and clicking the item of the window you are already focused on minimizes it. closing a window forgets its workspace. reopen it later and it opens on whichever workspace you are on, like anything else newly opened. reloading the page is different: nothing is closed, so every window comes back on the workspace it was on.","title":"Where a window opens, and what happens when you launch it again"},{"body":[{"spans":[{"t":"text","v":"Right-click a "},{"t":"strong","v":"pager dot"},{"t":"text","v":" for that workspace's options:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · What it does"}],"t":"sub"},{"spans":[{"t":"code","v":"Rename…"},{"t":"text","v":" — Give the workspace a custom name."}],"t":"bullet"},{"spans":[{"t":"code","v":"Remove workspace"},{"t":"text","v":" — Delete that workspace (disabled when only one workspace remains)."}],"t":"bullet"},{"spans":[{"t":"code","v":"Show names"},{"t":"text","v":" — Show each workspace's name on its pager dot."}],"t":"bullet"},{"spans":[{"t":"code","v":"Show numbers"},{"t":"text","v":" — Show workspace numbers on the dots instead."}],"t":"bullet"},{"spans":[{"t":"code","v":"New workspace"},{"t":"text","v":" — Append a fresh empty workspace."}],"t":"bullet"},{"spans":[{"t":"code","v":"Show names"},{"t":"text","v":" / "},{"t":"code","v":"Show numbers"},{"t":"text","v":" is a single toggle for how all dots are labeled; the active choice is marked with a "},{"t":"code","v":"✓"},{"t":"text","v":". It is also "},{"t":"strong","v":"Workspace labels"},{"t":"text","v":" under "},{"t":"strong","v":"Control Panel → Windows → Workspaces"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The workspace menu — right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar — renames and removes too, naming its target so there is no doubt which workspace it acts on:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · What it does"}],"t":"sub"},{"spans":[{"t":"code","v":"Rename <workspace>…"},{"t":"text","v":" — Rename the workspace you are on."}],"t":"bullet"},{"spans":[{"t":"code","v":"Remove <workspace>"},{"t":"text","v":" — Delete the workspace you are on (disabled when only one remains)."}],"t":"bullet"},{"spans":[{"t":"text","v":"These are the same two actions as the pager-dot menu, on a surface that does not depend on the pager — so they still work with the pager hidden."}],"t":"p"}],"search":"renaming, removing, and labeling workspaces right-click a pager dot for that workspace's options: menu item · what it does rename… — give the workspace a custom name. remove workspace — delete that workspace (disabled when only one workspace remains). show names — show each workspace's name on its pager dot. show numbers — show workspace numbers on the dots instead. new workspace — append a fresh empty workspace. show names / show numbers is a single toggle for how all dots are labeled; the active choice is marked with a ✓ . it is also workspace labels under control panel → windows → workspaces . the workspace menu — right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar — renames and removes too, naming its target so there is no doubt which workspace it acts on: menu item · what it does rename <workspace>… — rename the workspace you are on. remove <workspace> — delete the workspace you are on (disabled when only one remains). these are the same two actions as the pager-dot menu, on a surface that does not depend on the pager — so they still work with the pager hidden.","title":"Renaming, removing, and labeling workspaces"},{"body":[{"spans":[{"t":"text","v":"Hovering a pager dot pops up a small schematic of that workspace: its columns and rows drawn to scale and labelled with each window's title, a "},{"t":"code","v":"(+N)"},{"t":"text","v":" badge on a tabbed tile counting its hidden tabs, and a count of the workspace's floating windows underneath. It shows exactly what the strip would show — a minimized or disconnected window is left out of the preview the same way it is left out of the strip."}],"t":"p"}],"search":"hover preview hovering a pager dot pops up a small schematic of that workspace: its columns and rows drawn to scale and labelled with each window's title, a (+n) badge on a tabbed tile counting its hidden tabs, and a count of the workspace's floating windows underneath. it shows exactly what the strip would show — a minimized or disconnected window is left out of the preview the same way it is left out of the strip.","title":"Hover preview"},{"body":[{"spans":[{"t":"text","v":"Chips for windows on other workspaces are dimmed and carry a small badge naming their workspace, so the bar always indicates where you are. The mod's three settings all live together under "},{"t":"strong","v":"Control Panel → Windows → Workspaces"},{"t":"text","v":" — the mod places them beside the other window settings rather than in the shared "},{"t":"strong","v":"Mods"},{"t":"text","v":" applet, each carrying its mod badge:"}],"t":"p"},{"spans":[{"t":"text","v":"Setting · What it does"}],"t":"sub"},{"spans":[{"t":"strong","v":"Workspace labels"},{"t":"text","v":" — Numbers or names on the pager dots — the Control Panel twin of the pager dot menu's "},{"t":"code","v":"Show names"},{"t":"text","v":" / "},{"t":"code","v":"Show numbers"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Hide taskbar items from other workspaces"},{"t":"text","v":" — Leave out chips for windows on other workspaces entirely, instead of dimming them."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Hide the workspace pager from the taskbar"},{"t":"text","v":" — Remove the pager dots from the taskbar and give the space back. Off by default."}],"t":"bullet"},{"spans":[{"t":"text","v":"Clicking a chip for a window on another workspace switches there first, so a chip never does nothing — including a chip for a session that is currently closed."}],"t":"p"},{"spans":[{"t":"text","v":"Hiding the pager"}],"t":"sub"},{"spans":[{"t":"text","v":"If you switch workspaces from the keyboard, "},{"t":"strong","v":"Hide the workspace pager from the taskbar"},{"t":"text","v":" reclaims the space the dots take up. It hides the pager and nothing else — the badges and dimming, the chip-click workspace switch, "},{"t":"strong","v":"Send to workspace"},{"t":"text","v":", floating-window masking, and the seven shortcuts all keep working exactly as before."}],"t":"p"},{"spans":[{"t":"text","v":"Everything the dots could do is still reachable: the workspace menu (right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar) switches, adds, renames, and removes workspaces, and the shortcuts above switch. Turn the setting back off and the pager returns with its workspaces unchanged."}],"t":"p"},{"spans":[{"t":"text","v":"Like the other two, this setting follows the browser you set it from, and every browser viewing the same broker converges on it."}],"t":"p"}],"search":"taskbar behaviour chips for windows on other workspaces are dimmed and carry a small badge naming their workspace, so the bar always indicates where you are. the mod's three settings all live together under control panel → windows → workspaces — the mod places them beside the other window settings rather than in the shared mods applet, each carrying its mod badge: setting · what it does workspace labels — numbers or names on the pager dots — the control panel twin of the pager dot menu's show names / show numbers . hide taskbar items from other workspaces — leave out chips for windows on other workspaces entirely, instead of dimming them. hide the workspace pager from the taskbar — remove the pager dots from the taskbar and give the space back. off by default. clicking a chip for a window on another workspace switches there first, so a chip never does nothing — including a chip for a session that is currently closed. hiding the pager if you switch workspaces from the keyboard, hide the workspace pager from the taskbar reclaims the space the dots take up. it hides the pager and nothing else — the badges and dimming, the chip-click workspace switch, send to workspace , floating-window masking, and the seven shortcuts all keep working exactly as before. everything the dots could do is still reachable: the workspace menu (right-click the empty desktop, the empty tiling strip, or an empty part of the taskbar) switches, adds, renames, and removes workspaces, and the shortcuts above switch. turn the setting back off and the pager returns with its workspaces unchanged. like the other two, this setting follows the browser you set it from, and every browser viewing the same broker converges on it.","title":"Taskbar behaviour"},{"body":[{"spans":[{"t":"text","v":"The set of workspaces, their names, and which columns belong to each are part of the broker's shared layout, so every browser looking at that broker sees the same workspaces. Which workspace a "},{"t":"strong","v":"floating"},{"t":"text","v":" window belongs to is per browser, because a floating window's pixel geometry is per browser too."}],"t":"p"}],"search":"what is shared, and what is per browser the set of workspaces, their names, and which columns belong to each are part of the broker's shared layout, so every browser looking at that broker sees the same workspaces. which workspace a floating window belongs to is per browser, because a floating window's pixel geometry is per browser too.","title":"What is shared, and what is per browser"},{"body":[{"spans":[{"t":"text","v":"Disabling the mod is a different thing from hiding the pager, and much bigger. "},{"t":"strong","v":"Hide the workspace pager from the taskbar"},{"t":"text","v":" (above) removes the dots and leaves everything else running; disabling the mod removes the feature."}],"t":"p"},{"spans":[{"t":"text","v":"Nothing is destroyed and nothing is hidden either way. Every tiled column lives on the one desktop the tiling core owns — workspaces only decide which of them the strip draws. Disable the mod (Control Panel → Mods) and:"}],"t":"p"},{"spans":[{"t":"text","v":"every column from every workspace appears together on a single desktop;"}],"t":"bullet"},{"spans":[{"t":"text","v":"the pager disappears;"}],"t":"bullet"},{"spans":[{"t":"text","v":"the seven workspace shortcuts leave the Keyboard shortcuts list (your bindings are remembered, not deleted);"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Send to workspace"},{"t":"text","v":" and "},{"t":"strong","v":"On all workspaces"},{"t":"text","v":" leave the title-bar menus, and the whole workspace block leaves the empty-desktop / taskbar menu;"}],"t":"bullet"},{"spans":[{"t":"text","v":"the mod's three settings leave "},{"t":"strong","v":"Control Panel → Windows"},{"t":"text","v":";"}],"t":"bullet"},{"spans":[{"t":"text","v":"any floating window that was masked to another workspace becomes visible."}],"t":"bullet"},{"spans":[{"t":"text","v":"Re-enable it and your workspaces come back exactly as they were. A column created while the mod was off joins whichever workspace is active when you turn it back on."}],"t":"p"},{"spans":[{"t":"text","v":"One thing to know if you run "},{"t":"strong","v":"two browsers on the same broker with the mod on in one and off in the other"},{"t":"text","v":": the browser without it sees every column on one desktop, so rearranging them there rearranges your workspaces too. Stacking a window from workspace 1 into a column that belongs to workspace 2 really does move it to workspace 2 — the column is the membership. That is the same thing happening either way round; it just isn't labelled as a workspace move in the browser that can't see workspaces."}],"t":"p"}],"search":"turning workspaces off disabling the mod is a different thing from hiding the pager, and much bigger. hide the workspace pager from the taskbar (above) removes the dots and leaves everything else running; disabling the mod removes the feature. nothing is destroyed and nothing is hidden either way. every tiled column lives on the one desktop the tiling core owns — workspaces only decide which of them the strip draws. disable the mod (control panel → mods) and: every column from every workspace appears together on a single desktop; the pager disappears; the seven workspace shortcuts leave the keyboard shortcuts list (your bindings are remembered, not deleted); send to workspace and on all workspaces leave the title-bar menus, and the whole workspace block leaves the empty-desktop / taskbar menu; the mod's three settings leave control panel → windows ; any floating window that was masked to another workspace becomes visible. re-enable it and your workspaces come back exactly as they were. a column created while the mod was off joins whichever workspace is active when you turn it back on. one thing to know if you run two browsers on the same broker with the mod on in one and off in the other : the browser without it sees every column on one desktop, so rearranging them there rearranges your workspaces too. stacking a window from workspace 1 into a column that belongs to workspace 2 really does move it to workspace 2 — the column is the membership. that is the same thing happening either way round; it just isn't labelled as a workspace move in the browser that can't see workspaces.","title":"Turning workspaces off"}],"label":"Workspaces","order":7,"slug":"workspaces"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The taskbar runs along the bottom of the Browserland desktop. It holds the launch button, a button for each open window (sticky notes are the exception — they stay off the taskbar unless you opt them in), the workspace pager, host status, a fullscreen button, and clock, AI status, clipboard, update, and help chips."}],"t":"p"}],"search":"overview the taskbar runs along the bottom of the browserland desktop. it holds the launch button, a button for each open window (sticky notes are the exception — they stay off the taskbar unless you opt them in), the workspace pager, host status, a fullscreen button, and clock, ai status, clipboard, update, and help chips.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"From left to right, the taskbar contains:"}],"t":"p"},{"spans":[{"t":"text","v":"Element · What it does"}],"t":"sub"},{"spans":[{"t":"text","v":"Launch button ("},{"t":"strong","v":"+"},{"t":"text","v":") — By default, left-click launches a terminal on your default host — the local broker unless you pick another under Control Panel → Hosts (see Hosts-and-Multi-Browser) — and right-click opens the full profile / app menu — a Control Panel toggle can swap the two"}],"t":"bullet"},{"spans":[{"t":"text","v":"Window buttons — One per open window (sticky notes only when opted in — see below) — click to focus, right-click for per-window actions"}],"t":"bullet"},{"spans":[{"t":"text","v":"Pager dots — One dot per workspace — click to switch. From the Workspaces mod, enabled by default (see Workspaces)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Host status — One status chip for a single broker; with several, one aggregate badge that opens the broker menu. A Control Panel option decides whether it shows always, only when a broker needs attention, or never (see Hosts-and-Multi-Browser)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Fullscreen button ("},{"t":"code","v":"⛶"},{"t":"text","v":") — Toggles the browser into fullscreen"}],"t":"bullet"},{"spans":[{"t":"text","v":"Clock chip — Date & time readout, shown while the Clock mod is enabled"}],"t":"bullet"},{"spans":[{"t":"text","v":"AI status chip — Worst-case health of the major AI providers — click to open the status window. Shown while the "},{"t":"strong","v":"AI status"},{"t":"text","v":" mod is enabled; it ships "},{"t":"strong","v":"off by default"},{"t":"text","v":" because enabling it lets the broker fetch each provider's public status page, which makes the broker's egress IP visible to those hosts. Turn it on from Control Panel → Mods"}],"t":"bullet"},{"spans":[{"t":"text","v":"Clipboard chip — Opens (or focuses) the clipboard-history window. Shown while the "},{"t":"strong","v":"Clipboard"},{"t":"text","v":" mod is enabled; it ships "},{"t":"strong","v":"off by default"},{"t":"text","v":" because clipboards carry secrets, so it captures nothing until you opt in. Turn it on from Control Panel → Mods"}],"t":"bullet"},{"spans":[{"t":"text","v":"Update chip ("},{"t":"strong","v":"⬆"},{"t":"text","v":") — Whether every configured broker is current with upstream. One aggregate chip colors by the worst state (up to date, N behind, N ahead, or N unchecked). Click to open the Update check window, which shows one row per broker and explains why any broker could not be checked. Shown while the "},{"t":"strong","v":"Update check"},{"t":"text","v":" mod is enabled; it ships "},{"t":"strong","v":"off by default"},{"t":"text","v":", and enabling it asks the serving broker to start checking (only that click counts, and no restart is needed). Any broker in the list can be switched on from its row, remote ones behind a confirmation that names the machine. By default the chip hides itself while every configured broker is current"}],"t":"bullet"},{"spans":[{"t":"text","v":"Help chip ("},{"t":"strong","v":"?"},{"t":"text","v":") — Opens the in-app interface guide (see Getting-Started)"}],"t":"bullet"}],"search":"anatomy from left to right, the taskbar contains: element · what it does launch button ( + ) — by default, left-click launches a terminal on your default host — the local broker unless you pick another under control panel → hosts (see hosts-and-multi-browser) — and right-click opens the full profile / app menu — a control panel toggle can swap the two window buttons — one per open window (sticky notes only when opted in — see below) — click to focus, right-click for per-window actions pager dots — one dot per workspace — click to switch. from the workspaces mod, enabled by default (see workspaces) host status — one status chip for a single broker; with several, one aggregate badge that opens the broker menu. a control panel option decides whether it shows always, only when a broker needs attention, or never (see hosts-and-multi-browser) fullscreen button ( ⛶ ) — toggles the browser into fullscreen clock chip — date & time readout, shown while the clock mod is enabled ai status chip — worst-case health of the major ai providers — click to open the status window. shown while the ai status mod is enabled; it ships off by default because enabling it lets the broker fetch each provider's public status page, which makes the broker's egress ip visible to those hosts. turn it on from control panel → mods clipboard chip — opens (or focuses) the clipboard-history window. shown while the clipboard mod is enabled; it ships off by default because clipboards carry secrets, so it captures nothing until you opt in. turn it on from control panel → mods update chip ( ⬆ ) — whether every configured broker is current with upstream. one aggregate chip colors by the worst state (up to date, n behind, n ahead, or n unchecked). click to open the update check window, which shows one row per broker and explains why any broker could not be checked. shown while the update check mod is enabled; it ships off by default , and enabling it asks the serving broker to start checking (only that click counts, and no restart is needed). any broker in the list can be switched on from its row, remote ones behind a confirmation that names the machine. by default the chip hides itself while every configured broker is current help chip ( ? ) — opens the in-app interface guide (see getting-started) hosts and multi browser workspaces hosts and multi browser getting started","title":"Anatomy"},{"body":[{"spans":[{"t":"text","v":"Each open window has a button on the taskbar — except sticky notes, which by default stay off the taskbar and are reached from the "},{"t":"strong","v":"+"},{"t":"text","v":" menu instead. If you'd rather have them there, the Sticky mod's "},{"t":"strong","v":"show sticky-note windows on the taskbar"},{"t":"text","v":" setting (Control Panel → Mods) gives every open note a button too, applied live to notes already open."}],"t":"p"},{"spans":[{"t":"strong","v":"Click"},{"t":"text","v":" a button to focus and raise that window. If the window is minimized, clicking it restores it; if it lives on another workspace, Browserland switches to that workspace first. Clicking the button of the window that is "},{"t":"strong","v":"already focused and on top minimizes it"},{"t":"text","v":" — so the same button toggles a window in and out of view."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Right-click"},{"t":"text","v":" a button for per-window actions:"}],"t":"bullet"},{"spans":[{"t":"text","v":"Action · Effect"}],"t":"sub"},{"spans":[{"t":"text","v":"Focus — Switch to the window's workspace (if needed) and raise it, restoring it if minimized"}],"t":"bullet"},{"spans":[{"t":"text","v":"Minimize / Restore — Hide the window to the taskbar, or bring it back"}],"t":"bullet"},{"spans":[{"t":"text","v":"Close — Soft-close the window (a terminal's shell keeps running; a non-empty sticky note reopens from the "},{"t":"strong","v":"+"},{"t":"text","v":" menu; a text editor keeps its file on disk)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Terminate — Terminals only — hard-kill the shell process tree"}],"t":"bullet"},{"spans":[{"t":"text","v":"The taskbar menu does not have a send-to-workspace item — to move a window to another workspace, use its "},{"t":"strong","v":"title-bar"},{"t":"text","v":" right-click menu (see Workspaces). For the difference between Close, Terminate, and Delete, see Floating-Window-Controls."}],"t":"p"},{"spans":[{"t":"text","v":"Label components"}],"t":"sub"},{"spans":[{"t":"text","v":"Each window button — and the matching window title bar — shows a label built from up to four components:"}],"t":"p"},{"spans":[{"t":"text","v":"Component · Example · Notes"}],"t":"sub"},{"spans":[{"t":"strong","v":"window id"},{"t":"text","v":" — "},{"t":"code","v":"#42"},{"t":"text","v":" — The broker-assigned window number. On the taskbar it is dimmed and auto-hides on a very narrow button; in a window title bar it stays a leading badge"}],"t":"bullet"},{"spans":[{"t":"strong","v":"host"},{"t":"text","v":" — "},{"t":"code","v":"myhost:"},{"t":"text","v":" — The machine the session reports running on. The colon appears only when the host sits immediately before the title"}],"t":"bullet"},{"spans":[{"t":"strong","v":"title"},{"t":"text","v":" — "},{"t":"code","v":"claude"},{"t":"text","v":" — The program or agent title (or "},{"t":"code","v":"session <id>"},{"t":"text","v":" when it has none)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"pid"},{"t":"text","v":" — "},{"t":"code","v":"[12345]"},{"t":"text","v":" — The process id, in brackets"}],"t":"bullet"},{"spans":[{"t":"text","v":"By default the label reads "},{"t":"code","v":"#id host: title [pid]"},{"t":"text","v":" with the pid hidden — for example "},{"t":"code","v":"#42 myhost: claude"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"To change it, open "},{"t":"strong","v":"Control Panel → Windows → Taskbar / title labels"},{"t":"text","v":". Each component has a checkbox to show or hide it, and the rows can be reordered — drag a row, or use its ↑/↓ buttons — so the label follows the order you choose. At least one component always stays ticked (you cannot hide them all), and a ticked component that has no value for a given window (for example the host on an app window that has none) is simply left out — if that would leave the label empty, the title is shown instead, so the label is never blank. The hover tooltip always lists everything regardless of these settings. Like the other display toggles, this preference is stored per broker host and shared with your other browsers viewing that host."}],"t":"p"},{"spans":[{"t":"text","v":"Items for other workspaces"}],"t":"sub"},{"spans":[{"t":"text","v":"By default, buttons for windows on other workspaces still appear (dimmed) so you can jump to them. To show only the active workspace's windows in the taskbar, turn on "},{"t":"strong","v":"Hide taskbar items from other workspaces"},{"t":"text","v":" under Control Panel → Windows → Workspaces. This setting governs your browser."}],"t":"p"}],"search":"window buttons each open window has a button on the taskbar — except sticky notes, which by default stay off the taskbar and are reached from the + menu instead. if you'd rather have them there, the sticky mod's show sticky-note windows on the taskbar setting (control panel → mods) gives every open note a button too, applied live to notes already open. click a button to focus and raise that window. if the window is minimized, clicking it restores it; if it lives on another workspace, browserland switches to that workspace first. clicking the button of the window that is already focused and on top minimizes it — so the same button toggles a window in and out of view. right-click a button for per-window actions: action · effect focus — switch to the window's workspace (if needed) and raise it, restoring it if minimized minimize / restore — hide the window to the taskbar, or bring it back close — soft-close the window (a terminal's shell keeps running; a non-empty sticky note reopens from the + menu; a text editor keeps its file on disk) terminate — terminals only — hard-kill the shell process tree the taskbar menu does not have a send-to-workspace item — to move a window to another workspace, use its title-bar right-click menu (see workspaces). for the difference between close, terminate, and delete, see floating-window-controls. label components each window button — and the matching window title bar — shows a label built from up to four components: component · example · notes window id — #42 — the broker-assigned window number. on the taskbar it is dimmed and auto-hides on a very narrow button; in a window title bar it stays a leading badge host — myhost: — the machine the session reports running on. the colon appears only when the host sits immediately before the title title — claude — the program or agent title (or session <id> when it has none) pid — [12345] — the process id, in brackets by default the label reads #id host: title [pid] with the pid hidden — for example #42 myhost: claude . to change it, open control panel → windows → taskbar / title labels . each component has a checkbox to show or hide it, and the rows can be reordered — drag a row, or use its ↑/↓ buttons — so the label follows the order you choose. at least one component always stays ticked (you cannot hide them all), and a ticked component that has no value for a given window (for example the host on an app window that has none) is simply left out — if that would leave the label empty, the title is shown instead, so the label is never blank. the hover tooltip always lists everything regardless of these settings. like the other display toggles, this preference is stored per broker host and shared with your other browsers viewing that host. items for other workspaces by default, buttons for windows on other workspaces still appear (dimmed) so you can jump to them. to show only the active workspace's windows in the taskbar, turn on hide taskbar items from other workspaces under control panel → windows → workspaces. this setting governs your browser. workspaces floating window controls","title":"Window buttons"},{"body":[{"spans":[{"t":"text","v":"The launch button doubles as a Start button."}],"t":"p"},{"spans":[{"t":"text","v":"By default:"}],"t":"p"},{"spans":[{"t":"strong","v":"Left-click"},{"t":"text","v":" launches a terminal using your "},{"t":"strong","v":"default host's"},{"t":"text","v":" default profile. The default host is the local broker unless you pick another under Control Panel → Hosts (see Hosts-and-Multi-Browser)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Right-click"},{"t":"text","v":" opens the full launch menu: the launchable terminal profiles plus the other window types, in registration order — control panel, help, task manager, file manager, text editor, sticky note, scratchpad, session recorder — with more entries, such as clipboard history or an AI-provider status monitor, when the matching mods are enabled; see Window-Types for the authoritative list). Every broker leads with a "},{"t":"strong","v":"live status row"},{"t":"text","v":" (its state, and the old chip's click actions — log in, take over the lease, or hide/show its windows, which keeps the menu open) with its profiles below it, so you can launch on a remote host from here. With several hosts attached, the taskbar's aggregate broker badge opens this same menu (see Hosts-and-Multi-Browser)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Swapping the click gestures"}],"t":"sub"},{"spans":[{"t":"text","v":"If you open the picker more often than you use the one-click default, turn on "},{"t":"strong","v":"Control Panel → Desktop → Start button → \"Left-click opens the profile menu (right-click quick-launches)\""},{"t":"text","v":". With it enabled the two gestures swap: "},{"t":"strong","v":"left-click"},{"t":"text","v":" opens the launch menu and "},{"t":"strong","v":"right-click"},{"t":"text","v":" quick-launches your default host's default profile. The native browser context menu never appears either way. The toggle is off by default and, like the button label, applies to the browser you set it from."}],"t":"p"},{"spans":[{"t":"text","v":"Open in folder…"}],"t":"sub"},{"spans":[{"t":"text","v":"The right-click menu also includes an "},{"t":"strong","v":"Open in folder…"},{"t":"text","v":" item under each host's profiles. It opens a directory picker on "},{"t":"strong","v":"that host"},{"t":"text","v":", then starts that host's default profile rooted at the folder you choose. The picker browses the host you'll launch on (not your local machine), so the chosen path exists there. Cancel the picker to do nothing."}],"t":"p"},{"spans":[{"t":"text","v":"Renaming the button"}],"t":"sub"},{"spans":[{"t":"text","v":"The button shows "},{"t":"code","v":"+"},{"t":"text","v":" by default. To change its label, set Control Panel → Desktop → Start button. Leave it blank to fall back to "},{"t":"code","v":"+"},{"t":"text","v":". Only the visible label changes — the click gestures follow whichever mapping you have set (see Swapping the click gestures above)."}],"t":"p"}],"search":"the launch button (+) the launch button doubles as a start button. by default: left-click launches a terminal using your default host's default profile. the default host is the local broker unless you pick another under control panel → hosts (see hosts-and-multi-browser). right-click opens the full launch menu: the launchable terminal profiles plus the other window types, in registration order — control panel, help, task manager, file manager, text editor, sticky note, scratchpad, session recorder — with more entries, such as clipboard history or an ai-provider status monitor, when the matching mods are enabled; see window-types for the authoritative list). every broker leads with a live status row (its state, and the old chip's click actions — log in, take over the lease, or hide/show its windows, which keeps the menu open) with its profiles below it, so you can launch on a remote host from here. with several hosts attached, the taskbar's aggregate broker badge opens this same menu (see hosts-and-multi-browser). swapping the click gestures if you open the picker more often than you use the one-click default, turn on control panel → desktop → start button → \"left-click opens the profile menu (right-click quick-launches)\" . with it enabled the two gestures swap: left-click opens the launch menu and right-click quick-launches your default host's default profile. the native browser context menu never appears either way. the toggle is off by default and, like the button label, applies to the browser you set it from. open in folder… the right-click menu also includes an open in folder… item under each host's profiles. it opens a directory picker on that host , then starts that host's default profile rooted at the folder you choose. the picker browses the host you'll launch on (not your local machine), so the chosen path exists there. cancel the picker to do nothing. renaming the button the button shows + by default. to change its label, set control panel → desktop → start button. leave it blank to fall back to + . only the visible label changes — the click gestures follow whichever mapping you have set (see swapping the click gestures above). hosts and multi browser window types hosts and multi browser","title":"The launch button (+)"},{"body":[{"spans":[{"t":"text","v":"The fullscreen button ("},{"t":"code","v":"⛶"},{"t":"text","v":") toggles the browser into fullscreen and back. You can also bind a key to it — the default is "},{"t":"kbd","v":"Ctrl+Alt+f"},{"t":"text","v":" for the "},{"t":"strong","v":"Toggle fullscreen"},{"t":"text","v":" action. See Keyboard-Shortcuts."}],"t":"p"}],"search":"fullscreen the fullscreen button ( ⛶ ) toggles the browser into fullscreen and back. you can also bind a key to it — the default is ctrl+alt+f for the toggle fullscreen action. see keyboard-shortcuts. keyboard shortcuts","title":"Fullscreen"},{"body":[{"spans":[{"t":"text","v":"The pager dots on the right of the taskbar (left of the host chips) are one-click workspace switchers, and their right-click menus rename or remove workspaces and toggle names vs. numbers. They come from the "},{"t":"strong","v":"Workspaces"},{"t":"text","v":" mod (enabled by default), so the dots are absent if you turn it off. They are covered in full under Workspaces."}],"t":"p"},{"spans":[{"t":"text","v":"To keep workspaces but reclaim the space the dots take, turn on "},{"t":"strong","v":"Hide the workspace pager from the taskbar"},{"t":"text","v":" under Control Panel → Windows → Workspaces. Only the pager goes — the chip badges and dimming, the shortcuts, and "},{"t":"strong","v":"Send to workspace"},{"t":"text","v":" all stay, and the workspace menu (right-click an empty part of the taskbar or the desktop) still switches, adds, renames, and removes workspaces."}],"t":"p"}],"search":"pager dots the pager dots on the right of the taskbar (left of the host chips) are one-click workspace switchers, and their right-click menus rename or remove workspaces and toggle names vs. numbers. they come from the workspaces mod (enabled by default), so the dots are absent if you turn it off. they are covered in full under workspaces. to keep workspaces but reclaim the space the dots take, turn on hide the workspace pager from the taskbar under control panel → windows → workspaces. only the pager goes — the chip badges and dimming, the shortcuts, and send to workspace all stay, and the workspace menu (right-click an empty part of the taskbar or the desktop) still switches, adds, renames, and removes workspaces. workspaces","title":"Pager dots"},{"body":[{"spans":[{"t":"text","v":"The host-status area shows a chip for your broker (or one aggregate badge once you have several). By default it is always there, but it is only an indicator — every broker also has a live row in the start (+) menu carrying the same actions — so "},{"t":"strong","v":"Control Panel → Desktop → Broker status chip"},{"t":"text","v":" lets you turn it down:"}],"t":"p"},{"spans":[{"t":"text","v":"Show · Effect"}],"t":"sub"},{"spans":[{"t":"text","v":"Always — The default, and the same as before this option existed"}],"t":"bullet"},{"spans":[{"t":"text","v":"Only when a broker needs attention — The chip appears only while a broker is unreachable, wants a password, or is held by another browser — and disappears again once nothing is wrong"}],"t":"bullet"},{"spans":[{"t":"text","v":"Never — The chip is never drawn"}],"t":"bullet"},{"spans":[{"t":"text","v":"A broker "},{"t":"strong","v":"you hid yourself"},{"t":"text","v":" never counts as needing attention, so parking one you don't want right now keeps the chip quiet in attention mode. Nothing becomes unreachable when the chip is gone: right-click the "},{"t":"strong","v":"+"},{"t":"text","v":" button and every broker is listed with its state and its log-in / take-over / un-hide action, and the same hide toggle is a checkbox on each host's row under Control Panel → Hosts. Attention mode is also deliberately slow to fire — a broker has to miss two polls (about four seconds) before it counts as down, so a single dropped poll never makes the chip flicker into view."}],"t":"p"},{"spans":[{"t":"text","v":"The setting follows the browser you are sitting at rather than any one host, and is shared with other browsers viewing the same broker. See Hosts-and-Multi-Browser for what each chip state means."}],"t":"p"}],"search":"broker status chip the host-status area shows a chip for your broker (or one aggregate badge once you have several). by default it is always there, but it is only an indicator — every broker also has a live row in the start (+) menu carrying the same actions — so control panel → desktop → broker status chip lets you turn it down: show · effect always — the default, and the same as before this option existed only when a broker needs attention — the chip appears only while a broker is unreachable, wants a password, or is held by another browser — and disappears again once nothing is wrong never — the chip is never drawn a broker you hid yourself never counts as needing attention, so parking one you don't want right now keeps the chip quiet in attention mode. nothing becomes unreachable when the chip is gone: right-click the + button and every broker is listed with its state and its log-in / take-over / un-hide action, and the same hide toggle is a checkbox on each host's row under control panel → hosts. attention mode is also deliberately slow to fire — a broker has to miss two polls (about four seconds) before it counts as down, so a single dropped poll never makes the chip flicker into view. the setting follows the browser you are sitting at rather than any one host, and is shared with other browsers viewing the same broker. see hosts-and-multi-browser for what each chip state means. hosts and multi browser","title":"Broker status chip"}],"label":"Taskbar","order":8,"slug":"taskbar"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Almost every action in Browserland has a right-click home. This is the \"where do I right-click for X\" index: pick the surface you're pointing at, see what its menu offers, and jump to the page that explains each action in depth."}],"t":"p"},{"spans":[{"t":"text","v":"A few rules apply everywhere:"}],"t":"p"},{"spans":[{"t":"text","v":"Greyed-out items don't apply to the current window (for example, "},{"t":"strong","v":"Move column left"},{"t":"text","v":" when the window is already in the leftmost column)."}],"t":"bullet"},{"spans":[{"t":"text","v":"A "},{"t":"code","v":"✓"},{"t":"text","v":" marks the current setting (the active width preset, the current MCP mode, names-vs-numbers, and so on)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Press "},{"t":"kbd","v":"Escape"},{"t":"text","v":", or click anywhere outside the menu, to dismiss it."}],"t":"bullet"}],"search":"overview almost every action in browserland has a right-click home. this is the \"where do i right-click for x\" index: pick the surface you're pointing at, see what its menu offers, and jump to the page that explains each action in depth. a few rules apply everywhere: greyed-out items don't apply to the current window (for example, move column left when the window is already in the leftmost column). a ✓ marks the current setting (the active width preset, the current mcp mode, names-vs-numbers, and so on). press escape , or click anywhere outside the menu, to dismiss it.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Surface · How to open · Covered in depth on"}],"t":"sub"},{"spans":[{"t":"text","v":"Window title bar — Right-click a window's title bar — Arranging-Windows, Columns-and-Widths, Floating-Window-Controls"}],"t":"bullet"},{"spans":[{"t":"text","v":"Tab strip — The "},{"t":"code","v":"⊟"},{"t":"text","v":" button on a tile's tab strip — Arranging-Windows"}],"t":"bullet"},{"spans":[{"t":"text","v":"Taskbar item — Right-click a window's taskbar button — Taskbar"}],"t":"bullet"},{"spans":[{"t":"text","v":"Pager dot — Right-click a workspace dot in the taskbar — Workspaces"}],"t":"bullet"},{"spans":[{"t":"text","v":"Empty desktop / strip — Right-click the background (or empty taskbar) — Floating-Window-Controls, Workspaces"}],"t":"bullet"},{"spans":[{"t":"text","v":"Launch (+) button — Right-click the "},{"t":"code","v":"+"},{"t":"text","v":" button — Taskbar, Window-Types"}],"t":"bullet"}],"search":"at a glance surface · how to open · covered in depth on window title bar — right-click a window's title bar — arranging-windows, columns-and-widths, floating-window-controls tab strip — the ⊟ button on a tile's tab strip — arranging-windows taskbar item — right-click a window's taskbar button — taskbar pager dot — right-click a workspace dot in the taskbar — workspaces empty desktop / strip — right-click the background (or empty taskbar) — floating-window-controls, workspaces launch (+) button — right-click the + button — taskbar, window-types arranging windows columns and widths floating window controls arranging windows taskbar workspaces floating window controls workspaces taskbar window types","title":"At a glance"},{"body":[{"spans":[{"t":"text","v":"Right-click a window's title bar. The menu adapts to whether the window is "},{"t":"strong","v":"tiled"},{"t":"text","v":" or "},{"t":"strong","v":"floating"},{"t":"text","v":", and to whether it's a terminal or an app window (note, editor, file manager, task manager, Control Panel, help)."}],"t":"p"},{"spans":[{"t":"strong","v":"When the window is tiled:"}],"t":"p"},{"spans":[{"t":"strong","v":"Column width"},{"t":"text","v":" — set the column to "},{"t":"code","v":"⅓"},{"t":"text","v":", "},{"t":"code","v":"½"},{"t":"text","v":", "},{"t":"code","v":"⅔"},{"t":"text","v":", or "},{"t":"code","v":"max"},{"t":"text","v":". See Columns-and-Widths."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Stack into left column"},{"t":"text","v":" / "},{"t":"strong","v":"Stack into right column"},{"t":"text","v":" — merge this window down into a neighbor column as a row."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Move to own column"},{"t":"text","v":" — eject a window that shares a column into its own."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Move to new column"},{"t":"text","v":" — spawn a fresh column to its right."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tab into left column"},{"t":"text","v":" / "},{"t":"strong","v":"Tab into right column"},{"t":"text","v":" — tab this window into the neighbor column's live tile."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tab this window"},{"t":"text","v":" — seed or keep a tab strip in place for a lone window."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Untab tile (split to rows)"},{"t":"text","v":" — break a tabbed tile back into stacked rows."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Untab cell (side by side)"},{"t":"text","v":" — drop a nested split-group's tabs out as adjacent side-by-side cells."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Un-split row (split to rows)"},{"t":"text","v":" — explode a split row into stacked rows."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Move column left"},{"t":"text","v":" / "},{"t":"strong","v":"Move column right"},{"t":"text","v":" — shift the whole column along the strip."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Send to <workspace>"},{"t":"text","v":" / "},{"t":"strong","v":"Send to new workspace"},{"t":"text","v":" — move the window to another virtual desktop. See Workspaces."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Float this window"},{"t":"text","v":" — detach it from the strip as a floating window. See Snapping-and-Pop-out."}],"t":"bullet"},{"spans":[{"t":"text","v":"Tabbing, splitting, and stacking are all explained on Arranging-Windows; the column actions live on Columns-and-Widths."}],"t":"p"},{"spans":[{"t":"strong","v":"When the window is floating:"}],"t":"p"},{"spans":[{"t":"strong","v":"Tile this window"},{"t":"text","v":" — drop it straight into the tiling strip (no drag needed). See Snapping-and-Pop-out."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Lock to screen (pin)"},{"t":"text","v":" / "},{"t":"strong","v":"Unlock (scroll with strip)"},{"t":"text","v":" — pin the window so it doesn't scroll away with the tiling strip (this pins position, not always-on-top)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"On all workspaces"},{"t":"text","v":" / "},{"t":"strong","v":"Show on all workspaces"},{"t":"text","v":" — keep a floating window visible on every workspace, or limit it to this one. See Workspaces."}],"t":"bullet"},{"spans":[{"t":"strong","v":"On every window:"}],"t":"p"},{"spans":[{"t":"strong","v":"MCP access"},{"t":"text","v":" (terminals only) — set this window's agent access to "},{"t":"strong","v":"Off"},{"t":"text","v":", "},{"t":"strong","v":"Read"},{"t":"text","v":", or "},{"t":"strong","v":"Read-write"},{"t":"text","v":". See MCP-and-AI-Agents."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Minimize"},{"t":"text","v":" / "},{"t":"strong","v":"Restore"},{"t":"text","v":" — hide the window to the taskbar, or bring it back."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" — soft close. A terminal's shell keeps running; a non-empty sticky note reopens from Closed notes in the "},{"t":"strong","v":"+"},{"t":"text","v":" menu; a text editor keeps its file on the host (with a save prompt for unsaved changes); a file manager / task manager just closes."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminate"},{"t":"text","v":" (terminals only) — hard-kill the shell process tree (asks to confirm)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Delete note"},{"t":"text","v":" / "},{"t":"strong","v":"Delete file"},{"t":"text","v":" (note and editor windows — not the file manager, task manager, Control Panel, or help) — permanently discard that window and its stored document (asks to confirm)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Close, terminate, delete, and pinning are detailed on Floating-Window-Controls."}],"t":"p"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"strong","v":"Color and rename"},{"t":"text","v":" aren't on this right-click menu. Recolor a window from the "},{"t":"strong","v":"color button"},{"t":"text","v":" in its title bar (palette, custom, and recents), and "},{"t":"strong","v":"double-click"},{"t":"text","v":" an app window's title to rename it — both covered on Floating-Window-Controls."}],"t":"p"}],"search":"window title bar right-click a window's title bar. the menu adapts to whether the window is tiled or floating , and to whether it's a terminal or an app window (note, editor, file manager, task manager, control panel, help). when the window is tiled: column width — set the column to ⅓ , ½ , ⅔ , or max . see columns-and-widths. stack into left column / stack into right column — merge this window down into a neighbor column as a row. move to own column — eject a window that shares a column into its own. move to new column — spawn a fresh column to its right. tab into left column / tab into right column — tab this window into the neighbor column's live tile. tab this window — seed or keep a tab strip in place for a lone window. untab tile (split to rows) — break a tabbed tile back into stacked rows. untab cell (side by side) — drop a nested split-group's tabs out as adjacent side-by-side cells. un-split row (split to rows) — explode a split row into stacked rows. move column left / move column right — shift the whole column along the strip. send to <workspace> / send to new workspace — move the window to another virtual desktop. see workspaces. float this window — detach it from the strip as a floating window. see snapping-and-pop-out. tabbing, splitting, and stacking are all explained on arranging-windows; the column actions live on columns-and-widths. when the window is floating: tile this window — drop it straight into the tiling strip (no drag needed). see snapping-and-pop-out. lock to screen (pin) / unlock (scroll with strip) — pin the window so it doesn't scroll away with the tiling strip (this pins position, not always-on-top). on all workspaces / show on all workspaces — keep a floating window visible on every workspace, or limit it to this one. see workspaces. on every window: mcp access (terminals only) — set this window's agent access to off , read , or read-write . see mcp-and-ai-agents. minimize / restore — hide the window to the taskbar, or bring it back. close — soft close. a terminal's shell keeps running; a non-empty sticky note reopens from closed notes in the + menu; a text editor keeps its file on the host (with a save prompt for unsaved changes); a file manager / task manager just closes. terminate (terminals only) — hard-kill the shell process tree (asks to confirm). delete note / delete file (note and editor windows — not the file manager, task manager, control panel, or help) — permanently discard that window and its stored document (asks to confirm). close, terminate, delete, and pinning are detailed on floating-window-controls. tip: color and rename aren't on this right-click menu. recolor a window from the color button in its title bar (palette, custom, and recents), and double-click an app window's title to rename it — both covered on floating-window-controls. columns and widths workspaces snapping and pop out arranging windows columns and widths snapping and pop out workspaces mcp and ai agents floating window controls floating window controls","title":"Window title bar"},{"body":[{"spans":[{"t":"text","v":"When windows are stacked as tabs in one tile, the tile shows a tab strip. The strip's "},{"t":"code","v":"⊟"},{"t":"text","v":" button untabs it:"}],"t":"p"},{"spans":[{"t":"text","v":"On a "},{"t":"strong","v":"top-level tabbed tile"},{"t":"text","v":", "},{"t":"code","v":"⊟"},{"t":"text","v":" does "},{"t":"strong","v":"Split into rows"},{"t":"text","v":" (the same as the title-bar's "},{"t":"strong","v":"Untab tile (split to rows)"},{"t":"text","v":")."}],"t":"bullet"},{"spans":[{"t":"text","v":"On a "},{"t":"strong","v":"nested split group"},{"t":"text","v":", "},{"t":"code","v":"⊟"},{"t":"text","v":" does "},{"t":"strong","v":"Drop tabs side by side"},{"t":"text","v":" (the same as "},{"t":"strong","v":"Untab cell (side by side)"},{"t":"text","v":")."}],"t":"bullet"},{"spans":[{"t":"text","v":"Click any tab to switch to that window. Tabs and untabbing are explained on Arranging-Windows."}],"t":"p"}],"search":"tab strip when windows are stacked as tabs in one tile, the tile shows a tab strip. the strip's ⊟ button untabs it: on a top-level tabbed tile , ⊟ does split into rows (the same as the title-bar's untab tile (split to rows) ). on a nested split group , ⊟ does drop tabs side by side (the same as untab cell (side by side) ). click any tab to switch to that window. tabs and untabbing are explained on arranging-windows. arranging windows","title":"Tab strip"},{"body":[{"spans":[{"t":"text","v":"Each open window has a taskbar button. Left-click focuses and raises it (or restores it if minimized). Right-click for per-window actions:"}],"t":"p"},{"spans":[{"t":"strong","v":"Focus"},{"t":"text","v":" — switch to the window's workspace, restore it if minimized, and raise it. This works even for a closed or parked window (it reopens it)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Restore"},{"t":"text","v":" / "},{"t":"strong","v":"Minimize"},{"t":"text","v":" — bring a minimized window back, or hide it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" — soft close, same as the title-bar menu."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminate"},{"t":"text","v":" (terminals only) — hard-kill the shell, even on a parked session (asks to confirm)."}],"t":"bullet"},{"spans":[{"t":"text","v":"To send a window to another workspace, use its "},{"t":"strong","v":"title-bar"},{"t":"text","v":" menu (above). The taskbar is covered on Taskbar."}],"t":"p"}],"search":"taskbar item each open window has a taskbar button. left-click focuses and raises it (or restores it if minimized). right-click for per-window actions: focus — switch to the window's workspace, restore it if minimized, and raise it. this works even for a closed or parked window (it reopens it). restore / minimize — bring a minimized window back, or hide it. close — soft close, same as the title-bar menu. terminate (terminals only) — hard-kill the shell, even on a parked session (asks to confirm). to send a window to another workspace, use its title-bar menu (above). the taskbar is covered on taskbar. taskbar","title":"Taskbar item"},{"body":[{"spans":[{"t":"text","v":"Right-click a workspace dot at the bottom of the taskbar:"}],"t":"p"},{"spans":[{"t":"strong","v":"Rename…"},{"t":"text","v":" — give the workspace a name."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Remove workspace"},{"t":"text","v":" — delete it (disabled when only one workspace remains)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Show names"},{"t":"text","v":" / "},{"t":"strong","v":"Show numbers"},{"t":"text","v":" — choose whether the dots display workspace names or numbers."}],"t":"bullet"},{"spans":[{"t":"strong","v":"New workspace"},{"t":"text","v":" — append a fresh, empty workspace."}],"t":"bullet"},{"spans":[{"t":"text","v":"Workspaces and the pager are covered on Workspaces. This whole menu comes from the Workspaces mod — with it disabled there are no pager dots to right-click. Hiding the pager (Control Panel → Windows → Workspaces) removes the dots too, but rename and remove are on the empty desktop / taskbar menu below as well, so they survive that. Disabling the mod takes both menus away together."}],"t":"p"}],"search":"pager dot right-click a workspace dot at the bottom of the taskbar: rename… — give the workspace a name. remove workspace — delete it (disabled when only one workspace remains). show names / show numbers — choose whether the dots display workspace names or numbers. new workspace — append a fresh, empty workspace. workspaces and the pager are covered on workspaces. this whole menu comes from the workspaces mod — with it disabled there are no pager dots to right-click. hiding the pager (control panel → windows → workspaces) removes the dots too, but rename and remove are on the empty desktop / taskbar menu below as well, so they survive that. disabling the mod takes both menus away together. workspaces","title":"Pager dot"},{"body":[{"spans":[{"t":"text","v":"Right-click the desktop background (or an empty part of the taskbar). The menu depends on the window mode."}],"t":"p"},{"spans":[{"t":"strong","v":"In floating mode"},{"t":"text","v":" — one-shot arrangements plus a size lock:"}],"t":"p"},{"spans":[{"t":"strong","v":"Cascade"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile Horizontally"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile Vertically"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tile H + V"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Lock Size"},{"t":"text","v":" / "},{"t":"strong","v":"Unlock Size"},{"t":"text","v":" — snap every floating window to the default size and hide the resize handles, or restore free resizing."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Minimize All Windows"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Both modes"},{"t":"text","v":" then show the workspace block:"}],"t":"p"},{"spans":[{"t":"text","v":"A row per workspace, showing its name and a count of what it holds — columns in tiling mode, floating windows in floating mode (the "},{"t":"code","v":"✓"},{"t":"text","v":" marks the active one); click to jump to it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"New workspace"},{"t":"text","v":" — append a fresh, empty workspace."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Rename <workspace>…"},{"t":"text","v":" — rename the workspace you are on."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Remove <workspace>"},{"t":"text","v":" — delete the workspace you are on (disabled when only one remains)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Both modes then offer "},{"t":"strong","v":"🎛 Control panel"},{"t":"text","v":" — this menu does still use the emoji. "},{"t":"strong","v":"Undo <action>"},{"t":"text","v":" comes after it, as the "},{"t":"strong","v":"last"},{"t":"text","v":" item in the menu, appearing in floating mode once there is an arrange to reverse (single-level). The arrange and lock-size actions are covered on Floating-Window-Controls; the workspace block on Workspaces — it comes from the Workspaces mod, so with that disabled the tiling menu is just "},{"t":"strong","v":"Control panel"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Because this menu also opens from an empty part of the "},{"t":"strong","v":"taskbar"},{"t":"text","v":", it is the one workspace surface that is always reachable — no window can cover it — which is what makes hiding the pager safe."}],"t":"p"}],"search":"empty desktop / strip right-click the desktop background (or an empty part of the taskbar). the menu depends on the window mode. in floating mode — one-shot arrangements plus a size lock: cascade tile horizontally tile vertically tile h + v lock size / unlock size — snap every floating window to the default size and hide the resize handles, or restore free resizing. minimize all windows both modes then show the workspace block: a row per workspace, showing its name and a count of what it holds — columns in tiling mode, floating windows in floating mode (the ✓ marks the active one); click to jump to it. new workspace — append a fresh, empty workspace. rename <workspace>… — rename the workspace you are on. remove <workspace> — delete the workspace you are on (disabled when only one remains). both modes then offer 🎛 control panel — this menu does still use the emoji. undo <action> comes after it, as the last item in the menu, appearing in floating mode once there is an arrange to reverse (single-level). the arrange and lock-size actions are covered on floating-window-controls; the workspace block on workspaces — it comes from the workspaces mod, so with that disabled the tiling menu is just control panel . because this menu also opens from an empty part of the taskbar , it is the one workspace surface that is always reachable — no window can cover it — which is what makes hiding the pager safe. floating window controls workspaces","title":"Empty desktop / strip"},{"body":[{"spans":[{"t":"text","v":"Left-click the "},{"t":"code","v":"+"},{"t":"text","v":" to launch the default host's default terminal — the local broker unless you've picked another default in Control Panel → Hosts (see Hosts-and-Multi-Browser). Right-click it for the full launch menu:"}],"t":"p"},{"spans":[{"t":"strong","v":"Broker status rows"},{"t":"text","v":" — one per attached broker: a state dot and label doing what the taskbar chip does (log in, take over the lease, or hide/show that broker's windows — the hide toggle keeps the menu open). See Hosts-and-Multi-Browser."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminal profiles"},{"t":"text","v":" — each launchable profile for the host (the default is marked "},{"t":"code","v":"(default)"},{"t":"text","v":"), listed under its broker's status row."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Open in folder…"},{"t":"text","v":" — a directory picker (per host) that starts the host's default profile rooted at the folder you choose."}],"t":"bullet"},{"spans":[{"t":"strong","v":"App windows"},{"t":"text","v":" — in registration order: "},{"t":"strong","v":"Control panel"},{"t":"text","v":", "},{"t":"strong","v":"Help"},{"t":"text","v":", "},{"t":"strong","v":"Task manager"},{"t":"text","v":", "},{"t":"strong","v":"File manager"},{"t":"text","v":", "},{"t":"strong","v":"Text editor"},{"t":"text","v":", "},{"t":"strong","v":"Sticky note"},{"t":"text","v":", "},{"t":"strong","v":"Scratchpad"},{"t":"text","v":", "},{"t":"strong","v":"Session recorder"},{"t":"text","v":". Each carries a small SVG icon, not an emoji. Mods that ship off by default add their own rows once enabled, "},{"t":"strong","v":"Clipboard"},{"t":"text","v":" and "},{"t":"strong","v":"AI status"},{"t":"text","v":" among them. See Window-Types."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Closed notes"},{"t":"text","v":" — at the bottom, any non-empty sticky note you've closed; click one to reopen it."}],"t":"bullet"},{"spans":[{"t":"text","v":"The launch button and start-button label are covered on Taskbar."}],"t":"p"}],"search":"launch (+) button left-click the + to launch the default host's default terminal — the local broker unless you've picked another default in control panel → hosts (see hosts-and-multi-browser). right-click it for the full launch menu: broker status rows — one per attached broker: a state dot and label doing what the taskbar chip does (log in, take over the lease, or hide/show that broker's windows — the hide toggle keeps the menu open). see hosts-and-multi-browser. terminal profiles — each launchable profile for the host (the default is marked (default) ), listed under its broker's status row. open in folder… — a directory picker (per host) that starts the host's default profile rooted at the folder you choose. app windows — in registration order: control panel , help , task manager , file manager , text editor , sticky note , scratchpad , session recorder . each carries a small svg icon, not an emoji. mods that ship off by default add their own rows once enabled, clipboard and ai status among them. see window-types. closed notes — at the bottom, any non-empty sticky note you've closed; click one to reopen it. the launch button and start-button label are covered on taskbar. hosts and multi browser hosts and multi browser window types taskbar","title":"Launch (+) button"}],"label":"Context Menus","order":9,"slug":"context-menus"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland's look comes from three independent mods: a "},{"t":"strong","v":"color scheme"},{"t":"text","v":", a "},{"t":"strong","v":"background pattern"},{"t":"text","v":", and the "},{"t":"strong","v":"terminal font"},{"t":"text","v":". All three are configured from the Control Panel's "},{"t":"strong","v":"Desktop"},{"t":"text","v":" applet, and all three are browser-global — they follow the browser you are sitting at, not any one broker, so they stay the same across every host you connect to from here."}],"t":"p"}],"search":"overview browserland's look comes from three independent mods: a color scheme , a background pattern , and the terminal font . all three are configured from the control panel's desktop applet, and all three are browser-global — they follow the browser you are sitting at, not any one broker, so they stay the same across every host you connect to from here. getting started","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Color scheme"},{"t":"text","v":" setting (Control Panel → Desktop) picks the desktop's chrome colors — background, panel and accent tones. It ships enabled by default."}],"t":"p"},{"spans":[{"t":"text","v":"Scheme · Look"}],"t":"sub"},{"spans":[{"t":"text","v":"Night (dark) — The default — a dark desktop"}],"t":"bullet"},{"spans":[{"t":"text","v":"Day (light) — A light desktop"}],"t":"bullet"},{"spans":[{"t":"text","v":"Redmond (teal) — A period-desktop look: teal desktop, silver window chrome"}],"t":"bullet"},{"spans":[{"t":"text","v":"Midnight Blue — A dark blue desktop"}],"t":"bullet"},{"spans":[{"t":"text","v":"Sunday Orange — A warm dark-orange desktop"}],"t":"bullet"},{"spans":[{"t":"text","v":"Picking a scheme applies immediately and repaints the background pattern (below) in the new colors too, since the pattern is drawn from the scheme's own tones."}],"t":"p"}],"search":"color scheme the color scheme setting (control panel → desktop) picks the desktop's chrome colors — background, panel and accent tones. it ships enabled by default. scheme · look night (dark) — the default — a dark desktop day (light) — a light desktop redmond (teal) — a period-desktop look: teal desktop, silver window chrome midnight blue — a dark blue desktop sunday orange — a warm dark-orange desktop picking a scheme applies immediately and repaints the background pattern (below) in the new colors too, since the pattern is drawn from the scheme's own tones.","title":"Color scheme"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Background pattern"},{"t":"text","v":" setting (Control Panel → Desktop) paints a pattern on the desktop, behind the tiling strip and any floating windows. It ships enabled by default, set to "},{"t":"strong","v":"None"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Pattern · Look"}],"t":"sub"},{"spans":[{"t":"text","v":"None — The default — a plain desktop, no pattern"}],"t":"bullet"},{"spans":[{"t":"text","v":"Weave — A crosshatched weave"}],"t":"bullet"},{"spans":[{"t":"text","v":"Dither — A checkered dither"}],"t":"bullet"},{"spans":[{"t":"text","v":"Dots — A grid of dots"}],"t":"bullet"},{"spans":[{"t":"text","v":"Hatch — Diagonal hatching"}],"t":"bullet"},{"spans":[{"t":"text","v":"Tiles — A grid of tile lines"}],"t":"bullet"},{"spans":[{"t":"text","v":"The pattern is drawn using the current color scheme's tones, so switching schemes recolors the pattern automatically — you never need to re-pick it after changing the scheme."}],"t":"p"}],"search":"background pattern the background pattern setting (control panel → desktop) paints a pattern on the desktop, behind the tiling strip and any floating windows. it ships enabled by default, set to none . pattern · look none — the default — a plain desktop, no pattern weave — a crosshatched weave dither — a checkered dither dots — a grid of dots hatch — diagonal hatching tiles — a grid of tile lines the pattern is drawn using the current color scheme's tones, so switching schemes recolors the pattern automatically — you never need to re-pick it after changing the scheme.","title":"Background pattern"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Terminal font"},{"t":"text","v":" setting changes the font every terminal window renders with. Unlike the two settings above, it ships "},{"t":"strong","v":"off by default"},{"t":"text","v":" as a mod: enable "},{"t":"strong","v":"Terminal font"},{"t":"text","v":" under "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":" first (see Installing-Mods), which takes effect on the "},{"t":"strong","v":"next page load"},{"t":"text","v":". Once enabled, its select appears alongside Color scheme and Background pattern in Control Panel → Desktop."}],"t":"p"},{"spans":[{"t":"text","v":"The offered fonts are:"}],"t":"p"},{"spans":[{"t":"text","v":"Default (Consolas)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Cascadia Code"}],"t":"bullet"},{"spans":[{"t":"text","v":"Fira Code"}],"t":"bullet"},{"spans":[{"t":"text","v":"JetBrains Mono"}],"t":"bullet"},{"spans":[{"t":"text","v":"Source Code Pro"}],"t":"bullet"},{"spans":[{"t":"text","v":"Courier New"}],"t":"bullet"},{"spans":[{"t":"text","v":"System monospace"}],"t":"bullet"},{"spans":[{"t":"text","v":"Picking a font applies immediately to every open terminal window, and to every terminal you open afterward — no reload needed. A font you pick but don't have installed simply falls back to the default. If you later disable the mod, terminals return to the built-in default font."}],"t":"p"}],"search":"terminal font the terminal font setting changes the font every terminal window renders with. unlike the two settings above, it ships off by default as a mod: enable terminal font under control panel → mods first (see installing-mods), which takes effect on the next page load . once enabled, its select appears alongside color scheme and background pattern in control panel → desktop. the offered fonts are: default (consolas) cascadia code fira code jetbrains mono source code pro courier new system monospace picking a font applies immediately to every open terminal window, and to every terminal you open afterward — no reload needed. a font you pick but don't have installed simply falls back to the default. if you later disable the mod, terminals return to the built-in default font. installing mods","title":"Terminal font"}],"label":"Themes & Appearance","order":10,"slug":"themes-and-appearance"},{"cards":[{"body":[{"spans":[{"t":"text","v":"A "},{"t":"strong","v":"mod"},{"t":"text","v":" is a piece of the Browserland desktop — a taskbar chip, an app window, a Control Panel setting — and a broker can be handed a new one at runtime, without a restart. This page covers installing, uninstalling and managing mods from the Control Panel. For what a mod actually is, how one is written, and the trust it takes on, see Writing a Mod."}],"t":"p"}],"search":"overview a mod is a piece of the browserland desktop — a taskbar chip, an app window, a control panel setting — and a broker can be handed a new one at runtime, without a restart. this page covers installing, uninstalling and managing mods from the control panel. for what a mod actually is, how one is written, and the trust it takes on, see writing a mod. writing a mod","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Open "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":" on the broker's own tab (the broker this page is being served from — installing and uninstalling always act on that one broker, not a remote host you've added). The pane lists every mod that broker knows about, each with an enable checkbox, its provenance, and its live status."}],"t":"p"}],"search":"where to find it open control panel → mods on the broker's own tab (the broker this page is being served from — installing and uninstalling always act on that one broker, not a remote host you've added). the pane lists every mod that broker knows about, each with an enable checkbox, its provenance, and its live status.","title":"Where to find it"},{"body":[{"spans":[{"t":"text","v":"Click "},{"t":"strong","v":"Install a mod…"},{"t":"text","v":" and pick a mod's folder (or a single "},{"t":"code","v":".js"},{"t":"text","v":" file) from your computer. Before anything is sent, you get an "},{"t":"strong","v":"install preview"},{"t":"text","v":" — the exact files and manifest that will be uploaded, plus any advisory warnings (a missing field, a file over the size limit, and the like) so you can back out before committing to something the broker would reject anyway."}],"t":"p"}],"search":"installing a mod click install a mod… and pick a mod's folder (or a single .js file) from your computer. before anything is sent, you get an install preview — the exact files and manifest that will be uploaded, plus any advisory warnings (a missing field, a file over the size limit, and the like) so you can back out before committing to something the broker would reject anyway.","title":"Installing a mod"},{"body":[{"spans":[{"t":"text","v":"Every installed mod's row carries an "},{"t":"strong","v":"Uninstall"},{"t":"text","v":" button. "},{"t":"strong","v":"Shipped mods have no Uninstall button at all"},{"t":"text","v":" — they are part of the broker's own program and can't be removed this way; only a mod installed at runtime can be. Uninstalling gives you the option to also delete the mod's data stored on that broker; without it, the mod's code and pin are removed but the server-side data it left behind stays."}],"t":"p"}],"search":"uninstalling a mod every installed mod's row carries an uninstall button. shipped mods have no uninstall button at all — they are part of the broker's own program and can't be removed this way; only a mod installed at runtime can be. uninstalling gives you the option to also delete the mod's data stored on that broker; without it, the mod's code and pin are removed but the server-side data it left behind stays.","title":"Uninstalling a mod"},{"body":[{"spans":[{"t":"strong","v":"An install or uninstall does not take effect in the page you're looking at."},{"t":"text","v":" Every mod's script shares one execution scope with every other mod and with the desktop's own code, and JavaScript cannot re-declare or un-declare something already loaded — re-running a mod's script in the same page throws an error instead of updating it. So the broker takes the change immediately (the next browser to load its page sees it), but this tab needs a reload to pick it up. If you just installed something and it doesn't seem to be there yet, that's why — reload the page."}],"t":"p"}],"search":"reload to finish an install or uninstall does not take effect in the page you're looking at. every mod's script shares one execution scope with every other mod and with the desktop's own code, and javascript cannot re-declare or un-declare something already loaded — re-running a mod's script in the same page throws an error instead of updating it. so the broker takes the change immediately (the next browser to load its page sees it), but this tab needs a reload to pick it up. if you just installed something and it doesn't seem to be there yet, that's why — reload the page.","title":"Reload to finish"},{"body":[{"spans":[{"t":"text","v":"Each row in the Mods pane carries a small "},{"t":"strong","v":"shipped"},{"t":"text","v":" / "},{"t":"strong","v":"installed"},{"t":"text","v":" badge. "},{"t":"strong","v":"Shipped"},{"t":"text","v":" mods came in the broker's own program and were there before you touched anything. "},{"t":"strong","v":"Installed"},{"t":"text","v":" mods arrived through this install feature — from you, or from whoever administers that broker. The badge is how you tell the two apart at a glance."}],"t":"p"}],"search":"provenance: shipped or installed each row in the mods pane carries a small shipped / installed badge. shipped mods came in the broker's own program and were there before you touched anything. installed mods arrived through this install feature — from you, or from whoever administers that broker. the badge is how you tell the two apart at a glance.","title":"Provenance: shipped or installed"},{"body":[{"spans":[{"t":"text","v":"If an installed mod bricks the desktop — throws on load, breaks a shared piece of the page — you might not be able to reach the Control Panel to remove it. Add "},{"t":"code","v":"?nomods=1"},{"t":"text","v":" to the broker's URL by hand and reload: the desktop comes up with no mods fetched or run at all, shipped or installed, so you can get back into the Control Panel and sort things out. It's a one-time, read-only flag — nothing is saved, so leave it off the URL once you're done and mods load normally again."}],"t":"p"}],"search":"`?nomods=1` — the rescue hatch if an installed mod bricks the desktop — throws on load, breaks a shared piece of the page — you might not be able to reach the control panel to remove it. add ?nomods=1 to the broker's url by hand and reload: the desktop comes up with no mods fetched or run at all, shipped or installed, so you can get back into the control panel and sort things out. it's a one-time, read-only flag — nothing is saved, so leave it off the url once you're done and mods load normally again.","title":"`?nomods=1` — the rescue hatch"},{"body":[{"spans":[{"t":"text","v":"Unchecking a mod in the Mods pane stops it from running — but its code is still present in the page, still fetched, and its stylesheet is still live either way. Disabling is a way to turn a mod off, not a way to keep code you don't trust off your machine. Only uninstalling removes it."}],"t":"p"}],"search":"installed but disabled is not contained unchecking a mod in the mods pane stops it from running — but its code is still present in the page, still fetched, and its stylesheet is still live either way. disabling is a way to turn a mod off, not a way to keep code you don't trust off your machine. only uninstalling removes it.","title":"Installed but disabled is not contained"},{"body":[{"spans":[{"t":"text","v":"Nothing above is a security boundary. A mod runs with the same authority as the rest of the desktop — it can read tokens, reach every broker route the UI itself uses, and touch the same page state as core. Installing a mod is a trust decision, the same as installing any other program: only install one you'd be willing to review. See Writing a Mod for the full picture."}],"t":"p"}],"search":"a mod is not sandboxed nothing above is a security boundary. a mod runs with the same authority as the rest of the desktop — it can read tokens, reach every broker route the ui itself uses, and touch the same page state as core. installing a mod is a trust decision, the same as installing any other program: only install one you'd be willing to review. see writing a mod for the full picture. writing a mod","title":"A mod is not sandboxed"},{"body":[{"spans":[{"t":"text","v":"A handful of shipped mods start disabled and have to be switched on yourself, under "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":":"}],"t":"p"},{"spans":[{"t":"text","v":"Mod · Why it's off by default"}],"t":"sub"},{"spans":[{"t":"text","v":"Clipboard — Clipboard history can carry secrets, so nothing is captured until you opt in"}],"t":"bullet"},{"spans":[{"t":"text","v":"AI status — Checking providers' status pages exposes the broker's egress IP to those hosts"}],"t":"bullet"},{"spans":[{"t":"text","v":"Update check — The broker also needs its own operator-set switch before it will reach GitHub at all"}],"t":"bullet"},{"spans":[{"t":"text","v":"Terminal font — An opt-in setting rather than something everyone needs on"}],"t":"bullet"},{"spans":[{"t":"text","v":"Turning one on is the same as enabling any other mod — tick its checkbox in the Mods pane."}],"t":"p"}],"search":"mods that ship off by default a handful of shipped mods start disabled and have to be switched on yourself, under control panel → mods : mod · why it's off by default clipboard — clipboard history can carry secrets, so nothing is captured until you opt in ai status — checking providers' status pages exposes the broker's egress ip to those hosts update check — the broker also needs its own operator-set switch before it will reach github at all terminal font — an opt-in setting rather than something everyone needs on turning one on is the same as enabling any other mod — tick its checkbox in the mods pane.","title":"Mods that ship off by default"}],"label":"Installing Mods","order":11,"slug":"installing-mods"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland's desktop holds several kinds of windows, all opened from the launch ("},{"t":"strong","v":"+"},{"t":"text","v":") button on the taskbar: a "},{"t":"strong","v":"terminal"},{"t":"text","v":", a "},{"t":"strong","v":"sticky note"},{"t":"text","v":", a "},{"t":"strong","v":"CodeMirror text editor"},{"t":"text","v":", a "},{"t":"strong","v":"file manager"},{"t":"text","v":", a "},{"t":"strong","v":"task manager"},{"t":"text","v":", a "},{"t":"strong","v":"Scratchpad"},{"t":"text","v":", and the "},{"t":"strong","v":"Control Panel"},{"t":"text","v":". (The in-app "},{"t":"strong","v":"help"},{"t":"text","v":" guide opens the same way — see Getting-Started.) This page explains what each one is for and how it behaves."}],"t":"p"}],"search":"overview browserland's desktop holds several kinds of windows, all opened from the launch ( + ) button on the taskbar: a terminal , a sticky note , a codemirror text editor , a file manager , a task manager , a scratchpad , and the control panel . (the in-app help guide opens the same way — see getting-started.) this page explains what each one is for and how it behaves. getting started","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"+"},{"t":"text","v":" button at the left of the taskbar is how you create windows:"}],"t":"p"},{"spans":[{"t":"strong","v":"Left-click +"},{"t":"text","v":" to launch a terminal on your "},{"t":"strong","v":"default host"},{"t":"text","v":" — the local broker unless you pick another in Control Panel → Hosts."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Right-click +"},{"t":"text","v":" for the full profile / app menu. Alongside the terminal profiles (grouped per broker when you have more than one host), it lists the app windows:"}],"t":"bullet"},{"spans":[{"t":"text","v":"The items appear in this order, which is the order the window kinds register in — not alphabetical:"}],"t":"p"},{"spans":[{"t":"text","v":"Menu item · Opens"}],"t":"sub"},{"spans":[{"t":"code","v":"Control panel"},{"t":"text","v":" — The Control Panel (settings)"}],"t":"bullet"},{"spans":[{"t":"code","v":"Help"},{"t":"text","v":" — The in-app help guide"}],"t":"bullet"},{"spans":[{"t":"code","v":"Task manager"},{"t":"text","v":" — A task manager"}],"t":"bullet"},{"spans":[{"t":"code","v":"File manager"},{"t":"text","v":" — A file manager"}],"t":"bullet"},{"spans":[{"t":"code","v":"Text editor"},{"t":"text","v":" — A CodeMirror text editor"}],"t":"bullet"},{"spans":[{"t":"code","v":"Sticky note"},{"t":"text","v":" — A sticky note"}],"t":"bullet"},{"spans":[{"t":"code","v":"Scratchpad"},{"t":"text","v":" — Broker-stored shared notes (from the Scratchpad mod)"}],"t":"bullet"},{"spans":[{"t":"code","v":"Session recorder"},{"t":"text","v":" — The session-recording library (play back recorded terminals)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Each row carries a small SVG icon rather than an emoji. Mods that ship "},{"t":"strong","v":"off"},{"t":"text","v":" by default add their own rows once enabled — "},{"t":"strong","v":"Clipboard"},{"t":"text","v":" (clipboard history) and "},{"t":"strong","v":"AI status"},{"t":"text","v":" among them — and a "},{"t":"strong","v":"Closed notes"},{"t":"text","v":" entry appears when there are sticky notes to restore."}],"t":"p"},{"spans":[{"t":"text","v":"You can also open a terminal with the "},{"t":"strong","v":"New terminal"},{"t":"text","v":" shortcut ("},{"t":"kbd","v":"Ctrl+Alt+Enter"},{"t":"text","v":" — see Keyboard-Shortcuts). See Taskbar for the rest of the + menu, including \"Open in folder…\" and the configurable Start button label."}],"t":"p"}],"search":"opening windows from the + menu the + button at the left of the taskbar is how you create windows: left-click + to launch a terminal on your default host — the local broker unless you pick another in control panel → hosts. right-click + for the full profile / app menu. alongside the terminal profiles (grouped per broker when you have more than one host), it lists the app windows: the items appear in this order, which is the order the window kinds register in — not alphabetical: menu item · opens control panel — the control panel (settings) help — the in-app help guide task manager — a task manager file manager — a file manager text editor — a codemirror text editor sticky note — a sticky note scratchpad — broker-stored shared notes (from the scratchpad mod) session recorder — the session-recording library (play back recorded terminals) each row carries a small svg icon rather than an emoji. mods that ship off by default add their own rows once enabled — clipboard (clipboard history) and ai status among them — and a closed notes entry appears when there are sticky notes to restore. you can also open a terminal with the new terminal shortcut ( ctrl+alt+enter — see keyboard-shortcuts). see taskbar for the rest of the + menu, including \"open in folder…\" and the configurable start button label. keyboard shortcuts taskbar","title":"Opening windows from the + menu"},{"body":[{"spans":[{"t":"text","v":"A terminal runs a real shell on its broker host. The shell keeps running even when no browser is attached, so you can close the window, come back later, and the screen heals from a snapshot."}],"t":"p"},{"spans":[{"t":"text","v":"Each terminal title bar carries a per-window "},{"t":"strong","v":"robot button"},{"t":"text","v":" that sets that window's MCP (AI agent) access — off, read, or read-write. The robot icon briefly flashes when an agent reads the screen or types into the terminal. See MCP-and-AI-Agents for the full picture."}],"t":"p"},{"spans":[{"t":"text","v":"Two more chips can appear in a terminal's title bar, both ambient — neither is a button:"}],"t":"p"},{"spans":[{"t":"text","v":"a "},{"t":"strong","v":"git branch badge"},{"t":"text","v":" showing the branch that terminal's working directory is on, with a"}],"t":"bullet"},{"spans":[{"t":"text","v":"marker when the tree is dirty (from the Git mod);"}],"t":"p"},{"spans":[{"t":"text","v":"a "},{"t":"strong","v":"🖱 mouse-mode chip"},{"t":"text","v":", shown for exactly as long as a full-screen program owns the mouse"}],"t":"bullet"},{"spans":[{"t":"text","v":"("},{"t":"code","v":"lazygit"},{"t":"text","v":", "},{"t":"code","v":"btop"},{"t":"text","v":", "},{"t":"code","v":"mc"},{"t":"text","v":"). Its tooltip carries the escape gesture. It comes from the Mouse mode chip mod, which ships "},{"t":"strong","v":"enabled"},{"t":"text","v":" — see Keyboard-Shortcuts."}],"t":"p"},{"spans":[{"t":"text","v":"The title bar used to carry a "},{"t":"strong","v":"📋 Agent docs button"},{"t":"text","v":" that opened that folder's "},{"t":"code","v":"AGENTS.md"},{"t":"text","v":" / "},{"t":"code","v":"CLAUDE.md"},{"t":"text","v":". It has been "},{"t":"strong","v":"retired"},{"t":"text","v":": the button guessed which folder you meant from the terminal's detected working directory, and when that guess was wrong it opened — and, once you saved, wrote to — a different project's "},{"t":"code","v":"AGENTS.md"},{"t":"text","v":", with no way to tell from the window that it had. "},{"t":"code","v":"AGENTS.md"},{"t":"text","v":" and "},{"t":"code","v":"CLAUDE.md"},{"t":"text","v":" are ordinary files; open them with the "},{"t":"strong","v":"Text editor"},{"t":"text","v":" (Open) or the "},{"t":"strong","v":"File manager"},{"t":"text","v":" as you would any other. Agent-docs windows you already had open still restore and still save."}],"t":"p"},{"spans":[{"t":"text","v":"A new terminal from "},{"t":"kbd","v":"Ctrl+Alt+Enter"},{"t":"text","v":" always runs on the "},{"t":"strong","v":"local broker"},{"t":"text","v":"; a left-click on the "},{"t":"strong","v":"+"},{"t":"text","v":" button launches on your "},{"t":"strong","v":"default host"},{"t":"text","v":" — the local broker unless changed in Control Panel → Hosts. To start one on a specific host, pick its profile from the right-click "},{"t":"strong","v":"+"},{"t":"text","v":" menu. See Hosts-and-Multi-Browser for adding hosts and setting the default."}],"t":"p"},{"spans":[{"t":"strong","v":"Pasting images"},{"t":"text","v":": paste a screenshot (e.g. "},{"t":"kbd","v":"Win+Shift+S"},{"t":"text","v":") straight into a terminal with "},{"t":"kbd","v":"Ctrl+V"},{"t":"text","v":", "},{"t":"kbd","v":"Alt+V"},{"t":"text","v":", or a right-click. The image itself cannot cross into the remote shell's clipboard, so the desktop uploads it to that terminal's broker host and pastes the resulting "},{"t":"strong","v":"file path"},{"t":"text","v":" instead — AI agents like Claude Code read a pasted image path just like a drag-and-dropped file. Pasted images are kept for a few hours in the host's temp area and swept automatically. If the clipboard carries both text and an image, the text wins. "},{"t":"kbd","v":"Alt+V"},{"t":"text","v":" and right-click image capture need an https (or localhost) page; "},{"t":"kbd","v":"Ctrl+V"},{"t":"text","v":" image paste works everywhere."}],"t":"p"}],"search":"terminal a terminal runs a real shell on its broker host. the shell keeps running even when no browser is attached, so you can close the window, come back later, and the screen heals from a snapshot. each terminal title bar carries a per-window robot button that sets that window's mcp (ai agent) access — off, read, or read-write. the robot icon briefly flashes when an agent reads the screen or types into the terminal. see mcp-and-ai-agents for the full picture. two more chips can appear in a terminal's title bar, both ambient — neither is a button: a git branch badge showing the branch that terminal's working directory is on, with a marker when the tree is dirty (from the git mod); a 🖱 mouse-mode chip , shown for exactly as long as a full-screen program owns the mouse ( lazygit , btop , mc ). its tooltip carries the escape gesture. it comes from the mouse mode chip mod, which ships enabled — see keyboard-shortcuts. the title bar used to carry a 📋 agent docs button that opened that folder's agents.md / claude.md . it has been retired : the button guessed which folder you meant from the terminal's detected working directory, and when that guess was wrong it opened — and, once you saved, wrote to — a different project's agents.md , with no way to tell from the window that it had. agents.md and claude.md are ordinary files; open them with the text editor (open) or the file manager as you would any other. agent-docs windows you already had open still restore and still save. a new terminal from ctrl+alt+enter always runs on the local broker ; a left-click on the + button launches on your default host — the local broker unless changed in control panel → hosts. to start one on a specific host, pick its profile from the right-click + menu. see hosts-and-multi-browser for adding hosts and setting the default. pasting images : paste a screenshot (e.g. win+shift+s ) straight into a terminal with ctrl+v , alt+v , or a right-click. the image itself cannot cross into the remote shell's clipboard, so the desktop uploads it to that terminal's broker host and pastes the resulting file path instead — ai agents like claude code read a pasted image path just like a drag-and-dropped file. pasted images are kept for a few hours in the host's temp area and swept automatically. if the clipboard carries both text and an image, the text wins. alt+v and right-click image capture need an https (or localhost) page; ctrl+v image paste works everywhere. mcp and ai agents keyboard shortcuts hosts and multi browser","title":"Terminal"},{"body":[{"spans":[{"t":"text","v":"Every terminal title bar carries a "},{"t":"strong","v":"⏺ record button"},{"t":"text","v":": click to record that session, click again to stop and save it to the broker. The "},{"t":"strong","v":"Session recorder"},{"t":"text","v":" window from the "},{"t":"strong","v":"+"},{"t":"text","v":" menu lists saved recordings and plays them back in a window fixed at the original recorded size, with pause, speed, continuous reverse, and timestamped notes."}],"t":"p"},{"spans":[{"t":"strong","v":"Control Panel → Mods → Session recorder → auto-record every session"},{"t":"text","v":" (off by default) records every terminal as it opens, so nothing is missed for want of pressing ⏺ first. Switching it off again stops the recordings it started; a recording you began by hand keeps running. A long recording rolls over into numbered parts at the 50 MB size cap rather than stopping, and with auto-record on a page reload no longer warns before discarding the segment in progress. Recordings are never swept, so leaving it on grows the broker's "},{"t":"code","v":"webterm_recordings/"},{"t":"text","v":" directory until you delete from the library. See the "},{"t":"strong","v":"Session recorder"},{"t":"text","v":" section of the in-app help for details."}],"t":"p"}],"search":"session recorder every terminal title bar carries a ⏺ record button : click to record that session, click again to stop and save it to the broker. the session recorder window from the + menu lists saved recordings and plays them back in a window fixed at the original recorded size, with pause, speed, continuous reverse, and timestamped notes. control panel → mods → session recorder → auto-record every session (off by default) records every terminal as it opens, so nothing is missed for want of pressing ⏺ first. switching it off again stops the recordings it started; a recording you began by hand keeps running. a long recording rolls over into numbered parts at the 50 mb size cap rather than stopping, and with auto-record on a page reload no longer warns before discarding the segment in progress. recordings are never swept, so leaving it on grows the broker's webterm_recordings/ directory until you delete from the library. see the session recorder section of the in-app help for details.","title":"Session recorder"},{"body":[{"spans":[{"t":"text","v":"The Scratchpad is a single-instance window of tabbed notes stored on the broker, so it is shared across every browser connected to that host. Notes autosave as you type, and each tab keeps a browsable revision history. It comes from the default-on Scratchpad mod and requires the Text editor mod — disable either under Control Panel → Mods and the "},{"t":"code","v":"Scratchpad"},{"t":"text","v":" item disappears from the "},{"t":"strong","v":"+"},{"t":"text","v":" menu."}],"t":"p"}],"search":"scratchpad the scratchpad is a single-instance window of tabbed notes stored on the broker, so it is shared across every browser connected to that host. notes autosave as you type, and each tab keeps a browsable revision history. it comes from the default-on scratchpad mod and requires the text editor mod — disable either under control panel → mods and the scratchpad item disappears from the + menu.","title":"Scratchpad"},{"body":[{"spans":[{"t":"text","v":"The Control Panel is where you configure the desktop — appearance, window mode, drag-hold delay, hosts, MCP access, keyboard shortcuts, and more, with a tab per connected broker. It opens as a "},{"t":"strong","v":"moveable floating window"},{"t":"text","v":": drag its title bar to move it, drag its edges to resize, minimize it to the taskbar, or tile it like any other window. Open it from the "},{"t":"strong","v":"+"},{"t":"text","v":" menu's "},{"t":"strong","v":"Control panel"},{"t":"text","v":" item, the desktop / taskbar right-click menu (which does label it "},{"t":"strong","v":"🎛 Control panel"},{"t":"text","v":"), or the "},{"t":"strong","v":"Open control panel"},{"t":"text","v":" shortcut ("},{"t":"kbd","v":"Ctrl+Alt+p"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"text","v":"Inside a broker's tab the settings are grouped into "},{"t":"strong","v":"applets"},{"t":"text","v":" — Desktop, Windows, Input, Terminals, Startup, Access, Mods, Advanced — so the panel opens on a grid of captioned icons rather than one long scroll, and you pick a topic first. A filter box above the grid finds a setting by name, and a "},{"t":"strong","v":"Show everything"},{"t":"text","v":" toggle drops back to the flat list. See Getting-Started for what each applet holds."}],"t":"p"},{"spans":[{"t":"text","v":"Like the file manager and the task manager, the Control Panel is "},{"t":"strong","v":"ephemeral"},{"t":"text","v":": it edits settings that persist on their own (per-browser, or per-host via the broker), so the window itself has nothing to save — closing it just dismisses it, and it has no Delete. See Getting-Started for what the settings cover."}],"t":"p"}],"search":"control panel the control panel is where you configure the desktop — appearance, window mode, drag-hold delay, hosts, mcp access, keyboard shortcuts, and more, with a tab per connected broker. it opens as a moveable floating window : drag its title bar to move it, drag its edges to resize, minimize it to the taskbar, or tile it like any other window. open it from the + menu's control panel item, the desktop / taskbar right-click menu (which does label it 🎛 control panel ), or the open control panel shortcut ( ctrl+alt+p ). inside a broker's tab the settings are grouped into applets — desktop, windows, input, terminals, startup, access, mods, advanced — so the panel opens on a grid of captioned icons rather than one long scroll, and you pick a topic first. a filter box above the grid finds a setting by name, and a show everything toggle drops back to the flat list. see getting-started for what each applet holds. like the file manager and the task manager, the control panel is ephemeral : it edits settings that persist on their own (per-browser, or per-host via the broker), so the window itself has nothing to save — closing it just dismisses it, and it has no delete. see getting-started for what the settings cover. getting started getting started","title":"Control Panel"},{"body":[{"spans":[{"t":"text","v":"How a window goes away depends on its type:"}],"t":"p"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" is soft, but what it preserves varies. A terminal's shell keeps running so you can reattach later. A non-empty sticky note is retained and reopens from "},{"t":"strong","v":"Closed notes"},{"t":"text","v":" in the "},{"t":"strong","v":"+"},{"t":"text","v":" menu (an empty note is discarded). A text editor's content lives in a file on the host, so closing it (after the save prompt) leaves your file intact. A file manager, the task manager, and the Control Panel/help are ephemeral — closing them just dismisses them."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Terminate"},{"t":"text","v":" (terminals only) hard-kills the shell process tree."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Delete"},{"t":"text","v":" (on note and editor windows — not the file manager, task manager, Control Panel, or help — shown as Delete note / Delete file) permanently discards that window and its stored document."}],"t":"bullet"},{"spans":[{"t":"text","v":"These actions live on the title-bar right-click menu. For the full semantics — plus minimize, pin, recolor, rename, and arrange-all — see Floating-Window-Controls."}],"t":"p"}],"search":"closing, terminating, and deleting how a window goes away depends on its type: close is soft, but what it preserves varies. a terminal's shell keeps running so you can reattach later. a non-empty sticky note is retained and reopens from closed notes in the + menu (an empty note is discarded). a text editor's content lives in a file on the host, so closing it (after the save prompt) leaves your file intact. a file manager, the task manager, and the control panel/help are ephemeral — closing them just dismisses them. terminate (terminals only) hard-kills the shell process tree. delete (on note and editor windows — not the file manager, task manager, control panel, or help — shown as delete note / delete file) permanently discards that window and its stored document. these actions live on the title-bar right-click menu. for the full semantics — plus minimize, pin, recolor, rename, and arrange-all — see floating-window-controls. floating window controls","title":"Closing, terminating, and deleting"}],"label":"Window Types","order":12,"slug":"window-types"},{"cards":[{"body":[{"spans":[{"t":"text","v":"One Browserland desktop can attach to more than one broker at a time, so you can drive terminals on several machines from a single browser tab. This page covers adding remote hosts, reading their status chips, and the single-active-browser lease that keeps two browsers from fighting over the same layout."}],"t":"p"}],"search":"overview one browserland desktop can attach to more than one broker at a time, so you can drive terminals on several machines from a single browser tab. this page covers adding remote hosts, reading their status chips, and the single-active-browser lease that keeps two browsers from fighting over the same layout.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"By default the UI talks to the broker it was served from ("},{"t":"code","v":"http://127.0.0.1:4445/"},{"t":"text","v":"). To attach another broker, open "},{"t":"strong","v":"Control Panel → Hosts"},{"t":"text","v":" and add it:"}],"t":"p"},{"spans":[{"t":"text","v":"Enter a "},{"t":"strong","v":"label"},{"t":"text","v":" (how the host appears in the UI)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Enter the broker "},{"t":"strong","v":"URL"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"Enter the broker's "},{"t":"strong","v":"password"},{"t":"text","v":" — the broker's auth token, which doubles as its browser login. Every broker has one: either the "},{"t":"code","v":"auth_token"},{"t":"text","v":" its owner configured, or one it minted for itself on first run. Print it on that machine with "},{"t":"code","v":"python -m webterm.broker --print-token"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Serve remote brokers over https"},{"t":"text","v":" — the easy way is ["},{"t":"code","v":"tailscale serve"},{"t":"text","v":"](https://tailscale.com/kb/1312/serve) on each machine (real TLS inside your tailnet, no certificates to manage, the broker stays bound to loopback), or any https reverse proxy of your own. Two reasons: a browser blocks an "},{"t":"strong","v":"https page from talking to an http host"},{"t":"text","v":" (mixed content), so the cockpit page and every added host must share one scheme — all https or all plain http, never a mix; and only an https (or localhost) page is a secure context, which clipboard "},{"t":"strong","v":"image paste"},{"t":"text","v":" into a terminal ("},{"t":"kbd","v":"Alt+V"},{"t":"text","v":" / right-click) requires. All-plain-http over a trusted tailnet works too, minus those features."}],"t":"p"},{"spans":[{"t":"text","v":"Each host you add gets its own settings tab in the Control Panel. Settings like window mode, drag hold delay, MCP, keyboard shortcuts, the default terminal profile, and the default start path are stored "},{"t":"strong","v":"per host"},{"t":"text","v":", so they can differ from broker to broker. A separate set of "},{"t":"strong","v":"browser-global"},{"t":"text","v":" settings (theme and background, terminal font, the start-button label, restore-on-refresh, when the broker status chip shows, the taskbar workspace filter, whether the workspace pager shows, and the clock chip's time zone) belong to the browser you are sitting at and are shared across every host. For more on opening the Control Panel and the rest of its tabs, see Getting-Started."}],"t":"p"},{"spans":[{"t":"text","v":"One per-host setting is stored differently from all the others: "},{"t":"strong","v":"Clipboard (OSC 52)"},{"t":"text","v":", which decides whether programs running on that host may set your clipboard. It is off by default, it is authorised for one host at a time, and unlike every setting above it, it is kept in "},{"t":"strong","v":"your browser only"},{"t":"text","v":" and never written to the broker. The reason is that it is precisely the switch that grants a broker access to the computer you are sitting at, so a copy of it living on that broker would let the broker turn it on for itself. Trusting your own laptop's broker therefore does not extend to a remote box you attached to, and enabling it in one browser does not enable it in another. See Keyboard-Shortcuts for what a program can and cannot do once it is on."}],"t":"p"},{"spans":[{"t":"text","v":"The password you enter is the broker's browser-login token. The bearer token AI agents use to drive terminals over MCP is a "},{"t":"strong","v":"separate"},{"t":"text","v":" secret, configured on its own — if you plan to let agents work on this host, see MCP-and-AI-Agents."}],"t":"p"}],"search":"add a remote host by default the ui talks to the broker it was served from ( http://127.0.0.1:4445/ ). to attach another broker, open control panel → hosts and add it: enter a label (how the host appears in the ui). enter the broker url . enter the broker's password — the broker's auth token, which doubles as its browser login. every broker has one: either the auth_token its owner configured, or one it minted for itself on first run. print it on that machine with python -m webterm.broker --print-token . serve remote brokers over https — the easy way is [ tailscale serve ](https://tailscale.com/kb/1312/serve) on each machine (real tls inside your tailnet, no certificates to manage, the broker stays bound to loopback), or any https reverse proxy of your own. two reasons: a browser blocks an https page from talking to an http host (mixed content), so the cockpit page and every added host must share one scheme — all https or all plain http, never a mix; and only an https (or localhost) page is a secure context, which clipboard image paste into a terminal ( alt+v / right-click) requires. all-plain-http over a trusted tailnet works too, minus those features. each host you add gets its own settings tab in the control panel. settings like window mode, drag hold delay, mcp, keyboard shortcuts, the default terminal profile, and the default start path are stored per host , so they can differ from broker to broker. a separate set of browser-global settings (theme and background, terminal font, the start-button label, restore-on-refresh, when the broker status chip shows, the taskbar workspace filter, whether the workspace pager shows, and the clock chip's time zone) belong to the browser you are sitting at and are shared across every host. for more on opening the control panel and the rest of its tabs, see getting-started. one per-host setting is stored differently from all the others: clipboard (osc 52) , which decides whether programs running on that host may set your clipboard. it is off by default, it is authorised for one host at a time, and unlike every setting above it, it is kept in your browser only and never written to the broker. the reason is that it is precisely the switch that grants a broker access to the computer you are sitting at, so a copy of it living on that broker would let the broker turn it on for itself. trusting your own laptop's broker therefore does not extend to a remote box you attached to, and enabling it in one browser does not enable it in another. see keyboard-shortcuts for what a program can and cannot do once it is on. the password you enter is the broker's browser-login token. the bearer token ai agents use to drive terminals over mcp is a separate secret, configured on its own — if you plan to let agents work on this host, see mcp-and-ai-agents. getting started keyboard shortcuts mcp and ai agents","title":"Add a remote host"},{"body":[{"spans":[{"t":"text","v":"Your host list normally lives only in the browser you added it in: open the desktop from a different browser and you start with an empty list, and clearing local storage to fix a UI glitch also wipes your remote brokers. The optional "},{"t":"strong","v":"Broker registry"},{"t":"text","v":" fixes that by storing a copy of the list on a broker, so you can pull it back somewhere else. It is a convenience layer, not a change to how connections work — your host list stays browser-local and remains the source of truth, and the desktop always connects "},{"t":"strong","v":"directly"},{"t":"text","v":" to each broker. The broker only stores the list; it never proxies your traffic."}],"t":"p"},{"spans":[{"t":"text","v":"Find it in "},{"t":"strong","v":"Control Panel → Browser → Broker registry"},{"t":"text","v":", just below the Hosts list."}],"t":"p"},{"spans":[{"t":"strong","v":"Publish"},{"t":"text","v":" saves this browser's list to the broker. You pick which hosts to include and set the absolute address other machines use to reach this broker (a loopback address like "},{"t":"code","v":"localhost"},{"t":"text","v":" / "},{"t":"code","v":"127.x"},{"t":"text","v":" can't be shared, so the local broker is skipped when its address is loopback). Optionally publish to "},{"t":"strong","v":"every configured broker"},{"t":"text","v":" at once, so opening the desktop from any of them recovers the full list; each broker is reported separately. Publishing requires this browser to hold the target broker's active-writer lease."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Pull"},{"t":"text","v":" merges the registries published to "},{"t":"strong","v":"every broker you have configured"},{"t":"text","v":", not just this one — each row shows which broker it came from, and a row whose sources "},{"t":"strong","v":"disagree"},{"t":"text","v":" is flagged so you can see the conflict rather than silently taking one side. New hosts are checked by default; a host you already have that differs is unchecked (your local copy wins unless you tick it), and an identical one is greyed out. Matching is by the broker's verified identity, then its address, so pulling an updated list updates the hosts you already have instead of duplicating them, and it never touches your "},{"t":"strong","v":"this broker"},{"t":"text","v":" entry or your default-host choice. If you apply a host whose URL changed, its saved password is cleared (never carried across to a different address), so you may need to re-enter it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Passwords are not shared unless you ask."},{"t":"text","v":" The "},{"t":"strong","v":"Include passwords"},{"t":"text","v":" box is off by default and warns you: publishing a broker's token lets anyone who can read that registry log into every included broker — a lateral-movement path between your machines. Only include passwords when the broker holding the registry is as trusted as the brokers whose tokens it carries. "},{"t":"strong","v":"Forget passwords"},{"t":"text","v":" removes every token from the registry and its saved history, but it does "},{"t":"strong","v":"not"},{"t":"text","v":" revoke access — a browser that already pulled a token keeps it, and the token stays valid until you rotate it ("},{"t":"code","v":"--print-token"},{"t":"text","v":" shows the current one). If a token was ever exposed, rotate it."}],"t":"p"}],"search":"sharing your broker list your host list normally lives only in the browser you added it in: open the desktop from a different browser and you start with an empty list, and clearing local storage to fix a ui glitch also wipes your remote brokers. the optional broker registry fixes that by storing a copy of the list on a broker, so you can pull it back somewhere else. it is a convenience layer, not a change to how connections work — your host list stays browser-local and remains the source of truth, and the desktop always connects directly to each broker. the broker only stores the list; it never proxies your traffic. find it in control panel → browser → broker registry , just below the hosts list. publish saves this browser's list to the broker. you pick which hosts to include and set the absolute address other machines use to reach this broker (a loopback address like localhost / 127.x can't be shared, so the local broker is skipped when its address is loopback). optionally publish to every configured broker at once, so opening the desktop from any of them recovers the full list; each broker is reported separately. publishing requires this browser to hold the target broker's active-writer lease. pull merges the registries published to every broker you have configured , not just this one — each row shows which broker it came from, and a row whose sources disagree is flagged so you can see the conflict rather than silently taking one side. new hosts are checked by default; a host you already have that differs is unchecked (your local copy wins unless you tick it), and an identical one is greyed out. matching is by the broker's verified identity, then its address, so pulling an updated list updates the hosts you already have instead of duplicating them, and it never touches your this broker entry or your default-host choice. if you apply a host whose url changed, its saved password is cleared (never carried across to a different address), so you may need to re-enter it. passwords are not shared unless you ask. the include passwords box is off by default and warns you: publishing a broker's token lets anyone who can read that registry log into every included broker — a lateral-movement path between your machines. only include passwords when the broker holding the registry is as trusted as the brokers whose tokens it carries. forget passwords removes every token from the registry and its saved history, but it does not revoke access — a browser that already pulled a token keeps it, and the token stays valid until you rotate it ( --print-token shows the current one). if a token was ever exposed, rotate it.","title":"Sharing your broker list"},{"body":[{"spans":[{"t":"text","v":"Which mods run is normally "},{"t":"strong","v":"your browser's"},{"t":"text","v":" choice: "},{"t":"strong","v":"Control Panel → this broker → Mods"},{"t":"text","v":" lists everything installed and you tick what you want. The pane also does more than tick boxes now: "},{"t":"strong","v":"Install a mod…"},{"t":"text","v":" and "},{"t":"strong","v":"Uninstall"},{"t":"text","v":", a badge marking each row shipped or installed, and a preview of an install with its advisory warnings — see Installing-Mods. The enable choice itself and that choice stays in the browser you made it in. On a setup with several brokers that means the same decision has to be made again in every browser you sit at."}],"t":"p"},{"spans":[{"t":"text","v":"Each host tab therefore also has a "},{"t":"strong","v":"Mods on this broker"},{"t":"text","v":" section — including the tab of a remote broker. It lists the mods "},{"t":"strong","v":"that"},{"t":"text","v":" broker serves, each with three states:"}],"t":"p"},{"spans":[{"t":"strong","v":"Default"},{"t":"text","v":" — that broker says nothing, so each browser's own Mods list decides. The option says which way \"default\" falls for that mod ("},{"t":"code","v":"Default (on)"},{"t":"text","v":" / "},{"t":"code","v":"Default (off)"},{"t":"text","v":"), because a few mods ship off."}],"t":"bullet"},{"spans":[{"t":"strong","v":"On"},{"t":"text","v":" / "},{"t":"strong","v":"Off"},{"t":"text","v":" — that broker pins the mod. Every browser that loads its page gets that answer, and the mod's checkbox in their Mods list is locked and labelled "},{"t":"code","v":"pinned on"},{"t":"text","v":" / "},{"t":"code","v":"pinned off"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"A few things worth knowing:"}],"t":"p"},{"spans":[{"t":"strong","v":"It applies at page load, not now."},{"t":"text","v":" A pin is what that broker hands to the next browser that loads its page — including your own tab if you pinned something on "},{"t":"strong","v":"this broker"},{"t":"text","v":", so a change here is not live in the session you make it in."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Pinning on is a real switch, not a suggestion."},{"t":"text","v":" It turns the mod on for everyone on that broker, including mods that ship off by default (the clipboard history, for instance). Pin one on only if you want it running in every browser that broker serves."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Pinning OFF is a real switch too."},{"t":"text","v":" Most mods ship on, including "},{"t":"strong","v":"Workspaces"},{"t":"text","v":" (see Workspaces) — pinning that one off gives every browser on the broker a single desktop showing every tiled column at once. Nothing is lost by it: pin it back on and the workspaces return."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Pinning a mod on also pins what it needs."},{"t":"text","v":" The scratchpad needs the text editor, so pinning the scratchpad on shows the editor as "},{"t":"code","v":"on — required by scratchpad"},{"t":"text","v":". If you pin a dependency "},{"t":"strong","v":"off"},{"t":"text","v":" explicitly, that wins: the mod that needs it stays off and shows as blocked in the Mods list."}],"t":"bullet"},{"spans":[{"t":"strong","v":"A pin lives on the broker"},{"t":"text","v":", not in your browser, and not in the settings that sync between your browsers. It survives a restart, and it can be changed even while somebody else's browser is the active view on that broker — unlike the other settings on a host tab, which that broker only accepts from its active browser."}],"t":"bullet"},{"spans":[{"t":"strong","v":"A mod that broker doesn't have"},{"t":"text","v":" still shows if it is pinned there (as "},{"t":"code","v":"not installed on this broker"},{"t":"text","v":"), so a leftover pin is visible and removable rather than silently governing nothing."}],"t":"bullet"},{"spans":[{"t":"text","v":"If a broker can't answer, the section says why instead of showing an empty list: it may be "},{"t":"strong","v":"running an older build"},{"t":"text","v":" that can't report its mods (update it to manage them from here), "},{"t":"strong","v":"serving no desktop page"},{"t":"text","v":" at all (a headless broker has no mods to pin), "},{"t":"strong","v":"unreachable"},{"t":"text","v":", or "},{"t":"strong","v":"refusing your password"},{"t":"text","v":". Select the tab again to retry."}],"t":"p"},{"spans":[{"t":"text","v":"The broker-wide "},{"t":"strong","v":"master switch"},{"t":"text","v":" still outranks all of this: a broker started with "},{"t":"code","v":"mods_enabled: false"},{"t":"text","v":" runs no mods at all, and its section says so."}],"t":"p"}],"search":"mods on a broker which mods run is normally your browser's choice: control panel → this broker → mods lists everything installed and you tick what you want. the pane also does more than tick boxes now: install a mod… and uninstall , a badge marking each row shipped or installed, and a preview of an install with its advisory warnings — see installing-mods. the enable choice itself and that choice stays in the browser you made it in. on a setup with several brokers that means the same decision has to be made again in every browser you sit at. each host tab therefore also has a mods on this broker section — including the tab of a remote broker. it lists the mods that broker serves, each with three states: default — that broker says nothing, so each browser's own mods list decides. the option says which way \"default\" falls for that mod ( default (on) / default (off) ), because a few mods ship off. on / off — that broker pins the mod. every browser that loads its page gets that answer, and the mod's checkbox in their mods list is locked and labelled pinned on / pinned off . a few things worth knowing: it applies at page load, not now. a pin is what that broker hands to the next browser that loads its page — including your own tab if you pinned something on this broker , so a change here is not live in the session you make it in. pinning on is a real switch, not a suggestion. it turns the mod on for everyone on that broker, including mods that ship off by default (the clipboard history, for instance). pin one on only if you want it running in every browser that broker serves. pinning off is a real switch too. most mods ship on, including workspaces (see workspaces) — pinning that one off gives every browser on the broker a single desktop showing every tiled column at once. nothing is lost by it: pin it back on and the workspaces return. pinning a mod on also pins what it needs. the scratchpad needs the text editor, so pinning the scratchpad on shows the editor as on — required by scratchpad . if you pin a dependency off explicitly, that wins: the mod that needs it stays off and shows as blocked in the mods list. a pin lives on the broker , not in your browser, and not in the settings that sync between your browsers. it survives a restart, and it can be changed even while somebody else's browser is the active view on that broker — unlike the other settings on a host tab, which that broker only accepts from its active browser. a mod that broker doesn't have still shows if it is pinned there (as not installed on this broker ), so a leftover pin is visible and removable rather than silently governing nothing. if a broker can't answer, the section says why instead of showing an empty list: it may be running an older build that can't report its mods (update it to manage them from here), serving no desktop page at all (a headless broker has no mods to pin), unreachable , or refusing your password . select the tab again to retry. the broker-wide master switch still outranks all of this: a broker started with mods_enabled: false runs no mods at all, and its section says so. installing mods workspaces","title":"Mods on a broker"},{"body":[{"spans":[{"t":"text","v":"Setting the same mods up by hand on every machine is what "},{"t":"strong","v":"Control Panel → Browser → Sync mods"},{"t":"text","v":" is for. It copies which mods are on, plus the settings those mods own, to the brokers you pick. It never sends mod "},{"t":"strong","v":"code"},{"t":"text","v":" — a broker can only be told about mods it already serves, and one it doesn't have is reported rather than quietly skipped."}],"t":"p"},{"spans":[{"t":"text","v":"The two directions are deliberately not mirror images, because only one kind of per-mod state on another machine can be written from here at all:"}],"t":"p"},{"spans":[{"t":"strong","v":"Push to brokers…"},{"t":"text","v":" writes each target's own "},{"t":"strong","v":"mod policy"},{"t":"text","v":" (the pins above) and its "},{"t":"strong","v":"mod settings"},{"t":"text","v":". Pins are the only per-mod state on another machine that is remotely writable, and they are what that broker hands every browser that loads it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Adopt from a broker…"},{"t":"text","v":" changes "},{"t":"strong","v":"this browser's"},{"t":"text","v":" own Mods choices and this broker's mod settings. It pins nothing here — a local pin would lock this browser's own checkboxes, and since pins are read once at page load it wouldn't even apply until you reloaded."}],"t":"bullet"},{"spans":[{"t":"text","v":"So pushing configures a machine; adopting configures you."}],"t":"p"},{"spans":[{"t":"text","v":"Pins are kept to a minimum"}],"t":"sub"},{"spans":[{"t":"text","v":"By default a pin is written "},{"t":"strong","v":"only where that broker's own default would land somewhere else"},{"t":"text","v":", and a pin it no longer needs is "},{"t":"strong","v":"cleared"},{"t":"text","v":". Syncing two brokers that already agree therefore locks nothing, and pushing repeatedly doesn't pile up pins. Ticking "},{"t":"strong","v":"Lock every mod on the target"},{"t":"text","v":" instead pins every shared mod explicitly — that locks those checkboxes for everyone on that broker, and it is the only way to also override a choice a browser over there made just for itself."}],"t":"p"},{"spans":[{"t":"text","v":"As everywhere else with pins: what you push applies the next time a browser "},{"t":"strong","v":"loads"},{"t":"text","v":" that broker's page, not to a session already open there. Mod settings are shared state, so its browsers pick those up as they poll."}],"t":"p"},{"spans":[{"t":"text","v":"Nothing is written before you have seen it"}],"t":"sub"},{"spans":[{"t":"text","v":"Selecting brokers only opens a "},{"t":"strong","v":"preview"},{"t":"text","v":", which names — per broker — every pin it will create or clear, every setting that will change with its old and new value, and everything being left alone and why. Brokers that already match say so and are unticked."}],"t":"p"},{"spans":[{"t":"text","v":"Afterwards each broker keeps its own result row: a push is never all-or-nothing. "},{"t":"strong","v":"Retry"},{"t":"text","v":" rebuilds the whole plan against that broker as it is now rather than replaying a stale one, and "},{"t":"strong","v":"Undo pins"},{"t":"text","v":" puts back the pins that broker had before that push (settings are not undone — their old values were that broker's, and re-asserting a whole blob is exactly the overwrite this avoids)."}],"t":"p"},{"spans":[{"t":"text","v":"A broker whose mods can't be listed is "},{"t":"strong","v":"refused"},{"t":"text","v":" rather than written to blind, since without its mod list there's no way to tell which settings it has any use for: an "},{"t":"strong","v":"older build"},{"t":"text","v":" that can't report its mods (update it first), one "},{"t":"strong","v":"serving no desktop page"},{"t":"text","v":", one "},{"t":"strong","v":"unreachable"},{"t":"text","v":", or one "},{"t":"strong","v":"refusing your password"},{"t":"text","v":". A broker whose "},{"t":"strong","v":"master mod switch is off"},{"t":"text","v":" takes the settings but no pins, because a pin made then is inert and awkward to clear later. A host you have no password for is shown but not selectable."}],"t":"p"},{"spans":[{"t":"text","v":"One limit worth knowing: only settings owned by a mod that is "},{"t":"strong","v":"on"},{"t":"text","v":" in this browser travel, because a mod's settings control only exists while the mod is running. That's consistent — a mod that's off here is being turned off there too — but it means a value a disabled mod deliberately keeps (the terminal-font mod preserves your font so re-enabling restores it) stays behind. Turn the mod on, push, then turn it off again if you want that value to travel."}],"t":"p"}],"search":"copying a mod setup between brokers setting the same mods up by hand on every machine is what control panel → browser → sync mods is for. it copies which mods are on, plus the settings those mods own, to the brokers you pick. it never sends mod code — a broker can only be told about mods it already serves, and one it doesn't have is reported rather than quietly skipped. the two directions are deliberately not mirror images, because only one kind of per-mod state on another machine can be written from here at all: push to brokers… writes each target's own mod policy (the pins above) and its mod settings . pins are the only per-mod state on another machine that is remotely writable, and they are what that broker hands every browser that loads it. adopt from a broker… changes this browser's own mods choices and this broker's mod settings. it pins nothing here — a local pin would lock this browser's own checkboxes, and since pins are read once at page load it wouldn't even apply until you reloaded. so pushing configures a machine; adopting configures you. pins are kept to a minimum by default a pin is written only where that broker's own default would land somewhere else , and a pin it no longer needs is cleared . syncing two brokers that already agree therefore locks nothing, and pushing repeatedly doesn't pile up pins. ticking lock every mod on the target instead pins every shared mod explicitly — that locks those checkboxes for everyone on that broker, and it is the only way to also override a choice a browser over there made just for itself. as everywhere else with pins: what you push applies the next time a browser loads that broker's page, not to a session already open there. mod settings are shared state, so its browsers pick those up as they poll. nothing is written before you have seen it selecting brokers only opens a preview , which names — per broker — every pin it will create or clear, every setting that will change with its old and new value, and everything being left alone and why. brokers that already match say so and are unticked. afterwards each broker keeps its own result row: a push is never all-or-nothing. retry rebuilds the whole plan against that broker as it is now rather than replaying a stale one, and undo pins puts back the pins that broker had before that push (settings are not undone — their old values were that broker's, and re-asserting a whole blob is exactly the overwrite this avoids). a broker whose mods can't be listed is refused rather than written to blind, since without its mod list there's no way to tell which settings it has any use for: an older build that can't report its mods (update it first), one serving no desktop page , one unreachable , or one refusing your password . a broker whose master mod switch is off takes the settings but no pins, because a pin made then is inert and awkward to clear later. a host you have no password for is shown but not selectable. one limit worth knowing: only settings owned by a mod that is on in this browser travel, because a mod's settings control only exists while the mod is running. that's consistent — a mod that's off here is being turned off there too — but it means a value a disabled mod deliberately keeps (the terminal-font mod preserves your font so re-enabling restores it) stays behind. turn the mod on, push, then turn it off again if you want that value to travel.","title":"Copying a mod setup between brokers"},{"body":[{"spans":[{"t":"text","v":"Each host — including the local "},{"t":"strong","v":"this broker"},{"t":"text","v":" — can carry an optional "},{"t":"strong","v":"default terminal color"},{"t":"text","v":". Set it in "},{"t":"strong","v":"Control Panel → Hosts"},{"t":"text","v":" with the color dot on that host's row (the same swatch picker used in a window's title bar). When set, every "},{"t":"strong","v":"new"},{"t":"text","v":" terminal launched on that host starts in that color instead of the automatic palette pick, so you can tell at a glance which broker a window belongs to. The host's status chip also gets a thicker border in that color."}],"t":"p"},{"spans":[{"t":"text","v":"The default is only a starting color: recoloring an individual window with its own title-bar picker still wins and sticks, and clearing the host default (the "},{"t":"strong","v":"✕"},{"t":"text","v":" next to the dot) reverts new terminals to the automatic per-window colors. Like a host's password, the default color is stored in this browser only and is not shared with your other browsers."}],"t":"p"}],"search":"default color per host each host — including the local this broker — can carry an optional default terminal color . set it in control panel → hosts with the color dot on that host's row (the same swatch picker used in a window's title bar). when set, every new terminal launched on that host starts in that color instead of the automatic palette pick, so you can tell at a glance which broker a window belongs to. the host's status chip also gets a thicker border in that color. the default is only a starting color: recoloring an individual window with its own title-bar picker still wins and sticks, and clearing the host default (the ✕ next to the dot) reverts new terminals to the automatic per-window colors. like a host's password, the default color is stored in this browser only and is not shared with your other browsers.","title":"Default color per host"},{"body":[{"spans":[{"t":"text","v":"A launch "},{"t":"strong","v":"profile"},{"t":"text","v":" can also carry its own optional "},{"t":"strong","v":"default terminal color"},{"t":"text","v":", set in "},{"t":"strong","v":"Control Panel → (a host tab) → Terminals → Launch profiles"},{"t":"text","v":" with the color dot on that profile's row. When set, every "},{"t":"strong","v":"new"},{"t":"text","v":" terminal launched from that profile starts in that color — useful when the meaningful distinction is the profile rather than the host (say "},{"t":"code","v":"prod-ssh"},{"t":"text","v":" always red, "},{"t":"code","v":"scratch"},{"t":"text","v":" always green), regardless of which host runs it."}],"t":"p"},{"spans":[{"t":"text","v":"The profile color sits between the per-window and per-host colors in the order of precedence: a window you have recolored by hand keeps its own color; otherwise the launch profile's color wins; failing that the host's default color; and finally the automatic palette pick. Clearing it (the "},{"t":"strong","v":"✕"},{"t":"text","v":" next to the dot) drops back to the host/auto colors. Unlike the per-host default (stored in your browser only), the profile color lives in the broker's profile definition, so it is shared with every browser and viewer of that broker."}],"t":"p"}],"search":"default color per profile a launch profile can also carry its own optional default terminal color , set in control panel → (a host tab) → terminals → launch profiles with the color dot on that profile's row. when set, every new terminal launched from that profile starts in that color — useful when the meaningful distinction is the profile rather than the host (say prod-ssh always red, scratch always green), regardless of which host runs it. the profile color sits between the per-window and per-host colors in the order of precedence: a window you have recolored by hand keeps its own color; otherwise the launch profile's color wins; failing that the host's default color; and finally the automatic palette pick. clearing it (the ✕ next to the dot) drops back to the host/auto colors. unlike the per-host default (stored in your browser only), the profile color lives in the broker's profile definition, so it is shared with every browser and viewer of that broker.","title":"Default color per profile"},{"body":[{"spans":[{"t":"text","v":"A quick-launch of the START ("},{"t":"strong","v":"+"},{"t":"text","v":") button — a plain left-click by default — launches a terminal on your "},{"t":"strong","v":"default host"},{"t":"text","v":", using that host's own default terminal profile. Pick which host that is in "},{"t":"strong","v":"Control Panel → Hosts"},{"t":"text","v":" with the "},{"t":"strong","v":"Default"},{"t":"text","v":" button on that host's row; the current default is marked with a "},{"t":"strong","v":"default"},{"t":"text","v":" badge and its own button is disabled. The local "},{"t":"strong","v":"this broker"},{"t":"text","v":" is selectable too and is the default when you have not chosen one — so leaving it unset behaves exactly as before. (The button's picker menu still lets you launch on any host regardless of this setting.)"}],"t":"p"},{"spans":[{"t":"text","v":"If you delete the host that was your START default, it falls back to "},{"t":"strong","v":"this broker"},{"t":"text","v":" automatically, so the button keeps working. When the chosen host needs a password, quick-launching START surfaces its login prompt just like opening the host directly. Note that remote host identities are specific to the browser where you added them, so a non-local START default is only meaningful in that browser; other browsers fall back to launching locally."}],"t":"p"}],"search":"default host for the start (+) button a quick-launch of the start ( + ) button — a plain left-click by default — launches a terminal on your default host , using that host's own default terminal profile. pick which host that is in control panel → hosts with the default button on that host's row; the current default is marked with a default badge and its own button is disabled. the local this broker is selectable too and is the default when you have not chosen one — so leaving it unset behaves exactly as before. (the button's picker menu still lets you launch on any host regardless of this setting.) if you delete the host that was your start default, it falls back to this broker automatically, so the button keeps working. when the chosen host needs a password, quick-launching start surfaces its login prompt just like opening the host directly. note that remote host identities are specific to the browser where you added them, so a non-local start default is only meaningful in that browser; other browsers fall back to launching locally.","title":"Default host for the START (+) button"},{"body":[{"spans":[{"t":"text","v":"With a "},{"t":"strong","v":"single broker"},{"t":"text","v":", Browserland shows its status chip in the host-status area of the taskbar. The chip displays the host's label; its state tells you whether that host is reachable and whether this browser holds its lease:"}],"t":"p"},{"spans":[{"t":"text","v":"State · Meaning · Click does"}],"t":"sub"},{"spans":[{"t":"text","v":"ok — Reachable, and this browser is the active writer of its layout. — Hide / show that host's windows"}],"t":"bullet"},{"spans":[{"t":"text","v":"down — Unreachable (broker down, or a pre-CORS version). — Hide / show that host's windows"}],"t":"bullet"},{"spans":[{"t":"text","v":"auth-needed — Up, but the password is missing or wrong. — Open the login prompt"}],"t":"bullet"},{"spans":[{"t":"text","v":"lease — Reachable, but another browser holds the active-writer lease. — Take over the lease"}],"t":"bullet"},{"spans":[{"t":"text","v":"With "},{"t":"strong","v":"two or more brokers"},{"t":"text","v":", the chips collapse into a single "},{"t":"strong","v":"aggregate badge"},{"t":"text","v":" so they stop competing with the session buttons for taskbar width. The badge reads "},{"t":"code","v":"N brokers"},{"t":"text","v":", colored by the worst state present (auth-needed, then down, then lease) — and it appends "},{"t":"code","v":"· K need attention"},{"t":"text","v":", counting every host that is auth-needed, down, or lease, so one kind of fault never hides another. While first contact is still in flight it reads "},{"t":"code","v":"· checking…"},{"t":"text","v":". Hovering the badge lists every host with its state; it is struck through only when every broker is hidden. "},{"t":"strong","v":"Click it"},{"t":"text","v":" (or press Enter/Space — it is keyboard-focusable) to open the start (+) menu."}],"t":"p"},{"spans":[{"t":"text","v":"A broker "},{"t":"strong","v":"you hid yourself"},{"t":"text","v":" is not counted and does not color the badge — hiding one is a choice, not a fault, so parking an offline machine no longer leaves the badge permanently red. Its real state is still there in the badge's hover list and on its row in the (+) menu, both of which mark it "},{"t":"code","v":"— hidden"},{"t":"text","v":". If every broker is hidden, the badge drops its state color entirely and just goes struck through."}],"t":"p"},{"spans":[{"t":"text","v":"There, each broker has a "},{"t":"strong","v":"live status row"},{"t":"text","v":" above its profiles: a state-colored dot (a per-host identity color becomes the dot's fill, with the state as its ring), the host's label, and a state suffix such as "},{"t":"code","v":"— down"},{"t":"text","v":" or "},{"t":"code","v":"— password required"},{"t":"text","v":". The row performs the chip's action for its state — an "},{"t":"strong","v":"auth-needed"},{"t":"text","v":" row opens the login prompt, a "},{"t":"strong","v":"lease"},{"t":"text","v":" row requests the take-over, and an "},{"t":"strong","v":"ok"},{"t":"text","v":"/"},{"t":"strong","v":"down"},{"t":"text","v":" row toggles that broker's windows hidden or shown. The hide toggle "},{"t":"strong","v":"keeps the menu open"},{"t":"text","v":" (the row goes struck through and gains "},{"t":"code","v":"— hidden"},{"t":"text","v":"), so you can sweep several brokers in one visit; hiding masks windows, it never closes them. The same toggle also lives as a "},{"t":"strong","v":"hidden"},{"t":"text","v":" checkbox on each host's row in "},{"t":"strong","v":"Control Panel → Hosts"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"So the status surfaces stay interactive, not just indicators — and every recovery path that used to live on a chip (re-opening a cancelled login, taking back a lease, spotting a down broker) is one badge click away when several brokers are attached."}],"t":"p"},{"spans":[{"t":"text","v":"Because those recovery paths now live on the (+) menu rows rather than on the chip, the chip itself is optional. "},{"t":"strong","v":"Control Panel → Desktop → Broker status chip"},{"t":"text","v":" offers "},{"t":"strong","v":"Always"},{"t":"text","v":" (the default), "},{"t":"strong","v":"Only when a broker needs attention"},{"t":"text","v":" — it appears when a broker goes unreachable, wants a password, or is taken over by another browser, and vanishes again once nothing is wrong — and "},{"t":"strong","v":"Never"},{"t":"text","v":". A broker you hid yourself never triggers it. Whichever you pick, the (+) menu rows and the "},{"t":"strong","v":"hidden"},{"t":"text","v":" checkboxes under Control Panel → Hosts are unchanged, so logging in, taking over and un-hiding stay reachable with no chip at all. See Taskbar."}],"t":"p"}],"search":"host status chips with a single broker , browserland shows its status chip in the host-status area of the taskbar. the chip displays the host's label; its state tells you whether that host is reachable and whether this browser holds its lease: state · meaning · click does ok — reachable, and this browser is the active writer of its layout. — hide / show that host's windows down — unreachable (broker down, or a pre-cors version). — hide / show that host's windows auth-needed — up, but the password is missing or wrong. — open the login prompt lease — reachable, but another browser holds the active-writer lease. — take over the lease with two or more brokers , the chips collapse into a single aggregate badge so they stop competing with the session buttons for taskbar width. the badge reads n brokers , colored by the worst state present (auth-needed, then down, then lease) — and it appends · k need attention , counting every host that is auth-needed, down, or lease, so one kind of fault never hides another. while first contact is still in flight it reads · checking… . hovering the badge lists every host with its state; it is struck through only when every broker is hidden. click it (or press enter/space — it is keyboard-focusable) to open the start (+) menu. a broker you hid yourself is not counted and does not color the badge — hiding one is a choice, not a fault, so parking an offline machine no longer leaves the badge permanently red. its real state is still there in the badge's hover list and on its row in the (+) menu, both of which mark it — hidden . if every broker is hidden, the badge drops its state color entirely and just goes struck through. there, each broker has a live status row above its profiles: a state-colored dot (a per-host identity color becomes the dot's fill, with the state as its ring), the host's label, and a state suffix such as — down or — password required . the row performs the chip's action for its state — an auth-needed row opens the login prompt, a lease row requests the take-over, and an ok / down row toggles that broker's windows hidden or shown. the hide toggle keeps the menu open (the row goes struck through and gains — hidden ), so you can sweep several brokers in one visit; hiding masks windows, it never closes them. the same toggle also lives as a hidden checkbox on each host's row in control panel → hosts . so the status surfaces stay interactive, not just indicators — and every recovery path that used to live on a chip (re-opening a cancelled login, taking back a lease, spotting a down broker) is one badge click away when several brokers are attached. because those recovery paths now live on the (+) menu rows rather than on the chip, the chip itself is optional. control panel → desktop → broker status chip offers always (the default), only when a broker needs attention — it appears when a broker goes unreachable, wants a password, or is taken over by another browser, and vanishes again once nothing is wrong — and never . a broker you hid yourself never triggers it. whichever you pick, the (+) menu rows and the hidden checkboxes under control panel → hosts are unchanged, so logging in, taking over and un-hiding stay reachable with no chip at all. see taskbar. taskbar","title":"Host status chips"},{"body":[{"spans":[{"t":"text","v":"The (off-by-default) "},{"t":"strong","v":"update check"},{"t":"text","v":" mod is fleet-wide the same way the host badge above is: with more than one broker configured, it collapses into one aggregate chip on the taskbar, colored by the worst state across every configured host, with a tooltip listing each broker's own state. See Taskbar for where the chip lives and what its states mean."}],"t":"p"},{"spans":[{"t":"text","v":"Its hidden-broker rule is the "},{"t":"strong","v":"opposite"},{"t":"text","v":" of the host badge's, on purpose. Above, a broker you hide yourself drops out of the host badge entirely — it is not counted and does not color it, because hiding is a choice, not a fault. The update chip does not extend that courtesy: a hidden broker still counts toward its color and its "},{"t":"code","v":"N behind"},{"t":"text","v":" / "},{"t":"code","v":"N ahead"},{"t":"text","v":" / "},{"t":"code","v":"N unchecked"},{"t":"text","v":" counts, and still shows up in its tooltip, suffixed "},{"t":"code","v":"— hidden"},{"t":"text","v":". The reasoning is that hiding a broker only parks it on the desktop — it does not make that broker's build any less stale, so this chip may stop claiming a fault for a broker you hid but must never stop reporting one."}],"t":"p"}],"search":"update check chip the (off-by-default) update check mod is fleet-wide the same way the host badge above is: with more than one broker configured, it collapses into one aggregate chip on the taskbar, colored by the worst state across every configured host, with a tooltip listing each broker's own state. see taskbar for where the chip lives and what its states mean. its hidden-broker rule is the opposite of the host badge's, on purpose. above, a broker you hide yourself drops out of the host badge entirely — it is not counted and does not color it, because hiding is a choice, not a fault. the update chip does not extend that courtesy: a hidden broker still counts toward its color and its n behind / n ahead / n unchecked counts, and still shows up in its tooltip, suffixed — hidden . the reasoning is that hiding a broker only parks it on the desktop — it does not make that broker's build any less stale, so this chip may stop claiming a fault for a broker you hid but must never stop reporting one. taskbar","title":"Update check chip"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Update check"},{"t":"text","v":" window shows an "},{"t":"strong","v":"Update…"},{"t":"text","v":" button when the broker is behind upstream and update apply is available. Clicking it applies the previewed commit, then restarts the broker to run the new code. Applying an update is "},{"t":"strong","v":"manual-trigger only"},{"t":"text","v":" — no schedule, no auto-install — and it requires three gates on the broker: checking, applying, and restarting."}],"t":"p"},{"spans":[{"t":"text","v":"Each gate is granted from one of two places, and only one at a time owns it. On the same window, two rows do the granting:"}],"t":"p"},{"spans":[{"t":"strong","v":"Enable checking on this broker"},{"t":"text","v":" grants "},{"t":"code","v":"update_check_enabled"},{"t":"text","v":" — turning the update mod on for the first time asks for this automatically, on the broker serving the page."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" grants "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":" and "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" "},{"t":"strong","v":"together"},{"t":"text","v":", since applying without being able to restart would just leave the old code running. "},{"t":"strong","v":"Stop"},{"t":"text","v":" on this row gives back only those two — checking stays on, so the window keeps reporting what's available."}],"t":"bullet"},{"spans":[{"t":"text","v":"Either row can instead go "},{"t":"strong","v":"dead and name a config key"},{"t":"text","v":" — an operator who puts "},{"t":"code","v":"update_check_enabled"},{"t":"text","v":", "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":", or "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" in that broker's "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" ("},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":", either way) takes that one gate out of the GUI's hands entirely; only editing the file changes it from then on, and nothing granted from the window can escalate past it. A gate the config hasn't named stays the window's to grant, so a fresh install with no config overrides leaves all three to a human click."}],"t":"p"},{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Update…"},{"t":"text","v":" button is unavailable (and greyed with a reason) when one or more gates are still closed, or when the current state does not permit applying — the upstream commit is unreachable, the working tree is dirty, there are local commits to merge, or a dependency has changed. Clicking the button shows a confirm dialog with the commit range, count, and GitHub compare link, plus a preview of what happens to your sessions."}],"t":"p"},{"spans":[{"t":"text","v":"The update fetches from the pinned upstream with an explicit git subprocess and verifies the target before applying. See Technical-Reference#update-apply for the complete technical details, rollback story, and what happens to sessions when the broker restarts."}],"t":"p"}],"search":"updating the broker the update check window shows an update… button when the broker is behind upstream and update apply is available. clicking it applies the previewed commit, then restarts the broker to run the new code. applying an update is manual-trigger only — no schedule, no auto-install — and it requires three gates on the broker: checking, applying, and restarting. each gate is granted from one of two places, and only one at a time owns it. on the same window, two rows do the granting: enable checking on this broker grants update_check_enabled — turning the update mod on for the first time asks for this automatically, on the broker serving the page. allow this broker to update itself grants update_apply_enabled and restart_enabled together , since applying without being able to restart would just leave the old code running. stop on this row gives back only those two — checking stays on, so the window keeps reporting what's available. either row can instead go dead and name a config key — an operator who puts update_check_enabled , update_apply_enabled , or restart_enabled in that broker's broker_config.json ( true or false , either way) takes that one gate out of the gui's hands entirely; only editing the file changes it from then on, and nothing granted from the window can escalate past it. a gate the config hasn't named stays the window's to grant, so a fresh install with no config overrides leaves all three to a human click. the update… button is unavailable (and greyed with a reason) when one or more gates are still closed, or when the current state does not permit applying — the upstream commit is unreachable, the working tree is dirty, there are local commits to merge, or a dependency has changed. clicking the button shows a confirm dialog with the commit range, count, and github compare link, plus a preview of what happens to your sessions. the update fetches from the pinned upstream with an explicit git subprocess and verifies the target before applying. see technical-reference#update-apply for the complete technical details, rollback story, and what happens to sessions when the broker restarts. technical reference#update apply","title":"Updating the broker"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Update check"},{"t":"text","v":" window also carries a "},{"t":"strong","v":"Restart this broker"},{"t":"text","v":" button (when available). Clicking it triggers a broker-generation restart — the broker process is replaced with fresh code without stopping the machine; agents you launched stay alive and reconnect to the new broker. See Technical-Reference#broker-restart-183 for full details on what a restart is, how to enable it, what happens to your sessions, and when the button is greyed out."}],"t":"p"},{"spans":[{"t":"text","v":"Restart is granted the same way as the other two gates above: from the "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" row when it is writable, or from the broker's own "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" when a "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" key already owns it. This is deployment policy either way: a restart is a maintenance choice, not something a browser session earns just by being logged in."}],"t":"p"},{"spans":[{"t":"strong","v":"Granting a remote broker's row asks for its own confirmation first."},{"t":"text","v":" Checking on a remote broker names the machine and its address before switching it on. Self-update goes further: the confirm dialog is titled "},{"t":"strong","v":"Let that broker update itself?"},{"t":"text","v":", names that machine and its address, and warns plainly that it lets that machine download and run code from GitHub and restart itself. A broker running a build older than this fleet story degrades gracefully — its row is shown but says to update that broker rather than offering a switch that would fail."}],"t":"p"}],"search":"restarting this broker the update check window also carries a restart this broker button (when available). clicking it triggers a broker-generation restart — the broker process is replaced with fresh code without stopping the machine; agents you launched stay alive and reconnect to the new broker. see technical-reference#broker-restart-183 for full details on what a restart is, how to enable it, what happens to your sessions, and when the button is greyed out. restart is granted the same way as the other two gates above: from the allow this broker to update itself row when it is writable, or from the broker's own broker_config.json when a restart_enabled key already owns it. this is deployment policy either way: a restart is a maintenance choice, not something a browser session earns just by being logged in. granting a remote broker's row asks for its own confirmation first. checking on a remote broker names the machine and its address before switching it on. self-update goes further: the confirm dialog is titled let that broker update itself? , names that machine and its address, and warns plainly that it lets that machine download and run code from github and restart itself. a broker running a build older than this fleet story degrades gracefully — its row is shown but says to update that broker rather than offering a switch that would fail. technical reference#broker restart 183","title":"Restarting this broker"},{"body":[{"spans":[{"t":"text","v":"A broker allows only "},{"t":"strong","v":"one active browser at a time"},{"t":"text","v":" to be the WRITER of its layout. That permission is a lease: the browser holding it owns the window arrangement for that broker. This prevents two open tabs from overwriting each other's layout."}],"t":"p"},{"spans":[{"t":"text","v":"If you open the desktop in a second browser (or a second tab) while another already holds the lease, you won't see windows immediately. Instead you get a "},{"t":"strong","v":"Become active"},{"t":"text","v":" prompt:"}],"t":"p"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"text","v":"another browser is active  This broker allows one active browser at a time. Taking over will deactivate the other one."}],"t":"p"},{"spans":[{"t":"text","v":"Click "},{"t":"strong","v":"Become active"},{"t":"text","v":" to take over the lease. The previously active browser is deactivated and shows the same prompt, so it can take the lease back later."}],"t":"p"},{"spans":[{"t":"text","v":"Note that terminals keep running regardless of which browser holds the lease — the shells live in the agents, not the browser. Taking over the lease only changes who is editing the layout, not what the terminals are doing. For what the terminals, notes, editors, file managers, and the (off-by-default) AI-provider status monitor actually are, see Window-Types."}],"t":"p"}],"search":"single active browser (the lease) a broker allows only one active browser at a time to be the writer of its layout. that permission is a lease: the browser holding it owns the window arrangement for that broker. this prevents two open tabs from overwriting each other's layout. if you open the desktop in a second browser (or a second tab) while another already holds the lease, you won't see windows immediately. instead you get a become active prompt: tip: another browser is active this broker allows one active browser at a time. taking over will deactivate the other one. click become active to take over the lease. the previously active browser is deactivated and shows the same prompt, so it can take the lease back later. note that terminals keep running regardless of which browser holds the lease — the shells live in the agents, not the browser. taking over the lease only changes who is editing the layout, not what the terminals are doing. for what the terminals, notes, editors, file managers, and the (off-by-default) ai-provider status monitor actually are, see window-types. window types","title":"Single active browser (the lease)"}],"label":"Hosts & Multi-Browser","order":13,"slug":"hosts-and-multi-browser"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland can let an AI harness (an MCP client) "},{"t":"strong","v":"list, observe, drive, and launch"},{"t":"text","v":" your terminals — including full-screen TUIs — over a token-gated interface. Nothing is reachable until you turn it on, and you stay in control: access is granted per host and per window, and the broker is always the sole authority."}],"t":"p"},{"spans":[{"t":"text","v":"This page covers MCP access from your point of view at the desktop. For the full HTTP contract and setup details, follow the reference links at the bottom."}],"t":"p"}],"search":"overview browserland can let an ai harness (an mcp client) list, observe, drive, and launch your terminals — including full-screen tuis — over a token-gated interface. nothing is reachable until you turn it on, and you stay in control: access is granted per host and per window, and the broker is always the sole authority. this page covers mcp access from your point of view at the desktop. for the full http contract and setup details, follow the reference links at the bottom.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"An MCP client (such as an AI coding agent) connects to your broker and can attach to the exact terminals you are working in. Because each terminal is a real PTY that survives reloads, the agent's handle to a session stays valid across browser reloads and broker restarts."}],"t":"p"},{"spans":[{"t":"text","v":"The agent can only see and touch the terminals you allow. There are three layers of control, all opt-in:"}],"t":"p"},{"spans":[{"t":"strong","v":"Master enable"},{"t":"text","v":" for the host (Control Panel → Access → MCP access)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Per-window access"},{"t":"text","v":" — "},{"t":"code","v":"off"},{"t":"text","v":", "},{"t":"code","v":"read"},{"t":"text","v":", or "},{"t":"code","v":"readwrite"},{"t":"text","v":" — set on each terminal."}],"t":"bullet"},{"spans":[{"t":"text","v":"A separate gate for whether the agent may "},{"t":"strong","v":"launch new terminals"},{"t":"text","v":"."}],"t":"bullet"}],"search":"what mcp access means an mcp client (such as an ai coding agent) connects to your broker and can attach to the exact terminals you are working in. because each terminal is a real pty that survives reloads, the agent's handle to a session stays valid across browser reloads and broker restarts. the agent can only see and touch the terminals you allow. there are three layers of control, all opt-in: master enable for the host (control panel → access → mcp access). per-window access — off , read , or readwrite — set on each terminal. a separate gate for whether the agent may launch new terminals .","title":"What MCP access means"},{"body":[{"spans":[{"t":"text","v":"Every terminal window has a "},{"t":"strong","v":"robot button"},{"t":"text","v":" that sets that window's MCP access for agents:"}],"t":"p"},{"spans":[{"t":"text","v":"Mode · What an agent can do"}],"t":"sub"},{"spans":[{"t":"code","v":"off"},{"t":"text","v":" — Nothing — the window is hidden from MCP entirely"}],"t":"bullet"},{"spans":[{"t":"code","v":"read"},{"t":"text","v":" — Observe only — read the current screen"}],"t":"bullet"},{"spans":[{"t":"code","v":"readwrite"},{"t":"text","v":" — Observe "},{"t":"strong","v":"and"},{"t":"text","v":" type into the terminal ("},{"t":"code","v":"send_input"},{"t":"text","v":") — shown as \"Read-write\" in the UI"}],"t":"bullet"},{"spans":[{"t":"text","v":"When an agent reads the screen or types into a terminal, that window's "},{"t":"strong","v":"robot icon briefly flashes"},{"t":"text","v":", so you can see at a glance when a harness is touching a session."}],"t":"p"},{"spans":[{"t":"text","v":"You can also set per-window access from a window's right-click menu (see Context-Menus). New windows start in the host's global "},{"t":"strong","v":"default mode"},{"t":"text","v":" (configured below)."}],"t":"p"},{"spans":[{"t":"text","v":"The robot button is specific to "},{"t":"strong","v":"terminals"},{"t":"text","v":". For the full list of window types, see Window-Types."}],"t":"p"}],"search":"per-window access (the robot button) every terminal window has a robot button that sets that window's mcp access for agents: mode · what an agent can do off — nothing — the window is hidden from mcp entirely read — observe only — read the current screen readwrite — observe and type into the terminal ( send_input ) — shown as \"read-write\" in the ui when an agent reads the screen or types into a terminal, that window's robot icon briefly flashes , so you can see at a glance when a harness is touching a session. you can also set per-window access from a window's right-click menu (see context-menus). new windows start in the host's global default mode (configured below). the robot button is specific to terminals . for the full list of window types, see window-types. context menus window types","title":"Per-window access (the robot button)"},{"body":[{"spans":[{"t":"text","v":"MCP is configured per host in "},{"t":"strong","v":"Control Panel → Access → MCP access"},{"t":"text","v":":"}],"t":"p"},{"spans":[{"t":"strong","v":"Enable MCP access"},{"t":"text","v":" — the master switch for that host. While it is off, every MCP call is refused."}],"t":"bullet"},{"spans":[{"t":"strong","v":"default mode"},{"t":"text","v":" — the per-window access ("},{"t":"code","v":"off"},{"t":"text","v":" / "},{"t":"code","v":"read"},{"t":"text","v":" / "},{"t":"code","v":"readwrite"},{"t":"text","v":") that new terminals start with."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Allow MCP to launch terminals"},{"t":"text","v":" — an optional, separate gate that lets agents create new terminals (not just drive existing ones)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"token"},{"t":"text","v":" — a secret that authenticates the agent (set it yourself or hit "},{"t":"strong","v":"generate"},{"t":"text","v":"). This is a "},{"t":"strong","v":"bearer secret distinct from the browser/UI password"},{"t":"text","v":" you use to log in; granting MCP access does not hand out your login."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"text","v":"Settings are per host, so each broker you add has its own MCP switch, default mode, launch gate, and token. See Hosts-and-Multi-Browser for adding and managing hosts."}],"t":"p"},{"spans":[{"t":"text","v":"The in-app guide also shows a live "},{"t":"strong","v":"MCP status (this host)"},{"t":"text","v":" entry — whether MCP is enabled, the default mode for new windows, and whether launch-via-MCP is allowed."}],"t":"p"}],"search":"enable mcp for a host mcp is configured per host in control panel → access → mcp access : enable mcp access — the master switch for that host. while it is off, every mcp call is refused. default mode — the per-window access ( off / read / readwrite ) that new terminals start with. allow mcp to launch terminals — an optional, separate gate that lets agents create new terminals (not just drive existing ones). token — a secret that authenticates the agent (set it yourself or hit generate ). this is a bearer secret distinct from the browser/ui password you use to log in; granting mcp access does not hand out your login. tip: settings are per host, so each broker you add has its own mcp switch, default mode, launch gate, and token. see hosts-and-multi-browser for adding and managing hosts. the in-app guide also shows a live mcp status (this host) entry — whether mcp is enabled, the default mode for new windows, and whether launch-via-mcp is allowed. hosts and multi browser","title":"Enable MCP for a host"},{"body":[{"spans":[{"t":"text","v":"Access is layered and "},{"t":"strong","v":"off by default"},{"t":"text","v":" — you opt in at every level:"}],"t":"p"},{"spans":[{"t":"strong","v":"Master enable is OFF by default."},{"t":"text","v":" While it is off, every MCP call returns "},{"t":"code","v":"403 mcp_disabled"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Per-window mode"},{"t":"text","v":" is "},{"t":"code","v":"off"},{"t":"text","v":" / "},{"t":"code","v":"read"},{"t":"text","v":" / "},{"t":"code","v":"readwrite"},{"t":"text","v":", with a global "},{"t":"code","v":"default_mode"},{"t":"text","v":" for new windows. "},{"t":"code","v":"off"},{"t":"text","v":" hides the window, "},{"t":"code","v":"read"},{"t":"text","v":" allows observation, and "},{"t":"code","v":"readwrite"},{"t":"text","v":" additionally allows typing."}],"t":"bullet"},{"spans":[{"t":"code","v":"allow_launch"},{"t":"text","v":" is a separate gate — turning MCP on does not, by itself, let agents spawn terminals."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The MCP token"},{"t":"text","v":" is a separate bearer secret from your browser auth/UI password."}],"t":"bullet"},{"spans":[{"t":"text","v":"The agents are just producers; the broker stays the sole authority and gates every call by these rules."}],"t":"p"}],"search":"safety model access is layered and off by default — you opt in at every level: master enable is off by default. while it is off, every mcp call returns 403 mcp_disabled . per-window mode is off / read / readwrite , with a global default_mode for new windows. off hides the window, read allows observation, and readwrite additionally allows typing. allow_launch is a separate gate — turning mcp on does not, by itself, let agents spawn terminals. the mcp token is a separate bearer secret from your browser auth/ui password. the agents are just producers; the broker stays the sole authority and gates every call by these rules.","title":"Safety model"},{"body":[{"spans":[{"t":"text","v":"An MCP client sees these tools, each mapping to a broker endpoint:"}],"t":"p"},{"spans":[{"t":"text","v":"Tool · What it does"}],"t":"sub"},{"spans":[{"t":"code","v":"mcp_info(host?)"},{"t":"text","v":" — Reports feature flags ("},{"t":"code","v":"allow_launch"},{"t":"text","v":", "},{"t":"code","v":"default_mode"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"code","v":"list_terminals"},{"t":"text","v":" — Lists all running sessions across hosts (id, title, cwd, agent, kind, cols/rows, mode)"}],"t":"bullet"},{"spans":[{"t":"code","v":"list_profiles(host?)"},{"t":"text","v":" — Lists launchable profile names and the default"}],"t":"bullet"},{"spans":[{"t":"code","v":"read_screen(id, …)"},{"t":"text","v":" — Returns the terminal's screen. Far more than a bounded grid: "},{"t":"code","v":"view=\"scrollback\""},{"t":"text","v":" and "},{"t":"code","v":"lines"},{"t":"text","v":" widen what is returned, "},{"t":"code","v":"since"},{"t":"text","v":" returns only what is new, "},{"t":"code","v":"attrs"},{"t":"text","v":" adds styling runs, and it can "},{"t":"strong","v":"wait"},{"t":"text","v":" rather than poll — "},{"t":"code","v":"wait_for_change"},{"t":"text","v":", "},{"t":"code","v":"wait_for_text"},{"t":"text","v":", "},{"t":"code","v":"wait_for_regex"},{"t":"text","v":", "},{"t":"code","v":"wait_absent"},{"t":"text","v":", "},{"t":"code","v":"wait_for_idle"}],"t":"bullet"},{"spans":[{"t":"code","v":"send_input(id, data)"},{"t":"text","v":" — Types text into a window (window must be "},{"t":"code","v":"readwrite"},{"t":"text","v":"; newlines are sent as Enter)"}],"t":"bullet"},{"spans":[{"t":"code","v":"send_keys(id, keys, delay_ms?)"},{"t":"text","v":" — Sends control/escape keys, e.g. "},{"t":"code","v":"[\"C-c\"]"},{"t":"text","v":", "},{"t":"code","v":"[\"Esc\"]"},{"t":"text","v":", "},{"t":"code","v":"[\"Up\",\"Enter\"]"},{"t":"text","v":". Also accepts "},{"t":"code","v":"S-"},{"t":"text","v":" (shift) modifiers and a bare "},{"t":"code","v":"LF"},{"t":"text","v":" token, and "},{"t":"code","v":"delay_ms"},{"t":"text","v":" paces the keystrokes"}],"t":"bullet"},{"spans":[{"t":"code","v":"set_pace(id, pace_ms)"},{"t":"text","v":" — Sets a per-terminal default inter-key pacing for "},{"t":"code","v":"send_keys"},{"t":"text","v":" (window must be "},{"t":"code","v":"readwrite"},{"t":"text","v":"; ephemeral, "},{"t":"code","v":"0"},{"t":"text","v":" disables, capped at 1000 ms)"}],"t":"bullet"},{"spans":[{"t":"code","v":"reset_terminal(id)"},{"t":"text","v":" — Wipes Browserland's screen buffer for the window so the next "},{"t":"code","v":"read_screen"},{"t":"text","v":" starts clean (window must be "},{"t":"code","v":"readwrite"},{"t":"text","v":"; does not touch the running app)"}],"t":"bullet"},{"spans":[{"t":"code","v":"flush_input(id)"},{"t":"text","v":" — Discards keystrokes queued to the app but not yet consumed (window must be "},{"t":"code","v":"readwrite"},{"t":"text","v":"; a no-op on a Windows/ConPTY agent)"}],"t":"bullet"},{"spans":[{"t":"code","v":"launch_terminal(profile?, cols, rows, title?, cwd?, host?)"},{"t":"text","v":" — Launches a new terminal (broker must have "},{"t":"code","v":"allow_launch"},{"t":"text","v":" enabled)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Window ids"}],"t":"sub"},{"spans":[{"t":"text","v":"Window ids are namespaced as "},{"t":"code","v":"\"<host>:<int>\""},{"t":"text","v":" — for example "},{"t":"code","v":"\"default:12345\""},{"t":"text","v":". The host part matches the name of the host the terminal belongs to, so one MCP server can front several brokers at once. See Hosts-and-Multi-Browser for how hosts are named."}],"t":"p"}],"search":"tools overview an mcp client sees these tools, each mapping to a broker endpoint: tool · what it does mcp_info(host?) — reports feature flags ( allow_launch , default_mode ) list_terminals — lists all running sessions across hosts (id, title, cwd, agent, kind, cols/rows, mode) list_profiles(host?) — lists launchable profile names and the default read_screen(id, …) — returns the terminal's screen. far more than a bounded grid: view=\"scrollback\" and lines widen what is returned, since returns only what is new, attrs adds styling runs, and it can wait rather than poll — wait_for_change , wait_for_text , wait_for_regex , wait_absent , wait_for_idle send_input(id, data) — types text into a window (window must be readwrite ; newlines are sent as enter) send_keys(id, keys, delay_ms?) — sends control/escape keys, e.g. [\"c-c\"] , [\"esc\"] , [\"up\",\"enter\"] . also accepts s- (shift) modifiers and a bare lf token, and delay_ms paces the keystrokes set_pace(id, pace_ms) — sets a per-terminal default inter-key pacing for send_keys (window must be readwrite ; ephemeral, 0 disables, capped at 1000 ms) reset_terminal(id) — wipes browserland's screen buffer for the window so the next read_screen starts clean (window must be readwrite ; does not touch the running app) flush_input(id) — discards keystrokes queued to the app but not yet consumed (window must be readwrite ; a no-op on a windows/conpty agent) launch_terminal(profile?, cols, rows, title?, cwd?, host?) — launches a new terminal (broker must have allow_launch enabled) window ids window ids are namespaced as \"<host>:<int>\" — for example \"default:12345\" . the host part matches the name of the host the terminal belongs to, so one mcp server can front several brokers at once. see hosts-and-multi-browser for how hosts are named. hosts and multi browser","title":"Tools overview"}],"label":"MCP & AI Agents","order":14,"slug":"mcp-and-ai-agents"},{"cards":[{"body":[{"spans":[{"t":"text","v":"A practical guide to standing Browserland up — single machine first, then multiple machines over Tailscale — written so a "},{"t":"strong","v":"human or a coding agent"},{"t":"text","v":" can follow it without guessing at the architecture."}],"t":"p"},{"spans":[{"t":"text","v":"If you are an AI agent helping someone set this up: read "},{"t":"strong","v":"[The mental model](#the-mental-model)"},{"t":"text","v":" first. Most setup mistakes come from confusing the three roles below, or from hand-editing "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" when the right action was a click in the browser UI (see "},{"t":"strong","v":"[Don't hand-edit this](#dont-hand-edit-this)"},{"t":"text","v":")."}],"t":"p"}],"search":"overview a practical guide to standing browserland up — single machine first, then multiple machines over tailscale — written so a human or a coding agent can follow it without guessing at the architecture. if you are an ai agent helping someone set this up: read [the mental model](#the-mental-model) first. most setup mistakes come from confusing the three roles below, or from hand-editing broker_config.json when the right action was a click in the browser ui (see [don't hand-edit this](#dont-hand-edit-this) ).","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Browserland has exactly three roles. Keep them straight and everything else follows."}],"t":"p"},{"spans":[{"t":"text","v":"Role · What it is · Where it runs"}],"t":"sub"},{"spans":[{"t":"strong","v":"Broker"},{"t":"text","v":" — A small web server. Serves the desktop UI, relays bytes, and spawns agents from pre-approved "},{"t":"strong","v":"profiles"},{"t":"text","v":". The authority. — "},{"t":"strong","v":"One per machine."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Agent"},{"t":"text","v":" — A headless process that owns a real PTY (a terminal) and streams it to a broker over one WebSocket. — On the same machine as the broker that launches it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Browser"},{"t":"text","v":" — The desktop UI. Renders windows, sends your keystrokes. Can attach to "},{"t":"strong","v":"several brokers at once"},{"t":"text","v":". — Wherever you point a browser."}],"t":"bullet"},{"spans":[{"t":"text","v":"The shape that trips people up:"}],"t":"p"},{"spans":[{"t":"strong","v":"One broker per machine."},{"t":"text","v":" A broker is not a \"client\" you point at another"}],"t":"bullet"},{"spans":[{"t":"text","v":"machine. Each machine you want terminals on runs its own broker."}],"t":"p"},{"spans":[{"t":"strong","v":"Agents are local to their broker."},{"t":"text","v":" You normally don't run an agent on"}],"t":"bullet"},{"spans":[{"t":"text","v":"machine A pointed at a broker on machine B. You run a broker on B, and B launches B's agents. (A remote agent is possible — see TECHNICAL.md — but it's the exception, not the multi-machine model.)"}],"t":"p"},{"spans":[{"t":"strong","v":"Brokers are joined in the browser, not in config."},{"t":"text","v":" To see machine B's"}],"t":"bullet"},{"spans":[{"t":"text","v":"terminals next to machine A's, you don't edit a config file — you open the UI and add B as a "},{"t":"strong","v":"host"},{"t":"text","v":" (Control Panel → Hosts)."}],"t":"p"},{"spans":[{"t":"text","v":"So the canonical two-machine setup is:"}],"t":"p"},{"spans":[{"t":"code","v":"  Machine A                         Machine B\n  ┌──────────────┐                  ┌──────────────┐\n  │ broker A     │                  │ broker B     │\n  │  └ agents A  │                  │  └ agents B  │\n  └──────┬───────┘                  └──────┬───────┘\n         │                                 │\n         └──────────► browser ◄────────────┘\n            (Control Panel → Hosts: adds B by its Tailscale IP)"}],"t":"pre"}],"search":"the mental model browserland has exactly three roles. keep them straight and everything else follows. role · what it is · where it runs broker — a small web server. serves the desktop ui, relays bytes, and spawns agents from pre-approved profiles . the authority. — one per machine. agent — a headless process that owns a real pty (a terminal) and streams it to a broker over one websocket. — on the same machine as the broker that launches it. browser — the desktop ui. renders windows, sends your keystrokes. can attach to several brokers at once . — wherever you point a browser. the shape that trips people up: one broker per machine. a broker is not a \"client\" you point at another machine. each machine you want terminals on runs its own broker. agents are local to their broker. you normally don't run an agent on machine a pointed at a broker on machine b. you run a broker on b, and b launches b's agents. (a remote agent is possible — see technical.md — but it's the exception, not the multi-machine model.) brokers are joined in the browser, not in config. to see machine b's terminals next to machine a's, you don't edit a config file — you open the ui and add b as a host (control panel → hosts). so the canonical two-machine setup is: machine a machine b ┌──────────────┐ ┌──────────────┐ │ broker a │ │ broker b │ │ └ agents a │ │ └ agents b │ └──────┬───────┘ └──────┬───────┘ │ │ └──────────► browser ◄────────────┘ (control panel → hosts: adds b by its tailscale ip)","title":"The mental model"},{"body":[{"spans":[{"t":"text","v":"Follow the "},{"t":"strong","v":"[Quick start](https://github.com/JohnConnorNPC/browserland/blob/main/README.md#quick-start)"},{"t":"text","v":" in the README: install, run the broker, open "},{"t":"code","v":"http://127.0.0.1:4445/"},{"t":"text","v":", click "},{"t":"strong","v":"new terminal"},{"t":"text","v":". On a single loopback machine you need no config file — the defaults work."}],"t":"p"},{"spans":[{"t":"text","v":"A "},{"t":"strong","v":"token is required on every connection"},{"t":"text","v":", including loopback, but you do not have to create one: with nothing configured the broker mints its own into "},{"t":"code","v":"webterm_token.json"},{"t":"text","v":" beside its state store and prints the URL to open, token included. Need it again later:"}],"t":"p"},{"spans":[{"t":"code","v":"python -m webterm.broker --print-token"}],"t":"pre"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"strong","v":"Upgrading an existing tokenless install?"},{"t":"text","v":" Read "},{"t":"strong","v":"[[Upgrading]]"},{"t":"text","v":" first — terminals launched by the old broker cannot reconnect and must be relaunched once."}],"t":"p"},{"spans":[{"t":"text","v":"You only need the rest of this page once a second machine, a network bind, or an unattended broker enters the picture."}],"t":"p"}],"search":"single machine follow the [quick start](https://github.com/johnconnornpc/browserland/blob/main/readme.md#quick-start) in the readme: install, run the broker, open http://127.0.0.1:4445/ , click new terminal . on a single loopback machine you need no config file — the defaults work. a token is required on every connection , including loopback, but you do not have to create one: with nothing configured the broker mints its own into webterm_token.json beside its state store and prints the url to open, token included. need it again later: python -m webterm.broker --print-token tip: upgrading an existing tokenless install? read [[upgrading]] first — terminals launched by the old broker cannot reconnect and must be relaunched once. you only need the rest of this page once a second machine, a network bind, or an unattended broker enters the picture.","title":"Single machine"},{"body":[{"spans":[{"t":"text","v":"Goal: one browser tab showing terminals from every machine."}],"t":"p"},{"spans":[{"t":"strong","v":"Install Tailscale"},{"t":"text","v":" on each machine so they share a private network. Note"}],"t":"bullet"},{"spans":[{"t":"text","v":"each machine's Tailscale IP ("},{"t":"code","v":"tailscale ip -4"},{"t":"text","v":") or MagicDNS name."}],"t":"p"},{"spans":[{"t":"text","v":"**On every machine, run a broker** — one each, not a broker-plus-remote-"}],"t":"bullet"},{"spans":[{"t":"text","v":"agent. Two things every non-loopback broker needs:"}],"t":"p"},{"spans":[{"t":"strong","v":"A token you chose yourself."},{"t":"text","v":" Every broker requires one, but a broker"}],"t":"bullet"},{"spans":[{"t":"text","v":"left to mint its own picks a different value per machine — workable, just more URLs to keep track of. Setting "},{"t":"code","v":"\"auth_token\""},{"t":"text","v":" in "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" (or "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":") means you know the value up front and can reuse it across the fleet. Either way the UI's add-host form asks for it."}],"t":"p"},{"spans":[{"t":"strong","v":"A way for the other machines' browsers to reach it."},{"t":"text","v":" Two options:"}],"t":"bullet"},{"spans":[{"t":"text","v":"**Recommended — keep the loopback bind and front it with"}],"t":"bullet"},{"spans":[{"t":"text","v":"["},{"t":"code","v":"tailscale serve"},{"t":"text","v":"](https://tailscale.com/kb/1312/serve)** (or your own https reverse proxy, e.g. Caddy/nginx):"}],"t":"p"},{"spans":[{"t":"code","v":"tailscale serve --bg 4445"}],"t":"pre"},{"spans":[{"t":"text","v":"That publishes "},{"t":"code","v":"https://<machine>.<tailnet>.ts.net/"},{"t":"text","v":" — real TLS with zero certificate management, reachable only inside your tailnet — and proxies it (WebSockets included) to the broker still bound to "},{"t":"code","v":"127.0.0.1:4445"},{"t":"text","v":". Proxied requests reach the broker from loopback — which used to mean they skipped auth entirely, handing the whole tailnet an exempt path. Since #142 there is no loopback exemption, so this topology is gated by the token like any other."}],"t":"p"},{"spans":[{"t":"strong","v":"Plain http directly"},{"t":"text","v":": set "},{"t":"code","v":"\"host\""},{"t":"text","v":" to the machine's Tailscale IP (or"}],"t":"bullet"},{"spans":[{"t":"code","v":"0.0.0.0"},{"t":"text","v":" if you trust the tailnet) instead of "},{"t":"code","v":"127.0.0.1"},{"t":"text","v":", so the other machines can reach "},{"t":"code","v":"http://<tailscale-ip>:4445/"},{"t":"text","v":". Keep "},{"t":"code","v":"\"port\": 4445"},{"t":"text","v":" unless you have a reason."}],"t":"p"},{"spans":[{"t":"text","v":"Treat your home machine as just another broker — it can stay on "},{"t":"code","v":"127.0.0.1"},{"t":"text","v":" if you only ever drive it from its own browser, but give it a token + tailnet bind too if you want to reach it from elsewhere."}],"t":"p"},{"spans":[{"t":"strong","v":"Open the UI on one machine"},{"t":"text","v":" ("},{"t":"code","v":"http://<that-machine>:4445/"},{"t":"text","v":"). This is your"}],"t":"bullet"},{"spans":[{"t":"text","v":"\"cockpit\" — it doesn't have to be special, any broker's UI can host the view."}],"t":"p"},{"spans":[{"t":"strong","v":"Add the other brokers as hosts."},{"t":"text","v":" Control Panel → "},{"t":"strong","v":"Hosts"},{"t":"text","v":" → add each"}],"t":"bullet"},{"spans":[{"t":"text","v":"remote broker by its URL — "},{"t":"code","v":"https://<machine>.<tailnet>.ts.net/"},{"t":"text","v":" if you fronted it with "},{"t":"code","v":"tailscale serve"},{"t":"text","v":", else "},{"t":"code","v":"http://<tailscale-ip>:4445/"},{"t":"text","v":" — and its token/password. The "},{"t":"strong","v":"browser connects directly"},{"t":"text","v":" to each host (cross-origin "},{"t":"code","v":"fetch"},{"t":"text","v":" + WebSocket); hosts and passwords live per-browser in "},{"t":"code","v":"localStorage"},{"t":"text","v":", never in any config file."}],"t":"p"},{"spans":[{"t":"text","v":"Broker status lives in the taskbar: a single broker gets its own chip, always (green ok / red down / amber password-needed / blue lease-inactive; the chip doubles as its hide toggle), while two or more collapse into one aggregate badge — the worst state colors it, and clicking it opens the start (+) menu where every broker has a live status row carrying those same actions. Requirements worth knowing:"}],"t":"p"},{"spans":[{"t":"strong","v":"Both brokers must run the same webterm version"},{"t":"text","v":" — a too-old remote shows up"}],"t":"bullet"},{"spans":[{"t":"text","v":"as a red \"down\" chip even while it's running (CORS is version-gated)."}],"t":"p"},{"spans":[{"t":"strong","v":"Pick ONE scheme for the whole fleet — and prefer https."},{"t":"text","v":" An https page"}],"t":"bullet"},{"spans":[{"t":"text","v":"fetching an http broker is blocked by the browser as "},{"t":"strong","v":"mixed content"},{"t":"text","v":", so a half-and-half fleet breaks: the cockpit and every host must all be https, or all be plain http. "},{"t":"code","v":"tailscale serve"},{"t":"text","v":" on every machine is the easy all-https answer (an https proxy of your own works too). Https also makes the page a secure context, which some browser features require — clipboard "},{"t":"strong","v":"image paste"},{"t":"text","v":" into a terminal ("},{"t":"kbd","v":"Alt+V"},{"t":"text","v":" / right-click) only works on an https or localhost page. All-plain-http over a trusted tailnet still works; you just forfeit those."}],"t":"p"},{"spans":[{"t":"text","v":"Full auth/CORS details: "},{"t":"strong","v":"[[Technical Reference → Multiple hosts|Technical-Reference]]"},{"t":"text","v":"."}],"t":"p"}],"search":"multiple machines over tailscale goal: one browser tab showing terminals from every machine. install tailscale on each machine so they share a private network. note each machine's tailscale ip ( tailscale ip -4 ) or magicdns name. **on every machine, run a broker** — one each, not a broker-plus-remote- agent. two things every non-loopback broker needs: a token you chose yourself. every broker requires one, but a broker left to mint its own picks a different value per machine — workable, just more urls to keep track of. setting \"auth_token\" in broker_config.json (or $web_terminal_token ) means you know the value up front and can reuse it across the fleet. either way the ui's add-host form asks for it. a way for the other machines' browsers to reach it. two options: **recommended — keep the loopback bind and front it with [ tailscale serve ](https://tailscale.com/kb/1312/serve)** (or your own https reverse proxy, e.g. caddy/nginx): tailscale serve --bg 4445 that publishes https://<machine>.<tailnet>.ts.net/ — real tls with zero certificate management, reachable only inside your tailnet — and proxies it (websockets included) to the broker still bound to 127.0.0.1:4445 . proxied requests reach the broker from loopback — which used to mean they skipped auth entirely, handing the whole tailnet an exempt path. since #142 there is no loopback exemption, so this topology is gated by the token like any other. plain http directly : set \"host\" to the machine's tailscale ip (or 0.0.0.0 if you trust the tailnet) instead of 127.0.0.1 , so the other machines can reach http://<tailscale-ip>:4445/ . keep \"port\": 4445 unless you have a reason. treat your home machine as just another broker — it can stay on 127.0.0.1 if you only ever drive it from its own browser, but give it a token + tailnet bind too if you want to reach it from elsewhere. open the ui on one machine ( http://<that-machine>:4445/ ). this is your \"cockpit\" — it doesn't have to be special, any broker's ui can host the view. add the other brokers as hosts. control panel → hosts → add each remote broker by its url — https://<machine>.<tailnet>.ts.net/ if you fronted it with tailscale serve , else http://<tailscale-ip>:4445/ — and its token/password. the browser connects directly to each host (cross-origin fetch + websocket); hosts and passwords live per-browser in localstorage , never in any config file. broker status lives in the taskbar: a single broker gets its own chip, always (green ok / red down / amber password-needed / blue lease-inactive; the chip doubles as its hide toggle), while two or more collapse into one aggregate badge — the worst state colors it, and clicking it opens the start (+) menu where every broker has a live status row carrying those same actions. requirements worth knowing: both brokers must run the same webterm version — a too-old remote shows up as a red \"down\" chip even while it's running (cors is version-gated). pick one scheme for the whole fleet — and prefer https. an https page fetching an http broker is blocked by the browser as mixed content , so a half-and-half fleet breaks: the cockpit and every host must all be https, or all be plain http. tailscale serve on every machine is the easy all-https answer (an https proxy of your own works too). https also makes the page a secure context, which some browser features require — clipboard image paste into a terminal ( alt+v / right-click) only works on an https or localhost page. all-plain-http over a trusted tailnet still works; you just forfeit those. full auth/cors details: [[technical reference → multiple hosts|technical-reference]] .","title":"Multiple machines over Tailscale"},{"body":[{"spans":[{"t":"text","v":"The single most common setup mistake (especially when an AI agent is driving) is editing the wrong thing. Rules of thumb:"}],"t":"p"},{"spans":[{"t":"strong","v":"Joining brokers is a UI action, not a config edit."},{"t":"text","v":" To attach machine B,"}],"t":"bullet"},{"spans":[{"t":"text","v":"use Control Panel → Hosts in the browser. There is "},{"t":"strong","v":"no"},{"t":"text","v":" \"remote brokers\" list in "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" — looking for one and inventing one is a dead end."}],"t":"p"},{"spans":[{"t":"text","v":"**"},{"t":"code","v":"broker_config.json"},{"t":"text","v":" describes this broker only**: its bind ("},{"t":"code","v":"host"},{"t":"text","v":"/"}],"t":"bullet"},{"spans":[{"t":"code","v":"port"},{"t":"text","v":"), "},{"t":"code","v":"auth_token"},{"t":"text","v":", MCP gates, and the launchable "},{"t":"code","v":"profiles"},{"t":"text","v":". Nothing in it points at other machines."}],"t":"p"},{"spans":[{"t":"code","v":"profiles"},{"t":"strong","v":" are an allow-list, deliberately."},{"t":"text","v":" The browser can only launch"}],"t":"bullet"},{"spans":[{"t":"text","v":"pre-approved profiles — it can never supply a raw command. The "},{"t":"code","v":"agent.profiles"},{"t":"text","v":" here are the "},{"t":"strong","v":"seed"},{"t":"text","v":"; the easiest way to add a WSL/zsh/PowerShell profile is "},{"t":"strong","v":"Control Panel → Launch profiles"},{"t":"text","v":" (add/edit/detect, applied live with no restart), which persists to a "},{"t":"code","v":"webterm_profiles.json"},{"t":"text","v":" sidecar that then owns the set. See "},{"t":"strong","v":"[[Launch-Profiles]]"},{"t":"text","v":" for copyable recipes and the sidecar-vs-"},{"t":"code","v":"broker_config"},{"t":"text","v":" rule."}],"t":"p"},{"spans":[{"t":"text","v":"**Tokens/passwords for remote hosts live in the browser** (localStorage),"}],"t":"bullet"},{"spans":[{"t":"text","v":"set through the add-host form — not in any file on disk."}],"t":"p"},{"spans":[{"t":"text","v":"If you're an agent and you find yourself about to edit "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" to \"connect to the other machine,\" stop: that's a Control Panel → Hosts action instead."}],"t":"p"}],"search":"don't hand-edit this the single most common setup mistake (especially when an ai agent is driving) is editing the wrong thing. rules of thumb: joining brokers is a ui action, not a config edit. to attach machine b, use control panel → hosts in the browser. there is no \"remote brokers\" list in broker_config.json — looking for one and inventing one is a dead end. ** broker_config.json describes this broker only**: its bind ( host / port ), auth_token , mcp gates, and the launchable profiles . nothing in it points at other machines. profiles are an allow-list, deliberately. the browser can only launch pre-approved profiles — it can never supply a raw command. the agent.profiles here are the seed ; the easiest way to add a wsl/zsh/powershell profile is control panel → launch profiles (add/edit/detect, applied live with no restart), which persists to a webterm_profiles.json sidecar that then owns the set. see [[launch-profiles]] for copyable recipes and the sidecar-vs- broker_config rule. **tokens/passwords for remote hosts live in the browser** (localstorage), set through the add-host form — not in any file on disk. if you're an agent and you find yourself about to edit broker_config.json to \"connect to the other machine,\" stop: that's a control panel → hosts action instead.","title":"Don't hand-edit this"},{"body":[{"spans":[{"t":"text","v":"A broker is a long-lived server; you usually want it running unattended and surviving logout — not tied to a terminal you happened to launch it from."}],"t":"p"},{"spans":[{"t":"text","v":"Windows (Task Scheduler — recommended)"}],"t":"sub"},{"spans":[{"t":"text","v":"Launching the broker from a "},{"t":"strong","v":"restricted or unusual parent process"},{"t":"text","v":" (a sandbox, a service with a stripped profile, or another tool's subprocess) can hand its child terminals the wrong environment — bad permissions on user-profile paths (PSReadLine history, Codex/agent state, application logs) and stray inherited environment variables. The fix is to start the broker from a "},{"t":"strong","v":"clean, normal user context"},{"t":"text","v":". Task Scheduler does exactly that."}],"t":"p"},{"spans":[{"t":"text","v":"Create a task that runs at logon as your own user:"}],"t":"p"},{"spans":[{"t":"code","v":"# adjust the path to your checkout and python\n$action  = New-ScheduledTaskAction -Execute \"python\" `\n             -Argument \"-m webterm.broker\" `\n             -WorkingDirectory \"X:\\path\\to\\browserland\"\n$trigger = New-ScheduledTaskTrigger -AtLogOn\n$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries `\n             -DontStopIfGoingOnBatteries -StartWhenAvailable\nRegister-ScheduledTask -TaskName \"Browserland broker\" `\n  -Action $action -Trigger $trigger -Settings $settings `\n  -RunLevel Limited                # run as a normal (non-elevated) user"}],"t":"pre"},{"spans":[{"t":"text","v":"Notes:"}],"t":"p"},{"spans":[{"t":"strong","v":"Run as the interactive user, non-elevated"},{"t":"text","v":" ("},{"t":"code","v":"-RunLevel Limited"},{"t":"text","v":"). Running"}],"t":"bullet"},{"spans":[{"t":"text","v":"the broker elevated or as "},{"t":"code","v":"SYSTEM"},{"t":"text","v":" is what causes the profile-path permission problems above — its spawned terminals inherit that context."}],"t":"p"},{"spans":[{"t":"strong","v":"Don't launch the broker from another tool's subprocess"},{"t":"text","v":" if that tool"}],"t":"bullet"},{"spans":[{"t":"text","v":"injects its own environment. A broker started inside, say, a coding-agent session can leak that agent's marker variables into every terminal it spawns; Task Scheduler avoids this by starting from a clean session. If you must restart it from such a context, scrub the offending variables from the environment first."}],"t":"p"},{"spans":[{"t":"text","v":"Set "},{"t":"code","v":"auth_token"},{"t":"text","v":" and a tailnet bind in "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" "},{"t":"strong","v":"before"},{"t":"text","v":" the"}],"t":"bullet"},{"spans":[{"t":"text","v":"task starts binding non-loopback (see above)."}],"t":"p"},{"spans":[{"t":"text","v":"Verify it's up: open "},{"t":"code","v":"http://127.0.0.1:4445/"},{"t":"text","v":", or"}],"t":"bullet"},{"spans":[{"t":"code","v":"Get-ScheduledTask \"Browserland broker\""},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Alternative: any non-restricted startup method works — a "},{"t":"code","v":".lnk"},{"t":"text","v":" in "},{"t":"code","v":"shell:startup"},{"t":"text","v":", NSSM-as-a-service, etc. The requirement is just "},{"t":"strong","v":"clean, non-elevated, normal-profile"},{"t":"text","v":" — Task Scheduler is the least-surprising way to get there."}],"t":"p"},{"spans":[{"t":"text","v":"Linux (systemd)"}],"t":"sub"},{"spans":[{"t":"text","v":"Installable units ship in "},{"t":"code","v":"launchers/systemd/"},{"t":"text","v":". See "},{"t":"strong","v":"[[Technical Reference → Linux deployment|Technical-Reference]]"},{"t":"text","v":" for the full walkthrough ("},{"t":"code","v":"webterm-broker.service"},{"t":"text","v":" / "},{"t":"code","v":"webterm-agent.service"},{"t":"text","v":", "},{"t":"code","v":"User="},{"t":"text","v":", paths, token)."}],"t":"p"}],"search":"running the broker in the background a broker is a long-lived server; you usually want it running unattended and surviving logout — not tied to a terminal you happened to launch it from. windows (task scheduler — recommended) launching the broker from a restricted or unusual parent process (a sandbox, a service with a stripped profile, or another tool's subprocess) can hand its child terminals the wrong environment — bad permissions on user-profile paths (psreadline history, codex/agent state, application logs) and stray inherited environment variables. the fix is to start the broker from a clean, normal user context . task scheduler does exactly that. create a task that runs at logon as your own user: # adjust the path to your checkout and python $action = new-scheduledtaskaction -execute \"python\" ` -argument \"-m webterm.broker\" ` -workingdirectory \"x:\\path\\to\\browserland\" $trigger = new-scheduledtasktrigger -atlogon $settings = new-scheduledtasksettingsset -allowstartifonbatteries ` -dontstopifgoingonbatteries -startwhenavailable register-scheduledtask -taskname \"browserland broker\" ` -action $action -trigger $trigger -settings $settings ` -runlevel limited # run as a normal (non-elevated) user notes: run as the interactive user, non-elevated ( -runlevel limited ). running the broker elevated or as system is what causes the profile-path permission problems above — its spawned terminals inherit that context. don't launch the broker from another tool's subprocess if that tool injects its own environment. a broker started inside, say, a coding-agent session can leak that agent's marker variables into every terminal it spawns; task scheduler avoids this by starting from a clean session. if you must restart it from such a context, scrub the offending variables from the environment first. set auth_token and a tailnet bind in broker_config.json before the task starts binding non-loopback (see above). verify it's up: open http://127.0.0.1:4445/ , or get-scheduledtask \"browserland broker\" . alternative: any non-restricted startup method works — a .lnk in shell:startup , nssm-as-a-service, etc. the requirement is just clean, non-elevated, normal-profile — task scheduler is the least-surprising way to get there. linux (systemd) installable units ship in launchers/systemd/ . see [[technical reference → linux deployment|technical-reference]] for the full walkthrough ( webterm-broker.service / webterm-agent.service , user= , paths, token).","title":"Running the broker in the background"}],"label":"Setup & Onboarding","order":15,"slug":"setup-and-onboarding","tier":"dev"},{"cards":[{"body":[{"spans":[{"t":"text","v":"A "},{"t":"strong","v":"profile"},{"t":"text","v":" is a named shell recipe the broker is allowed to spawn. The browser (and any MCP client) launches a terminal by sending a profile "},{"t":"strong","v":"name"},{"t":"text","v":" — never a command. The broker looks the name up in its allow-list and spawns the argv it finds there. This is deliberate: a client can only start pre-approved shells, so an untrusted page or agent can never turn "},{"t":"code","v":"/launch"},{"t":"text","v":" into \"run arbitrary command X\" (the RCE-by-design boundary)."}],"t":"p"},{"spans":[{"t":"text","v":"Profiles power the right-click "},{"t":"code","v":"+"},{"t":"text","v":" launch picker, the per-host "},{"t":"strong","v":"default terminal profile"},{"t":"text","v":", and every MCP "},{"t":"code","v":"launch_terminal"},{"t":"text","v":" call."}],"t":"p"}],"search":"overview a profile is a named shell recipe the broker is allowed to spawn. the browser (and any mcp client) launches a terminal by sending a profile name — never a command. the broker looks the name up in its allow-list and spawns the argv it finds there. this is deliberate: a client can only start pre-approved shells, so an untrusted page or agent can never turn /launch into \"run arbitrary command x\" (the rce-by-design boundary). profiles power the right-click + launch picker, the per-host default terminal profile , and every mcp launch_terminal call.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Each profile is an object under a short name:"}],"t":"p"},{"spans":[{"t":"code","v":"\"ubuntu\": {\n  \"command\": [\"wsl.exe\", \"-d\", \"Ubuntu\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"],\n  \"title\": \"Ubuntu (WSL)\",   // optional — the window title; defaults to the name\n  \"cwd\": null,                // optional — the shell's start dir; null = agent default\n  \"color\": \"#e06666\"          // optional — default terminal color (#rrggbb); null = none\n}"}],"t":"pre"},{"spans":[{"t":"code","v":"command"},{"t":"text","v":" (required) — the argv the broker runs, as a **list of separate"}],"t":"bullet"},{"spans":[{"t":"text","v":"tokens** (never one shell string). "},{"t":"code","v":"[\"bash\", \"-l\"]"},{"t":"text","v":", not "},{"t":"code","v":"\"bash -l\""},{"t":"text","v":". This is the whole point of the allow-list: no shell parses it, so there is nothing to quote-inject."}],"t":"p"},{"spans":[{"t":"code","v":"title"},{"t":"text","v":" (optional) — the window title. Omitted → the profile name."}],"t":"bullet"},{"spans":[{"t":"code","v":"cwd"},{"t":"text","v":" (optional) — a starting directory for that shell. Omitted/"},{"t":"code","v":"null"},{"t":"text","v":" →"}],"t":"bullet"},{"spans":[{"t":"text","v":"the agent's default. (A client may also pass a "},{"t":"code","v":"cwd"},{"t":"text","v":" at launch time; that is validated as an existing directory separately.)"}],"t":"p"},{"spans":[{"t":"code","v":"color"},{"t":"text","v":" (optional) — a default terminal "},{"t":"strong","v":"color"},{"t":"text","v":" as "},{"t":"code","v":"#rrggbb"},{"t":"text","v":", seeding"}],"t":"bullet"},{"spans":[{"t":"text","v":"every "},{"t":"strong","v":"new"},{"t":"text","v":" terminal launched from this profile (window frame + taskbar chip). Omitted/"},{"t":"code","v":"null"},{"t":"text","v":" → no profile color, so the terminal falls back to the per-host default color, then the automatic palette pick. A per-window recolor from the title bar still wins and sticks. Unlike the per-host default (browser- local), this lives in the broker's profile, so every browser/viewer sees it."}],"t":"p"}],"search":"the fields each profile is an object under a short name: \"ubuntu\": { \"command\": [\"wsl.exe\", \"-d\", \"ubuntu\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"], \"title\": \"ubuntu (wsl)\", // optional — the window title; defaults to the name \"cwd\": null, // optional — the shell's start dir; null = agent default \"color\": \"#e06666\" // optional — default terminal color (#rrggbb); null = none } command (required) — the argv the broker runs, as a **list of separate tokens** (never one shell string). [\"bash\", \"-l\"] , not \"bash -l\" . this is the whole point of the allow-list: no shell parses it, so there is nothing to quote-inject. title (optional) — the window title. omitted → the profile name. cwd (optional) — a starting directory for that shell. omitted/ null → the agent's default. (a client may also pass a cwd at launch time; that is validated as an existing directory separately.) color (optional) — a default terminal color as #rrggbb , seeding every new terminal launched from this profile (window frame + taskbar chip). omitted/ null → no profile color, so the terminal falls back to the per-host default color, then the automatic palette pick. a per-window recolor from the title bar still wins and sticks. unlike the per-host default (browser- local), this lives in the broker's profile, so every browser/viewer sees it.","title":"The fields"},{"body":[{"spans":[{"t":"text","v":"Two sources, in order:"}],"t":"p"},{"spans":[{"t":"code","v":"broker_config.json"},{"t":"strong","v":" → "},{"t":"code","v":"agent.profiles"},{"t":"text","v":" — the "},{"t":"strong","v":"seed"},{"t":"text","v":". Read-only at"}],"t":"bullet"},{"spans":[{"t":"text","v":"runtime; ships the out-of-the-box defaults for the host OS."}],"t":"p"},{"spans":[{"t":"code","v":"webterm_profiles.json"},{"t":"text","v":" — the "},{"t":"strong","v":"sidecar"},{"t":"text","v":", written by the Control Panel"}],"t":"bullet"},{"spans":[{"t":"text","v":"editor, sitting next to the "},{"t":"code","v":"/state"},{"t":"text","v":" store (override with "},{"t":"code","v":"profiles_state_path"},{"t":"text","v":"). Atomic-write, like "},{"t":"code","v":"webterm_mcp.json"},{"t":"text","v":" / "},{"t":"code","v":"webterm_state.json"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"Sidecar-owns-once-written:"},{"t":"text","v":" the first time you save a profile from the UI, the sidecar is created and from then on it owns the whole set — "},{"t":"code","v":"agent.profiles"},{"t":"text","v":" becomes just the seed. So if you hand-edit "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" after that and see no change, that is why (the broker logs it loudly at startup). "},{"t":"strong","v":"To go back to the "},{"t":"code","v":"broker_config.json"},{"t":"strong","v":" seed, delete "},{"t":"code","v":"webterm_profiles.json"},{"t":"strong","v":" and restart."},{"t":"text","v":" A missing, corrupt, or empty sidecar always falls back to the seed — it can never brick startup or leave zero launchable shells."}],"t":"p"}],"search":"where profiles live two sources, in order: broker_config.json → agent.profiles — the seed . read-only at runtime; ships the out-of-the-box defaults for the host os. webterm_profiles.json — the sidecar , written by the control panel editor, sitting next to the /state store (override with profiles_state_path ). atomic-write, like webterm_mcp.json / webterm_state.json . sidecar-owns-once-written: the first time you save a profile from the ui, the sidecar is created and from then on it owns the whole set — agent.profiles becomes just the seed. so if you hand-edit broker_config.json after that and see no change, that is why (the broker logs it loudly at startup). to go back to the broker_config.json seed, delete webterm_profiles.json and restart. a missing, corrupt, or empty sidecar always falls back to the seed — it can never brick startup or leave zero launchable shells.","title":"Where profiles live"},{"body":[{"spans":[{"t":"strong","v":"Control Panel → (a host tab) → Launch profiles."},{"t":"text","v":" You can:"}],"t":"p"},{"spans":[{"t":"strong","v":"Add profile"},{"t":"text","v":" — name, optional title, optional cwd, and the command as a"}],"t":"bullet"},{"spans":[{"t":"text","v":"textarea with "},{"t":"strong","v":"one argv token per line"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"Edit"},{"t":"text","v":" / "},{"t":"strong","v":"Delete"},{"t":"text","v":" / "},{"t":"strong","v":"Make default"},{"t":"text","v":" an existing profile."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Detect…"},{"t":"text","v":" — one-click environment scan that seeds the Add dialog:"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Windows"},{"t":"text","v":" → your installed "},{"t":"strong","v":"WSL distros"},{"t":"text","v":" ("},{"t":"code","v":"wsl.exe -l -q"},{"t":"text","v":")."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Linux/macOS"},{"t":"text","v":" → the shells actually present ("},{"t":"code","v":"bash"},{"t":"text","v":", "},{"t":"code","v":"zsh"},{"t":"text","v":", "},{"t":"code","v":"fish"},{"t":"text","v":", "},{"t":"code","v":"sh"},{"t":"text","v":")."}],"t":"bullet"},{"spans":[{"t":"text","v":"Every save "},{"t":"strong","v":"applies immediately"},{"t":"text","v":" — the live launcher swaps and the next launch (and the "},{"t":"code","v":"+"},{"t":"text","v":" picker) uses the new set, no broker restart. A profile whose "},{"t":"code","v":"command[0]"},{"t":"text","v":" isn't found on "},{"t":"code","v":"PATH"},{"t":"text","v":" is flagged "},{"t":"strong","v":"⚠ not found"},{"t":"text","v":"."}],"t":"p"}],"search":"editing profiles (no restart) control panel → (a host tab) → launch profiles. you can: add profile — name, optional title, optional cwd, and the command as a textarea with one argv token per line . edit / delete / make default an existing profile. detect… — one-click environment scan that seeds the add dialog: windows → your installed wsl distros ( wsl.exe -l -q ). linux/macos → the shells actually present ( bash , zsh , fish , sh ). every save applies immediately — the live launcher swaps and the next launch (and the + picker) uses the new set, no broker restart. a profile whose command[0] isn't found on path is flagged ⚠ not found .","title":"Editing profiles (no restart)"},{"body":[{"spans":[{"t":"text","v":"Editing profiles means defining commands the host will run, so the editor is "},{"t":"strong","v":"browser-realm only"},{"t":"text","v":", gated exactly like "},{"t":"code","v":"/file/*"},{"t":"text","v":" and "},{"t":"code","v":"/state"},{"t":"text","v":" — a valid token, on every interface, no exceptions. The endpoints:"}],"t":"p"},{"spans":[{"t":"code","v":"GET /profiles/config"},{"t":"text","v":" / "},{"t":"code","v":"POST /profiles/config"},{"t":"text","v":" — the "},{"t":"strong","v":"full"},{"t":"text","v":" objects"}],"t":"bullet"},{"spans":[{"t":"text","v":"(commands included). Browser realm only."}],"t":"p"},{"spans":[{"t":"code","v":"GET /profiles/detect"},{"t":"text","v":" — the environment scan. Browser realm only."}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /profiles"},{"t":"text","v":" and "},{"t":"code","v":"GET /mcp/profiles"},{"t":"text","v":" — "},{"t":"strong","v":"names only"},{"t":"text","v":", unchanged. An MCP/AI"}],"t":"bullet"},{"spans":[{"t":"text","v":"agent can list profile names to launch, but can never read a command or define a new profile."}],"t":"p"},{"spans":[{"t":"text","v":"Writing a profile is "},{"t":"strong","v":"no more powerful than "},{"t":"code","v":"/file/write"},{"t":"text","v":", which the same gate already grants (both let an authenticated caller make the host run code). Since #142 there is no tokenless broker and no loopback exemption, so a cross-origin page — which carries no token — cannot reach these endpoints at all. That closes the old caveat about running a tokenless broker while the same browser visits untrusted sites."}],"t":"p"}],"search":"security model editing profiles means defining commands the host will run, so the editor is browser-realm only , gated exactly like /file/* and /state — a valid token, on every interface, no exceptions. the endpoints: get /profiles/config / post /profiles/config — the full objects (commands included). browser realm only. get /profiles/detect — the environment scan. browser realm only. get /profiles and get /mcp/profiles — names only , unchanged. an mcp/ai agent can list profile names to launch, but can never read a command or define a new profile. writing a profile is no more powerful than /file/write , which the same gate already grants (both let an authenticated caller make the host run code). since #142 there is no tokenless broker and no loopback exemption, so a cross-origin page — which carries no token — cannot reach these endpoints at all. that closes the old caveat about running a tokenless broker while the same browser visits untrusted sites.","title":"Security model"},{"body":[{"spans":[{"t":"text","v":"Copy a "},{"t":"code","v":"command"},{"t":"text","v":" into the Add-profile dialog (one token per line) or into "},{"t":"code","v":"broker_config.json"},{"t":"text","v":"'s "},{"t":"code","v":"agent.profiles"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Windows → WSL"}],"t":"sub"},{"spans":[{"t":"code","v":"// Ubuntu (Detect… fills these in for every installed distro)\n\"ubuntu\": { \"command\": [\"wsl.exe\", \"-d\", \"Ubuntu\",  \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] },\n\"debian\": { \"command\": [\"wsl.exe\", \"-d\", \"Debian\",  \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] },\n\"kali\":   { \"command\": [\"wsl.exe\", \"-d\", \"kali-linux\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] }"}],"t":"pre"},{"spans":[{"t":"text","v":"Swap "},{"t":"code","v":"Ubuntu"},{"t":"text","v":"/"},{"t":"code","v":"Debian"},{"t":"text","v":"/"},{"t":"code","v":"kali-linux"},{"t":"text","v":" for the exact name from "},{"t":"code","v":"wsl.exe -l -q"},{"t":"text","v":". "},{"t":"code","v":"--cd ~"},{"t":"text","v":" starts in the distro's home; drop it to inherit the broker's cwd."}],"t":"p"},{"spans":[{"t":"text","v":"Windows → PowerShell / cmd / Git-Bash"}],"t":"sub"},{"spans":[{"t":"code","v":"\"pwsh\":       { \"command\": [\"pwsh\", \"-NoLogo\"] },                 // PowerShell 7+\n\"powershell\": { \"command\": [\"powershell.exe\", \"-NoLogo\", \"-NoProfile\"] },\n\"cmd\":        { \"command\": [\"cmd.exe\"] },\n\"git-bash\":   { \"command\": [\"C:\\\\Program Files\\\\Git\\\\bin\\\\bash.exe\", \"-l\", \"-i\"] }"}],"t":"pre"},{"spans":[{"t":"code","v":"-NoProfile"},{"t":"text","v":" skips your PowerShell profile scripts (faster, cleaner). Adjust the Git-Bash path to your install."}],"t":"p"},{"spans":[{"t":"text","v":"Linux / macOS"}],"t":"sub"},{"spans":[{"t":"code","v":"\"bash\": { \"command\": [\"bash\", \"-l\"] },\n\"zsh\":  { \"command\": [\"zsh\",  \"-l\"] },\n\"fish\": { \"command\": [\"fish\", \"-l\"] },\n\"sh\":   { \"command\": [\"sh\",   \"-l\"] }"}],"t":"pre"},{"spans":[{"t":"code","v":"-l"},{"t":"text","v":" makes it a login shell (sources your profile / rc). "},{"t":"code","v":"Detect…"},{"t":"text","v":" proposes each of these that is actually installed."}],"t":"p"},{"spans":[{"t":"text","v":"Launching an app instead of a shell"}],"t":"sub"},{"spans":[{"t":"text","v":"Nothing says "},{"t":"code","v":"command"},{"t":"text","v":" has to be a shell. A profile can launch a TUI or an agent CLI directly, so the window is the app:"}],"t":"p"},{"spans":[{"t":"code","v":"// A TUI, straight into the window — no shell in between.\n\"btop\":    { \"command\": [\"btop\"], \"title\": \"btop\" },\n\"lazygit\": { \"command\": [\"wsl.exe\", \"-d\", \"Ubuntu\", \"--cd\", \"~\", \"--\", \"lazygit\"] },\n\n// Needs login-shell PATH (nvm / pyenv / ~/.local/bin) before the name resolves:\n// -l sources your rc files, -c runs the app, and `exec` keeps the shell out of\n// the way so quitting the app ends the session instead of dropping to a prompt.\n\"claude\":  { \"command\": [\"wsl.exe\", \"-d\", \"Ubuntu\", \"--cd\", \"~\", \"--\",\n                         \"bash\", \"-lc\", \"exec claude\"], \"color\": \"#c96442\" },\n\n// A remote TUI: -t forces a PTY, without which curses apps refuse to start.\n\"htop-lab\": { \"command\": [\"ssh\", \"-t\", \"user@lab\", \"htop\"] }"}],"t":"pre"},{"spans":[{"t":"text","v":"A profile that launches an app behaves differently from one that launches a shell, in ways worth knowing before you wonder what broke:"}],"t":"p"},{"spans":[{"t":"strong","v":"There is no prompt to come back to."},{"t":"text","v":" When the app exits, the session exits"}],"t":"bullet"},{"spans":[{"t":"text","v":"with it and the window closes. A shell profile survives the app and leaves you a prompt; this does not."}],"t":"p"},{"spans":[{"t":"strong","v":"The mouse is usually gone from the first frame."},{"t":"text","v":" Most full-screen apps turn"}],"t":"bullet"},{"spans":[{"t":"text","v":"on mouse reporting during startup, so drag-select is dead before you ever get a chance to try it. "},{"t":"code","v":"Shift"},{"t":"text","v":"-drag ("},{"t":"code","v":"Option"},{"t":"text","v":"-drag on macOS) forces a selection and "},{"t":"code","v":"Shift+scroll"},{"t":"text","v":" still reaches scrollback — see Keyboard Shortcuts. The "},{"t":"strong","v":"Mouse mode chip"},{"t":"text","v":" mod puts a 🖱 in the title bar while that is the case."}],"t":"p"},{"spans":[{"t":"strong","v":"No shell means no rc files."},{"t":"text","v":" "},{"t":"code","v":"[\"claude\"]"},{"t":"text","v":" only works if "},{"t":"code","v":"claude"},{"t":"text","v":" is already"}],"t":"bullet"},{"spans":[{"t":"text","v":"on the broker's own "},{"t":"code","v":"PATH"},{"t":"text","v":"; anything installed by a login shell needs the "},{"t":"code","v":"bash -lc \"exec …\""},{"t":"text","v":" form above."}],"t":"p"},{"spans":[{"t":"code","v":"Detect…"},{"t":"strong","v":" will not propose these."},{"t":"text","v":" It scans for shells and WSL distros, so"}],"t":"bullet"},{"spans":[{"t":"text","v":"app profiles are always hand-added."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"},{"spans":[{"t":"text","v":"See also: "},{"t":"strong","v":"[[Setup-and-Onboarding]]"},{"t":"text","v":" (config basics) and "},{"t":"strong","v":"[[Technical-Reference]]"},{"t":"text","v":" (endpoint reference, auth model)."}],"t":"p"}],"search":"recipe catalog copy a command into the add-profile dialog (one token per line) or into broker_config.json 's agent.profiles . windows → wsl // ubuntu (detect… fills these in for every installed distro) \"ubuntu\": { \"command\": [\"wsl.exe\", \"-d\", \"ubuntu\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] }, \"debian\": { \"command\": [\"wsl.exe\", \"-d\", \"debian\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] }, \"kali\": { \"command\": [\"wsl.exe\", \"-d\", \"kali-linux\", \"--cd\", \"~\", \"--\", \"bash\", \"-l\"] } swap ubuntu / debian / kali-linux for the exact name from wsl.exe -l -q . --cd ~ starts in the distro's home; drop it to inherit the broker's cwd. windows → powershell / cmd / git-bash \"pwsh\": { \"command\": [\"pwsh\", \"-nologo\"] }, // powershell 7+ \"powershell\": { \"command\": [\"powershell.exe\", \"-nologo\", \"-noprofile\"] }, \"cmd\": { \"command\": [\"cmd.exe\"] }, \"git-bash\": { \"command\": [\"c:\\\\program files\\\\git\\\\bin\\\\bash.exe\", \"-l\", \"-i\"] } -noprofile skips your powershell profile scripts (faster, cleaner). adjust the git-bash path to your install. linux / macos \"bash\": { \"command\": [\"bash\", \"-l\"] }, \"zsh\": { \"command\": [\"zsh\", \"-l\"] }, \"fish\": { \"command\": [\"fish\", \"-l\"] }, \"sh\": { \"command\": [\"sh\", \"-l\"] } -l makes it a login shell (sources your profile / rc). detect… proposes each of these that is actually installed. launching an app instead of a shell nothing says command has to be a shell. a profile can launch a tui or an agent cli directly, so the window is the app: // a tui, straight into the window — no shell in between. \"btop\": { \"command\": [\"btop\"], \"title\": \"btop\" }, \"lazygit\": { \"command\": [\"wsl.exe\", \"-d\", \"ubuntu\", \"--cd\", \"~\", \"--\", \"lazygit\"] }, // needs login-shell path (nvm / pyenv / ~/.local/bin) before the name resolves: // -l sources your rc files, -c runs the app, and `exec` keeps the shell out of // the way so quitting the app ends the session instead of dropping to a prompt. \"claude\": { \"command\": [\"wsl.exe\", \"-d\", \"ubuntu\", \"--cd\", \"~\", \"--\", \"bash\", \"-lc\", \"exec claude\"], \"color\": \"#c96442\" }, // a remote tui: -t forces a pty, without which curses apps refuse to start. \"htop-lab\": { \"command\": [\"ssh\", \"-t\", \"user@lab\", \"htop\"] } a profile that launches an app behaves differently from one that launches a shell, in ways worth knowing before you wonder what broke: there is no prompt to come back to. when the app exits, the session exits with it and the window closes. a shell profile survives the app and leaves you a prompt; this does not. the mouse is usually gone from the first frame. most full-screen apps turn on mouse reporting during startup, so drag-select is dead before you ever get a chance to try it. shift -drag ( option -drag on macos) forces a selection and shift+scroll still reaches scrollback — see keyboard shortcuts. the mouse mode chip mod puts a 🖱 in the title bar while that is the case. no shell means no rc files. [\"claude\"] only works if claude is already on the broker's own path ; anything installed by a login shell needs the bash -lc \"exec …\" form above. detect… will not propose these. it scans for shells and wsl distros, so app profiles are always hand-added. --- see also: [[setup-and-onboarding]] (config basics) and [[technical-reference]] (endpoint reference, auth model).","title":"Recipe catalog"}],"label":"Launch Profiles","order":16,"slug":"launch-profiles","tier":"dev"},{"cards":[{"body":[{"spans":[{"t":"text","v":"A "},{"t":"strong","v":"mod"},{"t":"text","v":" is a folder of JavaScript (plus optional CSS and an in-app help page) that extends the Browserland desktop: taskbar chips, app-window kinds, Control Panel settings, per-terminal title-bar widgets, menu items, key actions. A set of them ships in the repo, under "},{"t":"code","v":"webterm/broker/mods/"},{"t":"text","v":" (the README lists them), and a broker can also be handed one at runtime through "},{"t":"code","v":"POST /mods/install"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"This document is the authoring contract. It is derived from "},{"t":"code","v":"webterm/broker/86_js_mod_loader.js"},{"t":"text","v":", "},{"t":"code","v":"86a_js_mod_settings_text.js"},{"t":"text","v":", "},{"t":"code","v":"86b_js_mod_packages.js"},{"t":"text","v":", "},{"t":"code","v":"ui.py"},{"t":"text","v":", "},{"t":"code","v":"modinstall.py"},{"t":"text","v":" and "},{"t":"code","v":"help_corpus.py"},{"t":"text","v":" — when this file and those disagree, "},{"t":"strong","v":"they are right and this is stale"},{"t":"text","v":"; please fix it."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"overview a mod is a folder of javascript (plus optional css and an in-app help page) that extends the browserland desktop: taskbar chips, app-window kinds, control panel settings, per-terminal title-bar widgets, menu items, key actions. a set of them ships in the repo, under webterm/broker/mods/ (the readme lists them), and a broker can also be handed one at runtime through post /mods/install . this document is the authoring contract. it is derived from webterm/broker/86_js_mod_loader.js , 86a_js_mod_settings_text.js , 86b_js_mod_packages.js , ui.py , modinstall.py and help_corpus.py — when this file and those disagree, they are right and this is stale ; please fix it. ---","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"A mod is "},{"t":"strong","v":"same-origin code running inside the desktop page, with the page's full authority"},{"t":"text","v":". It is not isolated, not sandboxed, and not confined by anything in the "},{"t":"code","v":"ctx"},{"t":"text","v":" object it is handed."}],"t":"p"},{"spans":[{"t":"text","v":"Concretely, a mod can:"}],"t":"p"},{"spans":[{"t":"text","v":"read the browser's auth token, and every configured remote host's token;"}],"t":"bullet"},{"spans":[{"t":"code","v":"fetch"},{"t":"text","v":" "},{"t":"code","v":"/file/write"},{"t":"text","v":", "},{"t":"code","v":"/file/delete"},{"t":"text","v":", "},{"t":"code","v":"/session/kill"},{"t":"text","v":", "},{"t":"code","v":"/state"},{"t":"text","v":", "},{"t":"code","v":"/mcp/*"}],"t":"bullet"},{"spans":[{"t":"text","v":"and every other route directly, exactly as core does;"}],"t":"p"},{"spans":[{"t":"text","v":"reach into "},{"t":"code","v":"prefs"},{"t":"text","v":", "},{"t":"code","v":"windows"},{"t":"text","v":", the layout blob and every other core global,"}],"t":"bullet"},{"spans":[{"t":"text","v":"because they are ordinary bindings in the same script scope;"}],"t":"p"},{"spans":[{"t":"text","v":"rewrite "},{"t":"code","v":"window.__mods"},{"t":"text","v":" itself, including the loader functions that are"}],"t":"bullet"},{"spans":[{"t":"text","v":"supposed to police it."}],"t":"p"},{"spans":[{"t":"text","v":"So "},{"t":"code","v":"ctx"},{"t":"strong","v":" grants no privilege a mod does not already have"},{"t":"text","v":". It is a set of reviewed choke points, not a permission boundary. Its purpose is that \"don't merge a mod that reads "},{"t":"code","v":"~/.ssh"},{"t":"text","v":"\" stays a decision a reviewer can make from one call site instead of auditing every line. The loader says this itself, in the fragment header and again on "},{"t":"code","v":"ctx.file"},{"t":"text","v":", "},{"t":"code","v":"ctx.serverStore"},{"t":"text","v":", "},{"t":"code","v":"ctx.session"},{"t":"text","v":" and "},{"t":"code","v":"ctx.clipboard"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The consequence for the runtime-install feature is the whole of its trust model: "},{"t":"strong","v":"the trust event is the operator's token-gated install, not the fetch that follows."},{"t":"text","v":" "},{"t":"code","v":"POST /mods/install"},{"t":"text","v":" requires the browser auth token; the "},{"t":"code","v":"/mods/<id>/<gen>/<file>"},{"t":"text","v":" route the page then loads from is public, and has to be, because a "},{"t":"code","v":"<script src>"},{"t":"text","v":" cannot carry an "},{"t":"code","v":"Authorization"},{"t":"text","v":" header. Installing a mod is choosing to run somebody's code with this broker's full authority. Review it the way you would review a patch to the broker itself."}],"t":"p"},{"spans":[{"t":"text","v":"Two corollaries that people get wrong:"}],"t":"p"},{"spans":[{"t":"code","v":"tiers"},{"t":"strong","v":" is declared, not enforced"},{"t":"text","v":" — see §5."}],"t":"bullet"},{"spans":[{"t":"text","v":"Nothing in this document is a security guarantee. The caps, the filename"}],"t":"bullet"},{"spans":[{"t":"text","v":"grammar and the CSS-egress check in the install validator are hygiene and defence-in-depth; a mod's own JS can "},{"t":"code","v":"fetch()"},{"t":"text","v":" anything it likes once it runs."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"1. read this first: `ctx` is not a sandbox a mod is same-origin code running inside the desktop page, with the page's full authority . it is not isolated, not sandboxed, and not confined by anything in the ctx object it is handed. concretely, a mod can: read the browser's auth token, and every configured remote host's token; fetch /file/write , /file/delete , /session/kill , /state , /mcp/* and every other route directly, exactly as core does; reach into prefs , windows , the layout blob and every other core global, because they are ordinary bindings in the same script scope; rewrite window.__mods itself, including the loader functions that are supposed to police it. so ctx grants no privilege a mod does not already have . it is a set of reviewed choke points, not a permission boundary. its purpose is that \"don't merge a mod that reads ~/.ssh \" stays a decision a reviewer can make from one call site instead of auditing every line. the loader says this itself, in the fragment header and again on ctx.file , ctx.serverstore , ctx.session and ctx.clipboard . the consequence for the runtime-install feature is the whole of its trust model: the trust event is the operator's token-gated install, not the fetch that follows. post /mods/install requires the browser auth token; the /mods/<id>/<gen>/<file> route the page then loads from is public, and has to be, because a <script src> cannot carry an authorization header. installing a mod is choosing to run somebody's code with this broker's full authority. review it the way you would review a patch to the broker itself. two corollaries that people get wrong: tiers is declared, not enforced — see §5. nothing in this document is a security guarantee. the caps, the filename grammar and the css-egress check in the install validator are hygiene and defence-in-depth; a mod's own js can fetch() anything it likes once it runs. ---","title":"1. Read this first: `ctx` is not a sandbox"},{"body":[{"spans":[{"t":"text","v":" · "},{"t":"strong","v":"Shipped"},{"t":"text","v":" · "},{"t":"strong","v":"Installed"}],"t":"sub"},{"spans":[{"t":"text","v":"Lives in — "},{"t":"code","v":"webterm/broker/mods/<id>/"},{"t":"text","v":" (in the repo) — "},{"t":"code","v":"<mods_dir>/<id>/<gen>/"},{"t":"text","v":" on the broker (config "},{"t":"code","v":"mods_dir"},{"t":"text","v":", default "},{"t":"code","v":"<state dir>/webterm_mods"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"text","v":"Reaches the page by — being spliced into the one inline "},{"t":"code","v":"<script>"},{"t":"text","v":" at import ("},{"t":"code","v":"ui._MODS"},{"t":"text","v":") — a separate classic "},{"t":"code","v":"<script src=\"/mods/<id>/<gen>/<file>.js\">"},{"t":"text","v":" injected at boot"}],"t":"bullet"},{"spans":[{"t":"text","v":"Id — must "},{"t":"strong","v":"not"},{"t":"text","v":" start with "},{"t":"code","v":"x-"},{"t":"text","v":" — "},{"t":"strong","v":"must"},{"t":"text","v":" start with "},{"t":"code","v":"x-"}],"t":"bullet"},{"spans":[{"t":"text","v":"Load list — "},{"t":"code","v":"ui._MODS"},{"t":"text","v":", an explicit list — "},{"t":"code","v":"/info"},{"t":"text","v":"'s "},{"t":"code","v":"mods"},{"t":"text","v":" catalog, from the broker's in-memory index"}],"t":"bullet"},{"spans":[{"t":"text","v":"Manifest names its scripts via — nothing ("},{"t":"code","v":"entry"},{"t":"text","v":" is inert; "},{"t":"code","v":"_MODS"},{"t":"text","v":" decides) — "},{"t":"code","v":"scripts"},{"t":"text","v":", which is "},{"t":"strong","v":"required"}],"t":"bullet"},{"spans":[{"t":"text","v":"Default enabled — whatever "},{"t":"code","v":"defaultEnabled"},{"t":"text","v":" says — always "},{"t":"code","v":"false"},{"t":"text","v":", whatever the manifest says"}],"t":"bullet"},{"spans":[{"t":"text","v":"Changing it needs — a broker restart (the page is assembled at import) — a page reload (never a process restart)"}],"t":"bullet"},{"spans":[{"t":"text","v":"Its source is — public (it is in "},{"t":"code","v":"GET /"},{"t":"text","v":") — public ("},{"t":"code","v":"GET /mods/<id>/<gen>/<file>"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"text","v":"Everything in §3–§9 applies to both. §10 is the extra contract an installed mod has to meet."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"2. the two kinds of mod · shipped · installed lives in — webterm/broker/mods/<id>/ (in the repo) — <mods_dir>/<id>/<gen>/ on the broker (config mods_dir , default <state dir>/webterm_mods ) reaches the page by — being spliced into the one inline <script> at import ( ui._mods ) — a separate classic <script src=\"/mods/<id>/<gen>/<file>.js\"> injected at boot id — must not start with x- — must start with x- load list — ui._mods , an explicit list — /info 's mods catalog, from the broker's in-memory index manifest names its scripts via — nothing ( entry is inert; _mods decides) — scripts , which is required default enabled — whatever defaultenabled says — always false , whatever the manifest says changing it needs — a broker restart (the page is assembled at import) — a page reload (never a process restart) its source is — public (it is in get / ) — public ( get /mods/<id>/<gen>/<file> ) everything in §3–§9 applies to both. §10 is the extra contract an installed mod has to meet. ---","title":"2. The two kinds of mod"},{"body":[{"spans":[{"t":"code","v":"mods/clock/clock.js"},{"t":"text","v":" is the reference mod; this is its shape:"}],"t":"p"},{"spans":[{"t":"code","v":"registerMod({\n    id: 'clock',\n    version: '1.0.0',\n    ctxVersion: 1,\n    tiers: ['taskbar', 'settings'],\n    init: function (ctx) {\n        const chip = document.createElement('div');\n        chip.id = 'clock-chip';\n        ctx.taskbar.addStatusItem(chip);      // removed automatically on teardown\n\n        let timer = setInterval(function () {\n            chip.textContent = new Date().toLocaleTimeString();\n        }, 1000);\n        ctx.onUnload(function () { clearInterval(timer); });\n    },\n});"}],"t":"pre"},{"spans":[{"t":"text","v":"The rules that shape it:"}],"t":"p"},{"spans":[{"t":"strong","v":"Top level does nothing but call "},{"t":"code","v":"registerMod({…})"},{"t":"strong","v":"."},{"t":"text","v":" Registration is"}],"t":"bullet"},{"spans":[{"t":"text","v":"bookkeeping; all work happens in "},{"t":"code","v":"init(ctx)"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"init"},{"t":"strong","v":" is synchronous by contract."},{"t":"text","v":" "},{"t":"code","v":"initMod"},{"t":"text","v":" calls it and *ignores any"}],"t":"bullet"},{"spans":[{"t":"text","v":"returned promise*. Kick async work off inside it, but everything another mod or core might observe (a registered window kind, a settings control) must exist by the time it returns."}],"t":"p"},{"spans":[{"t":"strong","v":"Everything "},{"t":"code","v":"init"},{"t":"strong","v":" does must be reversible."},{"t":"text","v":" A mod can be disabled live from"}],"t":"bullet"},{"spans":[{"t":"text","v":"Control Panel → Mods, and its teardown has to leave no timers, listeners or DOM behind. "},{"t":"code","v":"ctx.onUnload(fn)"},{"t":"text","v":" registers a teardown; they run LIFO. Most "},{"t":"code","v":"ctx"},{"t":"text","v":" primitives auto-register their own teardown (the taskbar item above removes itself), so you only need "},{"t":"code","v":"onUnload"},{"t":"text","v":" for what you created yourself."}],"t":"p"},{"spans":[{"t":"strong","v":"A throw in "},{"t":"code","v":"init"},{"t":"strong","v":" disables only that mod."},{"t":"text","v":" "},{"t":"code","v":"initMod"},{"t":"text","v":" catches it, runs the"}],"t":"bullet"},{"spans":[{"t":"text","v":"partial teardown, releases the slot and logs; core and every other mod keep running. That isolation is why the \"make it throw\" bugs in §11 are so easy to ship without noticing."}],"t":"p"},{"spans":[{"t":"code","v":"registerMod"},{"t":"text","v":" itself throws — synchronously, as your script executes — on a malformed declaration (no object, no string "},{"t":"code","v":"id"},{"t":"text","v":", no function "},{"t":"code","v":"init"},{"t":"text","v":") or a duplicate id. Fields it normalizes: "},{"t":"code","v":"version"},{"t":"text","v":" (non-string → "},{"t":"code","v":"'0'"},{"t":"text","v":"), "},{"t":"code","v":"ctxVersion"},{"t":"text","v":" (non-number → "},{"t":"code","v":"null"},{"t":"text","v":", meaning \"pin nothing\"), "},{"t":"code","v":"tiers"},{"t":"text","v":" and "},{"t":"code","v":"requires"},{"t":"text","v":" (non-arrays → "},{"t":"code","v":"[]"},{"t":"text","v":", non-string members dropped), "},{"t":"code","v":"defaultEnabled"},{"t":"text","v":" (anything but literal "},{"t":"code","v":"false"},{"t":"text","v":" → "},{"t":"code","v":"true"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"3. the smallest mod that works mods/clock/clock.js is the reference mod; this is its shape: registermod({ id: 'clock', version: '1.0.0', ctxversion: 1, tiers: ['taskbar', 'settings'], init: function (ctx) { const chip = document.createelement('div'); chip.id = 'clock-chip'; ctx.taskbar.addstatusitem(chip); // removed automatically on teardown let timer = setinterval(function () { chip.textcontent = new date().tolocaletimestring(); }, 1000); ctx.onunload(function () { clearinterval(timer); }); }, }); the rules that shape it: top level does nothing but call registermod({…}) . registration is bookkeeping; all work happens in init(ctx) . init is synchronous by contract. initmod calls it and *ignores any returned promise*. kick async work off inside it, but everything another mod or core might observe (a registered window kind, a settings control) must exist by the time it returns. everything init does must be reversible. a mod can be disabled live from control panel → mods, and its teardown has to leave no timers, listeners or dom behind. ctx.onunload(fn) registers a teardown; they run lifo. most ctx primitives auto-register their own teardown (the taskbar item above removes itself), so you only need onunload for what you created yourself. a throw in init disables only that mod. initmod catches it, runs the partial teardown, releases the slot and logs; core and every other mod keep running. that isolation is why the \"make it throw\" bugs in §11 are so easy to ship without noticing. registermod itself throws — synchronously, as your script executes — on a malformed declaration (no object, no string id , no function init ) or a duplicate id. fields it normalizes: version (non-string → '0' ), ctxversion (non-number → null , meaning \"pin nothing\"), tiers and requires (non-arrays → [] , non-string members dropped), defaultenabled (anything but literal false → true ). ---","title":"3. The smallest mod that works"},{"body":[{"spans":[{"t":"text","v":"Every shipped mod has one. It is read by "},{"t":"strong","v":"Python"},{"t":"text","v":" — "},{"t":"code","v":"ui._mod_css"},{"t":"text","v":", "},{"t":"code","v":"ui.mod_catalog"},{"t":"text","v":", "},{"t":"code","v":"help_corpus.build_mod_sections"},{"t":"text","v":" — and never by the loader. The "},{"t":"code","v":"registerMod({…})"},{"t":"text","v":" call is read by the "},{"t":"strong","v":"loader"},{"t":"text","v":" and never by Python. Some fields therefore exist in both places, and a test ("},{"t":"code","v":"test_mod_catalog_matches_the_registerMod_declarations"},{"t":"text","v":") keeps the copies honest in both directions."}],"t":"p"},{"spans":[{"t":"text","v":"Every field at once — no real manifest uses all of them:"}],"t":"p"},{"spans":[{"t":"code","v":"{\n  \"id\": \"notes\",                    // the id; matches the directory name by convention\n  \"version\": \"1.0.0\",\n  \"ctxVersion\": 1,\n  \"title\": \"Notes\",                 // shown in the catalog / remote policy editor\n  \"description\": \"A short sentence an operator will read.\",\n  \"defaultEnabled\": false,          // optional; absent == true\n  \"requires\": [\"editor\"],           // optional; ids of mods that must be ACTIVE\n  \"styles\": [\"notes.css\"],          // optional; bare filenames in this mod's dir\n  \"permissions\": [\"file\"],          // closed vocabulary; checked only via the install door — see §10.5\n  \"help\": {                         // optional; only meaningful with a help.md\n    \"slug\": \"notes\", \"label\": \"Notes\", \"order\": 2100, \"icon\": \"📓\"\n  },\n  \"entry\": \"notes.js\",              // INERT — see below\n  \"author\": \"JohnConnorNPC\"         // INERT — see below\n}"}],"t":"pre"},{"spans":[{"t":"text","v":"Field by field:"}],"t":"p"},{"spans":[{"t":"code","v":"id"},{"t":"text","v":" — used by "},{"t":"code","v":"ui.mod_catalog()"},{"t":"text","v":" (falling back to the directory name if"}],"t":"bullet"},{"spans":[{"t":"text","v":"absent or non-string) and by "},{"t":"code","v":"help_corpus"},{"t":"text","v":" as the section's mod tag. It must match the "},{"t":"code","v":"id"},{"t":"text","v":" in "},{"t":"code","v":"registerMod"},{"t":"text","v":" or the catalog and the loader disagree."}],"t":"p"},{"spans":[{"t":"code","v":"version"},{"t":"strong","v":", "},{"t":"code","v":"title"},{"t":"strong","v":", "},{"t":"code","v":"description"},{"t":"text","v":" — catalog text. "},{"t":"code","v":"title"},{"t":"text","v":" falls back to"}],"t":"bullet"},{"spans":[{"t":"text","v":"the id. These are what another broker's Control Panel shows when it edits this broker's mod pins, so write "},{"t":"code","v":"description"},{"t":"text","v":" for an operator, not for yourself."}],"t":"p"},{"spans":[{"t":"code","v":"ctxVersion"},{"t":"text","v":" — informational in "},{"t":"code","v":"mod.json"},{"t":"text","v":". The one that acts is the"}],"t":"bullet"},{"spans":[{"t":"text","v":"number in "},{"t":"code","v":"registerMod"},{"t":"text","v":": a mismatch with the loader's "},{"t":"code","v":"window.__mods.ctxVersion"},{"t":"text","v":" makes "},{"t":"code","v":"initMod"},{"t":"text","v":" refuse the mod outright, and omitting it means \"pin nothing\". The contract is at "},{"t":"code","v":"1"},{"t":"text","v":", and every capability added to "},{"t":"code","v":"ctx"},{"t":"text","v":" since has been additive — so "},{"t":"strong","v":"feature-detect"},{"t":"text","v":" ("},{"t":"code","v":"if (ctx.theme)"},{"t":"text","v":") rather than declaring a higher number."}],"t":"p"},{"spans":[{"t":"code","v":"defaultEnabled"},{"t":"text","v":" — "},{"t":"code","v":"false"},{"t":"text","v":" ships the mod off until the operator opts in."}],"t":"bullet"},{"spans":[{"t":"text","v":"Absent means on. Mirrored in "},{"t":"code","v":"registerMod"},{"t":"text","v":". For an "},{"t":"strong","v":"installed"},{"t":"text","v":" mod this field is accepted and then ignored: the broker always reports "},{"t":"code","v":"default_enabled: false"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"requires"},{"t":"text","v":" — see §6."}],"t":"bullet"},{"spans":[{"t":"code","v":"styles"},{"t":"text","v":" — the only field that changes the served bytes. Each entry must"}],"t":"bullet"},{"spans":[{"t":"text","v":"be a "},{"t":"strong","v":"bare"},{"t":"text","v":" "},{"t":"code","v":"<file>.css"},{"t":"text","v":" name ("},{"t":"code","v":"ui._is_bare_css"},{"t":"text","v":" rejects "},{"t":"code","v":"/"},{"t":"text","v":", "},{"t":"code","v":"\\"},{"t":"text","v":", "},{"t":"code","v":".."},{"t":"text","v":", absolute paths, nested dirs and non-"},{"t":"code","v":".css"},{"t":"text","v":"), must exist, must be valid UTF-8 with "},{"t":"strong","v":"no BOM"},{"t":"text","v":", must "},{"t":"strong","v":"end in a newline"},{"t":"text","v":", and must be ≤ "},{"t":"code","v":"ui._MAX_LINES"},{"t":"text","v":" (2500) lines. Any failure is logged and the stylesheet is silently dropped from the page — the strict version of the same check lives in "},{"t":"code","v":"tests/test_ui_assets.py"},{"t":"text","v":" and fails CI."}],"t":"p"},{"spans":[{"t":"code","v":"permissions"},{"t":"text","v":" — a closed-vocabulary declaration of which "},{"t":"code","v":"ctx"},{"t":"text","v":" capability"}],"t":"bullet"},{"spans":[{"t":"text","v":"families this package's source actually uses. Every shipped "},{"t":"code","v":"mod.json"},{"t":"text","v":" carries one, and it is read twice: the broker joins it onto the shipped rows of the "},{"t":"code","v":"/info"},{"t":"text","v":" catalog, so Control Panel → Mods can show it, and "},{"t":"code","v":"tests/test_ui_assets.py"},{"t":"text","v":" sweeps "},{"t":"code","v":"mods/**"},{"t":"text","v":" to keep every declaration truthful and inside the vocabulary. What the shipped copy does not get is the install-time check: only the "},{"t":"strong","v":"installed"},{"t":"text","v":"-mod door ("},{"t":"code","v":"modinstall.validate_package"},{"t":"text","v":", run by both "},{"t":"code","v":"POST /mods/install"},{"t":"text","v":" and the "},{"t":"code","v":"mods_dir"},{"t":"text","v":" scanner) lints a declaration against the source at the moment it is stored, which is why the pane words the two cases differently. The full rules — the vocabulary, the refusal, the absent-vs-"},{"t":"code","v":"[]"},{"t":"text","v":" distinction — are in §10.5."}],"t":"p"},{"spans":[{"t":"code","v":"help"},{"t":"text","v":" — "},{"t":"code","v":"slug"},{"t":"text","v":" / "},{"t":"code","v":"label"},{"t":"text","v":" / "},{"t":"code","v":"order"},{"t":"text","v":" / "},{"t":"code","v":"icon"},{"t":"text","v":" for the in-app Help section"}],"t":"bullet"},{"spans":[{"t":"text","v":"(§9). All optional; slug defaults to the mod id, label to "},{"t":"code","v":"title"},{"t":"text","v":", order to a computed value after the wiki pages."}],"t":"p"},{"spans":[{"t":"code","v":"entry"},{"t":"text","v":" — "},{"t":"strong","v":"inert."},{"t":"text","v":" Every shipped manifest declares it, and the test suite"}],"t":"bullet"},{"spans":[{"t":"text","v":"pins its value for each one, but "},{"t":"strong","v":"no runtime code reads it."},{"t":"text","v":" What actually loads is "},{"t":"code","v":"ui._MODS"},{"t":"text","v":", an explicit list of "},{"t":"code","v":"mods/<id>/<file>.js"},{"t":"text","v":" paths. Editing "},{"t":"code","v":"entry"},{"t":"text","v":" changes nothing. (The "},{"t":"code","v":"editor"},{"t":"text","v":" mod ships two scripts, "},{"t":"code","v":"codemirror.js"},{"t":"text","v":" and "},{"t":"code","v":"editor.js"},{"t":"text","v":", which is exactly the case a single "},{"t":"code","v":"entry"},{"t":"text","v":" cannot express.) The installed-mod manifest replaces it with a required "},{"t":"code","v":"scripts"},{"t":"text","v":" "},{"t":"strong","v":"list"},{"t":"text","v":", and the install validator "},{"t":"strong","v":"rejects"},{"t":"text","v":" "},{"t":"code","v":"entry"},{"t":"text","v":" as an unknown key."}],"t":"p"},{"spans":[{"t":"code","v":"author"},{"t":"text","v":" — "},{"t":"strong","v":"inert."},{"t":"text","v":" Every shipped manifest declares it; nothing reads"}],"t":"bullet"},{"spans":[{"t":"text","v":"it, nothing asserts it, and it is not part of the id namespace. Second-level scoping for an installed mod is a convention baked into the id ("},{"t":"code","v":"x-<author>-<name>"},{"t":"text","v":"), not this field — see §10."}],"t":"p"},{"spans":[{"t":"text","v":"Anything else — ignored by the shipped-mod readers, and "},{"t":"strong","v":"rejected"},{"t":"text","v":" by the"}],"t":"bullet"},{"spans":[{"t":"text","v":"install validator ("},{"t":"code","v":"unknown_manifest_key"},{"t":"text","v":"), so a typo is loud rather than silent."}],"t":"p"},{"spans":[{"t":"text","v":"Note what is not in "},{"t":"code","v":"mod.json"},{"t":"text","v":": "},{"t":"code","v":"tiers"},{"t":"text","v":". It lives only in the "},{"t":"code","v":"registerMod"},{"t":"text","v":" call, and "},{"t":"code","v":"ui.mod_catalog()"},{"t":"text","v":" deliberately does not report it."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"4. `mod.json` every shipped mod has one. it is read by python — ui._mod_css , ui.mod_catalog , help_corpus.build_mod_sections — and never by the loader. the registermod({…}) call is read by the loader and never by python. some fields therefore exist in both places, and a test ( test_mod_catalog_matches_the_registermod_declarations ) keeps the copies honest in both directions. every field at once — no real manifest uses all of them: { \"id\": \"notes\", // the id; matches the directory name by convention \"version\": \"1.0.0\", \"ctxversion\": 1, \"title\": \"notes\", // shown in the catalog / remote policy editor \"description\": \"a short sentence an operator will read.\", \"defaultenabled\": false, // optional; absent == true \"requires\": [\"editor\"], // optional; ids of mods that must be active \"styles\": [\"notes.css\"], // optional; bare filenames in this mod's dir \"permissions\": [\"file\"], // closed vocabulary; checked only via the install door — see §10.5 \"help\": { // optional; only meaningful with a help.md \"slug\": \"notes\", \"label\": \"notes\", \"order\": 2100, \"icon\": \"📓\" }, \"entry\": \"notes.js\", // inert — see below \"author\": \"johnconnornpc\" // inert — see below } field by field: id — used by ui.mod_catalog() (falling back to the directory name if absent or non-string) and by help_corpus as the section's mod tag. it must match the id in registermod or the catalog and the loader disagree. version , title , description — catalog text. title falls back to the id. these are what another broker's control panel shows when it edits this broker's mod pins, so write description for an operator, not for yourself. ctxversion — informational in mod.json . the one that acts is the number in registermod : a mismatch with the loader's window.__mods.ctxversion makes initmod refuse the mod outright, and omitting it means \"pin nothing\". the contract is at 1 , and every capability added to ctx since has been additive — so feature-detect ( if (ctx.theme) ) rather than declaring a higher number. defaultenabled — false ships the mod off until the operator opts in. absent means on. mirrored in registermod . for an installed mod this field is accepted and then ignored: the broker always reports default_enabled: false . requires — see §6. styles — the only field that changes the served bytes. each entry must be a bare <file>.css name ( ui._is_bare_css rejects / , \\ , .. , absolute paths, nested dirs and non- .css ), must exist, must be valid utf-8 with no bom , must end in a newline , and must be ≤ ui._max_lines (2500) lines. any failure is logged and the stylesheet is silently dropped from the page — the strict version of the same check lives in tests/test_ui_assets.py and fails ci. permissions — a closed-vocabulary declaration of which ctx capability families this package's source actually uses. every shipped mod.json carries one, and it is read twice: the broker joins it onto the shipped rows of the /info catalog, so control panel → mods can show it, and tests/test_ui_assets.py sweeps mods/** to keep every declaration truthful and inside the vocabulary. what the shipped copy does not get is the install-time check: only the installed -mod door ( modinstall.validate_package , run by both post /mods/install and the mods_dir scanner) lints a declaration against the source at the moment it is stored, which is why the pane words the two cases differently. the full rules — the vocabulary, the refusal, the absent-vs- [] distinction — are in §10.5. help — slug / label / order / icon for the in-app help section (§9). all optional; slug defaults to the mod id, label to title , order to a computed value after the wiki pages. entry — inert. every shipped manifest declares it, and the test suite pins its value for each one, but no runtime code reads it. what actually loads is ui._mods , an explicit list of mods/<id>/<file>.js paths. editing entry changes nothing. (the editor mod ships two scripts, codemirror.js and editor.js , which is exactly the case a single entry cannot express.) the installed-mod manifest replaces it with a required scripts list , and the install validator rejects entry as an unknown key. author — inert. every shipped manifest declares it; nothing reads it, nothing asserts it, and it is not part of the id namespace. second-level scoping for an installed mod is a convention baked into the id ( x-<author>-<name> ), not this field — see §10. anything else — ignored by the shipped-mod readers, and rejected by the install validator ( unknown_manifest_key ), so a typo is loud rather than silent. note what is not in mod.json : tiers . it lives only in the registermod call, and ui.mod_catalog() deliberately does not report it. ---","title":"4. `mod.json`"},{"body":[{"spans":[{"t":"text","v":"Two similarly-shaped declarations answer two different questions. "},{"t":"code","v":"tiers"},{"t":"text","v":" says what a mod claims to touch, and nothing checks it. "},{"t":"code","v":"needs"},{"t":"text","v":" says what ctx surface a mod cannot run without, and the loader refuses to "},{"t":"code","v":"init"},{"t":"text","v":" it if that surface is missing. Read them together — they are opposites, not variants of the same idea."}],"t":"p"},{"spans":[{"t":"code","v":"tiers"},{"t":"text","v":" — declared, not enforced"}],"t":"sub"},{"spans":[{"t":"code","v":"tiers: ['file', 'window'],"}],"t":"pre"},{"spans":[{"t":"code","v":"tiers"},{"t":"text","v":" is a "},{"t":"strong","v":"self-declared"},{"t":"text","v":" list of the "},{"t":"code","v":"ctx"},{"t":"text","v":" capability families a mod uses. The Mods pane renders them as badges next to the enable checkbox so an operator can see what a mod touches before switching it on; a mod that declares none renders as "},{"t":"code","v":"unspecified"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Nothing enforces it. The loader filters out non-strings and stores the rest. A mod that declares "},{"t":"code","v":"tiers: []"},{"t":"text","v":" and then calls "},{"t":"code","v":"ctx.file.delete()"},{"t":"text","v":" is not stopped, warned or logged — because, per §1, it could have called "},{"t":"code","v":"/file/delete"},{"t":"text","v":" directly anyway. "},{"t":"strong","v":"A tier declaration is a claim a reviewer checks, not a capability the runtime grants."}],"t":"p"},{"spans":[{"t":"text","v":"The vocabulary in use across the shipped set is "},{"t":"code","v":"settings"},{"t":"text","v":", "},{"t":"code","v":"taskbar"},{"t":"text","v":", "},{"t":"code","v":"window"},{"t":"text","v":", "},{"t":"code","v":"file"},{"t":"text","v":", "},{"t":"code","v":"session"},{"t":"text","v":", "},{"t":"code","v":"storage"},{"t":"text","v":", "},{"t":"code","v":"clipboard"},{"t":"text","v":". It is open — no list is validated — and it is deliberately imprecise at the edges: "},{"t":"code","v":"mods/mod-sync/mod-sync.js"},{"t":"text","v":" declares "},{"t":"code","v":"tiers: ['settings']"},{"t":"text","v":" while administering other brokers' mod pins, because the vocabulary has no token for that and inventing one would imply an enforcement that does not exist."}],"t":"p"},{"spans":[{"t":"code","v":"needs"},{"t":"text","v":" — a presence gate (#197)"}],"t":"sub"},{"spans":[{"t":"code","v":"needs: ['file', 'windows.onTerminalCreate'],"}],"t":"pre"},{"spans":[{"t":"code","v":"needs"},{"t":"text","v":" is an array of ctx-surface names this mod cannot function without — a bare member name ("},{"t":"code","v":"'file'"},{"t":"text","v":") or a "},{"t":"strong","v":"dotted path"},{"t":"text","v":" into one ("},{"t":"code","v":"'windows.onTerminalCreate'"},{"t":"text","v":"). Unlike "},{"t":"code","v":"tiers"},{"t":"text","v":", it is checked: once "},{"t":"code","v":"initMod"},{"t":"text","v":" starts building this mod's "},{"t":"code","v":"ctx"},{"t":"text","v":", the loader resolves every entry against that live object — "},{"t":"strong","v":"own"},{"t":"text","v":" properties only at each segment, so a mod never \"has\" "},{"t":"code","v":"toString"},{"t":"text","v":" or "},{"t":"code","v":"constructor"},{"t":"text","v":" just because the prototype does, and a member that throws on access counts as absent too. An unmet entry "},{"t":"strong","v":"blocks init outright"},{"t":"text","v":": nothing partially initializes, the claimed slot is released, and the Mods pane row reads "},{"t":"code","v":"blocked (needs windows.onTerminalCreate)"},{"t":"text","v":" — naming every unmet entry — instead of showing the mod as active while it quietly does nothing, which is the failure mode "},{"t":"code","v":"mods/git/git.js"},{"t":"text","v":" guards against today with a plain "},{"t":"code","v":"if (!ctx.windows) return;"},{"t":"text","v":" rather than a declared, pane-visible reason."}],"t":"p"},{"spans":[{"t":"text","v":"That label is a different thing from a "},{"t":"code","v":"requires"},{"t":"text","v":"-blocked row's "},{"t":"code","v":"needs: editor"},{"t":"text","v":" text (§6) even though both use the English word \"needs\": "},{"t":"code","v":"requires"},{"t":"text","v":" names other "},{"t":"strong","v":"mod ids"},{"t":"text","v":" and is checked first, before a "},{"t":"code","v":"ctx"},{"t":"text","v":" is even built; "},{"t":"code","v":"needs"},{"t":"text","v":" names "},{"t":"strong","v":"ctx surface"},{"t":"text","v":" and is checked once "},{"t":"code","v":"init"},{"t":"text","v":" is actually about to run. A mod that fails both is reported on the "},{"t":"code","v":"requires"},{"t":"text","v":" refusal, since that is the one the loader reaches first."}],"t":"p"},{"spans":[{"t":"text","v":"A few things worth being precise about:"}],"t":"p"},{"spans":[{"t":"strong","v":"A pin does not override it."},{"t":"text","v":" A pin is policy — should this mod be on;"}],"t":"bullet"},{"spans":[{"t":"code","v":"needs"},{"t":"text","v":" is a fact about what this build's "},{"t":"code","v":"ctx"},{"t":"text","v":" offers. A pinned-on mod with an unmet need still reads "},{"t":"code","v":"blocked (needs …)"},{"t":"text","v":"; pinning it cannot conjure the missing surface."}],"t":"p"},{"spans":[{"t":"text","v":"**An older loader, or a page built without the ctx-extender registry,"}],"t":"bullet"},{"spans":[{"t":"text","v":"silently drops the field.** "},{"t":"code","v":"needs"},{"t":"text","v":" is normalized the same way "},{"t":"code","v":"tiers"},{"t":"text","v":" and "},{"t":"code","v":"requires"},{"t":"text","v":" are, by a companion fragment ("},{"t":"code","v":"86c_js_mod_ctx_ext.js"},{"t":"text","v":") that a given page may not carry; where it is absent, the gate simply never runs and the mod inits exactly as it would have before "},{"t":"code","v":"needs"},{"t":"text","v":" existed — absence is never an error (#157). Keep your own "},{"t":"code","v":"typeof ctx.x"},{"t":"text","v":" guard as the second line of defence regardless; "},{"t":"code","v":"needs"},{"t":"text","v":" makes the degradation visible, it does not replace it."}],"t":"p"},{"spans":[{"t":"code","v":"needs"},{"t":"strong","v":" is not "},{"t":"code","v":"requires"},{"t":"strong","v":" and not "},{"t":"code","v":"permissions"},{"t":"strong","v":"."},{"t":"text","v":" "},{"t":"code","v":"requires"},{"t":"text","v":" cascades"}],"t":"bullet"},{"spans":[{"t":"text","v":"through pins and take-downs (§6); "},{"t":"code","v":"permissions"},{"t":"text","v":" is an install-time, server-side review lint over source text (§10.5). "},{"t":"code","v":"needs"},{"t":"text","v":" is client-side, checked once at init against the live "},{"t":"code","v":"ctx"},{"t":"text","v":" — it asks what a mod can run against here, not what other mods must be active or what a reviewer permits."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.capabilities"},{"t":"strong","v":" (§8) is the companion read"},{"t":"text","v":" — a mod can inspect what a"}],"t":"bullet"},{"spans":[{"t":"text","v":"build offers instead of only declaring what it requires."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"5. `tiers` and `needs` — declared vs. gated two similarly-shaped declarations answer two different questions. tiers says what a mod claims to touch, and nothing checks it. needs says what ctx surface a mod cannot run without, and the loader refuses to init it if that surface is missing. read them together — they are opposites, not variants of the same idea. tiers — declared, not enforced tiers: ['file', 'window'], tiers is a self-declared list of the ctx capability families a mod uses. the mods pane renders them as badges next to the enable checkbox so an operator can see what a mod touches before switching it on; a mod that declares none renders as unspecified . nothing enforces it. the loader filters out non-strings and stores the rest. a mod that declares tiers: [] and then calls ctx.file.delete() is not stopped, warned or logged — because, per §1, it could have called /file/delete directly anyway. a tier declaration is a claim a reviewer checks, not a capability the runtime grants. the vocabulary in use across the shipped set is settings , taskbar , window , file , session , storage , clipboard . it is open — no list is validated — and it is deliberately imprecise at the edges: mods/mod-sync/mod-sync.js declares tiers: ['settings'] while administering other brokers' mod pins, because the vocabulary has no token for that and inventing one would imply an enforcement that does not exist. needs — a presence gate (#197) needs: ['file', 'windows.onterminalcreate'], needs is an array of ctx-surface names this mod cannot function without — a bare member name ( 'file' ) or a dotted path into one ( 'windows.onterminalcreate' ). unlike tiers , it is checked: once initmod starts building this mod's ctx , the loader resolves every entry against that live object — own properties only at each segment, so a mod never \"has\" tostring or constructor just because the prototype does, and a member that throws on access counts as absent too. an unmet entry blocks init outright : nothing partially initializes, the claimed slot is released, and the mods pane row reads blocked (needs windows.onterminalcreate) — naming every unmet entry — instead of showing the mod as active while it quietly does nothing, which is the failure mode mods/git/git.js guards against today with a plain if (!ctx.windows) return; rather than a declared, pane-visible reason. that label is a different thing from a requires -blocked row's needs: editor text (§6) even though both use the english word \"needs\": requires names other mod ids and is checked first, before a ctx is even built; needs names ctx surface and is checked once init is actually about to run. a mod that fails both is reported on the requires refusal, since that is the one the loader reaches first. a few things worth being precise about: a pin does not override it. a pin is policy — should this mod be on; needs is a fact about what this build's ctx offers. a pinned-on mod with an unmet need still reads blocked (needs …) ; pinning it cannot conjure the missing surface. **an older loader, or a page built without the ctx-extender registry, silently drops the field.** needs is normalized the same way tiers and requires are, by a companion fragment ( 86c_js_mod_ctx_ext.js ) that a given page may not carry; where it is absent, the gate simply never runs and the mod inits exactly as it would have before needs existed — absence is never an error (#157). keep your own typeof ctx.x guard as the second line of defence regardless; needs makes the degradation visible, it does not replace it. needs is not requires and not permissions . requires cascades through pins and take-downs (§6); permissions is an install-time, server-side review lint over source text (§10.5). needs is client-side, checked once at init against the live ctx — it asks what a mod can run against here, not what other mods must be active or what a reviewer permits. ctx.capabilities (§8) is the companion read — a mod can inspect what a build offers instead of only declaring what it requires. ---","title":"5. `tiers` and `needs` — declared vs. gated"},{"body":[{"spans":[{"t":"code","v":"requires: ['editor'],"}],"t":"pre"},{"spans":[{"t":"text","v":"Each listed id must be an "},{"t":"strong","v":"active"},{"t":"text","v":" mod for this one to "},{"t":"code","v":"init"},{"t":"text","v":". If any is missing, "},{"t":"code","v":"initMod"},{"t":"text","v":" returns "},{"t":"code","v":"{ok:false, reason:'requires', missing:[…]}"},{"t":"text","v":" — the mod is cleanly blocked, with no slot claimed and no partial init, and the Mods pane shows "},{"t":"code","v":"needs: editor"},{"t":"text","v":". Enabling the dependency later brings the dependent up through the same cascade ("},{"t":"code","v":"_bringUp"},{"t":"text","v":"); disabling a dependency tears its dependents down first ("},{"t":"code","v":"_takeDown"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"strong","v":"Order is resolved at runtime."},{"t":"text","v":" "},{"t":"code","v":"_topoSortRegistered()"},{"t":"text","v":" (in "},{"t":"code","v":"86b_js_mod_packages.js"},{"t":"text","v":") runs at boot, after the installed packages have loaded and before pins are resolved, and reorders "},{"t":"code","v":"window.__mods.registered"},{"t":"text","v":" "},{"t":"strong","v":"in place"},{"t":"text","v":" so a dependency always precedes its dependents. That in-place reorder is what lets "},{"t":"code","v":"_bringUp"},{"t":"text","v":", "},{"t":"code","v":"_takeDown"},{"t":"text","v":", "},{"t":"code","v":"_applyPolicyLive"},{"t":"text","v":" and "},{"t":"code","v":"_resolvePins"},{"t":"text","v":" keep their single forward/backward passes."}],"t":"p"},{"spans":[{"t":"strong","v":"Kahn's algorithm"},{"t":"text","v":" gives the order, with ties broken by the current"}],"t":"bullet"},{"spans":[{"t":"text","v":"registration index — so when nothing installed participates the sort is the identity permutation and the shipped order is preserved exactly."}],"t":"p"},{"spans":[{"t":"strong","v":"Kahn's residual is not the cycle set."},{"t":"text","v":" For "},{"t":"code","v":"A→B, B→A, C→A"},{"t":"text","v":" the residual is"}],"t":"bullet"},{"spans":[{"t":"code","v":"{A,B,C}"},{"t":"text","v":", but "},{"t":"code","v":"C"},{"t":"text","v":" is merely blocked by a cycle. So the residual is split with "},{"t":"strong","v":"Tarjan"},{"t":"text","v":": an SCC of size > 1 (or a self-loop) is "},{"t":"code","v":"cycle"},{"t":"text","v":", everything else in the residual is "},{"t":"code","v":"blocked-by-cycle"},{"t":"text","v":". "},{"t":"code","v":"initMod"},{"t":"text","v":" refuses a "},{"t":"code","v":"cycle"},{"t":"text","v":" mod with its own "},{"t":"code","v":"{ok:false, reason:'cycle'}"},{"t":"text","v":" rather than letting it read as an ordinary dependency block."}],"t":"p"},{"spans":[{"t":"text","v":"The graph is built over "},{"t":"strong","v":"shipped ∪ installed"},{"t":"text","v":" ids. An edge to an id in"}],"t":"bullet"},{"spans":[{"t":"text","v":"neither is dropped from the sort and recorded in "},{"t":"code","v":"window.__mods.missingRequires"},{"t":"text","v":" — it must not contribute an indegree, or an installed mod requiring a shipped mod would come out marked cyclic. An edge to an id that the broker knows but this page did not register (its package 404'd, or it is off) is not an edge and not \"missing\" either; the row reads "},{"t":"code","v":"blocked"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The broker does the same analysis over the installed half, in"}],"t":"bullet"},{"spans":[{"t":"code","v":"modinstall.catalog()"},{"t":"text","v":" ("},{"t":"code","v":"_kahn"},{"t":"text","v":" + "},{"t":"code","v":"_cycle_members"},{"t":"text","v":"), and stamps each row with "},{"t":"code","v":"error: null | \"requires_cycle\" | \"blocked_by_cycle\""},{"t":"text","v":". The "},{"t":"strong","v":"client's verdict wins"},{"t":"text","v":" where they differ, because the client sees the whole graph and the broker only sorts the installed half."}],"t":"p"},{"spans":[{"t":"strong","v":"For shipped mods the positional rule survives as a style rule."},{"t":"text","v":" "},{"t":"code","v":"test_requires_declared_before_dependency_in_mods_list"},{"t":"text","v":" still asserts that every "},{"t":"code","v":"requires"},{"t":"text","v":" names a known mod appearing strictly earlier in "},{"t":"code","v":"ui._MODS"},{"t":"text","v":". It used to be the only thing standing between the project and a cycle; now it is there because it keeps the shipped half of the graph readable top-to-bottom. It still enforces all three of: the positional order, that the dependency exists, and that a shipped "},{"t":"code","v":"requires"},{"t":"text","v":" never names an "},{"t":"code","v":"x-"},{"t":"text","v":" id (so shipped→installed edges are unrepresentable, which is what lets "},{"t":"code","v":"/info"},{"t":"text","v":" emit every shipped row first)."}],"t":"p"},{"spans":[{"t":"code","v":"requires"},{"t":"text","v":" is rare in the shipped set, and where it appears it is a mod reusing the "},{"t":"code","v":"editor"},{"t":"text","v":" mod's window kind and its single shared CodeMirror build."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"6. `requires`, and how load order is decided requires: ['editor'], each listed id must be an active mod for this one to init . if any is missing, initmod returns {ok:false, reason:'requires', missing:[…]} — the mod is cleanly blocked, with no slot claimed and no partial init, and the mods pane shows needs: editor . enabling the dependency later brings the dependent up through the same cascade ( _bringup ); disabling a dependency tears its dependents down first ( _takedown ). order is resolved at runtime. _toposortregistered() (in 86b_js_mod_packages.js ) runs at boot, after the installed packages have loaded and before pins are resolved, and reorders window.__mods.registered in place so a dependency always precedes its dependents. that in-place reorder is what lets _bringup , _takedown , _applypolicylive and _resolvepins keep their single forward/backward passes. kahn's algorithm gives the order, with ties broken by the current registration index — so when nothing installed participates the sort is the identity permutation and the shipped order is preserved exactly. kahn's residual is not the cycle set. for a→b, b→a, c→a the residual is {a,b,c} , but c is merely blocked by a cycle. so the residual is split with tarjan : an scc of size > 1 (or a self-loop) is cycle , everything else in the residual is blocked-by-cycle . initmod refuses a cycle mod with its own {ok:false, reason:'cycle'} rather than letting it read as an ordinary dependency block. the graph is built over shipped ∪ installed ids. an edge to an id in neither is dropped from the sort and recorded in window.__mods.missingrequires — it must not contribute an indegree, or an installed mod requiring a shipped mod would come out marked cyclic. an edge to an id that the broker knows but this page did not register (its package 404'd, or it is off) is not an edge and not \"missing\" either; the row reads blocked . the broker does the same analysis over the installed half, in modinstall.catalog() ( _kahn + _cycle_members ), and stamps each row with error: null | \"requires_cycle\" | \"blocked_by_cycle\" . the client's verdict wins where they differ, because the client sees the whole graph and the broker only sorts the installed half. for shipped mods the positional rule survives as a style rule. test_requires_declared_before_dependency_in_mods_list still asserts that every requires names a known mod appearing strictly earlier in ui._mods . it used to be the only thing standing between the project and a cycle; now it is there because it keeps the shipped half of the graph readable top-to-bottom. it still enforces all three of: the positional order, that the dependency exists, and that a shipped requires never names an x- id (so shipped→installed edges are unrepresentable, which is what lets /info emit every shipped row first). requires is rare in the shipped set, and where it appears it is a mod reusing the editor mod's window kind and its single shared codemirror build. ---","title":"6. `requires`, and how load order is decided"},{"body":[{"spans":[{"t":"text","v":"There is "},{"t":"strong","v":"no "},{"t":"code","v":"index.html"},{"t":"strong","v":" in this repo."},{"t":"text","v":" "},{"t":"code","v":"ui.py"},{"t":"text","v":" assembles the served page at "},{"t":"strong","v":"import time"},{"t":"text","v":" from the ordered fragments beside it, in five segments:"}],"t":"p"},{"spans":[{"t":"code","v":"core fragments up to 15_css_dialogs.css\n  + every mod stylesheet declared in a mod.json `styles`\n  + the rest of core up to 90_js_mod_boot.js\n  + every path in _MODS, in order\n  + 90_js_mod_boot.js and the tail"}],"t":"pre"},{"spans":[{"t":"text","v":"joined with the "},{"t":"strong","v":"empty string"},{"t":"text","v":" (every fragment already ends in its own newline), and the result is "},{"t":"code","v":"ui.INDEX_HTML"},{"t":"text","v":", captured once onto "},{"t":"code","v":"app.ctx.index_html"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Consequences you will hit:"}],"t":"p"},{"spans":[{"t":"strong","v":"A fragment or mod-script edit needs a broker restart."},{"t":"text","v":" The page is built at"}],"t":"bullet"},{"spans":[{"t":"text","v":"import and cached; nothing re-reads the tree per request. Editing "},{"t":"code","v":"mods/clock/clock.js"},{"t":"text","v":" and reloading the browser shows you the old bytes. This is the single most common way to waste an hour. ("},{"t":"code","v":"curl"},{"t":"text","v":" the served page and grep it if you are unsure what is actually being served.)"}],"t":"p"},{"spans":[{"t":"strong","v":"Every mod script lands in ONE "},{"t":"code","v":"<script>"},{"t":"strong","v":", sharing one scope"},{"t":"text","v":" with core and"}],"t":"bullet"},{"spans":[{"t":"text","v":"with every other mod. Top-level "},{"t":"code","v":"const"},{"t":"text","v":"/"},{"t":"code","v":"let"},{"t":"text","v":"/"},{"t":"code","v":"function"},{"t":"text","v":" names are global. A "},{"t":"code","v":"const"},{"t":"text","v":"/"},{"t":"code","v":"let"},{"t":"text","v":"/"},{"t":"code","v":"class"},{"t":"text","v":" collision is a compile error for the whole bundle — a blank desktop, not one broken mod — and a duplicate "},{"t":"code","v":"function"},{"t":"text","v":"/"},{"t":"code","v":"var"},{"t":"text","v":" is worse, because it silently overwrites. Prefix everything you declare at top level."}],"t":"p"},{"spans":[{"t":"strong","v":"A literal "},{"t":"code","v":"</script>"},{"t":"strong","v":" anywhere in a mod script terminates the element"},{"t":"text","v":" and"}],"t":"bullet"},{"spans":[{"t":"text","v":"breaks the page. Split it ("},{"t":"code","v":"'<' + '/script>'"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"code","v":"_MODS"},{"t":"strong","v":" is an explicit list, not a glob"},{"t":"text","v":", and the drift guard"}],"t":"bullet"},{"spans":[{"t":"text","v":"("},{"t":"code","v":"test_mod_scripts_exist_on_disk_and_match_mods_dir"},{"t":"text","v":") is "},{"t":"strong","v":"bidirectional"},{"t":"text","v":": every path in "},{"t":"code","v":"_MODS"},{"t":"text","v":" must exist, and every "},{"t":"code","v":"*.js"},{"t":"text","v":" under "},{"t":"code","v":"mods/"},{"t":"text","v":" must be declared. A new mod file that is not in "},{"t":"code","v":"_MODS"},{"t":"text","v":" fails CI; so does a stray scratch copy left in a mod directory."}],"t":"p"},{"spans":[{"t":"text","v":"The CSP is `script-src 'sha256-<hash of the inline script>' 'self';"}],"t":"bullet"},{"spans":[{"t":"text","v":"frame-ancestors 'none'"},{"t":"code","v":". The hash is computed from the very bytes served, so adding a mod moves it automatically — but it also means the page must have exactly **one** inline "},{"t":"text","v":"<script>"},{"t":"code","v":", and "},{"t":"text","v":"ui.inline_script_hash` raises when the app is built if that stops being true."}],"t":"p"},{"spans":[{"t":"strong","v":"A mod's CSS is spliced in regardless of whether the mod is enabled."},{"t":"text","v":" It is"}],"t":"bullet"},{"spans":[{"t":"text","v":"present-but-inert: its selectors match nothing until the mod's JS adds the markup. Same posture as the spliced-but-not-initialized JS."}],"t":"p"},{"spans":[{"t":"strong","v":"A shipped mod can never meet a loader other than the one it shipped beside."}],"t":"bullet"},{"spans":[{"t":"code","v":"ui.assemble()"},{"t":"text","v":" puts every "},{"t":"code","v":"_MODS"},{"t":"text","v":" path into the same string as "},{"t":"code","v":"86_js_mod_loader.js"},{"t":"text","v":" and its "},{"t":"code","v":"86*"},{"t":"text","v":" companions, and that one string is what the broker serves. There is no path by which an in-tree mod script is delivered next to some other build's loader, so for a shipped mod \"does this build's "},{"t":"code","v":"ctx"},{"t":"text","v":" have X\" is settled at the commit rather than at runtime — which is what makes a feature-detect fallback in an in-tree mod dead code for anything already in "},{"t":"code","v":"ctx"},{"t":"text","v":" at that commit (see "},{"t":"code","v":"ctx.visibility"},{"t":"text","v":", §8). "},{"t":"strong","v":"An installed package is the opposite case"},{"t":"text","v":" (§10.2): it is fetched as its own "},{"t":"code","v":"<script src>"},{"t":"text","v":" by whichever broker it was installed on, and that broker may be older than the package, so a portable mod keeps its detection or declares "},{"t":"code","v":"needs"},{"t":"text","v":" (§5)."}],"t":"p"},{"spans":[{"t":"text","v":"Adding a shipped mod is therefore: create "},{"t":"code","v":"mods/<id>/"},{"t":"text","v":", write "},{"t":"code","v":"mod.json"},{"t":"text","v":" and "},{"t":"code","v":"<id>.js"},{"t":"text","v":", append the script path to "},{"t":"code","v":"ui._MODS"},{"t":"text","v":" "},{"t":"strong","v":"after any mod it requires"},{"t":"text","v":", and restart the broker."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"7. how a shipped mod reaches the page there is no index.html in this repo. ui.py assembles the served page at import time from the ordered fragments beside it, in five segments: core fragments up to 15_css_dialogs.css + every mod stylesheet declared in a mod.json `styles` + the rest of core up to 90_js_mod_boot.js + every path in _mods, in order + 90_js_mod_boot.js and the tail joined with the empty string (every fragment already ends in its own newline), and the result is ui.index_html , captured once onto app.ctx.index_html . consequences you will hit: a fragment or mod-script edit needs a broker restart. the page is built at import and cached; nothing re-reads the tree per request. editing mods/clock/clock.js and reloading the browser shows you the old bytes. this is the single most common way to waste an hour. ( curl the served page and grep it if you are unsure what is actually being served.) every mod script lands in one <script> , sharing one scope with core and with every other mod. top-level const / let / function names are global. a const / let / class collision is a compile error for the whole bundle — a blank desktop, not one broken mod — and a duplicate function / var is worse, because it silently overwrites. prefix everything you declare at top level. a literal </script> anywhere in a mod script terminates the element and breaks the page. split it ( '<' + '/script>' ). _mods is an explicit list, not a glob , and the drift guard ( test_mod_scripts_exist_on_disk_and_match_mods_dir ) is bidirectional : every path in _mods must exist, and every *.js under mods/ must be declared. a new mod file that is not in _mods fails ci; so does a stray scratch copy left in a mod directory. the csp is `script-src 'sha256-<hash of the inline script>' 'self'; frame-ancestors 'none' . the hash is computed from the very bytes served, so adding a mod moves it automatically — but it also means the page must have exactly **one** inline <script> , and ui.inline_script_hash` raises when the app is built if that stops being true. a mod's css is spliced in regardless of whether the mod is enabled. it is present-but-inert: its selectors match nothing until the mod's js adds the markup. same posture as the spliced-but-not-initialized js. a shipped mod can never meet a loader other than the one it shipped beside. ui.assemble() puts every _mods path into the same string as 86_js_mod_loader.js and its 86* companions, and that one string is what the broker serves. there is no path by which an in-tree mod script is delivered next to some other build's loader, so for a shipped mod \"does this build's ctx have x\" is settled at the commit rather than at runtime — which is what makes a feature-detect fallback in an in-tree mod dead code for anything already in ctx at that commit (see ctx.visibility , §8). an installed package is the opposite case (§10.2): it is fetched as its own <script src> by whichever broker it was installed on, and that broker may be older than the package, so a portable mod keeps its detection or declares needs (§5). adding a shipped mod is therefore: create mods/<id>/ , write mod.json and <id>.js , append the script path to ui._mods after any mod it requires , and restart the broker. ---","title":"7. How a shipped mod reaches the page"},{"body":[{"spans":[{"t":"code","v":"makeCtx(modId, rec)"},{"t":"text","v":" builds a fresh object per mod. Everything below is additive to v1 — "},{"t":"code","v":"ctxVersion"},{"t":"text","v":" stays "},{"t":"code","v":"1"},{"t":"text","v":", so "},{"t":"strong","v":"feature-detect"},{"t":"text","v":" rather than bumping it."}],"t":"p"},{"spans":[{"t":"text","v":"Lifecycle"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.id"},{"t":"text","v":", "},{"t":"code","v":"ctx.ctxVersion"},{"t":"text","v":" — this mod's id, and the loader's contract"}],"t":"bullet"},{"spans":[{"t":"text","v":"version."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.onUnload(fn)"},{"t":"text","v":" — register a teardown. LIFO, each isolated in its own"}],"t":"bullet"},{"spans":[{"t":"code","v":"try"},{"t":"text","v":". Register it "},{"t":"strong","v":"before"},{"t":"text","v":" anything that can throw (§11)."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.visibility"},{"t":"text","v":" — "},{"t":"code","v":"pausableInterval(fn, ms)"},{"t":"text","v":" → a "},{"t":"code","v":"{stop}"},{"t":"text","v":" handle, and"}],"t":"bullet"},{"spans":[{"t":"code","v":"onVisibility(fn)"},{"t":"text","v":" → an unsubscribe. The interval slows to 10× "},{"t":"code","v":"ms"},{"t":"text","v":" while the tab is hidden and coalesces the runs it skipped into exactly one catch-up when the tab comes back (including after a bfcache restore, where no tick fired at all). Both fail closed once the mod is tearing down, and both register their own teardown, so neither can outlive a disable."}],"t":"p"},{"spans":[{"t":"strong","v":"For a shipped mod this is a floor, not a maybe."},{"t":"text","v":" It is a plain member of the object "},{"t":"code","v":"makeCtx"},{"t":"text","v":" builds — not one an extender adds afterwards — so if your "},{"t":"code","v":"init(ctx)"},{"t":"text","v":" is running at all, "},{"t":"code","v":"ctx.visibility"},{"t":"text","v":" is on that "},{"t":"code","v":"ctx"},{"t":"text","v":"; and per §7 an in-tree mod is served beside the very loader that put it there. A "},{"t":"code","v":"ctx.visibility ? … : setInterval(…)"},{"t":"text","v":" fallback in a shipped mod is therefore unreachable code. The ones still carrying such a fallback — "},{"t":"code","v":"mods/aistatus/"},{"t":"text","v":", "},{"t":"code","v":"mods/clock/"},{"t":"text","v":", "},{"t":"code","v":"mods/git/"},{"t":"text","v":", "},{"t":"code","v":"mods/task-manager/"},{"t":"text","v":" and "},{"t":"code","v":"mods/update/"},{"t":"text","v":" — are a migration of their own, not something to copy from."}],"t":"p"},{"spans":[{"t":"strong","v":"The floor stops at the repo."},{"t":"text","v":" An installed package is loaded by whatever broker it was installed on (§7, §10.2), and that loader really may predate "},{"t":"code","v":"ctx.visibility"},{"t":"text","v":" — so portable source keeps the feature detect, or declares "},{"t":"code","v":"needs: ['visibility']"},{"t":"text","v":" (§5) and blocks visibly instead."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.signal"},{"t":"text","v":" (#198) — one "},{"t":"code","v":"AbortSignal"},{"t":"text","v":" per "},{"t":"strong","v":"activation"},{"t":"text","v":", aborted at the"}],"t":"bullet"},{"spans":[{"t":"text","v":"head of teardown. Pass it explicitly to the calls you want cancelled; it has its own section below."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.capabilities"},{"t":"text","v":" (#197) — a "},{"t":"strong","v":"frozen"},{"t":"text","v":", per-mod map, `{family:"}],"t":"bullet"},{"spans":[{"t":"text","v":"<integer version>}"},{"t":"code","v":", of the "},{"t":"text","v":"ctx"},{"t":"code","v":" surface this build's loader actually hands you — e.g. "},{"t":"text","v":"{file: 1, serverStore: 1, windows: 1, settings: 1, theme: 1, …}"},{"t":"code","v":". It exists so a mod can ask \"does this build offer X\" directly instead of sniffing loader-private names the way "},{"t":"text","v":"mod-sync"},{"t":"code","v":" currently does ("},{"t":"text","v":"typeof _pin"},{"t":"code","v":", "},{"t":"text","v":"typeof _modTextOk"},{"t":"code","v":"). Additive on "},{"t":"text","v":"ctxVersion"},{"t":"code","v":" 1 like everything else on this page — feature-detect it ("},{"t":"text","v":"ctx.capabilities && ctx.capabilities.theme"},{"t":"code","v":") before reading it on a build old enough to predate it. The map is built lazily on first read and then frozen: your copy cannot be mutated into lying to you, and mutating it never reaches another mod's copy or the "},{"t":"text","v":"needs"},{"t":"code","v":" gate (§5) any mod is checked against. It is **observed, not promised** — a family whose own ctx extender threw during setup is simply absent from the map, exactly as it is absent from "},{"t":"text","v":"ctx` itself, so the two can never disagree."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.events.on(name, fn)"},{"t":"text","v":" (#195) — subscribe to the core → mod event bus."}],"t":"bullet"},{"spans":[{"t":"text","v":"Returns an unsubscribe fn, which also rides teardown. Five declared events, two of which replay their current state to a late subscriber; the table and its rules are the contract, so read them below before you use it."}],"t":"p"},{"spans":[{"t":"text","v":"Storage"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.storage"},{"t":"text","v":" — "},{"t":"code","v":"get(key)"},{"t":"text","v":" / "},{"t":"code","v":"set(key, value)"},{"t":"text","v":" / "},{"t":"code","v":"remove(key)"},{"t":"text","v":" over"}],"t":"bullet"},{"spans":[{"t":"code","v":"localStorage"},{"t":"text","v":", namespaced "},{"t":"code","v":"webterm:mod:<id>:<key>"},{"t":"text","v":". "},{"t":"strong","v":"Per-browser"},{"t":"text","v":", string values, never synced. Every call swallows storage failures (private mode, quota) and "},{"t":"code","v":"get"},{"t":"text","v":" returns "},{"t":"code","v":"null"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.prefs"},{"t":"text","v":" (#196) — the sanctioned browser-local tier: `get(key,"}],"t":"bullet"},{"spans":[{"t":"text","v":"default)"},{"t":"code","v":" / "},{"t":"text","v":"set(key, value)"},{"t":"code","v":" / "},{"t":"text","v":"remove(key)"},{"t":"code","v":" over its own "},{"t":"text","v":"webterm:modprefs:<id>:<key>"},{"t":"code","v":" namespace, with **JSON** values rather than strings. A **sibling** of "},{"t":"text","v":"ctx.storage"},{"t":"code","v":", not a corner of it — the namespace is the whole contract, and it is what makes these records survive \"Reset local view\" and structurally unable to ride "},{"t":"text","v":"/state`. See below."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.serverStore"},{"t":"text","v":" — a durable per-mod key/value store on the broker"}],"t":"bullet"},{"spans":[{"t":"text","v":"("},{"t":"code","v":"/mod-store/<modId>"},{"t":"text","v":"), scoped to this mod's id so one mod can neither read nor write another's. Every call resolves to the parsed body plus the HTTP "},{"t":"code","v":"status"},{"t":"text","v":", and never rejects: "},{"t":"code","v":"get(opts)"},{"t":"text","v":" → "},{"t":"code","v":"{status, rev, value, revisions:[{rev,ts}]}"},{"t":"text","v":"; "},{"t":"code","v":"set(value, baseRev, opts)"},{"t":"text","v":" → "},{"t":"code","v":"{status, ok, rev, …}"},{"t":"text","v":" (a "},{"t":"code","v":"409"},{"t":"text","v":" carries the live value inlined so you can rebase in one round trip); "},{"t":"code","v":"getRevision(n, opts)"},{"t":"text","v":" → "},{"t":"code","v":"{status, ok, rev, value}"},{"t":"text","v":". Writes ride the "},{"t":"code","v":"/state"},{"t":"text","v":" single-active-client lease: a non-active browser reads fine but its "},{"t":"code","v":"set()"},{"t":"text","v":" resolves "},{"t":"code","v":"409 {error:'not_active'}"},{"t":"text","v":". "},{"t":"code","v":"opts.host"},{"t":"text","v":" routes to another broker by host id, and an unknown id "},{"t":"strong","v":"fails closed"},{"t":"text","v":" — "},{"t":"code","v":"{status:0, error:'no_host'}"},{"t":"text","v":" with no request made. Reading another broker is why "},{"t":"code","v":"status"},{"t":"text","v":" matters: a "},{"t":"code","v":"401"},{"t":"text","v":" (\"it refused our password\") and a "},{"t":"code","v":"200"},{"t":"text","v":" with "},{"t":"code","v":"value:null"},{"t":"text","v":" (\"nothing published there\") are the same empty body and only the status tells them apart. The status is applied after the body, so a response body carrying its own "},{"t":"code","v":"status"},{"t":"text","v":" cannot overwrite the transport's."}],"t":"p"},{"spans":[{"t":"strong","v":"A mod that stores credentials MUST set "},{"t":"code","v":"opts.noHistory = true"},{"t":"strong","v":" on every write."},{"t":"text","v":" The store keeps a revision ring, so without it the value each write replaces — the previous password, in the clear — stays readable through "},{"t":"code","v":"getRevision(n)"},{"t":"text","v":" and on the broker's disk. The flag is sticky per record: it suppresses new revisions, clears the ring on the write that sets it, and survives later writes that omit the option (absent means \"leave it alone\", which is what makes a 409 rebase safe — pass the same "},{"t":"code","v":"opts"},{"t":"text","v":" to the retry). Turning it back on is deliberate and one-way: "},{"t":"code","v":"noHistory:false"},{"t":"text","v":" is refused unless "},{"t":"code","v":"purgeRevisions:true"},{"t":"text","v":" rides the same call. Only a strict boolean reaches the wire, and a broker that predates the flag silently ignores it — check "},{"t":"code","v":"modstore.noHistory"},{"t":"text","v":" in that broker's "},{"t":"code","v":"/info"},{"t":"text","v":" "},{"t":"strong","v":"before"},{"t":"text","v":" sending a credential, because the write that would stop the archiving is itself the write that archives."}],"t":"p"},{"spans":[{"t":"text","v":"Two members are built "},{"t":"strong","v":"on top of"},{"t":"text","v":" "},{"t":"code","v":"get"},{"t":"text","v":"/"},{"t":"code","v":"set"},{"t":"text","v":" and have their own subsections below: "},{"t":"code","v":"update(fn, opts)"},{"t":"text","v":" (#196), the compare-and-swap that re-applies your reducer over the winner of a "},{"t":"code","v":"409"},{"t":"text","v":", and "},{"t":"code","v":"saveChain(opts)"},{"t":"text","v":" (#196), the debounced, single-in-flight save pipeline built on "},{"t":"code","v":"update"},{"t":"text","v":". Use them. Three mods hand-rolled a compare-and-swap before these existed, and the rebase that is easiest to write by hand — adopt the winner's "},{"t":"code","v":"rev"},{"t":"text","v":", re-"},{"t":"code","v":"PUT"},{"t":"text","v":" the object you already had — is the one that erases every key another browser changed in between (#158; it is why "},{"t":"code","v":"mods/mod-sync/"},{"t":"text","v":" deliberately does not use core's "},{"t":"code","v":"putHostState"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"text","v":"Host I/O"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.file"},{"t":"text","v":" — "},{"t":"code","v":"read"},{"t":"text","v":" / "},{"t":"code","v":"write"},{"t":"text","v":" / "},{"t":"code","v":"list"},{"t":"text","v":" / "},{"t":"kbd","v":"delete"},{"t":"text","v":" / "},{"t":"code","v":"upload"},{"t":"text","v":" / "},{"t":"code","v":"mkdir"},{"t":"text","v":" /"}],"t":"bullet"},{"spans":[{"t":"code","v":"copy"},{"t":"text","v":" / "},{"t":"code","v":"move"},{"t":"text","v":" / "},{"t":"code","v":"zip"},{"t":"text","v":" / "},{"t":"code","v":"unzip"},{"t":"text","v":" / "},{"t":"code","v":"stat"},{"t":"text","v":" / "},{"t":"code","v":"setattr"},{"t":"text","v":" / "},{"t":"code","v":"hash"},{"t":"text","v":", plus the chunked family ("},{"t":"code","v":"readChunk"},{"t":"text","v":", "},{"t":"code","v":"uploadBegin"},{"t":"text","v":", "},{"t":"code","v":"uploadChunk"},{"t":"text","v":", "},{"t":"code","v":"uploadCommit"},{"t":"text","v":", "},{"t":"code","v":"uploadAbort"},{"t":"text","v":"). Paths are "},{"t":"strong","v":"host-wide absolute native paths"},{"t":"text","v":" — there is no root confinement. Every call resolves to a parsed result object, never a rejected promise: "},{"t":"code","v":"{ok:true, …}"},{"t":"text","v":" or "},{"t":"code","v":"{ok:false, error}"},{"t":"text","v":". "},{"t":"code","v":"opts.host"},{"t":"text","v":" is a host "},{"t":"strong","v":"id string"},{"t":"text","v":"; omitted / "},{"t":"code","v":"''"},{"t":"text","v":" / "},{"t":"code","v":"'local'"},{"t":"text","v":" means this broker, and an unknown remote id "},{"t":"strong","v":"fails closed"},{"t":"text","v":" with "},{"t":"code","v":"{ok:false, error:'host_not_found'}"},{"t":"text","v":" and makes no request."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.session"},{"t":"text","v":" — "},{"t":"code","v":"procs(id, opts)"},{"t":"text","v":", the destructive "},{"t":"code","v":"kill(id, pid, opts)"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"text","v":"and "},{"t":"code","v":"git(id, opts)"},{"t":"text","v":". Resolves to "},{"t":"code","v":"{status, json}"},{"t":"text","v":" so you honour the broker's status contract verbatim — note that "},{"t":"code","v":"409 {error:'session_gone'}"},{"t":"text","v":" from "},{"t":"code","v":"kill"},{"t":"text","v":" is the session-destroy "},{"t":"strong","v":"success"},{"t":"text","v":" path, not a failure."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.clipboard.observe(fn)"},{"t":"text","v":" — "},{"t":"code","v":"fn(dir, text)"},{"t":"text","v":" with "},{"t":"code","v":"dir"},{"t":"text","v":" "},{"t":"code","v":"'out'"},{"t":"text","v":" (copied"}],"t":"bullet"},{"spans":[{"t":"text","v":"out of a terminal) or "},{"t":"code","v":"'in'"},{"t":"text","v":" (pasted in), for every copy/paste core captures. Returns an unsubscribe fn and is also dropped on teardown, so capturing stops the moment the mod is disabled."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.hosts.invalidate(id)"},{"t":"text","v":" (#195) — \"forget everything this page knows"}],"t":"bullet"},{"spans":[{"t":"text","v":"about host "},{"t":"code","v":"<id>"},{"t":"text","v":"\": clears every registered per-host cache and repaints the host surfaces, returning a boolean. It notifies nobody and saves nothing — see below."}],"t":"p"},{"spans":[{"t":"text","v":"Desktop, taskbar and menus"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.taskbar.addStatusItem(node)"},{"t":"text","v":" — mount a node in the taskbar (before"}],"t":"bullet"},{"spans":[{"t":"code","v":"#help-chip"},{"t":"text","v":"). Auto-removed on teardown."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.taskbar.onItemsRendered(cb)"},{"t":"text","v":" — after every chip rebuild."}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.taskbar.interceptActivate(fn)"},{"t":"text","v":" — "},{"t":"code","v":"fn(key)"},{"t":"text","v":" runs before core resolves"}],"t":"bullet"},{"spans":[{"t":"text","v":"a taskbar activation; return "},{"t":"code","v":"true"},{"t":"text","v":" if you changed what the desktop shows."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.desktop"},{"t":"text","v":" — "},{"t":"code","v":"columnFilter(fn)"},{"t":"text","v":", "},{"t":"code","v":"onColumnCreated(fn)"},{"t":"text","v":", "},{"t":"code","v":"onPlaced(fn)"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"code","v":"onForgotten(fn)"},{"t":"text","v":", "},{"t":"code","v":"onReveal(fn)"},{"t":"text","v":", "},{"t":"code","v":"onLayoutRender(fn)"},{"t":"text","v":". Each family is "},{"t":"strong","v":"one slot"},{"t":"text","v":": a second registration throws "},{"t":"code","v":"ModConflictError"},{"t":"text","v":", so two mods can never silently fight over the desktop."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.registerKeyActions([{id,label,run}])"},{"t":"text","v":" — merges into the live action"}],"t":"bullet"},{"spans":[{"t":"text","v":"list every reader goes through (dispatcher, the Keyboard Shortcuts pane, the help corpus). A duplicate action id throws rather than last-wins."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.registerWindowMenuItems(fn)"},{"t":"strong","v":" / "},{"t":"code","v":"ctx.registerDesktopMenuItems(fn)"},{"t":"text","v":" —"}],"t":"bullet"},{"spans":[{"t":"text","v":"register a callback; the callback returns an "},{"t":"strong","v":"array"},{"t":"text","v":" of "},{"t":"code","v":"renderMenu"},{"t":"text","v":" items (separators included, so you own your own grouping — "},{"t":"code","v":"renderMenu"},{"t":"text","v":" does not collapse a stray one). The desktop callback runs in "},{"t":"strong","v":"both"},{"t":"text","v":" window modes and receives "},{"t":"code","v":"{ tiling }"},{"t":"text","v":" saying which — check it rather than assuming tiling."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.windows.onTerminalCreate(cb)"},{"t":"text","v":" — subscribes to every terminal window,"}],"t":"bullet"},{"spans":[{"t":"strong","v":"replayed over those already open"},{"t":"text","v":" and fired for future ones. "},{"t":"code","v":"cb"},{"t":"text","v":" gets "},{"t":"code","v":"{ win, titleBar, host, wireId, addTitleBarItem, onDispose, onModTeardown }"},{"t":"text","v":" — one subscription's replay and its create-time emit are mutually exclusive, so each window reaches your callback exactly once and you need no decorate-once "},{"t":"code","v":"WeakSet"},{"t":"text","v":". "},{"t":"code","v":"onModTeardown"},{"t":"text","v":" (#195) is the per-window disposer; it has its own section below."}],"t":"p"},{"spans":[{"t":"text","v":"Control Panel"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.settings.boolean(key, def, opts)"},{"t":"text","v":" → checkbox"}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.settings.radio(key, options, opts)"},{"t":"text","v":" → radio group"}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.settings.select(key, options, opts)"},{"t":"text","v":" → "},{"t":"code","v":"<select>"}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.settings.combo(key, options, opts)"},{"t":"text","v":" → searchable "},{"t":"code","v":"<input list>"}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.settings.text(key, opts)"},{"t":"text","v":" → free text (the fifth primitive)"}],"t":"bullet"},{"spans":[{"t":"text","v":"All five are read-through accessors onto the "},{"t":"strong","v":"shared "},{"t":"code","v":"/state"},{"t":"strong","v":" settings blob"},{"t":"text","v":" (not namespaced localStorage), so a value syncs across your browsers, and all five return "},{"t":"code","v":"{get, set, onChange}"},{"t":"text","v":". "},{"t":"code","v":"opts"},{"t":"text","v":" carries "},{"t":"code","v":"label"},{"t":"text","v":", "},{"t":"code","v":"title"},{"t":"text","v":", "},{"t":"code","v":"def"},{"t":"text","v":" ("},{"t":"code","v":"boolean"},{"t":"text","v":" takes its default as the second argument instead), "},{"t":"code","v":"isBrowserGlobal"},{"t":"text","v":" (default true — hidden on remote-host tabs) and "},{"t":"code","v":"mount"},{"t":"text","v":" ("},{"t":"code","v":"'mods'"},{"t":"text","v":", the default per-host pane, or "},{"t":"code","v":"'browser'"},{"t":"text","v":", beside the Hosts list). Reads are "},{"t":"strong","v":"non-destructive"},{"t":"text","v":": nothing writes the default back."}],"t":"p"},{"spans":[{"t":"code","v":"text"},{"t":"text","v":" is the only primitive that is "},{"t":"strong","v":"not choice-constrained"},{"t":"text","v":", and that distinction matters — "},{"t":"code","v":"combo"},{"t":"text","v":" looks free but treats its list as the legal domain and refuses anything outside it. "},{"t":"code","v":"text"},{"t":"text","v":" takes "},{"t":"code","v":"options"},{"t":"text","v":" as "},{"t":"strong","v":"suggestions"},{"t":"text","v":" (a datalist), plus "},{"t":"code","v":"placeholder"},{"t":"text","v":", "},{"t":"code","v":"maxLength"},{"t":"text","v":" (capped at "},{"t":"code","v":"MAX_MOD_TEXT_LEN"},{"t":"text","v":", 1024 UTF-16 code units) and "},{"t":"code","v":"validate"},{"t":"text","v":". Its three rules:"}],"t":"p"},{"spans":[{"t":"code","v":"read()"},{"t":"text","v":" is "},{"t":"strong","v":"structural only"},{"t":"text","v":" — a bounded, control-char-free,"}],"t":"bullet"},{"spans":[{"t":"text","v":"well-formed string is returned as-is, even one this build's own validator would refuse, and it never writes. So an upgrade, a different engine or a stricter validator can no longer make a stored value evaporate."}],"t":"p"},{"spans":[{"t":"code","v":"coerce()"},{"t":"text","v":" gates what we write: String → strip control chars → drop"}],"t":"bullet"},{"spans":[{"t":"text","v":"unpaired surrogates → trim → cap."}],"t":"p"},{"spans":[{"t":"code","v":"validate"},{"t":"text","v":" is "},{"t":"strong","v":"write-only and its rejection is visible"},{"t":"text","v":" — the rejected"}],"t":"bullet"},{"spans":[{"t":"text","v":"draft stays on screen next to a "},{"t":"code","v":".set-err"},{"t":"text","v":" message and the stored value is untouched. It must be "},{"t":"strong","v":"synchronous"},{"t":"text","v":": a promise is truthy, so an async validator would accept everything, and the loader logs and rejects if you pass one."}],"t":"p"},{"spans":[{"t":"text","v":"Writes are debounced, and a pending one is flushed on teardown. "},{"t":"code","v":"mods/clock/clock.js"},{"t":"text","v":" is the worked example: its time-zone box uses "},{"t":"code","v":"text"},{"t":"text","v":" precisely because "},{"t":"code","v":"Intl.supportedValuesOf('timeZone')"},{"t":"text","v":" is engine-dependent, and a "},{"t":"code","v":"combo"},{"t":"text","v":" refused zones that the same engine would happily render."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.registerSettingsPane(spec)"},{"t":"text","v":" — a full custom section, for anything"}],"t":"bullet"},{"spans":[{"t":"text","v":"richer. "},{"t":"code","v":"spec.render()"},{"t":"text","v":" builds the DOM and is called "},{"t":"strong","v":"exactly once"},{"t":"text","v":"; "},{"t":"code","v":"spec.reflect(settings)"},{"t":"text","v":" syncs it on Control Panel open "},{"t":"strong","v":"and on every "},{"t":"code","v":"/state"},{"t":"strong","v":" convergence"},{"t":"text","v":", so it must be idempotent and must preserve an in-progress edit."}],"t":"p"},{"spans":[{"t":"text","v":"Theme"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.theme.get()        // -> {name, dark}\nctx.theme.vars()       // -> {'--bg': '#1e1e1e', …}\nctx.theme.onChange(fn) // -> unsubscribe fn"}],"t":"pre"},{"spans":[{"t":"text","v":"Derived from the "},{"t":"strong","v":"live DOM"},{"t":"text","v":", so it is right with the theme mod disabled, absent or replaced."}],"t":"p"},{"spans":[{"t":"code","v":"dark"},{"t":"text","v":" is the honest half: a YIQ test on the computed "},{"t":"code","v":"--bg"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"code","v":"name"},{"t":"text","v":" is a hint — the synced "},{"t":"code","v":"theme"},{"t":"text","v":" key, reported only when an inline"}],"t":"bullet"},{"spans":[{"t":"code","v":"--bg"},{"t":"text","v":" on "},{"t":"code","v":"documentElement"},{"t":"text","v":" proves a theme mod actually applied one. Branch on "},{"t":"code","v":"dark"},{"t":"text","v":" when you need certainty."}],"t":"p"},{"spans":[{"t":"code","v":"onChange(fn)"},{"t":"text","v":" fires "},{"t":"strong","v":"after"},{"t":"text","v":" the new vars are on screen, only on a real"}],"t":"bullet"},{"spans":[{"t":"text","v":"change of "},{"t":"code","v":"{name, dark}"},{"t":"text","v":", and is "},{"t":"strong","v":"not replayed"},{"t":"text","v":" on register — call "},{"t":"code","v":"get()"},{"t":"text","v":" in your "},{"t":"code","v":"init"},{"t":"text","v":". It is auto-dropped on teardown. A change that moves a public var without moving "},{"t":"code","v":"name"},{"t":"text","v":" or "},{"t":"code","v":"dark"},{"t":"text","v":" does "},{"t":"strong","v":"not"},{"t":"text","v":" notify."}],"t":"p"},{"spans":[{"t":"code","v":"vars()"},{"t":"strong","v":" is a separate call on purpose"},{"t":"text","v":": it costs a "},{"t":"code","v":"getPropertyValue"},{"t":"text","v":" per"}],"t":"bullet"},{"spans":[{"t":"text","v":"variable, which a subscriber that only branches on "},{"t":"code","v":"dark"},{"t":"text","v":" should not pay."}],"t":"p"},{"spans":[{"t":"text","v":"The public CSS-variable contract "},{"t":"code","v":"vars()"},{"t":"text","v":" resolves is "},{"t":"code","v":"--bg"},{"t":"text","v":", "},{"t":"code","v":"--bg-2"},{"t":"text","v":", "},{"t":"code","v":"--bg-3"},{"t":"text","v":", "},{"t":"code","v":"--fg"},{"t":"text","v":", "},{"t":"code","v":"--fg-dim"},{"t":"text","v":", "},{"t":"code","v":"--accent-default"},{"t":"text","v":", "},{"t":"code","v":"--sel-bg"},{"t":"text","v":", "},{"t":"code","v":"--ok"},{"t":"text","v":", "},{"t":"code","v":"--warn"},{"t":"text","v":", "},{"t":"code","v":"--danger"},{"t":"text","v":". Deliberately "},{"t":"strong","v":"not"},{"t":"text","v":" in it: "},{"t":"code","v":"--accent"},{"t":"text","v":" (per-window, so there is no document-level value — write "},{"t":"code","v":"var(--accent, var(--accent-default))"},{"t":"text","v":" inside a window instead) and the geometry vars ("},{"t":"code","v":"--taskbar-h"},{"t":"text","v":", "},{"t":"code","v":"--title-h"},{"t":"text","v":", "},{"t":"code","v":"--handle-thick"},{"t":"text","v":", "},{"t":"code","v":"--corner-size"},{"t":"text","v":"). Everything else in the stylesheets is private core."}],"t":"p"},{"spans":[{"t":"strong","v":"Values come back as the CSSOM reports a custom property — a substituted token stream, not a resolved colour."},{"t":"text","v":" A theme mod writes six of these as plain hex, but the four derived ones ("},{"t":"code","v":"--sel-bg"},{"t":"text","v":", "},{"t":"code","v":"--ok"},{"t":"text","v":", "},{"t":"code","v":"--warn"},{"t":"text","v":", "},{"t":"code","v":"--danger"},{"t":"text","v":") read back as e.g. "},{"t":"code","v":"color-mix(in srgb, #4aa3ff 28%, #1e1e1e)"},{"t":"text","v":". That is a valid CSS colour string — assign it to a style property and it paints — but do "},{"t":"strong","v":"not"},{"t":"text","v":" try to parse a number out of it."}],"t":"p"},{"spans":[{"t":"text","v":"Windows and Help"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.registerWindowKind(spec)"},{"t":"text","v":" — register a brand-new app-window kind:"}],"t":"bullet"},{"spans":[{"t":"code","v":"{ appKind, factory(appData) -> win, serialize(win) -> record|null,\n  restore?(record), retainOnClose?(record) -> bool,\n  menu?: { label, launch(), iconKey?, iconGlyph?, closedItems?() } }"}],"t":"pre"},{"spans":[{"t":"text","v":"It is the same registry core's built-ins use, so a mod kind is a first-class window everywhere the registry is consulted. A duplicate "},{"t":"code","v":"appKind"},{"t":"text","v":" throws (and "},{"t":"code","v":"initMod"},{"t":"text","v":" rolls the mod back); the kind is removed on teardown. An optional "},{"t":"code","v":"restore"},{"t":"text","v":" is called "},{"t":"strong","v":"directly"},{"t":"text","v":", so it does not get "},{"t":"code","v":"openAppWindow"},{"t":"text","v":"'s dedup-by-id — it must check "},{"t":"code","v":"windows.get(record.id)"},{"t":"text","v":" itself, because a lease-loss rebuild re-runs it."}],"t":"p"},{"spans":[{"t":"strong","v":"The launcher icon."},{"t":"text","v":" "},{"t":"code","v":"menu.iconKey"},{"t":"text","v":" names an entry in core's "},{"t":"code","v":"APP_ICON_SVG"},{"t":"text","v":" table, which is "},{"t":"strong","v":"closed"},{"t":"text","v":" — a key core does not ship resolves to nothing. "},{"t":"code","v":"menu.iconGlyph"},{"t":"text","v":" is the mod-owned alternative: a short "},{"t":"strong","v":"text/emoji"},{"t":"text","v":" glyph that "},{"t":"code","v":"renderMenu"},{"t":"text","v":" paints with "},{"t":"code","v":"textContent"},{"t":"text","v":". It is normalized by core's "},{"t":"code","v":"appIconGlyph()"},{"t":"text","v":", which refuses an over-long raw string, strips control characters, bidi controls, invisible-payload carriers, lone surrogates and all whitespace, requires at least one substantive code point left over, and caps the result at "},{"t":"code","v":"APP_GLYPH_MAX"},{"t":"text","v":" code points — read that function for the exact rules rather than trusting this paragraph."}],"t":"p"},{"spans":[{"t":"text","v":"There is deliberately "},{"t":"strong","v":"no "},{"t":"code","v":"ctx.registerAppIcon(svg)"},{"t":"text","v":": the API offers no way to put markup into the shared menu renderer, partly because the value a mod passes need not be its own (it might come from a "},{"t":"code","v":"/mod-store"},{"t":"text","v":" blob or a peer's "},{"t":"code","v":"/state"},{"t":"text","v":"). That is hygiene for this API — per §1 it constrains nothing a mod could do by reaching into the DOM itself."}],"t":"p"},{"spans":[{"t":"text","v":"Two traps: an "},{"t":"code","v":"iconKey"},{"t":"text","v":" is not owned by anyone, so a mod can name a shipped key and get that icon — only the "},{"t":"strong","v":"label"},{"t":"text","v":" identifies a launcher; and "},{"t":"code","v":"iconKey"},{"t":"text","v":" takes "},{"t":"strong","v":"precedence"},{"t":"text","v":" over "},{"t":"code","v":"iconGlyph"},{"t":"text","v":". Declare one or the other, and do not rely on the fallback for a key core does not currently ship."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.registerHelpCards(cards)"},{"t":"text","v":" — contribute typed Help cards. **Never raw"}],"t":"bullet"},{"spans":[{"t":"text","v":"HTML**: "},{"t":"code","v":"{ slug, section, title, body:[block], keys?, search? }"},{"t":"text","v":" where "},{"t":"code","v":"block = {t:'p'|'bullet'|'sub', spans:[span]}"},{"t":"text","v":" and "},{"t":"code","v":"span = {t:'text'|'strong'|'code'|'kbd', v:String}"},{"t":"text","v":". Unknown types degrade to text; values are coerced to "},{"t":"code","v":"String"},{"t":"text","v":". Removed on teardown, and an open Help window re-renders."}],"t":"p"},{"spans":[{"t":"text","v":"App windows: "},{"t":"code","v":"ctx.windows.createAppWindow"},{"t":"text","v":" (#194)"}],"t":"sub"},{"spans":[{"t":"text","v":"Nine shipped mods used to hand-build the same ~30-field app-window scaffold: a "},{"t":"code","v":"win"},{"t":"text","v":" literal pushed straight into the core "},{"t":"code","v":"windows"},{"t":"text","v":" Map, a synthetic "},{"t":"code","v":"kind:'app'"},{"t":"text","v":" session, a hand-appended taskbar chip, and a teardown that iterated core state to find its own windows. Core now owns all of that; a mod supplies only what is specific to its window. "},{"t":"code","v":"mods/clipboard/clipboard.js"},{"t":"text","v":" is the worked example — it shipped as the reference migration alongside this API, and its old scaffold, "},{"t":"code","v":"CLIP_WIN_ID"},{"t":"text","v":" hack included, is gone."}],"t":"p"},{"spans":[{"t":"code","v":"const h = ctx.windows.createAppWindow({\n    kind: 'clipboard',              // required: the appKind\n    id: 'app:clip',                 // optional; see the singleton default below\n    title: 'Clipboard',\n    sid: 'clip', badge: '#clip',    // chip / session short id, taskbar badge\n    appClass: 'app-clip', bodyClass: 'clip-body',\n    singleton: true,                // open-or-focus, deduped on KIND — see below\n    geom: undefined, color: undefined, locked: undefined, floatGeom: undefined,\n    resizable: true,                // false = no resize handles (recorder's playback window)\n    toolbar: function (el, win, h) { /* the .app-toolbar strip, optional */ },\n    body: function (el, win, h) { /* the window's content */ },\n    onClose: function () { /* replaces the × action; default is closeWindow */ },\n});"}],"t":"pre"},{"spans":[{"t":"code","v":"kind"},{"t":"text","v":" is the only required field, and it must be a kind "},{"t":"strong","v":"this mod has already registered"},{"t":"text","v":" with "},{"t":"code","v":"ctx.registerWindowKind"},{"t":"text","v":" — register in "},{"t":"code","v":"init()"},{"t":"text","v":", build later (on a launch, a restore, a click). Two things ride on that: a mod cannot name another mod's kind and have the dedupe hand it that mod's live window (which its "},{"t":"code","v":"closeAll()"},{"t":"text","v":", or simply its being disabled, would then close), and a window of a kind nobody registered would persist a "},{"t":"code","v":"/state"},{"t":"text","v":" record nothing can ever restore. A create is also refused outright while the mod is being torn down — the take-down below walks one snapshot, so a window opened after it would outlive both the pass and its kind. Both refusals throw."}],"t":"p"},{"spans":[{"t":"text","v":"Everything else falls back to the same defaults the deleted scaffolds computed by hand: "},{"t":"code","v":"title"},{"t":"text","v":"/"},{"t":"code","v":"sid"},{"t":"text","v":" default to "},{"t":"code","v":"kind"},{"t":"text","v":", "},{"t":"code","v":"badge"},{"t":"text","v":" to "},{"t":"code","v":"'#' + sid"},{"t":"text","v":", "},{"t":"code","v":"appClass"},{"t":"text","v":" to "},{"t":"code","v":"'app-' + kind"},{"t":"text","v":", "},{"t":"code","v":"bodyClass"},{"t":"text","v":" to "},{"t":"code","v":"appClass + '-body'"},{"t":"text","v":", "},{"t":"code","v":"geom"},{"t":"text","v":" to "},{"t":"code","v":"clampGeom(appDefaultGeom(kind))"},{"t":"text","v":", "},{"t":"code","v":"color"},{"t":"text","v":" to "},{"t":"code","v":"normalizeHex(defaultColor(id))"},{"t":"text","v":", "},{"t":"code","v":"locked"},{"t":"text","v":" to "},{"t":"code","v":"true"},{"t":"text","v":", and "},{"t":"code","v":"id"},{"t":"text","v":" (absent "},{"t":"code","v":"singleton"},{"t":"text","v":") to a fresh "},{"t":"code","v":"newAppId(kind)"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Core owns, exactly as every deleted copy did it by hand: the chrome ("},{"t":"code","v":"buildAppChrome"},{"t":"text","v":" + "},{"t":"code","v":"wireAppChrome"},{"t":"text","v":" + the eight resize handles), the desktop insertion, the window record itself (the object that lands in the core "},{"t":"code","v":"windows"},{"t":"text","v":" Map), the synthetic "},{"t":"code","v":"kind:'app'"},{"t":"text","v":" session and its taskbar chip ("},{"t":"code","v":"#taskbar-empty"},{"t":"text","v":" removed once it exists), and the placement tail ("},{"t":"code","v":"finishWindowPlacement"},{"t":"text","v":"). Your "},{"t":"code","v":"toolbar"},{"t":"text","v":"/"},{"t":"code","v":"body"},{"t":"text","v":" builders run after all of that, against a window that already has a layout box and a record to hang per-window state on — though not necessarily its final box, since placement may still tile it."}],"t":"p"},{"spans":[{"t":"code","v":"createAppWindow"},{"t":"text","v":" returns a "},{"t":"strong","v":"frozen handle"},{"t":"text","v":", not the window record:"}],"t":"p"},{"spans":[{"t":"text","v":"member · does"}],"t":"sub"},{"spans":[{"t":"code","v":"h.win"},{"t":"text","v":" — the underlying window record — the escape hatch the trust model (§1) permits, not the API itself"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.dom"},{"t":"text","v":", "},{"t":"code","v":"h.body"},{"t":"text","v":", "},{"t":"code","v":"h.toolbar"},{"t":"text","v":" — the chrome root, the body element, the toolbar element ("},{"t":"code","v":"null"},{"t":"text","v":" if you passed no "},{"t":"code","v":"toolbar"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.isOpen()"},{"t":"text","v":" — still a live member of the core "},{"t":"code","v":"windows"},{"t":"text","v":" Map"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.focus()"},{"t":"text","v":" — reveal-and-focus"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.close()"},{"t":"text","v":" — close this window"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.setTitle(text)"},{"t":"text","v":" / "},{"t":"code","v":"h.setColor(hex)"},{"t":"text","v":" — update title bar, session and taskbar chip together"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.save()"},{"t":"text","v":" — "},{"t":"code","v":"saveAppWindow"},{"t":"text","v":", for a persisted kind"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.addTitleBarItem(node)"},{"t":"text","v":" — inserted before the minimize button"}],"t":"bullet"},{"spans":[{"t":"code","v":"h.onDispose(fn)"},{"t":"text","v":" — "},{"t":"code","v":"win.cleanups.push"},{"t":"text","v":", spelled the way "},{"t":"code","v":"onTerminalCreate"},{"t":"text","v":"'s bag spells it"}],"t":"bullet"},{"spans":[{"t":"text","v":"Every member is gated on the window still being live — a handle you keep past its window's death (or past a later window reusing the same id) is inert, not lethal."}],"t":"p"},{"spans":[{"t":"code","v":"singleton: true"},{"t":"text","v":" formalizes the open-or-focus hack every windowed mod re-derived by hand (clipboard's old "},{"t":"code","v":"CLIP_WIN_ID"},{"t":"text","v":"). With it, "},{"t":"code","v":"id"},{"t":"text","v":" defaults to the stable "},{"t":"code","v":"'app:' + kind"},{"t":"text","v":", and a create call "},{"t":"strong","v":"dedupes on "},{"t":"code","v":"kind"},{"t":"text","v":" over every live window in the core "},{"t":"code","v":"windows"},{"t":"text","v":" Map — not just the windows this mod's own registry already knows about, because core's unknown-kind restore fallback (#167) may have rebuilt a window of this kind, under some other id, before this mod ever loaded. Whichever window it finds is adopted (never double-built) and, unless the call is a restore, brought to front. Without "},{"t":"code","v":"singleton"},{"t":"text","v":", dedupe is by "},{"t":"code","v":"id"},{"t":"text","v":" alone: a second create at a still-open id adopts and focuses it; a create at an id mid-close throws (a reopen has to wait for the close to finish); a create at an id already open under a different "},{"t":"code","v":"kind"},{"t":"text","v":" throws rather than stranding the live window."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.windows.list()"},{"t":"text","v":" — this mod's own live windows, oldest first, as a fresh frozen array. Dead entries are pruned on the way past, so a "},{"t":"code","v":"list()"},{"t":"text","v":" after a lease-loss rebuild reads empty instead of a wall of ghosts. "},{"t":"code","v":"mods/clipboard/clipboard.js"},{"t":"text","v":"'s "},{"t":"code","v":"renderAll()"},{"t":"text","v":" is the shipped example: a loop over "},{"t":"code","v":"ctx.windows.list()"},{"t":"text","v":" replaced a hand-kept "},{"t":"code","v":"liveWins"},{"t":"text","v":" set."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.windows.closeAll()"},{"t":"text","v":" closes every one of this mod's factory-built windows and returns how many it closed. It is the same pass the loader also stages automatically: "},{"t":"strong","v":"a mod's factory windows are closed by core before its "},{"t":"code","v":"onUnload"},{"t":"strong","v":" chain runs, and while its window kinds are still registered"},{"t":"text","v":" — so a migrated mod needs no teardown-ordering care of its own. That property is why "},{"t":"code","v":"registerWindowKind"},{"t":"text","v":"'s restore/teardown ordering used to matter by hand: close a window after its kind is deregistered and the close reaches "},{"t":"code","v":"saveAppWindow"},{"t":"text","v":" with no registry entry for the kind, falls back to the shared serializer, and leaves a junk "},{"t":"code","v":"/state"},{"t":"text","v":" record for a kind nothing can restore. Modeling the close as an "},{"t":"code","v":"onUnload"},{"t":"text","v":" entry would make it the oldest entry in the LIFO chain and therefore run it last — after "},{"t":"code","v":"deleteWindowKind"},{"t":"text","v":", which rides that same chain — exactly the wrong order. So it is never registered as an unload: the loader calls this same close pass directly, at the head of teardown, before the first "},{"t":"code","v":"onUnload"},{"t":"text","v":" runs and before any kind the mod registered is deregistered. This is what let "},{"t":"code","v":"clipboard.js"},{"t":"text","v":" delete its own "},{"t":"code","v":"ctx.onUnload"},{"t":"text","v":" window-closing block entirely. Call "},{"t":"code","v":"closeAll()"},{"t":"text","v":" yourself when you want a mod's windows gone without disabling the mod."}],"t":"p"},{"spans":[{"t":"strong","v":"The restore hook's third argument."},{"t":"text","v":" "},{"t":"code","v":"registerWindowKind"},{"t":"text","v":"'s "},{"t":"code","v":"restore"},{"t":"text","v":" is called directly by core, outside any mod's "},{"t":"code","v":"ctx"},{"t":"text","v":" closure, so it is handed the per-mod window API as a third argument:"}],"t":"p"},{"spans":[{"t":"code","v":"restore: function (record, opts, api) {\n    const h = api.createAppWindow({ kind: 'scratchpad', id: record.id, … });\n    return h.win;        // or `return h;` — api unwraps a handle either way\n}"}],"t":"pre"},{"spans":[{"t":"code","v":"api"},{"t":"text","v":" is "},{"t":"code","v":"{createAppWindow, list}"},{"t":"text","v":" — the same two members "},{"t":"code","v":"ctx.windows"},{"t":"text","v":" exposes, scoped to this mod's record. Two defaults ride the call and both are overridable in your own spec: "},{"t":"code","v":"spec.id"},{"t":"text","v":" defaults to "},{"t":"code","v":"record.id"},{"t":"text","v":" (so a restore that builds elsewhere does not orphan the record it was handed, or step around the id dedupe), and "},{"t":"code","v":"spec.restoring"},{"t":"text","v":" defaults to "},{"t":"code","v":"opts.restoring"},{"t":"text","v":" (core's automatic restore pass always passes "},{"t":"code","v":"true"},{"t":"text","v":") — the same suppression "},{"t":"code","v":"openAppWindow"},{"t":"text","v":"'s own "},{"t":"code","v":"restoring"},{"t":"text","v":" gives: no reveal, no un-minimize, no re-home to whatever workspace the page happened to boot on (#152)."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.windows.onAppWindowCreate(handler)"},{"t":"text","v":" fires for app windows whose "},{"t":"code","v":"appKind"},{"t":"text","v":" is one of "},{"t":"strong","v":"this mod's registered kinds"},{"t":"text","v":" — every kind it has passed to "},{"t":"code","v":"registerWindowKind"},{"t":"text","v":", however the window was actually built, including a window core rebuilt through its unknown-kind fallback before this mod ever registered that kind. It is "},{"t":"strong","v":"not"},{"t":"text","v":" scoped to \"windows this mod's factory built.\" Subscribing "},{"t":"strong","v":"replays"},{"t":"text","v":" every matching window that already exists, then fires for every one built afterwards — exactly once per subscription: a replayed window is never re-delivered as a live create, and a live create is never re-delivered by a later replay, so a mod needs no "},{"t":"code","v":"WeakSet"},{"t":"text","v":" of its own to avoid double-decorating. This is sticky's init-time catch-up pass ("},{"t":"code","v":"mods/sticky/sticky.js"},{"t":"text","v":") made first-class — whether a mod's windows already exist when its "},{"t":"code","v":"init"},{"t":"text","v":" runs is a race (#167), not an ordering, and this hook makes both orders come out identical."}],"t":"p"},{"spans":[{"t":"code","v":"const off = ctx.windows.onAppWindowCreate(function (info) {\n    // info: {win, id, kind, dom, body, titleBar, replayed, addTitleBarItem, onDispose}\n    addChip(info.id);\n    info.onDispose(function () { removeChip(info.id); });\n});"}],"t":"pre"},{"spans":[{"t":"code","v":"off()"},{"t":"text","v":" also rides "},{"t":"code","v":"ctx.onUnload"},{"t":"text","v":" automatically, so a disabled mod stops decorating windows even if it never calls the function it was handed."}],"t":"p"},{"spans":[{"t":"strong","v":"THE TRAP."},{"t":"text","v":" A "},{"t":"code","v":"registerWindowKind"},{"t":"text","v":" factory — "},{"t":"code","v":"factory(appData)"},{"t":"text","v":" — must "},{"t":"code","v":"return h.win"},{"t":"strong","v":", never the handle"},{"t":"text","v":": "},{"t":"code","v":"openAppWindow"},{"t":"text","v":" hands a factory's return value straight back to its own callers (and its own id-dedupe branch returns a window record too), so a factory that returns the handle breaks every caller expecting a record. A "},{"t":"strong","v":"restore"},{"t":"text","v":" hook is the one place a handle is accepted — "},{"t":"code","v":"api"},{"t":"text","v":" unwraps it by identity before core ever sees it, so "},{"t":"code","v":"return h;"},{"t":"text","v":" and "},{"t":"code","v":"return h.win;"},{"t":"text","v":" are both correct there, but a factory only has the one right answer. "},{"t":"code","v":"mods/clipboard/clipboard.js"},{"t":"text","v":"'s "},{"t":"code","v":"openClipboardWindow"},{"t":"text","v":" says exactly this at its own "},{"t":"code","v":"return h.win"},{"t":"text","v":" line."}],"t":"p"},{"spans":[{"t":"text","v":"Core events: "},{"t":"code","v":"ctx.events.on"},{"t":"text","v":" (#195)"}],"t":"sub"},{"spans":[{"t":"text","v":"Every host- and state-lifecycle fact core wants to tell a mod has travelled as an ad-hoc duck-typed protocol: a "},{"t":"code","v":"win._onHostAuth"},{"t":"text","v":" field core invokes "},{"t":"strong","v":"by name"},{"t":"text","v":", a "},{"t":"code","v":"win._hostRemoved"},{"t":"text","v":" beside it, Help polling the core-private "},{"t":"code","v":"_stateReady"},{"t":"text","v":" 20 × 500 ms for the initial adoption, workspaces reading the core-private "},{"t":"code","v":"_deactivated"},{"t":"text","v":". Nothing registers those and nothing types them, so core cannot refactor its auth form, its host list or its boot sequencing without auditing every mod. "},{"t":"code","v":"ctx.events"},{"t":"text","v":" is the one channel that replaces all of it — and it is the channel to write "},{"t":"strong","v":"new"},{"t":"text","v":" code against; migrating the remaining readers off those private names is still in progress (#204)."}],"t":"p"},{"spans":[{"t":"code","v":"const off = ctx.events.on('state:adopted', function (payload, meta) {\n    // payload — the table's shape below, frozen\n    // meta    — {name, shape, gen, replayed}, frozen\n});\noff();      // unsubscribe; `off` also rides this mod's teardown automatically"}],"t":"pre"},{"spans":[{"t":"strong","v":"The table is the contract."}],"t":"p"},{"spans":[{"t":"text","v":"name · shape · payload · replay on subscribe"}],"t":"sub"},{"spans":[{"t":"code","v":"host:auth"},{"t":"text","v":" — edge — "},{"t":"code","v":"{hostId}"},{"t":"text","v":" — no"}],"t":"bullet"},{"spans":[{"t":"code","v":"host:changed"},{"t":"text","v":" — edge — "},{"t":"code","v":"{hostId}"},{"t":"text","v":" — no"}],"t":"bullet"},{"spans":[{"t":"code","v":"host:removed"},{"t":"text","v":" — edge — "},{"t":"code","v":"{hostId}"},{"t":"text","v":" — no"}],"t":"bullet"},{"spans":[{"t":"code","v":"state:adopted"},{"t":"text","v":" — level — "},{"t":"code","v":"{gen}"},{"t":"text","v":" — "},{"t":"strong","v":"yes, exactly once"}],"t":"bullet"},{"spans":[{"t":"code","v":"lease:changed"},{"t":"text","v":" — level — "},{"t":"code","v":"{active, gen}"},{"t":"text","v":" — "},{"t":"strong","v":"yes, exactly once"}],"t":"bullet"},{"spans":[{"t":"text","v":"A name this build does not declare is refused "},{"t":"strong","v":"inertly"},{"t":"text","v":" — a "},{"t":"code","v":"console.info"},{"t":"text","v":" and an unsubscribe fn that does nothing — never a throw, so a mod written against a newer build keeps running here (§5's \"absence is never an error\")."}],"t":"p"},{"spans":[{"t":"strong","v":"Edge vs. level is the whole design, not a flag."},{"t":"text","v":" An edge event is a thing that "},{"t":"strong","v":"happened"},{"t":"text","v":": a subscriber that was not there missed it, and replaying it later would be a lie about when it happened. A level event is a state that "},{"t":"strong","v":"is"},{"t":"text","v":": a subscriber arriving late is entitled to the current one and gets it immediately, exactly once, out of its own "},{"t":"code","v":"on()"},{"t":"text","v":" call. The two level events are precisely the two a mod otherwise has to poll or read a core private for — the replay is what a 20-tick "},{"t":"code","v":"_stateReady"},{"t":"text","v":" timer is for, done properly. A level only replays once it exists: subscribe before core has learned the lease and there is nothing retained to hand you, so you are told at the first transition like everyone else."}],"t":"p"},{"spans":[{"t":"strong","v":"Exactly once — replay XOR live."},{"t":"text","v":" A subscriber that was there before the emit is told live; one that arrives afterwards is told by its own replay. Never zero, never twice — including the nasty case where a handler subscribes from inside another handler, i.e. after an emit has been applied but before it has been delivered. It is enforced per "},{"t":"strong","v":"subscription"},{"t":"text","v":", so two mods each get their own exactly-once and a mod that unsubscribes and re-subscribes is told again."}],"t":"p"},{"spans":[{"t":"code","v":"gen"},{"t":"strong","v":" is stamped by the bus."},{"t":"text","v":" A "},{"t":"code","v":"gen"},{"t":"text","v":" key on the payload a core site passes is "},{"t":"strong","v":"dropped"},{"t":"text","v":"; the bus owns that field. It is a monotonic counter over every emit that actually happened, across all five names — an ordering aid, so you can tell a replay of "},{"t":"code","v":"gen"},{"t":"text","v":" 3 arriving after a live "},{"t":"code","v":"gen"},{"t":"text","v":" 5 is stale. It is "},{"t":"strong","v":"not"},{"t":"text","v":" the "},{"t":"code","v":"/state"},{"t":"text","v":" revision and "},{"t":"strong","v":"not"},{"t":"text","v":" a lease epoch. Only a level payload carries it; an edge does not, but the meta bag carries it for both. Payloads are a shallow "},{"t":"strong","v":"copy"},{"t":"text","v":" of what core passed, and frozen."}],"t":"p"},{"spans":[{"t":"strong","v":"A level emit that changes nothing does not happen."},{"t":"text","v":" Equality is shallow "},{"t":"code","v":"Object.is"},{"t":"text","v":" over the own keys, ignoring "},{"t":"code","v":"gen"},{"t":"text","v":": no generation is taken, no handler is called, nothing is re-retained. That is why "},{"t":"code","v":"state:adopted"},{"t":"text","v":" fires "},{"t":"strong","v":"exactly once ever"},{"t":"text","v":" even though core has two adopt latches — the boot pull and the lease-loss rebuild both emit an empty payload, so whichever runs second is a no-op — and why a repeated "},{"t":"code","v":"{active: false}"},{"t":"text","v":" from the lease socket is silent."}],"t":"p"},{"spans":[{"t":"strong","v":"Fire-after-apply."},{"t":"text","v":" Core emits after it has mutated, and the bus applies its own state (the generation, the retained level) before it calls anybody, so a handler observes finished state rather than a half-applied one. Two limits the code states out loud and this page will not overstate:"}],"t":"p"},{"spans":[{"t":"code","v":"lease:changed"},{"t":"text","v":" is "},{"t":"strong","v":"HOME only"},{"t":"text","v":". The payload has no "},{"t":"code","v":"hostId"},{"t":"text","v":" — one retained"}],"t":"bullet"},{"spans":[{"t":"text","v":"level cannot speak for N brokers, and a remote broker's lease is a per-host mask with no event in this build's table."}],"t":"p"},{"spans":[{"t":"text","v":"On a lease "},{"t":"strong","v":"loss"},{"t":"text","v":" the view is fully torn down before any handler runs; on a"}],"t":"bullet"},{"spans":[{"t":"strong","v":"gain"},{"t":"text","v":" the rebuild is still in flight. The event names the "},{"t":"strong","v":"lease"},{"t":"text","v":", which core has genuinely learned, not the view."}],"t":"p"},{"spans":[{"t":"strong","v":"Handler isolation."},{"t":"text","v":" A throwing handler is logged and its siblings still run, and the throw never reaches the core mutation site that emitted."}],"t":"p"},{"spans":[{"t":"strong","v":"Auto-unsubscribe at teardown."},{"t":"text","v":" "},{"t":"code","v":"off"},{"t":"text","v":" is pushed onto this mod's LIFO teardown chain as well as returned, so a disabled mod stops hearing about hosts even if it never calls the function it was handed."}],"t":"p"},{"spans":[{"t":"strong","v":"Re-entrancy is coalesced and bounded."},{"t":"text","v":" An emit raised from inside a handler joins the pass already running instead of nesting one, so a cascade's stack depth is constant and the delivery order stays the order things were applied in. Level events coalesce while still queued (two lease changes inside one pass deliver the final state once); an "},{"t":"strong","v":"edge never coalesces"},{"t":"text","v":", because two auth successes are two facts. The pass is capped — exhausting it drops what is left with a "},{"t":"code","v":"console.warn"},{"t":"text","v":", rather than livelocking the UI thread."}],"t":"p"},{"spans":[{"t":"strong","v":"Only core emits."},{"t":"text","v":" "},{"t":"code","v":"ctx.events"},{"t":"text","v":" carries "},{"t":"code","v":"on"},{"t":"text","v":" and nothing else. A mod that could emit "},{"t":"code","v":"host:removed"},{"t":"text","v":" could lie to every other mod about core state, and inter-mod messaging is a separate surface with its own trust story (#199). Adding an emit later is additive."}],"t":"p"},{"spans":[{"t":"strong","v":"Where each event comes from"},{"t":"text","v":", so you know what it means:"}],"t":"p"},{"spans":[{"t":"code","v":"host:auth"},{"t":"text","v":" — a completed host authentication, and a poll that finds a host's"}],"t":"bullet"},{"spans":[{"t":"text","v":"auth has recovered."}],"t":"p"},{"spans":[{"t":"code","v":"host:changed"},{"t":"text","v":" — a committed host add/edit, the host colour swatch, and the"}],"t":"bullet"},{"spans":[{"t":"text","v":"hidden toggle. Only paths that actually mutated emit it. The edge carries an id, not a snapshot: re-read what you care about and decide."}],"t":"p"},{"spans":[{"t":"code","v":"host:removed"},{"t":"text","v":" — after the whole removal has finished, so it means \"that"}],"t":"bullet"},{"spans":[{"t":"text","v":"host is gone\", not \"it is going\"."}],"t":"p"},{"spans":[{"t":"code","v":"state:adopted"},{"t":"text","v":" — the first "},{"t":"code","v":"/state"},{"t":"text","v":" adoption, whether that is the boot pull"}],"t":"bullet"},{"spans":[{"t":"text","v":"or a lease-loss rebuild that beat it."}],"t":"p"},{"spans":[{"t":"code","v":"lease:changed"},{"t":"text","v":" — the home single-active lease, per control frame."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Feature-detect it."},{"t":"text","v":" "},{"t":"code","v":"events"},{"t":"text","v":" is its own capability family at version 1, so "},{"t":"code","v":"ctx.capabilities.events"},{"t":"text","v":", "},{"t":"code","v":"if (ctx.events)"},{"t":"text","v":" / "},{"t":"code","v":"typeof ctx.events.on === 'function'"},{"t":"text","v":" and "},{"t":"code","v":"needs: ['events']"},{"t":"text","v":" all work. "},{"t":"code","v":"mods/help/help.js"},{"t":"text","v":" is the shipped worked example — its corpus refetch on "},{"t":"code","v":"host:auth"},{"t":"text","v":", replacing the "},{"t":"code","v":"win._onHostAuth"},{"t":"text","v":" field it used to write. Note it "},{"t":"strong","v":"feature-detects instead of declaring "},{"t":"code","v":"needs: ['events']"},{"t":"text","v":", deliberately: a "},{"t":"code","v":"needs"},{"t":"text","v":" blocks the whole mod, and Help without the bus is missing one corpus refresh, while Help blocked has no chip, no window kind and no hotkey. Weigh that trade the same way."}],"t":"p"},{"spans":[{"t":"text","v":"Core still invokes "},{"t":"code","v":"win._onHostAuth"},{"t":"text","v":" by name for the three mods #204 has yet to migrate. That is legacy, not a pattern — do not add a fourth."}],"t":"p"},{"spans":[{"t":"text","v":"Per-window teardown: "},{"t":"code","v":"info.onModTeardown"},{"t":"text","v":" (#195)"}],"t":"sub"},{"spans":[{"t":"text","v":"A mod that decorates a "},{"t":"strong","v":"terminal"},{"t":"text","v":" window has two exits to cover, owned by two channels, neither of which covers the other:"}],"t":"p"},{"spans":[{"t":"text","v":"chain · fires when · does "},{"t":"strong","v":"not"},{"t":"text","v":" fire when"}],"t":"sub"},{"spans":[{"t":"code","v":"win.cleanups"},{"t":"text","v":" — "},{"t":"code","v":"info.onDispose"},{"t":"text","v":" — the "},{"t":"strong","v":"window"},{"t":"text","v":" closes ("},{"t":"code","v":"closeWindow"},{"t":"text","v":", and the lease-loss rebuild) — the mod is disabled with that window still open"}],"t":"bullet"},{"spans":[{"t":"code","v":"rec.unloads"},{"t":"text","v":" — "},{"t":"code","v":"ctx.onUnload"},{"t":"text","v":" — the "},{"t":"strong","v":"mod"},{"t":"text","v":" is disabled — one of the windows it decorated closes"}],"t":"bullet"},{"spans":[{"t":"text","v":"So a widget needed a disposer on both, an idempotence flag so the first exit disarmed the second, and a set so the mod-side entry could still reach the open windows. Core keeps all of that now:"}],"t":"p"},{"spans":[{"t":"code","v":"ctx.windows.onTerminalCreate(function (info) {\n    info.addTitleBarItem(btn);\n    if (!(typeof info.onModTeardown === 'function'\n          && info.onModTeardown(teardown))) {\n        info.onDispose(teardown);        // refusal / older build — see below\n    }\n});"}],"t":"pre"},{"spans":[{"t":"strong","v":"Exactly once, on the first of the two."},{"t":"text","v":" Both exits fire the same"}],"t":"bullet"},{"spans":[{"t":"text","v":"per-(mod, window) state, and it is marked spent before its callbacks run — so close-then-disable, disable-then-close, and a callback that closes its own window or disables its own mod from inside the teardown it is running in are all the same single call."}],"t":"p"},{"spans":[{"t":"strong","v":"A disable does not close the window."},{"t":"text","v":" A terminal belongs to core, not to"}],"t":"bullet"},{"spans":[{"t":"text","v":"the mod that decorated it: this removes the decoration and leaves everything else where it was. (#194's staged take-down does close a mod's windows at disable — those are the mod's own factory windows. A terminal never is.)"}],"t":"p"},{"spans":[{"t":"strong","v":"It returns a boolean"},{"t":"text","v":", and a refusal is never a throw: "},{"t":"code","v":"false"},{"t":"text","v":" for a"}],"t":"bullet"},{"spans":[{"t":"text","v":"non-function, for a window that is already gone, for this mod being mid-teardown, and for a state that has already fired. You can see the "},{"t":"code","v":"false"},{"t":"text","v":" and run your own cleanup, which is exactly what the fallback above is."}],"t":"p"},{"spans":[{"t":"strong","v":"Callbacks run LIFO"},{"t":"text","v":", each isolated, so one broken disposer strands neither"}],"t":"bullet"},{"spans":[{"t":"text","v":"the window's remaining ones nor the chain behind them."}],"t":"p"},{"spans":[{"t":"strong","v":"The state is per (mod, window), not per delivery."},{"t":"text","v":" A mod may hold more than"}],"t":"bullet"},{"spans":[{"t":"text","v":"one "},{"t":"code","v":"onTerminalCreate"},{"t":"text","v":" subscription and every one is handed a bag for the same window; they all register into one state, so a window carries one "},{"t":"code","v":"win.cleanups"},{"t":"text","v":" entry per "},{"t":"strong","v":"mod"},{"t":"text","v":" however many teardowns that mod registers on it."}],"t":"p"},{"spans":[{"t":"strong","v":"Where it sits in the LIFO chain."},{"t":"text","v":" The mod-side entry is pushed at your"}],"t":"bullet"},{"spans":[{"t":"text","v":"first accepted registration — normally inside "},{"t":"code","v":"init()"},{"t":"text","v":", on the "},{"t":"code","v":"onTerminalCreate"},{"t":"text","v":" replay — so it is an early entry and therefore runs late, after disposers you registered afterwards. A self-contained widget teardown belongs there; if you need your widget gone before some other resource you own, tear that one down from the same callback."}],"t":"p"},{"spans":[{"t":"strong","v":"No new capability entry, and "},{"t":"code","v":"needs"},{"t":"strong","v":" cannot name it."},{"t":"text","v":" "},{"t":"code","v":"windows"},{"t":"text","v":" is already"}],"t":"bullet"},{"spans":[{"t":"text","v":"a v1 family and this adds a member to it, so the map is unchanged; "},{"t":"code","v":"needs"},{"t":"text","v":" resolves a path on "},{"t":"code","v":"ctx"},{"t":"text","v":" (§5) and the per-delivery info bag is not reachable from "},{"t":"code","v":"ctx"},{"t":"text","v":". Feature-detect with "},{"t":"code","v":"typeof info.onModTeardown === 'function'"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"One accepted cost: a disable leaves its spent "},{"t":"code","v":"win.cleanups"},{"t":"text","v":" entry on the"}],"t":"bullet"},{"spans":[{"t":"text","v":"window. Splicing it out at fire time is the one thing this must not do — "},{"t":"code","v":"closeWindow"},{"t":"text","v":" iterates that array with "},{"t":"code","v":"for…of"},{"t":"text","v":", and removing an entry mid-drain skips the next one — so an enable/disable cycle on a long-lived terminal leaves one dead closure per cycle."}],"t":"p"},{"spans":[{"t":"code","v":"mods/git/git.js"},{"t":"strong","v":" is the shipped reference migration."},{"t":"text","v":" Its hand-kept "},{"t":"code","v":"disposers"},{"t":"text","v":" Set, the "},{"t":"code","v":"ctx.onUnload"},{"t":"text","v":" block that drained it and the decorate-once "},{"t":"code","v":"decorated"},{"t":"text","v":" "},{"t":"code","v":"WeakSet"},{"t":"text","v":" in front of both are all gone, replaced by the one registration above at the same site. (The "},{"t":"code","v":"WeakSet"},{"t":"text","v":" needed no replacement: "},{"t":"code","v":"onTerminalCreate"},{"t":"text","v":" already delivers each window once per subscription.) What it "},{"t":"strong","v":"keeps"},{"t":"text","v":" is the "},{"t":"code","v":"info.onDispose"},{"t":"text","v":" fallback, deliberately: "},{"t":"code","v":"onModTeardown"},{"t":"text","v":" is absent on a page assembled without the extension fragment and returns "},{"t":"code","v":"false"},{"t":"text","v":" on a dead window or a mod mid-teardown, and in those cases arming the window-close half alone is still worth it — a leaked 15 s poll plus a live document-level listener on every terminal is the worse failure. It is the one line the mod rode before, not a second set."}],"t":"p"},{"spans":[{"t":"text","v":"Forgetting a host: "},{"t":"code","v":"ctx.hosts.invalidate"},{"t":"text","v":" (#195)"}],"t":"sub"},{"spans":[{"t":"code","v":"hosts"},{"t":"text","v":" is a "},{"t":"strong","v":"new family"},{"t":"text","v":" at capability version 1, and "},{"t":"code","v":"invalidate(id)"},{"t":"text","v":" is its first member: \"forget everything this page knows about host "},{"t":"code","v":"<id>"},{"t":"text","v":"\"."}],"t":"p"},{"spans":[{"t":"text","v":"It clears "},{"t":"strong","v":"every registered per-host cache"},{"t":"text","v":" and then repaints the host surfaces (the hosts list, the settings tabs, the host status chips and the taskbar). The list is core's, not a caller's — each cache registers itself beside its own declaration — so the eight this build has (the per-host state cache and its save chain, the auth-prompt latch, the poll record, the "},{"t":"code","v":"/control"},{"t":"text","v":" socket, and three config caches) and whatever the next fragment adds are covered by the one call. The cautionary example is in the tree: "},{"t":"code","v":"mods/host-registry/"},{"t":"text","v":" still carries its own hand-maintained copy of that list, and its own comment calls that copy \"a known latent gap\". A list kept by a stranger is right on the day it is written and nothing makes it stay right — call this instead."}],"t":"p"},{"spans":[{"t":"text","v":"It returns "},{"t":"code","v":"true"},{"t":"text","v":" when it ran and "},{"t":"code","v":"false"},{"t":"text","v":" for a blank / non-string id (nothing to forget, and no repaint owed) or an internal failure — "},{"t":"strong","v":"never a throw"},{"t":"text","v":", so calling it from inside an event handler cannot take a repaint down with a bad argument."}],"t":"p"},{"spans":[{"t":"text","v":"Two things it deliberately does "},{"t":"strong","v":"not"},{"t":"text","v":" do:"}],"t":"p"},{"spans":[{"t":"strong","v":"It does not emit."},{"t":"text","v":" Invalidation and notification are separate acts: only"}],"t":"bullet"},{"spans":[{"t":"text","v":"core mutation sites emit "},{"t":"code","v":"host:changed"},{"t":"text","v":". So a "},{"t":"code","v":"host:changed"},{"t":"text","v":" subscriber may call "},{"t":"code","v":"ctx.hosts.invalidate()"},{"t":"text","v":" without recursing."}],"t":"p"},{"spans":[{"t":"strong","v":"It does not save."},{"t":"text","v":" "},{"t":"code","v":"savePrefs()"},{"t":"text","v":" is a mutation (localStorage plus a"}],"t":"bullet"},{"spans":[{"t":"code","v":"/state"},{"t":"text","v":" push) and stays with the caller that actually changed something, so a mod can drop stale per-host state without writing prefs it does not own."}],"t":"p"},{"spans":[{"t":"text","v":"Clearing a cache does not cancel an in-flight request; it drops the stale value so the next request uses the new url/token, and the poll loop re-primes on its next tick."}],"t":"p"},{"spans":[{"t":"text","v":"Feature-detect with "},{"t":"code","v":"typeof ctx.hosts.invalidate === 'function'"},{"t":"text","v":" or "},{"t":"code","v":"needs: ['hosts.invalidate']"},{"t":"text","v":". On a build whose core has no invalidation path the family is not installed "},{"t":"strong","v":"at all"},{"t":"text","v":", so "},{"t":"code","v":"ctx.capabilities"},{"t":"text","v":" reports "},{"t":"code","v":"hosts"},{"t":"text","v":" absent — true — rather than handing you a member that silently does nothing."}],"t":"p"},{"spans":[{"t":"text","v":"Compare-and-swap writes: "},{"t":"code","v":"ctx.serverStore.update"},{"t":"text","v":" (#196)"}],"t":"sub"},{"spans":[{"t":"code","v":"const res = await ctx.serverStore.update(fn, {\n    host?, retries?, purgeRevisions?, noHistory? });\n// -> {ok:true, rev, value} | {ok:false, error, status?, rev?, value?}"}],"t":"pre"},{"spans":[{"t":"code","v":"get"},{"t":"text","v":" → "},{"t":"code","v":"fn(value, rev)"},{"t":"text","v":" → "},{"t":"code","v":"set"},{"t":"text","v":", and "},{"t":"strong","v":"on a "},{"t":"code","v":"409 conflict"},{"t":"strong","v":" it re-reads and re-applies "},{"t":"code","v":"fn"},{"t":"strong","v":" to the winner's value"},{"t":"text","v":". The blob that lost is never re-"},{"t":"code","v":"PUT"},{"t":"text","v":". That single behaviour is the whole reason the helper exists."}],"t":"p"},{"spans":[{"t":"code","v":"fn"},{"t":"strong","v":" must be pure over "},{"t":"code","v":"(value, rev)"},{"t":"strong","v":"."},{"t":"text","v":" It is invoked "},{"t":"strong","v":"once per attempt"},{"t":"text","v":", so a side effect inside it happens once per attempt — bumping a counter, appending to a log, showing a toast, queuing another save. That is a caller bug, and the code deliberately does not defend against it, because the only available defence is to snapshot once and re-send that snapshot after a "},{"t":"code","v":"409"},{"t":"text","v":" — which is the #158 clobber, now with retries. Everything the write depends on that can change between attempts (the live editor buffer, the current selection, "},{"t":"code","v":"Date.now()"},{"t":"text","v":") belongs "},{"t":"strong","v":"inside"},{"t":"text","v":" "},{"t":"code","v":"fn"},{"t":"text","v":", where it re-runs over the winner."}],"t":"p"},{"spans":[{"t":"text","v":"Which is also why the shape is a "},{"t":"strong","v":"reducer and not a snapshot-getter"},{"t":"text","v":". An "},{"t":"code","v":"update(gatherValue)"},{"t":"text","v":" API cannot rebase: it has nothing to apply the winner's value to, so its only move after a "},{"t":"code","v":"409"},{"t":"text","v":" is to overwrite. The winner's value has to flow through your function."}],"t":"p"},{"spans":[{"t":"text","v":"The rest of the contract:"}],"t":"p"},{"spans":[{"t":"code","v":"value"},{"t":"strong","v":" is the winner's value"},{"t":"text","v":", freshly parsed on every attempt, and"}],"t":"bullet"},{"spans":[{"t":"code","v":"null"},{"t":"text","v":" on a store nobody has written yet. "},{"t":"code","v":"rev"},{"t":"text","v":" is the revision it was read at. Mutating "},{"t":"code","v":"value"},{"t":"text","v":" in place is safe, but the next value must be "},{"t":"strong","v":"returned"},{"t":"text","v":" — a reducer that only mutates returns "},{"t":"code","v":"undefined"},{"t":"text","v":", which is refused ("},{"t":"code","v":"bad_value"},{"t":"text","v":") "},{"t":"strong","v":"before"},{"t":"text","v":" any write, because the server 400s on a missing "},{"t":"code","v":"value"},{"t":"text","v":" and a round trip is a slow way to learn that."}],"t":"p"},{"spans":[{"t":"strong","v":"An async reducer is first-class"},{"t":"text","v":" — "},{"t":"code","v":"fn"},{"t":"text","v":" may return a promise."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Bounded, and the conflict is surfaced."},{"t":"text","v":" One write plus a bounded number of"}],"t":"bullet"},{"spans":[{"t":"text","v":"rebases; "},{"t":"code","v":"retries"},{"t":"text","v":" moves that budget and is "},{"t":"strong","v":"clamped"},{"t":"text","v":" — the ceiling is not the caller's to remove, because the mirror failure of a silent clobber is a silent infinite rebase against a hot writer, and every attempt also writes into the store's revision ring (#175). Exhausting the budget resolves "},{"t":"code","v":"{ok:false, error:'conflict'}"},{"t":"text","v":" with the last winner's "},{"t":"code","v":"rev"},{"t":"text","v":" and "},{"t":"code","v":"value"},{"t":"text","v":" still inlined, and "},{"t":"strong","v":"you"},{"t":"text","v":" decide whether to widen, warn or drop."}],"t":"p"},{"spans":[{"t":"code","v":"not_active"},{"t":"strong","v":" is refused, not rebased."},{"t":"text","v":" It is a "},{"t":"code","v":"409"},{"t":"text","v":" too, but it means"}],"t":"bullet"},{"spans":[{"t":"text","v":"this browser does not hold the "},{"t":"code","v":"/state"},{"t":"text","v":" write lease, so re-applying "},{"t":"code","v":"fn"},{"t":"text","v":" at a fresher revision would burn the entire budget only to be refused again. It comes straight back with the live "},{"t":"code","v":"rev"},{"t":"text","v":" inlined."}],"t":"p"},{"spans":[{"t":"strong","v":"A "},{"t":"code","v":"409"},{"t":"strong","v":" that inlines the live value costs no extra round trip"},{"t":"text","v":"; one that"}],"t":"bullet"},{"spans":[{"t":"text","v":"does not (an older broker, or a remote one whose body is untrusted input) falls back to an authoritative re-read — and "},{"t":"strong","v":"a failed read aborts without writing"},{"t":"text","v":". So does the initial read: a response that is not this store's own "},{"t":"code","v":"200"},{"t":"text","v":" shape is a refusal, never \"an empty store\", because reading it as rev 0 / "},{"t":"code","v":"value: null"},{"t":"text","v":" would run your reducer down its \"nothing stored yet\" branch on a store nobody could actually read."}],"t":"p"},{"spans":[{"t":"code","v":"opts"},{"t":"strong","v":" is snapshotted once and rides every attempt"},{"t":"text","v":", rebases included, so"}],"t":"bullet"},{"spans":[{"t":"code","v":"purgeRevisions"},{"t":"text","v":" (#65) and "},{"t":"code","v":"noHistory"},{"t":"text","v":" (#192) cannot be dropped by a retry. "},{"t":"code","v":"noHistory"},{"t":"text","v":" keeps "},{"t":"code","v":"set()"},{"t":"text","v":"'s strict tri-state: it is "},{"t":"strong","v":"presence-keyed"},{"t":"text","v":", and "},{"t":"strong","v":"absent is not "},{"t":"code","v":"false"},{"t":"text","v":" — absence means \"leave this record's flag exactly as it is\", so a retry that helpfully sent "},{"t":"code","v":"noHistory:false"},{"t":"text","v":" would resume archiving the very credentials the flag exists to keep out of the ring."}],"t":"p"},{"spans":[{"t":"strong","v":"Fail-closed, inherited."},{"t":"text","v":" Every request goes through your own"}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.serverStore.get"},{"t":"text","v":"/"},{"t":"code","v":"set"},{"t":"text","v":", so it is mod-scoped, lease-carrying and host-routed exactly as they are: an unknown "},{"t":"code","v":"opts.host"},{"t":"text","v":" makes "},{"t":"strong","v":"no request"},{"t":"text","v":" and refuses before any "},{"t":"code","v":"PUT"},{"t":"text","v":" is attempted."}],"t":"p"},{"spans":[{"t":"strong","v":"It never rejects."},{"t":"text","v":" Every outcome resolves to a result, including"}],"t":"bullet"},{"spans":[{"t":"code","v":"{ok:false, error:'fn_failed'}"},{"t":"text","v":" when your reducer throws (it is logged) and "},{"t":"code","v":"bad_fn"},{"t":"text","v":" when the first argument is not a function."}],"t":"p"},{"spans":[{"t":"text","v":"Feature-detect with "},{"t":"code","v":"typeof ctx.serverStore.update === 'function'"},{"t":"text","v":" or "},{"t":"code","v":"needs: ['serverStore.update']"},{"t":"text","v":". It is a new "},{"t":"strong","v":"member"},{"t":"text","v":", not a new family, so there is no capability entry for it: "},{"t":"code","v":"ctx.capabilities"},{"t":"text","v":" is keyed by family name ("},{"t":"code","v":"serverStore"},{"t":"text","v":"), never by dotted path — the same as "},{"t":"code","v":"ctx.windows.createAppWindow"},{"t":"text","v":". "},{"t":"code","v":"ctxVersion"},{"t":"text","v":" stays "},{"t":"code","v":"1"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The save chain: "},{"t":"code","v":"ctx.serverStore.saveChain"},{"t":"text","v":" (#196)"}],"t":"sub"},{"spans":[{"t":"code","v":"const chain = ctx.serverStore.saveChain({\n    host?, debounceMs?, retries?, purgeRevisions?, noHistory? });\nchain.save(fn);      // -> true (queued) | false (a dead chain, or a non-fn)\nchain.flush();       // -> Promise<result>, never rejects\nchain.onState(fn);   // 'idle' | 'saving' | 'conflict'  -> unsubscribe fn"}],"t":"pre"},{"spans":[{"t":"code","v":"scratchpad"},{"t":"text","v":"'s save pipeline, canned. It adds "},{"t":"strong","v":"nothing"},{"t":"text","v":" to the write itself — every send goes through "},{"t":"code","v":"update()"},{"t":"text","v":", so the "},{"t":"code","v":"409"},{"t":"text","v":" rebase, the bounded budget, the fail-closed host lookup and the verbatim "},{"t":"code","v":"purgeRevisions"},{"t":"text","v":" / "},{"t":"code","v":"noHistory"},{"t":"text","v":" passthrough are inherited rather than re-implemented. What the chain owns is when to write, how many writes to make, and what to tell the user while it happens."}],"t":"p"},{"spans":[{"t":"code","v":"save(fn)"},{"t":"text","v":" takes the "},{"t":"strong","v":"same pure "},{"t":"code","v":"(value, rev)"},{"t":"strong","v":" reducer"},{"t":"text","v":" "},{"t":"code","v":"update()"},{"t":"text","v":" takes, and the purity rule above applies unchanged — including this specific case: "},{"t":"strong","v":"do not call this chain's own "},{"t":"code","v":"save()"},{"t":"strong","v":" from inside a reducer"},{"t":"text","v":", or a rebase queues the same work twice."}],"t":"p"},{"spans":[{"t":"strong","v":"Coalesced by composition, not by replacement."},{"t":"text","v":" Rapid saves queue, and the"}],"t":"bullet"},{"spans":[{"t":"text","v":"queue is spliced into "},{"t":"strong","v":"one"},{"t":"text","v":" batch composed in order — "},{"t":"code","v":"fn2(fn1(value, rev), rev)"},{"t":"text","v":" — handed to "},{"t":"code","v":"update()"},{"t":"text","v":" as a single reducer. So two queued saves "},{"t":"strong","v":"both apply"},{"t":"text","v":"; an earlier caller's delta is never dropped on the floor. For a reducer that produces the whole document every time (scratchpad's) that degenerates to last-wins, which is exactly the old behaviour. Two consequences: every reducer in a batch sees the "},{"t":"strong","v":"same"},{"t":"text","v":" base "},{"t":"code","v":"rev"},{"t":"text","v":", and one reducer that throws or forgets to return refuses the "},{"t":"strong","v":"whole"},{"t":"text","v":" batch, under "},{"t":"code","v":"update()"},{"t":"text","v":"'s own error names."}],"t":"p"},{"spans":[{"t":"strong","v":"Single in flight, per chain."},{"t":"text","v":" At most one write from a given chain is"}],"t":"bullet"},{"spans":[{"t":"text","v":"outstanding; saves arriving during it leave as exactly one more write. Per chain is the honest scope — two chains in one mod address the "},{"t":"strong","v":"same"},{"t":"text","v":" per-mod record on a given broker ("},{"t":"code","v":"opts.host"},{"t":"text","v":" only picks whose copy), so they serialize with each other only through the server's revision CAS, each re-applying over the other's winner. Correct, but it spends conflict budget: write one chain per record."}],"t":"p"},{"spans":[{"t":"strong","v":"Trailing debounce."},{"t":"text","v":" Each "},{"t":"code","v":"save()"},{"t":"text","v":" pushes the deadline out, which is what"}],"t":"bullet"},{"spans":[{"t":"text","v":"makes a burst one write — and means a genuinely continuous writer only leaves via the in-flight and "},{"t":"code","v":"flush()"},{"t":"text","v":" paths. "},{"t":"code","v":"debounceMs"},{"t":"text","v":" overrides the default and is "},{"t":"strong","v":"clamped"},{"t":"text","v":", for the reason in the next bullet: a pending batch is dropped at teardown, so an absurd debounce is data loss wearing a save's clothes."}],"t":"p"},{"spans":[{"t":"code","v":"onState"},{"t":"text","v":" reports "},{"t":"code","v":"'saving'"},{"t":"text","v":" while a write is outstanding, "},{"t":"code","v":"'conflict'"}],"t":"bullet"},{"spans":[{"t":"text","v":"for an unresolved refusable "},{"t":"code","v":"409"},{"t":"text","v":" ("},{"t":"code","v":"conflict"},{"t":"text","v":" or "},{"t":"code","v":"not_active"},{"t":"text","v":" — scratchpad's read-only banner), "},{"t":"code","v":"'idle'"},{"t":"text","v":" otherwise. It is deduplicated, it "},{"t":"strong","v":"fires once on subscribe"},{"t":"text","v":" so a banner mounted after the fact is correct without reconstructing anything, a throwing listener cannot cost the chain its pump, and the returned unsubscribe is yours to call."}],"t":"p"},{"spans":[{"t":"code","v":"flush()"},{"t":"text","v":" forces the pending batch past the debounce and resolves once an"}],"t":"bullet"},{"spans":[{"t":"text","v":"in-flight write and anything already queued have completed — never waiting on writes queued after it. With nothing outstanding it resolves "},{"t":"code","v":"{ok:true, idle:true}"},{"t":"text","v":", except while a conflict is unresolved, where it reports that result rather than a fresh "},{"t":"code","v":"ok"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"It never retries a failed batch by itself."},{"t":"text","v":" "},{"t":"code","v":"update()"},{"t":"text","v":" already rebases a"}],"t":"bullet"},{"spans":[{"t":"code","v":"409"},{"t":"text","v":" up to its budget; past that the batch is dropped and the chain reports "},{"t":"code","v":"'conflict'"},{"t":"text","v":". Scratchpad's answer is to re-attempt on the next interaction while the banner is up; yours is yours."}],"t":"p"},{"spans":[{"t":"strong","v":"The trap: a chain drops its pending batch at teardown."},{"t":"text","v":" Deliberately. The timer is cleared, the queue is discarded, the state listeners are released, and every outstanding "},{"t":"code","v":"flush()"},{"t":"text","v":" promise resolves "},{"t":"code","v":"{ok:false, error:'unloaded'}"},{"t":"text","v":" rather than hanging the caller awaiting it. From the first instant of a disable the chain reads dead — "},{"t":"code","v":"save()"},{"t":"text","v":" returns "},{"t":"code","v":"false"},{"t":"text","v":", "},{"t":"code","v":"flush()"},{"t":"text","v":" resolves "},{"t":"code","v":"{ok:false, error:'unloaded'}"},{"t":"text","v":", and a disable landing between "},{"t":"code","v":"update()"},{"t":"text","v":"'s read and its write is a drop, not a race."}],"t":"p"},{"spans":[{"t":"text","v":"That is not the same posture as the "},{"t":"code","v":"ctx.settings.text"},{"t":"text","v":" debounce, which does flush on teardown, and the difference is the point: a settings commit reaches "},{"t":"code","v":"localStorage"},{"t":"text","v":" synchronously, so a teardown can complete it, while a chain write is a network round trip that a "},{"t":"strong","v":"synchronous"},{"t":"text","v":" disable cannot await. \"Flush on the way down\" is precisely the write that lands after a re-enable and clobbers the new activation."}],"t":"p"},{"spans":[{"t":"text","v":"So "},{"t":"strong","v":"a deliberate final flush must be called while the mod is still alive"},{"t":"text","v":" — from a window-close cleanup ("},{"t":"code","v":"win.cleanups"},{"t":"text","v":", which also runs on an ordinary close, with the mod up) or from an explicit save action such as a "},{"t":"kbd","v":"Ctrl+S"},{"t":"text","v":" handler. "},{"t":"strong","v":"Never from "},{"t":"code","v":"ctx.onUnload"},{"t":"text","v":", where the activation is already marked dead and the batch is dropped by design. "},{"t":"code","v":"mods/scratchpad/"},{"t":"text","v":" is the worked example on both counts: it flushes from "},{"t":"code","v":"win.cleanups"},{"t":"text","v":" (capturing the editor buffer first, because a later cleanup in the same drain destroys the editor view while "},{"t":"code","v":"flush()"},{"t":"text","v":" is still resolving), and its "},{"t":"kbd","v":"Ctrl+S"},{"t":"text","v":" path is a "},{"t":"code","v":"save()"},{"t":"text","v":" followed by a "},{"t":"code","v":"flush()"},{"t":"text","v":". What no client-side check can recall is a request already handed to the network; that residual window is named here rather than papered over."}],"t":"p"},{"spans":[{"t":"text","v":"Feature-detect with "},{"t":"code","v":"typeof ctx.serverStore.saveChain === 'function'"},{"t":"text","v":" or "},{"t":"code","v":"needs: ['serverStore.saveChain']"},{"t":"text","v":" — a "},{"t":"strong","v":"member"},{"t":"text","v":", so no capability entry, same as "},{"t":"code","v":"update()"},{"t":"text","v":" above. Scratchpad declares the "},{"t":"code","v":"needs"},{"t":"text","v":" and blocks: saving is that mod, so a build without the chain should read "},{"t":"code","v":"blocked (needs serverStore.saveChain)"},{"t":"text","v":" rather than open a notes window that silently drops every keystroke. "},{"t":"code","v":"mods/help/"},{"t":"text","v":" makes the opposite call for "},{"t":"code","v":"ctx.events"},{"t":"text","v":" and feature-detects instead, because a missing bus costs it one corpus refresh rather than all of its persistence. "},{"t":"strong","v":"Block when the loss is total; degrade when it is partial"},{"t":"text","v":" — the two shipped mods are there to be copied from, in whichever direction fits."}],"t":"p"},{"spans":[{"t":"text","v":"Browser-local preferences: "},{"t":"code","v":"ctx.prefs"},{"t":"text","v":" (#196)"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.prefs.get(key, default);   // -> the stored JSON value, or `default`\nctx.prefs.set(key, value);     // -> true once it reads back\nctx.prefs.remove(key);         // -> true (false only for a bad key)"}],"t":"pre"},{"spans":[{"t":"text","v":"A mod's own preferences, per "},{"t":"strong","v":"browser"},{"t":"text","v":", with a contract instead of a coincidence. Before this, both ways of reaching for core's "},{"t":"code","v":"prefs"},{"t":"text","v":" object were wrong: a "},{"t":"strong","v":"bare"},{"t":"text","v":" key is wiped by \"Reset local view\"'s non-underscore sweep, and an "},{"t":"strong","v":"underscore"},{"t":"text","v":" key stays local only because the "},{"t":"code","v":"/state"},{"t":"text","v":" blob happens to serialize "},{"t":"code","v":"_settings"},{"t":"text","v":" and "},{"t":"code","v":"_layout"},{"t":"text","v":" and nothing else — one edit there and it syncs, exactly as "},{"t":"code","v":"_settings"},{"t":"text","v":" already does (#153)."}],"t":"p"},{"spans":[{"t":"strong","v":"The namespace is the decision."},{"t":"text","v":" "},{"t":"code","v":"webterm:modprefs:<id>:<key>"},{"t":"text","v":", one "},{"t":"code","v":"localStorage"},{"t":"text","v":" record per key, a "},{"t":"strong","v":"sibling"},{"t":"text","v":" of the "},{"t":"code","v":"webterm:mod:<id>:"},{"t":"text","v":" namespace "},{"t":"code","v":"ctx.storage"},{"t":"text","v":" owns — never a sub-prefix inside it, and never a key on the core "},{"t":"code","v":"prefs"},{"t":"text","v":" object. Three consequences, each of them the point:"}],"t":"p"},{"spans":[{"t":"strong","v":"\"Reset local view\" cannot reach it."},{"t":"text","v":" That sweep deletes keys off the"}],"t":"bullet"},{"spans":[{"t":"code","v":"prefs"},{"t":"text","v":" object and re-serializes it; a "},{"t":"code","v":"modprefs"},{"t":"text","v":" record is not in it."}],"t":"p"},{"spans":[{"t":"strong","v":"The sync path cannot reach it."},{"t":"text","v":" The "},{"t":"code","v":"/state"},{"t":"text","v":" blob is built from"}],"t":"bullet"},{"spans":[{"t":"code","v":"prefs._settings"},{"t":"text","v":" and "},{"t":"code","v":"prefs._layout"},{"t":"text","v":", so a "},{"t":"code","v":"ctx.prefs"},{"t":"text","v":" write leaves the serialized state byte-identical and no "},{"t":"code","v":"/state"},{"t":"text","v":" "},{"t":"code","v":"PUT"},{"t":"text","v":" is attributable to one. It is not \"local by convention\" — it is structurally unable to sync. A future \"sync my prefs\" is therefore a "},{"t":"strong","v":"new tier"},{"t":"text","v":", not a flag on this one."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.storage"},{"t":"strong","v":" keeps its semantics exactly"},{"t":"text","v":" — no reserved-prefix policing,"}],"t":"bullet"},{"spans":[{"t":"text","v":"and no way to clobber a preference record through the raw surface, since a mod id may not contain "},{"t":"code","v":":"},{"t":"text","v":" and both namespaces end in one."}],"t":"p"},{"spans":[{"t":"text","v":"Values are "},{"t":"strong","v":"JSON"},{"t":"text","v":", not strings (that is the other difference from "},{"t":"code","v":"ctx.storage"},{"t":"text","v":"). The failure posture is "},{"t":"code","v":"ctx.storage"},{"t":"text","v":"'s, copied deliberately:"}],"t":"p"},{"spans":[{"t":"code","v":"get(key, default)"},{"t":"text","v":" returns "},{"t":"code","v":"default"},{"t":"text","v":" for a missing key, a bad key and a"}],"t":"bullet"},{"spans":[{"t":"strong","v":"corrupt record"},{"t":"text","v":" — a half-written or hand-edited value is a miss, never a throw that takes down the "},{"t":"code","v":"init()"},{"t":"text","v":" reading it."}],"t":"p"},{"spans":[{"t":"code","v":"set"},{"t":"text","v":" returns "},{"t":"code","v":"false"},{"t":"text","v":" for a bad key or a value JSON cannot represent (a cycle,"}],"t":"bullet"},{"spans":[{"t":"code","v":"undefined"},{"t":"text","v":", a function) — and in that case "},{"t":"strong","v":"nothing is written"},{"t":"text","v":", so a refused "},{"t":"code","v":"set"},{"t":"text","v":" never destroys what is already stored."}],"t":"p"},{"spans":[{"t":"text","v":"A "},{"t":"code","v":"localStorage"},{"t":"text","v":" that refuses (private mode, quota) is swallowed and the value"}],"t":"bullet"},{"spans":[{"t":"text","v":"is kept "},{"t":"strong","v":"in memory for the life of the page"},{"t":"text","v":" — not the activation — so a disable and re-enable still finds what "},{"t":"code","v":"localStorage"},{"t":"text","v":" would have kept. A refused "},{"t":"code","v":"remove"},{"t":"text","v":" reads as removed for the rest of the session rather than resurrecting the record it could not delete."}],"t":"p"},{"spans":[{"t":"code","v":"prefs"},{"t":"text","v":" is a "},{"t":"strong","v":"new family"},{"t":"text","v":" at capability version 1, so it does have a capability entry: gate with "},{"t":"code","v":"ctx.prefs && typeof ctx.prefs.get === 'function'"},{"t":"text","v":", "},{"t":"code","v":"ctx.capabilities.prefs"},{"t":"text","v":", or "},{"t":"code","v":"needs: ['prefs']"},{"t":"text","v":". "},{"t":"code","v":"ctxVersion"},{"t":"text","v":" stays "},{"t":"code","v":"1"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"And it is storage, not a boundary: like everything else on this page (§1), the records sit in the page's own "},{"t":"code","v":"localStorage"},{"t":"text","v":", where any script on the origin can read them. Namespacing keeps mods from colliding, not from looking."}],"t":"p"},{"spans":[{"t":"text","v":"Per-activation cancellation: "},{"t":"code","v":"ctx.signal"},{"t":"text","v":" (#198)"}],"t":"sub"},{"spans":[{"t":"code","v":"ctx.signal                             // an AbortSignal for THIS activation\n\nconst res = await ctx.file.read(path, { signal: ctx.signal });\nif (res.aborted) return;               // this resumes AFTER teardown finished —\nstatusNode.textContent = res.error;    // check before touching anything of yours\n\nif (!ctx.signal.aborted) {             // `.aborted`, never bare `if (ctx.signal)`\n    const t = setTimeout(poll, 5000);\n    ctx.signal.addEventListener('abort', function () { clearTimeout(t); });\n}"}],"t":"pre"},{"spans":[{"t":"text","v":"One "},{"t":"code","v":"AbortSignal"},{"t":"text","v":" per "},{"t":"strong","v":"activation"},{"t":"text","v":", aborted when that activation ends. It replaces the dead-flag pattern mods hand-rolled to stop an in-flight loop from touching a desktop it no longer belongs to ("},{"t":"code","v":"mods/update/"},{"t":"text","v":"'s "},{"t":"code","v":"restartOpDead"},{"t":"text","v":" / "},{"t":"code","v":"applyOpDead"},{"t":"text","v":" are the worked examples, and are still written by hand — the migrations are not part of this)."}],"t":"p"},{"spans":[{"t":"strong","v":"Per activation, and freshness is structural."},{"t":"text","v":" The controller hangs off the"}],"t":"bullet"},{"spans":[{"t":"text","v":"per-mod record "},{"t":"code","v":"initMod"},{"t":"text","v":" builds, and it builds a new one every time a mod comes up. A disabled-then-re-enabled mod therefore gets a "},{"t":"strong","v":"new, un-aborted"},{"t":"text","v":" signal; the old activation's signal stays aborted forever, which is the point — its loops must not come back to life when the mod does."}],"t":"p"},{"spans":[{"t":"strong","v":"The abort is the first thing teardown does."},{"t":"text","v":" In order: the record is marked"}],"t":"bullet"},{"spans":[{"t":"text","v":"as unloading, "},{"t":"strong","v":"then the signal aborts"},{"t":"text","v":", then #194's staged close of the mod's factory app windows, then the LIFO "},{"t":"code","v":"onUnload"},{"t":"text","v":" chain (which is where #195's per-window "},{"t":"code","v":"onModTeardown"},{"t":"text","v":" disposers ride, as an entry on that list). Abort "},{"t":"strong","v":"listeners"},{"t":"text","v":" fire synchronously, so a listener is told to stop before anything starts taking your windows away, and before every "},{"t":"code","v":"onUnload"},{"t":"text","v":". That ordering belongs to listeners and to nothing else — read the next bullet before you assume it covers an "},{"t":"code","v":"await"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"Cancelling the work is not cancelling the continuation."},{"t":"text","v":" The take-down"}],"t":"bullet"},{"spans":[{"t":"text","v":"path is entirely synchronous: by the time an aborted "},{"t":"code","v":"await"},{"t":"text","v":" resumes — a later microtask — the staged window close and the whole "},{"t":"code","v":"onUnload"},{"t":"text","v":" drain have already run to completion. So the line after your "},{"t":"code","v":"await"},{"t":"text","v":" executes against a torn-down activation, and for "},{"t":"code","v":"ctx.file"},{"t":"text","v":" that is worse rather than better, because an abort "},{"t":"strong","v":"resolves"},{"t":"text","v":" "},{"t":"code","v":"{ok:false, aborted:true, error:'cancelled'}"},{"t":"text","v":" instead of throwing: there is no rejection to fall out of, and the naive "},{"t":"code","v":"statusNode.textContent = res.error"},{"t":"text","v":" writes into a window that is gone. "},{"t":"strong","v":"A continuation that touches activation-owned state must check first"},{"t":"text","v":" — the result's "},{"t":"code","v":"aborted"},{"t":"text","v":", or "},{"t":"code","v":"ctx.signal.aborted"},{"t":"text","v":" — which is precisely the job "},{"t":"code","v":"mods/update/"},{"t":"text","v":"'s "},{"t":"code","v":"restartOpDead"},{"t":"text","v":" / "},{"t":"code","v":"applyOpDead"},{"t":"text","v":" still do by hand. The signal cancels the request for you; it does not return you to a live mod."}],"t":"p"},{"spans":[{"t":"code","v":"if (ctx.signal)"},{"t":"strong","v":" is a capability check, not a liveness check."},{"t":"text","v":" The ctx is"}],"t":"bullet"},{"spans":[{"t":"text","v":"not dismantled at teardown, so "},{"t":"code","v":"ctx.signal"},{"t":"text","v":" stays truthy forever — it is the aborted signal by then. An async callback that resumes after teardown, sees a truthy "},{"t":"code","v":"ctx.signal"},{"t":"text","v":", arms a "},{"t":"code","v":"setTimeout"},{"t":"text","v":" and registers an abort listener to clear it has built a leak: an already-aborted signal never replays its abort event to a listener added afterwards, so that listener never runs and the timer fires against torn-down state. (Core codes to the same fact — "},{"t":"code","v":"hostFetch"},{"t":"text","v":" tests "},{"t":"code","v":"callerSignal.aborted"},{"t":"text","v":" up front rather than relying on a listener.) Check "},{"t":"code","v":"ctx.signal.aborted"},{"t":"text","v":" "},{"t":"strong","v":"before arming anything"},{"t":"text","v":" on a path that can resume late."}],"t":"p"},{"spans":[{"t":"strong","v":"An abort listener is teardown-time mod code."},{"t":"text","v":" It runs after the record"}],"t":"bullet"},{"spans":[{"t":"text","v":"reads dead, so from inside one a "},{"t":"code","v":"saveChain"},{"t":"text","v":" is already dropping batches and a settings write cannot re-enter your own theme subscriber. It is not a place to rescue a last save from — see the pairing rule below."}],"t":"p"},{"spans":[{"t":"strong","v":"Explicit pass, never ambient."},{"t":"text","v":" The loader injects "},{"t":"code","v":"ctx.signal"},{"t":"text","v":" into"}],"t":"bullet"},{"spans":[{"t":"text","v":"nothing. "},{"t":"code","v":"ctx.file"},{"t":"text","v":" and "},{"t":"code","v":"ctx.session"},{"t":"text","v":" forward "},{"t":"code","v":"opts.signal"},{"t":"text","v":" only when you hand it over, and "},{"t":"code","v":"hostFetch"},{"t":"text","v":" "},{"t":"strong","v":"composes"},{"t":"text","v":" it with that call's deadline instead of replacing it, so a cancel and a timeout stay distinguishable at the result. An ambient signal would silently change every existing call site and abort work a mod deliberately wants to finish."}],"t":"p"},{"spans":[{"t":"strong","v":"An aborted call still resolves; it never rejects."},{"t":"text","v":" "},{"t":"code","v":"ctx.file"},{"t":"text","v":" maps the"}],"t":"bullet"},{"spans":[{"t":"text","v":"abort to "},{"t":"code","v":"{ok:false, aborted:true, error:'cancelled'}"},{"t":"text","v":" — "},{"t":"code","v":"'cancelled'"},{"t":"text","v":", the word the file family already used for a user pressing Cancel, distinct from its "},{"t":"code","v":"{ok:false, timedOut:true, …}"},{"t":"text","v":" deadline. "},{"t":"code","v":"ctx.session"},{"t":"text","v":" keeps its status-carrying envelope and resolves "},{"t":"code","v":"{status:0, json:{ok:false, error:'AbortError: mod \"<id>\" was torn down'}}"},{"t":"text","v":" — the stringified reason, named so a console says which mod's teardown cancelled the request."}],"t":"p"},{"spans":[{"t":"strong","v":"Feature-detect it as a value, not a function."},{"t":"text","v":" "},{"t":"code","v":"if (ctx.signal)"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"code","v":"ctx.capabilities.signal"},{"t":"text","v":", or "},{"t":"code","v":"needs: ['signal']"},{"t":"text","v":"; "},{"t":"code","v":"typeof ctx.signal === 'function'"},{"t":"text","v":" is the detection that would be wrong, because unlike every family whose members are callable this one is the value — which is also why it owns a capability entry rather than being gated as a member of one. A build whose engine has no constructible "},{"t":"code","v":"AbortController"},{"t":"text","v":" gets "},{"t":"strong","v":"no "},{"t":"code","v":"ctx.signal"},{"t":"strong","v":" at all"},{"t":"text","v":" rather than a placeholder that never aborts, and "},{"t":"code","v":"ctx.capabilities"},{"t":"text","v":" reports it absent, so "},{"t":"code","v":"needs: ['signal']"},{"t":"text","v":" blocks with a reason instead of running against a signal that quietly does nothing. "},{"t":"code","v":"ctxVersion"},{"t":"text","v":" stays "},{"t":"code","v":"1"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"**The pairing rule: work that is supposed to outlive the teardown must not carry the signal."},{"t":"strong","v":" They are two halves of one decision, and reading only the first half is how a migration breaks a save. The shipped referent is #196's deliberate final "},{"t":"code","v":"flush()"},{"t":"strong","v":": "},{"t":"code","v":"mods/scratchpad/"},{"t":"strong","v":" calls it from a "},{"t":"text","v":"window-close cleanup** precisely so a last edit reaches the server, and neither "},{"t":"code","v":"serverStore.update()"},{"t":"text","v":" nor "},{"t":"code","v":"saveChain"},{"t":"text","v":" takes a "},{"t":"code","v":"signal"},{"t":"text","v":" at all — there is no option to pass one, and that is deliberate. The failure to avoid is threading "},{"t":"code","v":"ctx.signal"},{"t":"text","v":" through the writes you make on that same close-time path (a final "},{"t":"code","v":"ctx.file.write"},{"t":"text","v":", a last "},{"t":"code","v":"ctx.session"},{"t":"text","v":" call): a disable would then abort exactly the work you added the cleanup for. "},{"t":"strong","v":"If it must survive the teardown, it does not take the signal."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"8. the `ctx` surface (contract v1) makectx(modid, rec) builds a fresh object per mod. everything below is additive to v1 — ctxversion stays 1 , so feature-detect rather than bumping it. lifecycle ctx.id , ctx.ctxversion — this mod's id, and the loader's contract version. ctx.onunload(fn) — register a teardown. lifo, each isolated in its own try . register it before anything that can throw (§11). ctx.visibility — pausableinterval(fn, ms) → a {stop} handle, and onvisibility(fn) → an unsubscribe. the interval slows to 10× ms while the tab is hidden and coalesces the runs it skipped into exactly one catch-up when the tab comes back (including after a bfcache restore, where no tick fired at all). both fail closed once the mod is tearing down, and both register their own teardown, so neither can outlive a disable. for a shipped mod this is a floor, not a maybe. it is a plain member of the object makectx builds — not one an extender adds afterwards — so if your init(ctx) is running at all, ctx.visibility is on that ctx ; and per §7 an in-tree mod is served beside the very loader that put it there. a ctx.visibility ? … : setinterval(…) fallback in a shipped mod is therefore unreachable code. the ones still carrying such a fallback — mods/aistatus/ , mods/clock/ , mods/git/ , mods/task-manager/ and mods/update/ — are a migration of their own, not something to copy from. the floor stops at the repo. an installed package is loaded by whatever broker it was installed on (§7, §10.2), and that loader really may predate ctx.visibility — so portable source keeps the feature detect, or declares needs: ['visibility'] (§5) and blocks visibly instead. ctx.signal (#198) — one abortsignal per activation , aborted at the head of teardown. pass it explicitly to the calls you want cancelled; it has its own section below. ctx.capabilities (#197) — a frozen , per-mod map, `{family: <integer version>} , of the ctx surface this build's loader actually hands you — e.g. {file: 1, serverstore: 1, windows: 1, settings: 1, theme: 1, …} . it exists so a mod can ask \"does this build offer x\" directly instead of sniffing loader-private names the way mod-sync currently does ( typeof _pin , typeof _modtextok ). additive on ctxversion 1 like everything else on this page — feature-detect it ( ctx.capabilities && ctx.capabilities.theme ) before reading it on a build old enough to predate it. the map is built lazily on first read and then frozen: your copy cannot be mutated into lying to you, and mutating it never reaches another mod's copy or the needs gate (§5) any mod is checked against. it is **observed, not promised** — a family whose own ctx extender threw during setup is simply absent from the map, exactly as it is absent from ctx` itself, so the two can never disagree. ctx.events.on(name, fn) (#195) — subscribe to the core → mod event bus. returns an unsubscribe fn, which also rides teardown. five declared events, two of which replay their current state to a late subscriber; the table and its rules are the contract, so read them below before you use it. storage ctx.storage — get(key) / set(key, value) / remove(key) over localstorage , namespaced webterm:mod:<id>:<key> . per-browser , string values, never synced. every call swallows storage failures (private mode, quota) and get returns null . ctx.prefs (#196) — the sanctioned browser-local tier: `get(key, default) / set(key, value) / remove(key) over its own webterm:modprefs:<id>:<key> namespace, with **json** values rather than strings. a **sibling** of ctx.storage , not a corner of it — the namespace is the whole contract, and it is what makes these records survive \"reset local view\" and structurally unable to ride /state`. see below. ctx.serverstore — a durable per-mod key/value store on the broker ( /mod-store/<modid> ), scoped to this mod's id so one mod can neither read nor write another's. every call resolves to the parsed body plus the http status , and never rejects: get(opts) → {status, rev, value, revisions:[{rev,ts}]} ; set(value, baserev, opts) → {status, ok, rev, …} (a 409 carries the live value inlined so you can rebase in one round trip); getrevision(n, opts) → {status, ok, rev, value} . writes ride the /state single-active-client lease: a non-active browser reads fine but its set() resolves 409 {error:'not_active'} . opts.host routes to another broker by host id, and an unknown id fails closed — {status:0, error:'no_host'} with no request made. reading another broker is why status matters: a 401 (\"it refused our password\") and a 200 with value:null (\"nothing published there\") are the same empty body and only the status tells them apart. the status is applied after the body, so a response body carrying its own status cannot overwrite the transport's. a mod that stores credentials must set opts.nohistory = true on every write. the store keeps a revision ring, so without it the value each write replaces — the previous password, in the clear — stays readable through getrevision(n) and on the broker's disk. the flag is sticky per record: it suppresses new revisions, clears the ring on the write that sets it, and survives later writes that omit the option (absent means \"leave it alone\", which is what makes a 409 rebase safe — pass the same opts to the retry). turning it back on is deliberate and one-way: nohistory:false is refused unless purgerevisions:true rides the same call. only a strict boolean reaches the wire, and a broker that predates the flag silently ignores it — check modstore.nohistory in that broker's /info before sending a credential, because the write that would stop the archiving is itself the write that archives. two members are built on top of get / set and have their own subsections below: update(fn, opts) (#196), the compare-and-swap that re-applies your reducer over the winner of a 409 , and savechain(opts) (#196), the debounced, single-in-flight save pipeline built on update . use them. three mods hand-rolled a compare-and-swap before these existed, and the rebase that is easiest to write by hand — adopt the winner's rev , re- put the object you already had — is the one that erases every key another browser changed in between (#158; it is why mods/mod-sync/ deliberately does not use core's puthoststate ). host i/o ctx.file — read / write / list / delete / upload / mkdir / copy / move / zip / unzip / stat / setattr / hash , plus the chunked family ( readchunk , uploadbegin , uploadchunk , uploadcommit , uploadabort ). paths are host-wide absolute native paths — there is no root confinement. every call resolves to a parsed result object, never a rejected promise: {ok:true, …} or {ok:false, error} . opts.host is a host id string ; omitted / '' / 'local' means this broker, and an unknown remote id fails closed with {ok:false, error:'host_not_found'} and makes no request. ctx.session — procs(id, opts) , the destructive kill(id, pid, opts) , and git(id, opts) . resolves to {status, json} so you honour the broker's status contract verbatim — note that 409 {error:'session_gone'} from kill is the session-destroy success path, not a failure. ctx.clipboard.observe(fn) — fn(dir, text) with dir 'out' (copied out of a terminal) or 'in' (pasted in), for every copy/paste core captures. returns an unsubscribe fn and is also dropped on teardown, so capturing stops the moment the mod is disabled. ctx.hosts.invalidate(id) (#195) — \"forget everything this page knows about host <id> \": clears every registered per-host cache and repaints the host surfaces, returning a boolean. it notifies nobody and saves nothing — see below. desktop, taskbar and menus ctx.taskbar.addstatusitem(node) — mount a node in the taskbar (before #help-chip ). auto-removed on teardown. ctx.taskbar.onitemsrendered(cb) — after every chip rebuild. ctx.taskbar.interceptactivate(fn) — fn(key) runs before core resolves a taskbar activation; return true if you changed what the desktop shows. ctx.desktop — columnfilter(fn) , oncolumncreated(fn) , onplaced(fn) , onforgotten(fn) , onreveal(fn) , onlayoutrender(fn) . each family is one slot : a second registration throws modconflicterror , so two mods can never silently fight over the desktop. ctx.registerkeyactions([{id,label,run}]) — merges into the live action list every reader goes through (dispatcher, the keyboard shortcuts pane, the help corpus). a duplicate action id throws rather than last-wins. ctx.registerwindowmenuitems(fn) / ctx.registerdesktopmenuitems(fn) — register a callback; the callback returns an array of rendermenu items (separators included, so you own your own grouping — rendermenu does not collapse a stray one). the desktop callback runs in both window modes and receives { tiling } saying which — check it rather than assuming tiling. ctx.windows.onterminalcreate(cb) — subscribes to every terminal window, replayed over those already open and fired for future ones. cb gets { win, titlebar, host, wireid, addtitlebaritem, ondispose, onmodteardown } — one subscription's replay and its create-time emit are mutually exclusive, so each window reaches your callback exactly once and you need no decorate-once weakset . onmodteardown (#195) is the per-window disposer; it has its own section below. control panel ctx.settings.boolean(key, def, opts) → checkbox ctx.settings.radio(key, options, opts) → radio group ctx.settings.select(key, options, opts) → <select> ctx.settings.combo(key, options, opts) → searchable <input list> ctx.settings.text(key, opts) → free text (the fifth primitive) all five are read-through accessors onto the shared /state settings blob (not namespaced localstorage), so a value syncs across your browsers, and all five return {get, set, onchange} . opts carries label , title , def ( boolean takes its default as the second argument instead), isbrowserglobal (default true — hidden on remote-host tabs) and mount ( 'mods' , the default per-host pane, or 'browser' , beside the hosts list). reads are non-destructive : nothing writes the default back. text is the only primitive that is not choice-constrained , and that distinction matters — combo looks free but treats its list as the legal domain and refuses anything outside it. text takes options as suggestions (a datalist), plus placeholder , maxlength (capped at max_mod_text_len , 1024 utf-16 code units) and validate . its three rules: read() is structural only — a bounded, control-char-free, well-formed string is returned as-is, even one this build's own validator would refuse, and it never writes. so an upgrade, a different engine or a stricter validator can no longer make a stored value evaporate. coerce() gates what we write: string → strip control chars → drop unpaired surrogates → trim → cap. validate is write-only and its rejection is visible — the rejected draft stays on screen next to a .set-err message and the stored value is untouched. it must be synchronous : a promise is truthy, so an async validator would accept everything, and the loader logs and rejects if you pass one. writes are debounced, and a pending one is flushed on teardown. mods/clock/clock.js is the worked example: its time-zone box uses text precisely because intl.supportedvaluesof('timezone') is engine-dependent, and a combo refused zones that the same engine would happily render. ctx.registersettingspane(spec) — a full custom section, for anything richer. spec.render() builds the dom and is called exactly once ; spec.reflect(settings) syncs it on control panel open and on every /state convergence , so it must be idempotent and must preserve an in-progress edit. theme ctx.theme.get() // -> {name, dark} ctx.theme.vars() // -> {'--bg': '#1e1e1e', …} ctx.theme.onchange(fn) // -> unsubscribe fn derived from the live dom , so it is right with the theme mod disabled, absent or replaced. dark is the honest half: a yiq test on the computed --bg . name is a hint — the synced theme key, reported only when an inline --bg on documentelement proves a theme mod actually applied one. branch on dark when you need certainty. onchange(fn) fires after the new vars are on screen, only on a real change of {name, dark} , and is not replayed on register — call get() in your init . it is auto-dropped on teardown. a change that moves a public var without moving name or dark does not notify. vars() is a separate call on purpose : it costs a getpropertyvalue per variable, which a subscriber that only branches on dark should not pay. the public css-variable contract vars() resolves is --bg , --bg-2 , --bg-3 , --fg , --fg-dim , --accent-default , --sel-bg , --ok , --warn , --danger . deliberately not in it: --accent (per-window, so there is no document-level value — write var(--accent, var(--accent-default)) inside a window instead) and the geometry vars ( --taskbar-h , --title-h , --handle-thick , --corner-size ). everything else in the stylesheets is private core. values come back as the cssom reports a custom property — a substituted token stream, not a resolved colour. a theme mod writes six of these as plain hex, but the four derived ones ( --sel-bg , --ok , --warn , --danger ) read back as e.g. color-mix(in srgb, #4aa3ff 28%, #1e1e1e) . that is a valid css colour string — assign it to a style property and it paints — but do not try to parse a number out of it. windows and help ctx.registerwindowkind(spec) — register a brand-new app-window kind: { appkind, factory(appdata) -> win, serialize(win) -> record|null, restore?(record), retainonclose?(record) -> bool, menu?: { label, launch(), iconkey?, iconglyph?, closeditems?() } } it is the same registry core's built-ins use, so a mod kind is a first-class window everywhere the registry is consulted. a duplicate appkind throws (and initmod rolls the mod back); the kind is removed on teardown. an optional restore is called directly , so it does not get openappwindow 's dedup-by-id — it must check windows.get(record.id) itself, because a lease-loss rebuild re-runs it. the launcher icon. menu.iconkey names an entry in core's app_icon_svg table, which is closed — a key core does not ship resolves to nothing. menu.iconglyph is the mod-owned alternative: a short text/emoji glyph that rendermenu paints with textcontent . it is normalized by core's appiconglyph() , which refuses an over-long raw string, strips control characters, bidi controls, invisible-payload carriers, lone surrogates and all whitespace, requires at least one substantive code point left over, and caps the result at app_glyph_max code points — read that function for the exact rules rather than trusting this paragraph. there is deliberately no ctx.registerappicon(svg) : the api offers no way to put markup into the shared menu renderer, partly because the value a mod passes need not be its own (it might come from a /mod-store blob or a peer's /state ). that is hygiene for this api — per §1 it constrains nothing a mod could do by reaching into the dom itself. two traps: an iconkey is not owned by anyone, so a mod can name a shipped key and get that icon — only the label identifies a launcher; and iconkey takes precedence over iconglyph . declare one or the other, and do not rely on the fallback for a key core does not currently ship. ctx.registerhelpcards(cards) — contribute typed help cards. **never raw html**: { slug, section, title, body:[block], keys?, search? } where block = {t:'p'|'bullet'|'sub', spans:[span]} and span = {t:'text'|'strong'|'code'|'kbd', v:string} . unknown types degrade to text; values are coerced to string . removed on teardown, and an open help window re-renders. app windows: ctx.windows.createappwindow (#194) nine shipped mods used to hand-build the same ~30-field app-window scaffold: a win literal pushed straight into the core windows map, a synthetic kind:'app' session, a hand-appended taskbar chip, and a teardown that iterated core state to find its own windows. core now owns all of that; a mod supplies only what is specific to its window. mods/clipboard/clipboard.js is the worked example — it shipped as the reference migration alongside this api, and its old scaffold, clip_win_id hack included, is gone. const h = ctx.windows.createappwindow({ kind: 'clipboard', // required: the appkind id: 'app:clip', // optional; see the singleton default below title: 'clipboard', sid: 'clip', badge: '#clip', // chip / session short id, taskbar badge appclass: 'app-clip', bodyclass: 'clip-body', singleton: true, // open-or-focus, deduped on kind — see below geom: undefined, color: undefined, locked: undefined, floatgeom: undefined, resizable: true, // false = no resize handles (recorder's playback window) toolbar: function (el, win, h) { /* the .app-toolbar strip, optional */ }, body: function (el, win, h) { /* the window's content */ }, onclose: function () { /* replaces the × action; default is closewindow */ }, }); kind is the only required field, and it must be a kind this mod has already registered with ctx.registerwindowkind — register in init() , build later (on a launch, a restore, a click). two things ride on that: a mod cannot name another mod's kind and have the dedupe hand it that mod's live window (which its closeall() , or simply its being disabled, would then close), and a window of a kind nobody registered would persist a /state record nothing can ever restore. a create is also refused outright while the mod is being torn down — the take-down below walks one snapshot, so a window opened after it would outlive both the pass and its kind. both refusals throw. everything else falls back to the same defaults the deleted scaffolds computed by hand: title / sid default to kind , badge to '#' + sid , appclass to 'app-' + kind , bodyclass to appclass + '-body' , geom to clampgeom(appdefaultgeom(kind)) , color to normalizehex(defaultcolor(id)) , locked to true , and id (absent singleton ) to a fresh newappid(kind) . core owns, exactly as every deleted copy did it by hand: the chrome ( buildappchrome + wireappchrome + the eight resize handles), the desktop insertion, the window record itself (the object that lands in the core windows map), the synthetic kind:'app' session and its taskbar chip ( #taskbar-empty removed once it exists), and the placement tail ( finishwindowplacement ). your toolbar / body builders run after all of that, against a window that already has a layout box and a record to hang per-window state on — though not necessarily its final box, since placement may still tile it. createappwindow returns a frozen handle , not the window record: member · does h.win — the underlying window record — the escape hatch the trust model (§1) permits, not the api itself h.dom , h.body , h.toolbar — the chrome root, the body element, the toolbar element ( null if you passed no toolbar ) h.isopen() — still a live member of the core windows map h.focus() — reveal-and-focus h.close() — close this window h.settitle(text) / h.setcolor(hex) — update title bar, session and taskbar chip together h.save() — saveappwindow , for a persisted kind h.addtitlebaritem(node) — inserted before the minimize button h.ondispose(fn) — win.cleanups.push , spelled the way onterminalcreate 's bag spells it every member is gated on the window still being live — a handle you keep past its window's death (or past a later window reusing the same id) is inert, not lethal. singleton: true formalizes the open-or-focus hack every windowed mod re-derived by hand (clipboard's old clip_win_id ). with it, id defaults to the stable 'app:' + kind , and a create call dedupes on kind over every live window in the core windows map — not just the windows this mod's own registry already knows about, because core's unknown-kind restore fallback (#167) may have rebuilt a window of this kind, under some other id, before this mod ever loaded. whichever window it finds is adopted (never double-built) and, unless the call is a restore, brought to front. without singleton , dedupe is by id alone: a second create at a still-open id adopts and focuses it; a create at an id mid-close throws (a reopen has to wait for the close to finish); a create at an id already open under a different kind throws rather than stranding the live window. ctx.windows.list() — this mod's own live windows, oldest first, as a fresh frozen array. dead entries are pruned on the way past, so a list() after a lease-loss rebuild reads empty instead of a wall of ghosts. mods/clipboard/clipboard.js 's renderall() is the shipped example: a loop over ctx.windows.list() replaced a hand-kept livewins set. ctx.windows.closeall() closes every one of this mod's factory-built windows and returns how many it closed. it is the same pass the loader also stages automatically: a mod's factory windows are closed by core before its onunload chain runs, and while its window kinds are still registered — so a migrated mod needs no teardown-ordering care of its own. that property is why registerwindowkind 's restore/teardown ordering used to matter by hand: close a window after its kind is deregistered and the close reaches saveappwindow with no registry entry for the kind, falls back to the shared serializer, and leaves a junk /state record for a kind nothing can restore. modeling the close as an onunload entry would make it the oldest entry in the lifo chain and therefore run it last — after deletewindowkind , which rides that same chain — exactly the wrong order. so it is never registered as an unload: the loader calls this same close pass directly, at the head of teardown, before the first onunload runs and before any kind the mod registered is deregistered. this is what let clipboard.js delete its own ctx.onunload window-closing block entirely. call closeall() yourself when you want a mod's windows gone without disabling the mod. the restore hook's third argument. registerwindowkind 's restore is called directly by core, outside any mod's ctx closure, so it is handed the per-mod window api as a third argument: restore: function (record, opts, api) { const h = api.createappwindow({ kind: 'scratchpad', id: record.id, … }); return h.win; // or `return h;` — api unwraps a handle either way } api is {createappwindow, list} — the same two members ctx.windows exposes, scoped to this mod's record. two defaults ride the call and both are overridable in your own spec: spec.id defaults to record.id (so a restore that builds elsewhere does not orphan the record it was handed, or step around the id dedupe), and spec.restoring defaults to opts.restoring (core's automatic restore pass always passes true ) — the same suppression openappwindow 's own restoring gives: no reveal, no un-minimize, no re-home to whatever workspace the page happened to boot on (#152). ctx.windows.onappwindowcreate(handler) fires for app windows whose appkind is one of this mod's registered kinds — every kind it has passed to registerwindowkind , however the window was actually built, including a window core rebuilt through its unknown-kind fallback before this mod ever registered that kind. it is not scoped to \"windows this mod's factory built.\" subscribing replays every matching window that already exists, then fires for every one built afterwards — exactly once per subscription: a replayed window is never re-delivered as a live create, and a live create is never re-delivered by a later replay, so a mod needs no weakset of its own to avoid double-decorating. this is sticky's init-time catch-up pass ( mods/sticky/sticky.js ) made first-class — whether a mod's windows already exist when its init runs is a race (#167), not an ordering, and this hook makes both orders come out identical. const off = ctx.windows.onappwindowcreate(function (info) { // info: {win, id, kind, dom, body, titlebar, replayed, addtitlebaritem, ondispose} addchip(info.id); info.ondispose(function () { removechip(info.id); }); }); off() also rides ctx.onunload automatically, so a disabled mod stops decorating windows even if it never calls the function it was handed. the trap. a registerwindowkind factory — factory(appdata) — must return h.win , never the handle : openappwindow hands a factory's return value straight back to its own callers (and its own id-dedupe branch returns a window record too), so a factory that returns the handle breaks every caller expecting a record. a restore hook is the one place a handle is accepted — api unwraps it by identity before core ever sees it, so return h; and return h.win; are both correct there, but a factory only has the one right answer. mods/clipboard/clipboard.js 's openclipboardwindow says exactly this at its own return h.win line. core events: ctx.events.on (#195) every host- and state-lifecycle fact core wants to tell a mod has travelled as an ad-hoc duck-typed protocol: a win._onhostauth field core invokes by name , a win._hostremoved beside it, help polling the core-private _stateready 20 × 500 ms for the initial adoption, workspaces reading the core-private _deactivated . nothing registers those and nothing types them, so core cannot refactor its auth form, its host list or its boot sequencing without auditing every mod. ctx.events is the one channel that replaces all of it — and it is the channel to write new code against; migrating the remaining readers off those private names is still in progress (#204). const off = ctx.events.on('state:adopted', function (payload, meta) { // payload — the table's shape below, frozen // meta — {name, shape, gen, replayed}, frozen }); off(); // unsubscribe; `off` also rides this mod's teardown automatically the table is the contract. name · shape · payload · replay on subscribe host:auth — edge — {hostid} — no host:changed — edge — {hostid} — no host:removed — edge — {hostid} — no state:adopted — level — {gen} — yes, exactly once lease:changed — level — {active, gen} — yes, exactly once a name this build does not declare is refused inertly — a console.info and an unsubscribe fn that does nothing — never a throw, so a mod written against a newer build keeps running here (§5's \"absence is never an error\"). edge vs. level is the whole design, not a flag. an edge event is a thing that happened : a subscriber that was not there missed it, and replaying it later would be a lie about when it happened. a level event is a state that is : a subscriber arriving late is entitled to the current one and gets it immediately, exactly once, out of its own on() call. the two level events are precisely the two a mod otherwise has to poll or read a core private for — the replay is what a 20-tick _stateready timer is for, done properly. a level only replays once it exists: subscribe before core has learned the lease and there is nothing retained to hand you, so you are told at the first transition like everyone else. exactly once — replay xor live. a subscriber that was there before the emit is told live; one that arrives afterwards is told by its own replay. never zero, never twice — including the nasty case where a handler subscribes from inside another handler, i.e. after an emit has been applied but before it has been delivered. it is enforced per subscription , so two mods each get their own exactly-once and a mod that unsubscribes and re-subscribes is told again. gen is stamped by the bus. a gen key on the payload a core site passes is dropped ; the bus owns that field. it is a monotonic counter over every emit that actually happened, across all five names — an ordering aid, so you can tell a replay of gen 3 arriving after a live gen 5 is stale. it is not the /state revision and not a lease epoch. only a level payload carries it; an edge does not, but the meta bag carries it for both. payloads are a shallow copy of what core passed, and frozen. a level emit that changes nothing does not happen. equality is shallow object.is over the own keys, ignoring gen : no generation is taken, no handler is called, nothing is re-retained. that is why state:adopted fires exactly once ever even though core has two adopt latches — the boot pull and the lease-loss rebuild both emit an empty payload, so whichever runs second is a no-op — and why a repeated {active: false} from the lease socket is silent. fire-after-apply. core emits after it has mutated, and the bus applies its own state (the generation, the retained level) before it calls anybody, so a handler observes finished state rather than a half-applied one. two limits the code states out loud and this page will not overstate: lease:changed is home only . the payload has no hostid — one retained level cannot speak for n brokers, and a remote broker's lease is a per-host mask with no event in this build's table. on a lease loss the view is fully torn down before any handler runs; on a gain the rebuild is still in flight. the event names the lease , which core has genuinely learned, not the view. handler isolation. a throwing handler is logged and its siblings still run, and the throw never reaches the core mutation site that emitted. auto-unsubscribe at teardown. off is pushed onto this mod's lifo teardown chain as well as returned, so a disabled mod stops hearing about hosts even if it never calls the function it was handed. re-entrancy is coalesced and bounded. an emit raised from inside a handler joins the pass already running instead of nesting one, so a cascade's stack depth is constant and the delivery order stays the order things were applied in. level events coalesce while still queued (two lease changes inside one pass deliver the final state once); an edge never coalesces , because two auth successes are two facts. the pass is capped — exhausting it drops what is left with a console.warn , rather than livelocking the ui thread. only core emits. ctx.events carries on and nothing else. a mod that could emit host:removed could lie to every other mod about core state, and inter-mod messaging is a separate surface with its own trust story (#199). adding an emit later is additive. where each event comes from , so you know what it means: host:auth — a completed host authentication, and a poll that finds a host's auth has recovered. host:changed — a committed host add/edit, the host colour swatch, and the hidden toggle. only paths that actually mutated emit it. the edge carries an id, not a snapshot: re-read what you care about and decide. host:removed — after the whole removal has finished, so it means \"that host is gone\", not \"it is going\". state:adopted — the first /state adoption, whether that is the boot pull or a lease-loss rebuild that beat it. lease:changed — the home single-active lease, per control frame. feature-detect it. events is its own capability family at version 1, so ctx.capabilities.events , if (ctx.events) / typeof ctx.events.on === 'function' and needs: ['events'] all work. mods/help/help.js is the shipped worked example — its corpus refetch on host:auth , replacing the win._onhostauth field it used to write. note it feature-detects instead of declaring needs: ['events'] , deliberately: a needs blocks the whole mod, and help without the bus is missing one corpus refresh, while help blocked has no chip, no window kind and no hotkey. weigh that trade the same way. core still invokes win._onhostauth by name for the three mods #204 has yet to migrate. that is legacy, not a pattern — do not add a fourth. per-window teardown: info.onmodteardown (#195) a mod that decorates a terminal window has two exits to cover, owned by two channels, neither of which covers the other: chain · fires when · does not fire when win.cleanups — info.ondispose — the window closes ( closewindow , and the lease-loss rebuild) — the mod is disabled with that window still open rec.unloads — ctx.onunload — the mod is disabled — one of the windows it decorated closes so a widget needed a disposer on both, an idempotence flag so the first exit disarmed the second, and a set so the mod-side entry could still reach the open windows. core keeps all of that now: ctx.windows.onterminalcreate(function (info) { info.addtitlebaritem(btn); if (!(typeof info.onmodteardown === 'function' && info.onmodteardown(teardown))) { info.ondispose(teardown); // refusal / older build — see below } }); exactly once, on the first of the two. both exits fire the same per-(mod, window) state, and it is marked spent before its callbacks run — so close-then-disable, disable-then-close, and a callback that closes its own window or disables its own mod from inside the teardown it is running in are all the same single call. a disable does not close the window. a terminal belongs to core, not to the mod that decorated it: this removes the decoration and leaves everything else where it was. (#194's staged take-down does close a mod's windows at disable — those are the mod's own factory windows. a terminal never is.) it returns a boolean , and a refusal is never a throw: false for a non-function, for a window that is already gone, for this mod being mid-teardown, and for a state that has already fired. you can see the false and run your own cleanup, which is exactly what the fallback above is. callbacks run lifo , each isolated, so one broken disposer strands neither the window's remaining ones nor the chain behind them. the state is per (mod, window), not per delivery. a mod may hold more than one onterminalcreate subscription and every one is handed a bag for the same window; they all register into one state, so a window carries one win.cleanups entry per mod however many teardowns that mod registers on it. where it sits in the lifo chain. the mod-side entry is pushed at your first accepted registration — normally inside init() , on the onterminalcreate replay — so it is an early entry and therefore runs late, after disposers you registered afterwards. a self-contained widget teardown belongs there; if you need your widget gone before some other resource you own, tear that one down from the same callback. no new capability entry, and needs cannot name it. windows is already a v1 family and this adds a member to it, so the map is unchanged; needs resolves a path on ctx (§5) and the per-delivery info bag is not reachable from ctx . feature-detect with typeof info.onmodteardown === 'function' . one accepted cost: a disable leaves its spent win.cleanups entry on the window. splicing it out at fire time is the one thing this must not do — closewindow iterates that array with for…of , and removing an entry mid-drain skips the next one — so an enable/disable cycle on a long-lived terminal leaves one dead closure per cycle. mods/git/git.js is the shipped reference migration. its hand-kept disposers set, the ctx.onunload block that drained it and the decorate-once decorated weakset in front of both are all gone, replaced by the one registration above at the same site. (the weakset needed no replacement: onterminalcreate already delivers each window once per subscription.) what it keeps is the info.ondispose fallback, deliberately: onmodteardown is absent on a page assembled without the extension fragment and returns false on a dead window or a mod mid-teardown, and in those cases arming the window-close half alone is still worth it — a leaked 15 s poll plus a live document-level listener on every terminal is the worse failure. it is the one line the mod rode before, not a second set. forgetting a host: ctx.hosts.invalidate (#195) hosts is a new family at capability version 1, and invalidate(id) is its first member: \"forget everything this page knows about host <id> \". it clears every registered per-host cache and then repaints the host surfaces (the hosts list, the settings tabs, the host status chips and the taskbar). the list is core's, not a caller's — each cache registers itself beside its own declaration — so the eight this build has (the per-host state cache and its save chain, the auth-prompt latch, the poll record, the /control socket, and three config caches) and whatever the next fragment adds are covered by the one call. the cautionary example is in the tree: mods/host-registry/ still carries its own hand-maintained copy of that list, and its own comment calls that copy \"a known latent gap\". a list kept by a stranger is right on the day it is written and nothing makes it stay right — call this instead. it returns true when it ran and false for a blank / non-string id (nothing to forget, and no repaint owed) or an internal failure — never a throw , so calling it from inside an event handler cannot take a repaint down with a bad argument. two things it deliberately does not do: it does not emit. invalidation and notification are separate acts: only core mutation sites emit host:changed . so a host:changed subscriber may call ctx.hosts.invalidate() without recursing. it does not save. saveprefs() is a mutation (localstorage plus a /state push) and stays with the caller that actually changed something, so a mod can drop stale per-host state without writing prefs it does not own. clearing a cache does not cancel an in-flight request; it drops the stale value so the next request uses the new url/token, and the poll loop re-primes on its next tick. feature-detect with typeof ctx.hosts.invalidate === 'function' or needs: ['hosts.invalidate'] . on a build whose core has no invalidation path the family is not installed at all , so ctx.capabilities reports hosts absent — true — rather than handing you a member that silently does nothing. compare-and-swap writes: ctx.serverstore.update (#196) const res = await ctx.serverstore.update(fn, { host?, retries?, purgerevisions?, nohistory? }); // -> {ok:true, rev, value} | {ok:false, error, status?, rev?, value?} get → fn(value, rev) → set , and on a 409 conflict it re-reads and re-applies fn to the winner's value . the blob that lost is never re- put . that single behaviour is the whole reason the helper exists. fn must be pure over (value, rev) . it is invoked once per attempt , so a side effect inside it happens once per attempt — bumping a counter, appending to a log, showing a toast, queuing another save. that is a caller bug, and the code deliberately does not defend against it, because the only available defence is to snapshot once and re-send that snapshot after a 409 — which is the #158 clobber, now with retries. everything the write depends on that can change between attempts (the live editor buffer, the current selection, date.now() ) belongs inside fn , where it re-runs over the winner. which is also why the shape is a reducer and not a snapshot-getter . an update(gathervalue) api cannot rebase: it has nothing to apply the winner's value to, so its only move after a 409 is to overwrite. the winner's value has to flow through your function. the rest of the contract: value is the winner's value , freshly parsed on every attempt, and null on a store nobody has written yet. rev is the revision it was read at. mutating value in place is safe, but the next value must be returned — a reducer that only mutates returns undefined , which is refused ( bad_value ) before any write, because the server 400s on a missing value and a round trip is a slow way to learn that. an async reducer is first-class — fn may return a promise. bounded, and the conflict is surfaced. one write plus a bounded number of rebases; retries moves that budget and is clamped — the ceiling is not the caller's to remove, because the mirror failure of a silent clobber is a silent infinite rebase against a hot writer, and every attempt also writes into the store's revision ring (#175). exhausting the budget resolves {ok:false, error:'conflict'} with the last winner's rev and value still inlined, and you decide whether to widen, warn or drop. not_active is refused, not rebased. it is a 409 too, but it means this browser does not hold the /state write lease, so re-applying fn at a fresher revision would burn the entire budget only to be refused again. it comes straight back with the live rev inlined. a 409 that inlines the live value costs no extra round trip ; one that does not (an older broker, or a remote one whose body is untrusted input) falls back to an authoritative re-read — and a failed read aborts without writing . so does the initial read: a response that is not this store's own 200 shape is a refusal, never \"an empty store\", because reading it as rev 0 / value: null would run your reducer down its \"nothing stored yet\" branch on a store nobody could actually read. opts is snapshotted once and rides every attempt , rebases included, so purgerevisions (#65) and nohistory (#192) cannot be dropped by a retry. nohistory keeps set() 's strict tri-state: it is presence-keyed , and absent is not false — absence means \"leave this record's flag exactly as it is\", so a retry that helpfully sent nohistory:false would resume archiving the very credentials the flag exists to keep out of the ring. fail-closed, inherited. every request goes through your own ctx.serverstore.get / set , so it is mod-scoped, lease-carrying and host-routed exactly as they are: an unknown opts.host makes no request and refuses before any put is attempted. it never rejects. every outcome resolves to a result, including {ok:false, error:'fn_failed'} when your reducer throws (it is logged) and bad_fn when the first argument is not a function. feature-detect with typeof ctx.serverstore.update === 'function' or needs: ['serverstore.update'] . it is a new member , not a new family, so there is no capability entry for it: ctx.capabilities is keyed by family name ( serverstore ), never by dotted path — the same as ctx.windows.createappwindow . ctxversion stays 1 . the save chain: ctx.serverstore.savechain (#196) const chain = ctx.serverstore.savechain({ host?, debouncems?, retries?, purgerevisions?, nohistory? }); chain.save(fn); // -> true (queued) | false (a dead chain, or a non-fn) chain.flush(); // -> promise<result>, never rejects chain.onstate(fn); // 'idle' | 'saving' | 'conflict' -> unsubscribe fn scratchpad 's save pipeline, canned. it adds nothing to the write itself — every send goes through update() , so the 409 rebase, the bounded budget, the fail-closed host lookup and the verbatim purgerevisions / nohistory passthrough are inherited rather than re-implemented. what the chain owns is when to write, how many writes to make, and what to tell the user while it happens. save(fn) takes the same pure (value, rev) reducer update() takes, and the purity rule above applies unchanged — including this specific case: do not call this chain's own save() from inside a reducer , or a rebase queues the same work twice. coalesced by composition, not by replacement. rapid saves queue, and the queue is spliced into one batch composed in order — fn2(fn1(value, rev), rev) — handed to update() as a single reducer. so two queued saves both apply ; an earlier caller's delta is never dropped on the floor. for a reducer that produces the whole document every time (scratchpad's) that degenerates to last-wins, which is exactly the old behaviour. two consequences: every reducer in a batch sees the same base rev , and one reducer that throws or forgets to return refuses the whole batch, under update() 's own error names. single in flight, per chain. at most one write from a given chain is outstanding; saves arriving during it leave as exactly one more write. per chain is the honest scope — two chains in one mod address the same per-mod record on a given broker ( opts.host only picks whose copy), so they serialize with each other only through the server's revision cas, each re-applying over the other's winner. correct, but it spends conflict budget: write one chain per record. trailing debounce. each save() pushes the deadline out, which is what makes a burst one write — and means a genuinely continuous writer only leaves via the in-flight and flush() paths. debouncems overrides the default and is clamped , for the reason in the next bullet: a pending batch is dropped at teardown, so an absurd debounce is data loss wearing a save's clothes. onstate reports 'saving' while a write is outstanding, 'conflict' for an unresolved refusable 409 ( conflict or not_active — scratchpad's read-only banner), 'idle' otherwise. it is deduplicated, it fires once on subscribe so a banner mounted after the fact is correct without reconstructing anything, a throwing listener cannot cost the chain its pump, and the returned unsubscribe is yours to call. flush() forces the pending batch past the debounce and resolves once an in-flight write and anything already queued have completed — never waiting on writes queued after it. with nothing outstanding it resolves {ok:true, idle:true} , except while a conflict is unresolved, where it reports that result rather than a fresh ok . it never retries a failed batch by itself. update() already rebases a 409 up to its budget; past that the batch is dropped and the chain reports 'conflict' . scratchpad's answer is to re-attempt on the next interaction while the banner is up; yours is yours. the trap: a chain drops its pending batch at teardown. deliberately. the timer is cleared, the queue is discarded, the state listeners are released, and every outstanding flush() promise resolves {ok:false, error:'unloaded'} rather than hanging the caller awaiting it. from the first instant of a disable the chain reads dead — save() returns false , flush() resolves {ok:false, error:'unloaded'} , and a disable landing between update() 's read and its write is a drop, not a race. that is not the same posture as the ctx.settings.text debounce, which does flush on teardown, and the difference is the point: a settings commit reaches localstorage synchronously, so a teardown can complete it, while a chain write is a network round trip that a synchronous disable cannot await. \"flush on the way down\" is precisely the write that lands after a re-enable and clobbers the new activation. so a deliberate final flush must be called while the mod is still alive — from a window-close cleanup ( win.cleanups , which also runs on an ordinary close, with the mod up) or from an explicit save action such as a ctrl+s handler. never from ctx.onunload , where the activation is already marked dead and the batch is dropped by design. mods/scratchpad/ is the worked example on both counts: it flushes from win.cleanups (capturing the editor buffer first, because a later cleanup in the same drain destroys the editor view while flush() is still resolving), and its ctrl+s path is a save() followed by a flush() . what no client-side check can recall is a request already handed to the network; that residual window is named here rather than papered over. feature-detect with typeof ctx.serverstore.savechain === 'function' or needs: ['serverstore.savechain'] — a member , so no capability entry, same as update() above. scratchpad declares the needs and blocks: saving is that mod, so a build without the chain should read blocked (needs serverstore.savechain) rather than open a notes window that silently drops every keystroke. mods/help/ makes the opposite call for ctx.events and feature-detects instead, because a missing bus costs it one corpus refresh rather than all of its persistence. block when the loss is total; degrade when it is partial — the two shipped mods are there to be copied from, in whichever direction fits. browser-local preferences: ctx.prefs (#196) ctx.prefs.get(key, default); // -> the stored json value, or `default` ctx.prefs.set(key, value); // -> true once it reads back ctx.prefs.remove(key); // -> true (false only for a bad key) a mod's own preferences, per browser , with a contract instead of a coincidence. before this, both ways of reaching for core's prefs object were wrong: a bare key is wiped by \"reset local view\"'s non-underscore sweep, and an underscore key stays local only because the /state blob happens to serialize _settings and _layout and nothing else — one edit there and it syncs, exactly as _settings already does (#153). the namespace is the decision. webterm:modprefs:<id>:<key> , one localstorage record per key, a sibling of the webterm:mod:<id>: namespace ctx.storage owns — never a sub-prefix inside it, and never a key on the core prefs object. three consequences, each of them the point: \"reset local view\" cannot reach it. that sweep deletes keys off the prefs object and re-serializes it; a modprefs record is not in it. the sync path cannot reach it. the /state blob is built from prefs._settings and prefs._layout , so a ctx.prefs write leaves the serialized state byte-identical and no /state put is attributable to one. it is not \"local by convention\" — it is structurally unable to sync. a future \"sync my prefs\" is therefore a new tier , not a flag on this one. ctx.storage keeps its semantics exactly — no reserved-prefix policing, and no way to clobber a preference record through the raw surface, since a mod id may not contain : and both namespaces end in one. values are json , not strings (that is the other difference from ctx.storage ). the failure posture is ctx.storage 's, copied deliberately: get(key, default) returns default for a missing key, a bad key and a corrupt record — a half-written or hand-edited value is a miss, never a throw that takes down the init() reading it. set returns false for a bad key or a value json cannot represent (a cycle, undefined , a function) — and in that case nothing is written , so a refused set never destroys what is already stored. a localstorage that refuses (private mode, quota) is swallowed and the value is kept in memory for the life of the page — not the activation — so a disable and re-enable still finds what localstorage would have kept. a refused remove reads as removed for the rest of the session rather than resurrecting the record it could not delete. prefs is a new family at capability version 1, so it does have a capability entry: gate with ctx.prefs && typeof ctx.prefs.get === 'function' , ctx.capabilities.prefs , or needs: ['prefs'] . ctxversion stays 1 . and it is storage, not a boundary: like everything else on this page (§1), the records sit in the page's own localstorage , where any script on the origin can read them. namespacing keeps mods from colliding, not from looking. per-activation cancellation: ctx.signal (#198) ctx.signal // an abortsignal for this activation const res = await ctx.file.read(path, { signal: ctx.signal }); if (res.aborted) return; // this resumes after teardown finished — statusnode.textcontent = res.error; // check before touching anything of yours if (!ctx.signal.aborted) { // `.aborted`, never bare `if (ctx.signal)` const t = settimeout(poll, 5000); ctx.signal.addeventlistener('abort', function () { cleartimeout(t); }); } one abortsignal per activation , aborted when that activation ends. it replaces the dead-flag pattern mods hand-rolled to stop an in-flight loop from touching a desktop it no longer belongs to ( mods/update/ 's restartopdead / applyopdead are the worked examples, and are still written by hand — the migrations are not part of this). per activation, and freshness is structural. the controller hangs off the per-mod record initmod builds, and it builds a new one every time a mod comes up. a disabled-then-re-enabled mod therefore gets a new, un-aborted signal; the old activation's signal stays aborted forever, which is the point — its loops must not come back to life when the mod does. the abort is the first thing teardown does. in order: the record is marked as unloading, then the signal aborts , then #194's staged close of the mod's factory app windows, then the lifo onunload chain (which is where #195's per-window onmodteardown disposers ride, as an entry on that list). abort listeners fire synchronously, so a listener is told to stop before anything starts taking your windows away, and before every onunload . that ordering belongs to listeners and to nothing else — read the next bullet before you assume it covers an await . cancelling the work is not cancelling the continuation. the take-down path is entirely synchronous: by the time an aborted await resumes — a later microtask — the staged window close and the whole onunload drain have already run to completion. so the line after your await executes against a torn-down activation, and for ctx.file that is worse rather than better, because an abort resolves {ok:false, aborted:true, error:'cancelled'} instead of throwing: there is no rejection to fall out of, and the naive statusnode.textcontent = res.error writes into a window that is gone. a continuation that touches activation-owned state must check first — the result's aborted , or ctx.signal.aborted — which is precisely the job mods/update/ 's restartopdead / applyopdead still do by hand. the signal cancels the request for you; it does not return you to a live mod. if (ctx.signal) is a capability check, not a liveness check. the ctx is not dismantled at teardown, so ctx.signal stays truthy forever — it is the aborted signal by then. an async callback that resumes after teardown, sees a truthy ctx.signal , arms a settimeout and registers an abort listener to clear it has built a leak: an already-aborted signal never replays its abort event to a listener added afterwards, so that listener never runs and the timer fires against torn-down state. (core codes to the same fact — hostfetch tests callersignal.aborted up front rather than relying on a listener.) check ctx.signal.aborted before arming anything on a path that can resume late. an abort listener is teardown-time mod code. it runs after the record reads dead, so from inside one a savechain is already dropping batches and a settings write cannot re-enter your own theme subscriber. it is not a place to rescue a last save from — see the pairing rule below. explicit pass, never ambient. the loader injects ctx.signal into nothing. ctx.file and ctx.session forward opts.signal only when you hand it over, and hostfetch composes it with that call's deadline instead of replacing it, so a cancel and a timeout stay distinguishable at the result. an ambient signal would silently change every existing call site and abort work a mod deliberately wants to finish. an aborted call still resolves; it never rejects. ctx.file maps the abort to {ok:false, aborted:true, error:'cancelled'} — 'cancelled' , the word the file family already used for a user pressing cancel, distinct from its {ok:false, timedout:true, …} deadline. ctx.session keeps its status-carrying envelope and resolves {status:0, json:{ok:false, error:'aborterror: mod \"<id>\" was torn down'}} — the stringified reason, named so a console says which mod's teardown cancelled the request. feature-detect it as a value, not a function. if (ctx.signal) , ctx.capabilities.signal , or needs: ['signal'] ; typeof ctx.signal === 'function' is the detection that would be wrong, because unlike every family whose members are callable this one is the value — which is also why it owns a capability entry rather than being gated as a member of one. a build whose engine has no constructible abortcontroller gets no ctx.signal at all rather than a placeholder that never aborts, and ctx.capabilities reports it absent, so needs: ['signal'] blocks with a reason instead of running against a signal that quietly does nothing. ctxversion stays 1 . **the pairing rule: work that is supposed to outlive the teardown must not carry the signal. they are two halves of one decision, and reading only the first half is how a migration breaks a save. the shipped referent is #196's deliberate final flush() : mods/scratchpad/ calls it from a window-close cleanup** precisely so a last edit reaches the server, and neither serverstore.update() nor savechain takes a signal at all — there is no option to pass one, and that is deliberate. the failure to avoid is threading ctx.signal through the writes you make on that same close-time path (a final ctx.file.write , a last ctx.session call): a disable would then abort exactly the work you added the cleanup for. if it must survive the teardown, it does not take the signal. ---","title":"8. The `ctx` surface (contract v1)"},{"body":[{"spans":[{"t":"text","v":"A mod may ship a "},{"t":"code","v":"help.md"},{"t":"text","v":" beside its "},{"t":"code","v":"mod.json"},{"t":"text","v":", in the same wiki Markdown the "},{"t":"code","v":"wiki/"},{"t":"text","v":" pages use. "},{"t":"code","v":"help_corpus.build_mod_sections()"},{"t":"text","v":" parses every mod dir that has "},{"t":"strong","v":"both"},{"t":"text","v":" files, turns it into a Help section tagged with the owning mod id (so the frontend hides it while the mod is disabled), and "},{"t":"code","v":"build_full_corpus()"},{"t":"text","v":" merges it with the wiki corpus, sorted by "},{"t":"code","v":"(order, slug)"},{"t":"text","v":". A slug that collides — with another mod or with a wiki page — is a hard "},{"t":"code","v":"BuildError"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"webterm/broker/help_corpus.json"},{"t":"text","v":" is the "},{"t":"strong","v":"packaged, tooling-generated"},{"t":"text","v":" fallback, and it bakes the shipped mod sections in. It is never hand-edited."}],"t":"p"},{"spans":[{"t":"text","v":"An "},{"t":"strong","v":"installed"},{"t":"text","v":" mod's "},{"t":"code","v":"help.md"},{"t":"text","v":" is captured into the broker's index at install or scan time and layered onto the served corpus at "},{"t":"strong","v":"serve time only"},{"t":"text","v":" — never into the packaged JSON, which is why the regenerator's output does not depend on what happens to be installed on your machine. It is also the one part of an install that does appear without a page reload."}],"t":"p"},{"spans":[{"t":"strong","v":"Tip: "},{"t":"strong","v":"If you add, remove or edit any "},{"t":"code","v":"help.md"},{"t":"strong","v":" (or the "},{"t":"code","v":"help"},{"t":"strong","v":" block in a "},{"t":"code","v":"mod.json"},{"t":"strong","v":"), you must run:"},{"t":"text","v":"  ``"},{"t":"code","v":" python -m webterm.broker.help_corpus "},{"t":"text","v":"``"}],"t":"p"},{"spans":[{"t":"code","v":"tests/test_help_corpus.py::test_packaged_json_in_sync_with_wiki"},{"t":"text","v":" regenerates the corpus and asserts a "},{"t":"strong","v":"byte-exact"},{"t":"text","v":" match against the checked-in file. It fails on the smallest drift, and the fix is always to run the regenerator, never to edit the test."}],"t":"p"},{"spans":[{"t":"code","v":"help.md"},{"t":"text","v":" is optional. Skip it when the UI already explains the whole feature — a mod that is one Control Panel control has nothing a Help page would add. That is an authoring judgement, not a rule."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"9. help: `help.md` and the regen you must not skip a mod may ship a help.md beside its mod.json , in the same wiki markdown the wiki/ pages use. help_corpus.build_mod_sections() parses every mod dir that has both files, turns it into a help section tagged with the owning mod id (so the frontend hides it while the mod is disabled), and build_full_corpus() merges it with the wiki corpus, sorted by (order, slug) . a slug that collides — with another mod or with a wiki page — is a hard builderror . webterm/broker/help_corpus.json is the packaged, tooling-generated fallback, and it bakes the shipped mod sections in. it is never hand-edited. an installed mod's help.md is captured into the broker's index at install or scan time and layered onto the served corpus at serve time only — never into the packaged json, which is why the regenerator's output does not depend on what happens to be installed on your machine. it is also the one part of an install that does appear without a page reload. tip: if you add, remove or edit any help.md (or the help block in a mod.json ), you must run: `` python -m webterm.broker.help_corpus `` tests/test_help_corpus.py::test_packaged_json_in_sync_with_wiki regenerates the corpus and asserts a byte-exact match against the checked-in file. it fails on the smallest drift, and the fix is always to run the regenerator, never to edit the test. help.md is optional. skip it when the ui already explains the whole feature — a mod that is one control panel control has nothing a help page would add. that is an authoring judgement, not a rule. ---","title":"9. Help: `help.md` and the regen you must not skip"},{"body":[{"spans":[{"t":"text","v":"10.1 The "},{"t":"code","v":"x-"},{"t":"text","v":" namespace rule"}],"t":"sub"},{"spans":[{"t":"text","v":"**An id is reserved for first-party (shipped) mods iff it does not start with "},{"t":"code","v":"x-"},{"t":"text","v":"; an installed mod's id must.**"}],"t":"p"},{"spans":[{"t":"text","v":"The id shape is the same on both sides — "},{"t":"code","v":"_MODSTORE_ID_RE"},{"t":"text","v":" in "},{"t":"code","v":"app.py"},{"t":"text","v":" and "},{"t":"code","v":"MOD_ID_RE"},{"t":"text","v":" in the loader, both "},{"t":"code","v":"[a-z0-9][a-z0-9-]{0,63}"},{"t":"text","v":" — and "},{"t":"code","v":"x-notes"},{"t":"text","v":" already matched it before the rule existed. That is the point: the rule is a prefix convention layered on top, so "},{"t":"strong","v":"no key a shipped mod owns moves, and neither validator has to change."}],"t":"p"},{"spans":[{"t":"text","v":"What the one-character prefix buys is that a single lexical test separates all five namespaces a mod id keys, because all five derive from the same string:"}],"t":"p"},{"spans":[{"t":"code","v":"/mod-store/<id>"},{"t":"text","v":" — the durable per-mod server store;"}],"t":"bullet"},{"spans":[{"t":"text","v":"the per-broker mod-policy pin map ("},{"t":"code","v":"webterm_mod_policy.json"},{"t":"text","v":", "},{"t":"code","v":"POST /mods/policy"},{"t":"text","v":");"}],"t":"bullet"},{"spans":[{"t":"text","v":"the "},{"t":"code","v":"webterm:mod:<id>:"},{"t":"text","v":" localStorage prefix behind "},{"t":"code","v":"ctx.storage"},{"t":"text","v":";"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm:mods:disabled"},{"t":"text","v":", the per-browser enable-override set;"}],"t":"bullet"},{"spans":[{"t":"text","v":"the catalog — "},{"t":"code","v":"ui._MODS"},{"t":"text","v":" / "},{"t":"code","v":"mods/<id>/"},{"t":"text","v":" on one side, "},{"t":"code","v":"<mods_dir>/<id>/"},{"t":"text","v":" on"}],"t":"bullet"},{"spans":[{"t":"text","v":"the other."}],"t":"p"},{"spans":[{"t":"text","v":"It is enforced in three places, and a change to the rule has to update all three: the install validator ("},{"t":"code","v":"400 reserved_id"},{"t":"text","v":"), the scanner (a non-"},{"t":"code","v":"x-"},{"t":"text","v":" directory in "},{"t":"code","v":"mods_dir"},{"t":"text","v":" is skipped with a loud log, so a hand-dropped "},{"t":"code","v":"clock/"},{"t":"text","v":" cannot shadow the shipped "},{"t":"code","v":"clock"},{"t":"text","v":"), and CI (no shipped id starts with "},{"t":"code","v":"x-"},{"t":"text","v":", no shipped "},{"t":"code","v":"requires"},{"t":"text","v":" names an "},{"t":"code","v":"x-"},{"t":"text","v":" id)."}],"t":"p"},{"spans":[{"t":"code","v":"x-<author>-<name>"},{"t":"strong","v":" (e.g. "},{"t":"code","v":"x-johnconnornpc-notes"},{"t":"strong","v":") is a documented convention, not an enforced field."},{"t":"text","v":" Nothing validates the middle segment and there is no "},{"t":"code","v":"vendor"},{"t":"text","v":" key in the manifest. Carrying a real scope component in the id would mean widening both validators, the policy sanitiser and the mod-store key filter in lockstep — four places to keep synchronized, for a component nothing would enforce anyway."}],"t":"p"},{"spans":[{"t":"strong","v":"Not covered, plainly:"},{"t":"text","v":" DOM ids a mod writes in its own markup. The loader-generated ids are built from the mod id and so are namespaced; a mod that writes "},{"t":"code","v":"id=\"clock-chip\""},{"t":"text","v":" itself is not. That is authoring convention, exactly as it already is for shipped mods."}],"t":"p"},{"spans":[{"t":"strong","v":"There is no automatic migration of pre-existing "},{"t":"code","v":"x-"},{"t":"strong","v":" state."},{"t":"text","v":" "},{"t":"code","v":"/mod-store"},{"t":"text","v":" and the pin map already accept any id-shaped key, so a browser can hold "},{"t":"code","v":"/mod-store/x-notes"},{"t":"text","v":", a pin for "},{"t":"code","v":"x-notes"},{"t":"text","v":", and "},{"t":"code","v":"webterm:mod:x-notes:*"},{"t":"text","v":" before anything called "},{"t":"code","v":"x-notes"},{"t":"text","v":" was ever installed. Installing it adopts all of it. The install response says so — "},{"t":"code","v":"adopts_existing_state: {mod_store, pin}"},{"t":"text","v":" — for the server-side half; localStorage cannot be inspected server-side and is not covered."}],"t":"p"},{"spans":[{"t":"text","v":"10.2 The portable-mod contract"}],"t":"sub"},{"spans":[{"t":"text","v":"An installed mod is loaded as a "},{"t":"strong","v":"separate classic "},{"t":"code","v":"<script src>"},{"t":"text","v":", not as an "},{"t":"code","v":"import()"},{"t":"text","v":" and not spliced into the bundle. Classic (not module) is deliberate: a module would impose module scope plus strict mode and would not publish its top-level function declarations. CSP "},{"t":"code","v":"'self'"},{"t":"text","v":" authorises both identically, so this is a source-mobility decision, not a security one."}],"t":"p"},{"spans":[{"t":"strong","v":"A spliced fragment and a separately-loaded classic script are not equivalent."},{"t":"text","v":" The real differences:"}],"t":"p"},{"spans":[{"t":"text","v":" · spliced into the bundle · loaded as "},{"t":"code","v":"<script src>"}],"t":"sub"},{"spans":[{"t":"code","v":"\"use strict\""},{"t":"text","v":" at the top — a plain string expression — "},{"t":"strong","v":"inert"},{"t":"text","v":" — a "},{"t":"strong","v":"directive"},{"t":"text","v":" — the whole file is strict"}],"t":"bullet"},{"spans":[{"t":"text","v":"declaration instantiation — all of the inline script's declarations exist before any of its statements run, so your top-level function is visible to code earlier in the bundle — your declarations do not exist until the file is fetched "},{"t":"strong","v":"and executed"}],"t":"bullet"},{"spans":[{"t":"text","v":"execution timing — page-script eval, before "},{"t":"code","v":"loadMods()"},{"t":"text","v":" — after "},{"t":"code","v":"/info"},{"t":"text","v":", asynchronously, possibly after boot"}],"t":"bullet"},{"spans":[{"t":"code","v":"document.currentScript"},{"t":"text","v":" — the one inline "},{"t":"code","v":"<script>"},{"t":"text","v":" (no package stamp) — the injected element, carrying "},{"t":"code","v":"dataset.modPackage"}],"t":"bullet"},{"spans":[{"t":"text","v":"a literal "},{"t":"code","v":"</script>"},{"t":"text","v":" — "},{"t":"strong","v":"fatal"},{"t":"text","v":" — closes the element, breaks the page — harmless"}],"t":"bullet"},{"spans":[{"t":"text","v":"a "},{"t":"code","v":"const"},{"t":"text","v":"/"},{"t":"code","v":"let"},{"t":"text","v":"/"},{"t":"code","v":"class"},{"t":"text","v":" name collision — a compile error for the "},{"t":"strong","v":"whole bundle"},{"t":"text","v":" — blank desktop — a compile error for "},{"t":"strong","v":"that script only"},{"t":"text","v":" — one dead mod"}],"t":"bullet"},{"spans":[{"t":"text","v":"So the contract, if you want source that moves in both directions unchanged:"}],"t":"p"},{"spans":[{"t":"text","v":"**Nothing at top level runs except the "},{"t":"code","v":"registerMod({…})"},{"t":"text","v":" call.** No DOM"}],"t":"bullet"},{"spans":[{"t":"text","v":"writes, no fetches, no timers, no reads of desktop state — all work in "},{"t":"code","v":"init(ctx)"},{"t":"text","v":". Plain declarations are fine (see rule 5); side effects are not."}],"t":"p"},{"spans":[{"t":"strong","v":"No top-level "},{"t":"code","v":"\"use strict\""},{"t":"strong","v":" directive."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Nothing outside your package may depend on your top-level names."},{"t":"text","v":" A"}],"t":"bullet"},{"spans":[{"t":"text","v":"top-level call into your functions from another fragment cannot work: as an installed package, your declarations do not exist when the bundle evaluates. Cross-mod calls at runtime are fine, but only once the provider is "},{"t":"strong","v":"active"},{"t":"text","v":" — declare it in "},{"t":"code","v":"requires"},{"t":"text","v":" and do not assume every package has executed by the time boot finishes (the load deadline explicitly lets boot proceed with a script still in flight). This is why some shipped mods are not portable as-is: core's app-window store calls "},{"t":"code","v":"openNoteOrEditorWindow"},{"t":"text","v":", which the "},{"t":"code","v":"editor"},{"t":"text","v":" mod declares at top level."}],"t":"p"},{"spans":[{"t":"strong","v":"No literal "},{"t":"code","v":"</script>"},{"t":"strong","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Prefix every top-level "},{"t":"code","v":"const"},{"t":"strong","v":" / "},{"t":"code","v":"let"},{"t":"strong","v":" / "},{"t":"code","v":"function"},{"t":"strong","v":" name."},{"t":"text","v":" In-tree a"}],"t":"bullet"},{"spans":[{"t":"text","v":"collision with core kills the page; out of tree it kills your mod."}],"t":"p"},{"spans":[{"t":"text","v":"Rules 1, 2 and 4 are enforced by the portable-mod lint in "},{"t":"code","v":"tests/test_ui_assets.py"},{"t":"text","v":", and every shipped mod passes them. Rule 3's known in-tree exceptions are pinned there too, as an exact set of seven edges:"}],"t":"p"},{"spans":[{"t":"text","v":"top-level name (owner) · reached from"}],"t":"sub"},{"spans":[{"t":"code","v":"openNoteOrEditorWindow"},{"t":"text","v":" ("},{"t":"code","v":"editor"},{"t":"text","v":") — core "},{"t":"code","v":"54_js_app_windows_store.js"},{"t":"text","v":", "},{"t":"code","v":"sticky"}],"t":"bullet"},{"spans":[{"t":"code","v":"loadCodeMirror"},{"t":"text","v":" ("},{"t":"code","v":"editor"},{"t":"text","v":") — "},{"t":"code","v":"scratchpad"}],"t":"bullet"},{"spans":[{"t":"code","v":"applyPattern"},{"t":"text","v":" ("},{"t":"code","v":"pattern"},{"t":"text","v":") — "},{"t":"code","v":"theme"}],"t":"bullet"},{"spans":[{"t":"code","v":"findHelpWindow"},{"t":"text","v":", "},{"t":"code","v":"refreshHelpCorpus"},{"t":"text","v":" ("},{"t":"code","v":"help"},{"t":"text","v":") — core "},{"t":"code","v":"86_js_mod_loader.js"}],"t":"bullet"},{"spans":[{"t":"code","v":"toggleHelpWindow"},{"t":"text","v":" ("},{"t":"code","v":"help"},{"t":"text","v":") — core "},{"t":"code","v":"78_js_keybindings.js"}],"t":"bullet"},{"spans":[{"t":"text","v":"The set is a drift guard "},{"t":"strong","v":"both ways"},{"t":"text","v":" — a stale edge fails as loudly as a new one — so decoupling a mod forces it to shrink. It did: #177 retired "},{"t":"code","v":"agent-docs"},{"t":"text","v":" (see "},{"t":"code","v":"webterm/broker/mods-deprecated/README.md"},{"t":"text","v":"), which took two edges with it, "},{"t":"code","v":"editorFile"},{"t":"text","v":" ("},{"t":"code","v":"editor"},{"t":"text","v":") ← "},{"t":"code","v":"agent-docs"},{"t":"text","v":" and "},{"t":"code","v":"openAgentDocsWindow"},{"t":"text","v":" ("},{"t":"code","v":"agent-docs"},{"t":"text","v":") ← "},{"t":"code","v":"editor"},{"t":"text","v":". The surviving call site in "},{"t":"code","v":"editor.js"},{"t":"text","v":" is a "},{"t":"code","v":"typeof openAgentDocsWindow === 'function'"},{"t":"text","v":" guard, which owns no shipped name and so is not an edge."}],"t":"p"},{"spans":[{"t":"text","v":"So "},{"t":"code","v":"editor"},{"t":"text","v":", "},{"t":"code","v":"help"},{"t":"text","v":" and "},{"t":"code","v":"pattern"},{"t":"text","v":" cannot be republished as installable packages unchanged. "},{"t":"strong","v":"Rule 5 is not enforced"},{"t":"text","v":" — check it yourself. The lint is a floor, not a proof: it reads source text, so it cannot see a call hidden inside "},{"t":"code","v":"registerMod"},{"t":"text","v":"'s own argument."}],"t":"p"},{"spans":[{"t":"text","v":"10.3 Installing takes effect on the next page load — always"}],"t":"sub"},{"spans":[{"t":"code","v":"POST /mods/install"},{"t":"text","v":", "},{"t":"code","v":"/mods/uninstall"},{"t":"text","v":" and "},{"t":"code","v":"/mods/rescan"},{"t":"text","v":" change what the broker serves "},{"t":"strong","v":"immediately"},{"t":"text","v":" — the catalog, the asset routes and the Help corpus all swap at once — but they change "},{"t":"strong","v":"nothing in an already-open desktop"},{"t":"text","v":". Install and uninstall say so in their own response, with "},{"t":"code","v":"\"applies\": \"next_page_load\""},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"This is "},{"t":"strong","v":"forced, not unimplemented."},{"t":"text","v":" JavaScript global lexical bindings cannot be removed. A mod whose top level says "},{"t":"code","v":"const DB = …"},{"t":"text","v":" cannot be re-executed in the same page: the second execution dies with "},{"t":"code","v":"SyntaxError: Identifier 'DB' has already been declared"},{"t":"text","v":", and a "},{"t":"code","v":"var"},{"t":"text","v":"-based mod instead hits "},{"t":"code","v":"registerMod"},{"t":"text","v":"'s duplicate-id throw. "},{"t":"code","v":"_takeDown()"},{"t":"text","v":" is a teardown, not an unloader — it reverses "},{"t":"code","v":"init"},{"t":"text","v":", it does not un-execute a script. So there is no live replace, no live-uninstall teardown, and no discovery of an install another client performed. That is the same contract the per-broker mod pins already ship."}],"t":"p"},{"spans":[{"t":"text","v":"What is delivered is what the feature was for: "},{"t":"strong","v":"no process restart"},{"t":"text","v":" and no source edit. Drop a mod into a live broker, reload the tab, and it is there."}],"t":"p"},{"spans":[{"t":"text","v":"Related loader behaviour worth knowing:"}],"t":"p"},{"spans":[{"t":"text","v":"**Packages load in parallel; the scripts within one package load in"}],"t":"bullet"},{"spans":[{"t":"text","v":"manifest order, one after the next.** Across packages, order is irrelevant because the topological sort runs afterwards, and document-ordered async would let one slow file head-of-line-block every other mod. Within a package the topo sort says nothing at all — it orders mod declarations, not the files of one mod — so "},{"t":"code","v":"scripts"},{"t":"text","v":" is honoured as an ordered list. "},{"t":"strong","v":"A multi-script package must call "},{"t":"code","v":"registerMod"},{"t":"strong","v":" from its LAST script"},{"t":"text","v":": a registration is acted on the moment it arrives, so registering from the first file would let the mod init before its siblings had loaded."}],"t":"p"},{"spans":[{"t":"strong","v":"Stylesheets are injected but not awaited"},{"t":"text","v":", and are live regardless of"}],"t":"bullet"},{"spans":[{"t":"text","v":"enable, pin or init — deliberately the same posture as a shipped mod's CSS. A teardown cannot remove them."}],"t":"p"},{"spans":[{"t":"text","v":"Each "},{"t":"code","v":"(id, gen, file)"},{"t":"text","v":" URL is fetched "},{"t":"strong","v":"at most once per page load"},{"t":"text","v":", through"}],"t":"bullet"},{"spans":[{"t":"text","v":"a shared in-flight promise map, so the post-login retry cannot double-execute a package."}],"t":"p"},{"spans":[{"t":"code","v":"MOD_SCRIPT_TIMEOUT_MS"},{"t":"text","v":" is a "},{"t":"strong","v":"proceed-anyway deadline, not a cancel"},{"t":"text","v":" —"}],"t":"bullet"},{"spans":[{"t":"text","v":"nothing can cancel an in-flight "},{"t":"code","v":"<script>"},{"t":"text","v":". A package that lands later still executes, and "},{"t":"code","v":"_lateRegister"},{"t":"text","v":" brings it up if this page requested it."}],"t":"p"},{"spans":[{"t":"code","v":"registerMod"},{"t":"text","v":" binds a declaration to the package whose script is running"}],"t":"bullet"},{"spans":[{"t":"text","v":"("},{"t":"code","v":"document.currentScript.dataset.modPackage"},{"t":"text","v":") and refuses a declaration whose id is not that package's, with a "},{"t":"code","v":"wrong-id"},{"t":"text","v":" row. This is a "},{"t":"strong","v":"correctness convention, not a boundary"},{"t":"text","v":": while a stamped package script runs synchronously, a declaration for a different id is refused instead of silently colliding with (say) the shipped "},{"t":"code","v":"clock"},{"t":"text","v":". A package that registers from a promise or timeout has "},{"t":"code","v":"currentScript === null"},{"t":"text","v":" and is simply unattributable, at which point the duplicate-id "},{"t":"code","v":"ModConflictError"},{"t":"text","v":" is the only backstop."}],"t":"p"},{"spans":[{"t":"code","v":"?nomods=1"},{"t":"text","v":" on the page URL makes "},{"t":"code","v":"loadMods()"},{"t":"text","v":" return before it fetches or"}],"t":"bullet"},{"spans":[{"t":"text","v":"inits anything. It is the rescue hatch for a mod that bricks the desktop and therefore makes the Control Panel — where you would uninstall it — unreachable. It is read-only and deliberately not scrubbed from the URL the way "},{"t":"code","v":"?token="},{"t":"text","v":" is, because replaceState-ing it away would mean a reload silently re-enabling the mod you are there to remove."}],"t":"p"},{"spans":[{"t":"text","v":"10.4 An installed mod's code and styles are public"}],"t":"sub"},{"spans":[{"t":"code","v":"GET /mods/<modId>/<gen>/<name>"},{"t":"text","v":" is "},{"t":"strong","v":"public"},{"t":"text","v":", like "},{"t":"code","v":"GET /"},{"t":"text","v":" and "},{"t":"code","v":"/vendor/*"},{"t":"text","v":", and that is forced rather than chosen: a "},{"t":"code","v":"<script src>"},{"t":"text","v":" cannot carry an "},{"t":"code","v":"Authorization"},{"t":"text","v":" header, "},{"t":"code","v":"?token="},{"t":"text","v":" is structurally banned, and a "},{"t":"code","v":"fetch"},{"t":"text","v":"+"},{"t":"code","v":"blob:"},{"t":"text","v":" workaround would need "},{"t":"code","v":"blob:"},{"t":"text","v":" in "},{"t":"code","v":"script-src"},{"t":"text","v":". The posture is the existing one — "},{"t":"code","v":"GET /"},{"t":"text","v":" is public and already carries every shipped mod's source."}],"t":"p"},{"spans":[{"t":"text","v":"So "},{"t":"strong","v":"an installed mod's source and its stylesheets are readable without a token"},{"t":"text","v":" — by anyone who knows the id and the generation and the file name. "},{"t":"strong","v":"Do not put a secret in a mod"},{"t":"text","v":" — not in its code and not in its stylesheet."}],"t":"p"},{"spans":[{"t":"code","v":"help.md"},{"t":"text","v":" is a different matter: only "},{"t":"code","v":".js"},{"t":"text","v":"/"},{"t":"code","v":".css"},{"t":"text","v":" are servable, so this route cannot hand it out at all. Its only surface is "},{"t":"code","v":"/help-corpus.json"},{"t":"text","v":", which is public for the same bootstrap reason "},{"t":"code","v":"GET /"},{"t":"text","v":" is (the Help window has to render on the login page) — but since #173 it serves the installed mods' help sections "},{"t":"strong","v":"only to a caller holding the token"},{"t":"text","v":". Without one it is the wiki + shipped-mod corpus alone, so installed ids, help text and manifest label/icon are not enumerable. Treat that as not published, not as secret storage: it is one token away, and a mod's help is written to be read."}],"t":"p"},{"spans":[{"t":"strong","v":"The generation is not a second secret."},{"t":"text","v":" "},{"t":"code","v":"<gen>"},{"t":"text","v":" is a content hash of the package, so anyone holding the exact bytes of a publicly distributed mod recomputes it and can ask this broker for the file: a "},{"t":"code","v":"200"},{"t":"text","v":" rather than a "},{"t":"code","v":"404"},{"t":"text","v":" confirms that mod is installed. So #173 removed "},{"t":"strong","v":"enumeration"},{"t":"text","v":" — the list of installed ids, and the help text with it — not every last bit about a mod someone already knows to ask for. A mod whose bytes were never published stays unguessable in both segments."}],"t":"p"},{"spans":[{"t":"text","v":"10.5 The install API"}],"t":"sub"},{"spans":[{"t":"code","v":"POST /mods/install          // browser auth token; NOT lease-gated\n{ \"manifest\": { \"id\": \"x-notes\", \"version\": \"1.0.0\", \"ctxVersion\": 1,\n                \"title\": \"Notes\", \"description\": \"…\",\n                \"scripts\": [\"notes.js\"], \"styles\": [\"notes.css\"],\n                \"requires\": [\"editor\"], \"tiers\": [\"settings\"],\n                \"permissions\": [\"file\"],\n                \"help\": { \"label\": \"Notes\", \"icon\": \"📓\", \"order\": 2100 } },\n  \"files\": { \"notes.js\": \"…\", \"notes.css\": \"…\", \"help.md\": \"…\" },\n  \"replace\": false }\n\nPOST /mods/uninstall   {\"id\": \"x-notes\", \"purge\": false}\nPOST /mods/rescan      {}\nGET  /mods/installed                    // operator detail for the Control Panel"}],"t":"pre"},{"spans":[{"t":"text","v":"All four are browser-realm token-gated, "},{"t":"code","v":"serve_ui"},{"t":"text","v":"-gated (a headless broker registers none of them), and deliberately "},{"t":"strong","v":"not"},{"t":"text","v":" lease-gated — this is broker configuration, and the point is being able to administer a broker somebody else is using."}],"t":"p"},{"spans":[{"t":"strong","v":"The payload is JSON with text files, deliberately not an archive."},{"t":"text","v":" No zip parser means no zip-slip, no zip bomb, no symlink entries. Mods are text; binary rides "},{"t":"code","v":"data:"},{"t":"text","v":" URIs. "},{"t":"code","v":"mod.json"},{"t":"text","v":" is "},{"t":"strong","v":"written by the broker"},{"t":"text","v":" from the canonical manifest and is a reserved key in "},{"t":"code","v":"files"},{"t":"text","v":", so a payload cannot ship a manifest that disagrees with what was validated."}],"t":"p"},{"spans":[{"t":"text","v":"Differences from a shipped manifest, all enforced:"}],"t":"p"},{"spans":[{"t":"code","v":"scripts"},{"t":"text","v":" is "},{"t":"strong","v":"required"},{"t":"text","v":" and is an ordered non-empty list of "},{"t":"code","v":".js"},{"t":"text","v":" names, all"}],"t":"bullet"},{"spans":[{"t":"text","v":"present in "},{"t":"code","v":"files"},{"t":"text","v":". "},{"t":"code","v":"entry"},{"t":"text","v":" is not accepted ("},{"t":"code","v":"unknown_manifest_key"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"code","v":"id"},{"t":"text","v":" must pass the id regex ("},{"t":"code","v":"bad_mod_id"},{"t":"text","v":" otherwise) "},{"t":"strong","v":"and"},{"t":"text","v":" start with "},{"t":"code","v":"x-"}],"t":"bullet"},{"spans":[{"t":"text","v":"("},{"t":"code","v":"reserved_id"},{"t":"text","v":" otherwise)."}],"t":"p"},{"spans":[{"t":"strong","v":"Unknown keys are rejected"},{"t":"text","v":", not ignored."}],"t":"bullet"},{"spans":[{"t":"code","v":"help.slug"},{"t":"text","v":" is accepted and dropped — an installed section's slug is **forced"}],"t":"bullet"},{"spans":[{"t":"text","v":"to the mod id**, which is "},{"t":"code","v":"x-"},{"t":"text","v":"-prefixed, and CI pins that no wiki page stem and no shipped mod's "},{"t":"code","v":"help.slug"},{"t":"text","v":" uses that prefix. The merge still checks for a collision and drops the installed section if one somehow appears, because merging installed help must never be able to blank the Help window."}],"t":"p"},{"spans":[{"t":"code","v":"defaultEnabled"},{"t":"text","v":" is accepted and ignored; the catalog always reports"}],"t":"bullet"},{"spans":[{"t":"code","v":"default_enabled: false"},{"t":"text","v":". Install and enable are separate steps, which matches how the shipped default-off mods already behave. "},{"t":"strong","v":"This is not containment:"},{"t":"text","v":" an installed package's scripts are fetched and executed on every page load whatever its enabled state — being off only means "},{"t":"code","v":"init()"},{"t":"text","v":" is not called, and its stylesheets are live either way."}],"t":"p"},{"spans":[{"t":"code","v":"permissions"},{"t":"text","v":" is checked against the source text, not merely displayed — the"}],"t":"bullet"},{"spans":[{"t":"text","v":"install-time capability lint (#193), covered on its own below."}],"t":"p"},{"spans":[{"t":"text","v":"Filenames must match "},{"t":"code","v":"[A-Za-z0-9][A-Za-z0-9._-]{0,63}"},{"t":"text","v":" with a "},{"t":"code","v":".js"},{"t":"text","v":"/"},{"t":"code","v":".css"},{"t":"text","v":"/"},{"t":"code","v":".md"}],"t":"bullet"},{"spans":[{"t":"text","v":"suffix, must avoid "},{"t":"code","v":"?"},{"t":"text","v":", "},{"t":"code","v":"#"},{"t":"text","v":", "},{"t":"code","v":"%"},{"t":"text","v":" and "},{"t":"code","v":":"},{"t":"text","v":" (an NTFS alternate data stream: "},{"t":"code","v":"base.css:payload.js"},{"t":"text","v":" passes a naive bare-name test), must not be a Windows device stem ("},{"t":"code","v":"CON"},{"t":"text","v":", "},{"t":"code","v":"PRN"},{"t":"text","v":", "},{"t":"code","v":"AUX"},{"t":"text","v":", "},{"t":"code","v":"NUL"},{"t":"text","v":", "},{"t":"code","v":"COM1-9"},{"t":"text","v":", "},{"t":"code","v":"LPT1-9"},{"t":"text","v":"), and must not collide with another name under "},{"t":"code","v":"casefold()"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Every file must be valid UTF-8, "},{"t":"strong","v":"BOM-free"},{"t":"text","v":", "},{"t":"strong","v":"newline-terminated"},{"t":"text","v":", and"}],"t":"bullet"},{"spans":[{"t":"text","v":"within "},{"t":"code","v":"ui._MAX_LINES"},{"t":"text","v":" lines — the same rule set the spliced fragments obey, imported from "},{"t":"code","v":"ui"},{"t":"text","v":" rather than retyped."}],"t":"p"},{"spans":[{"t":"text","v":"A "},{"t":"code","v":".css"},{"t":"text","v":" file may not reference an external origin: "},{"t":"code","v":"@import"},{"t":"text","v":" and absolute"}],"t":"bullet"},{"spans":[{"t":"code","v":"url(http…)"},{"t":"text","v":" / "},{"t":"code","v":"url(//…)"},{"t":"text","v":" are rejected ("},{"t":"code","v":"css_external_reference"},{"t":"text","v":"). The app's CSP sets only "},{"t":"code","v":"script-src"},{"t":"text","v":" and "},{"t":"code","v":"frame-ancestors"},{"t":"text","v":", so a stylesheet would otherwise be a silent egress channel. "},{"t":"strong","v":"Defence in depth, not a boundary"},{"t":"text","v":" — the mod's own JS can "},{"t":"code","v":"fetch()"},{"t":"text","v":" anything."}],"t":"p"},{"spans":[{"t":"text","v":"Caps, at the time of writing: 32 mods per broker, 32 files per mod, 256 KiB"}],"t":"bullet"},{"spans":[{"t":"text","v":"per file, 512 KiB per mod, 2 MiB request body (64 KiB for the uninstall and rescan bodies). The constants in "},{"t":"code","v":"modinstall.py"},{"t":"text","v":" are authoritative, and "},{"t":"code","v":"GET /mods/installed"},{"t":"text","v":" reports the live values under "},{"t":"code","v":"limits"},{"t":"text","v":" — read them from there rather than from here."}],"t":"p"},{"spans":[{"t":"text","v":"Every refusal carries a distinct code, because \"your mod was rejected\" with no reason is a support ticket: "},{"t":"code","v":"too_large"},{"t":"text","v":" · "},{"t":"code","v":"bad_json"},{"t":"text","v":" · "},{"t":"code","v":"bad_mod_id"},{"t":"text","v":" · "},{"t":"code","v":"bad_generation"},{"t":"text","v":" · "},{"t":"code","v":"reserved_id"},{"t":"text","v":" · "},{"t":"code","v":"id_in_use"},{"t":"text","v":" (409) · "},{"t":"code","v":"not_installed"},{"t":"text","v":" (404) · "},{"t":"code","v":"bad_file_name"},{"t":"text","v":" · "},{"t":"code","v":"reserved_file_name"},{"t":"text","v":" · "},{"t":"code","v":"too_many_files"},{"t":"text","v":" · "},{"t":"code","v":"file_too_large"},{"t":"text","v":" · "},{"t":"code","v":"total_too_large"},{"t":"text","v":" · "},{"t":"code","v":"bad_encoding"},{"t":"text","v":" · "},{"t":"code","v":"bad_scripts"},{"t":"text","v":" · "},{"t":"code","v":"bad_styles"},{"t":"text","v":" · "},{"t":"code","v":"bad_requires"},{"t":"text","v":" · "},{"t":"code","v":"bad_manifest_field"},{"t":"text","v":" · "},{"t":"code","v":"unknown_manifest_key"},{"t":"text","v":" · "},{"t":"code","v":"css_external_reference"},{"t":"text","v":" · "},{"t":"code","v":"undeclared_capability"},{"t":"text","v":" · "},{"t":"code","v":"too_many_mods"},{"t":"text","v":" (409) · "},{"t":"code","v":"write_failed"},{"t":"text","v":" (500). "},{"t":"code","v":"modinstall.ERROR_STATUS"},{"t":"text","v":" is the authoritative map; a client must tolerate a code it has not seen."}],"t":"p"},{"spans":[{"t":"strong","v":"The "},{"t":"code","v":"permissions"},{"t":"strong","v":" capability lint (#193)."},{"t":"text","v":" Unlike "},{"t":"code","v":"tiers"},{"t":"text","v":" (§5), which is a self-reported claim nothing checks, "},{"t":"code","v":"permissions"},{"t":"text","v":" is a "},{"t":"strong","v":"closed vocabulary"},{"t":"text","v":" ("},{"t":"code","v":"modinstall.PERMISSIONS"},{"t":"text","v":"), and "},{"t":"code","v":"validate_package"},{"t":"text","v":" scans every "},{"t":"code","v":".js"},{"t":"text","v":" file the package ships for direct, syntactic use of the capability each name covers:"}],"t":"p"},{"spans":[{"t":"text","v":"permission · what it covers"}],"t":"sub"},{"spans":[{"t":"code","v":"clipboard"},{"t":"text","v":" — "},{"t":"code","v":"ctx.clipboard"}],"t":"bullet"},{"spans":[{"t":"code","v":"egress"},{"t":"text","v":" — "},{"t":"code","v":"fetch("},{"t":"text","v":" or "},{"t":"code","v":"hostFetch("}],"t":"bullet"},{"spans":[{"t":"code","v":"file"},{"t":"text","v":" — "},{"t":"code","v":"ctx.file"}],"t":"bullet"},{"spans":[{"t":"code","v":"remote-admin"},{"t":"text","v":" — one of the broker's admin route strings, named literally — at the time of writing "},{"t":"code","v":"/mods/install"},{"t":"text","v":", "},{"t":"code","v":"/mods/uninstall"},{"t":"text","v":", "},{"t":"code","v":"/mods/rescan"},{"t":"text","v":", "},{"t":"code","v":"/mods/policy"},{"t":"text","v":", "},{"t":"code","v":"/update/policy"},{"t":"text","v":" (built from "},{"t":"code","v":"app.ADMIN_ROUTES"},{"t":"text","v":", so a new admin route becomes "},{"t":"code","v":"remote-admin"},{"t":"text","v":" automatically, with no second list to keep in sync)"}],"t":"bullet"},{"spans":[{"t":"code","v":"session"},{"t":"text","v":" — "},{"t":"code","v":"ctx.session"}],"t":"bullet"},{"spans":[{"t":"text","v":"A capability the source uses but "},{"t":"code","v":"permissions"},{"t":"text","v":" did not declare is refused with "},{"t":"code","v":"undeclared_capability"},{"t":"text","v":", naming the "},{"t":"strong","v":"first"},{"t":"text","v":" offender by file (sorted) then by position within it — e.g. "},{"t":"code","v":"notes.js:12 uses 'ctx.file', which needs permissions: [\"file\"] in the manifest"},{"t":"text","v":" as the response's "},{"t":"code","v":"detail"},{"t":"text","v":" text. "},{"t":"strong","v":"Declaring more than you use is fine"},{"t":"text","v":"; only a used-but-undeclared capability is refused."}],"t":"p"},{"spans":[{"t":"strong","v":"Absent and "},{"t":"code","v":"[]"},{"t":"strong","v":" are not the same declaration."},{"t":"text","v":" No "},{"t":"code","v":"permissions"},{"t":"text","v":" key at all means \"written before this lint existed\" — an already-installed generation from before #193 is grandfathered past the check (see "},{"t":"code","v":"/mods/rescan"},{"t":"text","v":" below) and keeps serving. "},{"t":"code","v":"permissions: []"},{"t":"text","v":" is a "},{"t":"strong","v":"positive claim"},{"t":"text","v":" — \"this package's source reaches none of the five\" — and is checked exactly like any other declaration. A new package should always declare truthfully, writing "},{"t":"code","v":"[]"},{"t":"text","v":" when it genuinely uses none of the five; there is no reason for a new manifest to omit the key."}],"t":"p"},{"spans":[{"t":"text","v":"A mention inside a "},{"t":"strong","v":"comment"},{"t":"text","v":" never counts, for any permission — the scanner blanks "},{"t":"code","v":"//"},{"t":"text","v":" and "},{"t":"code","v":"/* */"},{"t":"text","v":" comments (and regex literals) before it looks for anything, the same "},{"t":"code","v":"blank_js_literals"},{"t":"text","v":" machinery the portable-mod lint (§10.2) uses. For "},{"t":"code","v":"clipboard"},{"t":"text","v":" / "},{"t":"code","v":"egress"},{"t":"text","v":" / "},{"t":"code","v":"file"},{"t":"text","v":" / "},{"t":"code","v":"session"},{"t":"text","v":", a mention inside a "},{"t":"strong","v":"string literal"},{"t":"text","v":" doesn't count either — those four are matched against source with string bodies blanked too, so "},{"t":"code","v":"\"call ctx.file to persist\""},{"t":"text","v":" trips nothing. "},{"t":"code","v":"remote-admin"},{"t":"text","v":" is the one exception: its evidence is a literal — a mod naming "},{"t":"code","v":"/mods/policy"},{"t":"text","v":" inside a "},{"t":"code","v":"fetch()"},{"t":"text","v":" call — so that one check alone keeps string bodies intact and blanks only comments and regex literals. A route named in an actual string is a real use; the same text sitting in a "},{"t":"code","v":"//"},{"t":"text","v":" comment still is not."}],"t":"p"},{"spans":[{"t":"text","v":"The rule runs on "},{"t":"strong","v":"both doors"},{"t":"text","v":": the same "},{"t":"code","v":"validate_package"},{"t":"text","v":" call underlies "},{"t":"code","v":"POST /mods/install"},{"t":"text","v":" and the "},{"t":"code","v":"mods_dir"},{"t":"text","v":" scanner ("},{"t":"code","v":"POST /mods/rescan"},{"t":"text","v":" and every boot), so a package dropped by hand faces exactly the check the wire does — see the grandfathering note under "},{"t":"code","v":"/mods/rescan"},{"t":"text","v":" below for the one carve-out (an already-installed generation)."}],"t":"p"},{"spans":[{"t":"strong","v":"Generations."},{"t":"text","v":" A mod's assets live at "},{"t":"code","v":"<mods_dir>/<id>/<gen>/"},{"t":"text","v":", where "},{"t":"code","v":"gen"},{"t":"text","v":" is a sha256 over the canonical manifest bytes plus the sorted "},{"t":"code","v":"(name, sha256)"},{"t":"text","v":" pairs. Because that hash is "},{"t":"strong","v":"in the URL"},{"t":"text","v":", a replacement can never be served under the old URL — "},{"t":"code","v":"Cache-Control: immutable"},{"t":"text","v":" is honest and SRI has something stable to pin. "},{"t":"code","v":"<id>/CURRENT"},{"t":"text","v":" is the atomic commit pointer, and "},{"t":"code","v":"RETAINED_GENERATIONS"},{"t":"text","v":" older generations are kept so a page that started booting against one survives a replacement mid-flight; an old enough generation is eventually swept and then 404s."}],"t":"p"},{"spans":[{"t":"strong","v":"Uninstall"},{"t":"text","v":" is not idempotent at the HTTP level: a retry after a lost response answers "},{"t":"code","v":"404 not_installed"},{"t":"text","v":" even though the first attempt succeeded. Catching a typo was judged worth that ambiguity — treat \"404 after a retry\" as possible success. "},{"t":"code","v":"purge: true"},{"t":"text","v":" additionally clears this broker's "},{"t":"code","v":"/mod-store/<id>"},{"t":"text","v":" and its pin, in a fixed lock order and "},{"t":"strong","v":"data-first, code-last"},{"t":"text","v":" so a crash leaves code without data rather than data without code. It cannot touch what other browsers hold in "},{"t":"code","v":"localStorage"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mods/rescan"},{"t":"text","v":" re-reads "},{"t":"code","v":"mods_dir"},{"t":"text","v":". It is an operator convenience and "},{"t":"strong","v":"not a trust boundary"},{"t":"text","v":": it treats anyone who can write "},{"t":"code","v":"mods_dir"},{"t":"text","v":" as authorized to supply code the broker will serve and the desktop will execute with full authority. Keep that directory as protected as the broker's own configuration. What the scanner does owe is coherence: it refuses symlinks and reparse points, refuses anything whose realpath leaves "},{"t":"code","v":"mods_dir"},{"t":"text","v":", refuses a non-"},{"t":"code","v":"x-"},{"t":"text","v":" directory, and validates every byte it captured under exactly the same rules an install obeys — "},{"t":"strong","v":"including the "},{"t":"code","v":"permissions"},{"t":"strong","v":" lint above"},{"t":"text","v":", so a hand-dropped package cannot dodge it by skipping "},{"t":"code","v":"POST /mods/install"},{"t":"text","v":" entirely. The one exception is a generation this store "},{"t":"strong","v":"already installed"},{"t":"text","v":" (its own "},{"t":"code","v":".gen.json"},{"t":"text","v":" names this directory and lists exactly the files in it): that generation is grandfathered past the lint, so it keeps serving — across a rescan and a restart — even with "},{"t":"code","v":"permissions"},{"t":"text","v":" absent, rather than going dark retroactively the day the check shipped. Replacing or reinstalling it, through either door, requires the declaration like any new package. The destructive sweep refuses to run at all in a directory without the "},{"t":"code","v":".browserland-mods"},{"t":"text","v":" marker file. Unpacking an archive into a live "},{"t":"code","v":"mods_dir"},{"t":"text","v":" is not the recommended path; use the API, or stop the broker first."}],"t":"p"},{"spans":[{"t":"text","v":"10.6 Status vocabulary"}],"t":"sub"},{"spans":[{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":" is where an operator lives with all of this: it lists the union of catalog packages and registered declarations, joined on id, badges each row "},{"t":"code","v":"shipped"},{"t":"text","v":" or "},{"t":"code","v":"installed"},{"t":"text","v":", and carries the "},{"t":"strong","v":"Install a mod…"},{"t":"text","v":" and "},{"t":"strong","v":"Uninstall"},{"t":"text","v":" actions. The union matters — cycle rows, 404s and SRI mismatches never call "},{"t":"code","v":"registerMod"},{"t":"text","v":", so a pane driven off the registration list simply could not show them."}],"t":"p"},{"spans":[{"t":"text","v":"Each row's "},{"t":"code","v":"state"},{"t":"text","v":" is one of the following. The vocabulary is produced by the loader's status model, so treat an unfamiliar value defensively rather than assuming this list is complete:"}],"t":"p"},{"spans":[{"t":"code","v":"active"},{"t":"text","v":" · "},{"t":"code","v":"off"},{"t":"text","v":" · "},{"t":"code","v":"blocked"},{"t":"text","v":" (enabled, but a "},{"t":"code","v":"requires"},{"t":"text","v":" is not active) · "},{"t":"code","v":"cycle"},{"t":"text","v":" · "},{"t":"code","v":"blocked-by-cycle"},{"t":"text","v":" · "},{"t":"code","v":"failed"},{"t":"text","v":" (deps satisfied, "},{"t":"code","v":"init()"},{"t":"text","v":" threw) · "},{"t":"code","v":"fetch-failed"},{"t":"text","v":" (404, transport failure, "},{"t":"strong","v":"or SRI mismatch"},{"t":"text","v":") · "},{"t":"code","v":"timeout"},{"t":"text","v":" · "},{"t":"code","v":"loading"},{"t":"text","v":" · "},{"t":"code","v":"no-register"},{"t":"text","v":" (the script loaded but never called "},{"t":"code","v":"registerMod"},{"t":"text","v":" — the compile-error case, which still fires "},{"t":"code","v":"load"},{"t":"text","v":" on the element) · "},{"t":"code","v":"wrong-id"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"10. publishing an installed mod 10.1 the x- namespace rule **an id is reserved for first-party (shipped) mods iff it does not start with x- ; an installed mod's id must.** the id shape is the same on both sides — _modstore_id_re in app.py and mod_id_re in the loader, both [a-z0-9][a-z0-9-]{0,63} — and x-notes already matched it before the rule existed. that is the point: the rule is a prefix convention layered on top, so no key a shipped mod owns moves, and neither validator has to change. what the one-character prefix buys is that a single lexical test separates all five namespaces a mod id keys, because all five derive from the same string: /mod-store/<id> — the durable per-mod server store; the per-broker mod-policy pin map ( webterm_mod_policy.json , post /mods/policy ); the webterm:mod:<id>: localstorage prefix behind ctx.storage ; webterm:mods:disabled , the per-browser enable-override set; the catalog — ui._mods / mods/<id>/ on one side, <mods_dir>/<id>/ on the other. it is enforced in three places, and a change to the rule has to update all three: the install validator ( 400 reserved_id ), the scanner (a non- x- directory in mods_dir is skipped with a loud log, so a hand-dropped clock/ cannot shadow the shipped clock ), and ci (no shipped id starts with x- , no shipped requires names an x- id). x-<author>-<name> (e.g. x-johnconnornpc-notes ) is a documented convention, not an enforced field. nothing validates the middle segment and there is no vendor key in the manifest. carrying a real scope component in the id would mean widening both validators, the policy sanitiser and the mod-store key filter in lockstep — four places to keep synchronized, for a component nothing would enforce anyway. not covered, plainly: dom ids a mod writes in its own markup. the loader-generated ids are built from the mod id and so are namespaced; a mod that writes id=\"clock-chip\" itself is not. that is authoring convention, exactly as it already is for shipped mods. there is no automatic migration of pre-existing x- state. /mod-store and the pin map already accept any id-shaped key, so a browser can hold /mod-store/x-notes , a pin for x-notes , and webterm:mod:x-notes:* before anything called x-notes was ever installed. installing it adopts all of it. the install response says so — adopts_existing_state: {mod_store, pin} — for the server-side half; localstorage cannot be inspected server-side and is not covered. 10.2 the portable-mod contract an installed mod is loaded as a separate classic <script src> , not as an import() and not spliced into the bundle. classic (not module) is deliberate: a module would impose module scope plus strict mode and would not publish its top-level function declarations. csp 'self' authorises both identically, so this is a source-mobility decision, not a security one. a spliced fragment and a separately-loaded classic script are not equivalent. the real differences: · spliced into the bundle · loaded as <script src> \"use strict\" at the top — a plain string expression — inert — a directive — the whole file is strict declaration instantiation — all of the inline script's declarations exist before any of its statements run, so your top-level function is visible to code earlier in the bundle — your declarations do not exist until the file is fetched and executed execution timing — page-script eval, before loadmods() — after /info , asynchronously, possibly after boot document.currentscript — the one inline <script> (no package stamp) — the injected element, carrying dataset.modpackage a literal </script> — fatal — closes the element, breaks the page — harmless a const / let / class name collision — a compile error for the whole bundle — blank desktop — a compile error for that script only — one dead mod so the contract, if you want source that moves in both directions unchanged: **nothing at top level runs except the registermod({…}) call.** no dom writes, no fetches, no timers, no reads of desktop state — all work in init(ctx) . plain declarations are fine (see rule 5); side effects are not. no top-level \"use strict\" directive. nothing outside your package may depend on your top-level names. a top-level call into your functions from another fragment cannot work: as an installed package, your declarations do not exist when the bundle evaluates. cross-mod calls at runtime are fine, but only once the provider is active — declare it in requires and do not assume every package has executed by the time boot finishes (the load deadline explicitly lets boot proceed with a script still in flight). this is why some shipped mods are not portable as-is: core's app-window store calls opennoteoreditorwindow , which the editor mod declares at top level. no literal </script> . prefix every top-level const / let / function name. in-tree a collision with core kills the page; out of tree it kills your mod. rules 1, 2 and 4 are enforced by the portable-mod lint in tests/test_ui_assets.py , and every shipped mod passes them. rule 3's known in-tree exceptions are pinned there too, as an exact set of seven edges: top-level name (owner) · reached from opennoteoreditorwindow ( editor ) — core 54_js_app_windows_store.js , sticky loadcodemirror ( editor ) — scratchpad applypattern ( pattern ) — theme findhelpwindow , refreshhelpcorpus ( help ) — core 86_js_mod_loader.js togglehelpwindow ( help ) — core 78_js_keybindings.js the set is a drift guard both ways — a stale edge fails as loudly as a new one — so decoupling a mod forces it to shrink. it did: #177 retired agent-docs (see webterm/broker/mods-deprecated/readme.md ), which took two edges with it, editorfile ( editor ) ← agent-docs and openagentdocswindow ( agent-docs ) ← editor . the surviving call site in editor.js is a typeof openagentdocswindow === 'function' guard, which owns no shipped name and so is not an edge. so editor , help and pattern cannot be republished as installable packages unchanged. rule 5 is not enforced — check it yourself. the lint is a floor, not a proof: it reads source text, so it cannot see a call hidden inside registermod 's own argument. 10.3 installing takes effect on the next page load — always post /mods/install , /mods/uninstall and /mods/rescan change what the broker serves immediately — the catalog, the asset routes and the help corpus all swap at once — but they change nothing in an already-open desktop . install and uninstall say so in their own response, with \"applies\": \"next_page_load\" . this is forced, not unimplemented. javascript global lexical bindings cannot be removed. a mod whose top level says const db = … cannot be re-executed in the same page: the second execution dies with syntaxerror: identifier 'db' has already been declared , and a var -based mod instead hits registermod 's duplicate-id throw. _takedown() is a teardown, not an unloader — it reverses init , it does not un-execute a script. so there is no live replace, no live-uninstall teardown, and no discovery of an install another client performed. that is the same contract the per-broker mod pins already ship. what is delivered is what the feature was for: no process restart and no source edit. drop a mod into a live broker, reload the tab, and it is there. related loader behaviour worth knowing: **packages load in parallel; the scripts within one package load in manifest order, one after the next.** across packages, order is irrelevant because the topological sort runs afterwards, and document-ordered async would let one slow file head-of-line-block every other mod. within a package the topo sort says nothing at all — it orders mod declarations, not the files of one mod — so scripts is honoured as an ordered list. a multi-script package must call registermod from its last script : a registration is acted on the moment it arrives, so registering from the first file would let the mod init before its siblings had loaded. stylesheets are injected but not awaited , and are live regardless of enable, pin or init — deliberately the same posture as a shipped mod's css. a teardown cannot remove them. each (id, gen, file) url is fetched at most once per page load , through a shared in-flight promise map, so the post-login retry cannot double-execute a package. mod_script_timeout_ms is a proceed-anyway deadline, not a cancel — nothing can cancel an in-flight <script> . a package that lands later still executes, and _lateregister brings it up if this page requested it. registermod binds a declaration to the package whose script is running ( document.currentscript.dataset.modpackage ) and refuses a declaration whose id is not that package's, with a wrong-id row. this is a correctness convention, not a boundary : while a stamped package script runs synchronously, a declaration for a different id is refused instead of silently colliding with (say) the shipped clock . a package that registers from a promise or timeout has currentscript === null and is simply unattributable, at which point the duplicate-id modconflicterror is the only backstop. ?nomods=1 on the page url makes loadmods() return before it fetches or inits anything. it is the rescue hatch for a mod that bricks the desktop and therefore makes the control panel — where you would uninstall it — unreachable. it is read-only and deliberately not scrubbed from the url the way ?token= is, because replacestate-ing it away would mean a reload silently re-enabling the mod you are there to remove. 10.4 an installed mod's code and styles are public get /mods/<modid>/<gen>/<name> is public , like get / and /vendor/* , and that is forced rather than chosen: a <script src> cannot carry an authorization header, ?token= is structurally banned, and a fetch + blob: workaround would need blob: in script-src . the posture is the existing one — get / is public and already carries every shipped mod's source. so an installed mod's source and its stylesheets are readable without a token — by anyone who knows the id and the generation and the file name. do not put a secret in a mod — not in its code and not in its stylesheet. help.md is a different matter: only .js / .css are servable, so this route cannot hand it out at all. its only surface is /help-corpus.json , which is public for the same bootstrap reason get / is (the help window has to render on the login page) — but since #173 it serves the installed mods' help sections only to a caller holding the token . without one it is the wiki + shipped-mod corpus alone, so installed ids, help text and manifest label/icon are not enumerable. treat that as not published, not as secret storage: it is one token away, and a mod's help is written to be read. the generation is not a second secret. <gen> is a content hash of the package, so anyone holding the exact bytes of a publicly distributed mod recomputes it and can ask this broker for the file: a 200 rather than a 404 confirms that mod is installed. so #173 removed enumeration — the list of installed ids, and the help text with it — not every last bit about a mod someone already knows to ask for. a mod whose bytes were never published stays unguessable in both segments. 10.5 the install api post /mods/install // browser auth token; not lease-gated { \"manifest\": { \"id\": \"x-notes\", \"version\": \"1.0.0\", \"ctxversion\": 1, \"title\": \"notes\", \"description\": \"…\", \"scripts\": [\"notes.js\"], \"styles\": [\"notes.css\"], \"requires\": [\"editor\"], \"tiers\": [\"settings\"], \"permissions\": [\"file\"], \"help\": { \"label\": \"notes\", \"icon\": \"📓\", \"order\": 2100 } }, \"files\": { \"notes.js\": \"…\", \"notes.css\": \"…\", \"help.md\": \"…\" }, \"replace\": false } post /mods/uninstall {\"id\": \"x-notes\", \"purge\": false} post /mods/rescan {} get /mods/installed // operator detail for the control panel all four are browser-realm token-gated, serve_ui -gated (a headless broker registers none of them), and deliberately not lease-gated — this is broker configuration, and the point is being able to administer a broker somebody else is using. the payload is json with text files, deliberately not an archive. no zip parser means no zip-slip, no zip bomb, no symlink entries. mods are text; binary rides data: uris. mod.json is written by the broker from the canonical manifest and is a reserved key in files , so a payload cannot ship a manifest that disagrees with what was validated. differences from a shipped manifest, all enforced: scripts is required and is an ordered non-empty list of .js names, all present in files . entry is not accepted ( unknown_manifest_key ). id must pass the id regex ( bad_mod_id otherwise) and start with x- ( reserved_id otherwise). unknown keys are rejected , not ignored. help.slug is accepted and dropped — an installed section's slug is **forced to the mod id**, which is x- -prefixed, and ci pins that no wiki page stem and no shipped mod's help.slug uses that prefix. the merge still checks for a collision and drops the installed section if one somehow appears, because merging installed help must never be able to blank the help window. defaultenabled is accepted and ignored; the catalog always reports default_enabled: false . install and enable are separate steps, which matches how the shipped default-off mods already behave. this is not containment: an installed package's scripts are fetched and executed on every page load whatever its enabled state — being off only means init() is not called, and its stylesheets are live either way. permissions is checked against the source text, not merely displayed — the install-time capability lint (#193), covered on its own below. filenames must match [a-za-z0-9][a-za-z0-9._-]{0,63} with a .js / .css / .md suffix, must avoid ? , # , % and : (an ntfs alternate data stream: base.css:payload.js passes a naive bare-name test), must not be a windows device stem ( con , prn , aux , nul , com1-9 , lpt1-9 ), and must not collide with another name under casefold() . every file must be valid utf-8, bom-free , newline-terminated , and within ui._max_lines lines — the same rule set the spliced fragments obey, imported from ui rather than retyped. a .css file may not reference an external origin: @import and absolute url(http…) / url(//…) are rejected ( css_external_reference ). the app's csp sets only script-src and frame-ancestors , so a stylesheet would otherwise be a silent egress channel. defence in depth, not a boundary — the mod's own js can fetch() anything. caps, at the time of writing: 32 mods per broker, 32 files per mod, 256 kib per file, 512 kib per mod, 2 mib request body (64 kib for the uninstall and rescan bodies). the constants in modinstall.py are authoritative, and get /mods/installed reports the live values under limits — read them from there rather than from here. every refusal carries a distinct code, because \"your mod was rejected\" with no reason is a support ticket: too_large · bad_json · bad_mod_id · bad_generation · reserved_id · id_in_use (409) · not_installed (404) · bad_file_name · reserved_file_name · too_many_files · file_too_large · total_too_large · bad_encoding · bad_scripts · bad_styles · bad_requires · bad_manifest_field · unknown_manifest_key · css_external_reference · undeclared_capability · too_many_mods (409) · write_failed (500). modinstall.error_status is the authoritative map; a client must tolerate a code it has not seen. the permissions capability lint (#193). unlike tiers (§5), which is a self-reported claim nothing checks, permissions is a closed vocabulary ( modinstall.permissions ), and validate_package scans every .js file the package ships for direct, syntactic use of the capability each name covers: permission · what it covers clipboard — ctx.clipboard egress — fetch( or hostfetch( file — ctx.file remote-admin — one of the broker's admin route strings, named literally — at the time of writing /mods/install , /mods/uninstall , /mods/rescan , /mods/policy , /update/policy (built from app.admin_routes , so a new admin route becomes remote-admin automatically, with no second list to keep in sync) session — ctx.session a capability the source uses but permissions did not declare is refused with undeclared_capability , naming the first offender by file (sorted) then by position within it — e.g. notes.js:12 uses 'ctx.file', which needs permissions: [\"file\"] in the manifest as the response's detail text. declaring more than you use is fine ; only a used-but-undeclared capability is refused. absent and [] are not the same declaration. no permissions key at all means \"written before this lint existed\" — an already-installed generation from before #193 is grandfathered past the check (see /mods/rescan below) and keeps serving. permissions: [] is a positive claim — \"this package's source reaches none of the five\" — and is checked exactly like any other declaration. a new package should always declare truthfully, writing [] when it genuinely uses none of the five; there is no reason for a new manifest to omit the key. a mention inside a comment never counts, for any permission — the scanner blanks // and /* */ comments (and regex literals) before it looks for anything, the same blank_js_literals machinery the portable-mod lint (§10.2) uses. for clipboard / egress / file / session , a mention inside a string literal doesn't count either — those four are matched against source with string bodies blanked too, so \"call ctx.file to persist\" trips nothing. remote-admin is the one exception: its evidence is a literal — a mod naming /mods/policy inside a fetch() call — so that one check alone keeps string bodies intact and blanks only comments and regex literals. a route named in an actual string is a real use; the same text sitting in a // comment still is not. the rule runs on both doors : the same validate_package call underlies post /mods/install and the mods_dir scanner ( post /mods/rescan and every boot), so a package dropped by hand faces exactly the check the wire does — see the grandfathering note under /mods/rescan below for the one carve-out (an already-installed generation). generations. a mod's assets live at <mods_dir>/<id>/<gen>/ , where gen is a sha256 over the canonical manifest bytes plus the sorted (name, sha256) pairs. because that hash is in the url , a replacement can never be served under the old url — cache-control: immutable is honest and sri has something stable to pin. <id>/current is the atomic commit pointer, and retained_generations older generations are kept so a page that started booting against one survives a replacement mid-flight; an old enough generation is eventually swept and then 404s. uninstall is not idempotent at the http level: a retry after a lost response answers 404 not_installed even though the first attempt succeeded. catching a typo was judged worth that ambiguity — treat \"404 after a retry\" as possible success. purge: true additionally clears this broker's /mod-store/<id> and its pin, in a fixed lock order and data-first, code-last so a crash leaves code without data rather than data without code. it cannot touch what other browsers hold in localstorage . post /mods/rescan re-reads mods_dir . it is an operator convenience and not a trust boundary : it treats anyone who can write mods_dir as authorized to supply code the broker will serve and the desktop will execute with full authority. keep that directory as protected as the broker's own configuration. what the scanner does owe is coherence: it refuses symlinks and reparse points, refuses anything whose realpath leaves mods_dir , refuses a non- x- directory, and validates every byte it captured under exactly the same rules an install obeys — including the permissions lint above , so a hand-dropped package cannot dodge it by skipping post /mods/install entirely. the one exception is a generation this store already installed (its own .gen.json names this directory and lists exactly the files in it): that generation is grandfathered past the lint, so it keeps serving — across a rescan and a restart — even with permissions absent, rather than going dark retroactively the day the check shipped. replacing or reinstalling it, through either door, requires the declaration like any new package. the destructive sweep refuses to run at all in a directory without the .browserland-mods marker file. unpacking an archive into a live mods_dir is not the recommended path; use the api, or stop the broker first. 10.6 status vocabulary control panel → mods is where an operator lives with all of this: it lists the union of catalog packages and registered declarations, joined on id, badges each row shipped or installed , and carries the install a mod… and uninstall actions. the union matters — cycle rows, 404s and sri mismatches never call registermod , so a pane driven off the registration list simply could not show them. each row's state is one of the following. the vocabulary is produced by the loader's status model, so treat an unfamiliar value defensively rather than assuming this list is complete: active · off · blocked (enabled, but a requires is not active) · cycle · blocked-by-cycle · failed (deps satisfied, init() threw) · fetch-failed (404, transport failure, or sri mismatch ) · timeout · loading · no-register (the script loaded but never called registermod — the compile-error case, which still fires load on the element) · wrong-id . ---","title":"10. Publishing an installed mod"},{"body":[{"spans":[{"t":"strong","v":"TDZ inside "},{"t":"code","v":"init()"},{"t":"strong","v":" disables the whole mod."},{"t":"text","v":" If a hoisted "},{"t":"code","v":"function"},{"t":"text","v":" is invoked before a "},{"t":"code","v":"const"},{"t":"text","v":"/"},{"t":"code","v":"let"},{"t":"text","v":" it reads has been initialized — typically because the declaration sits further down the same scope — the read throws a "},{"t":"code","v":"ReferenceError"},{"t":"text","v":" (\"Cannot access X before initialization\"), "},{"t":"code","v":"initMod"},{"t":"text","v":" catches it, and "},{"t":"strong","v":"the entire mod is disabled"},{"t":"text","v":", not the one feature. It has happened more than once. Declare mutable state before anything that could call the closures reading it, and prefer function-local declarations over fragment-level ones that a hoisted function reads."}],"t":"p"},{"spans":[{"t":"strong","v":"CI never executes UI JavaScript."},{"t":"text","v":" The Python suite asserts source text and served bytes. A TDZ error, a typo in a callback or a wrong property name is invisible to "},{"t":"code","v":"pytest"},{"t":"text","v":" and only appears in a real browser. Load the page and check the console."}],"t":"p"},{"spans":[{"t":"strong","v":"Register "},{"t":"code","v":"ctx.onUnload"},{"t":"strong","v":" before anything that can throw."},{"t":"text","v":" If "},{"t":"code","v":"init"},{"t":"text","v":" throws after allocating a timer but before registering its teardown, "},{"t":"code","v":"initMod"},{"t":"text","v":"'s rollback runs an "},{"t":"code","v":"unloads"},{"t":"text","v":" list that never learned about it — the mod is disabled and the timer runs forever."}],"t":"p"},{"spans":[{"t":"strong","v":"A fragment edit needs a broker restart."},{"t":"text","v":" See §7. The page is assembled at import."}],"t":"p"},{"spans":[{"t":"code","v":"prefs"},{"t":"strong","v":"/settings values sync."},{"t":"text","v":" A "},{"t":"code","v":"ctx.settings.*"},{"t":"text","v":" key lives in the shared "},{"t":"code","v":"/state"},{"t":"text","v":" blob, so it is not browser-local even if it feels like a local preference. Use "},{"t":"code","v":"ctx.prefs"},{"t":"text","v":" (or "},{"t":"code","v":"ctx.storage"},{"t":"text","v":") for genuinely per-browser state, and "},{"t":"code","v":"isBrowserGlobal"},{"t":"text","v":" only controls which Control Panel tab shows the widget."}],"t":"p"},{"spans":[{"t":"strong","v":"A "},{"t":"code","v":"saveChain"},{"t":"strong","v":" flush from "},{"t":"code","v":"ctx.onUnload"},{"t":"strong","v":" is a no-op."},{"t":"text","v":" The chain drops its pending batch the instant a teardown starts, so a \"flush on the way down\" registered as an unload disposer saves nothing and resolves "},{"t":"code","v":"{ok:false, error:'unloaded'}"},{"t":"text","v":" — by design, because a disable is synchronous and cannot await a network write, and one that landed anyway would clobber the next activation. Put the deliberate final flush somewhere the mod is still alive: a window-close cleanup, or an explicit save action (§8)."}],"t":"p"},{"spans":[{"t":"code","v":"ctx.signal"},{"t":"strong","v":" on a close-time save aborts the save."},{"t":"text","v":" The signal exists to kill work that must not outlive the activation, so threading it through every call reflexively is exactly wrong for the one path that is meant to survive — the deliberate final flush and the writes around it (§8). If it has to land after the teardown starts, it does not take the signal."}],"t":"p"},{"spans":[{"t":"text","v":"**A reducer is re-run, so it must not do anything.** "},{"t":"code","v":"serverStore.update"},{"t":"text","v":" and "},{"t":"code","v":"saveChain.save"},{"t":"text","v":" invoke your "},{"t":"code","v":"fn"},{"t":"text","v":" once per attempt, and a "},{"t":"code","v":"409"},{"t":"text","v":" means another attempt. A side effect in there — a counter, a toast, another "},{"t":"code","v":"save()"},{"t":"text","v":" — runs once per attempt, and it is the one part of the CAS contract nothing can check for you."}],"t":"p"},{"spans":[{"t":"strong","v":"Reflect must be idempotent."},{"t":"text","v":" "},{"t":"code","v":"spec.reflect"},{"t":"text","v":" on a settings pane runs on every "},{"t":"code","v":"/state"},{"t":"text","v":" convergence, not just on open. Rebuilding your DOM there destroys an in-progress edit."}],"t":"p"},{"spans":[{"t":"code","v":"renderMenu"},{"t":"strong","v":" never collapses separators."},{"t":"text","v":" If you contribute menu items conditionally, contribute your separator conditionally too, or you get a stray rule."}],"t":"p"},{"spans":[{"t":"strong","v":"A per-mod enable is per-browser; a pin is per-broker."},{"t":"text","v":" A broker pin outranks the per-browser set, which in turn outranks your "},{"t":"code","v":"defaultEnabled"},{"t":"text","v":", and a pinned mod refuses "},{"t":"code","v":"setModEnabled"},{"t":"text","v":" outright. Your default is a starting point, not a promise about what any given browser is running."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"11. traps that have actually bitten this codebase tdz inside init() disables the whole mod. if a hoisted function is invoked before a const / let it reads has been initialized — typically because the declaration sits further down the same scope — the read throws a referenceerror (\"cannot access x before initialization\"), initmod catches it, and the entire mod is disabled , not the one feature. it has happened more than once. declare mutable state before anything that could call the closures reading it, and prefer function-local declarations over fragment-level ones that a hoisted function reads. ci never executes ui javascript. the python suite asserts source text and served bytes. a tdz error, a typo in a callback or a wrong property name is invisible to pytest and only appears in a real browser. load the page and check the console. register ctx.onunload before anything that can throw. if init throws after allocating a timer but before registering its teardown, initmod 's rollback runs an unloads list that never learned about it — the mod is disabled and the timer runs forever. a fragment edit needs a broker restart. see §7. the page is assembled at import. prefs /settings values sync. a ctx.settings.* key lives in the shared /state blob, so it is not browser-local even if it feels like a local preference. use ctx.prefs (or ctx.storage ) for genuinely per-browser state, and isbrowserglobal only controls which control panel tab shows the widget. a savechain flush from ctx.onunload is a no-op. the chain drops its pending batch the instant a teardown starts, so a \"flush on the way down\" registered as an unload disposer saves nothing and resolves {ok:false, error:'unloaded'} — by design, because a disable is synchronous and cannot await a network write, and one that landed anyway would clobber the next activation. put the deliberate final flush somewhere the mod is still alive: a window-close cleanup, or an explicit save action (§8). ctx.signal on a close-time save aborts the save. the signal exists to kill work that must not outlive the activation, so threading it through every call reflexively is exactly wrong for the one path that is meant to survive — the deliberate final flush and the writes around it (§8). if it has to land after the teardown starts, it does not take the signal. **a reducer is re-run, so it must not do anything.** serverstore.update and savechain.save invoke your fn once per attempt, and a 409 means another attempt. a side effect in there — a counter, a toast, another save() — runs once per attempt, and it is the one part of the cas contract nothing can check for you. reflect must be idempotent. spec.reflect on a settings pane runs on every /state convergence, not just on open. rebuilding your dom there destroys an in-progress edit. rendermenu never collapses separators. if you contribute menu items conditionally, contribute your separator conditionally too, or you get a stray rule. a per-mod enable is per-browser; a pin is per-broker. a broker pin outranks the per-browser set, which in turn outranks your defaultenabled , and a pinned mod refuses setmodenabled outright. your default is a starting point, not a promise about what any given browser is running. ---","title":"11. Traps that have actually bitten this codebase"},{"body":[{"spans":[{"t":"text","v":"[ ] "},{"t":"code","v":"webterm/broker/mods/<id>/mod.json"},{"t":"text","v":" — "},{"t":"code","v":"id"},{"t":"text","v":" matching the directory and"}],"t":"bullet"},{"spans":[{"t":"text","v":"the "},{"t":"code","v":"registerMod"},{"t":"text","v":" call, plus "},{"t":"code","v":"version"},{"t":"text","v":", "},{"t":"code","v":"ctxVersion"},{"t":"text","v":", "},{"t":"code","v":"title"},{"t":"text","v":", "},{"t":"code","v":"description"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] "},{"t":"code","v":"webterm/broker/mods/<id>/<id>.js"},{"t":"text","v":" — one top-level "},{"t":"code","v":"registerMod({…})"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"text","v":"prefixed top-level names, no "},{"t":"code","v":"</script>"},{"t":"text","v":", everything in "},{"t":"code","v":"init(ctx)"},{"t":"text","v":", teardown registered first."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] "},{"t":"code","v":"tiers"},{"t":"text","v":" declared honestly in "},{"t":"code","v":"registerMod"},{"t":"text","v":" (§5)."}],"t":"bullet"},{"spans":[{"t":"text","v":"[ ] "},{"t":"code","v":"defaultEnabled"},{"t":"text","v":" and "},{"t":"code","v":"requires"},{"t":"text","v":" "},{"t":"strong","v":"identical"},{"t":"text","v":" in "},{"t":"code","v":"mod.json"},{"t":"text","v":" and"}],"t":"bullet"},{"spans":[{"t":"code","v":"registerMod"},{"t":"text","v":" — a test asserts both directions."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] Path appended to "},{"t":"code","v":"ui._MODS"},{"t":"text","v":", "},{"t":"strong","v":"after"},{"t":"text","v":" anything it "},{"t":"code","v":"requires"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"[ ] Any "},{"t":"code","v":".css"},{"t":"text","v":" listed in "},{"t":"code","v":"styles"},{"t":"text","v":", UTF-8, no BOM, newline-terminated, within"}],"t":"bullet"},{"spans":[{"t":"code","v":"ui._MAX_LINES"},{"t":"text","v":", with "},{"t":"strong","v":"prefixed"},{"t":"text","v":" selectors (§13)."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] Optional "},{"t":"code","v":"help.md"},{"t":"text","v":" + the "},{"t":"code","v":"help"},{"t":"text","v":" block — then run"}],"t":"bullet"},{"spans":[{"t":"code","v":"python -m webterm.broker.help_corpus"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] "},{"t":"code","v":"python -m pytest tests -q"},{"t":"text","v":" clean, and the page loaded in a real browser"}],"t":"bullet"},{"spans":[{"t":"text","v":"with the console open."}],"t":"p"},{"spans":[{"t":"text","v":"[ ] Restart the broker before you believe anything you see."}],"t":"bullet"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"12. checklist for a new shipped mod [ ] webterm/broker/mods/<id>/mod.json — id matching the directory and the registermod call, plus version , ctxversion , title , description . [ ] webterm/broker/mods/<id>/<id>.js — one top-level registermod({…}) , prefixed top-level names, no </script> , everything in init(ctx) , teardown registered first. [ ] tiers declared honestly in registermod (§5). [ ] defaultenabled and requires identical in mod.json and registermod — a test asserts both directions. [ ] path appended to ui._mods , after anything it requires . [ ] any .css listed in styles , utf-8, no bom, newline-terminated, within ui._max_lines , with prefixed selectors (§13). [ ] optional help.md + the help block — then run python -m webterm.broker.help_corpus . [ ] python -m pytest tests -q clean, and the page loaded in a real browser with the console open. [ ] restart the broker before you believe anything you see. ---","title":"12. Checklist for a new shipped mod"},{"body":[{"spans":[{"t":"strong","v":"Prefix everything you own."},{"t":"text","v":" A mod's stylesheet is concatenated into the same "},{"t":"code","v":"<style>"},{"t":"text","v":" zone as core's, so an unprefixed "},{"t":"code","v":".row"},{"t":"text","v":" or "},{"t":"code","v":".item"},{"t":"text","v":" is a page-wide selector. Use "},{"t":"code","v":"<id>-…"},{"t":"text","v":" or a class on your own root node."}],"t":"p"},{"spans":[{"t":"strong","v":"Contract surface"},{"t":"text","v":" — shared primitives you are expected to reuse rather than restyle:"}],"t":"p"},{"spans":[{"t":"code","v":".set-*"},{"t":"text","v":" — the Control Panel primitives ("},{"t":"code","v":".set-section"},{"t":"text","v":", "},{"t":"code","v":".set-row"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"code","v":".set-check"},{"t":"text","v":", "},{"t":"code","v":".set-title"},{"t":"text","v":", "},{"t":"code","v":".set-hint"},{"t":"text","v":", "},{"t":"code","v":".set-err"},{"t":"text","v":", "},{"t":"code","v":".set-browser-global"},{"t":"text","v":"), which the "},{"t":"code","v":"ctx.settings.*"},{"t":"text","v":" primitives emit for you."}],"t":"p"},{"spans":[{"t":"code","v":".app-toolbar"},{"t":"text","v":" and its children ("},{"t":"code","v":".app-host-btn"},{"t":"text","v":", "},{"t":"code","v":".app-file-name"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"code","v":".app-dirty"},{"t":"text","v":") — the app-window toolbar chrome."}],"t":"p"},{"spans":[{"t":"code","v":".tb-btn"},{"t":"text","v":" — title-bar buttons."}],"t":"bullet"},{"spans":[{"t":"code","v":".app-tab"},{"t":"text","v":", "},{"t":"code","v":".app-tabs"},{"t":"text","v":", "},{"t":"code","v":".app-tab-dot"},{"t":"text","v":" — in-window tab strips."}],"t":"bullet"},{"spans":[{"t":"code","v":".app-dialog-*"},{"t":"text","v":" — the shared dialog primitives behind "},{"t":"code","v":"openDialog"},{"t":"text","v":","}],"t":"bullet"},{"spans":[{"t":"code","v":"openTextPrompt"},{"t":"text","v":", "},{"t":"code","v":"openConfirmDialog"},{"t":"text","v":", "},{"t":"code","v":"openInfoModal"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The Help card primitives, for anything rendered into the Help window."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Private core"},{"t":"text","v":" — do not target, do not depend on: everything in "},{"t":"code","v":"13_css_tiling.css"},{"t":"text","v":" and "},{"t":"code","v":"14_css_dragdrop.css"},{"t":"text","v":" (the tiling strip, gutters, drop zones), and any selector not listed above. It changes without notice."}],"t":"p"},{"spans":[{"t":"strong","v":"Known debt, not a pattern to copy."},{"t":"text","v":" Some mods that were extracted from core still leave their stylesheets behind in it — at the time of writing "},{"t":"code","v":"editor"},{"t":"text","v":", "},{"t":"code","v":"file-manager"},{"t":"text","v":", "},{"t":"code","v":"task-manager"},{"t":"text","v":" and "},{"t":"code","v":"sticky"},{"t":"text","v":" ("},{"t":"code","v":".app-editor*"},{"t":"text","v":", "},{"t":"code","v":".app-fm*"},{"t":"text","v":", "},{"t":"code","v":".tm-*"},{"t":"text","v":", "},{"t":"code","v":".app-note"},{"t":"text","v":" in "},{"t":"code","v":"11_css_apps.css"},{"t":"text","v":"). That is migration debt to unwind, not an example: a new mod ships its own stylesheet through the manifest's "},{"t":"code","v":"styles"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Prefer the theme variables (§8) over hardcoded colours: "},{"t":"code","v":"--bg"},{"t":"text","v":", "},{"t":"code","v":"--bg-2"},{"t":"text","v":", "},{"t":"code","v":"--bg-3"},{"t":"text","v":", "},{"t":"code","v":"--fg"},{"t":"text","v":", "},{"t":"code","v":"--fg-dim"},{"t":"text","v":", "},{"t":"code","v":"--accent-default"},{"t":"text","v":", "},{"t":"code","v":"--sel-bg"},{"t":"text","v":", "},{"t":"code","v":"--ok"},{"t":"text","v":", "},{"t":"code","v":"--warn"},{"t":"text","v":", "},{"t":"code","v":"--danger"},{"t":"text","v":", and "},{"t":"code","v":"var(--accent, var(--accent-default))"},{"t":"text","v":" inside a window."}],"t":"p"}],"search":"13. css: what you own, what you may use, what is private prefix everything you own. a mod's stylesheet is concatenated into the same <style> zone as core's, so an unprefixed .row or .item is a page-wide selector. use <id>-… or a class on your own root node. contract surface — shared primitives you are expected to reuse rather than restyle: .set-* — the control panel primitives ( .set-section , .set-row , .set-check , .set-title , .set-hint , .set-err , .set-browser-global ), which the ctx.settings.* primitives emit for you. .app-toolbar and its children ( .app-host-btn , .app-file-name , .app-dirty ) — the app-window toolbar chrome. .tb-btn — title-bar buttons. .app-tab , .app-tabs , .app-tab-dot — in-window tab strips. .app-dialog-* — the shared dialog primitives behind opendialog , opentextprompt , openconfirmdialog , openinfomodal . the help card primitives, for anything rendered into the help window. private core — do not target, do not depend on: everything in 13_css_tiling.css and 14_css_dragdrop.css (the tiling strip, gutters, drop zones), and any selector not listed above. it changes without notice. known debt, not a pattern to copy. some mods that were extracted from core still leave their stylesheets behind in it — at the time of writing editor , file-manager , task-manager and sticky ( .app-editor* , .app-fm* , .tm-* , .app-note in 11_css_apps.css ). that is migration debt to unwind, not an example: a new mod ships its own stylesheet through the manifest's styles . prefer the theme variables (§8) over hardcoded colours: --bg , --bg-2 , --bg-3 , --fg , --fg-dim , --accent-default , --sel-bg , --ok , --warn , --danger , and var(--accent, var(--accent-default)) inside a window.","title":"13. CSS: what you own, what you may use, what is private"}],"label":"Writing a Mod","order":17,"slug":"writing-a-mod","tier":"dev"},{"cards":[{"body":[{"spans":[{"t":"strong","v":"Browserland"},{"t":"text","v":" (codename "},{"t":"code","v":"webterm"},{"t":"text","v":", still the name of the Python package and its directories) is a self-contained web terminal system: "},{"t":"strong","v":"headless PTY agents"},{"t":"text","v":" + a "},{"t":"strong","v":"WebSocket broker"},{"t":"text","v":", speaking Browserland's own web-terminal producer protocol. The name evokes a place — a little world inside your browser where a whole fleet of terminals and AI coding agents live, are launched, and are driven from a single tab. ("},{"t":"code","v":"webterm"},{"t":"text","v":" remains the Python package/module name.) The protocol is deliberately small: any producer that speaks these JSON frames over a single WebSocket can register with the broker."}],"t":"p"},{"spans":[{"t":"code","v":"┌─────────┐  binary ANSI + JSON   ┌────────┐  /ws?session=<id>  ┌─────────┐\n│  agent   │ ──────────────────▶ │ broker │ ──────────────────▶ │ browser │\n│ (PTY +   │ ◀────────────────── │ :4445  │ ◀────────────────── │ xterm.js│\n│  ConPTY/ │  input/resize/      └────────┘  input/paste/resize └─────────┘\n│  openpty)│  snapshot_please\n└─────────┘"}],"t":"pre"}],"search":"overview browserland (codename webterm , still the name of the python package and its directories) is a self-contained web terminal system: headless pty agents + a websocket broker , speaking browserland's own web-terminal producer protocol. the name evokes a place — a little world inside your browser where a whole fleet of terminals and ai coding agents live, are launched, and are driven from a single tab. ( webterm remains the python package/module name.) the protocol is deliberately small: any producer that speaks these json frames over a single websocket can register with the broker. ┌─────────┐ binary ansi + json ┌────────┐ /ws?session=<id> ┌─────────┐ │ agent │ ──────────────────▶ │ broker │ ──────────────────▶ │ browser │ │ (pty + │ ◀────────────────── │ :4445 │ ◀────────────────── │ xterm.js│ │ conpty/ │ input/resize/ └────────┘ input/paste/resize └─────────┘ │ openpty)│ snapshot_please └─────────┘","title":"Overview"},{"body":[{"spans":[{"t":"code","v":"# broker (default 127.0.0.1:4445)\npython -m webterm.broker\n\n# an agent running cmd.exe, registered with the local broker\npython -m webterm.agent -- cmd.exe\n\n# then open http://127.0.0.1:4445/ and click the session (or \"new terminal\")"}],"t":"pre"},{"spans":[{"t":"text","v":"Linux:"}],"t":"p"},{"spans":[{"t":"code","v":"./launchers/run-broker.sh                  # broker\n./launchers/run-agent.sh -- bash -l        # agent"}],"t":"pre"}],"search":"quick start (this box, windows) # broker (default 127.0.0.1:4445) python -m webterm.broker # an agent running cmd.exe, registered with the local broker python -m webterm.agent -- cmd.exe # then open http://127.0.0.1:4445/ and click the session (or \"new terminal\") linux: ./launchers/run-broker.sh # broker ./launchers/run-agent.sh -- bash -l # agent","title":"Quick start (this box, Windows)"},{"body":[{"spans":[{"t":"text","v":"Path · What"}],"t":"sub"},{"spans":[{"t":"code","v":"webterm/protocol.py"},{"t":"text","v":" — the ONLY place the JSON frame shapes live"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/agent/"},{"t":"text","v":" — headless producer: PTY backend + ring buffer + OSC title sniffer + reconnecting WS client"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/agent/backends/linux_pty.py"},{"t":"text","v":" — "},{"t":"code","v":"pty.openpty"},{"t":"text","v":" + "},{"t":"code","v":"Popen"},{"t":"text","v":" + "},{"t":"code","v":"loop.add_reader"},{"t":"text","v":"; TIOCSCTTY so Ctrl-C works"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/agent/backends/win_conpty.py"},{"t":"text","v":" — Windows PTY via low-level "},{"t":"code","v":"winpty.PTY"},{"t":"text","v":" + reader thread (Proactor loop has no "},{"t":"code","v":"add_reader"},{"t":"text","v":" for pipes). "},{"t":"strong","v":"Backend auto-selection:"},{"t":"text","v":" ConPTY silently drops the "},{"t":"code","v":"0x03"},{"t":"text","v":" → "},{"t":"code","v":"CTRL_C_EVENT"},{"t":"text","v":" translation when the hosting process has no console (verified empirically — exactly the headless cases this agent exists for), so on "},{"t":"code","v":"auto"},{"t":"text","v":" (and forced "},{"t":"code","v":"conpty"},{"t":"text","v":") the agent first acquires a hidden console ("},{"t":"code","v":"AllocConsole"},{"t":"text","v":" + "},{"t":"code","v":"SW_HIDE"},{"t":"text","v":") and re-enables Ctrl-C ("},{"t":"code","v":"SetConsoleCtrlHandler(None, FALSE)"},{"t":"text","v":", undoing "},{"t":"code","v":"CREATE_NEW_PROCESS_GROUP"},{"t":"text","v":"'s disable — #25), and detached agents then run ConPTY; "},{"t":"code","v":"auto"},{"t":"text","v":" falls back to WinPTY only when acquisition or the re-enable fails, and "},{"t":"code","v":"winpty"},{"t":"text","v":" forces the legacy backend without acquiring a console."}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/agent/snapshot/raw.py"},{"t":"text","v":" — tier-1 snapshot: "},{"t":"code","v":"ESC[0m ESC[2J ESC[H"},{"t":"text","v":" + ring replay"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/agent/snapshot/pyte_snap.py"},{"t":"text","v":" — tier-2 (optional, "},{"t":"code","v":"--snapshot-mode pyte"},{"t":"text","v":"): replay ring through pyte, render the settled grid"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm/broker/"},{"t":"text","v":" — Sanic app: desktop UI page, "},{"t":"code","v":"/ws"},{"t":"text","v":" relay, "},{"t":"code","v":"/browserland"},{"t":"text","v":" producer WS, "},{"t":"code","v":"/sessions"},{"t":"text","v":", profiles-only "},{"t":"code","v":"POST /launch"}],"t":"bullet"},{"spans":[{"t":"code","v":"launchers/"},{"t":"text","v":" — venv-bootstrapping run scripts for both OSes"}],"t":"bullet"}],"search":"components path · what webterm/protocol.py — the only place the json frame shapes live webterm/agent/ — headless producer: pty backend + ring buffer + osc title sniffer + reconnecting ws client webterm/agent/backends/linux_pty.py — pty.openpty + popen + loop.add_reader ; tiocsctty so ctrl-c works webterm/agent/backends/win_conpty.py — windows pty via low-level winpty.pty + reader thread (proactor loop has no add_reader for pipes). backend auto-selection: conpty silently drops the 0x03 → ctrl_c_event translation when the hosting process has no console (verified empirically — exactly the headless cases this agent exists for), so on auto (and forced conpty ) the agent first acquires a hidden console ( allocconsole + sw_hide ) and re-enables ctrl-c ( setconsolectrlhandler(none, false) , undoing create_new_process_group 's disable — #25), and detached agents then run conpty; auto falls back to winpty only when acquisition or the re-enable fails, and winpty forces the legacy backend without acquiring a console. webterm/agent/snapshot/raw.py — tier-1 snapshot: esc[0m esc[2j esc[h + ring replay webterm/agent/snapshot/pyte_snap.py — tier-2 (optional, --snapshot-mode pyte ): replay ring through pyte, render the settled grid webterm/broker/ — sanic app: desktop ui page, /ws relay, /browserland producer ws, /sessions , profiles-only post /launch launchers/ — venv-bootstrapping run scripts for both oses","title":"Components"},{"body":[{"spans":[{"t":"code","v":"python -m webterm.agent [opts] [--] command...\n  --broker-url URL     $BROWSERLAND_BROKER_URL > flag > ws://127.0.0.1:4445/browserland\n  --auth-token TOK     $WEB_TERMINAL_TOKEN; appended as ?token= (REQUIRED\n                       for every broker since #142, loopback included)\n  --cols/--rows        initial PTY size (default 80x24)\n  --title T            initial title (default: command basename)\n  --window-id N        pin the session id (default: random 48-bit)\n  --ring-bytes N       snapshot ring cap (default 262144)\n  --snapshot-mode raw|pyte\n  --cwd DIR"}],"t":"pre"},{"spans":[{"t":"text","v":"The agent's exit code is the child's exit code. While the broker is down the PTY keeps running; the client reconnects with exponential backoff (0.5 s → 10 s cap, ×2 per failure, reset on success) and re-hellos with the current title/dims. Missed bytes are not replayed — the browser's attach triggers "},{"t":"code","v":"snapshot_please"},{"t":"text","v":", which heals from the ring."}],"t":"p"},{"spans":[{"t":"code","v":"psutil"},{"t":"strong","v":" is an optional, best-effort dependency"},{"t":"text","v":" ("},{"t":"code","v":"pip install -e \".[procs]\""},{"t":"text","v":"), not declared in core deps. It powers the task-manager process list ("},{"t":"code","v":"enumerate_procs"},{"t":"text","v":"), the foreground-agent badge ("},{"t":"code","v":"foreground_command"},{"t":"text","v":"), and live-cwd tracking ("},{"t":"code","v":"cwd"},{"t":"text","v":") — each degrades to empty/None without it. Destroying a window also prefers the psutil path (identity-checked "},{"t":"code","v":"create_time"},{"t":"text","v":" guard), but the Linux backend's "},{"t":"code","v":"kill_proc_fallback"},{"t":"text","v":" kills the shell's whole POSIX session by SID (the shell is its own session leader via "},{"t":"code","v":"start_new_session"},{"t":"text","v":", disjoint from the agent's session) when psutil is absent, so \"destroy window\" works either way. Windows without psutil still returns "},{"t":"code","v":"psutil_unavailable"},{"t":"text","v":" for destroy (no fallback yet)."}],"t":"p"}],"search":"agent python -m webterm.agent [opts] [--] command... --broker-url url $browserland_broker_url > flag > ws://127.0.0.1:4445/browserland --auth-token tok $web_terminal_token; appended as ?token= (required for every broker since #142, loopback included) --cols/--rows initial pty size (default 80x24) --title t initial title (default: command basename) --window-id n pin the session id (default: random 48-bit) --ring-bytes n snapshot ring cap (default 262144) --snapshot-mode raw|pyte --cwd dir the agent's exit code is the child's exit code. while the broker is down the pty keeps running; the client reconnects with exponential backoff (0.5 s → 10 s cap, ×2 per failure, reset on success) and re-hellos with the current title/dims. missed bytes are not replayed — the browser's attach triggers snapshot_please , which heals from the ring. psutil is an optional, best-effort dependency ( pip install -e \".[procs]\" ), not declared in core deps. it powers the task-manager process list ( enumerate_procs ), the foreground-agent badge ( foreground_command ), and live-cwd tracking ( cwd ) — each degrades to empty/none without it. destroying a window also prefers the psutil path (identity-checked create_time guard), but the linux backend's kill_proc_fallback kills the shell's whole posix session by sid (the shell is its own session leader via start_new_session , disjoint from the agent's session) when psutil is absent, so \"destroy window\" works either way. windows without psutil still returns psutil_unavailable for destroy (no fallback yet).","title":"Agent"},{"body":[{"spans":[{"t":"code","v":"python -m webterm.broker [--host 127.0.0.1] [--port 4445] [--config PATH] [--headless]"}],"t":"pre"},{"spans":[{"t":"code","v":"--headless"},{"t":"text","v":" (or config "},{"t":"code","v":"\"serve_ui\": false"},{"t":"text","v":", default "},{"t":"code","v":"true"},{"t":"text","v":") serves the full JSON/WS API but "},{"t":"strong","v":"not"},{"t":"text","v":" the desktop page or the in-app Help — "},{"t":"code","v":"GET /"},{"t":"text","v":" returns "},{"t":"code","v":"200 {\"ui\": false}"},{"t":"text","v":", "},{"t":"code","v":"/help-corpus.json"},{"t":"text","v":" 404s, and both UI constants ("},{"t":"code","v":"INDEX_HTML"},{"t":"text","v":", "},{"t":"code","v":"HELP_CORPUS"},{"t":"text","v":") are never assembled (#87). "},{"t":"code","v":"--headless"},{"t":"text","v":" overrides the config key (like "},{"t":"code","v":"--host"},{"t":"text","v":"/"},{"t":"code","v":"--port"},{"t":"text","v":"); there is no "},{"t":"code","v":"--no-headless"},{"t":"text","v":". "},{"t":"code","v":"GET /info"},{"t":"text","v":" reports the active mode as "},{"t":"code","v":"serve_ui"},{"t":"text","v":" (the one additive change to a JSON route); existing JSON/WS behavior is otherwise unchanged in either mode."}],"t":"p"},{"spans":[{"t":"text","v":"Config ("},{"t":"code","v":"broker_config.json"},{"t":"text","v":", path overridable via "},{"t":"code","v":"$WEB_TERMINAL_CONFIG"},{"t":"text","v":"; see "},{"t":"code","v":"broker_config.example.json"},{"t":"text","v":"): "},{"t":"code","v":"auth_token"},{"t":"text","v":", plus "},{"t":"code","v":"agent.profiles"},{"t":"text","v":" for "},{"t":"code","v":"/launch"},{"t":"text","v":". "},{"t":"strong","v":"Profiles only"},{"t":"text","v":" — "},{"t":"code","v":"/launch"},{"t":"text","v":" accepts "},{"t":"code","v":"{\"profile\": \"cmd\", \"cols\": 120, \"rows\": 32, \"title\": \"...\"}"},{"t":"text","v":" and never a client-supplied command/cwd/env. Responses: "},{"t":"code","v":"200"},{"t":"text","v":" registered, "},{"t":"code","v":"202"},{"t":"text","v":" spawned but no hello within 10 s, "},{"t":"code","v":"400"},{"t":"text","v":" unknown profile, "},{"t":"code","v":"401"},{"t":"text","v":"/"},{"t":"code","v":"403"},{"t":"text","v":" auth, "},{"t":"code","v":"429"},{"t":"text","v":" too many pending, "},{"t":"code","v":"500"},{"t":"text","v":" agent exited early."}],"t":"p"},{"spans":[{"t":"code","v":"GET /update/check?refresh=1"},{"t":"text","v":" re-asks GitHub through the daily cache; the 30-minute poll never sets it, and only a literal "},{"t":"code","v":"1"},{"t":"text","v":" counts. Because a forced refresh bypasses the TTL it bypasses the quota strategy with it, so the broker floors it at one per 60 s and caps it at six per rolling hour, leaving most of GitHub's 60/hour-per-source-IP for everything else on that machine. An active rate-limit negative cache outranks the button entirely — the reset GitHub named in a header is honoured, not clicked through. Admission is decided and reserved "},{"t":"strong","v":"inside"},{"t":"text","v":" the same lock the fetch holds (checking before the lock would let a queue of requests each pre-authorise themselves and then fetch in turn), and attempts are stamped before the request goes out, since a failed fetch spends the same quota as a successful one. A refresh that finds a newer publish than the one it queued behind "},{"t":"strong","v":"coalesces"},{"t":"text","v":" rather than re-asking, tracked by a publish serial read before the lock. A refused refresh answers "},{"t":"code","v":"200"},{"t":"text","v":" with the cached result plus "},{"t":"code","v":"refreshed: false"},{"t":"text","v":" and "},{"t":"code","v":"refresh_refused: {reason, retry_after_s}"},{"t":"text","v":" — never an error, and never a bare "},{"t":"code","v":"200"},{"t":"text","v":" a client could render as \"just checked\"."}],"t":"p"},{"spans":[{"t":"text","v":"Update apply (#182 Part 2)"}],"t":"sub"},{"spans":[{"t":"code","v":"POST /update/apply"},{"t":"strong","v":" applies the previewed upstream commit"},{"t":"text","v":", then exits with code 75 so the supervisor restarts into the new code. It is "},{"t":"strong","v":"manual-trigger only"},{"t":"text","v":" — no schedule, no timer, no auto-install. Requires all three of "},{"t":"code","v":"update_check_enabled"},{"t":"text","v":", "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":", and "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" to be true. Each resolves independently: a key "},{"t":"strong","v":"present"},{"t":"text","v":" in "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" (either "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":") owns that one gate and locks it against the GUI; an "},{"t":"strong","v":"absent"},{"t":"text","v":" key leaves the gate to a sidecar ("},{"t":"code","v":"webterm_update_policy.json"},{"t":"text","v":") written by "},{"t":"code","v":"POST /update/policy"},{"t":"text","v":" — the desktop's one-click consent when the update mod is turned on grants whichever of the three the config hasn't already claimed, and the Update window's \"Allow this broker to update itself\" row grants apply + restart together afterward. "},{"t":"code","v":"POST /update/policy"},{"t":"text","v":" takes any "},{"t":"strong","v":"subset"},{"t":"text","v":" of the three keys; keys it does not name keep whatever the sidecar already had, so two operators granting different gates never clobber each other. A corrupt sidecar fails every gate it would otherwise decide closed, never through to a default."}],"t":"p"},{"spans":[{"t":"text","v":"The route takes "},{"t":"code","v":"{\"target_sha\": \"<40-hex>\"}"},{"t":"text","v":" — exactly the commit the UI previewed in the last successful check. The server re-proves every precondition fresh:"}],"t":"p"},{"spans":[{"t":"text","v":"The target commit is not some random hash — it is the one the latest check named as ahead of / current-with upstream."}],"t":"bullet"},{"spans":[{"t":"text","v":"The working tree is clean (no staged/unstaged changes; uncommitted work must be committed or stashed)."}],"t":"bullet"},{"spans":[{"t":"text","v":"No local commits would be discarded (if the check said \"3 behind\", applying is safe; if it said \"2 ahead / 3 behind\", applying would discard work)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Dependencies match or are acceptable (a dependency added/removed by the update is reported so the operator can decide whether to install/uninstall first)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Restart is possible (the gate is on, the supervisor exists, the mechanism is available)."}],"t":"bullet"},{"spans":[{"t":"text","v":"The git phase lives in "},{"t":"code","v":"webterm/broker/update.py"},{"t":"text","v":"'s marked mutation section: "},{"t":"code","v":"git fetch <explicit https URL> <ref>"},{"t":"text","v":" (never "},{"t":"code","v":"git pull"},{"t":"text","v":", never a remote name — a fork's "},{"t":"code","v":"origin"},{"t":"text","v":" must be irrelevant), then "},{"t":"code","v":"git merge --ff-only <sha>"},{"t":"text","v":" with hooks disabled. Objects are fetched with a 120-second timeout; the merge with a 60-second timeout. Stderr is captured for reporting; the report names every locked file on Windows, bounded to 2000 bytes."}],"t":"p"},{"spans":[{"t":"text","v":"Failures are reported "},{"t":"strong","v":"accumulating every reason"},{"t":"text","v":" — the UI shows all unmet preconditions in one pass rather than trial-and-error. The response is "},{"t":"code","v":"202"},{"t":"text","v":" with a "},{"t":"code","v":"last_deploy"},{"t":"text","v":" section surfaced in the next "},{"t":"code","v":"/update/check"},{"t":"text","v":" so the UI can report the outcome."}],"t":"p"},{"spans":[{"t":"strong","v":"The broker that was running the old code exits and is replaced."},{"t":"text","v":" A rollback is automatic: if the new broker never comes up (supervisor restarts it, but it exits again before a worker-ready timeout), the supervisor automatically reverts to the recorded prior commit sha (held in "},{"t":"code","v":"deploy.journal"},{"t":"text","v":" beside the checkout). The outcome ("},{"t":"code","v":"rolled-back"},{"t":"text","v":", "},{"t":"code","v":"deploy-failed"},{"t":"text","v":", etc.) rides in the next check response under "},{"t":"code","v":"last_deploy.outcome"},{"t":"text","v":", visible to the operator."}],"t":"p"},{"spans":[{"t":"strong","v":"Agents survive the restart"},{"t":"text","v":" and reconnect to the new worker, but terminals do not re-execute: they keep running the old code they imported. The UI offers a "},{"t":"strong","v":"Fresh terminal"},{"t":"text","v":" button to spawn a new one. Agents (AI coding assistants) also survive, but any in-memory state (context, plan, accumulated tokens) is lost — the restart is opaque to them, so they reconnect to an empty board."}],"t":"p"},{"spans":[{"t":"text","v":"The "},{"t":"code","v":"deploy.journal"},{"t":"text","v":" records every apply attempt (sha before, sha after, outcome, why it failed if it did) outside the checkout, so "},{"t":"code","v":"git gc"},{"t":"text","v":" cannot erase it. Rollback success is proved by a changed "},{"t":"code","v":"bootId"},{"t":"text","v":" (reported in "},{"t":"code","v":"/info"},{"t":"text","v":"); rollback failure is kept as "},{"t":"code","v":"rollback-impossible"},{"t":"text","v":" and requires manual repair."}],"t":"p"},{"spans":[{"t":"text","v":"Broker restart (#183)"}],"t":"sub"},{"spans":[{"t":"code","v":"python -m webterm.broker"},{"t":"strong","v":" is now a supervisor"},{"t":"text","v":" that spawns the actual server as a worker subprocess. The launcher scripts (bash and PowerShell) and the systemd unit all use "},{"t":"code","v":"exec"},{"t":"text","v":" to start the supervisor, making it the main process — there is no wrapper loop. When restarted from the UI, the supervisor stays alive and re-spawns the worker, a broker-generation restart: the server process is replaced with fresh code, but agents reconnect to the new worker and survive. It is not a service restart — the launcher preamble and systemd unit environment are not re-run."}],"t":"p"},{"spans":[{"t":"text","v":"Restart is "},{"t":"strong","v":"opt-in"},{"t":"text","v":", resolved the same per-key way every update gate is: if "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" names "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" (either "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":"), that key owns the gate and only editing the file can move it; if it is absent, the gate is the desktop's to grant, via the Update window's \"Allow this broker to update itself\" row (which grants "},{"t":"code","v":"apply_enabled"},{"t":"text","v":" and "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" together — see the update mod's own docs). A restart from the UI drains gracefully (stops accepting new work, waits for in-flight critical writes to finish or time out, disconnects idle sessions), and the confirm dialog reports session continuity: guaranteed (agents that survive), at_risk (lost), and unknown. A per-boot cooldown ("},{"t":"code","v":"restart_cooldown_seconds"},{"t":"text","v":", default 90, "},{"t":"code","v":"0"},{"t":"text","v":" disables — also config-file only) refuses back-to-back restarts; 90 is deliberately longer than the supervisor's 60-second budget window, so deliberate restarts can never exhaust the crash budget."}],"t":"p"},{"spans":[{"t":"text","v":"The mechanism is a sentinel file and exit code 75: the worker exits 75 (normally reserved for another purpose — this supervisor owns it now) "},{"t":"strong","v":"and"},{"t":"text","v":" writes a nonce-matched intent sentinel. The supervisor relaunches only if the sentinel matches, refuses any exit-75 without a sentinel, and treats an unarmed 75 as a crash. A restart can happen from systemd ("},{"t":"code","v":"Restart=always"},{"t":"text","v":" or "},{"t":"code","v":"on-failure"},{"t":"text","v":", not "},{"t":"code","v":"on-success"},{"t":"text","v":"), a Windows scheduled task ("},{"t":"code","v":"Stop-ScheduledTask"},{"t":"text","v":" kills abruptly — graceful drains do not complete), or "},{"t":"code","v":"python -m webterm.broker"},{"t":"text","v":" by hand (no restart). The supervisor has a restart budget — five relaunches inside 60 seconds with exponential backoff (0.5 s initial, doubling per crash, clamped at 30 s, reset only when a worker stays up 10 s). A crash loop that exhausts the budget exits with a non-zero code; systemd's own "},{"t":"code","v":"StartLimitAction"},{"t":"text","v":" can then step in."}],"t":"p"},{"spans":[{"t":"code","v":"GET /info"},{"t":"text","v":" reports "},{"t":"code","v":"restart.available"},{"t":"text","v":" (can we restart now), "},{"t":"code","v":"restart.mechanism"},{"t":"text","v":" (supervisor / systemd / none), "},{"t":"code","v":"restart.bootId"},{"t":"text","v":" (this process's id — changes on restart, lets a client confirm it happened), and session continuity. A client confirms a restart by watching "},{"t":"code","v":"bootId"},{"t":"text","v":" change."}],"t":"p"},{"spans":[{"t":"text","v":"Which build is running (#22)"}],"t":"sub"},{"spans":[{"t":"text","v":"There is one build id in this project: "},{"t":"code","v":"webterm.build_version()"},{"t":"text","v":" — the package version from "},{"t":"code","v":"webterm/__init__.py"},{"t":"text","v":" (the "},{"t":"strong","v":"single source"},{"t":"text","v":"; "},{"t":"code","v":"pyproject.toml"},{"t":"text","v":" declares "},{"t":"code","v":"dynamic = [\"version\"]"},{"t":"text","v":" and reads that attribute) plus the git short hash when running from a checkout, e.g. "},{"t":"code","v":"0.8.0+ba4b62e"},{"t":"text","v":". It is derived from the package's own directory (never the process cwd, so a wheel installed inside an unrelated repo cannot report that repo's commit), cached per process, and never raises."}],"t":"p"},{"spans":[{"t":"text","v":"It rides the agent hello, "},{"t":"code","v":"GET /info"},{"t":"text","v":" and "},{"t":"code","v":"GET /mcp/info"},{"t":"text","v":" — and it is baked into the served desktop page:"}],"t":"p"},{"spans":[{"t":"code","v":"<meta name=\"webterm-build\" content=\"0.8.0+ba4b62e\">"}],"t":"pre"},{"spans":[{"t":"text","v":"A broker serves the page it assembled at "},{"t":"strong","v":"import"},{"t":"text","v":", so a "},{"t":"code","v":"git pull"},{"t":"text","v":" changes nothing until the process is restarted. That is the gap this closes:"}],"t":"p"},{"spans":[{"t":"code","v":"curl -s http://host:4445/ | grep webterm-build\ngit -C /path/to/checkout rev-parse --short HEAD"}],"t":"pre"},{"spans":[{"t":"text","v":"The stamp's "},{"t":"code","v":"+hash"},{"t":"text","v":" matches → the running process is the pulled code; it does not → a restart is still pending. The stamp describes the "},{"t":"strong","v":"running process"},{"t":"text","v":", not the checkout on disk, exactly because "},{"t":"code","v":"build_version()"},{"t":"text","v":" is cached at import and never recomputed per request."}],"t":"p"},{"spans":[{"t":"text","v":"Read it for what it is: the revision "},{"t":"code","v":"HEAD"},{"t":"text","v":" pointed at when that process first asked. It cannot see uncommitted edits (there is no dirty marker — two different dirty trees would share a hash, which is a worse signal than none), and if a pull lands while a broker is running, modules imported before and after it come from different revisions and no single stamp can say so. A caching reverse proxy in front of "},{"t":"code","v":"GET /"},{"t":"text","v":" will also happily serve you an old stamp."}],"t":"p"},{"spans":[{"t":"text","v":"It is a "},{"t":"code","v":"<meta>"},{"t":"text","v":" in the head and deliberately "},{"t":"strong","v":"not"},{"t":"text","v":" a value inside the page's one inline script: "},{"t":"code","v":"script-src"},{"t":"text","v":" authorizes that script by "},{"t":"code","v":"sha256"},{"t":"text","v":" of its exact bytes (see Auth model), so a per-commit string in there would change the CSP hash on every commit. Script that wants the value reads the tag — "},{"t":"code","v":"document.querySelector('meta[name=\"webterm-build\"]').content"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Auth model"}],"t":"sub"},{"spans":[{"t":"strong","v":"A token is required on every surface, on every interface, always"},{"t":"text","v":" (#142) — no loopback exemption, no opt-out. Resolution (never returns nothing): "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":" → config "},{"t":"code","v":"auth_token"},{"t":"text","v":" → the "},{"t":"code","v":"webterm_token.json"},{"t":"text","v":" sidecar beside the state store → "},{"t":"strong","v":"mint"},{"t":"text","v":" "},{"t":"code","v":"secrets.token_urlsafe(32)"},{"t":"text","v":" into it. Compared with "},{"t":"code","v":"hmac.compare_digest"},{"t":"text","v":". "},{"t":"code","v":"python -m webterm.broker --print-token"},{"t":"text","v":" reports it without minting or starting a server."}],"t":"p"},{"spans":[{"t":"text","v":"Loopback was never a sound exemption: "},{"t":"code","v":"tailscale serve"},{"t":"text","v":" in front of a "},{"t":"code","v":"127.0.0.1"},{"t":"text","v":" bind (the topology Setup-and-Onboarding recommends) makes every tailnet request arrive from loopback, and any page in the user's browser reaches loopback too. Upgrading a tokenless install is a "},{"t":"strong","v":"breaking change"},{"t":"text","v":" — see Upgrading."}],"t":"p"},{"spans":[{"t":"strong","v":"How the token travels (#144)."},{"t":"text","v":" HTTP sends it as "},{"t":"code","v":"Authorization: Bearer"},{"t":"text","v":", never in the query string. A URL credential leaks where a header does not: any script on the page can read the full URL out of "},{"t":"code","v":"performance.getEntriesByType('resource')"},{"t":"text","v":", a DevTools HAR export carries it into a bug report, and a reverse proxy logs it. There is deliberately "},{"t":"strong","v":"no"},{"t":"text","v":" client function that builds a token-bearing HTTP URL, so one cannot be reintroduced by accident — "},{"t":"code","v":"hostFetch(host, path, opts)"},{"t":"text","v":" is the only way to call the API."}],"t":"p"},{"spans":[{"t":"text","v":"Two places still carry "},{"t":"code","v":"?token="},{"t":"text","v":", both unavoidable and both deliberate:"}],"t":"p"},{"spans":[{"t":"strong","v":"WebSocket dials"},{"t":"text","v":" ("},{"t":"code","v":"/ws"},{"t":"text","v":", "},{"t":"code","v":"/control"},{"t":"text","v":", "},{"t":"code","v":"/browserland"},{"t":"text","v":"). The browser WebSocket API"}],"t":"bullet"},{"spans":[{"t":"text","v":"cannot set request headers on the handshake, so "},{"t":"code","v":"hostWsUrl"},{"t":"text","v":" appends the token. Closing this needs a short-lived connect-ticket scheme, not a refactor."}],"t":"p"},{"spans":[{"t":"strong","v":"The "},{"t":"code","v":"?token="},{"t":"strong","v":" deep link"},{"t":"text","v":" ("},{"t":"code","v":"http://host:4445/?token=…"},{"t":"text","v":"). It is how a token reaches a"}],"t":"bullet"},{"spans":[{"t":"text","v":"fresh browser at all; it is adopted into localStorage and scrubbed from the address bar via "},{"t":"code","v":"history.replaceState"},{"t":"text","v":" on load."}],"t":"p"},{"spans":[{"t":"text","v":"So this narrows the exposure from every request to the handshake and the first load — it does not eliminate it. Operationally: an "},{"t":"code","v":"Authorization"},{"t":"text","v":" header makes a cross-origin request non-simple, so remote hosts pay an "},{"t":"code","v":"OPTIONS"},{"t":"text","v":" preflight; the broker answers every one and sets "},{"t":"code","v":"Access-Control-Max-Age: 86400"},{"t":"text","v":", making it one round trip per host per day rather than per request. "},{"t":"code","v":"ACAO: *"},{"t":"text","v":" stays legal because a header set explicitly by "},{"t":"code","v":"fetch()"},{"t":"text","v":" is "},{"t":"strong","v":"not"},{"t":"text","v":" a credentialed request in the CORS sense (that needs "},{"t":"code","v":"credentials: 'include'"},{"t":"text","v":"), which is also why a wrong token still returns a readable 401 instead of an opaque failure — that is what keeps the login overlay working. "},{"t":"strong","v":"If you front the broker with a reverse proxy, it must forward the "},{"t":"code","v":"Authorization"},{"t":"strong","v":" header"},{"t":"text","v":"; a proxy that strips it makes every host look like a wrong password."}],"t":"p"},{"spans":[{"t":"text","v":"The mint is "},{"t":"code","v":"O_CREAT|O_EXCL"},{"t":"text","v":" (0600 on POSIX; Windows inherits the directory ACL), not an atomic replace: two brokers racing on one state dir both see no file, and last-writer-wins would leave the loser running a token that is not the one on disk. The O_EXCL loser re-reads and adopts the winner. An unwritable directory yields an ephemeral token — the broker still starts, but warns on every boot that it changes on restart and "},{"t":"code","v":"--print-token"},{"t":"text","v":" cannot recover it."}],"t":"p"},{"spans":[{"t":"text","v":"Surface · Rule"}],"t":"sub"},{"spans":[{"t":"code","v":"GET /"},{"t":"text","v":", "},{"t":"code","v":"GET /help-corpus.json"},{"t":"text","v":" — "},{"t":"strong","v":"unauthenticated."},{"t":"text","v":" The token is typed into that page and auth is query/header-only with no cookies, so gating the document deadlocks the bootstrap — every reload, bookmark and new tab would 401 forever. Neither carries host- or session-derived data. Headless, "},{"t":"code","v":"GET /"},{"t":"text","v":" is "},{"t":"code","v":"200 {\"ui\": false}"},{"t":"text","v":", so health probes keep working. The corpus is public but "},{"t":"strong","v":"auth-sensitive"},{"t":"text","v":" (#173): with no token it is the wiki + shipped-mod corpus alone, exactly what it was before #163; the sections contributed by "},{"t":"strong","v":"installed"},{"t":"text","v":" mods — their help text, their manifest label/icon, and hence the list of installed ids — are merged in only for a caller holding the token. Same "},{"t":"code","v":"request_token_ok"},{"t":"text","v":" as every gated route, answering with a smaller "},{"t":"code","v":"200"},{"t":"text","v":" instead of a "},{"t":"code","v":"401"},{"t":"text","v":", plus "},{"t":"code","v":"Cache-Control: no-store"},{"t":"text","v":" and "},{"t":"code","v":"Vary: Authorization"},{"t":"text","v":" so no cache can hand one audience the other's body."}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /vendor/*"},{"t":"text","v":", "},{"t":"code","v":"GET /mods/<id>/<gen>/<name>"},{"t":"text","v":" (#163) — "},{"t":"strong","v":"unauthenticated, and forced rather than chosen"},{"t":"text","v":": the browser needs the vendored xterm to render the login page, before any token exists, and a "},{"t":"code","v":"<script src>"},{"t":"text","v":" cannot carry an "},{"t":"code","v":"Authorization"},{"t":"text","v":" header ("},{"t":"code","v":"?token="},{"t":"text","v":" is structurally banned by #144). Both serve from an in-memory allowlist dict, so a client-supplied name can never reach the filesystem. Consequence: an "},{"t":"strong","v":"installed mod's source and styles are publicly readable"},{"t":"text","v":", exactly as "},{"t":"code","v":"GET /"},{"t":"text","v":" already carries every shipped mod's source — but reaching one takes the id "},{"t":"strong","v":"and"},{"t":"text","v":" the generation "},{"t":"strong","v":"and"},{"t":"text","v":" the file name, and #173 stopped the help corpus from handing out that first ingredient. Neither ingredient is a broker secret, though: "},{"t":"code","v":"<gen>"},{"t":"text","v":" is a content hash of the package, so someone holding a "},{"t":"strong","v":"publicly distributed"},{"t":"text","v":" mod's bytes recomputes it and can confirm that mod is installed here ("},{"t":"code","v":"200"},{"t":"text","v":" vs "},{"t":"code","v":"404"},{"t":"text","v":"). #173 stops the corpus "},{"t":"strong","v":"enumerating"},{"t":"text","v":" installed ids; it cannot hide a mod you already know to ask for by name. "},{"t":"code","v":"help.md"},{"t":"text","v":" is not servable here at all."}],"t":"bullet"},{"spans":[{"t":"code","v":"OPTIONS"},{"t":"text","v":" preflights — unauthenticated by design (they carry no credentials). Explicit routes, because route resolution happens before request middleware."}],"t":"bullet"},{"spans":[{"t":"code","v":"WS /browserland"},{"t":"text","v":" (producers) — token required, loopback included. Was the one gate the token never covered — and WebSockets are not CORS-gated, so any website could dial "},{"t":"code","v":"ws://127.0.0.1:4445/browserland"},{"t":"text","v":", re-register a live "},{"t":"code","v":"window_id"},{"t":"text","v":" (kicking the real agent off with 1012) and inject fabricated terminal output. Refusal is a post-upgrade WS close "},{"t":"strong","v":"4401"},{"t":"text","v":" (an HTTP reject would surface as an opaque 1006)."}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /launch"},{"t":"text","v":" — token required — never an open RCE on any bind."}],"t":"bullet"},{"spans":[{"t":"code","v":"WS /ws"},{"t":"text","v":", "},{"t":"code","v":"WS /control"},{"t":"text","v":", "},{"t":"code","v":"GET /sessions"},{"t":"text","v":", "},{"t":"code","v":"GET /profiles"},{"t":"text","v":" — token required ("},{"t":"code","v":"?token="},{"t":"text","v":", "},{"t":"code","v":"?auth="},{"t":"text","v":", or "},{"t":"code","v":"Authorization: Bearer"},{"t":"text","v":"). Always "},{"t":"strong","v":"401 "},{"t":"code","v":"auth_required"},{"t":"text","v":", never 403: the login overlay and the taskbar host chips pop on 401 only."}],"t":"bullet"},{"spans":[{"t":"code","v":"GET"},{"t":"text","v":"/"},{"t":"code","v":"POST /profiles/config"},{"t":"text","v":", "},{"t":"code","v":"GET /profiles/detect"},{"t":"text","v":" (profile editor, #70) — browser token, same as "},{"t":"code","v":"/file/*"},{"t":"text","v":" and "},{"t":"code","v":"/mcp/config"},{"t":"text","v":". Full commands are browser-realm only; "},{"t":"code","v":"/profiles"},{"t":"text","v":" and "},{"t":"code","v":"/mcp/profiles"},{"t":"text","v":" stay names-only."}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /status/fetch"},{"t":"text","v":" (AI-provider status proxy, #112) — browser token, same as "},{"t":"code","v":"/info"},{"t":"text","v":" and "},{"t":"code","v":"/state"},{"t":"text","v":". The broker's "},{"t":"strong","v":"only outbound HTTP"},{"t":"text","v":": an allowlist of AI-provider Atlassian Statuspage hosts, reached by provider "},{"t":"strong","v":"id"},{"t":"text","v":" (the client passes ids, never a URL — unknown ids are dropped, an all-unknown request is "},{"t":"code","v":"400"},{"t":"text","v":"). Each fetch is "},{"t":"strong","v":"https-only, no-redirect, no-proxy, "},{"t":"code","v":"200"},{"t":"strong","v":"-only, 512 KiB-capped, 4 s timeout"},{"t":"text","v":", with a 60 s per-id cache; any failure degrades to an "},{"t":"code","v":"unknown"},{"t":"text","v":" row (never blocks the UI). "},{"t":"strong","v":"Privacy"},{"t":"text","v":": enabling the (default-off) "},{"t":"code","v":"aistatus"},{"t":"text","v":" mod is what turns this on — the broker's egress IP then becomes visible to those status hosts, so it ships disabled until you opt in."}],"t":"bullet"},{"spans":[{"t":"text","v":"CORS (JSON API) — "},{"t":"code","v":"Access-Control-Allow-Origin: *"},{"t":"text","v":" "},{"t":"strong","v":"unconditionally on every response"},{"t":"text","v":", including the 401 — lets the multi-host UI on another broker's origin read this one, and without it a cross-origin login probe surfaces as a fetch TypeError (\"wrong password\" indistinguishable from \"host down\"). Auth is token-in-query/header and never cookies, so "},{"t":"code","v":"*"},{"t":"text","v":" grants no ambient credential; the real gate is the token on every route."}],"t":"bullet"},{"spans":[{"t":"text","v":"Response headers — "},{"t":"code","v":"Referrer-Policy: no-referrer"},{"t":"text","v":" (the desktop URL carries the token, so an outbound link must not leak it in "},{"t":"code","v":"Referer"},{"t":"text","v":"), "},{"t":"code","v":"X-Frame-Options: DENY"},{"t":"text","v":" + CSP "},{"t":"code","v":"frame-ancestors 'none'"},{"t":"text","v":" ("},{"t":"code","v":"GET /"},{"t":"text","v":" is public so login can bootstrap, but an attacker page must not be able to iframe the real UI and clickjack a browser that already holds a token), and CSP "},{"t":"code","v":"script-src"},{"t":"text","v":" — see below."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Third-party code (#143, #146)."},{"t":"text","v":" The page used to pull xterm from jsdelivr and, lazily, CodeMirror from esm.sh — third-party code executing in the same origin that holds "},{"t":"code","v":"prefs._hosts[].token"},{"t":"text","v":" for "},{"t":"strong","v":"every"},{"t":"text","v":" configured host, tokens that gate "},{"t":"code","v":"/launch"},{"t":"text","v":" and host-wide "},{"t":"code","v":"/file/*"},{"t":"text","v":". Both are now vendored, and "},{"t":"strong","v":"no third-party origin executes in the page at all"},{"t":"text","v":":"}],"t":"p"},{"spans":[{"t":"strong","v":"xterm is vendored"},{"t":"text","v":" into "},{"t":"code","v":"webterm/broker/vendor/"},{"t":"text","v":" and served by the broker at "},{"t":"code","v":"/vendor/*"},{"t":"text","v":" (public, like "},{"t":"code","v":"GET /"},{"t":"text","v":" — the browser needs it to render the login page, before any token exists). There is no CDN to compromise, no SRI hash to keep in sync, and the terminal now works "},{"t":"strong","v":"offline"},{"t":"text","v":", which it never did before. The bytes are the exact published npm files, pinned by sha384 in "},{"t":"code","v":"tests/test_vendor_assets.py"},{"t":"text","v":"; an upgrade is a commit that changes both. Served from an allowlist dict, so a client-supplied name can never reach the filesystem."}],"t":"bullet"},{"spans":[{"t":"strong","v":"CodeMirror is vendored"},{"t":"text","v":" into "},{"t":"code","v":"webterm/broker/vendor/codemirror/"},{"t":"text","v":" and served at "},{"t":"code","v":"/vendor/codemirror/*"},{"t":"text","v":" by a second route with a literal segment (so the lookup is still one dict get on a prefixed key — a name can't express a path). The text editor works offline too. See below for why it took its own generator."}],"t":"bullet"},{"spans":[{"t":"code","v":"script-src"},{"t":"text","v":" allows only "},{"t":"code","v":"'self'"},{"t":"text","v":" and a "},{"t":"code","v":"'sha256-…'"},{"t":"text","v":" for the page's single inline "},{"t":"code","v":"<script>"},{"t":"text","v":". The hash is computed at startup from the assembled "},{"t":"code","v":"INDEX_HTML"},{"t":"text","v":" itself ("},{"t":"code","v":"ui.inline_script_hash"},{"t":"text","v":"), so it tracks the bundle automatically and needs no "},{"t":"code","v":"'unsafe-inline'"},{"t":"text","v":" — meaning an "},{"t":"strong","v":"injected"},{"t":"text","v":" inline script cannot execute either. Nothing else is set (no "},{"t":"code","v":"default-src"},{"t":"text","v":"/"},{"t":"code","v":"style-src"},{"t":"text","v":"/"},{"t":"code","v":"connect-src"},{"t":"text","v":"): those would have to enumerate every inline style, the "},{"t":"code","v":"data:"},{"t":"text","v":" favicon, "},{"t":"code","v":"blob:"},{"t":"text","v":" download URLs and every federated host, each a way to break the app for no gain here."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Why CodeMirror needed a generator (#146)."},{"t":"text","v":" xterm was four self-contained files with exact-pinned URLs — download, hash, commit. CodeMirror 6 is ~116 modules across 40 npm packages that must share "},{"t":"strong","v":"one"},{"t":"text","v":" instance each of "},{"t":"code","v":"@codemirror/state"},{"t":"text","v":", "},{"t":"code","v":"view"},{"t":"text","v":" and "},{"t":"code","v":"language"},{"t":"text","v":"; a second concrete build of "},{"t":"code","v":"state"},{"t":"text","v":" throws and drops the editor to a plain "},{"t":"code","v":"<textarea>"},{"t":"text","v":", while a second "},{"t":"code","v":"view"},{"t":"text","v":" or "},{"t":"code","v":"language"},{"t":"text","v":" is "},{"t":"strong","v":"silent"},{"t":"text","v":" — syntax highlighting just stops. That is why "},{"t":"code","v":"CM_VER"},{"t":"text","v":" in "},{"t":"code","v":"mods/editor/codemirror.js"},{"t":"text","v":" uses semver "},{"t":"strong","v":"ranges"},{"t":"text","v":": esm.sh collapsed every range onto one build at request time. Mutable URLs meant nothing could be hashed, which is why esm.sh stayed in "},{"t":"code","v":"script-src"},{"t":"text","v":" after #143."}],"t":"p"},{"spans":[{"t":"code","v":"python -m webterm.broker.vendor_codemirror"},{"t":"text","v":" moves that guarantee from request time to "},{"t":"strong","v":"generate time"},{"t":"text","v":". It parses "},{"t":"code","v":"CM_VER"},{"t":"text","v":" (still the single source of truth for what the editor loads), crawls the graph from esm.sh keying every module by its final resolved URL, rewrites each import specifier to a relative sibling path, and "},{"t":"strong","v":"refuses to write anything if any package appears twice"},{"t":"text","v":". Run it by hand on upgrade; "},{"t":"code","v":"--check"},{"t":"text","v":" re-verifies the committed tree offline. Two details worth knowing:"}],"t":"p"},{"spans":[{"t":"text","v":"It only rewrites the module "},{"t":"strong","v":"prologue"},{"t":"text","v":", not the whole file. "},{"t":"code","v":"@codemirror/lang-javascript"},{"t":"text","v":" ships completion snippets whose payload is literal JavaScript source ("},{"t":"code","v":"p('import {${names}} from \"${module}\"…')"},{"t":"text","v":") — a file-wide regex matches inside that string and corrupts it. Static imports are top-level-only and esbuild hoists them, so the prologue is where they all are; the generator then asserts nothing package-path-shaped survives past it."}],"t":"bullet"},{"spans":[{"t":"text","v":"The first run found "},{"t":"code","v":"autocomplete"},{"t":"text","v":", "},{"t":"code","v":"lang-html"},{"t":"text","v":" and "},{"t":"code","v":"lang-javascript"},{"t":"text","v":" each loading "},{"t":"strong","v":"twice"},{"t":"text","v":" — our exact pins had drifted behind what the transitive "},{"t":"code","v":"^"},{"t":"text","v":" ranges resolved to, so esm.sh was serving both. Those three became ranges. The assertion that caught it now runs in "},{"t":"code","v":"tests/test_vendor_assets.py"},{"t":"text","v":" against the committed graph."}],"t":"bullet"},{"spans":[{"t":"text","v":"Per-file sha384s live in the generated "},{"t":"code","v":"vendor/codemirror/manifest.json"},{"t":"text","v":" (which is also the served allowlist); only the manifest's own hash is pinned in the test file, so an upgrade is one hand-updated constant. Like the xterm pins, that is "},{"t":"strong","v":"change-control friction, not a security guarantee"},{"t":"text","v":" — it makes a silent change impossible, not a deliberate one."}],"t":"p"},{"spans":[{"t":"strong","v":"Recordings are stored gzipped (#159)."},{"t":"text","v":" A recording is JSONL carrying base64 PTY output — about the most compressible thing the app produces, and an always-on capture rolls a fresh 50 MiB segment forever. New recordings land as "},{"t":"code","v":"<id>.blrec.gz"},{"t":"text","v":" (measured ~8x smaller); "},{"t":"code","v":"<id>.blrec"},{"t":"text","v":" keeps meaning an older uncompressed file and is never rewritten. "},{"t":"strong","v":"The suffix is the encoding"},{"t":"text","v":" — nothing sniffs content, so a corrupt "},{"t":"code","v":".gz"},{"t":"text","v":" reports as a corrupt gzip rather than being retried as JSONL. Each uploaded chunk is compressed as its own gzip member, which keeps appends streaming and commit a plain "},{"t":"code","v":"os.replace"},{"t":"text","v":"; concatenated members are valid gzip and Python/GNU tooling reads them transparently. "},{"t":"code","v":"GET /recording"},{"t":"text","v":" still serves "},{"t":"strong","v":"plain JSONL"},{"t":"text","v":" on the wire, deliberately: HTTP client decoders commonly stop at the first member (httpx does), so passing the stored bytes through under "},{"t":"code","v":"Content-Encoding: gzip"},{"t":"text","v":" would silently truncate multi-chunk recordings. Disk was the growth problem; the wire is unchanged."}],"t":"p"},{"spans":[{"t":"strong","v":"Auditing recordings for secrets (#145)."},{"t":"text","v":" A recording captures the terminal's output byte-for-byte, so anything the terminal echoed is in it — a secret pasted onto a visible command line, an API key printed by a config dump, or this broker's own token if someone ran "},{"t":"code","v":"--print-token"},{"t":"text","v":" while recording. Recordings are durable and downloadable, so:"}],"t":"p"},{"spans":[{"t":"code","v":"python -m webterm.broker --scan-recordings          # 0 = clean, 1 = found, 2 = incomplete\npython -m webterm.broker --scan-recordings --json   # machine-readable\npython -m webterm.broker --scan-recordings --secret <OLD_TOKEN>   # rotated token"}],"t":"pre"},{"spans":[{"t":"text","v":"It resolves the live "},{"t":"code","v":"auth_token"},{"t":"text","v":" and "},{"t":"code","v":"mcp_token"},{"t":"text","v":" "},{"t":"strong","v":"read-only"},{"t":"text","v":" (auditing must never mint one — that would search for a value the broker isn't even running) and streams each file, "},{"t":"strong","v":"base64-decoding "},{"t":"code","v":"d"},{"t":"strong","v":" before searching"},{"t":"text","v":". That decode is the whole point: a plain "},{"t":"code","v":"grep <token> *.blrec"},{"t":"text","v":" finds nothing even when the token is present and returns a confident false all-clear. Output payloads arrive in arbitrary PTY chunks, so the scanner carries a "},{"t":"code","v":"len(secret)-1"},{"t":"text","v":" byte tail across events — a secret split over two (or twenty) events is still found, and one that straddles a resize/gap event is too. It scans "},{"t":"strong","v":"both encodings"},{"t":"text","v":"; a compressed recording it could not open would be a live secret that quietly stopped being findable, so a file that fails to decompress is reported as an error (exit 2, incomplete) and never skipped into a clean result. By hand, the compressed equivalent of the futile "},{"t":"code","v":"grep"},{"t":"text","v":" above is "},{"t":"code","v":"zgrep"},{"t":"text","v":" / "},{"t":"code","v":"gzip -dc … | grep"},{"t":"text","v":" — equally futile, and for the same base64 reason."}],"t":"p"},{"spans":[{"t":"strong","v":"Its limits are printed on every run, including clean ones"},{"t":"text","v":", because a tool like this recreating the false all-clear would be worse than not having it: it finds a secret only where it appears as contiguous bytes. A secret the terminal broke up — interleaved with colour/cursor escapes, redrawn by the shell as you typed, echoed a character at a time — is not found, and a rotated secret is only searched for if passed with "},{"t":"code","v":"--secret"},{"t":"text","v":". It is read-only: recordings are archived artifacts and nothing here rewrites one."}],"t":"p"},{"spans":[{"t":"text","v":"Tokens are passed to spawned agents via "},{"t":"strong","v":"env only"},{"t":"text","v":" (never argv — visible in process lists), and auth failures log only path + client IP (the token rides in query strings — Sanic's access log is pinned off for the same reason). The agent then "},{"t":"strong","v":"removes"},{"t":"text","v":" "},{"t":"code","v":"WEB_TERMINAL_TOKEN"},{"t":"text","v":" before spawning the user's shell, so a terminal cannot "},{"t":"code","v":"echo"},{"t":"text","v":" the credential that gates host-wide file access and shell spawning."}],"t":"p"},{"spans":[{"t":"text","v":"An agent refused with 4401 backs off to the 10 s cap (the rejection arrives post-upgrade, after the backoff has already reset, so the naive loop retries at ~1 Hz forever) and logs one error naming "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":". It keeps retrying rather than giving up: the token can come back without the process restarting."}],"t":"p"},{"spans":[{"t":"strong","v":"Browser login"},{"t":"text","v":": the token doubles as the UI's password. The page is served ungated; a login overlay probes "},{"t":"code","v":"/sessions"},{"t":"text","v":" and stores the token in "},{"t":"strong","v":"localStorage"},{"t":"text","v":" (per browser, per host). "},{"t":"code","v":"?token="},{"t":"text","v":"/"},{"t":"code","v":"?auth="},{"t":"text","v":" URLs still work as deep links — the token is adopted into localStorage and then scrubbed from the URL via "},{"t":"code","v":"history.replaceState"},{"t":"text","v":" (behavior change from the URL-persistence era: copied links no longer carry auth; "},{"t":"code","v":"?session="},{"t":"text","v":" deep links survive the scrub)."}],"t":"p"},{"spans":[{"t":"text","v":"Note: the broker pins "},{"t":"code","v":"app.config.AUTO_EXTEND = False"},{"t":"text","v":". sanic-ext, when merely installed, silently injects its own CORS middleware and an unauthenticated "},{"t":"code","v":"/docs"},{"t":"text","v":" + "},{"t":"code","v":"/openapi.json"},{"t":"text","v":"; CORS here is hand-rolled and token-gated instead."}],"t":"p"},{"spans":[{"t":"text","v":"Multiple hosts"}],"t":"sub"},{"spans":[{"t":"text","v":"Control Panel → Hosts lets the UI attach to sessions on additional brokers (e.g. a WSL box over Tailscale). The "},{"t":"strong","v":"browser connects directly"},{"t":"text","v":" to each host (cross-origin "},{"t":"code","v":"fetch /sessions"},{"t":"text","v":" + "},{"t":"code","v":"ws://host/ws"},{"t":"text","v":"); hosts and their passwords live per-browser in localStorage. Requirements:"}],"t":"p"},{"spans":[{"t":"text","v":"you need the remote broker's token (every broker has one — either"}],"t":"bullet"},{"spans":[{"t":"text","v":"configured or minted; "},{"t":"code","v":"python -m webterm.broker --print-token"},{"t":"text","v":" prints it) and a way in: either a non-loopback bind (plain http), or — recommended — a loopback bind fronted by "},{"t":"code","v":"tailscale serve"},{"t":"text","v":" / an https reverse proxy. Setting "},{"t":"code","v":"auth_token"},{"t":"text","v":" explicitly is worth it across a fleet so every host shares one password instead of a per-machine minted value;"}],"t":"p"},{"spans":[{"t":"strong","v":"both brokers must run this webterm version"},{"t":"text","v":" — a pre-CORS remote reads"}],"t":"bullet"},{"spans":[{"t":"text","v":"as a red \"down\" broker even when it's running;"}],"t":"p"},{"spans":[{"t":"text","v":"one scheme for the whole fleet — an https page fetching an http remote"}],"t":"bullet"},{"spans":[{"t":"text","v":"is blocked as mixed content, so cockpit + hosts must be all-https (preferred: unlocks secure-context features like clipboard image paste) or all-plain-http (see SETUP.md → Multiple machines over Tailscale)."}],"t":"p"},{"spans":[{"t":"text","v":"A single broker's status chip is shown in the taskbar by default, even healthy and local. Since the per-broker actions moved onto the (+) menu's rows it is a pure indicator, so the browser-global "},{"t":"code","v":"hostStatusChip"},{"t":"text","v":" setting can switch it to "},{"t":"code","v":"attention"},{"t":"text","v":" (drawn only while some broker is auth-needed / down / lease — a broker the user HID does not count) or "},{"t":"code","v":"never"},{"t":"text","v":"; hiding is a "},{"t":"code","v":"hide-broker-chip"},{"t":"text","v":" body class, never an inline display, so the node stays put as the ws-pager's anchor and eats no taskbar gap slot. Four states: green ok / red down / amber password-needed / blue lease; an ok/down chip toggles that host's window visibility, while auth and lease chips are click-to-log-in / take-over. With two or more brokers the chips collapse into one worst-state aggregate badge whose click opens the start (+) menu, where each broker has a live status row with the same per-host actions (the hide toggle keeps the menu open). Window prefs are keyed per host; a down host never closes or re-dials another host's windows."}],"t":"p"},{"spans":[{"t":"text","v":"Remote agents against this broker:"}],"t":"p"},{"spans":[{"t":"code","v":"BROWSERLAND_BROKER_URL='ws://broker-host:4445/browserland' \\\nWEB_TERMINAL_TOKEN='...' ./launchers/run-agent.sh -- bash -l"}],"t":"pre"},{"spans":[{"t":"text","v":"Any producer can register the same way: "},{"t":"code","v":"BROWSERLAND_BROKER_URL=ws://host:4445/browserland?token=..."},{"t":"text","v":"."}],"t":"p"}],"search":"broker python -m webterm.broker [--host 127.0.0.1] [--port 4445] [--config path] [--headless] --headless (or config \"serve_ui\": false , default true ) serves the full json/ws api but not the desktop page or the in-app help — get / returns 200 {\"ui\": false} , /help-corpus.json 404s, and both ui constants ( index_html , help_corpus ) are never assembled (#87). --headless overrides the config key (like --host / --port ); there is no --no-headless . get /info reports the active mode as serve_ui (the one additive change to a json route); existing json/ws behavior is otherwise unchanged in either mode. config ( broker_config.json , path overridable via $web_terminal_config ; see broker_config.example.json ): auth_token , plus agent.profiles for /launch . profiles only — /launch accepts {\"profile\": \"cmd\", \"cols\": 120, \"rows\": 32, \"title\": \"...\"} and never a client-supplied command/cwd/env. responses: 200 registered, 202 spawned but no hello within 10 s, 400 unknown profile, 401 / 403 auth, 429 too many pending, 500 agent exited early. get /update/check?refresh=1 re-asks github through the daily cache; the 30-minute poll never sets it, and only a literal 1 counts. because a forced refresh bypasses the ttl it bypasses the quota strategy with it, so the broker floors it at one per 60 s and caps it at six per rolling hour, leaving most of github's 60/hour-per-source-ip for everything else on that machine. an active rate-limit negative cache outranks the button entirely — the reset github named in a header is honoured, not clicked through. admission is decided and reserved inside the same lock the fetch holds (checking before the lock would let a queue of requests each pre-authorise themselves and then fetch in turn), and attempts are stamped before the request goes out, since a failed fetch spends the same quota as a successful one. a refresh that finds a newer publish than the one it queued behind coalesces rather than re-asking, tracked by a publish serial read before the lock. a refused refresh answers 200 with the cached result plus refreshed: false and refresh_refused: {reason, retry_after_s} — never an error, and never a bare 200 a client could render as \"just checked\". update apply (#182 part 2) post /update/apply applies the previewed upstream commit , then exits with code 75 so the supervisor restarts into the new code. it is manual-trigger only — no schedule, no timer, no auto-install. requires all three of update_check_enabled , update_apply_enabled , and restart_enabled to be true. each resolves independently: a key present in broker_config.json (either true or false ) owns that one gate and locks it against the gui; an absent key leaves the gate to a sidecar ( webterm_update_policy.json ) written by post /update/policy — the desktop's one-click consent when the update mod is turned on grants whichever of the three the config hasn't already claimed, and the update window's \"allow this broker to update itself\" row grants apply + restart together afterward. post /update/policy takes any subset of the three keys; keys it does not name keep whatever the sidecar already had, so two operators granting different gates never clobber each other. a corrupt sidecar fails every gate it would otherwise decide closed, never through to a default. the route takes {\"target_sha\": \"<40-hex>\"} — exactly the commit the ui previewed in the last successful check. the server re-proves every precondition fresh: the target commit is not some random hash — it is the one the latest check named as ahead of / current-with upstream. the working tree is clean (no staged/unstaged changes; uncommitted work must be committed or stashed). no local commits would be discarded (if the check said \"3 behind\", applying is safe; if it said \"2 ahead / 3 behind\", applying would discard work). dependencies match or are acceptable (a dependency added/removed by the update is reported so the operator can decide whether to install/uninstall first). restart is possible (the gate is on, the supervisor exists, the mechanism is available). the git phase lives in webterm/broker/update.py 's marked mutation section: git fetch <explicit https url> <ref> (never git pull , never a remote name — a fork's origin must be irrelevant), then git merge --ff-only <sha> with hooks disabled. objects are fetched with a 120-second timeout; the merge with a 60-second timeout. stderr is captured for reporting; the report names every locked file on windows, bounded to 2000 bytes. failures are reported accumulating every reason — the ui shows all unmet preconditions in one pass rather than trial-and-error. the response is 202 with a last_deploy section surfaced in the next /update/check so the ui can report the outcome. the broker that was running the old code exits and is replaced. a rollback is automatic: if the new broker never comes up (supervisor restarts it, but it exits again before a worker-ready timeout), the supervisor automatically reverts to the recorded prior commit sha (held in deploy.journal beside the checkout). the outcome ( rolled-back , deploy-failed , etc.) rides in the next check response under last_deploy.outcome , visible to the operator. agents survive the restart and reconnect to the new worker, but terminals do not re-execute: they keep running the old code they imported. the ui offers a fresh terminal button to spawn a new one. agents (ai coding assistants) also survive, but any in-memory state (context, plan, accumulated tokens) is lost — the restart is opaque to them, so they reconnect to an empty board. the deploy.journal records every apply attempt (sha before, sha after, outcome, why it failed if it did) outside the checkout, so git gc cannot erase it. rollback success is proved by a changed bootid (reported in /info ); rollback failure is kept as rollback-impossible and requires manual repair. broker restart (#183) python -m webterm.broker is now a supervisor that spawns the actual server as a worker subprocess. the launcher scripts (bash and powershell) and the systemd unit all use exec to start the supervisor, making it the main process — there is no wrapper loop. when restarted from the ui, the supervisor stays alive and re-spawns the worker, a broker-generation restart: the server process is replaced with fresh code, but agents reconnect to the new worker and survive. it is not a service restart — the launcher preamble and systemd unit environment are not re-run. restart is opt-in , resolved the same per-key way every update gate is: if broker_config.json names restart_enabled (either true or false ), that key owns the gate and only editing the file can move it; if it is absent, the gate is the desktop's to grant, via the update window's \"allow this broker to update itself\" row (which grants apply_enabled and restart_enabled together — see the update mod's own docs). a restart from the ui drains gracefully (stops accepting new work, waits for in-flight critical writes to finish or time out, disconnects idle sessions), and the confirm dialog reports session continuity: guaranteed (agents that survive), at_risk (lost), and unknown. a per-boot cooldown ( restart_cooldown_seconds , default 90, 0 disables — also config-file only) refuses back-to-back restarts; 90 is deliberately longer than the supervisor's 60-second budget window, so deliberate restarts can never exhaust the crash budget. the mechanism is a sentinel file and exit code 75: the worker exits 75 (normally reserved for another purpose — this supervisor owns it now) and writes a nonce-matched intent sentinel. the supervisor relaunches only if the sentinel matches, refuses any exit-75 without a sentinel, and treats an unarmed 75 as a crash. a restart can happen from systemd ( restart=always or on-failure , not on-success ), a windows scheduled task ( stop-scheduledtask kills abruptly — graceful drains do not complete), or python -m webterm.broker by hand (no restart). the supervisor has a restart budget — five relaunches inside 60 seconds with exponential backoff (0.5 s initial, doubling per crash, clamped at 30 s, reset only when a worker stays up 10 s). a crash loop that exhausts the budget exits with a non-zero code; systemd's own startlimitaction can then step in. get /info reports restart.available (can we restart now), restart.mechanism (supervisor / systemd / none), restart.bootid (this process's id — changes on restart, lets a client confirm it happened), and session continuity. a client confirms a restart by watching bootid change. which build is running (#22) there is one build id in this project: webterm.build_version() — the package version from webterm/__init__.py (the single source ; pyproject.toml declares dynamic = [\"version\"] and reads that attribute) plus the git short hash when running from a checkout, e.g. 0.8.0+ba4b62e . it is derived from the package's own directory (never the process cwd, so a wheel installed inside an unrelated repo cannot report that repo's commit), cached per process, and never raises. it rides the agent hello, get /info and get /mcp/info — and it is baked into the served desktop page: <meta name=\"webterm-build\" content=\"0.8.0+ba4b62e\"> a broker serves the page it assembled at import , so a git pull changes nothing until the process is restarted. that is the gap this closes: curl -s http://host:4445/ | grep webterm-build git -c /path/to/checkout rev-parse --short head the stamp's +hash matches → the running process is the pulled code; it does not → a restart is still pending. the stamp describes the running process , not the checkout on disk, exactly because build_version() is cached at import and never recomputed per request. read it for what it is: the revision head pointed at when that process first asked. it cannot see uncommitted edits (there is no dirty marker — two different dirty trees would share a hash, which is a worse signal than none), and if a pull lands while a broker is running, modules imported before and after it come from different revisions and no single stamp can say so. a caching reverse proxy in front of get / will also happily serve you an old stamp. it is a <meta> in the head and deliberately not a value inside the page's one inline script: script-src authorizes that script by sha256 of its exact bytes (see auth model), so a per-commit string in there would change the csp hash on every commit. script that wants the value reads the tag — document.queryselector('meta[name=\"webterm-build\"]').content . auth model a token is required on every surface, on every interface, always (#142) — no loopback exemption, no opt-out. resolution (never returns nothing): $web_terminal_token → config auth_token → the webterm_token.json sidecar beside the state store → mint secrets.token_urlsafe(32) into it. compared with hmac.compare_digest . python -m webterm.broker --print-token reports it without minting or starting a server. loopback was never a sound exemption: tailscale serve in front of a 127.0.0.1 bind (the topology setup-and-onboarding recommends) makes every tailnet request arrive from loopback, and any page in the user's browser reaches loopback too. upgrading a tokenless install is a breaking change — see upgrading. how the token travels (#144). http sends it as authorization: bearer , never in the query string. a url credential leaks where a header does not: any script on the page can read the full url out of performance.getentriesbytype('resource') , a devtools har export carries it into a bug report, and a reverse proxy logs it. there is deliberately no client function that builds a token-bearing http url, so one cannot be reintroduced by accident — hostfetch(host, path, opts) is the only way to call the api. two places still carry ?token= , both unavoidable and both deliberate: websocket dials ( /ws , /control , /browserland ). the browser websocket api cannot set request headers on the handshake, so hostwsurl appends the token. closing this needs a short-lived connect-ticket scheme, not a refactor. the ?token= deep link ( http://host:4445/?token=… ). it is how a token reaches a fresh browser at all; it is adopted into localstorage and scrubbed from the address bar via history.replacestate on load. so this narrows the exposure from every request to the handshake and the first load — it does not eliminate it. operationally: an authorization header makes a cross-origin request non-simple, so remote hosts pay an options preflight; the broker answers every one and sets access-control-max-age: 86400 , making it one round trip per host per day rather than per request. acao: * stays legal because a header set explicitly by fetch() is not a credentialed request in the cors sense (that needs credentials: 'include' ), which is also why a wrong token still returns a readable 401 instead of an opaque failure — that is what keeps the login overlay working. if you front the broker with a reverse proxy, it must forward the authorization header ; a proxy that strips it makes every host look like a wrong password. the mint is o_creat|o_excl (0600 on posix; windows inherits the directory acl), not an atomic replace: two brokers racing on one state dir both see no file, and last-writer-wins would leave the loser running a token that is not the one on disk. the o_excl loser re-reads and adopts the winner. an unwritable directory yields an ephemeral token — the broker still starts, but warns on every boot that it changes on restart and --print-token cannot recover it. surface · rule get / , get /help-corpus.json — unauthenticated. the token is typed into that page and auth is query/header-only with no cookies, so gating the document deadlocks the bootstrap — every reload, bookmark and new tab would 401 forever. neither carries host- or session-derived data. headless, get / is 200 {\"ui\": false} , so health probes keep working. the corpus is public but auth-sensitive (#173): with no token it is the wiki + shipped-mod corpus alone, exactly what it was before #163; the sections contributed by installed mods — their help text, their manifest label/icon, and hence the list of installed ids — are merged in only for a caller holding the token. same request_token_ok as every gated route, answering with a smaller 200 instead of a 401 , plus cache-control: no-store and vary: authorization so no cache can hand one audience the other's body. get /vendor/* , get /mods/<id>/<gen>/<name> (#163) — unauthenticated, and forced rather than chosen : the browser needs the vendored xterm to render the login page, before any token exists, and a <script src> cannot carry an authorization header ( ?token= is structurally banned by #144). both serve from an in-memory allowlist dict, so a client-supplied name can never reach the filesystem. consequence: an installed mod's source and styles are publicly readable , exactly as get / already carries every shipped mod's source — but reaching one takes the id and the generation and the file name, and #173 stopped the help corpus from handing out that first ingredient. neither ingredient is a broker secret, though: <gen> is a content hash of the package, so someone holding a publicly distributed mod's bytes recomputes it and can confirm that mod is installed here ( 200 vs 404 ). #173 stops the corpus enumerating installed ids; it cannot hide a mod you already know to ask for by name. help.md is not servable here at all. options preflights — unauthenticated by design (they carry no credentials). explicit routes, because route resolution happens before request middleware. ws /browserland (producers) — token required, loopback included. was the one gate the token never covered — and websockets are not cors-gated, so any website could dial ws://127.0.0.1:4445/browserland , re-register a live window_id (kicking the real agent off with 1012) and inject fabricated terminal output. refusal is a post-upgrade ws close 4401 (an http reject would surface as an opaque 1006). post /launch — token required — never an open rce on any bind. ws /ws , ws /control , get /sessions , get /profiles — token required ( ?token= , ?auth= , or authorization: bearer ). always 401 auth_required , never 403: the login overlay and the taskbar host chips pop on 401 only. get / post /profiles/config , get /profiles/detect (profile editor, #70) — browser token, same as /file/* and /mcp/config . full commands are browser-realm only; /profiles and /mcp/profiles stay names-only. get /status/fetch (ai-provider status proxy, #112) — browser token, same as /info and /state . the broker's only outbound http : an allowlist of ai-provider atlassian statuspage hosts, reached by provider id (the client passes ids, never a url — unknown ids are dropped, an all-unknown request is 400 ). each fetch is https-only, no-redirect, no-proxy, 200 -only, 512 kib-capped, 4 s timeout , with a 60 s per-id cache; any failure degrades to an unknown row (never blocks the ui). privacy : enabling the (default-off) aistatus mod is what turns this on — the broker's egress ip then becomes visible to those status hosts, so it ships disabled until you opt in. cors (json api) — access-control-allow-origin: * unconditionally on every response , including the 401 — lets the multi-host ui on another broker's origin read this one, and without it a cross-origin login probe surfaces as a fetch typeerror (\"wrong password\" indistinguishable from \"host down\"). auth is token-in-query/header and never cookies, so * grants no ambient credential; the real gate is the token on every route. response headers — referrer-policy: no-referrer (the desktop url carries the token, so an outbound link must not leak it in referer ), x-frame-options: deny + csp frame-ancestors 'none' ( get / is public so login can bootstrap, but an attacker page must not be able to iframe the real ui and clickjack a browser that already holds a token), and csp script-src — see below. third-party code (#143, #146). the page used to pull xterm from jsdelivr and, lazily, codemirror from esm.sh — third-party code executing in the same origin that holds prefs._hosts[].token for every configured host, tokens that gate /launch and host-wide /file/* . both are now vendored, and no third-party origin executes in the page at all : xterm is vendored into webterm/broker/vendor/ and served by the broker at /vendor/* (public, like get / — the browser needs it to render the login page, before any token exists). there is no cdn to compromise, no sri hash to keep in sync, and the terminal now works offline , which it never did before. the bytes are the exact published npm files, pinned by sha384 in tests/test_vendor_assets.py ; an upgrade is a commit that changes both. served from an allowlist dict, so a client-supplied name can never reach the filesystem. codemirror is vendored into webterm/broker/vendor/codemirror/ and served at /vendor/codemirror/* by a second route with a literal segment (so the lookup is still one dict get on a prefixed key — a name can't express a path). the text editor works offline too. see below for why it took its own generator. script-src allows only 'self' and a 'sha256-…' for the page's single inline <script> . the hash is computed at startup from the assembled index_html itself ( ui.inline_script_hash ), so it tracks the bundle automatically and needs no 'unsafe-inline' — meaning an injected inline script cannot execute either. nothing else is set (no default-src / style-src / connect-src ): those would have to enumerate every inline style, the data: favicon, blob: download urls and every federated host, each a way to break the app for no gain here. why codemirror needed a generator (#146). xterm was four self-contained files with exact-pinned urls — download, hash, commit. codemirror 6 is ~116 modules across 40 npm packages that must share one instance each of @codemirror/state , view and language ; a second concrete build of state throws and drops the editor to a plain <textarea> , while a second view or language is silent — syntax highlighting just stops. that is why cm_ver in mods/editor/codemirror.js uses semver ranges : esm.sh collapsed every range onto one build at request time. mutable urls meant nothing could be hashed, which is why esm.sh stayed in script-src after #143. python -m webterm.broker.vendor_codemirror moves that guarantee from request time to generate time . it parses cm_ver (still the single source of truth for what the editor loads), crawls the graph from esm.sh keying every module by its final resolved url, rewrites each import specifier to a relative sibling path, and refuses to write anything if any package appears twice . run it by hand on upgrade; --check re-verifies the committed tree offline. two details worth knowing: it only rewrites the module prologue , not the whole file. @codemirror/lang-javascript ships completion snippets whose payload is literal javascript source ( p('import {${names}} from \"${module}\"…') ) — a file-wide regex matches inside that string and corrupts it. static imports are top-level-only and esbuild hoists them, so the prologue is where they all are; the generator then asserts nothing package-path-shaped survives past it. the first run found autocomplete , lang-html and lang-javascript each loading twice — our exact pins had drifted behind what the transitive ^ ranges resolved to, so esm.sh was serving both. those three became ranges. the assertion that caught it now runs in tests/test_vendor_assets.py against the committed graph. per-file sha384s live in the generated vendor/codemirror/manifest.json (which is also the served allowlist); only the manifest's own hash is pinned in the test file, so an upgrade is one hand-updated constant. like the xterm pins, that is change-control friction, not a security guarantee — it makes a silent change impossible, not a deliberate one. recordings are stored gzipped (#159). a recording is jsonl carrying base64 pty output — about the most compressible thing the app produces, and an always-on capture rolls a fresh 50 mib segment forever. new recordings land as <id>.blrec.gz (measured ~8x smaller); <id>.blrec keeps meaning an older uncompressed file and is never rewritten. the suffix is the encoding — nothing sniffs content, so a corrupt .gz reports as a corrupt gzip rather than being retried as jsonl. each uploaded chunk is compressed as its own gzip member, which keeps appends streaming and commit a plain os.replace ; concatenated members are valid gzip and python/gnu tooling reads them transparently. get /recording still serves plain jsonl on the wire, deliberately: http client decoders commonly stop at the first member (httpx does), so passing the stored bytes through under content-encoding: gzip would silently truncate multi-chunk recordings. disk was the growth problem; the wire is unchanged. auditing recordings for secrets (#145). a recording captures the terminal's output byte-for-byte, so anything the terminal echoed is in it — a secret pasted onto a visible command line, an api key printed by a config dump, or this broker's own token if someone ran --print-token while recording. recordings are durable and downloadable, so: python -m webterm.broker --scan-recordings # 0 = clean, 1 = found, 2 = incomplete python -m webterm.broker --scan-recordings --json # machine-readable python -m webterm.broker --scan-recordings --secret <old_token> # rotated token it resolves the live auth_token and mcp_token read-only (auditing must never mint one — that would search for a value the broker isn't even running) and streams each file, base64-decoding d before searching . that decode is the whole point: a plain grep <token> *.blrec finds nothing even when the token is present and returns a confident false all-clear. output payloads arrive in arbitrary pty chunks, so the scanner carries a len(secret)-1 byte tail across events — a secret split over two (or twenty) events is still found, and one that straddles a resize/gap event is too. it scans both encodings ; a compressed recording it could not open would be a live secret that quietly stopped being findable, so a file that fails to decompress is reported as an error (exit 2, incomplete) and never skipped into a clean result. by hand, the compressed equivalent of the futile grep above is zgrep / gzip -dc … | grep — equally futile, and for the same base64 reason. its limits are printed on every run, including clean ones , because a tool like this recreating the false all-clear would be worse than not having it: it finds a secret only where it appears as contiguous bytes. a secret the terminal broke up — interleaved with colour/cursor escapes, redrawn by the shell as you typed, echoed a character at a time — is not found, and a rotated secret is only searched for if passed with --secret . it is read-only: recordings are archived artifacts and nothing here rewrites one. tokens are passed to spawned agents via env only (never argv — visible in process lists), and auth failures log only path + client ip (the token rides in query strings — sanic's access log is pinned off for the same reason). the agent then removes web_terminal_token before spawning the user's shell, so a terminal cannot echo the credential that gates host-wide file access and shell spawning. an agent refused with 4401 backs off to the 10 s cap (the rejection arrives post-upgrade, after the backoff has already reset, so the naive loop retries at ~1 hz forever) and logs one error naming $web_terminal_token . it keeps retrying rather than giving up: the token can come back without the process restarting. browser login : the token doubles as the ui's password. the page is served ungated; a login overlay probes /sessions and stores the token in localstorage (per browser, per host). ?token= / ?auth= urls still work as deep links — the token is adopted into localstorage and then scrubbed from the url via history.replacestate (behavior change from the url-persistence era: copied links no longer carry auth; ?session= deep links survive the scrub). note: the broker pins app.config.auto_extend = false . sanic-ext, when merely installed, silently injects its own cors middleware and an unauthenticated /docs + /openapi.json ; cors here is hand-rolled and token-gated instead. multiple hosts control panel → hosts lets the ui attach to sessions on additional brokers (e.g. a wsl box over tailscale). the browser connects directly to each host (cross-origin fetch /sessions + ws://host/ws ); hosts and their passwords live per-browser in localstorage. requirements: you need the remote broker's token (every broker has one — either configured or minted; python -m webterm.broker --print-token prints it) and a way in: either a non-loopback bind (plain http), or — recommended — a loopback bind fronted by tailscale serve / an https reverse proxy. setting auth_token explicitly is worth it across a fleet so every host shares one password instead of a per-machine minted value; both brokers must run this webterm version — a pre-cors remote reads as a red \"down\" broker even when it's running; one scheme for the whole fleet — an https page fetching an http remote is blocked as mixed content, so cockpit + hosts must be all-https (preferred: unlocks secure-context features like clipboard image paste) or all-plain-http (see setup.md → multiple machines over tailscale). a single broker's status chip is shown in the taskbar by default, even healthy and local. since the per-broker actions moved onto the (+) menu's rows it is a pure indicator, so the browser-global hoststatuschip setting can switch it to attention (drawn only while some broker is auth-needed / down / lease — a broker the user hid does not count) or never ; hiding is a hide-broker-chip body class, never an inline display, so the node stays put as the ws-pager's anchor and eats no taskbar gap slot. four states: green ok / red down / amber password-needed / blue lease; an ok/down chip toggles that host's window visibility, while auth and lease chips are click-to-log-in / take-over. with two or more brokers the chips collapse into one worst-state aggregate badge whose click opens the start (+) menu, where each broker has a live status row with the same per-host actions (the hide toggle keeps the menu open). window prefs are keyed per host; a down host never closes or re-dials another host's windows. remote agents against this broker: browserland_broker_url='ws://broker-host:4445/browserland' \\ web_terminal_token='...' ./launchers/run-agent.sh -- bash -l any producer can register the same way: browserland_broker_url=ws://host:4445/browserland?token=... . setup and onboarding upgrading","title":"Broker"},{"body":[{"spans":[{"t":"strong","v":"Install"},{"t":"text","v":" (for running and testing):"}],"t":"p"},{"spans":[{"t":"code","v":"python3 -m venv .venv\n.venv/bin/pip install -e \".[dev,pyte]\""}],"t":"pre"},{"spans":[{"t":"text","v":"Zero-setup alternative: "},{"t":"code","v":"launchers/run-broker.sh"},{"t":"text","v":" / "},{"t":"code","v":"run-agent.sh"},{"t":"text","v":" bootstrap a venv with the runtime deps on first run — fine for running, but they don't install pytest, so use the editable install for testing."}],"t":"p"},{"spans":[{"t":"strong","v":"Config"},{"t":"text","v":": copy "},{"t":"code","v":"broker_config.linux.example.json"},{"t":"text","v":" to "},{"t":"code","v":"broker_config.json"},{"t":"text","v":" (or point "},{"t":"code","v":"$WEB_TERMINAL_CONFIG"},{"t":"text","v":" at it). Default profiles are "},{"t":"code","v":"bash"},{"t":"text","v":" ("},{"t":"code","v":"bash -l"},{"t":"text","v":", the default) and "},{"t":"code","v":"sh"},{"t":"text","v":" (plain "},{"t":"code","v":"sh"},{"t":"text","v":" — the escape hatch when login-shell rc noise is unwanted). A token is required on every bind including loopback; set "},{"t":"code","v":"auth_token"},{"t":"text","v":" (or "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":") to choose the value yourself, or let the broker mint one into "},{"t":"code","v":"webterm_token.json"},{"t":"text","v":" and read it back with "},{"t":"code","v":"--print-token"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"systemd"},{"t":"text","v":": installable units live in "},{"t":"code","v":"launchers/systemd/"},{"t":"text","v":" ("},{"t":"code","v":"webterm-broker.service"},{"t":"text","v":", "},{"t":"code","v":"webterm-agent.service"},{"t":"text","v":"). Copy to "},{"t":"code","v":"/etc/systemd/system/"},{"t":"text","v":", edit "},{"t":"code","v":"User="},{"t":"text","v":", the "},{"t":"code","v":"/opt/web_terminal"},{"t":"text","v":" paths and the token, then "},{"t":"code","v":"systemctl daemon-reload && systemctl enable --now ..."},{"t":"text","v":". Caveats: the "},{"t":"code","v":"bash -l"},{"t":"text","v":" profile sources the service user's login profile (use the "},{"t":"code","v":"sh"},{"t":"text","v":" profile if that produces noise), and "},{"t":"code","v":"/launch"},{"t":"text","v":"-spawned agents are session leaders that survive broker restarts — they re-register within ~10 s of the broker coming back."}],"t":"p"},{"spans":[{"t":"strong","v":"Tests on Linux"},{"t":"text","v":": "},{"t":"code","v":"python -m pytest tests -q"},{"t":"text","v":" → the full suite passes; the Windows-ConPTY e2e suites skip on Linux. This includes a real-POSIX-PTY suite ("},{"t":"code","v":"tests/test_linux_pty.py"},{"t":"text","v":") that was written blind on Windows and verified against a real Linux PTY before deploying."}],"t":"p"}],"search":"linux deployment install (for running and testing): python3 -m venv .venv .venv/bin/pip install -e \".[dev,pyte]\" zero-setup alternative: launchers/run-broker.sh / run-agent.sh bootstrap a venv with the runtime deps on first run — fine for running, but they don't install pytest, so use the editable install for testing. config : copy broker_config.linux.example.json to broker_config.json (or point $web_terminal_config at it). default profiles are bash ( bash -l , the default) and sh (plain sh — the escape hatch when login-shell rc noise is unwanted). a token is required on every bind including loopback; set auth_token (or $web_terminal_token ) to choose the value yourself, or let the broker mint one into webterm_token.json and read it back with --print-token . systemd : installable units live in launchers/systemd/ ( webterm-broker.service , webterm-agent.service ). copy to /etc/systemd/system/ , edit user= , the /opt/web_terminal paths and the token, then systemctl daemon-reload && systemctl enable --now ... . caveats: the bash -l profile sources the service user's login profile (use the sh profile if that produces noise), and /launch -spawned agents are session leaders that survive broker restarts — they re-register within ~10 s of the broker coming back. tests on linux : python -m pytest tests -q → the full suite passes; the windows-conpty e2e suites skip on linux. this includes a real-posix-pty suite ( tests/test_linux_pty.py ) that was written blind on windows and verified against a real linux pty before deploying.","title":"Linux deployment"},{"body":[{"spans":[{"t":"text","v":"Producer → broker: "},{"t":"code","v":"hello"},{"t":"text","v":" (required first frame; same "},{"t":"code","v":"window_id"},{"t":"text","v":" replaces), "},{"t":"code","v":"title"},{"t":"text","v":", "},{"t":"code","v":"resized"},{"t":"text","v":" as JSON text; raw ANSI output "},{"t":"strong","v":"and"},{"t":"text","v":" snapshots as unframed binary. Broker → producer: "},{"t":"code","v":"input"},{"t":"text","v":", "},{"t":"code","v":"resize"},{"t":"text","v":" (reply "},{"t":"code","v":"resized"},{"t":"text","v":"), "},{"t":"code","v":"snapshot_please"},{"t":"text","v":" (reply one binary full redraw). Browser attach: broker sends "},{"t":"code","v":"resized"},{"t":"text","v":" before requesting the snapshot. Snapshots start "},{"t":"code","v":"ESC[0m ESC[2J ESC[H"},{"t":"text","v":" — deliberately no "},{"t":"code","v":"ESC c"},{"t":"text","v":"/"},{"t":"code","v":"ESC[3J"},{"t":"text","v":", since already-attached browsers receive them too — and end with a DEC-mode re-assert postamble ("},{"t":"code","v":"?2004h/l"},{"t":"text","v":" + "},{"t":"code","v":"?1h/l"},{"t":"text","v":" + mouse reporting, per the agent's live sniffer; #138, #154) so a reloaded xterm.js recovers bracketed-paste, application-cursor and mouse-tracking state. Mouse reporting is two mutually-exclusive groups — tracking level ("},{"t":"code","v":"9/1000/1002/1003"},{"t":"text","v":") and extended encoding ("},{"t":"code","v":"1006/1016"},{"t":"text","v":") — and within each the postamble emits the "},{"t":"strong","v":"resets first and the set last"},{"t":"text","v":", because xterm.js maps DECRST of any group member back to the group default, so a trailing reset would undo the set. Without this, a repainting TUI's "},{"t":"code","v":"ESC[2J"},{"t":"text","v":" reliably survives "},{"t":"code","v":"raw.py"},{"t":"text","v":"'s "},{"t":"code","v":"_trim"},{"t":"text","v":" while the mouse DECSETs it emitted at startup do not, and every click in "},{"t":"code","v":"lazygit"},{"t":"text","v":"/"},{"t":"code","v":"btop"},{"t":"text","v":" dies at the first browser reload. The browser→broker "},{"t":"code","v":"paste"},{"t":"text","v":" frame type is a legacy alias for "},{"t":"code","v":"input"},{"t":"text","v":" (#138), kept only for tabs served before the UI switched to xterm's "},{"t":"code","v":"paste()"},{"t":"text","v":". Windows caveat: ConPTY never forwards an app's "},{"t":"code","v":"?2004h"},{"t":"text","v":" request to the terminal side (verified live on Server 2022), so for a detected foreground Claude Code — which parses "},{"t":"code","v":"ESC[200~"},{"t":"text","v":" regardless — the UI hand-brackets pastes when xterm's own bracketed-paste mode is off ("},{"t":"code","v":"pasteTextToTerm"},{"t":"text","v":" in the window lifecycle fragment)."}],"t":"p"},{"spans":[{"t":"text","v":"Known limits: no scrollback replay, no alt-screen modeling in snapshots. Mouse input is forwarded (xterm.js encodes reports onto "},{"t":"code","v":"onData"},{"t":"text","v":" like any other key), but the sniffer inherits "},{"t":"code","v":"altscreen.py"},{"t":"text","v":"'s heuristic limits — 8-bit CSI ("},{"t":"code","v":"0x9b"},{"t":"text","v":") is not matched, and "},{"t":"code","v":"1005"},{"t":"text","v":"/"},{"t":"code","v":"1015"},{"t":"text","v":" encodings are untracked because the vendored xterm.js logs both as unsupported."}],"t":"p"}],"search":"protocol (reference) producer → broker: hello (required first frame; same window_id replaces), title , resized as json text; raw ansi output and snapshots as unframed binary. broker → producer: input , resize (reply resized ), snapshot_please (reply one binary full redraw). browser attach: broker sends resized before requesting the snapshot. snapshots start esc[0m esc[2j esc[h — deliberately no esc c / esc[3j , since already-attached browsers receive them too — and end with a dec-mode re-assert postamble ( ?2004h/l + ?1h/l + mouse reporting, per the agent's live sniffer; #138, #154) so a reloaded xterm.js recovers bracketed-paste, application-cursor and mouse-tracking state. mouse reporting is two mutually-exclusive groups — tracking level ( 9/1000/1002/1003 ) and extended encoding ( 1006/1016 ) — and within each the postamble emits the resets first and the set last , because xterm.js maps decrst of any group member back to the group default, so a trailing reset would undo the set. without this, a repainting tui's esc[2j reliably survives raw.py 's _trim while the mouse decsets it emitted at startup do not, and every click in lazygit / btop dies at the first browser reload. the browser→broker paste frame type is a legacy alias for input (#138), kept only for tabs served before the ui switched to xterm's paste() . windows caveat: conpty never forwards an app's ?2004h request to the terminal side (verified live on server 2022), so for a detected foreground claude code — which parses esc[200~ regardless — the ui hand-brackets pastes when xterm's own bracketed-paste mode is off ( pastetexttoterm in the window lifecycle fragment). known limits: no scrollback replay, no alt-screen modeling in snapshots. mouse input is forwarded (xterm.js encodes reports onto ondata like any other key), but the sniffer inherits altscreen.py 's heuristic limits — 8-bit csi ( 0x9b ) is not matched, and 1005 / 1015 encodings are untracked because the vendored xterm.js logs both as unsupported.","title":"Protocol (reference)"},{"body":[{"spans":[{"t":"text","v":"The broker exposes an "},{"t":"strong","v":"opt-in HTTP API"},{"t":"text","v":" (the "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" surface) that an MCP server wraps as MCP tools so an AI agent can list, observe, drive, and launch terminals. A ready-to-run server ships in ["},{"t":"code","v":"webterm/mcptool/"},{"t":"text","v":"](../webterm/mcptool/) (stdio transport; "},{"t":"code","v":"pip install -e \".[mcp]\""},{"t":"text","v":"). Nine token-gated endpoints make up the contract:"}],"t":"p"},{"spans":[{"t":"text","v":"Method · Path · Purpose"}],"t":"sub"},{"spans":[{"t":"text","v":"GET — "},{"t":"code","v":"/mcp/info"},{"t":"text","v":" — feature flags ("},{"t":"code","v":"allow_launch"},{"t":"text","v":", "},{"t":"code","v":"default_mode"},{"t":"text","v":")"}],"t":"bullet"},{"spans":[{"t":"text","v":"GET — "},{"t":"code","v":"/mcp/terminals"},{"t":"text","v":" — list MCP-visible terminals"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/read"},{"t":"text","v":" — render a terminal's screen as plain text"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" — type into a terminal ("},{"t":"code","v":"readwrite"},{"t":"text","v":" only)"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/reset"},{"t":"text","v":" — clear a terminal's screen-render buffer ("},{"t":"code","v":"readwrite"},{"t":"text","v":" only)"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/flush"},{"t":"text","v":" — discard a terminal's unread queued input ("},{"t":"code","v":"readwrite"},{"t":"text","v":" only)"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/pace"},{"t":"text","v":" — set a terminal's default send_keys pacing ("},{"t":"code","v":"readwrite"},{"t":"text","v":" only)"}],"t":"bullet"},{"spans":[{"t":"text","v":"GET — "},{"t":"code","v":"/mcp/profiles"},{"t":"text","v":" — launchable profile names"}],"t":"bullet"},{"spans":[{"t":"text","v":"POST — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" — spawn a new terminal from a profile"}],"t":"bullet"},{"spans":[{"t":"text","v":"The interface is "},{"t":"strong","v":"disabled by default"},{"t":"text","v":" — nothing under "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" answers until an admin both enables it and sets a token (below). The broker serves the desktop UI and this API, not this document, so editing these docs needs no restart."}],"t":"p"},{"spans":[{"t":"text","v":"Enabling MCP & the token"}],"t":"sub"},{"spans":[{"t":"text","v":"MCP needs "},{"t":"strong","v":"both"},{"t":"text","v":" the master "},{"t":"code","v":"enabled"},{"t":"text","v":" flag "},{"t":"strong","v":"and"},{"t":"text","v":" a token. With either missing every "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" request is "},{"t":"strong","v":"403 "},{"t":"code","v":"{\"error\":\"mcp_disabled\"}"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Get a token any of three ways (precedence "},{"t":"strong","v":"env > sidecar > config seed"},{"t":"text","v":"):"}],"t":"p"},{"spans":[{"t":"text","v":"**Control Panel → MCP access → *Generate*** — mints a "},{"t":"code","v":"token_urlsafe(32)"}],"t":"bullet"},{"spans":[{"t":"text","v":"secret and persists it to the sidecar (below). The normal path."}],"t":"p"},{"spans":[{"t":"text","v":"config "},{"t":"code","v":"mcp_token"},{"t":"text","v":" — a seed value in "},{"t":"code","v":"broker_config.json"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"env "},{"t":"code","v":"WEB_TERMINAL_MCP_TOKEN"},{"t":"text","v":" — "},{"t":"strong","v":"pins"},{"t":"text","v":" the token over both of the above,"}],"t":"bullet"},{"spans":[{"t":"text","v":"is "},{"t":"strong","v":"never written to disk"},{"t":"text","v":", and disables UI token edits (the Control Panel shows "},{"t":"code","v":"token_env_pinned: true"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"text","v":"Pass it on every "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" request as an "},{"t":"code","v":"Authorization: Bearer <token>"},{"t":"text","v":" header or a "},{"t":"code","v":"?token=<token>"},{"t":"text","v":" / "},{"t":"code","v":"?auth=<token>"},{"t":"text","v":" query parameter (compared in constant time). A missing/invalid token while the feature is enabled is "},{"t":"strong","v":"401 "},{"t":"code","v":"{\"error\":\"auth_required\"}"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"This MCP token is a "},{"t":"strong","v":"separate secret from the browser "},{"t":"code","v":"auth_token"},{"t":"text","v":" and gates "},{"t":"strong","v":"only"},{"t":"text","v":" the "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" data plane. Like every other surface it has "},{"t":"strong","v":"no loopback exemption"},{"t":"text","v":" (MCP is opt-in, so even a local caller needs the token). CORS preflights exist for browser callers; a server-side MCP client makes ordinary requests and is not subject to CORS."}],"t":"p"},{"spans":[{"t":"text","v":"Access modes"}],"t":"sub"},{"spans":[{"t":"text","v":"Each terminal has an "},{"t":"strong","v":"effective access mode"},{"t":"text","v":" — its per-window override if set, otherwise the broker-wide "},{"t":"code","v":"default_mode"},{"t":"text","v":":"}],"t":"p"},{"spans":[{"t":"text","v":"Mode · Effect"}],"t":"sub"},{"spans":[{"t":"code","v":"off"},{"t":"text","v":" — hidden from "},{"t":"code","v":"/mcp/terminals"},{"t":"text","v":"; "},{"t":"code","v":"/mcp/read"},{"t":"text","v":" + "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" → "},{"t":"strong","v":"404 "},{"t":"code","v":"unknown_or_off"}],"t":"bullet"},{"spans":[{"t":"code","v":"read"},{"t":"text","v":" — visible + "},{"t":"code","v":"/mcp/read"},{"t":"text","v":"; "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" → "},{"t":"strong","v":"403 "},{"t":"code","v":"read_only"}],"t":"bullet"},{"spans":[{"t":"code","v":"readwrite"},{"t":"text","v":" — read "},{"t":"strong","v":"and"},{"t":"text","v":" "},{"t":"code","v":"/mcp/input"}],"t":"bullet"},{"spans":[{"t":"code","v":"default_mode"},{"t":"text","v":" (global, default "},{"t":"code","v":"off"},{"t":"text","v":") — applies to every window without an"}],"t":"bullet"},{"spans":[{"t":"text","v":"override; change it live in Control Panel → MCP access or "},{"t":"code","v":"POST /mcp/config"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"per-window override"},{"t":"text","v":" — set from the window title-bar "},{"t":"strong","v":"MCP access"},{"t":"text","v":" menu or"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /session/mcp"},{"t":"text","v":"; "},{"t":"strong","v":"in-memory only"},{"t":"text","v":" (resets on broker restart / agent relaunch)."}],"t":"p"},{"spans":[{"t":"code","v":"allow_launch"},{"t":"text","v":" (global, default "},{"t":"code","v":"false"},{"t":"text","v":") — independent flag gating"}],"t":"bullet"},{"spans":[{"t":"code","v":"/mcp/launch"},{"t":"text","v":" only."}],"t":"p"},{"spans":[{"t":"text","v":"Endpoints (MCP-token-gated)"}],"t":"sub"},{"spans":[{"t":"text","v":"All require a valid MCP token; "},{"t":"code","v":"read"},{"t":"text","v":"/"},{"t":"code","v":"input"},{"t":"text","v":" additionally require the target's effective mode to permit them."}],"t":"p"},{"spans":[{"t":"text","v":"Method · Path · Requires · Purpose"}],"t":"sub"},{"spans":[{"t":"code","v":"GET /mcp/info"},{"t":"text","v":" — token — feature flags"}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /mcp/terminals"},{"t":"text","v":" — token — visible terminals"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/read"},{"t":"text","v":" — mode ≥ "},{"t":"code","v":"read"},{"t":"text","v":" — screen as text"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/input"},{"t":"text","v":" — mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" — send keystrokes"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/reset"},{"t":"text","v":" — mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" — clear the screen-render buffer"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/flush"},{"t":"text","v":" — mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" — discard unread queued input"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/pace"},{"t":"text","v":" — mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" — set the default send_keys pacing"}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /mcp/profiles"},{"t":"text","v":" — token — profile names"}],"t":"bullet"},{"spans":[{"t":"code","v":"POST /mcp/launch"},{"t":"text","v":" — "},{"t":"code","v":"allow_launch"},{"t":"text","v":" — spawn a terminal"}],"t":"bullet"},{"spans":[{"t":"code","v":"GET /mcp/info"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"allow_launch\":false,\"default_mode\":\"off\",\"version\":\"0.8.0+ba4b62e\"}"},{"t":"text","v":". "},{"t":"code","v":"version"},{"t":"text","v":" is this broker's build id ("},{"t":"code","v":"webterm.build_version()"},{"t":"text","v":" — package version + git short hash, or the bare package version off a checkout) for stale-deploy detection (#22)."}],"t":"p"},{"spans":[{"t":"code","v":"GET /mcp/terminals"},{"t":"text","v":" → an array; windows whose effective mode is "},{"t":"code","v":"off"},{"t":"text","v":" are omitted. "},{"t":"code","v":"agent"},{"t":"text","v":" is the detected foreground-agent name ("},{"t":"code","v":"\"\""},{"t":"text","v":" when none), "},{"t":"code","v":"kind"},{"t":"text","v":" the producer kind ("},{"t":"code","v":"\"agent\""},{"t":"text","v":" vs a non-agent "},{"t":"code","v":"\"terminal\""},{"t":"text","v":"), "},{"t":"code","v":"mode"},{"t":"text","v":" the effective access mode. "},{"t":"code","v":"version"},{"t":"text","v":" is the producer's reported build id ("},{"t":"code","v":"\"\""},{"t":"text","v":" for a pre-#22 agent / a non-agent producer); for "},{"t":"strong","v":"agent"},{"t":"text","v":" producers a "},{"t":"code","v":"stale"},{"t":"text","v":" boolean flags a build differing from this broker's (a deploy predating a fix — reliable when builds carry a git hash). "},{"t":"code","v":"app_cursor"},{"t":"text","v":" is the cached DECCKM (application-cursor mode) the MCP "},{"t":"code","v":"send_keys"},{"t":"text","v":" reads to pick CSI vs SS3 arrows (#23); "},{"t":"code","v":"pace_ms"},{"t":"text","v":" is the window's default "},{"t":"code","v":"send_keys"},{"t":"text","v":" inter-key pacing (#133, "},{"t":"code","v":"0"},{"t":"text","v":" = single-burst, set via "},{"t":"code","v":"/mcp/pace"},{"t":"text","v":") the MCP server reads so a no-"},{"t":"code","v":"delay_ms"},{"t":"text","v":" send auto-paces. "},{"t":"code","v":"pyte"},{"t":"text","v":" (#134) is whether the agent has pyte installed: "},{"t":"code","v":"false"},{"t":"text","v":" means its "},{"t":"code","v":"read_screen"},{"t":"text","v":" uses the dependency-free textgrid fallback — no "},{"t":"code","v":"attr_runs"},{"t":"text","v":" (#128) and no keyframe repair (#130), so a sparse alt-screen frame after ring eviction comes back flagged "},{"t":"code","v":"partial"},{"t":"text","v":" only. It defaults "},{"t":"code","v":"true"},{"t":"text","v":" for a pre-#134 agent that omits the field:"}],"t":"p"},{"spans":[{"t":"code","v":"[{\"id\":4503603655475937,\"title\":\"bash\",\"host\":\"JC-SERVER\",\"cwd\":\"/home/me\",\n  \"agent\":\"\",\"kind\":\"agent\",\"cols\":80,\"rows\":24,\"mode\":\"read\",\n  \"version\":\"0.8.0+ba4b62e\",\"stale\":false,\"app_cursor\":false,\"pace_ms\":0,\n  \"pyte\":true}]"}],"t":"pre"},{"spans":[{"t":"code","v":"POST /mcp/read"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>}"},{"t":"text","v":" plus optional fields: "},{"t":"code","v":"view: \"scrollback\""},{"t":"text","v":" + "},{"t":"code","v":"lines"},{"t":"text","v":" reads scrollback history instead of the live screen ("},{"t":"code","v":"lines"},{"t":"text","v":" is hard-capped at 1000, #21; the reply echoes "},{"t":"code","v":"view"},{"t":"text","v":" and "},{"t":"code","v":"history_lines"},{"t":"text","v":"); "},{"t":"code","v":"since"},{"t":"text","v":" (a prior "},{"t":"code","v":"content_hash"},{"t":"text","v":") asks for a delta reply — the response always carries a "},{"t":"code","v":"delta"},{"t":"text","v":" boolean, with "},{"t":"code","v":"changed_rows"},{"t":"text","v":" present only for a real delta (#52); "},{"t":"code","v":"attrs: true"},{"t":"text","v":" opts into an "},{"t":"code","v":"attr_runs"},{"t":"text","v":" styled-run map (fg/bg/reverse) so a color-only change is observable (#128)."}],"t":"p"},{"spans":[{"t":"code","v":"{\"ok\":true,\"id\":4503603655475937,\"cols\":80,\"rows\":24,\"text\":\"<screen lines>\\n...\",\"content_hash\":\"…\",\"stable_hash\":\"…\",\"idle_ms\":0}"}],"t":"pre"},{"spans":[{"t":"code","v":"content_hash"},{"t":"text","v":" is a stable 128-bit digest of the rendered text (#26). "},{"t":"code","v":"stable_hash"},{"t":"text","v":" (#135) is the same digest with the HARDWARE-cursor cell masked to a sentinel, so a cursor BLINK in place (the cell toggling between the block glyph and the character under it) leaves it unchanged while a cursor MOVE — which exposes a different underlying cell — still changes it. It is always present (empty string for a degraded read or an older agent) and equals "},{"t":"code","v":"content_hash"},{"t":"text","v":" when there is no cursor."}],"t":"p"},{"spans":[{"t":"code","v":"idle_ms"},{"t":"text","v":" is a best-effort count of ms since the terminal last emitted PTY output (a current agent always reports it — "},{"t":"code","v":"0"},{"t":"text","v":" means output landed just now; an older agent omits it, so it reads as unknown rather than a misleading "},{"t":"code","v":"0"},{"t":"text","v":"). It is unreliable for a perpetually-animating app that paints every frame (its "},{"t":"code","v":"idle_ms"},{"t":"text","v":" never grows), so pacing/flush and a semantic content wait are the real settle signals there, not "},{"t":"code","v":"idle_ms"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"The read can also BLOCK for one bounded round-trip instead of returning immediately, on any ONE of four mutually-exclusive wait signals (combining two → "},{"t":"strong","v":"400 "},{"t":"code","v":"conflicting_wait"},{"t":"text","v":"), all capped by "},{"t":"code","v":"timeout_ms"},{"t":"text","v":" (≤ 15000): "},{"t":"code","v":"wait_for_change"},{"t":"text","v":" (a prior "},{"t":"code","v":"content_hash"},{"t":"text","v":" — wake when the screen differs, #26), "},{"t":"code","v":"wait_for_text"},{"t":"text","v":" / "},{"t":"code","v":"wait_for_regex"},{"t":"text","v":" (+ "},{"t":"code","v":"wait_absent"},{"t":"text","v":" — wake when the grid contains or no longer contains a match, #51), and "},{"t":"code","v":"wait_for_idle"},{"t":"text","v":" (a settle window in ms — wake once "},{"t":"code","v":"stable_hash"},{"t":"text","v":" has held steady for that many ms, i.e. output went quiet, #135). The predicate and idle waits set "},{"t":"code","v":"matched"},{"t":"text","v":" (true when satisfied, false on timeout); the RPC timeout is stretched by "},{"t":"code","v":"timeout_ms"},{"t":"text","v":" so the agent's hold outlives it. "},{"t":"code","v":"wait_for_idle"},{"t":"text","v":" settles only a CALMER TUI — a fully-animating app (Dwarf Fortress animates creatures/water/the "},{"t":"code","v":"*PAUSED*"},{"t":"text","v":" marquee off the cursor every frame) never reaches output-idle, so there use pacing/flush plus a semantic "},{"t":"code","v":"wait_for_text"},{"t":"text","v":" check, not idle."}],"t":"p"},{"spans":[{"t":"text","v":"The agent renders the screen off its event loop. With pyte it returns the full grid; without pyte it falls back to a dependency-free in-house emulator ("},{"t":"code","v":"agent/snapshot/textgrid.py"},{"t":"text","v":") that still produces a "},{"t":"strong","v":"bounded"},{"t":"text","v":" "},{"t":"code","v":"rows"},{"t":"text","v":"×"},{"t":"code","v":"cols"},{"t":"text","v":" grid — so a full-screen TUI reads as a clean grid (box-drawing/braille intact), not an unbounded raw-ANSI dump (#15). Both paths are real grid renders, so "},{"t":"code","v":"\"degraded\": true"},{"t":"text","v":" is now reserved for the rare last-ditch raw decode (it no longer appears for ordinary TUIs). The textgrid fallback has no keyframe repair (that is pyte-only, #130), so a sparse alt-screen frame after ring eviction comes back flagged "},{"t":"code","v":"\"partial\": true"},{"t":"text","v":" (#134) — the grid is a real render, just possibly incomplete; the per-terminal "},{"t":"code","v":"pyte"},{"t":"text","v":" flag on "},{"t":"code","v":"/mcp/terminals"},{"t":"text","v":" tells a caller up front which path an agent is on. Only "},{"t":"strong","v":"agent"},{"t":"text","v":" producers answer; a non-agent terminal producer has no handler, so the request times out → "},{"t":"strong","v":"502 "},{"t":"code","v":"{\"error\":\"no_producer_rpc\"}"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/input"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>, \"data\": \"<str>\"}"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true}"},{"t":"text","v":". Requires effective mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" (else "},{"t":"strong","v":"403 "},{"t":"code","v":"read_only"},{"t":"text","v":"); "},{"t":"code","v":"data"},{"t":"text","v":" must be a string (else "},{"t":"strong","v":"400 "},{"t":"code","v":"bad_data"},{"t":"text","v":") and ≤ "},{"t":"strong","v":"256 KiB"},{"t":"text","v":" UTF-8 (else "},{"t":"strong","v":"413 "},{"t":"code","v":"too_large"},{"t":"text","v":"). Forwarded "},{"t":"strong","v":"verbatim"},{"t":"text","v":" straight to the PTY and "},{"t":"strong","v":"deliberately bypasses the single-active-browser lease"},{"t":"text","v":" — MCP is its own authorized channel. The high-level MCP "},{"t":"code","v":"send_input"},{"t":"text","v":" tool maps newlines in "},{"t":"code","v":"data"},{"t":"text","v":" to a carriage return before calling this endpoint, so a command submits on PowerShell/PSReadLine (which treats a bare "},{"t":"code","v":"\\n"},{"t":"text","v":" as a soft continuation) and on a Unix shell alike (#13); the endpoint itself is byte-exact, so drive it directly for a literal LF or raw-mode bytes."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/reset"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>}"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"id\":<int>}"},{"t":"text","v":". Requires effective mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" (else "},{"t":"strong","v":"403 "},{"t":"code","v":"read_only"},{"t":"text","v":"). A correlated producer round-trip (like "},{"t":"code","v":"/mcp/read"},{"t":"text","v":"): the agent clears its PTY-output ring so the next "},{"t":"code","v":"read_screen"},{"t":"text","v":" renders from a clean slate, then acks. Only "},{"t":"strong","v":"agent"},{"t":"text","v":" producers answer; a non-agent producer times out → "},{"t":"strong","v":"502 "},{"t":"code","v":"no_producer_rpc"},{"t":"text","v":" (and a rare agent-side failure → "},{"t":"strong","v":"502 "},{"t":"code","v":"reset_failed"},{"t":"text","v":"). It touches Browserland's render buffer only — it sends nothing to the running app."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/flush"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>}"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"id\":<int>}"},{"t":"text","v":". Requires effective mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" (else "},{"t":"strong","v":"403 "},{"t":"code","v":"read_only"},{"t":"text","v":"). The "},{"t":"strong","v":"input-side mirror"},{"t":"text","v":" of "},{"t":"code","v":"/mcp/reset"},{"t":"text","v":": where reset clears the OUTPUT ring, this discards keystrokes queued toward the app but not yet consumed (a runaway "},{"t":"code","v":"send_keys"},{"t":"text","v":" backlog a frame-polling TUI hasn't drained), so the next "},{"t":"code","v":"read_screen"},{"t":"text","v":" reflects the settled state. Same correlated round-trip — only "},{"t":"strong","v":"agent"},{"t":"text","v":" producers answer, so a non-agent producer times out → "},{"t":"strong","v":"502 "},{"t":"code","v":"no_producer_rpc"},{"t":"text","v":" (a rare agent-side failure → "},{"t":"strong","v":"502 "},{"t":"code","v":"flush_failed"},{"t":"text","v":"). On a Windows/ConPTY agent it is a best-effort no-op (that backend exposes no input-queue flush primitive) and still acks "},{"t":"code","v":"ok"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/pace"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>, \"pace_ms\": <int>}"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"id\":<int>,\"pace_ms\":<clamped int>}"},{"t":"text","v":". Requires effective mode "},{"t":"code","v":"readwrite"},{"t":"text","v":" (else "},{"t":"strong","v":"403 "},{"t":"code","v":"read_only"},{"t":"text","v":"). Sets the window's "},{"t":"strong","v":"default"},{"t":"text","v":" "},{"t":"code","v":"send_keys"},{"t":"text","v":" inter-key pacing so a subsequent MCP "},{"t":"code","v":"send_keys"},{"t":"text","v":" that passes no "},{"t":"code","v":"delay_ms"},{"t":"text","v":" auto-paces (one key per POST) — for a frame-polling raw-input TUI (Dwarf Fortress) that drops a burst read faster than it renders. "},{"t":"code","v":"pace_ms"},{"t":"text","v":" must be an integer (else "},{"t":"strong","v":"400 "},{"t":"code","v":"bad_pace"},{"t":"text","v":") and is "},{"t":"strong","v":"clamped"},{"t":"text","v":" to "},{"t":"code","v":"[0, 1000]"},{"t":"text","v":" ("},{"t":"code","v":"0"},{"t":"text","v":" disables → single-burst; an over-cap value pins to "},{"t":"code","v":"1000"},{"t":"text","v":"). Unlike "},{"t":"code","v":"/mcp/reset"},{"t":"text","v":"/"},{"t":"code","v":"/mcp/flush"},{"t":"text","v":" this is "},{"t":"strong","v":"broker-local"},{"t":"text","v":" with "},{"t":"strong","v":"no producer round-trip"},{"t":"text","v":" — it just stamps the window's in-memory "},{"t":"code","v":"pace_ms"},{"t":"text","v":", which "},{"t":"code","v":"/mcp/terminals"},{"t":"text","v":" surfaces for the MCP server's client-side pacer. The value is "},{"t":"strong","v":"ephemeral per-connection"},{"t":"text","v":" (resets on agent relaunch, like a per-window mode override)."}],"t":"p"},{"spans":[{"t":"code","v":"GET /mcp/profiles"},{"t":"text","v":" → "},{"t":"code","v":"{\"default\":\"cmd\",\"profiles\":[\"cmd\",\"powershell\"]}"},{"t":"text","v":" (the broker's configured "},{"t":"code","v":"agent.profiles"},{"t":"text","v":"; "},{"t":"code","v":"bash"},{"t":"text","v":"/"},{"t":"code","v":"sh"},{"t":"text","v":" on Linux)."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/launch"},{"t":"text","v":" — requires "},{"t":"code","v":"allow_launch"},{"t":"text","v":" (else "},{"t":"strong","v":"403 "},{"t":"code","v":"launch_disabled"},{"t":"text","v":"). Body reuses the "},{"t":"code","v":"/launch"},{"t":"text","v":" shape: "},{"t":"code","v":"{\"profile\": <str>, \"cols\": 80, \"rows\": 24, \"title\": <str>, \"cwd\": <str>}"},{"t":"text","v":", all optional (dims default 80×24, "},{"t":"code","v":"profile"},{"t":"text","v":" defaults to the broker default; "},{"t":"code","v":"cwd"},{"t":"text","v":" must be an existing dir). Response is the launcher's:"}],"t":"p"},{"spans":[{"t":"code","v":"{\"ok\":true,\"id\":4503603655475937,\"registered\":true,\"agent_pid\":12345}"}],"t":"pre"},{"spans":[{"t":"strong","v":"200"},{"t":"text","v":" when the agent registered within 10 s, "},{"t":"strong","v":"202"},{"t":"text","v":" when it spawned but had not said "},{"t":"code","v":"hello"},{"t":"text","v":" yet ("},{"t":"code","v":"\"registered\":false"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"text","v":"Error reference"}],"t":"sub"},{"spans":[{"t":"text","v":"Status · "},{"t":"code","v":"error"},{"t":"text","v":" · When"}],"t":"sub"},{"spans":[{"t":"text","v":"403 — "},{"t":"code","v":"mcp_disabled"},{"t":"text","v":" — feature disabled or no token configured"}],"t":"bullet"},{"spans":[{"t":"text","v":"401 — "},{"t":"code","v":"auth_required"},{"t":"text","v":" — missing/invalid MCP token"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"bad_json"},{"t":"text","v":" — body is not a JSON object"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"bad_id"},{"t":"text","v":" — "},{"t":"code","v":"id"},{"t":"text","v":" missing or not an integer"}],"t":"bullet"},{"spans":[{"t":"text","v":"404 — "},{"t":"code","v":"unknown_or_off"},{"t":"text","v":" — no such window, or its effective mode is "},{"t":"code","v":"off"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"bad_data"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" "},{"t":"code","v":"data"},{"t":"text","v":" is not a string"}],"t":"bullet"},{"spans":[{"t":"text","v":"403 — "},{"t":"code","v":"read_only"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" on a window not in "},{"t":"code","v":"readwrite"}],"t":"bullet"},{"spans":[{"t":"text","v":"413 — "},{"t":"code","v":"too_large"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/input"},{"t":"text","v":" payload > 256 KiB"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"bad_pace"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/pace"},{"t":"text","v":" "},{"t":"code","v":"pace_ms"},{"t":"text","v":" missing or not an integer"}],"t":"bullet"},{"spans":[{"t":"text","v":"502 — "},{"t":"code","v":"no_producer_rpc"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/read"},{"t":"text","v":" · "},{"t":"code","v":"/mcp/reset"},{"t":"text","v":" · "},{"t":"code","v":"/mcp/flush"},{"t":"text","v":" producer did not answer (non-agent / timeout)"}],"t":"bullet"},{"spans":[{"t":"text","v":"502 — "},{"t":"code","v":"reset_failed"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/reset"},{"t":"text","v":" agent could not clear its render buffer"}],"t":"bullet"},{"spans":[{"t":"text","v":"502 — "},{"t":"code","v":"flush_failed"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/flush"},{"t":"text","v":" agent could not flush its pending input"}],"t":"bullet"},{"spans":[{"t":"text","v":"403 — "},{"t":"code","v":"launch_disabled"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" with "},{"t":"code","v":"allow_launch:false"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"unknown_profile"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" profile not in config"}],"t":"bullet"},{"spans":[{"t":"text","v":"400 — "},{"t":"code","v":"bad_dims"},{"t":"text","v":" / "},{"t":"code","v":"bad_cwd"},{"t":"text","v":" / "},{"t":"code","v":"cwd_not_dir"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" bad "},{"t":"code","v":"cols"},{"t":"text","v":"/"},{"t":"code","v":"rows"},{"t":"text","v":"/"},{"t":"code","v":"cwd"}],"t":"bullet"},{"spans":[{"t":"text","v":"429 — "},{"t":"code","v":"too_many_pending_launches"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" backpressure"}],"t":"bullet"},{"spans":[{"t":"text","v":"500 — "},{"t":"code","v":"spawn_failed"},{"t":"text","v":" / "},{"t":"code","v":"agent_exited_early"},{"t":"text","v":" — "},{"t":"code","v":"/mcp/launch"},{"t":"text","v":" agent failed to start"}],"t":"bullet"},{"spans":[{"t":"text","v":"Admin surface (browser "},{"t":"code","v":"auth_token"},{"t":"text","v":"-gated)"}],"t":"sub"},{"spans":[{"t":"text","v":"These configure MCP and are "},{"t":"strong","v":"not"},{"t":"text","v":" part of the MCP token's surface. They use the browser "},{"t":"code","v":"auth_token"},{"t":"text","v":" (the same mandatory-token gate as "},{"t":"code","v":"/state"},{"t":"text","v":" and "},{"t":"code","v":"/file/*"},{"t":"text","v":"): always required, on every interface, as "},{"t":"code","v":"Authorization: Bearer"},{"t":"text","v":" or "},{"t":"code","v":"?token="},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"GET /mcp/config"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"enabled\":false,\"token\":\"\",\"default_mode\":\"off\",\"allow_launch\":false, \"token_env_pinned\":false}"},{"t":"text","v":" ("},{"t":"code","v":"token"},{"t":"text","v":" is the live secret, "},{"t":"code","v":"\"\""},{"t":"text","v":" when unset)."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mcp/config"},{"t":"text","v":" — partial update of any of "},{"t":"code","v":"enabled"},{"t":"text","v":", "},{"t":"code","v":"default_mode"},{"t":"text","v":" ("},{"t":"code","v":"off"},{"t":"text","v":"/"},{"t":"code","v":"read"},{"t":"text","v":"/"},{"t":"code","v":"readwrite"},{"t":"text","v":"), "},{"t":"code","v":"allow_launch"},{"t":"text","v":", "},{"t":"code","v":"token"},{"t":"text","v":", or "},{"t":"code","v":"generate:true"},{"t":"text","v":" (mint a fresh "},{"t":"code","v":"token_urlsafe(32)"},{"t":"text","v":"). Validated before any write; returns the GET shape. While the env pins the token, "},{"t":"code","v":"token"},{"t":"text","v":"/"},{"t":"code","v":"generate"},{"t":"text","v":" edits are "},{"t":"strong","v":"ignored"},{"t":"text","v":" (the live token stays the env value). Errors: "},{"t":"code","v":"bad_json"},{"t":"text","v":" (400), "},{"t":"code","v":"bad_mode"},{"t":"text","v":" (400), "},{"t":"code","v":"bad_token"},{"t":"text","v":" (400, non-string "},{"t":"code","v":"token"},{"t":"text","v":")."}],"t":"p"},{"spans":[{"t":"code","v":"POST /session/mcp"},{"t":"text","v":" — body "},{"t":"code","v":"{\"id\": <int>, \"mode\": \"off\"|\"read\"|\"readwrite\"}"},{"t":"text","v":" → "},{"t":"code","v":"{\"ok\":true,\"id\":<int>,\"mode\":<str>}"},{"t":"text","v":". Sets the "},{"t":"strong","v":"in-memory"},{"t":"text","v":" per-window override (resets on restart / relaunch). "},{"t":"code","v":"bad_mode"},{"t":"text","v":" (400) on an invalid mode."}],"t":"p"},{"spans":[{"t":"strong","v":"Sidecar "},{"t":"code","v":"webterm_mcp.json"},{"t":"text","v":" — the durable MCP config, written atomically next to the "},{"t":"code","v":"/state"},{"t":"text","v":" store (default "},{"t":"code","v":"<state_path dir>/webterm_mcp.json"},{"t":"text","v":"; override with config "},{"t":"code","v":"mcp_state_path"},{"t":"text","v":"). Schema:"}],"t":"p"},{"spans":[{"t":"code","v":"{\"token\": \"<secret or null>\", \"default_mode\": \"off\",\n \"allow_launch\": false, \"enabled\": true}"}],"t":"pre"},{"spans":[{"t":"code","v":"token"},{"t":"text","v":" is "},{"t":"code","v":"null"},{"t":"text","v":" when the env pins it (the secret stays off disk). The file self-heals if hand-edited or truncated. "},{"t":"strong","v":"Per-window modes are not persisted"},{"t":"text","v":" — only these broker-wide knobs are."}],"t":"p"},{"spans":[{"t":"strong","v":"Precedence."},{"t":"text","v":" Token: env "},{"t":"code","v":"WEB_TERMINAL_MCP_TOKEN"},{"t":"text","v":" > sidecar "},{"t":"code","v":"token"},{"t":"text","v":" > config "},{"t":"code","v":"mcp_token"},{"t":"text","v":". Effective mode: per-window override > global "},{"t":"code","v":"default_mode"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Launch-profile editor (browser "},{"t":"code","v":"auth_token"},{"t":"text","v":"-gated) — #70"}],"t":"sub"},{"spans":[{"t":"text","v":"The Control Panel edits the launch-profile allow-list here. Same mandatory browser-token gate as "},{"t":"code","v":"/mcp/config"},{"t":"text","v":" above — "},{"t":"strong","v":"never"},{"t":"text","v":" the MCP token, so the commands (the RCE-by-design half of profiles-only) only ever travel to an already-authenticated browser. "},{"t":"code","v":"/profiles"},{"t":"text","v":" and "},{"t":"code","v":"/mcp/profiles"},{"t":"text","v":" stay "},{"t":"strong","v":"names only"},{"t":"text","v":", so an MCP/AI agent still can't read a command or define a profile."}],"t":"p"},{"spans":[{"t":"code","v":"GET /profiles/config"},{"t":"text","v":" → the "},{"t":"strong","v":"full"},{"t":"text","v":" objects for this host: "},{"t":"code","v":"{\"ok\":true,\"default_profile\":\"cmd\",\"profiles\":{\"cmd\":{\"command\":[...],\"title\":..., \"cwd\":...}},\"os\":\"windows|posix\",\"source\":\"config|sidecar\",\"exists\":{\"cmd\":true}}"},{"t":"text","v":". "},{"t":"code","v":"exists[name]"},{"t":"text","v":" is "},{"t":"code","v":"shutil.which(command[0]) is not None"},{"t":"text","v":" (a red flag for a shell that isn't installed)."}],"t":"p"},{"spans":[{"t":"code","v":"POST /profiles/config"},{"t":"text","v":" — "},{"t":"strong","v":"replace"},{"t":"text","v":" semantics; body "},{"t":"code","v":"{\"profiles\":{...},\"default_profile\":\"...\"}"},{"t":"text","v":". Validated in full "},{"t":"strong","v":"before"},{"t":"text","v":" any write (a bad field changes nothing), then written to the sidecar atomically and the live launcher is swapped — "},{"t":"strong","v":"no restart"},{"t":"text","v":". Returns the GET shape. Errors (all "},{"t":"code","v":"400"},{"t":"text","v":" unless noted): "},{"t":"code","v":"too_large"},{"t":"text","v":" (413, body > 256 KiB), "},{"t":"code","v":"bad_json"},{"t":"text","v":", "},{"t":"code","v":"bad_profiles"},{"t":"text","v":" (not a dict), "},{"t":"code","v":"no_profiles"},{"t":"text","v":" (empty — would brick "},{"t":"code","v":"/launch"},{"t":"text","v":"), "},{"t":"code","v":"too_many_profiles"},{"t":"text","v":" (> 200), "},{"t":"code","v":"bad_name"},{"t":"text","v":" (empty / > 64 chars / control chars / outside "},{"t":"code","v":"[A-Za-z0-9 ._+-]"},{"t":"text","v":"), "},{"t":"code","v":"bad_profile"},{"t":"text","v":", "},{"t":"code","v":"bad_command"},{"t":"text","v":" (not a non-empty list of non-empty control-char-free strings), "},{"t":"code","v":"command_too_long"},{"t":"text","v":", "},{"t":"code","v":"command_token_too_long"},{"t":"text","v":", "},{"t":"code","v":"bad_title"},{"t":"text","v":"/"},{"t":"code","v":"title_too_long"},{"t":"text","v":", "},{"t":"code","v":"bad_cwd"},{"t":"text","v":"/"},{"t":"code","v":"cwd_too_long"},{"t":"text","v":", "},{"t":"code","v":"default_not_member"},{"t":"text","v":". An empty "},{"t":"code","v":"default_profile"},{"t":"text","v":" resolves to the first profile."}],"t":"p"},{"spans":[{"t":"code","v":"GET /profiles/detect"},{"t":"text","v":" → read-only environment scan seeding the editor: "},{"t":"code","v":"{\"ok\":true,\"suggestions\":[{\"name\",\"title\",\"command\",\"exists\"}]}"},{"t":"text","v":". Windows lists WSL distros ("},{"t":"code","v":"wsl.exe -l -q"},{"t":"text","v":"); POSIX lists installed "},{"t":"code","v":"bash"},{"t":"text","v":"/"},{"t":"code","v":"zsh"},{"t":"text","v":"/"},{"t":"code","v":"fish"},{"t":"text","v":"/"},{"t":"code","v":"sh"},{"t":"text","v":". Never errors on a missing tool — the list is just empty. Runs off the event loop."}],"t":"p"},{"spans":[{"t":"strong","v":"Sidecar "},{"t":"code","v":"webterm_profiles.json"},{"t":"text","v":" — the durable profile set, written atomically next to the "},{"t":"code","v":"/state"},{"t":"text","v":" store (default "},{"t":"code","v":"<state_path dir>/webterm_profiles.json"},{"t":"text","v":"; override with config "},{"t":"code","v":"profiles_state_path"},{"t":"text","v":"). Schema:"}],"t":"p"},{"spans":[{"t":"code","v":"{\"profiles\": {\"<name>\": {\"command\": [\"...\"], \"title\": null, \"cwd\": null}},\n \"default_profile\": \"<name>\"}"}],"t":"pre"},{"spans":[{"t":"text","v":"Seeded from "},{"t":"code","v":"broker_config.json"},{"t":"text","v":"'s "},{"t":"code","v":"agent.profiles"},{"t":"text","v":"; "},{"t":"strong","v":"once written it owns the set"},{"t":"text","v":" ("},{"t":"code","v":"agent.profiles"},{"t":"text","v":" becomes seed-only). Self-heals — a missing/corrupt/empty sidecar falls back to the seed, never bricking startup or "},{"t":"code","v":"/launch"},{"t":"text","v":". Full recipe catalog + the security rationale: "},{"t":"strong","v":"[[Launch-Profiles]]"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"text","v":"Mods (browser "},{"t":"code","v":"auth_token"},{"t":"text","v":"-gated) — #157 / #163 / #172"}],"t":"sub"},{"spans":[{"t":"text","v":"The desktop's mod system. Authoring contract, the "},{"t":"code","v":"ctx"},{"t":"text","v":" API and the trust model: "},{"t":"strong","v":"[[Writing-a-Mod]]"},{"t":"text","v":". All of these are "},{"t":"code","v":"serve_ui"},{"t":"strong","v":"-gated"},{"t":"text","v":" — a headless broker registers none of them and reports "},{"t":"code","v":"mods: []"},{"t":"text","v":" — and, like "},{"t":"code","v":"/mcp/config"},{"t":"text","v":", they take the "},{"t":"strong","v":"browser"},{"t":"text","v":" token, never the MCP one. They are deliberately "},{"t":"strong","v":"not"},{"t":"text","v":" lease-gated: this is broker configuration, and the point is being able to administer a broker somebody else is using right now."}],"t":"p"},{"spans":[{"t":"strong","v":"A mod is not sandboxed."},{"t":"text","v":" It runs same-origin with the page's full authority and can reach "},{"t":"code","v":"/file/*"},{"t":"text","v":", "},{"t":"code","v":"/session/*"},{"t":"text","v":", "},{"t":"code","v":"/state"},{"t":"text","v":" and "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" directly; "},{"t":"code","v":"ctx"},{"t":"text","v":" grants it nothing it lacks. So the trust event is the "},{"t":"strong","v":"token-gated install"},{"t":"text","v":", not the fetch that follows."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mods/policy"},{"t":"text","v":" — this broker's mod pins, "},{"t":"code","v":"{modId: bool}"},{"t":"text","v":", applied to every browser that loads its page (an absent id leaves the choice to that browser). Stored in the "},{"t":"code","v":"webterm_mod_policy.json"},{"t":"text","v":" sidecar beside the "},{"t":"code","v":"/state"},{"t":"text","v":" store (override with config "},{"t":"code","v":"mod_policy_path"},{"t":"text","v":"); reported by "},{"t":"code","v":"GET /info"},{"t":"text","v":" as "},{"t":"code","v":"mod_policy"},{"t":"text","v":". Applies on the next page load."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mods/install"},{"t":"text","v":" — body "},{"t":"code","v":"{\"manifest\": {…}, \"files\": {\"<name>\": \"<text>\"}, \"replace\": false}"},{"t":"text","v":", ≤ 2 MiB, validated "},{"t":"strong","v":"entirely in memory"},{"t":"text","v":" before a byte is written. The mod id must match "},{"t":"code","v":"[a-z0-9][a-z0-9-]{0,63}"},{"t":"text","v":" "},{"t":"strong","v":"and start with "},{"t":"code","v":"x-"},{"t":"text","v":" ("},{"t":"code","v":"reserved_id"},{"t":"text","v":" otherwise — the un-prefixed namespace is reserved for shipped mods). "},{"t":"code","v":"manifest.scripts"},{"t":"text","v":" is a required non-empty list of "},{"t":"code","v":".js"},{"t":"text","v":" names; "},{"t":"code","v":"entry"},{"t":"text","v":" and every other unknown key are "},{"t":"strong","v":"rejected"},{"t":"text","v":", not ignored. Files are text only ("},{"t":"code","v":".js"},{"t":"text","v":"/"},{"t":"code","v":".css"},{"t":"text","v":"/"},{"t":"code","v":".md"},{"t":"text","v":"), UTF-8, BOM-free, newline-terminated, ≤ "},{"t":"code","v":"ui._MAX_LINES"},{"t":"text","v":" lines; a "},{"t":"code","v":".css"},{"t":"text","v":" may not "},{"t":"code","v":"@import"},{"t":"text","v":" or reference an absolute "},{"t":"code","v":"url(http…)"},{"t":"text","v":". Caps: 32 mods, 32 files/mod, 256 KiB/file, 512 KiB/mod. Returns "},{"t":"code","v":"{\"ok\":true,\"id\",\"gen\",\"replaced\",\"adopts_existing_state\":{\"mod_store\",\"pin\"}, \"mod\":{…},\"applies\":\"next_page_load\"}"},{"t":"text","v":". Errors (each distinct): "},{"t":"code","v":"too_large"},{"t":"text","v":" (413), "},{"t":"code","v":"bad_json"},{"t":"text","v":", "},{"t":"code","v":"bad_mod_id"},{"t":"text","v":", "},{"t":"code","v":"bad_generation"},{"t":"text","v":", "},{"t":"code","v":"reserved_id"},{"t":"text","v":", "},{"t":"code","v":"id_in_use"},{"t":"text","v":" (409), "},{"t":"code","v":"bad_file_name"},{"t":"text","v":", "},{"t":"code","v":"reserved_file_name"},{"t":"text","v":", "},{"t":"code","v":"too_many_files"},{"t":"text","v":", "},{"t":"code","v":"file_too_large"},{"t":"text","v":" (413), "},{"t":"code","v":"total_too_large"},{"t":"text","v":" (413), "},{"t":"code","v":"bad_encoding"},{"t":"text","v":", "},{"t":"code","v":"bad_scripts"},{"t":"text","v":", "},{"t":"code","v":"bad_styles"},{"t":"text","v":", "},{"t":"code","v":"bad_requires"},{"t":"text","v":", "},{"t":"code","v":"bad_manifest_field"},{"t":"text","v":", "},{"t":"code","v":"unknown_manifest_key"},{"t":"text","v":", "},{"t":"code","v":"css_external_reference"},{"t":"text","v":", "},{"t":"code","v":"too_many_mods"},{"t":"text","v":" (409), "},{"t":"code","v":"write_failed"},{"t":"text","v":" (500)."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mods/uninstall"},{"t":"text","v":" — "},{"t":"code","v":"{\"id\": \"x-notes\", \"purge\": false}"},{"t":"text","v":". "},{"t":"code","v":"purge:true"},{"t":"text","v":" also clears this broker's "},{"t":"code","v":"/mod-store/<id>"},{"t":"text","v":" and its pin (lock order "},{"t":"code","v":"mods_install → mod_policy → modstore"},{"t":"text","v":", data-first/code-last); it cannot touch what other browsers hold in "},{"t":"code","v":"localStorage"},{"t":"text","v":". "},{"t":"strong","v":"Not idempotent at the HTTP level"},{"t":"text","v":": a retry after a lost response answers "},{"t":"code","v":"404 not_installed"},{"t":"text","v":" even though the first attempt succeeded — treat that as possible success."}],"t":"p"},{"spans":[{"t":"code","v":"POST /mods/rescan"},{"t":"text","v":" — re-read "},{"t":"code","v":"mods_dir"},{"t":"text","v":" and sweep litter. An operator convenience, "},{"t":"strong","v":"not a trust boundary"},{"t":"text","v":" (anyone who can write "},{"t":"code","v":"mods_dir"},{"t":"text","v":" can write the broker's source tree); the scanner refuses symlinks/reparse points, refuses anything whose realpath leaves "},{"t":"code","v":"mods_dir"},{"t":"text","v":", refuses a non-"},{"t":"code","v":"x-"},{"t":"text","v":" directory, and validates every byte under the same rules an install obeys. The destructive sweep refuses to run in a directory without the "},{"t":"code","v":".browserland-mods"},{"t":"text","v":" marker."}],"t":"p"},{"spans":[{"t":"code","v":"GET /mods/installed"},{"t":"text","v":" — the operator detail "},{"t":"code","v":"/info"},{"t":"text","v":" leaves out: "},{"t":"code","v":"mods_dir"},{"t":"text","v":", per-file sizes and sha256s, "},{"t":"code","v":"installed_at"},{"t":"text","v":", declared "},{"t":"code","v":"tiers"},{"t":"text","v":", "},{"t":"code","v":"has_help"},{"t":"text","v":", the effective "},{"t":"code","v":"limits"},{"t":"text","v":", and "},{"t":"strong","v":"what was skipped and why"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"code","v":"GET /mods/<modId>/<gen>/<name>"},{"t":"text","v":" — one file of one generation of one installed mod. "},{"t":"strong","v":"Public"},{"t":"text","v":", and forced rather than chosen: a "},{"t":"code","v":"<script src>"},{"t":"text","v":" cannot carry an "},{"t":"code","v":"Authorization"},{"t":"text","v":" header, "},{"t":"code","v":"?token="},{"t":"text","v":" is structurally banned (#144), and a "},{"t":"code","v":"fetch"},{"t":"text","v":"+"},{"t":"code","v":"blob:"},{"t":"text","v":" workaround would need "},{"t":"code","v":"blob:"},{"t":"text","v":" in "},{"t":"code","v":"script-src"},{"t":"text","v":". Same posture as "},{"t":"code","v":"GET /"},{"t":"text","v":", which already carries every shipped mod's source. Served from an in-memory allowlist dict keyed "},{"t":"code","v":"\"<id>/<gen>/<name>\""},{"t":"text","v":" — a client-supplied segment can only ever hit a known key, so traversal is unrepresentable rather than defended against, and no blocking IO reaches the event loop. Only "},{"t":"code","v":".js"},{"t":"text","v":"/"},{"t":"code","v":".css"},{"t":"text","v":" are servable. "},{"t":"code","v":"Cache-Control: public, max-age=31536000, immutable"},{"t":"text","v":" is honest because "},{"t":"code","v":"<gen>"},{"t":"text","v":" is a content hash. "},{"t":"strong","v":"Installed source and styles are therefore readable by anyone who knows the id, the generation and the file name — do not put a secret in a mod."},{"t":"text","v":" "},{"t":"code","v":"help.md"},{"t":"text","v":" is not servable here at all ("},{"t":"code","v":"content_type"},{"t":"text","v":" returns "},{"t":"code","v":"None"},{"t":"text","v":" for it); its only surface is "},{"t":"code","v":"/help-corpus.json"},{"t":"text","v":", which since #173 withholds the installed sections — and so the installed ids — from a caller with no token."}],"t":"p"},{"spans":[{"t":"code","v":"<gen>"},{"t":"strong","v":" is not a second secret."},{"t":"text","v":" "},{"t":"code","v":"compute_gen"},{"t":"text","v":" is sha256 over the canonical manifest plus the sorted "},{"t":"code","v":"(name, sha256)"},{"t":"text","v":" pairs — no salt, no broker id, no install timestamp — so anyone holding the exact bytes of a distributed package recomputes the gen this broker stored, and a "},{"t":"code","v":"200"},{"t":"text","v":" here rather than a "},{"t":"code","v":"404"},{"t":"text","v":" confirms it is installed. That is a "},{"t":"strong","v":"confirmation oracle over a candidate set the caller already has"},{"t":"text","v":", not enumeration: a mod whose bytes were never published stays unguessable in both segments, and what leaks about a public one is one bit about code this route serves in full anyway. #173 is therefore partial by construction — it stops the corpus "},{"t":"strong","v":"listing"},{"t":"text","v":" installed ids, it cannot hide a publicly distributed mod from someone who asks for it by name. Salting the gen would close it and is deliberately not done: a generation's directory name must content-address its own bytes (the scanner refuses one that does not), which is what makes reinstalling identical bytes a no-op, keeps two brokers' URLs for one package agreeing, and makes "},{"t":"code","v":"immutable"},{"t":"text","v":" honest."}],"t":"p"},{"spans":[{"t":"strong","v":"Store "},{"t":"code","v":"mods_dir"},{"t":"text","v":" — runtime-installed mods live in a directory beside the "},{"t":"code","v":"/state"},{"t":"text","v":" store (default "},{"t":"code","v":"<state dir>/webterm_mods"},{"t":"text","v":"; override with config "},{"t":"code","v":"mods_dir"},{"t":"text","v":"), deliberately "},{"t":"strong","v":"not"},{"t":"text","v":" "},{"t":"code","v":"webterm/broker/mods/"},{"t":"text","v":", whose drift guard is bidirectional. Layout "},{"t":"code","v":"<id>/CURRENT"},{"t":"text","v":" (the atomic "},{"t":"code","v":"{\"gen\": …}"},{"t":"text","v":" commit pointer) + "},{"t":"code","v":"<id>/<gen>/"},{"t":"text","v":" holding "},{"t":"code","v":"mod.json"},{"t":"text","v":", the mod's files and broker-written "},{"t":"code","v":".gen.json"},{"t":"text","v":". "},{"t":"code","v":"gen"},{"t":"text","v":" is a sha256 over the canonical manifest plus the sorted "},{"t":"code","v":"(name, sha256)"},{"t":"text","v":" pairs; "},{"t":"strong","v":"two generations are retained"},{"t":"text","v":", so a page that started booting against one cannot be handed a file from the next mid-flight."}],"t":"p"},{"spans":[{"t":"strong","v":"Install/uninstall applies on the NEXT PAGE LOAD, never live"},{"t":"text","v":", and that is forced: global lexical bindings cannot be removed, so re-executing a mod script in the same page dies with "},{"t":"code","v":"SyntaxError: Identifier … has already been declared"},{"t":"text","v":". "},{"t":"code","v":"?nomods=1"},{"t":"text","v":" on the desktop URL boots with no mod fetched or initialized at all — the rescue hatch when an installed mod makes the Control Panel unreachable."}],"t":"p"},{"spans":[{"t":"text","v":"curl"}],"t":"sub"},{"spans":[{"t":"code","v":"B=http://127.0.0.1:4445\n\n# Admin (add -H \"Authorization: Bearer $AUTH\" — a token is always required):\n# enable MCP, mint a token, default new windows to read, allow launching.\ncurl -s -X POST $B/mcp/config -H 'content-type: application/json' \\\n  -d '{\"enabled\":true,\"generate\":true,\"default_mode\":\"read\",\"allow_launch\":true}'\n# -> {\"ok\":true,\"enabled\":true,\"token\":\"abc123...\",\"default_mode\":\"read\",...}\nTOK='abc123...'   # the token from above\n\ncurl -s $B/mcp/info      -H \"Authorization: Bearer $TOK\"\ncurl -s $B/mcp/terminals -H \"Authorization: Bearer $TOK\"\ncurl -s $B/mcp/profiles  -H \"Authorization: Bearer $TOK\"\n\n# Read a terminal's screen (use an id from /mcp/terminals):\ncurl -s -X POST $B/mcp/read -H \"Authorization: Bearer $TOK\" \\\n  -H 'content-type: application/json' -d '{\"id\":4503603655475937}'\n\n# Promote that window to readwrite (admin), then type into it:\ncurl -s -X POST $B/session/mcp -H 'content-type: application/json' \\\n  -d '{\"id\":4503603655475937,\"mode\":\"readwrite\"}'\ncurl -s -X POST $B/mcp/input -H \"Authorization: Bearer $TOK\" \\\n  -H 'content-type: application/json' -d '{\"id\":4503603655475937,\"data\":\"ls\\n\"}'\n\n# Launch a new terminal from a profile:\ncurl -s -X POST $B/mcp/launch -H \"Authorization: Bearer $TOK\" \\\n  -H 'content-type: application/json' -d '{\"profile\":\"bash\",\"cols\":100,\"rows\":30}'"}],"t":"pre"},{"spans":[{"t":"text","v":"The shipped MCP server"}],"t":"sub"},{"spans":[{"t":"text","v":"A ready-to-run MCP server lives in ["},{"t":"code","v":"webterm/mcptool/"},{"t":"text","v":"](../webterm/mcptool/) — a thin stdio wrapper that maps each endpoint above to an MCP tool ("},{"t":"code","v":"mcp_info"},{"t":"text","v":", "},{"t":"code","v":"list_terminals"},{"t":"text","v":", "},{"t":"code","v":"list_profiles"},{"t":"text","v":", "},{"t":"code","v":"read_screen"},{"t":"text","v":", "},{"t":"code","v":"send_input"},{"t":"text","v":", "},{"t":"code","v":"send_keys"},{"t":"text","v":", "},{"t":"code","v":"set_pace"},{"t":"text","v":", "},{"t":"code","v":"reset_terminal"},{"t":"text","v":", "},{"t":"code","v":"flush_input"},{"t":"text","v":", "},{"t":"code","v":"launch_terminal"},{"t":"text","v":"). It connects to the broker over HTTP and authenticates with the MCP token."}],"t":"p"},{"spans":[{"t":"code","v":"pip install -e \".[mcp]\"                      # needs Python >=3.10 (the mcp SDK)\nclaude mcp add browserland --env BROWSERLAND_MCP_TOKEN=… -- python -m webterm.mcptool"}],"t":"pre"},{"spans":[{"t":"text","v":"See ["},{"t":"code","v":"webterm/mcptool/README.md"},{"t":"text","v":"](../webterm/mcptool/README.md) for config (env/flags), the tool list, and how access modes / "},{"t":"code","v":"allow_launch"},{"t":"text","v":" still govern behavior."}],"t":"p"}],"search":"mcp http interface the broker exposes an opt-in http api (the /mcp/* surface) that an mcp server wraps as mcp tools so an ai agent can list, observe, drive, and launch terminals. a ready-to-run server ships in [ webterm/mcptool/ ](../webterm/mcptool/) (stdio transport; pip install -e \".[mcp]\" ). nine token-gated endpoints make up the contract: method · path · purpose get — /mcp/info — feature flags ( allow_launch , default_mode ) get — /mcp/terminals — list mcp-visible terminals post — /mcp/read — render a terminal's screen as plain text post — /mcp/input — type into a terminal ( readwrite only) post — /mcp/reset — clear a terminal's screen-render buffer ( readwrite only) post — /mcp/flush — discard a terminal's unread queued input ( readwrite only) post — /mcp/pace — set a terminal's default send_keys pacing ( readwrite only) get — /mcp/profiles — launchable profile names post — /mcp/launch — spawn a new terminal from a profile the interface is disabled by default — nothing under /mcp/* answers until an admin both enables it and sets a token (below). the broker serves the desktop ui and this api, not this document, so editing these docs needs no restart. enabling mcp & the token mcp needs both the master enabled flag and a token. with either missing every /mcp/* request is 403 {\"error\":\"mcp_disabled\"} . get a token any of three ways (precedence env > sidecar > config seed ): **control panel → mcp access → *generate*** — mints a token_urlsafe(32) secret and persists it to the sidecar (below). the normal path. config mcp_token — a seed value in broker_config.json . env web_terminal_mcp_token — pins the token over both of the above, is never written to disk , and disables ui token edits (the control panel shows token_env_pinned: true ). pass it on every /mcp/* request as an authorization: bearer <token> header or a ?token=<token> / ?auth=<token> query parameter (compared in constant time). a missing/invalid token while the feature is enabled is 401 {\"error\":\"auth_required\"} . this mcp token is a separate secret from the browser auth_token and gates only the /mcp/* data plane. like every other surface it has no loopback exemption (mcp is opt-in, so even a local caller needs the token). cors preflights exist for browser callers; a server-side mcp client makes ordinary requests and is not subject to cors. access modes each terminal has an effective access mode — its per-window override if set, otherwise the broker-wide default_mode : mode · effect off — hidden from /mcp/terminals ; /mcp/read + /mcp/input → 404 unknown_or_off read — visible + /mcp/read ; /mcp/input → 403 read_only readwrite — read and /mcp/input default_mode (global, default off ) — applies to every window without an override; change it live in control panel → mcp access or post /mcp/config . per-window override — set from the window title-bar mcp access menu or post /session/mcp ; in-memory only (resets on broker restart / agent relaunch). allow_launch (global, default false ) — independent flag gating /mcp/launch only. endpoints (mcp-token-gated) all require a valid mcp token; read / input additionally require the target's effective mode to permit them. method · path · requires · purpose get /mcp/info — token — feature flags get /mcp/terminals — token — visible terminals post /mcp/read — mode ≥ read — screen as text post /mcp/input — mode readwrite — send keystrokes post /mcp/reset — mode readwrite — clear the screen-render buffer post /mcp/flush — mode readwrite — discard unread queued input post /mcp/pace — mode readwrite — set the default send_keys pacing get /mcp/profiles — token — profile names post /mcp/launch — allow_launch — spawn a terminal get /mcp/info → {\"ok\":true,\"allow_launch\":false,\"default_mode\":\"off\",\"version\":\"0.8.0+ba4b62e\"} . version is this broker's build id ( webterm.build_version() — package version + git short hash, or the bare package version off a checkout) for stale-deploy detection (#22). get /mcp/terminals → an array; windows whose effective mode is off are omitted. agent is the detected foreground-agent name ( \"\" when none), kind the producer kind ( \"agent\" vs a non-agent \"terminal\" ), mode the effective access mode. version is the producer's reported build id ( \"\" for a pre-#22 agent / a non-agent producer); for agent producers a stale boolean flags a build differing from this broker's (a deploy predating a fix — reliable when builds carry a git hash). app_cursor is the cached decckm (application-cursor mode) the mcp send_keys reads to pick csi vs ss3 arrows (#23); pace_ms is the window's default send_keys inter-key pacing (#133, 0 = single-burst, set via /mcp/pace ) the mcp server reads so a no- delay_ms send auto-paces. pyte (#134) is whether the agent has pyte installed: false means its read_screen uses the dependency-free textgrid fallback — no attr_runs (#128) and no keyframe repair (#130), so a sparse alt-screen frame after ring eviction comes back flagged partial only. it defaults true for a pre-#134 agent that omits the field: [{\"id\":4503603655475937,\"title\":\"bash\",\"host\":\"jc-server\",\"cwd\":\"/home/me\", \"agent\":\"\",\"kind\":\"agent\",\"cols\":80,\"rows\":24,\"mode\":\"read\", \"version\":\"0.8.0+ba4b62e\",\"stale\":false,\"app_cursor\":false,\"pace_ms\":0, \"pyte\":true}] post /mcp/read — body {\"id\": <int>} plus optional fields: view: \"scrollback\" + lines reads scrollback history instead of the live screen ( lines is hard-capped at 1000, #21; the reply echoes view and history_lines ); since (a prior content_hash ) asks for a delta reply — the response always carries a delta boolean, with changed_rows present only for a real delta (#52); attrs: true opts into an attr_runs styled-run map (fg/bg/reverse) so a color-only change is observable (#128). {\"ok\":true,\"id\":4503603655475937,\"cols\":80,\"rows\":24,\"text\":\"<screen lines>\\n...\",\"content_hash\":\"…\",\"stable_hash\":\"…\",\"idle_ms\":0} content_hash is a stable 128-bit digest of the rendered text (#26). stable_hash (#135) is the same digest with the hardware-cursor cell masked to a sentinel, so a cursor blink in place (the cell toggling between the block glyph and the character under it) leaves it unchanged while a cursor move — which exposes a different underlying cell — still changes it. it is always present (empty string for a degraded read or an older agent) and equals content_hash when there is no cursor. idle_ms is a best-effort count of ms since the terminal last emitted pty output (a current agent always reports it — 0 means output landed just now; an older agent omits it, so it reads as unknown rather than a misleading 0 ). it is unreliable for a perpetually-animating app that paints every frame (its idle_ms never grows), so pacing/flush and a semantic content wait are the real settle signals there, not idle_ms . the read can also block for one bounded round-trip instead of returning immediately, on any one of four mutually-exclusive wait signals (combining two → 400 conflicting_wait ), all capped by timeout_ms (≤ 15000): wait_for_change (a prior content_hash — wake when the screen differs, #26), wait_for_text / wait_for_regex (+ wait_absent — wake when the grid contains or no longer contains a match, #51), and wait_for_idle (a settle window in ms — wake once stable_hash has held steady for that many ms, i.e. output went quiet, #135). the predicate and idle waits set matched (true when satisfied, false on timeout); the rpc timeout is stretched by timeout_ms so the agent's hold outlives it. wait_for_idle settles only a calmer tui — a fully-animating app (dwarf fortress animates creatures/water/the *paused* marquee off the cursor every frame) never reaches output-idle, so there use pacing/flush plus a semantic wait_for_text check, not idle. the agent renders the screen off its event loop. with pyte it returns the full grid; without pyte it falls back to a dependency-free in-house emulator ( agent/snapshot/textgrid.py ) that still produces a bounded rows × cols grid — so a full-screen tui reads as a clean grid (box-drawing/braille intact), not an unbounded raw-ansi dump (#15). both paths are real grid renders, so \"degraded\": true is now reserved for the rare last-ditch raw decode (it no longer appears for ordinary tuis). the textgrid fallback has no keyframe repair (that is pyte-only, #130), so a sparse alt-screen frame after ring eviction comes back flagged \"partial\": true (#134) — the grid is a real render, just possibly incomplete; the per-terminal pyte flag on /mcp/terminals tells a caller up front which path an agent is on. only agent producers answer; a non-agent terminal producer has no handler, so the request times out → 502 {\"error\":\"no_producer_rpc\"} . post /mcp/input — body {\"id\": <int>, \"data\": \"<str>\"} → {\"ok\":true} . requires effective mode readwrite (else 403 read_only ); data must be a string (else 400 bad_data ) and ≤ 256 kib utf-8 (else 413 too_large ). forwarded verbatim straight to the pty and deliberately bypasses the single-active-browser lease — mcp is its own authorized channel. the high-level mcp send_input tool maps newlines in data to a carriage return before calling this endpoint, so a command submits on powershell/psreadline (which treats a bare \\n as a soft continuation) and on a unix shell alike (#13); the endpoint itself is byte-exact, so drive it directly for a literal lf or raw-mode bytes. post /mcp/reset — body {\"id\": <int>} → {\"ok\":true,\"id\":<int>} . requires effective mode readwrite (else 403 read_only ). a correlated producer round-trip (like /mcp/read ): the agent clears its pty-output ring so the next read_screen renders from a clean slate, then acks. only agent producers answer; a non-agent producer times out → 502 no_producer_rpc (and a rare agent-side failure → 502 reset_failed ). it touches browserland's render buffer only — it sends nothing to the running app. post /mcp/flush — body {\"id\": <int>} → {\"ok\":true,\"id\":<int>} . requires effective mode readwrite (else 403 read_only ). the input-side mirror of /mcp/reset : where reset clears the output ring, this discards keystrokes queued toward the app but not yet consumed (a runaway send_keys backlog a frame-polling tui hasn't drained), so the next read_screen reflects the settled state. same correlated round-trip — only agent producers answer, so a non-agent producer times out → 502 no_producer_rpc (a rare agent-side failure → 502 flush_failed ). on a windows/conpty agent it is a best-effort no-op (that backend exposes no input-queue flush primitive) and still acks ok . post /mcp/pace — body {\"id\": <int>, \"pace_ms\": <int>} → {\"ok\":true,\"id\":<int>,\"pace_ms\":<clamped int>} . requires effective mode readwrite (else 403 read_only ). sets the window's default send_keys inter-key pacing so a subsequent mcp send_keys that passes no delay_ms auto-paces (one key per post) — for a frame-polling raw-input tui (dwarf fortress) that drops a burst read faster than it renders. pace_ms must be an integer (else 400 bad_pace ) and is clamped to [0, 1000] ( 0 disables → single-burst; an over-cap value pins to 1000 ). unlike /mcp/reset / /mcp/flush this is broker-local with no producer round-trip — it just stamps the window's in-memory pace_ms , which /mcp/terminals surfaces for the mcp server's client-side pacer. the value is ephemeral per-connection (resets on agent relaunch, like a per-window mode override). get /mcp/profiles → {\"default\":\"cmd\",\"profiles\":[\"cmd\",\"powershell\"]} (the broker's configured agent.profiles ; bash / sh on linux). post /mcp/launch — requires allow_launch (else 403 launch_disabled ). body reuses the /launch shape: {\"profile\": <str>, \"cols\": 80, \"rows\": 24, \"title\": <str>, \"cwd\": <str>} , all optional (dims default 80×24, profile defaults to the broker default; cwd must be an existing dir). response is the launcher's: {\"ok\":true,\"id\":4503603655475937,\"registered\":true,\"agent_pid\":12345} 200 when the agent registered within 10 s, 202 when it spawned but had not said hello yet ( \"registered\":false ). error reference status · error · when 403 — mcp_disabled — feature disabled or no token configured 401 — auth_required — missing/invalid mcp token 400 — bad_json — body is not a json object 400 — bad_id — id missing or not an integer 404 — unknown_or_off — no such window, or its effective mode is off 400 — bad_data — /mcp/input data is not a string 403 — read_only — /mcp/input on a window not in readwrite 413 — too_large — /mcp/input payload > 256 kib 400 — bad_pace — /mcp/pace pace_ms missing or not an integer 502 — no_producer_rpc — /mcp/read · /mcp/reset · /mcp/flush producer did not answer (non-agent / timeout) 502 — reset_failed — /mcp/reset agent could not clear its render buffer 502 — flush_failed — /mcp/flush agent could not flush its pending input 403 — launch_disabled — /mcp/launch with allow_launch:false 400 — unknown_profile — /mcp/launch profile not in config 400 — bad_dims / bad_cwd / cwd_not_dir — /mcp/launch bad cols / rows / cwd 429 — too_many_pending_launches — /mcp/launch backpressure 500 — spawn_failed / agent_exited_early — /mcp/launch agent failed to start admin surface (browser auth_token -gated) these configure mcp and are not part of the mcp token's surface. they use the browser auth_token (the same mandatory-token gate as /state and /file/* ): always required, on every interface, as authorization: bearer or ?token= . get /mcp/config → {\"ok\":true,\"enabled\":false,\"token\":\"\",\"default_mode\":\"off\",\"allow_launch\":false, \"token_env_pinned\":false} ( token is the live secret, \"\" when unset). post /mcp/config — partial update of any of enabled , default_mode ( off / read / readwrite ), allow_launch , token , or generate:true (mint a fresh token_urlsafe(32) ). validated before any write; returns the get shape. while the env pins the token, token / generate edits are ignored (the live token stays the env value). errors: bad_json (400), bad_mode (400), bad_token (400, non-string token ). post /session/mcp — body {\"id\": <int>, \"mode\": \"off\"|\"read\"|\"readwrite\"} → {\"ok\":true,\"id\":<int>,\"mode\":<str>} . sets the in-memory per-window override (resets on restart / relaunch). bad_mode (400) on an invalid mode. sidecar webterm_mcp.json — the durable mcp config, written atomically next to the /state store (default <state_path dir>/webterm_mcp.json ; override with config mcp_state_path ). schema: {\"token\": \"<secret or null>\", \"default_mode\": \"off\", \"allow_launch\": false, \"enabled\": true} token is null when the env pins it (the secret stays off disk). the file self-heals if hand-edited or truncated. per-window modes are not persisted — only these broker-wide knobs are. precedence. token: env web_terminal_mcp_token > sidecar token > config mcp_token . effective mode: per-window override > global default_mode . launch-profile editor (browser auth_token -gated) — #70 the control panel edits the launch-profile allow-list here. same mandatory browser-token gate as /mcp/config above — never the mcp token, so the commands (the rce-by-design half of profiles-only) only ever travel to an already-authenticated browser. /profiles and /mcp/profiles stay names only , so an mcp/ai agent still can't read a command or define a profile. get /profiles/config → the full objects for this host: {\"ok\":true,\"default_profile\":\"cmd\",\"profiles\":{\"cmd\":{\"command\":[...],\"title\":..., \"cwd\":...}},\"os\":\"windows|posix\",\"source\":\"config|sidecar\",\"exists\":{\"cmd\":true}} . exists[name] is shutil.which(command[0]) is not none (a red flag for a shell that isn't installed). post /profiles/config — replace semantics; body {\"profiles\":{...},\"default_profile\":\"...\"} . validated in full before any write (a bad field changes nothing), then written to the sidecar atomically and the live launcher is swapped — no restart . returns the get shape. errors (all 400 unless noted): too_large (413, body > 256 kib), bad_json , bad_profiles (not a dict), no_profiles (empty — would brick /launch ), too_many_profiles (> 200), bad_name (empty / > 64 chars / control chars / outside [a-za-z0-9 ._+-] ), bad_profile , bad_command (not a non-empty list of non-empty control-char-free strings), command_too_long , command_token_too_long , bad_title / title_too_long , bad_cwd / cwd_too_long , default_not_member . an empty default_profile resolves to the first profile. get /profiles/detect → read-only environment scan seeding the editor: {\"ok\":true,\"suggestions\":[{\"name\",\"title\",\"command\",\"exists\"}]} . windows lists wsl distros ( wsl.exe -l -q ); posix lists installed bash / zsh / fish / sh . never errors on a missing tool — the list is just empty. runs off the event loop. sidecar webterm_profiles.json — the durable profile set, written atomically next to the /state store (default <state_path dir>/webterm_profiles.json ; override with config profiles_state_path ). schema: {\"profiles\": {\"<name>\": {\"command\": [\"...\"], \"title\": null, \"cwd\": null}}, \"default_profile\": \"<name>\"} seeded from broker_config.json 's agent.profiles ; once written it owns the set ( agent.profiles becomes seed-only). self-heals — a missing/corrupt/empty sidecar falls back to the seed, never bricking startup or /launch . full recipe catalog + the security rationale: [[launch-profiles]] . mods (browser auth_token -gated) — #157 / #163 / #172 the desktop's mod system. authoring contract, the ctx api and the trust model: [[writing-a-mod]] . all of these are serve_ui -gated — a headless broker registers none of them and reports mods: [] — and, like /mcp/config , they take the browser token, never the mcp one. they are deliberately not lease-gated: this is broker configuration, and the point is being able to administer a broker somebody else is using right now. a mod is not sandboxed. it runs same-origin with the page's full authority and can reach /file/* , /session/* , /state and /mcp/* directly; ctx grants it nothing it lacks. so the trust event is the token-gated install , not the fetch that follows. post /mods/policy — this broker's mod pins, {modid: bool} , applied to every browser that loads its page (an absent id leaves the choice to that browser). stored in the webterm_mod_policy.json sidecar beside the /state store (override with config mod_policy_path ); reported by get /info as mod_policy . applies on the next page load. post /mods/install — body {\"manifest\": {…}, \"files\": {\"<name>\": \"<text>\"}, \"replace\": false} , ≤ 2 mib, validated entirely in memory before a byte is written. the mod id must match [a-z0-9][a-z0-9-]{0,63} and start with x- ( reserved_id otherwise — the un-prefixed namespace is reserved for shipped mods). manifest.scripts is a required non-empty list of .js names; entry and every other unknown key are rejected , not ignored. files are text only ( .js / .css / .md ), utf-8, bom-free, newline-terminated, ≤ ui._max_lines lines; a .css may not @import or reference an absolute url(http…) . caps: 32 mods, 32 files/mod, 256 kib/file, 512 kib/mod. returns {\"ok\":true,\"id\",\"gen\",\"replaced\",\"adopts_existing_state\":{\"mod_store\",\"pin\"}, \"mod\":{…},\"applies\":\"next_page_load\"} . errors (each distinct): too_large (413), bad_json , bad_mod_id , bad_generation , reserved_id , id_in_use (409), bad_file_name , reserved_file_name , too_many_files , file_too_large (413), total_too_large (413), bad_encoding , bad_scripts , bad_styles , bad_requires , bad_manifest_field , unknown_manifest_key , css_external_reference , too_many_mods (409), write_failed (500). post /mods/uninstall — {\"id\": \"x-notes\", \"purge\": false} . purge:true also clears this broker's /mod-store/<id> and its pin (lock order mods_install → mod_policy → modstore , data-first/code-last); it cannot touch what other browsers hold in localstorage . not idempotent at the http level : a retry after a lost response answers 404 not_installed even though the first attempt succeeded — treat that as possible success. post /mods/rescan — re-read mods_dir and sweep litter. an operator convenience, not a trust boundary (anyone who can write mods_dir can write the broker's source tree); the scanner refuses symlinks/reparse points, refuses anything whose realpath leaves mods_dir , refuses a non- x- directory, and validates every byte under the same rules an install obeys. the destructive sweep refuses to run in a directory without the .browserland-mods marker. get /mods/installed — the operator detail /info leaves out: mods_dir , per-file sizes and sha256s, installed_at , declared tiers , has_help , the effective limits , and what was skipped and why . get /mods/<modid>/<gen>/<name> — one file of one generation of one installed mod. public , and forced rather than chosen: a <script src> cannot carry an authorization header, ?token= is structurally banned (#144), and a fetch + blob: workaround would need blob: in script-src . same posture as get / , which already carries every shipped mod's source. served from an in-memory allowlist dict keyed \"<id>/<gen>/<name>\" — a client-supplied segment can only ever hit a known key, so traversal is unrepresentable rather than defended against, and no blocking io reaches the event loop. only .js / .css are servable. cache-control: public, max-age=31536000, immutable is honest because <gen> is a content hash. installed source and styles are therefore readable by anyone who knows the id, the generation and the file name — do not put a secret in a mod. help.md is not servable here at all ( content_type returns none for it); its only surface is /help-corpus.json , which since #173 withholds the installed sections — and so the installed ids — from a caller with no token. <gen> is not a second secret. compute_gen is sha256 over the canonical manifest plus the sorted (name, sha256) pairs — no salt, no broker id, no install timestamp — so anyone holding the exact bytes of a distributed package recomputes the gen this broker stored, and a 200 here rather than a 404 confirms it is installed. that is a confirmation oracle over a candidate set the caller already has , not enumeration: a mod whose bytes were never published stays unguessable in both segments, and what leaks about a public one is one bit about code this route serves in full anyway. #173 is therefore partial by construction — it stops the corpus listing installed ids, it cannot hide a publicly distributed mod from someone who asks for it by name. salting the gen would close it and is deliberately not done: a generation's directory name must content-address its own bytes (the scanner refuses one that does not), which is what makes reinstalling identical bytes a no-op, keeps two brokers' urls for one package agreeing, and makes immutable honest. store mods_dir — runtime-installed mods live in a directory beside the /state store (default <state dir>/webterm_mods ; override with config mods_dir ), deliberately not webterm/broker/mods/ , whose drift guard is bidirectional. layout <id>/current (the atomic {\"gen\": …} commit pointer) + <id>/<gen>/ holding mod.json , the mod's files and broker-written .gen.json . gen is a sha256 over the canonical manifest plus the sorted (name, sha256) pairs; two generations are retained , so a page that started booting against one cannot be handed a file from the next mid-flight. install/uninstall applies on the next page load, never live , and that is forced: global lexical bindings cannot be removed, so re-executing a mod script in the same page dies with syntaxerror: identifier … has already been declared . ?nomods=1 on the desktop url boots with no mod fetched or initialized at all — the rescue hatch when an installed mod makes the control panel unreachable. curl b=http://127.0.0.1:4445 # admin (add -h \"authorization: bearer $auth\" — a token is always required): # enable mcp, mint a token, default new windows to read, allow launching. curl -s -x post $b/mcp/config -h 'content-type: application/json' \\ -d '{\"enabled\":true,\"generate\":true,\"default_mode\":\"read\",\"allow_launch\":true}' # -> {\"ok\":true,\"enabled\":true,\"token\":\"abc123...\",\"default_mode\":\"read\",...} tok='abc123...' # the token from above curl -s $b/mcp/info -h \"authorization: bearer $tok\" curl -s $b/mcp/terminals -h \"authorization: bearer $tok\" curl -s $b/mcp/profiles -h \"authorization: bearer $tok\" # read a terminal's screen (use an id from /mcp/terminals): curl -s -x post $b/mcp/read -h \"authorization: bearer $tok\" \\ -h 'content-type: application/json' -d '{\"id\":4503603655475937}' # promote that window to readwrite (admin), then type into it: curl -s -x post $b/session/mcp -h 'content-type: application/json' \\ -d '{\"id\":4503603655475937,\"mode\":\"readwrite\"}' curl -s -x post $b/mcp/input -h \"authorization: bearer $tok\" \\ -h 'content-type: application/json' -d '{\"id\":4503603655475937,\"data\":\"ls\\n\"}' # launch a new terminal from a profile: curl -s -x post $b/mcp/launch -h \"authorization: bearer $tok\" \\ -h 'content-type: application/json' -d '{\"profile\":\"bash\",\"cols\":100,\"rows\":30}' the shipped mcp server a ready-to-run mcp server lives in [ webterm/mcptool/ ](../webterm/mcptool/) — a thin stdio wrapper that maps each endpoint above to an mcp tool ( mcp_info , list_terminals , list_profiles , read_screen , send_input , send_keys , set_pace , reset_terminal , flush_input , launch_terminal ). it connects to the broker over http and authenticates with the mcp token. pip install -e \".[mcp]\" # needs python >=3.10 (the mcp sdk) claude mcp add browserland --env browserland_mcp_token=… -- python -m webterm.mcptool see [ webterm/mcptool/readme.md ](../webterm/mcptool/readme.md) for config (env/flags), the tool list, and how access modes / allow_launch still govern behavior.","title":"MCP HTTP interface"},{"body":[{"spans":[{"t":"code","v":"python -m pytest tests -q"}],"t":"pre"},{"spans":[{"t":"text","v":"The full suite passes (the platform PTY e2e suites skip on the other OS): protocol shapes, ring eviction, OSC sniffer split at every byte index, raw/pyte snapshot rendering, agent↔fake-broker integration (reconnect, snapshot ordering, title re-hello), real-ConPTY round trip on Windows, real-POSIX-PTY round trip on Linux (bash echo/resize/"},{"t":"code","v":"stty size"},{"t":"text","v":"/OSC title/exit code, Ctrl-C via TIOCSCTTY, huge-paste backpressure), broker e2e as a subprocess (auth gates, CORS on error paths and preflights, sanic-ext neutralized, relay invariants, "},{"t":"code","v":"/launch"},{"t":"text","v":" → detached agent on both platforms), the mandatory-token policy (token bootstrap/mint/adopt, a router-enumerating guard that every non-public route answers 401 "},{"t":"code","v":"auth_required"},{"t":"text","v":", and the WS 4401 gates on loopback), non-loopback auth negatives + CORS positives via the box's LAN IP, plus the newer surfaces: the MCP tools (read/reset/flush/pace), the file API, session recording, the status-fetch proxy, the textgrid fallback renderer, and UI asset assembly."}],"t":"p"},{"spans":[{"t":"code","v":"pyte"},{"t":"text","v":" is optional: "},{"t":"code","v":"pip install pyte"},{"t":"text","v":". It is required only for "},{"t":"code","v":"--snapshot-mode pyte"},{"t":"text","v":" ("},{"t":"code","v":"raw"},{"t":"text","v":" starts without it), but installing it also upgrades MCP "},{"t":"code","v":"read_screen"},{"t":"text","v":" regardless of snapshot mode — attr_runs (#128) and keyframe repair (#130); without it the dependency-free textgrid fallback is used and a sparse alt-screen frame after ring eviction comes back flagged "},{"t":"code","v":"partial"},{"t":"text","v":" (#134). Windows agents need "},{"t":"code","v":"pywinpty>=2"},{"t":"text","v":"."}],"t":"p"}],"search":"tests python -m pytest tests -q the full suite passes (the platform pty e2e suites skip on the other os): protocol shapes, ring eviction, osc sniffer split at every byte index, raw/pyte snapshot rendering, agent↔fake-broker integration (reconnect, snapshot ordering, title re-hello), real-conpty round trip on windows, real-posix-pty round trip on linux (bash echo/resize/ stty size /osc title/exit code, ctrl-c via tiocsctty, huge-paste backpressure), broker e2e as a subprocess (auth gates, cors on error paths and preflights, sanic-ext neutralized, relay invariants, /launch → detached agent on both platforms), the mandatory-token policy (token bootstrap/mint/adopt, a router-enumerating guard that every non-public route answers 401 auth_required , and the ws 4401 gates on loopback), non-loopback auth negatives + cors positives via the box's lan ip, plus the newer surfaces: the mcp tools (read/reset/flush/pace), the file api, session recording, the status-fetch proxy, the textgrid fallback renderer, and ui asset assembly. pyte is optional: pip install pyte . it is required only for --snapshot-mode pyte ( raw starts without it), but installing it also upgrades mcp read_screen regardless of snapshot mode — attr_runs (#128) and keyframe repair (#130); without it the dependency-free textgrid fallback is used and a sparse alt-screen frame after ring eviction comes back flagged partial (#134). windows agents need pywinpty>=2 .","title":"Tests"},{"body":[{"spans":[{"t":"text","v":"This broker's relay and registry were adapted from "},{"t":"code","v":"xterm-py"},{"t":"text","v":" (<https://github.com/JohnConnorNPC/xterm-py>), a separate relay/registry/UI codebase, which is not part of or modified by this project. This broker defaults to port "},{"t":"strong","v":"4445"},{"t":"text","v":"."}],"t":"p"}],"search":"relationship to xterm-py this broker's relay and registry were adapted from xterm-py (<https://github.com/johnconnornpc/xterm-py>), a separate relay/registry/ui codebase, which is not part of or modified by this project. this broker defaults to port 4445 .","title":"Relationship to xterm-py"}],"label":"Technical Reference","order":18,"slug":"technical-reference","tier":"dev"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Breaking changes, what they cost you, and how to recover. Newest first."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"overview breaking changes, what they cost you, and how to recover. newest first. ---","title":"Overview"},{"body":[{"spans":[{"t":"strong","v":"Nothing to do, and nothing is lost."},{"t":"text","v":" This is here because the persisted layout changes shape on first load, and because the feature can now be turned off."}],"t":"p"},{"spans":[{"t":"text","v":"What changed"}],"t":"sub"},{"spans":[{"t":"code","v":"prefs._layout"},{"t":"text","v":" used to group tiled columns under "},{"t":"code","v":"workspaces:[...]"},{"t":"text","v":" with an "},{"t":"code","v":"activeWs"},{"t":"text","v":" index, and the tiling core owned that model. It is now a single desktop:"}],"t":"p"},{"spans":[{"t":"code","v":"before   { mode, activeWs, workspaces:[ {id, name?, focusedCol, columns:[...]} ] }\nafter    { mode, focusedCol, columns:[...],\n           mods: { workspaces: { version:1, activeId, list:[{id,name?,focusedCol}] } } }"}],"t":"pre"},{"spans":[{"t":"text","v":"Every column lives in "},{"t":"code","v":"columns"},{"t":"text","v":" exactly once. Workspaces are a mod ("},{"t":"code","v":"mods/workspaces/"},{"t":"text","v":", "},{"t":"strong","v":"enabled by default"},{"t":"text","v":") that stamps a "},{"t":"code","v":"wsId"},{"t":"text","v":" on each column and tells the strip which ones to draw."}],"t":"p"},{"spans":[{"t":"text","v":"What it costs you"}],"t":"sub"},{"spans":[{"t":"text","v":"Nothing, on either path:"}],"t":"p"},{"spans":[{"t":"strong","v":"Upgrading."},{"t":"text","v":" The first "},{"t":"code","v":"getLayout()"},{"t":"text","v":" concatenates your existing workspaces'"}],"t":"bullet"},{"spans":[{"t":"text","v":"columns into "},{"t":"code","v":"columns"},{"t":"text","v":", in order, and hands the grouping to the mod, which rebuilds your workspaces from it. Names and the workspace you were last on are preserved. The migration is one-way and idempotent, and it is "},{"t":"strong","v":"non-destructive"},{"t":"text","v":" — no column is dropped even if the mod never loads — so a browser that migrates first cannot damage a peer that has not yet."}],"t":"p"},{"spans":[{"t":"strong","v":"Turning the mod off."},{"t":"text","v":" Every column from every workspace appears together"}],"t":"bullet"},{"spans":[{"t":"text","v":"on one desktop; the pager, the seven workspace shortcuts and the workspace menu items go away. Your grouping is kept, not deleted: turn it back on and the workspaces return exactly as they were. The same is true of pinning it off broker-wide (Control Panel -> Mods on this broker)."}],"t":"p"},{"spans":[{"t":"text","v":"Your keyboard bindings for "},{"t":"code","v":"workspace-prev"},{"t":"text","v":" / "},{"t":"code","v":"workspace-next"},{"t":"text","v":" / "},{"t":"code","v":"workspace-1..5"},{"t":"text","v":" are stored by action id and are untouched by any of this."}],"t":"p"},{"spans":[{"t":"text","v":"Downgrading"}],"t":"sub"},{"spans":[{"t":"text","v":"An older build reads the new blob as a layout with no "},{"t":"code","v":"workspaces"},{"t":"text","v":" key, which its "},{"t":"code","v":"reconcileLayout"},{"t":"text","v":" heals into one workspace holding "},{"t":"strong","v":"no"},{"t":"text","v":" columns — your windows come back as floating windows rather than tiled, and you would rebuild the layout. Keep a copy of "},{"t":"code","v":"webterm_state.json"},{"t":"text","v":" if you plan to roll back."}],"t":"p"},{"spans":[{"t":"text","v":"---"}],"t":"p"}],"search":"workspaces became a mod, and the layout blob changed shape (#148) nothing to do, and nothing is lost. this is here because the persisted layout changes shape on first load, and because the feature can now be turned off. what changed prefs._layout used to group tiled columns under workspaces:[...] with an activews index, and the tiling core owned that model. it is now a single desktop: before { mode, activews, workspaces:[ {id, name?, focusedcol, columns:[...]} ] } after { mode, focusedcol, columns:[...], mods: { workspaces: { version:1, activeid, list:[{id,name?,focusedcol}] } } } every column lives in columns exactly once. workspaces are a mod ( mods/workspaces/ , enabled by default ) that stamps a wsid on each column and tells the strip which ones to draw. what it costs you nothing, on either path: upgrading. the first getlayout() concatenates your existing workspaces' columns into columns , in order, and hands the grouping to the mod, which rebuilds your workspaces from it. names and the workspace you were last on are preserved. the migration is one-way and idempotent, and it is non-destructive — no column is dropped even if the mod never loads — so a browser that migrates first cannot damage a peer that has not yet. turning the mod off. every column from every workspace appears together on one desktop; the pager, the seven workspace shortcuts and the workspace menu items go away. your grouping is kept, not deleted: turn it back on and the workspaces return exactly as they were. the same is true of pinning it off broker-wide (control panel -> mods on this broker). your keyboard bindings for workspace-prev / workspace-next / workspace-1..5 are stored by action id and are untouched by any of this. downgrading an older build reads the new blob as a layout with no workspaces key, which its reconcilelayout heals into one workspace holding no columns — your windows come back as floating windows rather than tiled, and you would rebuild the layout. keep a copy of webterm_state.json if you plan to roll back. ---","title":"Workspaces became a mod, and the layout blob changed shape (#148)"},{"body":[{"spans":[{"t":"strong","v":"This breaks tokenless installs."},{"t":"text","v":" It cannot not break them — the whole point is that a request which used to be accepted is now refused. Read the table below to find your case."}],"t":"p"},{"spans":[{"t":"text","v":"What changed"}],"t":"sub"},{"spans":[{"t":"text","v":"Browserland used to treat "},{"t":"strong","v":"loopback as an auth exemption"},{"t":"text","v":". Three problems:"}],"t":"p"},{"spans":[{"t":"strong","v":"With no "},{"t":"code","v":"auth_token"},{"t":"strong","v":" configured"},{"t":"text","v":" — the shipped default —"}],"t":"bullet"},{"spans":[{"t":"code","v":"/sessions"},{"t":"text","v":", "},{"t":"code","v":"/profiles"},{"t":"text","v":", "},{"t":"code","v":"/ws"},{"t":"text","v":" and "},{"t":"code","v":"/control"},{"t":"text","v":" were gated by a check that did nothing when no token existed. On a "},{"t":"code","v":"0.0.0.0"},{"t":"text","v":" or tailnet bind that was an open read of every terminal's title, pid and cwd, an open attach to every PTY, and an open steal of the single-active-browser lease."}],"t":"p"},{"spans":[{"t":"strong","v":"Loopback never meant \"same machine.\""},{"t":"text","v":" The recommended topology is"}],"t":"bullet"},{"spans":[{"t":"code","v":"tailscale serve"},{"t":"text","v":" in front of a "},{"t":"code","v":"127.0.0.1"},{"t":"text","v":" bind — and a proxied request arrives from loopback. So the recommended deployment handed the whole tailnet an exempt path to "},{"t":"code","v":"/launch"},{"t":"text","v":" (shell spawn) and "},{"t":"code","v":"/file/*"},{"t":"text","v":" (host-wide read/write). A page in your own browser reaches loopback too."}],"t":"p"},{"spans":[{"t":"code","v":"/browserland"},{"t":"strong","v":" was exempt even when a token WAS configured"},{"t":"text","v":" — the one"}],"t":"bullet"},{"spans":[{"t":"text","v":"gate the token never covered. WebSockets are not CORS-gated, so any website you had open could dial "},{"t":"code","v":"ws://127.0.0.1:4445/browserland"},{"t":"text","v":", re-register a live window id (kicking the real agent off) and inject fabricated output into a terminal you trust."}],"t":"p"},{"spans":[{"t":"text","v":"Now: "},{"t":"strong","v":"a token is required on every surface, on every interface, always."},{"t":"text","v":" There is no loopback exemption and no opt-out."}],"t":"p"},{"spans":[{"t":"text","v":"The only unauthenticated responses are "},{"t":"code","v":"GET /"},{"t":"text","v":" (the token is typed into that page, and auth is query/header-only with no cookies — gating the document would 401 every reload, bookmark and new tab forever), "},{"t":"code","v":"GET /help-corpus.json"},{"t":"text","v":", and the "},{"t":"code","v":"OPTIONS"},{"t":"text","v":" preflights, which carry no credentials by design. "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" keeps its own separate "},{"t":"code","v":"mcp_token"},{"t":"text","v":" realm."}],"t":"p"},{"spans":[{"t":"strong","v":"A fresh install still works with one command."},{"t":"text","v":" With nothing configured the broker mints its own token into "},{"t":"code","v":"webterm_token.json"},{"t":"text","v":" beside its state store and prints a ready-to-open "},{"t":"code","v":"?token=…"},{"t":"text","v":" URL."}],"t":"p"},{"spans":[{"t":"text","v":"Does this affect me?"}],"t":"sub"},{"spans":[{"t":"text","v":"Your install · What breaks · Recovery"}],"t":"sub"},{"spans":[{"t":"strong","v":"Tokenless, loopback only"},{"t":"text","v":" (the shipped default) — The browser asks for a token on next load. "},{"t":"strong","v":"Terminals launched before the restart are stranded."},{"t":"text","v":" Scripts curling "},{"t":"code","v":"/sessions"},{"t":"text","v":", "},{"t":"code","v":"/file/*"},{"t":"text","v":" or "},{"t":"code","v":"/launch"},{"t":"text","v":" over loopback now get "},{"t":"code","v":"401"},{"t":"text","v":". — The token is printed at startup and by "},{"t":"code","v":"--print-token"},{"t":"text","v":". Relaunch the terminals."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Tokenless behind "},{"t":"code","v":"tailscale serve"},{"t":"strong","v":" / a proxy"},{"t":"text","v":" — Same, plus every remote browser must enter the token once. — Same."}],"t":"bullet"},{"spans":[{"t":"code","v":"auth_token"},{"t":"strong","v":" already configured"},{"t":"text","v":" — Almost nothing. Running agents already carry "},{"t":"code","v":"WEB_TERMINAL_TOKEN"},{"t":"text","v":" and reconnect fine; browsers already stored it. Only "},{"t":"strong","v":"hand-started local agents"},{"t":"text","v":" and tokenless local scripts break. — "},{"t":"code","v":"launchers/run-agent.{sh,ps1}"},{"t":"text","v":" now read the sidecar automatically, or set "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"strong","v":"MCP clients"},{"t":"text","v":" ("},{"t":"code","v":"webterm.mcptool"},{"t":"text","v":") — Nothing. Separate "},{"t":"code","v":"mcp_token"},{"t":"text","v":", "},{"t":"code","v":"Authorization: Bearer"},{"t":"text","v":", "},{"t":"code","v":"/mcp/*"},{"t":"text","v":" realm — untouched. — —"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Headless"},{"t":"text","v":" ("},{"t":"code","v":"serve_ui: false"},{"t":"text","v":") — Nothing. "},{"t":"code","v":"GET /"},{"t":"text","v":" still answers "},{"t":"code","v":"200 {\"ui\": false}"},{"t":"text","v":", so health probes keep working. — —"}],"t":"bullet"},{"spans":[{"t":"text","v":"Getting your token"}],"t":"sub"},{"spans":[{"t":"code","v":"python -m webterm.broker --print-token"}],"t":"pre"},{"spans":[{"t":"text","v":"Prints the token and the URL to open, without starting a server. It never mints one — asking for the token must not be the thing that creates it."}],"t":"p"},{"spans":[{"t":"text","v":"Resolution order, for the broker and for "},{"t":"code","v":"--print-token"},{"t":"text","v":" alike:"}],"t":"p"},{"spans":[{"t":"code","v":"$WEB_TERMINAL_TOKEN"}],"t":"bullet"},{"spans":[{"t":"code","v":"auth_token"},{"t":"text","v":" in "},{"t":"code","v":"broker_config.json"}],"t":"bullet"},{"spans":[{"t":"code","v":"webterm_token.json"},{"t":"text","v":" beside the state store"}],"t":"bullet"},{"spans":[{"t":"text","v":"(broker only) mint a new one into that file"}],"t":"bullet"},{"spans":[{"t":"text","v":"The one unavoidable loss: stranded terminals"}],"t":"sub"},{"spans":[{"t":"text","v":"If you were running "},{"t":"strong","v":"tokenless"},{"t":"text","v":", the terminals that broker launched were spawned with "},{"t":"code","v":"WEB_TERMINAL_TOKEN"},{"t":"text","v":" deliberately removed from their environment. After you restart the broker they cannot re-register."}],"t":"p"},{"spans":[{"t":"strong","v":"Their shells keep running. Their windows never come back."},{"t":"text","v":" There is no server-side rescue: an environment variable cannot be injected into a live process. The broker notices this happening and, after a few refused producer connections, logs what it means and points here."}],"t":"p"},{"spans":[{"t":"text","v":"So, before deploying:"}],"t":"p"},{"spans":[{"t":"text","v":"Finish or accept the loss of whatever is running in those terminals."}],"t":"bullet"},{"spans":[{"t":"text","v":"Consider setting an explicit "},{"t":"code","v":"auth_token"},{"t":"text","v":" in your config "},{"t":"strong","v":"before"},{"t":"text","v":" the"}],"t":"bullet"},{"spans":[{"t":"text","v":"cutover, so you know the value in advance rather than discovering it in the log afterwards."}],"t":"p"},{"spans":[{"t":"text","v":"Then restart the broker and relaunch your terminals once. From then on"}],"t":"bullet"},{"spans":[{"t":"text","v":"everything self-heals: terminals launched by the UI get the token from the broker automatically."}],"t":"p"},{"spans":[{"t":"text","v":"Other behaviour changes"}],"t":"sub"},{"spans":[{"t":"text","v":"**The startup banner prints the full "},{"t":"code","v":"?token=…"},{"t":"text","v":" URL only on the run that"}],"t":"bullet"},{"spans":[{"t":"text","v":"minted it, and only to an interactive terminal.** Under systemd, Docker or CI it prints a pointer to "},{"t":"code","v":"--print-token"},{"t":"text","v":" instead, so a live credential is not baked into aggregated logs. Sanic's access log is now off for the same reason (the token rides in the query string)."}],"t":"p"},{"spans":[{"t":"text","v":"**A first start against a directory that already holds broker state prints a"}],"t":"bullet"},{"spans":[{"t":"text","v":"loud "},{"t":"code","v":"UPGRADE NOTICE"},{"t":"text","v":"** — that combination means this install used to run tokenless."}],"t":"p"},{"spans":[{"t":"strong","v":"The token is no longer visible from inside your terminals."},{"t":"text","v":" The agent"}],"t":"bullet"},{"spans":[{"t":"text","v":"removes "},{"t":"code","v":"WEB_TERMINAL_TOKEN"},{"t":"text","v":" before spawning your shell, so "},{"t":"code","v":"echo $WEB_TERMINAL_TOKEN"},{"t":"text","v":" is empty. Shell-side automation that needs it should read "},{"t":"code","v":"webterm_token.json"},{"t":"text","v":" or run "},{"t":"code","v":"--print-token"},{"t":"text","v":"."}],"t":"p"},{"spans":[{"t":"strong","v":"An agent refused for a missing token"},{"t":"text","v":" retries every 10 s instead of once a"}],"t":"bullet"},{"spans":[{"t":"text","v":"second, and logs one error naming "},{"t":"code","v":"$WEB_TERMINAL_TOKEN"},{"t":"text","v":". It does not give up permanently — restoring the right token lets it reconnect on its own."}],"t":"p"},{"spans":[{"t":"strong","v":"New response headers:"},{"t":"text","v":" "},{"t":"code","v":"Referrer-Policy: no-referrer"},{"t":"text","v":" (the desktop URL"}],"t":"bullet"},{"spans":[{"t":"text","v":"carries the token, so an outbound link must not leak it in "},{"t":"code","v":"Referer"},{"t":"text","v":"), plus "},{"t":"code","v":"X-Frame-Options: DENY"},{"t":"text","v":" and "},{"t":"code","v":"Content-Security-Policy: frame-ancestors 'none'"},{"t":"text","v":" ("},{"t":"code","v":"GET /"},{"t":"text","v":" is public so login can bootstrap, but it must not be embeddable — an attacker page cannot read across origins, yet could otherwise clickjack a browser that already holds a token)."}],"t":"p"},{"spans":[{"t":"strong","v":"Error shapes:"},{"t":"text","v":" the "},{"t":"code","v":"disabled_no_token"},{"t":"text","v":" and "},{"t":"code","v":"launch_disabled_no_token"}],"t":"bullet"},{"spans":[{"t":"code","v":"403"},{"t":"text","v":"s are gone. Everything answers "},{"t":"code","v":"401 auth_required"},{"t":"text","v":", which is what the UI and the mods already match on."}],"t":"p"},{"spans":[{"t":"text","v":"File permissions"}],"t":"sub"},{"spans":[{"t":"code","v":"webterm_token.json"},{"t":"text","v":" is created "},{"t":"code","v":"0600"},{"t":"text","v":" on POSIX. "},{"t":"strong","v":"Windows has no POSIX file mode"},{"t":"text","v":": the file inherits the containing directory's ACL, so on a shared Windows host make sure the broker's working directory is not readable by other accounts. The file is git-ignored either way."}],"t":"p"},{"spans":[{"t":"text","v":"If the directory is not writable the broker still starts, but with an "},{"t":"strong","v":"ephemeral"},{"t":"text","v":" token: it changes on every restart, "},{"t":"code","v":"--print-token"},{"t":"text","v":" cannot recover it, and terminals do not survive a restart. That case warns loudly on every start."}],"t":"p"}],"search":"a token is required on every connection, including loopback (#142) this breaks tokenless installs. it cannot not break them — the whole point is that a request which used to be accepted is now refused. read the table below to find your case. what changed browserland used to treat loopback as an auth exemption . three problems: with no auth_token configured — the shipped default — /sessions , /profiles , /ws and /control were gated by a check that did nothing when no token existed. on a 0.0.0.0 or tailnet bind that was an open read of every terminal's title, pid and cwd, an open attach to every pty, and an open steal of the single-active-browser lease. loopback never meant \"same machine.\" the recommended topology is tailscale serve in front of a 127.0.0.1 bind — and a proxied request arrives from loopback. so the recommended deployment handed the whole tailnet an exempt path to /launch (shell spawn) and /file/* (host-wide read/write). a page in your own browser reaches loopback too. /browserland was exempt even when a token was configured — the one gate the token never covered. websockets are not cors-gated, so any website you had open could dial ws://127.0.0.1:4445/browserland , re-register a live window id (kicking the real agent off) and inject fabricated output into a terminal you trust. now: a token is required on every surface, on every interface, always. there is no loopback exemption and no opt-out. the only unauthenticated responses are get / (the token is typed into that page, and auth is query/header-only with no cookies — gating the document would 401 every reload, bookmark and new tab forever), get /help-corpus.json , and the options preflights, which carry no credentials by design. /mcp/* keeps its own separate mcp_token realm. a fresh install still works with one command. with nothing configured the broker mints its own token into webterm_token.json beside its state store and prints a ready-to-open ?token=… url. does this affect me? your install · what breaks · recovery tokenless, loopback only (the shipped default) — the browser asks for a token on next load. terminals launched before the restart are stranded. scripts curling /sessions , /file/* or /launch over loopback now get 401 . — the token is printed at startup and by --print-token . relaunch the terminals. tokenless behind tailscale serve / a proxy — same, plus every remote browser must enter the token once. — same. auth_token already configured — almost nothing. running agents already carry web_terminal_token and reconnect fine; browsers already stored it. only hand-started local agents and tokenless local scripts break. — launchers/run-agent.{sh,ps1} now read the sidecar automatically, or set $web_terminal_token . mcp clients ( webterm.mcptool ) — nothing. separate mcp_token , authorization: bearer , /mcp/* realm — untouched. — — headless ( serve_ui: false ) — nothing. get / still answers 200 {\"ui\": false} , so health probes keep working. — — getting your token python -m webterm.broker --print-token prints the token and the url to open, without starting a server. it never mints one — asking for the token must not be the thing that creates it. resolution order, for the broker and for --print-token alike: $web_terminal_token auth_token in broker_config.json webterm_token.json beside the state store (broker only) mint a new one into that file the one unavoidable loss: stranded terminals if you were running tokenless , the terminals that broker launched were spawned with web_terminal_token deliberately removed from their environment. after you restart the broker they cannot re-register. their shells keep running. their windows never come back. there is no server-side rescue: an environment variable cannot be injected into a live process. the broker notices this happening and, after a few refused producer connections, logs what it means and points here. so, before deploying: finish or accept the loss of whatever is running in those terminals. consider setting an explicit auth_token in your config before the cutover, so you know the value in advance rather than discovering it in the log afterwards. then restart the broker and relaunch your terminals once. from then on everything self-heals: terminals launched by the ui get the token from the broker automatically. other behaviour changes **the startup banner prints the full ?token=… url only on the run that minted it, and only to an interactive terminal.** under systemd, docker or ci it prints a pointer to --print-token instead, so a live credential is not baked into aggregated logs. sanic's access log is now off for the same reason (the token rides in the query string). **a first start against a directory that already holds broker state prints a loud upgrade notice ** — that combination means this install used to run tokenless. the token is no longer visible from inside your terminals. the agent removes web_terminal_token before spawning your shell, so echo $web_terminal_token is empty. shell-side automation that needs it should read webterm_token.json or run --print-token . an agent refused for a missing token retries every 10 s instead of once a second, and logs one error naming $web_terminal_token . it does not give up permanently — restoring the right token lets it reconnect on its own. new response headers: referrer-policy: no-referrer (the desktop url carries the token, so an outbound link must not leak it in referer ), plus x-frame-options: deny and content-security-policy: frame-ancestors 'none' ( get / is public so login can bootstrap, but it must not be embeddable — an attacker page cannot read across origins, yet could otherwise clickjack a browser that already holds a token). error shapes: the disabled_no_token and launch_disabled_no_token 403 s are gone. everything answers 401 auth_required , which is what the ui and the mods already match on. file permissions webterm_token.json is created 0600 on posix. windows has no posix file mode : the file inherits the containing directory's acl, so on a shared windows host make sure the broker's working directory is not readable by other accounts. the file is git-ignored either way. if the directory is not writable the broker still starts, but with an ephemeral token: it changes on every restart, --print-token cannot recover it, and terminals do not survive a restart. that case warns loudly on every start.","title":"A token is required on every connection, including loopback (#142)"}],"label":"Upgrading","order":19,"slug":"upgrading","tier":"dev"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Background pattern setting paints a subtle repeating texture on the desktop, behind the taskbar strip and every floating window. Its colors are drawn from the active color scheme, so switching Color scheme repaints the pattern to match rather than leaving it in stale colors."}],"t":"p"},{"spans":[{"t":"text","v":"Find it in "},{"t":"strong","v":"Control Panel → Desktop → Background pattern"},{"t":"text","v":". Like Color scheme, the choice is browser-global — it applies to every host you drive from this browser."}],"t":"p"}],"search":"overview the background pattern setting paints a subtle repeating texture on the desktop, behind the taskbar strip and every floating window. its colors are drawn from the active color scheme, so switching color scheme repaints the pattern to match rather than leaving it in stale colors. find it in control panel → desktop → background pattern . like color scheme, the choice is browser-global — it applies to every host you drive from this browser.","title":"Overview"},{"body":[{"spans":[{"t":"strong","v":"None"},{"t":"text","v":" (the default), "},{"t":"strong","v":"Weave"},{"t":"text","v":", "},{"t":"strong","v":"Dither"},{"t":"text","v":", "},{"t":"strong","v":"Dots"},{"t":"text","v":", "},{"t":"strong","v":"Hatch"},{"t":"text","v":", and "},{"t":"strong","v":"Tiles"},{"t":"text","v":" — each a different repeating pattern at a different scale, from a fine diagonal weave to a coarse tile grid."}],"t":"p"}],"search":"the six options none (the default), weave , dither , dots , hatch , and tiles — each a different repeating pattern at a different scale, from a fine diagonal weave to a coarse tile grid.","title":"The six options"}],"label":"Background pattern","mod":"pattern","order":2010,"slug":"pattern"},{"cards":[{"body":[{"spans":[{"t":"text","v":"A sticky note is a small, always-visible scratch pad for quick text. Notes use a fixed-width font, so pasted code, ASCII, and aligned columns line up the way they do in the terminal and editor. Notes stay out of the taskbar by default."}],"t":"p"}],"search":"overview a sticky note is a small, always-visible scratch pad for quick text. notes use a fixed-width font, so pasted code, ascii, and aligned columns line up the way they do in the terminal and editor. notes stay out of the taskbar by default.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Prefer a taskbar button per note? Turn on "},{"t":"strong","v":"show sticky-note windows on the taskbar"},{"t":"text","v":" under "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":" (the Sticky notes setting). Every open note then gets a taskbar button like any other window — click to focus or restore, click the active one to minimize — and the button tracks the note's title and color. Flipping the setting applies immediately to notes already open. The default (off) keeps today's behavior."}],"t":"p"}],"search":"notes on the taskbar prefer a taskbar button per note? turn on show sticky-note windows on the taskbar under control panel → mods (the sticky notes setting). every open note then gets a taskbar button like any other window — click to focus or restore, click the active one to minimize — and the button tracks the note's title and color. flipping the setting applies immediately to notes already open. the default (off) keeps today's behavior.","title":"Notes on the taskbar"},{"body":[{"spans":[{"t":"text","v":"If you close a sticky note that still has text in it, it is not lost: a non-empty closed note is listed under a "},{"t":"strong","v":"Closed notes"},{"t":"text","v":" section at the bottom of the "},{"t":"strong","v":"+"},{"t":"text","v":" (launch) menu. Click it there to reopen it. An empty note you close is simply discarded. For more on the launch menu, see Taskbar."}],"t":"p"}],"search":"reopening a closed note if you close a sticky note that still has text in it, it is not lost: a non-empty closed note is listed under a closed notes section at the bottom of the + (launch) menu. click it there to reopen it. an empty note you close is simply discarded. for more on the launch menu, see taskbar. taskbar","title":"Reopening a closed note"}],"icon":"📝","label":"Sticky note","mod":"sticky","order":2010,"slug":"sticky"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Terminal font setting picks the font every terminal renders with, in place of the built-in monospace default."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"disabled by default"},{"t":"text","v":" — turn it on first in "},{"t":"strong","v":"Control Panel → Mods"},{"t":"text","v":", which then adds the "},{"t":"strong","v":"Terminal font"},{"t":"text","v":" setting to "},{"t":"strong","v":"Control Panel → Desktop"},{"t":"text","v":". The choice is browser-global, so it applies to every terminal on every host you drive from this browser."}],"t":"p"}],"search":"overview the terminal font setting picks the font every terminal renders with, in place of the built-in monospace default. the mod ships disabled by default — turn it on first in control panel → mods , which then adds the terminal font setting to control panel → desktop . the choice is browser-global, so it applies to every terminal on every host you drive from this browser.","title":"Overview"},{"body":[{"spans":[{"t":"strong","v":"Default (Consolas)"},{"t":"text","v":" (the fallback while unset), "},{"t":"strong","v":"Cascadia Code"},{"t":"text","v":", "},{"t":"strong","v":"Fira Code"},{"t":"text","v":", "},{"t":"strong","v":"JetBrains Mono"},{"t":"text","v":", "},{"t":"strong","v":"Source Code Pro"},{"t":"text","v":", "},{"t":"strong","v":"Courier New"},{"t":"text","v":", and "},{"t":"strong","v":"System monospace"},{"t":"text","v":" — the browser's own generic monospace font. Picking a family that isn't actually installed falls back to whatever the browser substitutes for it; the mod doesn't install fonts, only selects among them."}],"t":"p"}],"search":"the choices default (consolas) (the fallback while unset), cascadia code , fira code , jetbrains mono , source code pro , courier new , and system monospace — the browser's own generic monospace font. picking a family that isn't actually installed falls back to whatever the browser substitutes for it; the mod doesn't install fonts, only selects among them.","title":"The choices"},{"body":[{"spans":[{"t":"text","v":"A change restyles every terminal already open, and every new one, without a reload. Turning the mod off resets every terminal back to the built-in default font."}],"t":"p"}],"search":"applies live, per terminal a change restyles every terminal already open, and every new one, without a reload. turning the mod off resets every terminal back to the built-in default font.","title":"Applies live, per terminal"}],"label":"Terminal font","mod":"termfont","order":2015,"slug":"termfont"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Color scheme setting recolors the desktop's chrome — backgrounds, text, and the accent color used for focus and highlights — using one of five built-in schemes. It never touches how a terminal's own program colors its output; a terminal keeps rendering whatever the shell or the program running in it sets, regardless of which scheme is picked here."}],"t":"p"},{"spans":[{"t":"text","v":"Find it in "},{"t":"strong","v":"Control Panel → Desktop → Color scheme"},{"t":"text","v":". The choice is browser-global, so it applies to every host you drive from this browser rather than being set per host."}],"t":"p"}],"search":"overview the color scheme setting recolors the desktop's chrome — backgrounds, text, and the accent color used for focus and highlights — using one of five built-in schemes. it never touches how a terminal's own program colors its output; a terminal keeps rendering whatever the shell or the program running in it sets, regardless of which scheme is picked here. find it in control panel → desktop → color scheme . the choice is browser-global, so it applies to every host you drive from this browser rather than being set per host.","title":"Overview"},{"body":[{"spans":[{"t":"strong","v":"Night (dark)"},{"t":"text","v":" — the default."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Day (light)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Redmond (teal)"},{"t":"text","v":" — a teal desktop with silver chrome, in the style of a classic desktop control panel."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Midnight Blue"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Sunday Orange"}],"t":"bullet"}],"search":"the five schemes night (dark) — the default. day (light) redmond (teal) — a teal desktop with silver chrome, in the style of a classic desktop control panel. midnight blue sunday orange","title":"The five schemes"},{"body":[{"spans":[{"t":"text","v":"If the Background pattern mod is also enabled, switching schemes repaints its pattern in the new scheme's colors right away — the two are meant to be used together."}],"t":"p"}],"search":"background pattern follows along if the background pattern mod is also enabled, switching schemes repaints its pattern in the new scheme's colors right away — the two are meant to be used together.","title":"Background pattern follows along"}],"label":"Color scheme","mod":"theme","order":2016,"slug":"theme"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The text editor is a CodeMirror-backed editor with syntax highlighting, used to view and edit files. It opens on the "},{"t":"strong","v":"active terminal's host"},{"t":"text","v":", at the Control Panel "},{"t":"strong","v":"Default start path"},{"t":"text","v":" when one is set for that host — otherwise at the active terminal's working directory."}],"t":"p"},{"spans":[{"t":"text","v":"The editor's content is backed by a real file on the host. Closing the editor with unsaved changes prompts you to save first; the editor window itself is not kept around, but your file on disk is safe once written."}],"t":"p"}],"search":"overview the text editor is a codemirror-backed editor with syntax highlighting, used to view and edit files. it opens on the active terminal's host , at the control panel default start path when one is set for that host — otherwise at the active terminal's working directory. the editor's content is backed by a real file on the host. closing the editor with unsaved changes prompts you to save first; the editor window itself is not kept around, but your file on disk is safe once written.","title":"Overview"}],"icon":"📄","label":"Text editor","mod":"editor","order":2020,"slug":"editor"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The file manager is a dual-pane file browser for moving around the filesystem and opening files into the text editor. Like the editor, both panes open at the Control Panel "},{"t":"strong","v":"Default start path"},{"t":"text","v":" when one is set, otherwise at the "},{"t":"strong","v":"active terminal's working directory"},{"t":"text","v":", always on the active terminal's host."}],"t":"p"}],"search":"overview the file manager is a dual-pane file browser for moving around the filesystem and opening files into the text editor. like the editor, both panes open at the control panel default start path when one is set, otherwise at the active terminal's working directory , always on the active terminal's host.","title":"Overview"}],"icon":"🗂","label":"File manager","mod":"file-manager","order":2030,"slug":"file-manager"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The task manager is a live monitor — not a saved document. It lists every running terminal / agent session across all hosts, and each entry expands to its child-process tree. From there you can "},{"t":"strong","v":"End"},{"t":"text","v":" an individual process, or "},{"t":"strong","v":"destroy"},{"t":"text","v":" the whole session — which kills its shell and closes the window."}],"t":"p"},{"spans":[{"t":"text","v":"Because it is a live monitor, the task manager is never saved: closing it just dismisses it."}],"t":"p"}],"search":"overview the task manager is a live monitor — not a saved document. it lists every running terminal / agent session across all hosts, and each entry expands to its child-process tree. from there you can end an individual process, or destroy the whole session — which kills its shell and closes the window. because it is a live monitor, the task manager is never saved: closing it just dismisses it.","title":"Overview"}],"icon":"🧰","label":"Task manager","mod":"task-manager","order":2040,"slug":"task-manager"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Git status widget puts a "},{"t":"strong","v":"⎇"},{"t":"text","v":" button and the current branch name on the title bar of every terminal whose working directory is a git repository, so you can see a shell's branch and whether it is dirty without running "},{"t":"code","v":"git status"},{"t":"text","v":" yourself. It appears whenever the Git status mod is enabled — there is no separate toggle. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"disabled by default"},{"t":"text","v":". Enabling it decorates every open terminal (and every one you open afterwards) and starts a slow background poll — each check runs a short "},{"t":"code","v":"git"},{"t":"text","v":" command in that terminal's live working directory through the broker, so it stays off until you opt in."}],"t":"p"}],"search":"overview the git status widget puts a ⎇ button and the current branch name on the title bar of every terminal whose working directory is a git repository, so you can see a shell's branch and whether it is dirty without running git status yourself. it appears whenever the git status mod is enabled — there is no separate toggle. turn it on or off from control panel → mods. the mod ships disabled by default . enabling it decorates every open terminal (and every one you open afterwards) and starts a slow background poll — each check runs a short git command in that terminal's live working directory through the broker, so it stays off until you opt in.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"When the terminal's directory is a repository, the ⎇ button lights up and the branch name is shown next to it (or "},{"t":"code","v":"detached"},{"t":"text","v":" on a detached HEAD). The label turns amber and gains a "},{"t":"strong","v":"●"},{"t":"text","v":" dirty badge — with a change count when one is known — whenever there are uncommitted changes."}],"t":"p"},{"spans":[{"t":"text","v":"The button is "},{"t":"strong","v":"muted"},{"t":"text","v":" (dimmed, no label) when the directory is not a git repository, and it is "},{"t":"strong","v":"hidden"},{"t":"text","v":" entirely on an older broker that does not provide the git endpoint."}],"t":"p"}],"search":"the button and branch label when the terminal's directory is a repository, the ⎇ button lights up and the branch name is shown next to it (or detached on a detached head). the label turns amber and gains a ● dirty badge — with a change count when one is known — whenever there are uncommitted changes. the button is muted (dimmed, no label) when the directory is not a git repository, and it is hidden entirely on an older broker that does not provide the git endpoint.","title":"The button and branch label"},{"body":[{"spans":[{"t":"text","v":"Click the ⎇ button to open a small popover anchored under it. For a repository it lists the branch (or "},{"t":"code","v":"detached HEAD"},{"t":"text","v":"), how far you are "},{"t":"strong","v":"ahead"},{"t":"text","v":" and "},{"t":"strong","v":"behind"},{"t":"text","v":" the upstream, and the counts of "},{"t":"strong","v":"staged"},{"t":"text","v":", "},{"t":"strong","v":"unstaged"},{"t":"text","v":", "},{"t":"strong","v":"untracked"},{"t":"text","v":", and "},{"t":"strong","v":"conflicts"},{"t":"text","v":" entries (conflicts are highlighted), plus whether the tree is clean or dirty. A "},{"t":"strong","v":"Refresh"},{"t":"text","v":" button re-checks on demand. Click the button again, click outside the popover, or press "},{"t":"kbd","v":"Escape"},{"t":"text","v":" to close it."}],"t":"p"},{"spans":[{"t":"text","v":"The widget refreshes on its own every 15 seconds while a terminal is open, and once more each time you open the popover."}],"t":"p"}],"search":"the status popover click the ⎇ button to open a small popover anchored under it. for a repository it lists the branch (or detached head ), how far you are ahead and behind the upstream, and the counts of staged , unstaged , untracked , and conflicts entries (conflicts are highlighted), plus whether the tree is clean or dirty. a refresh button re-checks on demand. click the button again, click outside the popover, or press escape to close it. the widget refreshes on its own every 15 seconds while a terminal is open, and once more each time you open the popover.","title":"The status popover"}],"icon":"⎇","label":"Git status","mod":"git","order":2045,"slug":"git"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Mouse mode chip puts a "},{"t":"strong","v":"🖱"},{"t":"text","v":" on a terminal's title bar for exactly as long as that terminal has mouse reporting switched on, and names the way out in its tooltip. It appears whenever the Mouse mode chip mod is enabled — there is no separate toggle. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"enabled"},{"t":"text","v":", because it costs nothing to leave on: the chip is invisible in an ordinary shell and only shows up when a full-screen app takes the mouse."}],"t":"p"}],"search":"overview the mouse mode chip puts a 🖱 on a terminal's title bar for exactly as long as that terminal has mouse reporting switched on, and names the way out in its tooltip. it appears whenever the mouse mode chip mod is enabled — there is no separate toggle. turn it on or off from control panel → mods. the mod ships enabled , because it costs nothing to leave on: the chip is invisible in an ordinary shell and only shows up when a full-screen app takes the mouse.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Full-screen apps — "},{"t":"code","v":"lazygit"},{"t":"text","v":", "},{"t":"code","v":"btop"},{"t":"text","v":", "},{"t":"code","v":"mc"},{"t":"text","v":", "},{"t":"code","v":"htop"},{"t":"text","v":", most agent CLIs — ask the terminal to report mouse activity so they can implement their own clicking and scrolling. From that moment the terminal hands clicks, drags and the wheel to the app instead of the browser, and "},{"t":"strong","v":"dragging across the screen no longer selects anything"},{"t":"text","v":". Nothing normally tells you that happened; the selection simply stops working, usually minutes after the app printed its splash screen."}],"t":"p"},{"spans":[{"t":"text","v":"The chip is the missing signal. It is on precisely while reporting is on and gone the moment it is switched off — when you quit the app, or when it drops back to a plain prompt."}],"t":"p"},{"spans":[{"t":"text","v":"It reports the terminal's state, not the app's intent, and says so: "},{"t":"strong","v":"\"Mouse reporting is on\""},{"t":"text","v":". Those are not the same claim. A TUI that crashed or was killed, or one that simply forgets to clean up — and even a "},{"t":"code","v":"cat"},{"t":"text","v":" of a file that happens to contain the escape sequence — leaves reporting on with nothing listening. The chip stays up, correctly, because your dragging really is being swallowed; press "},{"t":"kbd","v":"Enter"},{"t":"text","v":" at the shell prompt or run "},{"t":"code","v":"reset"},{"t":"text","v":" to clear it."}],"t":"p"}],"search":"why the chip matters full-screen apps — lazygit , btop , mc , htop , most agent clis — ask the terminal to report mouse activity so they can implement their own clicking and scrolling. from that moment the terminal hands clicks, drags and the wheel to the app instead of the browser, and dragging across the screen no longer selects anything . nothing normally tells you that happened; the selection simply stops working, usually minutes after the app printed its splash screen. the chip is the missing signal. it is on precisely while reporting is on and gone the moment it is switched off — when you quit the app, or when it drops back to a plain prompt. it reports the terminal's state, not the app's intent, and says so: \"mouse reporting is on\" . those are not the same claim. a tui that crashed or was killed, or one that simply forgets to clean up — and even a cat of a file that happens to contain the escape sequence — leaves reporting on with nothing listening. the chip stays up, correctly, because your dragging really is being swallowed; press enter at the shell prompt or run reset to clear it.","title":"Why the chip matters"},{"body":[{"spans":[{"t":"text","v":"Hover the chip and it tells you how much of the mouse was asked for (clicks, clicks and drags, or every mouse move) and how to work around it:"}],"t":"p"},{"spans":[{"t":"code","v":"Shift"},{"t":"strong","v":"-drag"},{"t":"text","v":" selects text anyway — the terminal takes that gesture before the app sees it. On macOS hold "},{"t":"code","v":"Option"},{"t":"text","v":" instead; "},{"t":"code","v":"Shift"},{"t":"text","v":" is not the Mac gesture."}],"t":"bullet"},{"spans":[{"t":"code","v":"Shift+scroll"},{"t":"text","v":" scrolls the terminal instead of the app. An app drawing on the alternate screen has no scrollback of its own to reach, so there this does nothing and the app's own scroll keys are what you want."}],"t":"bullet"},{"spans":[{"t":"kbd","v":"Ctrl+Shift+C"},{"t":"text","v":" copies the selection, since plain "},{"t":"kbd","v":"Ctrl+C"},{"t":"text","v":" belongs to the app."}],"t":"bullet"},{"spans":[{"t":"text","v":"See Keyboard Shortcuts for the full copy-and-paste story, including what pastes on a plain-http origin."}],"t":"p"}],"search":"the tooltip hover the chip and it tells you how much of the mouse was asked for (clicks, clicks and drags, or every mouse move) and how to work around it: shift -drag selects text anyway — the terminal takes that gesture before the app sees it. on macos hold option instead; shift is not the mac gesture. shift+scroll scrolls the terminal instead of the app. an app drawing on the alternate screen has no scrollback of its own to reach, so there this does nothing and the app's own scroll keys are what you want. ctrl+shift+c copies the selection, since plain ctrl+c belongs to the app. see keyboard shortcuts for the full copy-and-paste story, including what pastes on a plain-http origin. keyboard shortcuts","title":"The tooltip"},{"body":[{"spans":[{"t":"text","v":"The chip has no click action, no menu and no settings — it is a status light. It never changes what the app receives: enabling or disabling this mod cannot affect mouse reporting, only whether you are told about it."}],"t":"p"}],"search":"what it does not do the chip has no click action, no menu and no settings — it is a status light. it never changes what the app receives: enabling or disabling this mod cannot affect mouse reporting, only whether you are told about it.","title":"What it does not do"}],"icon":"🖱","label":"Mouse mode chip","mod":"mousemode","order":2047,"slug":"mousemode"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The clock chip shows the date and time at the bottom-right of the taskbar. It appears whenever the Clock mod is enabled — there is no separate toggle. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The Clock mod also contributes a "},{"t":"strong","v":"Time zone"},{"t":"text","v":" box to its Control Panel → Mods section. Start typing a city or region (for example "},{"t":"code","v":"tokyo"},{"t":"text","v":") and it suggests matching IANA zones, or type a zone name in full — the suggestions are a shortcut, not a limit, so any zone your browser understands is accepted. Leave it blank to follow the browser you are viewing from — the default. The chosen zone is browser-global, so it applies across every host you drive from this browser."}],"t":"p"},{"spans":[{"t":"text","v":"A zone your browser does not recognise is refused: a message appears under the box, what you typed stays there so you can fix it, and the saved zone is left alone. The reverse also holds — a zone chosen in one browser is kept as-is by every other browser, even one whose engine cannot offer it in the suggestion list. Such a browser shows its own local time in the chip while the saved zone stays untouched, so going back to the first browser still reads the zone you picked."}],"t":"p"}],"search":"overview the clock chip shows the date and time at the bottom-right of the taskbar. it appears whenever the clock mod is enabled — there is no separate toggle. turn it on or off from control panel → mods. the clock mod also contributes a time zone box to its control panel → mods section. start typing a city or region (for example tokyo ) and it suggests matching iana zones, or type a zone name in full — the suggestions are a shortcut, not a limit, so any zone your browser understands is accepted. leave it blank to follow the browser you are viewing from — the default. the chosen zone is browser-global, so it applies across every host you drive from this browser. a zone your browser does not recognise is refused: a message appears under the box, what you typed stays there so you can fix it, and the saved zone is left alone. the reverse also holds — a zone chosen in one browser is kept as-is by every other browser, even one whose engine cannot offer it in the suggestion list. such a browser shows its own local time in the chip while the saved zone stays untouched, so going back to the first browser still reads the zone you picked.","title":"Overview"}],"icon":"🕐","label":"Clock","mod":"clock","order":2050,"slug":"clock"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Browserland ships a searchable interface guide built into the app. Open it from the "},{"t":"strong","v":"?"},{"t":"text","v":" chip at the bottom-right of the taskbar, which appears whenever the Help mod is enabled — there is no separate toggle; turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"Type in the search box to filter every entry live."}],"t":"bullet"},{"spans":[{"t":"text","v":"The guide covers layout modes, snapping, tabs, splits, workspaces, the taskbar, MCP access, and more — the same topics as the wiki, available without leaving the desktop."}],"t":"bullet"},{"spans":[{"t":"text","v":"You can also bind a key to open it: the guide is the "},{"t":"strong","v":"Toggle help"},{"t":"text","v":" action, which is "},{"t":"strong","v":"unbound by default"},{"t":"text","v":". Assign a combo under "},{"t":"strong","v":"Control Panel → Keyboard shortcuts"},{"t":"text","v":" (see Keyboard-Shortcuts)."}],"t":"p"}],"search":"overview browserland ships a searchable interface guide built into the app. open it from the ? chip at the bottom-right of the taskbar, which appears whenever the help mod is enabled — there is no separate toggle; turn it on or off from control panel → mods. type in the search box to filter every entry live. the guide covers layout modes, snapping, tabs, splits, workspaces, the taskbar, mcp access, and more — the same topics as the wiki, available without leaving the desktop. you can also bind a key to open it: the guide is the toggle help action, which is unbound by default . assign a combo under control panel → keyboard shortcuts (see keyboard-shortcuts). keyboard shortcuts","title":"Overview"}],"icon":"❓","label":"Help","mod":"help","order":2060,"slug":"help"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The AI status chip shows the worst-case health of the major AI providers at a glance, so when a "},{"t":"code","v":"/loop"},{"t":"text","v":" or an MCP call starts failing you can tell whether it's you or the provider. It appears whenever the AI status mod is enabled — there is no separate toggle. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"disabled by default"},{"t":"text","v":". Enabling it lets the broker fetch each provider's public status page (Anthropic, OpenAI, Cohere, and GitHub Copilot) through its allowlisted "},{"t":"code","v":"/status/fetch"},{"t":"text","v":" proxy — which makes the broker's egress IP visible to those status hosts — so it stays off until you opt in. The broker only ever reaches that fixed allowlist of provider status pages; it never follows a caller-supplied URL."}],"t":"p"},{"spans":[{"t":"text","v":"The chip aggregates the worst indicator across the providers you have enabled: green when all are operational, amber for a minor or major issue, red for a critical outage, and grey when a provider can't be reached. Click it to open the "},{"t":"strong","v":"AI status"},{"t":"text","v":" window, which lists each provider with a colored dot, its current status, and any active incidents, plus a "},{"t":"strong","v":"Refresh"},{"t":"text","v":" button and a last-checked time."}],"t":"p"},{"spans":[{"t":"text","v":"If the broker itself has status checks switched off, the chip and window say so plainly instead of showing four unreachable providers — that is a broker-side setting an operator flips, not something wrong with any provider. The window offers the switch right where the refusal is shown — \"Switch on status checks on this broker\" — and granting it repaints immediately, with no reload needed."}],"t":"p"},{"spans":[{"t":"text","v":"The mod's Control Panel → Mods section has a checkbox per provider (so you can monitor only the ones you use) and a poll-interval selector (30 seconds to 5 minutes). Those settings are browser-global, shared across every host you drive from this browser."}],"t":"p"}],"search":"overview the ai status chip shows the worst-case health of the major ai providers at a glance, so when a /loop or an mcp call starts failing you can tell whether it's you or the provider. it appears whenever the ai status mod is enabled — there is no separate toggle. turn it on or off from control panel → mods. the mod ships disabled by default . enabling it lets the broker fetch each provider's public status page (anthropic, openai, cohere, and github copilot) through its allowlisted /status/fetch proxy — which makes the broker's egress ip visible to those status hosts — so it stays off until you opt in. the broker only ever reaches that fixed allowlist of provider status pages; it never follows a caller-supplied url. the chip aggregates the worst indicator across the providers you have enabled: green when all are operational, amber for a minor or major issue, red for a critical outage, and grey when a provider can't be reached. click it to open the ai status window, which lists each provider with a colored dot, its current status, and any active incidents, plus a refresh button and a last-checked time. if the broker itself has status checks switched off, the chip and window say so plainly instead of showing four unreachable providers — that is a broker-side setting an operator flips, not something wrong with any provider. the window offers the switch right where the refusal is shown — \"switch on status checks on this broker\" — and granting it repaints immediately, with no reload needed. the mod's control panel → mods section has a checkbox per provider (so you can monitor only the ones you use) and a poll-interval selector (30 seconds to 5 minutes). those settings are browser-global, shared across every host you drive from this browser.","title":"Overview"}],"icon":"🩺","label":"AI status","mod":"aistatus","order":2075,"slug":"aistatus"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The clipboard history keeps a rolling list of the last 20 things you have copied or pasted, in both directions, so you can grab an earlier value back after a newer copy has overwritten it. It appears whenever the Clipboard mod is enabled — there is no separate toggle. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"disabled by default"},{"t":"text","v":". Clipboards carry secrets — passwords, tokens, keys — so it captures "},{"t":"strong","v":"nothing"},{"t":"text","v":" until you opt in, and the moment you disable it capturing stops again. Enable it from Control Panel → Mods."}],"t":"p"}],"search":"overview the clipboard history keeps a rolling list of the last 20 things you have copied or pasted, in both directions, so you can grab an earlier value back after a newer copy has overwritten it. it appears whenever the clipboard mod is enabled — there is no separate toggle. turn it on or off from control panel → mods. the mod ships disabled by default . clipboards carry secrets — passwords, tokens, keys — so it captures nothing until you opt in, and the moment you disable it capturing stops again. enable it from control panel → mods.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Open the Clipboard window from the desktop "},{"t":"strong","v":"+"},{"t":"text","v":" (launch) menu → "},{"t":"strong","v":"📋 Clipboard"},{"t":"text","v":", or click the "},{"t":"strong","v":"📋"},{"t":"text","v":" chip in the taskbar near the clock. There is only one window at a time — opening it again (from either entry point) just focuses the existing window rather than stacking another. For more on the launch menu, see Taskbar."}],"t":"p"}],"search":"opening the window open the clipboard window from the desktop + (launch) menu → 📋 clipboard , or click the 📋 chip in the taskbar near the clock. there is only one window at a time — opening it again (from either entry point) just focuses the existing window rather than stacking another. for more on the launch menu, see taskbar. taskbar","title":"Opening the window"},{"body":[{"spans":[{"t":"text","v":"While enabled, it records each copy and paste as you work:"}],"t":"p"},{"spans":[{"t":"text","v":"Text "},{"t":"strong","v":"copied out"},{"t":"text","v":" of a terminal — by selecting it (auto-copy) or with "},{"t":"kbd","v":"Ctrl+Shift+C"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"Text "},{"t":"strong","v":"pasted in"},{"t":"text","v":" — with "},{"t":"kbd","v":"Ctrl+V"},{"t":"text","v":", a right-click, or the context-menu "},{"t":"strong","v":"Paste"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"An "},{"t":"strong","v":"image pasted in"},{"t":"text","v":" ("},{"t":"kbd","v":"Ctrl+V"},{"t":"text","v":", "},{"t":"kbd","v":"Alt+V"},{"t":"text","v":", or right-click) is recorded as the uploaded file path that lands in the prompt, not the image itself."}],"t":"bullet"},{"spans":[{"t":"text","v":"Each entry shows a direction arrow ("},{"t":"strong","v":"→"},{"t":"text","v":" copied out, "},{"t":"strong","v":"←"},{"t":"text","v":" pasted in), the time it was captured, and a one-line preview of the text. Repeating an identical copy or paste just refreshes the existing entry's time instead of adding a duplicate, and only the most recent 20 entries are kept."}],"t":"p"}],"search":"what it captures while enabled, it records each copy and paste as you work: text copied out of a terminal — by selecting it (auto-copy) or with ctrl+shift+c . text pasted in — with ctrl+v , a right-click, or the context-menu paste . an image pasted in ( ctrl+v , alt+v , or right-click) is recorded as the uploaded file path that lands in the prompt, not the image itself. each entry shows a direction arrow ( → copied out, ← pasted in), the time it was captured, and a one-line preview of the text. repeating an identical copy or paste just refreshes the existing entry's time instead of adding a duplicate, and only the most recent 20 entries are kept.","title":"What it captures"},{"body":[{"spans":[{"t":"text","v":"Click any entry to copy its full text back to the clipboard — handy when a newer copy has clobbered something you still need. "},{"t":"strong","v":"Clear history"},{"t":"text","v":" empties the list immediately, in every open clipboard window at once."}],"t":"p"}],"search":"re-copying and clearing click any entry to copy its full text back to the clipboard — handy when a newer copy has clobbered something you still need. clear history empties the list immediately, in every open clipboard window at once.","title":"Re-copying and clearing"},{"body":[{"spans":[{"t":"text","v":"The history lives in memory only. It is never written to disk or browser storage, so reloading the page clears it and it never restores across sessions — the safest posture for text that may contain secrets."}],"t":"p"}],"search":"nothing is saved the history lives in memory only. it is never written to disk or browser storage, so reloading the page clears it and it never restores across sessions — the safest posture for text that may contain secrets.","title":"Nothing is saved"}],"icon":"📋","label":"Clipboard","mod":"clipboard","order":2080,"slug":"clipboard"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Scratchpad is a notes window whose text is stored on the broker, not in your browser. Because the notes live server-side, they are shared across every browser you open on the same broker and they survive a page reload or a cache clear. It is a single window: opening it again from the launch menu just focuses the one that is already open. Your notes are organized as tabs, each a small code editor, and every save is kept in a revision history you can browse and restore."}],"t":"p"}],"search":"overview the scratchpad is a notes window whose text is stored on the broker, not in your browser. because the notes live server-side, they are shared across every browser you open on the same broker and they survive a page reload or a cache clear. it is a single window: opening it again from the launch menu just focuses the one that is already open. your notes are organized as tabs, each a small code editor, and every save is kept in a revision history you can browse and restore.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Open the Scratchpad from the desktop "},{"t":"strong","v":"+"},{"t":"text","v":" (launch) menu → "},{"t":"strong","v":"Scratchpad"},{"t":"text","v":". There is only ever one Scratchpad window; launching it again focuses the existing one rather than stacking another. For more on the launch menu, see Taskbar."}],"t":"p"},{"spans":[{"t":"text","v":"The Scratchpad shares the text editor's code-editing engine, so it is only available when the "},{"t":"strong","v":"Text editor"},{"t":"text","v":" mod is enabled — disabling the editor also hides the Scratchpad. Both are on by default."}],"t":"p"}],"search":"opening the window open the scratchpad from the desktop + (launch) menu → scratchpad . there is only ever one scratchpad window; launching it again focuses the existing one rather than stacking another. for more on the launch menu, see taskbar. the scratchpad shares the text editor's code-editing engine, so it is only available when the text editor mod is enabled — disabling the editor also hides the scratchpad. both are on by default. taskbar","title":"Opening the window"},{"body":[{"spans":[{"t":"text","v":"Each note is a tab across the top of the window:"}],"t":"p"},{"spans":[{"t":"strong","v":"New tab"},{"t":"text","v":" — click the "},{"t":"strong","v":"+"},{"t":"text","v":" in the toolbar. A fresh "},{"t":"strong","v":"Notes"},{"t":"text","v":" tab opens (up to 20 tabs)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Switch"},{"t":"text","v":" — click a tab."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Rename"},{"t":"text","v":" — double-click a tab's name, type the new name, and press "},{"t":"strong","v":"Enter"},{"t":"text","v":" (or "},{"t":"strong","v":"Escape"},{"t":"text","v":" to cancel)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Close"},{"t":"text","v":" — click the "},{"t":"strong","v":"×"},{"t":"text","v":" on a tab. The last remaining tab cannot be closed, so there is always at least one note."}],"t":"bullet"},{"spans":[{"t":"text","v":"Which tab is active is remembered per browser, so it is not counted as a content change and does not add a revision."}],"t":"p"}],"search":"tabs each note is a tab across the top of the window: new tab — click the + in the toolbar. a fresh notes tab opens (up to 20 tabs). switch — click a tab. rename — double-click a tab's name, type the new name, and press enter (or escape to cancel). close — click the × on a tab. the last remaining tab cannot be closed, so there is always at least one note. which tab is active is remembered per browser, so it is not counted as a content change and does not add a revision.","title":"Tabs"},{"body":[{"spans":[{"t":"text","v":"Typing autosaves after a short pause — there is no Save button, though "},{"t":"strong","v":"Ctrl+S"},{"t":"text","v":" forces an immediate save. Saves are content-only: switching tabs or moving the window never creates a revision. Note text is never written to browser storage, only to the broker."}],"t":"p"},{"spans":[{"t":"text","v":"Only the "},{"t":"strong","v":"active"},{"t":"text","v":" browser can save. If you open the Scratchpad in a second browser while another is active, a banner appears and the notes are read-only there — you can still read them, but edits will not be saved until that browser becomes the active one. Click "},{"t":"strong","v":"Take over"},{"t":"text","v":" in the banner (after making the browser active) to save your local changes."}],"t":"p"}],"search":"saving typing autosaves after a short pause — there is no save button, though ctrl+s forces an immediate save. saves are content-only: switching tabs or moving the window never creates a revision. note text is never written to browser storage, only to the broker. only the active browser can save. if you open the scratchpad in a second browser while another is active, a banner appears and the notes are read-only there — you can still read them, but edits will not be saved until that browser becomes the active one. click take over in the banner (after making the browser active) to save your local changes.","title":"Saving"},{"body":[{"spans":[{"t":"text","v":"Click "},{"t":"strong","v":"History"},{"t":"text","v":" in the toolbar to browse past revisions. Each save keeps the previous version in a bounded, newest-first ring (the oldest revisions eventually scroll off). Select a revision to preview its tabs and text, then click "},{"t":"strong","v":"Restore"},{"t":"text","v":" to bring it back — restoring writes the old content as a brand-new save, so nothing is lost and you can restore again if you change your mind."}],"t":"p"}],"search":"history click history in the toolbar to browse past revisions. each save keeps the previous version in a bounded, newest-first ring (the oldest revisions eventually scroll off). select a revision to preview its tabs and text, then click restore to bring it back — restoring writes the old content as a brand-new save, so nothing is lost and you can restore again if you change your mind.","title":"History"}],"icon":"🗒","label":"Scratchpad","mod":"scratchpad","order":2085,"slug":"scratchpad"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The update chip tells you whether the brokers you have configured are current with upstream, without opening a shell to run "},{"t":"code","v":"git fetch"},{"t":"text","v":". It checks "},{"t":"strong","v":"every configured host"},{"t":"text","v":", not just the serving broker, and each reports its own state. Turn it on or off from Control Panel → Mods."}],"t":"p"},{"spans":[{"t":"text","v":"The mod ships "},{"t":"strong","v":"disabled by default"},{"t":"text","v":", and each broker has a second switch of its own governing whether that machine may reach GitHub at all. The mod's toggle decides what your browser draws; the broker's decides whether the machine makes an outbound request. When it is on, the broker makes the request once and caches it for a day, so every tab and every browser shares one call rather than each burning GitHub's rate limit."}],"t":"p"},{"spans":[{"t":"strong","v":"Check now"},{"t":"text","v":" is what re-asks GitHub before that day is up — the periodic poll never does, so without it a commit pushed in the last 24 hours stays invisible (and an apply has no fresh commit to act on). Because it bypasses the daily cache it also bypasses the rate-limit strategy, so the broker limits it: one re-ask per minute, and six per hour. When it declines, you still get an answer — the one it already had — and the window says so and roughly how long until it will re-ask. That refusal is never dressed up as a fresh result. Two of the reasons are ordinary pacing; the third is GitHub itself rate-limiting that machine, and clicking harder is what extends it."}],"t":"p"},{"spans":[{"t":"strong","v":"Turning the mod on asks the serving broker to open every switch your click is allowed to open."},{"t":"text","v":" Ticking it in Control Panel → Mods is treated as your decision that this machine may check for updates — and, in the same request, that it may apply one and restart itself to run it, for whichever of those two this broker's config has not already claimed. The broker applies the grant immediately — no config file, no restart — but strictly per gate, never a blanket \"all three\": a gate a config key already owns ("},{"t":"code","v":"update_check_enabled"},{"t":"text","v":", "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":", or "},{"t":"code","v":"restart_enabled"},{"t":"text","v":", present as either "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":") is left exactly as that file says, and only the gates still undecided open. Only that one click counts: a page reload, a preference synced in from another browser, and a mod pinned on by another operator all leave every switch exactly as it was, so nothing here can make a machine start reaching the internet, applying an update, or restarting without somebody choosing it."}],"t":"p"},{"spans":[{"t":"text","v":"If your broker was already checking-off when you upgraded — its mod preference was set before this existed, so no click ever happens — the "},{"t":"strong","v":"Update check"},{"t":"text","v":" window carries that same grant as "},{"t":"strong","v":"two rows"},{"t":"text","v":": "},{"t":"strong","v":"Enable checking on this broker"},{"t":"text","v":", and "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":", which grants applying and restarting together in one switch (see Restarting the broker, below, for why the two travel as a pair). Each row is where you switch its own grant back off; turning the mod off does not touch either one — it only stops your browser drawing the chip. Both grants are stored beside the broker's state file, so they survive restarts."}],"t":"p"},{"spans":[{"t":"strong","v":"Any broker in the list can be switched on, not just the one serving the page"},{"t":"text","v":" — that is the case that matters when you administer a fleet remotely: the machine that needs switching on is usually one you have no desktop session on. Both rows work this way. Enabling a remote broker's checking asks you to confirm first — it names the machine and its address, because the rows look alike and the first check discloses that machine's address to GitHub in a way switching it off later does not undo. Enabling a remote broker's self-update row asks for its own confirmation, titled "},{"t":"strong","v":"Let that broker update itself?"},{"t":"text","v":" — it names that machine and its address too, and spells out plainly what the grant is: that machine will download and run code from GitHub and restart itself."}],"t":"p"},{"spans":[{"t":"text","v":"A checking row shows no control when there is nothing to do: when that broker's config owns the setting, when it is too old to have the switch, when it never answered, or when it is already checking because its own config says so. A broker running the build that first shipped this feature is also left alone — it accepts the change only from its own desktop, so the row says to update it rather than offering a button that would fail. The self-update row behaves differently: it stays visible on any broker new enough to report anything at all, and instead says why it cannot be switched — naming the config key that owns whichever gate is blocking it, or, on a broker that predates the row entirely, asking you to update that broker."}],"t":"p"},{"spans":[{"t":"text","v":"An operator who wants a gate taken out of the GUI's hands entirely puts its matching key in that broker's config file: "},{"t":"code","v":"update_check_enabled"},{"t":"text","v":" for checking, or "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":" and "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" together for the self-update row (the row needs both open to switch on, so pinning either one alone keeps the row shut). Naming a key there — either way, "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":" — makes that file authoritative for that one gate: the row goes dead and names the file, and editing it and restarting stays the way the setting changes. Owning one gate never reaches into another — a config that pins checking "},{"t":"code","v":"true"},{"t":"text","v":" does not grant applying, and a config that pins applying "},{"t":"code","v":"false"},{"t":"text","v":" does not stop the row from granting checking, or, once its own config allows it, restarting. All three keys are absent from the shipped example configs on purpose, so a normal install leaves every gate the browser's to decide."}],"t":"p"}],"search":"overview the update chip tells you whether the brokers you have configured are current with upstream, without opening a shell to run git fetch . it checks every configured host , not just the serving broker, and each reports its own state. turn it on or off from control panel → mods. the mod ships disabled by default , and each broker has a second switch of its own governing whether that machine may reach github at all. the mod's toggle decides what your browser draws; the broker's decides whether the machine makes an outbound request. when it is on, the broker makes the request once and caches it for a day, so every tab and every browser shares one call rather than each burning github's rate limit. check now is what re-asks github before that day is up — the periodic poll never does, so without it a commit pushed in the last 24 hours stays invisible (and an apply has no fresh commit to act on). because it bypasses the daily cache it also bypasses the rate-limit strategy, so the broker limits it: one re-ask per minute, and six per hour. when it declines, you still get an answer — the one it already had — and the window says so and roughly how long until it will re-ask. that refusal is never dressed up as a fresh result. two of the reasons are ordinary pacing; the third is github itself rate-limiting that machine, and clicking harder is what extends it. turning the mod on asks the serving broker to open every switch your click is allowed to open. ticking it in control panel → mods is treated as your decision that this machine may check for updates — and, in the same request, that it may apply one and restart itself to run it, for whichever of those two this broker's config has not already claimed. the broker applies the grant immediately — no config file, no restart — but strictly per gate, never a blanket \"all three\": a gate a config key already owns ( update_check_enabled , update_apply_enabled , or restart_enabled , present as either true or false ) is left exactly as that file says, and only the gates still undecided open. only that one click counts: a page reload, a preference synced in from another browser, and a mod pinned on by another operator all leave every switch exactly as it was, so nothing here can make a machine start reaching the internet, applying an update, or restarting without somebody choosing it. if your broker was already checking-off when you upgraded — its mod preference was set before this existed, so no click ever happens — the update check window carries that same grant as two rows : enable checking on this broker , and allow this broker to update itself , which grants applying and restarting together in one switch (see restarting the broker, below, for why the two travel as a pair). each row is where you switch its own grant back off; turning the mod off does not touch either one — it only stops your browser drawing the chip. both grants are stored beside the broker's state file, so they survive restarts. any broker in the list can be switched on, not just the one serving the page — that is the case that matters when you administer a fleet remotely: the machine that needs switching on is usually one you have no desktop session on. both rows work this way. enabling a remote broker's checking asks you to confirm first — it names the machine and its address, because the rows look alike and the first check discloses that machine's address to github in a way switching it off later does not undo. enabling a remote broker's self-update row asks for its own confirmation, titled let that broker update itself? — it names that machine and its address too, and spells out plainly what the grant is: that machine will download and run code from github and restart itself. a checking row shows no control when there is nothing to do: when that broker's config owns the setting, when it is too old to have the switch, when it never answered, or when it is already checking because its own config says so. a broker running the build that first shipped this feature is also left alone — it accepts the change only from its own desktop, so the row says to update it rather than offering a button that would fail. the self-update row behaves differently: it stays visible on any broker new enough to report anything at all, and instead says why it cannot be switched — naming the config key that owns whichever gate is blocking it, or, on a broker that predates the row entirely, asking you to update that broker. an operator who wants a gate taken out of the gui's hands entirely puts its matching key in that broker's config file: update_check_enabled for checking, or update_apply_enabled and restart_enabled together for the self-update row (the row needs both open to switch on, so pinning either one alone keeps the row shut). naming a key there — either way, true or false — makes that file authoritative for that one gate: the row goes dead and names the file, and editing it and restarting stays the way the setting changes. owning one gate never reaches into another — a config that pins checking true does not grant applying, and a config that pins applying false does not stop the row from granting checking, or, once its own config allows it, restarting. all three keys are absent from the shipped example configs on purpose, so a normal install leaves every gate the browser's to decide.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"The taskbar chip is one "},{"t":"strong","v":"aggregate"},{"t":"text","v":" showing the worst state across all configured brokers, with a tooltip listing every broker and its own state. With a single broker configured it looks exactly as it did before. The chip reads "},{"t":"strong","v":"\"up to date\" only when every configured broker actually answered and every one is current."},{"t":"text","v":" A broker that could not be checked never counts as current, so the phrase \"up to date\" means the whole fleet is known to be clean."}],"t":"p"},{"spans":[{"t":"text","v":"The chip has three states. "},{"t":"strong","v":"Up to date"},{"t":"text","v":" means every broker's commit matches upstream. "},{"t":"strong","v":"N behind"},{"t":"text","v":" (or "},{"t":"strong","v":"N ahead"},{"t":"text","v":", or "},{"t":"strong","v":"N unchecked"},{"t":"text","v":") means some brokers have that issue — the count is shown because different brokers can be in different states at the same time. "},{"t":"strong","v":"Version ?"},{"t":"text","v":" means at least one broker could not be checked, and the window explains why for each one."}],"t":"p"},{"spans":[{"t":"text","v":"By default the chip hides itself while the build is current across every broker, so it only takes up taskbar space when there is something to say; an unknown never hides."}],"t":"p"},{"spans":[{"t":"text","v":"A broker you have "},{"t":"strong","v":"hidden"},{"t":"text","v":" still counts here, unlike on the host status badge: it still colors the chip, still counts toward \"N behind\" / \"N ahead\" / \"N unchecked\", and its line in the tooltip is suffixed "},{"t":"code","v":"— hidden"},{"t":"text","v":" rather than dropped. That is the opposite of the host badge's rule (a broker you hide yourself is excluded from that badge entirely — see Hosts-and-Multi-Browser), and the divergence is deliberate: hiding a broker parks it on the desktop, it does not make that broker's build any less stale, so this chip may stop claiming a fault for a hidden broker but must never stop reporting one."}],"t":"p"}],"search":"the aggregate chip the taskbar chip is one aggregate showing the worst state across all configured brokers, with a tooltip listing every broker and its own state. with a single broker configured it looks exactly as it did before. the chip reads \"up to date\" only when every configured broker actually answered and every one is current. a broker that could not be checked never counts as current, so the phrase \"up to date\" means the whole fleet is known to be clean. the chip has three states. up to date means every broker's commit matches upstream. n behind (or n ahead , or n unchecked ) means some brokers have that issue — the count is shown because different brokers can be in different states at the same time. version ? means at least one broker could not be checked, and the window explains why for each one. by default the chip hides itself while the build is current across every broker, so it only takes up taskbar space when there is something to say; an unknown never hides. a broker you have hidden still counts here, unlike on the host status badge: it still colors the chip, still counts toward \"n behind\" / \"n ahead\" / \"n unchecked\", and its line in the tooltip is suffixed — hidden rather than dropped. that is the opposite of the host badge's rule (a broker you hide yourself is excluded from that badge entirely — see hosts-and-multi-browser), and the divergence is deliberate: hiding a broker parks it on the desktop, it does not make that broker's build any less stale, so this chip may stop claiming a fault for a hidden broker but must never stop reporting one. hosts and multi browser","title":"The aggregate chip"},{"body":[{"spans":[{"t":"text","v":"A peer that cannot answer is distinguished by "},{"t":"strong","v":"why"},{"t":"text","v":", and these are five genuinely different things that used to collapse into one \"unknown\":"}],"t":"p"},{"spans":[{"t":"strong","v":"too old to check"},{"t":"text","v":" — that broker predates the update route, so it is deliberately not asked (the request would fail its cross-origin preflight and arrive here as an unexplained network error, indistinguishable from a machine that is asleep)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"checking not enabled there"},{"t":"text","v":" — it has the route but nobody has switched checking on there (the broker answers 503 to tell us so); this is a choice, not a failure, and it is switched on from that broker's own desktop"}],"t":"bullet"},{"spans":[{"t":"strong","v":"password refused"},{"t":"text","v":" — it did not accept our saved password"}],"t":"bullet"},{"spans":[{"t":"strong","v":"did not answer"},{"t":"text","v":" — it could not be reached"}],"t":"bullet"},{"spans":[{"t":"strong","v":"no answer — asleep, or too old to check"},{"t":"text","v":" — a "},{"t":"strong","v":"headless"},{"t":"text","v":" broker (one that serves no desktop page) that publishes no update capability. An ordinary broker's own reported version is enough to prove it predates the route, but a headless peer answers with an empty mod list regardless of what routes it actually has, so there is nothing here to prove it either way — it is either too old to have the check, or new enough to have it and simply asleep. Both die in the same cross-origin preflight and come back as the same opaque network error, so rather than guess, the chip and the window say both are possible"}],"t":"bullet"},{"spans":[{"t":"text","v":"Plus the existing failure reasons for when a check does run and fails: "},{"t":"strong","v":"offline"},{"t":"text","v":" (GitHub unreachable), "},{"t":"strong","v":"rate-limited"},{"t":"text","v":", "},{"t":"strong","v":"no git checkout"},{"t":"text","v":", or the comparison "},{"t":"strong","v":"unavailable"},{"t":"text","v":" (committed but never pushed, garbage-collected, or the branch force-pushed)."}],"t":"p"}],"search":"why a broker might not answer a peer that cannot answer is distinguished by why , and these are five genuinely different things that used to collapse into one \"unknown\": too old to check — that broker predates the update route, so it is deliberately not asked (the request would fail its cross-origin preflight and arrive here as an unexplained network error, indistinguishable from a machine that is asleep) checking not enabled there — it has the route but nobody has switched checking on there (the broker answers 503 to tell us so); this is a choice, not a failure, and it is switched on from that broker's own desktop password refused — it did not accept our saved password did not answer — it could not be reached no answer — asleep, or too old to check — a headless broker (one that serves no desktop page) that publishes no update capability. an ordinary broker's own reported version is enough to prove it predates the route, but a headless peer answers with an empty mod list regardless of what routes it actually has, so there is nothing here to prove it either way — it is either too old to have the check, or new enough to have it and simply asleep. both die in the same cross-origin preflight and come back as the same opaque network error, so rather than guess, the chip and the window say both are possible plus the existing failure reasons for when a check does run and fails: offline (github unreachable), rate-limited , no git checkout , or the comparison unavailable (committed but never pushed, garbage-collected, or the branch force-pushed).","title":"Why a broker might not answer"},{"body":[{"spans":[{"t":"text","v":"Click the chip to open the "},{"t":"strong","v":"Update check"},{"t":"text","v":" window. It shows every configured broker as one row, its own state in words, and its build and commit. The upstream build is shown once (it is the same constant for every broker). The window shows when the checks last ran — the oldest of the batch, because a broker that answered an hour ago is not as fresh as one that just answered."}],"t":"p"},{"spans":[{"t":"text","v":"When a broker did not answer, the window explains why in full sentences. A manual-update note — stop the broker, "},{"t":"code","v":"git pull --ff-only"},{"t":"text","v":" in its checkout on its own machine, reinstall dependencies if "},{"t":"code","v":"pyproject.toml"},{"t":"text","v":" changed, then start it again and reload the page — appears only for brokers that are behind "},{"t":"strong","v":"and"},{"t":"text","v":" have no working one-click "},{"t":"strong","v":"Update…"},{"t":"text","v":" button (see below); it names exactly those brokers. A behind broker whose row carries a live button gets no by-hand instructions, because it doesn't need any."}],"t":"p"}],"search":"the detail window click the chip to open the update check window. it shows every configured broker as one row, its own state in words, and its build and commit. the upstream build is shown once (it is the same constant for every broker). the window shows when the checks last ran — the oldest of the batch, because a broker that answered an hour ago is not as fresh as one that just answered. when a broker did not answer, the window explains why in full sentences. a manual-update note — stop the broker, git pull --ff-only in its checkout on its own machine, reinstall dependencies if pyproject.toml changed, then start it again and reload the page — appears only for brokers that are behind and have no working one-click update… button (see below); it names exactly those brokers. a behind broker whose row carries a live button gets no by-hand instructions, because it doesn't need any.","title":"The detail window"},{"body":[{"spans":[{"t":"text","v":"This window also carries a "},{"t":"strong","v":"Restart this broker"},{"t":"text","v":" button (where it is available). Clicking it triggers a broker-generation restart: the supervisor process stays alive and re-spawns the broker worker, which picks up any code changes on disk without stopping the machine. It is not a service restart — systemd's launcher preamble and the unit environment are not re-run, and agents you launched before the restart reconnect to the new worker and survive."}],"t":"p"},{"spans":[{"t":"text","v":"Restart is "},{"t":"strong","v":"opt-in"},{"t":"text","v":", granted from whichever place owns the gate. When the "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" row (above) is writable, turning it on grants restart together with apply — live, no config edit, no process restart. When this broker's config already names "},{"t":"code","v":"restart_enabled"},{"t":"text","v":" — "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":" — that file decides instead, and the row goes dead and names it rather than offering a switch. The intent is unchanged either way: a restart is a deliberate choice, made once by whoever owns the gate, not something a browser session earns just by being logged in."}],"t":"p"},{"spans":[{"t":"text","v":"Before restarting, the broker "},{"t":"strong","v":"drains"},{"t":"text","v":" — it stops accepting new work, waits for critical writes (uploads, recording saves) already in flight to finish or time out, and disconnects idle sessions (those not currently running a command) with a warning. Sessions that survive are reported in a confirm dialog before anything stops: "},{"t":"strong","v":"guaranteed"},{"t":"text","v":" (agents that survive the restart), "},{"t":"strong","v":"at_risk"},{"t":"text","v":" (sessions that will be lost), and "},{"t":"strong","v":"unknown"},{"t":"text","v":" (those whose fate couldn't be determined). Every plain terminal on this broker — not an agent session — is lost. The restart waits up to 90 seconds for the new broker to start answering; if it does not, the UI says so and you may need to check the machine itself."}],"t":"p"},{"spans":[{"t":"text","v":"The button is disabled with a reason when:"}],"t":"p"},{"spans":[{"t":"strong","v":"Restart is switched off on this broker"},{"t":"text","v":" — when the "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" row is writable, turning it on grants this (together with apply); when this broker's config already names "},{"t":"code","v":"restart_enabled"},{"t":"text","v":", that file decides and the row names it instead. Either way this is not a permission a session earns by being logged in — someone has to grant it, from the row or from the file."}],"t":"bullet"},{"spans":[{"t":"strong","v":"This broker was started without the launcher"},{"t":"text","v":" — it is running "},{"t":"code","v":"python -m webterm.broker"},{"t":"text","v":" by hand or in a way the supervisor cannot restart it. Restart it manually on the machine itself."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The launcher's parent is no longer running"},{"t":"text","v":" — the process tree changed (you might have reparented the broker to a different shell). Restart manually."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Systemd will not restart this unit"},{"t":"text","v":" — the unit's "},{"t":"code","v":"Restart="},{"t":"text","v":" policy is "},{"t":"code","v":"no"},{"t":"text","v":", "},{"t":"code","v":"on-success"},{"t":"text","v":", "},{"t":"code","v":"on-abort"},{"t":"text","v":", "},{"t":"code","v":"on-abnormal"},{"t":"text","v":", or "},{"t":"code","v":"on-watchdog"},{"t":"text","v":", none of which respawn on a plain non-zero exit. A graceful stop now would leave nothing listening. Restart manually or change the unit."}],"t":"bullet"},{"spans":[{"t":"strong","v":"A restart is already under way"},{"t":"text","v":" — wait for it to finish and check back."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The broker started moments ago"},{"t":"text","v":" — a cooldown ("},{"t":"code","v":"restart_cooldown_seconds"},{"t":"text","v":", default 90, config-file only, "},{"t":"code","v":"0"},{"t":"text","v":" disables) refuses back-to-back restarts so a run of deliberate clicks cannot exhaust the supervisor's crash budget. It clears by itself, and the message says roughly how long is left."}],"t":"bullet"},{"spans":[{"t":"strong","v":"This broker could not read its restart policy"},{"t":"text","v":" — the broker could not contact systemd or parse the unit. Restart manually or give the broker time to try again."}],"t":"bullet"},{"spans":[{"t":"text","v":"The session cost is shown in the confirm dialog itself, a moment before anything stops."}],"t":"p"},{"spans":[{"t":"text","v":"After a restart, a new "},{"t":"code","v":"bootId"},{"t":"text","v":" is reported by the broker ("},{"t":"code","v":"GET /info"},{"t":"text","v":" returns "},{"t":"code","v":"restart.bootId"},{"t":"text","v":"). The window watches it for you: only a changed "},{"t":"code","v":"bootId"},{"t":"text","v":" is treated as proof the restart happened. If it never changes, the restart did not happen — the broker either refused it or failed to relaunch, and the window says so."}],"t":"p"}],"search":"restarting the broker this window also carries a restart this broker button (where it is available). clicking it triggers a broker-generation restart: the supervisor process stays alive and re-spawns the broker worker, which picks up any code changes on disk without stopping the machine. it is not a service restart — systemd's launcher preamble and the unit environment are not re-run, and agents you launched before the restart reconnect to the new worker and survive. restart is opt-in , granted from whichever place owns the gate. when the allow this broker to update itself row (above) is writable, turning it on grants restart together with apply — live, no config edit, no process restart. when this broker's config already names restart_enabled — true or false — that file decides instead, and the row goes dead and names it rather than offering a switch. the intent is unchanged either way: a restart is a deliberate choice, made once by whoever owns the gate, not something a browser session earns just by being logged in. before restarting, the broker drains — it stops accepting new work, waits for critical writes (uploads, recording saves) already in flight to finish or time out, and disconnects idle sessions (those not currently running a command) with a warning. sessions that survive are reported in a confirm dialog before anything stops: guaranteed (agents that survive the restart), at_risk (sessions that will be lost), and unknown (those whose fate couldn't be determined). every plain terminal on this broker — not an agent session — is lost. the restart waits up to 90 seconds for the new broker to start answering; if it does not, the ui says so and you may need to check the machine itself. the button is disabled with a reason when: restart is switched off on this broker — when the allow this broker to update itself row is writable, turning it on grants this (together with apply); when this broker's config already names restart_enabled , that file decides and the row names it instead. either way this is not a permission a session earns by being logged in — someone has to grant it, from the row or from the file. this broker was started without the launcher — it is running python -m webterm.broker by hand or in a way the supervisor cannot restart it. restart it manually on the machine itself. the launcher's parent is no longer running — the process tree changed (you might have reparented the broker to a different shell). restart manually. systemd will not restart this unit — the unit's restart= policy is no , on-success , on-abort , on-abnormal , or on-watchdog , none of which respawn on a plain non-zero exit. a graceful stop now would leave nothing listening. restart manually or change the unit. a restart is already under way — wait for it to finish and check back. the broker started moments ago — a cooldown ( restart_cooldown_seconds , default 90, config-file only, 0 disables) refuses back-to-back restarts so a run of deliberate clicks cannot exhaust the supervisor's crash budget. it clears by itself, and the message says roughly how long is left. this broker could not read its restart policy — the broker could not contact systemd or parse the unit. restart manually or give the broker time to try again. the session cost is shown in the confirm dialog itself, a moment before anything stops. after a restart, a new bootid is reported by the broker ( get /info returns restart.bootid ). the window watches it for you: only a changed bootid is treated as proof the restart happened. if it never changes, the restart did not happen — the broker either refused it or failed to relaunch, and the window says so.","title":"Restarting the broker"},{"body":[{"spans":[{"t":"text","v":"When the broker runs under a Windows scheduled task (instead of systemd), the "},{"t":"strong","v":"Stop"},{"t":"text","v":" task gives the broker no graceful shutdown window — it terminates the process immediately. Draining has no time to run, and sessions are disconnected without warning. If you use a scheduled task and need orderly restarts, stop the task, wait a moment for the broker to exit, then restart it by hand or let the scheduler do it on schedule. This is honest rather than hidden: the scheduled-task path stops brokers abruptly whether you restart or not, so the restart machinery has no grace period to exploit."}],"t":"p"}],"search":"windows scheduled-task note when the broker runs under a windows scheduled task (instead of systemd), the stop task gives the broker no graceful shutdown window — it terminates the process immediately. draining has no time to run, and sessions are disconnected without warning. if you use a scheduled task and need orderly restarts, stop the task, wait a moment for the broker to exit, then restart it by hand or let the scheduler do it on schedule. this is honest rather than hidden: the scheduled-task path stops brokers abruptly whether you restart or not, so the restart machinery has no grace period to exploit.","title":"Windows scheduled-task note"},{"body":[{"spans":[{"t":"text","v":"The "},{"t":"strong","v":"Update…"},{"t":"text","v":" button applies the update that the check has previewed. It is manual-trigger only — no schedule, no auto-install, never automatic. Clicking it shows a confirm dialog with the commit range from the current build to the target, the commit count, a GitHub compare link, and a preview of the session cost — which terminals and agents will survive the restart, and which will be lost."}],"t":"p"},{"spans":[{"t":"text","v":"The serving broker's own button sits beside its Restart control. Since the fleet update work (#205), "},{"t":"strong","v":"every behind remote broker's row carries the same button"},{"t":"text","v":", driving that broker's own apply: the confirm dialog names the broker it acts on, shows that broker's commit range, and reports what that broker says a restart would cost its sessions (or says plainly that the session impact is unknown when it hasn't reported one). Each row is busy on its own — applying to one broker never blocks another, and each target refuses a second apply while one is already under way. The button degrades honestly instead of failing noisily: a broker whose own gates are off gets a dead button whose words point at its consent row (or at its config when a config key owns the gate); a broker too old to carry the three-gate consent view — or one that hasn't reported support for applies driven from another broker's page — gets no button and a note saying to update that broker on its own machine."}],"t":"p"},{"spans":[{"t":"text","v":"Applying an update requires "},{"t":"strong","v":"three gates"},{"t":"text","v":", all on the broker that is doing the applying — checking, applying, and restarting. Each is owned by whichever decided it first: a present config key ("},{"t":"code","v":"update_check_enabled"},{"t":"text","v":" / "},{"t":"code","v":"update_apply_enabled"},{"t":"text","v":" / "},{"t":"code","v":"restart_enabled"},{"t":"text","v":") locks its own gate, "},{"t":"code","v":"true"},{"t":"text","v":" or "},{"t":"code","v":"false"},{"t":"text","v":", and only editing that file and restarting changes it; an absent key leaves the gate to this window instead — the "},{"t":"strong","v":"Enable checking on this broker"},{"t":"text","v":" row for the first, and "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" for the other two together (see above). Nothing here silently escalates: a broker that only ever had checking granted — by an old click, an old sidecar record, or a config that names only "},{"t":"code","v":"update_check_enabled"},{"t":"text","v":" — stays exactly that, forever, until the self-update row or its own config grants the other two as well."}],"t":"p"},{"spans":[{"t":"code","v":"update_check_enabled: true"},{"t":"text","v":" — the broker has checked for updates (required to know what commit to apply). Granted by the mod's own on-click consent, or the "},{"t":"strong","v":"Enable checking on this broker"},{"t":"text","v":" row, whenever the config doesn't already own it."}],"t":"bullet"},{"spans":[{"t":"code","v":"update_apply_enabled: true"},{"t":"text","v":" — the broker is permitted to apply updates. Granted together with the restart gate by "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":", whenever the config doesn't already own it."}],"t":"bullet"},{"spans":[{"t":"code","v":"restart_enabled: true"},{"t":"text","v":" — the broker is permitted to restart itself after applying (a restart without this gate is refused; applying without restarting would leave old code running). Granted by the same row, together with apply."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Stop"},{"t":"text","v":" on the self-update row only ever gives back what that row granted — applying and restarting — never checking: turning self-update off leaves the broker still checking for updates, so the window keeps reporting what's available even after it can no longer act on that by itself."}],"t":"p"},{"spans":[{"t":"text","v":"If all three are enabled and the current state allows applying, clicking "},{"t":"strong","v":"Update…"},{"t":"text","v":" starts the apply:"}],"t":"p"},{"spans":[{"t":"text","v":"The broker "},{"t":"strong","v":"fetches from the pinned upstream"},{"t":"text","v":" (a git subprocess to GitHub with the explicit HTTPS URL and explicit ref, never a remote name)."}],"t":"bullet"},{"spans":[{"t":"text","v":"It "},{"t":"strong","v":"verifies"},{"t":"text","v":" the target commit is now present and that a fast-forward is safe (no local work would be lost)."}],"t":"bullet"},{"spans":[{"t":"text","v":"It "},{"t":"strong","v":"checks dependencies"},{"t":"text","v":": if the apply introduces a dependency change (e.g. "},{"t":"code","v":"pyproject.toml"},{"t":"text","v":" was edited), the apply is refused with details so you can decide whether to run the installer first."}],"t":"bullet"},{"spans":[{"t":"text","v":"It "},{"t":"strong","v":"applies"},{"t":"text","v":" the commit via git fast-forward with repository hooks disabled, then re-spawns the broker process."}],"t":"bullet"},{"spans":[{"t":"text","v":"An update may be refused for any of these reasons:"}],"t":"p"},{"spans":[{"t":"strong","v":"Not a git checkout"},{"t":"text","v":" — this is a pip/wheel install, not a source checkout (out of scope for apply)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Dirty working tree"},{"t":"text","v":" — staged or unstaged local modifications exist; commit, stash, or revert them first."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Local commits ahead of upstream"},{"t":"text","v":" — the checkout carries commits upstream doesn't have; this is a human decision, so the apply refuses."}],"t":"bullet"},{"spans":[{"t":"strong","v":"No established update check"},{"t":"text","v":" — run a check that succeeds first, so the apply knows which commit it is applying."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Already current"},{"t":"text","v":" — the checkout is already at the upstream head."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Restart is not available"},{"t":"text","v":" — the broker cannot restart itself (the button shows why)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Apply is not enabled"},{"t":"text","v":" — this broker's apply gate is off, either because its config has not set "},{"t":"code","v":"update_apply_enabled: true"},{"t":"text","v":" or because nobody has granted it yet from the "},{"t":"strong","v":"Allow this broker to update itself"},{"t":"text","v":" row."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Dependency mismatch"},{"t":"text","v":" — the apply would introduce new or removed dependencies; install or uninstall them manually first."}],"t":"bullet"},{"spans":[{"t":"text","v":"After an update applies and the broker restarts, the "},{"t":"strong","v":"last deploy"},{"t":"text","v":" outcome is visible in the next update check — it shows whether the restart succeeded, whether the new build came up, and whether a rollback happened. A build that never comes up is automatically rolled back to the recorded previous commit, visible as a "},{"t":"code","v":"rolled-back"},{"t":"text","v":" outcome; the UI shows this so you know why the build changed back rather than forward."}],"t":"p"},{"spans":[{"t":"strong","v":"The broker that was running the old code exits and is replaced with new code."},{"t":"text","v":" Agents and their sessions survive the restart and reconnect to the new process, but "},{"t":"strong","v":"terminals and agents do not live-reload"},{"t":"text","v":": they keep running the old code they imported at startup. The update window offers a "},{"t":"strong","v":"Fresh terminal"},{"t":"text","v":" button to spawn a new one with the updated code."}],"t":"p"}],"search":"applying an update the update… button applies the update that the check has previewed. it is manual-trigger only — no schedule, no auto-install, never automatic. clicking it shows a confirm dialog with the commit range from the current build to the target, the commit count, a github compare link, and a preview of the session cost — which terminals and agents will survive the restart, and which will be lost. the serving broker's own button sits beside its restart control. since the fleet update work (#205), every behind remote broker's row carries the same button , driving that broker's own apply: the confirm dialog names the broker it acts on, shows that broker's commit range, and reports what that broker says a restart would cost its sessions (or says plainly that the session impact is unknown when it hasn't reported one). each row is busy on its own — applying to one broker never blocks another, and each target refuses a second apply while one is already under way. the button degrades honestly instead of failing noisily: a broker whose own gates are off gets a dead button whose words point at its consent row (or at its config when a config key owns the gate); a broker too old to carry the three-gate consent view — or one that hasn't reported support for applies driven from another broker's page — gets no button and a note saying to update that broker on its own machine. applying an update requires three gates , all on the broker that is doing the applying — checking, applying, and restarting. each is owned by whichever decided it first: a present config key ( update_check_enabled / update_apply_enabled / restart_enabled ) locks its own gate, true or false , and only editing that file and restarting changes it; an absent key leaves the gate to this window instead — the enable checking on this broker row for the first, and allow this broker to update itself for the other two together (see above). nothing here silently escalates: a broker that only ever had checking granted — by an old click, an old sidecar record, or a config that names only update_check_enabled — stays exactly that, forever, until the self-update row or its own config grants the other two as well. update_check_enabled: true — the broker has checked for updates (required to know what commit to apply). granted by the mod's own on-click consent, or the enable checking on this broker row, whenever the config doesn't already own it. update_apply_enabled: true — the broker is permitted to apply updates. granted together with the restart gate by allow this broker to update itself , whenever the config doesn't already own it. restart_enabled: true — the broker is permitted to restart itself after applying (a restart without this gate is refused; applying without restarting would leave old code running). granted by the same row, together with apply. stop on the self-update row only ever gives back what that row granted — applying and restarting — never checking: turning self-update off leaves the broker still checking for updates, so the window keeps reporting what's available even after it can no longer act on that by itself. if all three are enabled and the current state allows applying, clicking update… starts the apply: the broker fetches from the pinned upstream (a git subprocess to github with the explicit https url and explicit ref, never a remote name). it verifies the target commit is now present and that a fast-forward is safe (no local work would be lost). it checks dependencies : if the apply introduces a dependency change (e.g. pyproject.toml was edited), the apply is refused with details so you can decide whether to run the installer first. it applies the commit via git fast-forward with repository hooks disabled, then re-spawns the broker process. an update may be refused for any of these reasons: not a git checkout — this is a pip/wheel install, not a source checkout (out of scope for apply). dirty working tree — staged or unstaged local modifications exist; commit, stash, or revert them first. local commits ahead of upstream — the checkout carries commits upstream doesn't have; this is a human decision, so the apply refuses. no established update check — run a check that succeeds first, so the apply knows which commit it is applying. already current — the checkout is already at the upstream head. restart is not available — the broker cannot restart itself (the button shows why). apply is not enabled — this broker's apply gate is off, either because its config has not set update_apply_enabled: true or because nobody has granted it yet from the allow this broker to update itself row. dependency mismatch — the apply would introduce new or removed dependencies; install or uninstall them manually first. after an update applies and the broker restarts, the last deploy outcome is visible in the next update check — it shows whether the restart succeeded, whether the new build came up, and whether a rollback happened. a build that never comes up is automatically rolled back to the recorded previous commit, visible as a rolled-back outcome; the ui shows this so you know why the build changed back rather than forward. the broker that was running the old code exits and is replaced with new code. agents and their sessions survive the restart and reconnect to the new process, but terminals and agents do not live-reload : they keep running the old code they imported at startup. the update window offers a fresh terminal button to spawn a new one with the updated code.","title":"Applying an update"},{"body":[{"spans":[{"t":"text","v":"The working tree is not inspected: build ids carry no dirty-tree marker, so uncommitted local changes are invisible and the report reflects your last commit only. The comparison is taken from the serving broker; each remote broker's own comparison would be the same repository read hours apart and is not particularly useful."}],"t":"p"}],"search":"what this does not claim the working tree is not inspected: build ids carry no dirty-tree marker, so uncommitted local changes are invisible and the report reflects your last commit only. the comparison is taken from the serving broker; each remote broker's own comparison would be the same repository read hours apart and is not particularly useful.","title":"What this does not claim"}],"icon":"⬆","label":"Update check","mod":"update","order":2085,"slug":"update"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Record any terminal session and replay it later, exactly as it looked."}],"t":"p"}],"search":"overview record any terminal session and replay it later, exactly as it looked.","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Every terminal title bar carries a red ⏺ button. Click it to start recording that terminal; the button pulses and an elapsed timer appears beside it. Click again to stop — the recording is saved to the broker automatically (it also saves if you close the window mid-recording). Recordings live on the broker's disk beside its state store ("},{"t":"code","v":"webterm_recordings/"},{"t":"text","v":", override with "},{"t":"code","v":"recordings_dir"},{"t":"text","v":" in the broker config) and are never expired — only deleting them from the library removes them."}],"t":"p"}],"search":"recording every terminal title bar carries a red ⏺ button. click it to start recording that terminal; the button pulses and an elapsed timer appears beside it. click again to stop — the recording is saved to the broker automatically (it also saves if you close the window mid-recording). recordings live on the broker's disk beside its state store ( webterm_recordings/ , override with recordings_dir in the broker config) and are never expired — only deleting them from the library removes them.","title":"Recording"},{"body":[{"spans":[{"t":"strong","v":"Control Panel → Session recorder → auto-record every session"},{"t":"text","v":" (off by default) starts recording every terminal as it opens, without anyone pressing ⏺. Like the other browser-global settings it is shared across every browser viewing this broker."}],"t":"p"},{"spans":[{"t":"text","v":"Switching it "},{"t":"strong","v":"on"},{"t":"text","v":" also starts recording the terminals that are already"}],"t":"bullet"},{"spans":[{"t":"text","v":"open."}],"t":"p"},{"spans":[{"t":"text","v":"Switching it "},{"t":"strong","v":"off"},{"t":"text","v":" stops — and saves — the recordings it started. A"}],"t":"bullet"},{"spans":[{"t":"text","v":"recording you started by hand with ⏺ keeps going; the setting only owns the ones it opened itself."}],"t":"p"},{"spans":[{"t":"text","v":"⏺ still works while it is on: pressing it stops that terminal's recording"}],"t":"bullet"},{"spans":[{"t":"text","v":"and it stays stopped. Pressing it again starts a new one."}],"t":"p"},{"spans":[{"t":"text","v":"Nothing is captured while no browser has the terminal open. This records"}],"t":"bullet"},{"spans":[{"t":"text","v":"what a browser is showing, so a session running with the desktop closed is not recorded."}],"t":"p"},{"spans":[{"t":"text","v":"With auto-record on, a page reload does "},{"t":"strong","v":"not"},{"t":"text","v":" warn before discarding the segment in progress — a prompt on every single reload is worse than losing the tail of a recording that will be re-armed on the next load. A recording you started by hand still warns, and so does a reload while a recording is still uploading."}],"t":"p"},{"spans":[{"t":"text","v":"Recordings are never swept, so leaving this on accumulates files on the broker's disk indefinitely. There is no size or age limit yet — delete what you do not need from the library."}],"t":"p"},{"spans":[{"t":"text","v":"What is captured: the terminal's raw output stream (byte-faithful, so colors and TUI apps replay exactly), resizes, and the initial size/font. Typed input is recorded as timestamped "},{"t":"strong","v":"markers only"},{"t":"text","v":" — the keystrokes themselves are never stored. If the connection drops and reattaches mid-recording, a gap marker is recorded (shown red on the timeline) and the replay heals with the reattach redraw."}],"t":"p"},{"spans":[{"t":"strong","v":"A recording sees whatever the screen saw."},{"t":"text","v":" Not storing keystrokes protects input the terminal never echoes — a password at a "},{"t":"code","v":"sudo"},{"t":"text","v":" or "},{"t":"code","v":"ssh"},{"t":"text","v":" prompt does not appear. It does not protect anything the terminal prints: a secret typed or pasted onto a visible command line is echoed as output, and output is captured byte for byte. The same goes for anything a command prints — API keys in a "},{"t":"code","v":"env"},{"t":"text","v":"-style dump, tokens, connection strings. Recording also starts by capturing the screen that was "},{"t":"strong","v":"already there"},{"t":"text","v":", so content that scrolled past before you pressed ⏺ can still be in the file. Treat a "},{"t":"code","v":".blrec"},{"t":"text","v":" like a screen recording: check what is in it before sharing it. Turning on auto-record makes that guarantee more load-bearing, not less: everything every session prints is captured, including the sessions you would not have thought to record."}],"t":"p"},{"spans":[{"t":"strong","v":"Clipboard copies are in the recording too."},{"t":"text","v":" When a program copies by asking the terminal to set your clipboard (the per-host "},{"t":"strong","v":"Clipboard (OSC 52)"},{"t":"text","v":" setting — see the guide's Keyboard shortcuts page), capture happens upstream of the part that decodes it, so what lands in the file is the raw escape sequence with the copied text base64-encoded inside it. That is true whether or not you enabled the setting: the recorder captures the request, not the result. It is the same hazard as the paragraph above, minus the part where you can spot it by eye — so scan rather than skim."}],"t":"p"},{"spans":[{"t":"text","v":"To check a saved recording for this broker's own token, run "},{"t":"code","v":"python -m webterm.broker --scan-recordings"},{"t":"text","v":". It base64-decodes the output stream first -- a plain "},{"t":"code","v":"grep"},{"t":"text","v":" (or "},{"t":"code","v":"zgrep"},{"t":"text","v":") over a recording finds nothing even when the token is in there, and reports a false all-clear. It scans compressed and uncompressed recordings alike. It only finds a secret that appears as contiguous bytes, so a clean result is evidence, not proof."}],"t":"p"},{"spans":[{"t":"text","v":"Recordings are stored "},{"t":"strong","v":"gzipped"},{"t":"text","v":" on the broker ("},{"t":"code","v":".blrec.gz"},{"t":"text","v":", typically around 8x smaller than the raw capture); recordings saved before that change stay as they are and keep working. Downloading still gives you plain "},{"t":"code","v":".blrec"},{"t":"text","v":" JSONL either way. The size shown in the library is the recording's own size, not its compressed footprint -- hover it to see what it actually occupies on disk."}],"t":"p"},{"spans":[{"t":"text","v":"A recording is held in memory until you stop it — reloading the page discards an in-progress recording."}],"t":"p"}],"search":"auto-record every session control panel → session recorder → auto-record every session (off by default) starts recording every terminal as it opens, without anyone pressing ⏺. like the other browser-global settings it is shared across every browser viewing this broker. switching it on also starts recording the terminals that are already open. switching it off stops — and saves — the recordings it started. a recording you started by hand with ⏺ keeps going; the setting only owns the ones it opened itself. ⏺ still works while it is on: pressing it stops that terminal's recording and it stays stopped. pressing it again starts a new one. nothing is captured while no browser has the terminal open. this records what a browser is showing, so a session running with the desktop closed is not recorded. with auto-record on, a page reload does not warn before discarding the segment in progress — a prompt on every single reload is worse than losing the tail of a recording that will be re-armed on the next load. a recording you started by hand still warns, and so does a reload while a recording is still uploading. recordings are never swept, so leaving this on accumulates files on the broker's disk indefinitely. there is no size or age limit yet — delete what you do not need from the library. what is captured: the terminal's raw output stream (byte-faithful, so colors and tui apps replay exactly), resizes, and the initial size/font. typed input is recorded as timestamped markers only — the keystrokes themselves are never stored. if the connection drops and reattaches mid-recording, a gap marker is recorded (shown red on the timeline) and the replay heals with the reattach redraw. a recording sees whatever the screen saw. not storing keystrokes protects input the terminal never echoes — a password at a sudo or ssh prompt does not appear. it does not protect anything the terminal prints: a secret typed or pasted onto a visible command line is echoed as output, and output is captured byte for byte. the same goes for anything a command prints — api keys in a env -style dump, tokens, connection strings. recording also starts by capturing the screen that was already there , so content that scrolled past before you pressed ⏺ can still be in the file. treat a .blrec like a screen recording: check what is in it before sharing it. turning on auto-record makes that guarantee more load-bearing, not less: everything every session prints is captured, including the sessions you would not have thought to record. clipboard copies are in the recording too. when a program copies by asking the terminal to set your clipboard (the per-host clipboard (osc 52) setting — see the guide's keyboard shortcuts page), capture happens upstream of the part that decodes it, so what lands in the file is the raw escape sequence with the copied text base64-encoded inside it. that is true whether or not you enabled the setting: the recorder captures the request, not the result. it is the same hazard as the paragraph above, minus the part where you can spot it by eye — so scan rather than skim. to check a saved recording for this broker's own token, run python -m webterm.broker --scan-recordings . it base64-decodes the output stream first -- a plain grep (or zgrep ) over a recording finds nothing even when the token is in there, and reports a false all-clear. it scans compressed and uncompressed recordings alike. it only finds a secret that appears as contiguous bytes, so a clean result is evidence, not proof. recordings are stored gzipped on the broker ( .blrec.gz , typically around 8x smaller than the raw capture); recordings saved before that change stay as they are and keep working. downloading still gives you plain .blrec jsonl either way. the size shown in the library is the recording's own size, not its compressed footprint -- hover it to see what it actually occupies on disk. a recording is held in memory until you stop it — reloading the page discards an in-progress recording.","title":"Auto-record every session"},{"body":[{"spans":[{"t":"text","v":"A single recording holds 50 MB of captured output (or 250,000 recorded events, whichever comes first). At that point capture does "},{"t":"strong","v":"not"},{"t":"text","v":" stop: the segment so far is saved and a fresh one opens immediately on the same terminal, seeded with the screen as it stood at the boundary. The ⏺ timer restarts from 0:00 and the elapsed clock is per segment."}],"t":"p"},{"spans":[{"t":"text","v":"Segments of the same run are listed in the library as "},{"t":"strong","v":"part 2/3"},{"t":"text","v":" and so on, newest first, and a player window titles the part it is playing. Each part is a complete recording in its own right — it opens on the screen the previous part ended on — so there is no seeking across a boundary."}],"t":"p"}],"search":"long recordings roll over a single recording holds 50 mb of captured output (or 250,000 recorded events, whichever comes first). at that point capture does not stop: the segment so far is saved and a fresh one opens immediately on the same terminal, seeded with the screen as it stood at the boundary. the ⏺ timer restarts from 0:00 and the elapsed clock is per segment. segments of the same run are listed in the library as part 2/3 and so on, newest first, and a player window titles the part it is playing. each part is a complete recording in its own right — it opens on the screen the previous part ended on — so there is no seeking across a boundary.","title":"Long recordings roll over"},{"body":[{"spans":[{"t":"text","v":"Open "},{"t":"strong","v":"Session recorder"},{"t":"text","v":" from the right-click (+) menu to list recordings — play, download ("},{"t":"code","v":".blrec"},{"t":"text","v":", newline-delimited JSON), or delete (click ✕ twice) each one. "},{"t":"strong","v":"Play"},{"t":"text","v":" opens a player window fixed at the recording's original columns×rows — the window sizes itself to the recording, and follows any resizes that happened during it."}],"t":"p"}],"search":"playback open session recorder from the right-click (+) menu to list recordings — play, download ( .blrec , newline-delimited json), or delete (click ✕ twice) each one. play opens a player window fixed at the recording's original columns×rows — the window sizes itself to the recording, and follows any resizes that happened during it.","title":"Playback"},{"body":[{"spans":[{"t":"text","v":"The library lists recordings from "},{"t":"strong","v":"every broker you have connected"},{"t":"text","v":", merged into one list, and each row is tagged with the broker that holds it. Play, download, notes and delete all act on that broker — nothing is copied between them. The drop-down beside "},{"t":"strong","v":"Refresh"},{"t":"text","v":" narrows the list to one broker; it appears only when more than one is configured, and resets to All brokers when the page is reloaded."}],"t":"p"},{"spans":[{"t":"text","v":"A broker that does not answer gets its "},{"t":"strong","v":"own row"},{"t":"text","v":" rather than quietly shortening the list: unreachable, or password required with a "},{"t":"strong","v":"Sign in"},{"t":"text","v":" button. Signing in refills the list in place. A broker running a build older than the recorder reads as no recorder on this broker."}],"t":"p"},{"spans":[{"t":"text","v":"Two details worth knowing:"}],"t":"p"},{"spans":[{"t":"text","v":"The tag means "},{"t":"strong","v":"stored on"},{"t":"text","v":", not ran on. A recording is always saved to the"}],"t":"bullet"},{"spans":[{"t":"text","v":"broker this page is served from, even when the terminal itself is running on a remote one — so recording a remote session files it here."}],"t":"p"},{"spans":[{"t":"text","v":"Hiding a broker (the per-broker hide toggle) masks its "},{"t":"strong","v":"windows"},{"t":"text","v":", not its"}],"t":"bullet"},{"spans":[{"t":"text","v":"recordings. They stay listed, so you can still delete them."}],"t":"p"},{"spans":[{"t":"text","v":"The merged list is ordered newest-first by each recording's start time, which is taken from the clock of the browser that recorded it. Across machines with disagreeing clocks that ordering is an approximation."}],"t":"p"},{"spans":[{"t":"text","v":"Transport controls: play/pause, playback speed 0.25×–8×, and "},{"t":"strong","v":"◀◀ reverse"},{"t":"text","v":" — a true backwards animation, not just a jump. The scrubber seeks anywhere; seeking and reverse render from keyframes the player indexes when it loads a recording (\"indexing…\"). Note markers are gold on the timeline; connection gaps are red."}],"t":"p"}],"search":"several brokers the library lists recordings from every broker you have connected , merged into one list, and each row is tagged with the broker that holds it. play, download, notes and delete all act on that broker — nothing is copied between them. the drop-down beside refresh narrows the list to one broker; it appears only when more than one is configured, and resets to all brokers when the page is reloaded. a broker that does not answer gets its own row rather than quietly shortening the list: unreachable, or password required with a sign in button. signing in refills the list in place. a broker running a build older than the recorder reads as no recorder on this broker. two details worth knowing: the tag means stored on , not ran on. a recording is always saved to the broker this page is served from, even when the terminal itself is running on a remote one — so recording a remote session files it here. hiding a broker (the per-broker hide toggle) masks its windows , not its recordings. they stay listed, so you can still delete them. the merged list is ordered newest-first by each recording's start time, which is taken from the clock of the browser that recorded it. across machines with disagreeing clocks that ordering is an approximation. transport controls: play/pause, playback speed 0.25×–8×, and ◀◀ reverse — a true backwards animation, not just a jump. the scrubber seeks anywhere; seeking and reverse render from keyframes the player indexes when it loads a recording (\"indexing…\"). note markers are gold on the timeline; connection gaps are red.","title":"Several brokers"},{"body":[{"spans":[{"t":"text","v":"While playing (or paused), "},{"t":"strong","v":"✎+"},{"t":"text","v":" adds a note at the current timestamp. Notes appear as gold timeline markers — click one to jump there — and in the list under the transport bar, where they can be edited (✎) or deleted (✕). Notes are stored with the recording on the broker and survive reloads; concurrent edits from two windows are revision-checked so nothing is silently lost."}],"t":"p"}],"search":"notes while playing (or paused), ✎+ adds a note at the current timestamp. notes appear as gold timeline markers — click one to jump there — and in the list under the transport bar, where they can be edited (✎) or deleted (✕). notes are stored with the recording on the broker and survive reloads; concurrent edits from two windows are revision-checked so nothing is silently lost.","title":"Notes"}],"icon":"⏺","label":"Session recorder","mod":"recorder","order":2090,"slug":"recorder"},{"cards":[{"body":[{"spans":[{"t":"text","v":"The Broker registry is an optional way to share your "},{"t":"strong","v":"host list"},{"t":"text","v":" — the brokers you connect to in "},{"t":"strong","v":"Control Panel → Browser → Hosts"},{"t":"text","v":" — through a broker, so you can recover it in another browser or after clearing this browser's data. It is a convenience layer, not a change to how connections work: your host list still lives in this browser and stays the source of truth, and the desktop always connects "},{"t":"strong","v":"directly"},{"t":"text","v":" to each broker. The broker only stores a copy of the list; it never proxies your traffic."}],"t":"p"},{"spans":[{"t":"text","v":"You will find it in "},{"t":"strong","v":"Control Panel → Browser → Broker registry"},{"t":"text","v":", right below the Hosts list it manages. For adding and editing the hosts themselves, see Hosts-and-Multi-Browser."}],"t":"p"}],"search":"overview the broker registry is an optional way to share your host list — the brokers you connect to in control panel → browser → hosts — through a broker, so you can recover it in another browser or after clearing this browser's data. it is a convenience layer, not a change to how connections work: your host list still lives in this browser and stays the source of truth, and the desktop always connects directly to each broker. the broker only stores a copy of the list; it never proxies your traffic. you will find it in control panel → browser → broker registry , right below the hosts list it manages. for adding and editing the hosts themselves, see hosts-and-multi-browser. hosts and multi browser","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"The host list normally lives only in this browser's local storage. That means opening the desktop from a different browser starts with an empty list, and clearing local storage to fix a UI glitch also wipes your remote brokers. Publishing the list to a broker gives you a copy you can pull back from anywhere that can reach that broker."}],"t":"p"}],"search":"why you might want it the host list normally lives only in this browser's local storage. that means opening the desktop from a different browser starts with an empty list, and clearing local storage to fix a ui glitch also wipes your remote brokers. publishing the list to a broker gives you a copy you can pull back from anywhere that can reach that broker.","title":"Why you might want it"},{"body":[{"spans":[{"t":"text","v":"Click "},{"t":"strong","v":"Publish…"},{"t":"text","v":" to save this browser's host list to the broker. The dialog lets you:"}],"t":"p"},{"spans":[{"t":"strong","v":"Choose which hosts"},{"t":"text","v":" to include (all are checked by default)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Set this broker's address"},{"t":"text","v":" — the absolute URL other machines use to reach it. A loopback address ("},{"t":"code","v":"localhost"},{"t":"text","v":" / "},{"t":"code","v":"127.x"},{"t":"text","v":") can't be reached from another machine, so the local broker is skipped when its address is loopback."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Also publish to every configured broker"},{"t":"text","v":" — writes the same list to each broker you have, so opening the desktop from any of them can recover the full list. Each broker is reported separately; if one can't be written (for example another browser holds its active-writer lease), you are told which."}],"t":"bullet"},{"spans":[{"t":"text","v":"Publishing needs this browser to be the "},{"t":"strong","v":"active"},{"t":"text","v":" browser for the target broker (the same single-writer lease that governs the layout). A non-active browser is told to take over first."}],"t":"p"}],"search":"publish click publish… to save this browser's host list to the broker. the dialog lets you: choose which hosts to include (all are checked by default). set this broker's address — the absolute url other machines use to reach it. a loopback address ( localhost / 127.x ) can't be reached from another machine, so the local broker is skipped when its address is loopback. also publish to every configured broker — writes the same list to each broker you have, so opening the desktop from any of them can recover the full list. each broker is reported separately; if one can't be written (for example another browser holds its active-writer lease), you are told which. publishing needs this browser to be the active browser for the target broker (the same single-writer lease that governs the layout). a non-active browser is told to take over first.","title":"Publish"},{"body":[{"spans":[{"t":"text","v":"Click "},{"t":"strong","v":"Pull…"},{"t":"text","v":" to import hosts that were published to a broker. If you have more than one broker configured, it first asks "},{"t":"strong","v":"which brokers' lists to read"},{"t":"text","v":" — this one is ticked by default, and every other one is checked and listed with what it holds (\"3 hosts\", \"passwords encrypted\") or why it can't be read (\"refused our password — set it on the Browser tab\", \"could not be reached\", \"nothing published there\"). Brokers that can't be read are shown but can't be picked. There's a "},{"t":"strong","v":"Every broker that can be read"},{"t":"text","v":" tick if you want the lot."}],"t":"p"},{"spans":[{"t":"text","v":"This is what makes the registry recoverable: a list published to a broker you can reach is readable from here even if this broker's own registry is empty — including, in particular, when the host missing from your list is the broker holding it."}],"t":"p"},{"spans":[{"t":"text","v":"Reading several brokers merges their lists into one set of rows. Where two brokers list the same address, the first one in the order shown wins, and the row says which brokers it came from and which ones disagreed. Where two different addresses claim to be the same broker, both rows survive and both are flagged — at most one of them is telling the truth, and that's a decision for you, not a tie for the mod to break quietly."}],"t":"p"},{"spans":[{"t":"text","v":"Each entry is classified against what you already have:"}],"t":"p"},{"spans":[{"t":"strong","v":"new"},{"t":"text","v":" — a broker you don't have yet (checked by default)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"differs"},{"t":"text","v":" — a broker you already have, but with a different label, URL, or color (unchecked by default: your local copy wins unless you tick it). If the URL differs, applying it replaces your local URL "},{"t":"strong","v":"and clears that host's saved password"},{"t":"text","v":", so you may need to re-enter it — a saved password is never carried over to a different address. A list that carries a different password for a host you otherwise already have is also a "},{"t":"strong","v":"differs"},{"t":"text","v":", so a rotated password can be pulled back."}],"t":"bullet"},{"spans":[{"t":"strong","v":"already have"},{"t":"text","v":" — identical to what you have (greyed out, nothing to do)."}],"t":"bullet"},{"spans":[{"t":"text","v":"Matching is by the broker's "},{"t":"strong","v":"verified"},{"t":"text","v":" identity first, then its address, so pulling an updated list "},{"t":"strong","v":"updates"},{"t":"text","v":" the hosts you already have instead of duplicating them. The registry's own copy of a broker never replaces your "},{"t":"strong","v":"this broker"},{"t":"text","v":" entry, and your default-host choice is left untouched. Nothing is matched by the id a list carries — that belongs to the browser that published it, and treating it as an identity would let whoever wrote the list choose which of your hosts an entry lands on."}],"t":"p"}],"search":"pull click pull… to import hosts that were published to a broker. if you have more than one broker configured, it first asks which brokers' lists to read — this one is ticked by default, and every other one is checked and listed with what it holds (\"3 hosts\", \"passwords encrypted\") or why it can't be read (\"refused our password — set it on the browser tab\", \"could not be reached\", \"nothing published there\"). brokers that can't be read are shown but can't be picked. there's a every broker that can be read tick if you want the lot. this is what makes the registry recoverable: a list published to a broker you can reach is readable from here even if this broker's own registry is empty — including, in particular, when the host missing from your list is the broker holding it. reading several brokers merges their lists into one set of rows. where two brokers list the same address, the first one in the order shown wins, and the row says which brokers it came from and which ones disagreed. where two different addresses claim to be the same broker, both rows survive and both are flagged — at most one of them is telling the truth, and that's a decision for you, not a tie for the mod to break quietly. each entry is classified against what you already have: new — a broker you don't have yet (checked by default). differs — a broker you already have, but with a different label, url, or color (unchecked by default: your local copy wins unless you tick it). if the url differs, applying it replaces your local url and clears that host's saved password , so you may need to re-enter it — a saved password is never carried over to a different address. a list that carries a different password for a host you otherwise already have is also a differs , so a rotated password can be pulled back. already have — identical to what you have (greyed out, nothing to do). matching is by the broker's verified identity first, then its address, so pulling an updated list updates the hosts you already have instead of duplicating them. the registry's own copy of a broker never replaces your this broker entry, and your default-host choice is left untouched. nothing is matched by the id a list carries — that belongs to the browser that published it, and treating it as an identity would let whoever wrote the list choose which of your hosts an entry lands on.","title":"Pull"},{"body":[{"spans":[{"t":"text","v":"Passwords (broker tokens) are "},{"t":"strong","v":"not"},{"t":"text","v":" published unless you tick "},{"t":"strong","v":"Include passwords"},{"t":"text","v":", which is off by default and shows a warning. Publishing a token is a real risk: anyone who can read that broker's registry can then log into every broker whose token you included — a lateral-movement path between your machines. Only include passwords when the broker holding the registry is as trusted as the brokers whose tokens it will carry."}],"t":"p"},{"spans":[{"t":"text","v":"The same risk runs in the other direction when you read another broker's list, so passwords in it are ignored unless you tick "},{"t":"strong","v":"Accept passwords from other brokers"},{"t":"text","v":" in the source picker. That's the permission; you still tick each host by hand afterwards, and each row says which broker its password came from."}],"t":"p"},{"spans":[{"t":"text","v":"Two other things are never taken from another broker's list. A "},{"t":"strong","v":"loopback"},{"t":"text","v":" address ("},{"t":"code","v":"localhost"},{"t":"text","v":", "},{"t":"code","v":"127.x"},{"t":"text","v":") means the machine that published it, so importing it here would point a host at your broker carrying somebody else's password — those rows are dropped and counted. And a "},{"t":"strong","v":"hidden"},{"t":"text","v":" host arrives visible, because hidden takes a host out of the list you can see while this browser carries on talking to it."}],"t":"p"},{"spans":[{"t":"text","v":"The broker's password history"}],"t":"sub"},{"spans":[{"t":"text","v":"A broker keeps a short revision history of what is stored in it, so every republish used to file the copy it replaced — passwords and all — where anyone who can read the registry could read it back out. Publishing now tells the broker to keep "},{"t":"strong","v":"no history"},{"t":"text","v":" for the registry: the stored value is replaced with nothing archived behind it, and whatever history had already piled up is cleared by that same write. The instruction sticks to the registry, so a later publish can't quietly start a new history."}],"t":"p"},{"spans":[{"t":"text","v":"Older brokers don't understand that instruction, and they ignore it rather than refuse it — so it is checked "},{"t":"strong","v":"before"},{"t":"text","v":" anything is sent. A broker that doesn't advertise it is refused "},{"t":"strong","v":"for that broker only"},{"t":"text","v":": no password is sent there, every other broker in the same publish is written as usual, and you are offered a per-broker "},{"t":"strong","v":"Publish passwords anyway"},{"t":"text","v":" override that spells out what it costs. If a broker takes the write but doesn't confirm it dropped its history, that broker is reported as "},{"t":"strong","v":"still archiving"},{"t":"text","v":" by name, rather than being folded into \"published to 3 brokers\"."}],"t":"p"},{"spans":[{"t":"text","v":"Two things this does not do. It cannot reach copies that already left that broker — older backups and snapshots of its files still hold whatever they held — and dropping a history never recalls a password somebody has already read. Rotating the token remains the only real revocation."}],"t":"p"}],"search":"passwords passwords (broker tokens) are not published unless you tick include passwords , which is off by default and shows a warning. publishing a token is a real risk: anyone who can read that broker's registry can then log into every broker whose token you included — a lateral-movement path between your machines. only include passwords when the broker holding the registry is as trusted as the brokers whose tokens it will carry. the same risk runs in the other direction when you read another broker's list, so passwords in it are ignored unless you tick accept passwords from other brokers in the source picker. that's the permission; you still tick each host by hand afterwards, and each row says which broker its password came from. two other things are never taken from another broker's list. a loopback address ( localhost , 127.x ) means the machine that published it, so importing it here would point a host at your broker carrying somebody else's password — those rows are dropped and counted. and a hidden host arrives visible, because hidden takes a host out of the list you can see while this browser carries on talking to it. the broker's password history a broker keeps a short revision history of what is stored in it, so every republish used to file the copy it replaced — passwords and all — where anyone who can read the registry could read it back out. publishing now tells the broker to keep no history for the registry: the stored value is replaced with nothing archived behind it, and whatever history had already piled up is cleared by that same write. the instruction sticks to the registry, so a later publish can't quietly start a new history. older brokers don't understand that instruction, and they ignore it rather than refuse it — so it is checked before anything is sent. a broker that doesn't advertise it is refused for that broker only : no password is sent there, every other broker in the same publish is written as usual, and you are offered a per-broker publish passwords anyway override that spells out what it costs. if a broker takes the write but doesn't confirm it dropped its history, that broker is reported as still archiving by name, rather than being folded into \"published to 3 brokers\". two things this does not do. it cannot reach copies that already left that broker — older backups and snapshots of its files still hold whatever they held — and dropping a history never recalls a password somebody has already read. rotating the token remains the only real revocation.","title":"Passwords"},{"body":[{"spans":[{"t":"text","v":"The broker stores the registry as plain JSON in a file next to its state file, plus a short revision history. "},{"t":"strong","v":"Broker registry encryption"},{"t":"text","v":" encrypts that value in this browser before it is published, so the broker only ever receives ciphertext. It is set in "},{"t":"strong","v":"Control Panel → Browser → Broker registry encryption"},{"t":"text","v":", under the "},{"t":"strong","v":"Encrypt before publishing"},{"t":"text","v":" setting, which offers three modes:"}],"t":"p"},{"spans":[{"t":"strong","v":"Passwords only (recommended)"},{"t":"text","v":" (the default) — the labels and addresses stay readable; only the passwords are encrypted. Nothing changes until a publish actually carries a password, so with "},{"t":"strong","v":"Include passwords"},{"t":"text","v":" off this mode does nothing at all and never asks for a passphrase."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Whole list"},{"t":"text","v":" — the entire list is encrypted. The broker cannot see which machines you have, and the list is unreadable until you unlock it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Off — publish in the clear"},{"t":"text","v":" — the old behaviour: everything is published as before."}],"t":"bullet"},{"spans":[{"t":"text","v":"Publishing that needs a passphrase opens a prompt with an "},{"t":"strong","v":"Encrypt"},{"t":"text","v":" button, and pulling an encrypted registry opens the matching unlock prompt. The passphrase is held in memory for as long as the page is open — so publishing to every broker, or pulling twice in a row, only asks once — and it is "},{"t":"strong","v":"never stored anywhere"},{"t":"text","v":". There is no \"remember on this browser\": a key kept in local storage is worth exactly as much as the passphrase to anything that can read local storage. "},{"t":"strong","v":"Forget passphrase"},{"t":"text","v":" drops it. If you lose it, publish the list again with a new one."}],"t":"p"},{"spans":[{"t":"text","v":"What this protects you from — and what it does not"}],"t":"sub"},{"spans":[{"t":"text","v":"It protects the registry "},{"t":"strong","v":"at rest"},{"t":"text","v":", and from anyone who can "},{"t":"strong","v":"read"},{"t":"text","v":" the store: that file, a backup or snapshot of it, the revision history, another user or administrator of that machine, or a second browser that has the broker's token but not your passphrase."}],"t":"p"},{"spans":[{"t":"text","v":"It does "},{"t":"strong","v":"not"},{"t":"text","v":" protect you from a "},{"t":"strong","v":"compromised or hostile broker"},{"t":"text","v":". That broker serves the very code that does the encrypting, so it could serve a version that keeps your passphrase. Encryption makes publishing passwords safe against someone reading the broker's disk; it does not make it safe against the broker itself. If you don't trust the broker, don't publish passwords to it."}],"t":"p"},{"spans":[{"t":"text","v":"It also cannot stop somebody who can write the registry from deleting it, or from replacing it with a list of their own. What it does stop is a subtler trick: because each password is encrypted "},{"t":"strong","v":"together with"},{"t":"text","v":" the address it belongs to, nobody can edit the readable half to point one of your saved passwords at a machine of theirs. If the readable half and the encrypted contents disagree, you are told, and the encrypted (authenticated) list is the one used."}],"t":"p"},{"spans":[{"t":"text","v":"Encryption needs a secure context"}],"t":"sub"},{"spans":[{"t":"text","v":"Browsers only expose the crypto this uses on a "},{"t":"strong","v":"secure context"},{"t":"text","v":" — "},{"t":"code","v":"https://"},{"t":"text","v":", or "},{"t":"code","v":"localhost"},{"t":"text","v":". The broker terminates no TLS itself, so a broker reached at "},{"t":"code","v":"http://192.168.x.x:4445"},{"t":"text","v":" cannot encrypt. When that is the case the option says so, and publishing is "},{"t":"strong","v":"refused"},{"t":"text","v":" rather than quietly falling back to the clear. To publish from such a page, either reach the broker over https (or on localhost), or set encryption to "},{"t":"strong","v":"Off"},{"t":"text","v":" — which publishes in the clear, exactly as before."}],"t":"p"},{"spans":[{"t":"text","v":"Other things worth knowing"}],"t":"sub"},{"spans":[{"t":"strong","v":"Publishing to every broker"},{"t":"text","v":" encrypts once and writes the same ciphertext everywhere, so all of those brokers share one passphrase."}],"t":"bullet"},{"spans":[{"t":"strong","v":"The revision history is cleared"},{"t":"text","v":" on every publish, encrypted or not. Otherwise the plaintext copy you just replaced would still be sitting in the history of the store you had stopped trusting."}],"t":"bullet"},{"spans":[{"t":"text","v":"A wrong passphrase is reported as a wrong passphrase and changes nothing locally — you can simply try again. (It can also mean the stored copy was modified; the two are the same failure and cannot be told apart.)"}],"t":"bullet"},{"spans":[{"t":"strong","v":"Older versions of Browserland"},{"t":"text","v":" don't know about encryption. In passwords only mode they still read the list and import the labels and addresses, just without the passwords — the same as a list published without "},{"t":"strong","v":"Include passwords"},{"t":"text","v":". In whole list mode they report the registry as empty."}],"t":"bullet"}],"search":"encryption the broker stores the registry as plain json in a file next to its state file, plus a short revision history. broker registry encryption encrypts that value in this browser before it is published, so the broker only ever receives ciphertext. it is set in control panel → browser → broker registry encryption , under the encrypt before publishing setting, which offers three modes: passwords only (recommended) (the default) — the labels and addresses stay readable; only the passwords are encrypted. nothing changes until a publish actually carries a password, so with include passwords off this mode does nothing at all and never asks for a passphrase. whole list — the entire list is encrypted. the broker cannot see which machines you have, and the list is unreadable until you unlock it. off — publish in the clear — the old behaviour: everything is published as before. publishing that needs a passphrase opens a prompt with an encrypt button, and pulling an encrypted registry opens the matching unlock prompt. the passphrase is held in memory for as long as the page is open — so publishing to every broker, or pulling twice in a row, only asks once — and it is never stored anywhere . there is no \"remember on this browser\": a key kept in local storage is worth exactly as much as the passphrase to anything that can read local storage. forget passphrase drops it. if you lose it, publish the list again with a new one. what this protects you from — and what it does not it protects the registry at rest , and from anyone who can read the store: that file, a backup or snapshot of it, the revision history, another user or administrator of that machine, or a second browser that has the broker's token but not your passphrase. it does not protect you from a compromised or hostile broker . that broker serves the very code that does the encrypting, so it could serve a version that keeps your passphrase. encryption makes publishing passwords safe against someone reading the broker's disk; it does not make it safe against the broker itself. if you don't trust the broker, don't publish passwords to it. it also cannot stop somebody who can write the registry from deleting it, or from replacing it with a list of their own. what it does stop is a subtler trick: because each password is encrypted together with the address it belongs to, nobody can edit the readable half to point one of your saved passwords at a machine of theirs. if the readable half and the encrypted contents disagree, you are told, and the encrypted (authenticated) list is the one used. encryption needs a secure context browsers only expose the crypto this uses on a secure context — https:// , or localhost . the broker terminates no tls itself, so a broker reached at http://192.168.x.x:4445 cannot encrypt. when that is the case the option says so, and publishing is refused rather than quietly falling back to the clear. to publish from such a page, either reach the broker over https (or on localhost), or set encryption to off — which publishes in the clear, exactly as before. other things worth knowing publishing to every broker encrypts once and writes the same ciphertext everywhere, so all of those brokers share one passphrase. the revision history is cleared on every publish, encrypted or not. otherwise the plaintext copy you just replaced would still be sitting in the history of the store you had stopped trusting. a wrong passphrase is reported as a wrong passphrase and changes nothing locally — you can simply try again. (it can also mean the stored copy was modified; the two are the same failure and cannot be told apart.) older versions of browserland don't know about encryption. in passwords only mode they still read the list and import the labels and addresses, just without the passwords — the same as a list published without include passwords . in whole list mode they report the registry as empty.","title":"Encryption"},{"body":[{"spans":[{"t":"strong","v":"Forget passwords"},{"t":"text","v":" removes every token from this broker's registry, including its saved revision history, so a token can't be read back from an older stored version — and it leaves the registry set to keep no further history. It does "},{"t":"strong","v":"not"},{"t":"text","v":" revoke access: a browser that already pulled a token keeps it, and the broker's token stays valid until you rotate it. If a token was exposed, change it on that broker ("},{"t":"code","v":"python -m webterm.broker --print-token"},{"t":"text","v":" shows the current one). If the broker is an older version that can't clear its history, you are told so you can rotate the token instead."}],"t":"p"},{"spans":[{"t":"text","v":"It works "},{"t":"strong","v":"without the passphrase"},{"t":"text","v":" — that is the whole point of an emergency purge. Because an encrypted block is opaque, it is assumed to contain passwords and is removed whole. On a passwords only registry that leaves the readable list intact; on a whole list registry the list is inside the encrypted block, so removing the passwords means removing the list, and the confirmation says so before you agree to it."}],"t":"p"}],"search":"forget passwords forget passwords removes every token from this broker's registry, including its saved revision history, so a token can't be read back from an older stored version — and it leaves the registry set to keep no further history. it does not revoke access: a browser that already pulled a token keeps it, and the broker's token stays valid until you rotate it. if a token was exposed, change it on that broker ( python -m webterm.broker --print-token shows the current one). if the broker is an older version that can't clear its history, you are told so you can rotate the token instead. it works without the passphrase — that is the whole point of an emergency purge. because an encrypted block is opaque, it is assumed to contain passwords and is removed whole. on a passwords only registry that leaves the readable list intact; on a whole list registry the list is inside the encrypted block, so removing the passwords means removing the list, and the confirmation says so before you agree to it.","title":"Forget passwords"}],"icon":"🗂","label":"Broker registry","mod":"host-registry","order":2095,"slug":"host-registry"},{"cards":[{"body":[{"spans":[{"t":"text","v":"Sync mods copies "},{"t":"strong","v":"this broker's mod setup"},{"t":"text","v":" — which mods are on, and the settings those mods own — to the other brokers you have configured, so the same choices don't have to be made by hand on every machine. You will find it in "},{"t":"strong","v":"Control Panel → Browser → Sync mods"},{"t":"text","v":", next to the Broker registry."}],"t":"p"},{"spans":[{"t":"text","v":"It never sends mod "},{"t":"strong","v":"code"},{"t":"text","v":". A broker can only be told about mods it already serves; a mod it does not have is reported rather than silently skipped. For adding the brokers themselves, see Hosts-and-Multi-Browser."}],"t":"p"}],"search":"overview sync mods copies this broker's mod setup — which mods are on, and the settings those mods own — to the other brokers you have configured, so the same choices don't have to be made by hand on every machine. you will find it in control panel → browser → sync mods , next to the broker registry. it never sends mod code . a broker can only be told about mods it already serves; a mod it does not have is reported rather than silently skipped. for adding the brokers themselves, see hosts-and-multi-browser. hosts and multi browser","title":"Overview"},{"body":[{"spans":[{"t":"text","v":"Which mods run is normally "},{"t":"strong","v":"your browser's"},{"t":"text","v":" choice, kept in that browser only. There is no way to reach into another browser's choices from here — so the two directions write different things, and it is worth knowing which:"}],"t":"p"},{"spans":[{"t":"strong","v":"Push"},{"t":"text","v":" writes the target broker's "},{"t":"strong","v":"mod policy"},{"t":"text","v":" (the pins described in Hosts-and-Multi-Browser) plus its "},{"t":"strong","v":"mod settings"},{"t":"text","v":". Pins are what a broker hands every browser that loads its page, and they are the only per-mod state on another machine that can be written remotely."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Adopt"},{"t":"text","v":" changes "},{"t":"strong","v":"this browser's"},{"t":"text","v":" own Mods choices and this broker's mod settings. It pins nothing here."}],"t":"bullet"},{"spans":[{"t":"text","v":"So pushing configures a machine; adopting configures you."}],"t":"p"}],"search":"why the two directions are not mirror images which mods run is normally your browser's choice, kept in that browser only. there is no way to reach into another browser's choices from here — so the two directions write different things, and it is worth knowing which: push writes the target broker's mod policy (the pins described in hosts-and-multi-browser) plus its mod settings . pins are what a broker hands every browser that loads its page, and they are the only per-mod state on another machine that can be written remotely. adopt changes this browser's own mods choices and this broker's mod settings. it pins nothing here. so pushing configures a machine; adopting configures you. hosts and multi browser","title":"Why the two directions are not mirror images"},{"body":[{"spans":[{"t":"strong","v":"Push to brokers…"},{"t":"text","v":" lists every other broker you have configured. A broker with no saved password, or one that refused the password you have, is shown but cannot be selected — set its password on the "},{"t":"strong","v":"Browser"},{"t":"text","v":" tab first."}],"t":"p"},{"spans":[{"t":"text","v":"Nothing is written until you have seen the preview. It names, per broker, every pin that will be created or cleared, every setting that will change (with its old and new value), and everything that is being left alone and why. Brokers that already match are shown as such and unticked."}],"t":"p"},{"spans":[{"t":"text","v":"Afterwards each broker gets its own result row in the pane — a push is never all-or-nothing — with "},{"t":"strong","v":"Retry"},{"t":"text","v":" (which rebuilds the whole plan against that broker as it is now, rather than replaying a stale one) and "},{"t":"strong","v":"Undo pins"},{"t":"text","v":", which puts back the pins that broker had before the push."}],"t":"p"},{"spans":[{"t":"text","v":"Pins are kept to a minimum"}],"t":"sub"},{"spans":[{"t":"text","v":"By default a pin is written "},{"t":"strong","v":"only where that broker's own default would land somewhere else"},{"t":"text","v":", and a pin it no longer needs is "},{"t":"strong","v":"cleared"},{"t":"text","v":". Syncing two brokers that already agree therefore locks nothing, and repeated pushes don't accumulate pins."}],"t":"p"},{"spans":[{"t":"text","v":"Ticking "},{"t":"strong","v":"Lock every mod on the target"},{"t":"text","v":" instead pins every shared mod explicitly. That locks those checkboxes for every browser on that broker, and it is the only way to also override a choice a browser over there made locally."}],"t":"p"},{"spans":[{"t":"text","v":"Timing"}],"t":"sub"},{"spans":[{"t":"text","v":"A mod pin takes effect the next time a browser "},{"t":"strong","v":"loads"},{"t":"text","v":" that broker's page — not immediately, and not in a session that is already open there. Mod settings are shared state, so its browsers pick those up as they poll."}],"t":"p"},{"spans":[{"t":"text","v":"When a broker can't be pushed to"}],"t":"sub"},{"spans":[{"t":"text","v":"Each case gets its own row rather than a generic failure:"}],"t":"p"},{"spans":[{"t":"strong","v":"Older build"},{"t":"text","v":" — it does not report its mods, so its setup can't be read or changed from here. Nothing is written at all: without its list of mods there is no way to tell which settings it has any use for. Update it first."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Serves no desktop page"},{"t":"text","v":" — a headless broker runs no mods, so nothing here applies to it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Master mod switch off"},{"t":"text","v":" — it runs no mods at all ("},{"t":"code","v":"mods_enabled: false"},{"t":"text","v":"). Settings are still written; pins are not, because a pin made now would be inert and awkward to clear later (the pin editor disables itself while the master switch is off)."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Another browser is the active view there"},{"t":"text","v":" — its shared settings are only accepted from its active browser, so the settings half is refused. The pins are still written: unlike settings, a broker's mod policy can be changed while somebody else is using it."}],"t":"bullet"},{"spans":[{"t":"strong","v":"Unreachable"},{"t":"text","v":", or "},{"t":"strong","v":"refused our password"},{"t":"text","v":"."}],"t":"bullet"},{"spans":[{"t":"text","v":"Brokers that require an admin token"}],"t":"sub"},{"spans":[{"t":"text","v":"A broker can be configured ("},{"t":"code","v":"admin_token"},{"t":"text","v":" in its broker config) to require a separate "},{"t":"strong","v":"admin token"},{"t":"text","v":" for mod policy changes, on top of the ordinary page password. Push notices this from each broker's own capability report — an older broker predates the whole mechanism and is pushed to exactly as it always was — and asks for the admin token "},{"t":"strong","v":"once per push"},{"t":"text","v":", covering every broker in it that requires one."}],"t":"p"},{"spans":[{"t":"text","v":"The token is used for that push only: it is never stored anywhere, and it is never sent to a broker that does not require it. One prompt also means one token — if your brokers use different admin tokens, the ones it doesn't match refuse their policy changes and say so in their result rows, distinctly from a broker that was merely unreachable, and you push again for those. Mod settings are not admin-gated, so the settings half of a push still applies either way. "},{"t":"strong","v":"Retry"},{"t":"text","v":" and "},{"t":"strong","v":"Undo pins"},{"t":"text","v":" against such a broker ask again, for the same reason the token isn't kept."}],"t":"p"}],"search":"push push to brokers… lists every other broker you have configured. a broker with no saved password, or one that refused the password you have, is shown but cannot be selected — set its password on the browser tab first. nothing is written until you have seen the preview. it names, per broker, every pin that will be created or cleared, every setting that will change (with its old and new value), and everything that is being left alone and why. brokers that already match are shown as such and unticked. afterwards each broker gets its own result row in the pane — a push is never all-or-nothing — with retry (which rebuilds the whole plan against that broker as it is now, rather than replaying a stale one) and undo pins , which puts back the pins that broker had before the push. pins are kept to a minimum by default a pin is written only where that broker's own default would land somewhere else , and a pin it no longer needs is cleared . syncing two brokers that already agree therefore locks nothing, and repeated pushes don't accumulate pins. ticking lock every mod on the target instead pins every shared mod explicitly. that locks those checkboxes for every browser on that broker, and it is the only way to also override a choice a browser over there made locally. timing a mod pin takes effect the next time a browser loads that broker's page — not immediately, and not in a session that is already open there. mod settings are shared state, so its browsers pick those up as they poll. when a broker can't be pushed to each case gets its own row rather than a generic failure: older build — it does not report its mods, so its setup can't be read or changed from here. nothing is written at all: without its list of mods there is no way to tell which settings it has any use for. update it first. serves no desktop page — a headless broker runs no mods, so nothing here applies to it. master mod switch off — it runs no mods at all ( mods_enabled: false ). settings are still written; pins are not, because a pin made now would be inert and awkward to clear later (the pin editor disables itself while the master switch is off). another browser is the active view there — its shared settings are only accepted from its active browser, so the settings half is refused. the pins are still written: unlike settings, a broker's mod policy can be changed while somebody else is using it. unreachable , or refused our password . brokers that require an admin token a broker can be configured ( admin_token in its broker config) to require a separate admin token for mod policy changes, on top of the ordinary page password. push notices this from each broker's own capability report — an older broker predates the whole mechanism and is pushed to exactly as it always was — and asks for the admin token once per push , covering every broker in it that requires one. the token is used for that push only: it is never stored anywhere, and it is never sent to a broker that does not require it. one prompt also means one token — if your brokers use different admin tokens, the ones it doesn't match refuse their policy changes and say so in their result rows, distinctly from a broker that was merely unreachable, and you push again for those. mod settings are not admin-gated, so the settings half of a push still applies either way. retry and undo pins against such a broker ask again, for the same reason the token isn't kept.","title":"Push"},{"body":[{"spans":[{"t":"strong","v":"Adopt from a broker…"},{"t":"text","v":" makes this browser match a broker you pick. It reads what that broker hands every browser that loads it — its pins, or the shipped default of each mod where it has no pin — plus its mod settings, shows you the diff, and applies it here."}],"t":"p"},{"spans":[{"t":"text","v":"One limit follows from the same rule as above: a choice some browser over there made "},{"t":"strong","v":"only for itself"},{"t":"text","v":" is invisible from here. Adopting from a broker that pins nothing therefore gives you its defaults, not whatever its users happen to be running. Pushing is what creates pins, so adopting from a broker you have pushed to gives you exactly what you pushed."}],"t":"p"},{"spans":[{"t":"text","v":"A mod that "},{"t":"strong","v":"this"},{"t":"text","v":" broker pins is not this browser's call, so adopt reports it as refused instead of pretending to change it."}],"t":"p"}],"search":"adopt adopt from a broker… makes this browser match a broker you pick. it reads what that broker hands every browser that loads it — its pins, or the shipped default of each mod where it has no pin — plus its mod settings, shows you the diff, and applies it here. one limit follows from the same rule as above: a choice some browser over there made only for itself is invisible from here. adopting from a broker that pins nothing therefore gives you its defaults, not whatever its users happen to be running. pushing is what creates pins, so adopting from a broker you have pushed to gives you exactly what you pushed. a mod that this broker pins is not this browser's call, so adopt reports it as refused instead of pretending to change it.","title":"Adopt"},{"body":[{"spans":[{"t":"text","v":"Only settings owned by a mod that is "},{"t":"strong","v":"on here"},{"t":"text","v":" travel. A mod that is off here is being turned off there too, so its settings don't apply — but note the consequence: a value a disabled mod deliberately keeps (the terminal font mod preserves your font choice so re-enabling restores it) stays behind. If you want such a value to travel, turn the mod on, push, then turn it off again."}],"t":"p"},{"spans":[{"t":"text","v":"Core settings are never touched by either direction — only keys a mod owns. Per-host things like the default profile, start path and default host are deliberately not part of a mod setup."}],"t":"p"}],"search":"what travels, and what doesn't only settings owned by a mod that is on here travel. a mod that is off here is being turned off there too, so its settings don't apply — but note the consequence: a value a disabled mod deliberately keeps (the terminal font mod preserves your font choice so re-enabling restores it) stays behind. if you want such a value to travel, turn the mod on, push, then turn it off again. core settings are never touched by either direction — only keys a mod owns. per-host things like the default profile, start path and default host are deliberately not part of a mod setup.","title":"What travels, and what doesn't"}],"icon":"⇄","label":"Sync mods","mod":"mod-sync","order":2098,"slug":"mod-sync"}]}
