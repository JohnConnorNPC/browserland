# Loop status — handoff to next iteration

**Just handled:** F061 — Appearance settings → **done** (FRONTEND inspection-verified, index.html not edited, all live+persist). Theme: `#set-theme` radios (idx:1717, `.set-browser-global` 1715) built from `THEMES` (14534-45); handler (15634-41) `getSettings().theme`+`savePrefs()`+`applyTheme(rb.value)` writes CSS vars `--accent-default`/`--bg` via `root.style.setProperty` (6711-17)+re-pattern. Pattern: `#set-pattern` (1722, gbl 1719), opts 14546-51; handler (15642-46) +`applyPattern()` repaints `#desktop` backgroundImage/Size (6747-79). Term font: `#set-term-font` (1727, gbl 1724), opts 14552-57; handler (15647-51) +`applyTerminalFont()` (6855-72) sets `win.term.options.fontFamily` per live xterm + `fitAddon.fit()`/`refitSoon` reflow. Clock+help toggles: `#set-clock` (1734, gbl 1732, F057) handler 15652-56 applyClock; `#set-help-button` (1739, gbl 1737, #40) handler 15657-61 applyHelpButton. All `.set-browser-global` (1715/19/24/32/37) shown local-tab only `applyBrowserGlobalVisibility` (15250-54); restore `applyThemeSettings()` (6838-48) re-applies theme/pattern/clock/help/font, driven `applyDisplaySettings()` (6889) boot (16167)+every /state pull. No gap, no code change.

**Next to pick:** F062 — Settings persistence (index.html): browser-global vs per-host settings split; restore-on-refresh; taskbar workspace filter; settings+layout synced server-side via the /state store (F077), not localStorage-only. FRONTEND inspect-only. LAST desktop-shell item. (`savePrefs`/`loadPrefs`/`/state` GET+PUT, `putHostState`/`getHostState`, debounce, browser-global localStorage vs per-host /state.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Persistence: `savePrefs()` (debounced) writes browser-global→localStorage AND per-host settings+layout→broker `/state` (F077 GET/PUT); `loadPrefs`/boot restore; `putHostState`/`getHostState` 2313+; restore-on-refresh setting; taskbar-ws-filter `#set-*` 1821. F062 is the LAST F023-F062 frontend item — after it: F063 Add host, F064 host status (multi-host) then F065-F076/F080-F081 MCP (BACKEND .py — pytest-verifiable).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins when code+tests+issue-history show intent (F018/F022); but a USER-FACING WIKI/HELP promise the code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059). Backend (.py) CAN be patched + pytest-verified — F063+ may allow executed tests. Pre-existing index.html change — leave alone.
