# Loop status — handoff to next iteration

**Just handled:** F051 — Open in folder… → **done** (FRONTEND inspection-verified, index.html not edited). Menu item "Open in folder…" `profileMenuItems` (idx:13353-65, label 13354); handler (13356-64) `await openFileDialog({mode:'dir',startDir:'',host})`→`if(dir) launchProfile(host, hostDefaultProfile(host), dir)`. Picker `openFileDialog(opts)` (8110): `mode:'dir'`→"Choose a folder"/"Use this folder" (8129-32), `dlgHost=opts.host||localHost()` (8117) browses passed host; `navigate()` `fileApiPost('/file/list',{path},dlgHost)` (8220-21), dir-mode greys files (8197-202), commit returns `state.cwd` (8159-63). Launch cwd: `launchProfile(host,name,cwd)` (13203) `payload.cwd=cwd` (13215) POST `hostHttpUrl(host,'/launch')` (13216-20, F019 validated); `hostDefaultProfile(host)` (13189-99). Per-host threaded both picker+launch. Cancel no-op: `if(dir)` (13361), Escape (8229-30)/Cancel (8236)/reopen (8111)→`finish(null)`. No gap, no code change.

**Next to pick:** F052 — Start button label (index.html): configurable + button label (blank → `+`); behavior unchanged. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. + button `#btn-launch` 1663; settings/Control Panel `openSettings`(F060); per-host/global settings `getSettings()`/savePrefs; `openFileDialog` 8110 (dir/file picker, /file/list).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
