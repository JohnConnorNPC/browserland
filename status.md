# Loop status — handoff to next iteration

**Just handled:** F016 — Session list endpoint → **done**. `_sessions` (app.py:514-522) returns a BARE JSON ARRAY (not `{sessions:[]}`) built live each call from `BrokerRegistry.session_summaries()` (registry.py:289) → `WindowEntry.summary()` (registry.py:114-130), iterating `self._entries.values()` (no caching/staleness). Per-entry keys: `id`(=window_id), `pid, title, cols, rows`(dims FLAT not nested), `host, kind, agent`(empty string `""` when none, key always present, whitelist claude/grok/codex/opencode via `_whitelist_agent`), `cwd, version, app_cursor, mcp`. Frontend index.html:12765 asserts `Array.isArray(list)`. Auth (F017): gated by `auth_token` only when configured → 401 `{ok:false,error:"auth_required"}`; open with no token (no loopback gate on this route); CORS `*` rides every response incl 401 so login probe distinguishes auth-needed vs host-down. No code change. Verification: py_compile PASS; `pytest -k "session or summary or registry or sessions"` → 18 passed/3 skipped/0 failed; behavioral a(empty→`[]` no crash)/b(window_id=7 fields id/title/cwd/agent=claude/cols=120/rows=40 correct)/c(add 8, deregister 7→ live `[8]`) all PASS.

**Next to pick:** F017 — Auth model + token gating (webterm/broker/auth.py, app.py): env>config token, hmac.compare_digest, per-surface rules (producer loopback-exempt, /launch, /ws, /sessions); WS reject is close 4401. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
