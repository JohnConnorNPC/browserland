# Loop status — handoff to next iteration

**Just handled:** F059 — Keyboard shortcuts + rebinding → **done** (FRONTEND inspection-verified, all 3 documented facets present, index.html not edited). Default bindings + live dispatch: `DEFAULT_KEYBINDINGS` Ctrl+Alt combos for all 17 bound actions (idx:2564-82); capture dispatcher reads LIVE `getSettings().keybindings` (14049), linear-match pressed combo (14051-52), run action (14058) — rebind takes effect; self-heal `normalizeSettings` (2716-25/2797-803). Rebinding UI: "Keybindings" section `<div id="set-keybindings">` (1860-66), `renderKeybindings()` (15710-63) row per KEY_ACTIONS = label+combo(`unset` if none)+Set+Clear; Set arms `_kbRecording={actionId,done}` (15728-45), recorder keydown (14020-43) `comboFromEvent`, Escape cancel (14029), reject modifier-less (14038), write `tt.s.keybindings[act.id]=newCombo`+`tt.save()` (15742-43); Clear deletes (15750-56). Per-host (NOT browser-global): keybindings in per-host settings blob (`normalizeSettings` 2716-25), section is plain `.set-section` in host pane not `.set-browser-global`; `settingsTarget` (14571-94) local→getSettings()+savePrefs, remote→`entry.settings`+`putHostState` `/state`; comment 15706-09. toggle-help unbound: in KEY_ACTIONS (13986-87) but absent from DEFAULT_KEYBINDINGS (comment "#40: unbound by default" 13984-85), normalize fills only DEFAULT keys (2721) so stays `unset` (15722-24). NOTE (informational, NOT a gap — F059 line doesn't promise these): no reset-to-default (only per-row Clear/unbind), no conflict detection (dup combos silently first-match-win). No code change.

**Next to pick:** F060 — Control Panel (index.html): full-screen panel, a TAB PER CONNECTED BROKER (host), all settings sections within. FRONTEND inspect-only. (`openSettings` 13393; per-host tabs; `settingsTarget` 14571 local vs remote /state; sections: appearance F061, keybindings F059, MCP, profiles, hosts F063.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Control Panel: `openSettings()` 13393, full-screen overlay `#settings`/`.settings-*`; per-broker tabs from `hosts[]` (one tab per connected host), `settingsTarget` 14571 (local getSettings/savePrefs vs remote entry.settings/putHostState→/state F077); sections browser-global (`.set-browser-global`) + per-host (appearance/keybindings/clock/MCP/profiles). Remaining shell: F061 appearance, F062 settings persistence.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). Feature LINE is source of truth — unstated "likely also" extras aren't gaps (F047/F059). Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
