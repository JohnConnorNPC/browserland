# Loop status — handoff to next iteration

**Just handled:** F054 — Send to workspace / on all workspaces → **done** (FRONTEND inspection-verified, index.html not edited). Send to `<ws>`: `buildWindowMenu` tiled branch (idx:14238-44) iterate `L.workspaces` skip current `here`→`sendWindowToWorkspace(win,wi)` (6127-51): `removeKeyFromLayout(win.id)` (6135)+`newRow([win.id],1)`→`tgt.columns` push (6136-40)+`savePrefs()` (6141)+park+`requestRelayout`. Send to new ws (14245-51): `newWorkspace()`→`LL.workspaces` push+`savePrefs()` (14249)→`sendWindowToWorkspace(win, last)` (14250). On all workspaces (FLOATING branch `else` 14255, 14267-73): "✓ On all workspaces"/"Show on all workspaces"→`setWindowAllWorkspaces(win, windowWsId(win)!==null)`; flag `prefs._floatWs[key]` null=all-ws (`floatWsMap`/`windowWsId` 5911-21, `setWindowAllWorkspaces` 5928-30 sets null); `applyWorkspaceVisibility` honors `show=(wsId===null)||(wsId===activeId)` (5968) — null never hidden; gated floating (skip win.tiled 5934/5962, taskbar 5987/6000). Persist: tiled `savePrefs` (6141/14249), all-ws `setWindowWs`→`savePrefsLocal` (5925) into `prefs._floatWs`; reload re-apply 5949-75 (dangling self-heal), reopen re-home 12705-23. Help 14664. No gap, no code change.

**Next to pick:** F055 — Workspace manage (index.html): pager-dot Rename / Remove (disabled at one) / Show names vs numbers / New workspace. FRONTEND inspect-only. (Pager dot right-click menu; `renderWsDots` 5682.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Workspaces: `L.workspaces[]`(each has `name`?)/`L.activeWs`, `switchWorkspace` 6094/`addWorkspace` 6118/`newWorkspace`/`sendWindowToWorkspace` 6127; pager `renderWsDots` 5682 (.ws-dot, dot r-click→manage menu); ws-name-vs-number display setting.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
