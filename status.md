# Loop status — handoff to next iteration

**Just handled:** F053 — Workspaces + pager → **done** (FRONTEND inspection-verified, index.html not edited). Switch: `L.workspaces[]`+active `L.activeWs` (init/clamp 3604-05, `activeWorkspace()` 3642-45); `switchWorkspace(index)` (idx:6094-117) park old ws→`L.activeWs=index`→`savePrefs()` (6104)→`renderWorkspaces`+`applyWorkspaceVisibility`+`relayoutStrip`+`applyTaskbarWorkspace`. Pager: `<div id="ws-pager">` (1666); `renderWsDots` (5682-98) one `.ws-dot` per ws, active `' active'` when `i===L.activeWs` (5684, fast re-toggle 5675-76, CSS 1129-33), click→`switchWorkspace(i)` (5689). Shortcuts DEFAULT_KEYBINDINGS (2569-75): `workspace-prev`=Ctrl+Alt+ArrowUp, `workspace-next`=Ctrl+Alt+ArrowDown, `workspace-1..5`=Ctrl+Alt+1..5; handlers (13954-67) →switchWorkspace(activeWs∓1 / 0..4). Add: "+" dot `.ws-dot.add` (5699-704)→`addWorkspace()` (6118-24) push `newWorkspace()`+`savePrefs()`+`renderWorkspaces`+`switchWorkspace(last)`, CSS 1134. No gap, no code change.

**Next to pick:** F054 — Send to workspace / on all workspaces (index.html): title-bar "Send to <ws>" / "Send to new workspace"; "On all workspaces" for floating windows. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Workspaces: `L.workspaces[]`/`L.activeWs`, `switchWorkspace` 6094/`addWorkspace` 6118/`newWorkspace`; window→ws assignment (a `win.ws`/`onAllWorkspaces` field), `applyWorkspaceVisibility`. Menu `buildWindowMenu` 14151. Pager dots `renderWsDots` 5682 (dot r-click = F055 manage).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
