# Loop status — handoff to next iteration

**Just handled:** F040 — Pin / lock to screen → **done** (FRONTEND inspection-verified, index.html not edited). Flag `win.locked`/`getPref(win.id).locked` (per-window). Menu (idx:14258-64): "Lock to screen (pin)"(locked false)/"Unlock (scroll with strip)"(locked true)→`toggleWindowLock(win)`→`setWindowLocked` (5529-48): `getPref(win.id).locked=win.locked`+`savePrefs()` (5532-34), app→`saveAppWindow` (5541-45); serialized prefs `locked:!!win.locked` (2472). **Scroll-independence (load-bearing, exclusion not position:fixed):** `onStripScroll` (5515-28) shifts unlocked floating `win.geom.left-=delta` (5524) to travel w/ columns, BUT guard `if(disposed||tiled||minimized||locked) continue` (5523) SKIPS locked → fixed in viewport. Unlock = same paired item flips. Indicator 📌 CSS `.term-window.scroll-locked .title-text::before{content:'\1F4CC'}` (1366-69), `classList.toggle('scroll-locked',win.locked)` (5533/5543); restore on build `toggle('scroll-locked',!!pref.locked)` (7447), `locked:!!getPref(id).locked` (7564), app `appData.locked` (8470/10396/11227). Help 14677-78. No gap, no code change.

**Next to pick:** F041 — Color & rename (index.html): title-bar color button (palette/custom/recents); double-click an app window's title to rename. FRONTEND inspect-only. (Codex flagged: confirm palette/custom/recents + rename scope + window-type rules + persistence.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Per-window prefs `getPref(id)`/`savePrefs`; serialize ~2472; window build ~7440+ (terminal), 8470 (sticky), 10396 (editor), 11227 (filemgr). Menu `buildWindowMenu` 14151+.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
