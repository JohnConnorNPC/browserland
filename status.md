# Loop status — handoff to next iteration

**Just handled:** F036 — Pop tiled out to float → **done** (FRONTEND inspection-verified, index.html not edited). In `startTiledDrag` (idx:4374). Hold-to-arm: `holdMs=snapHoldMsFor(win)`+`floatMode` flag (4393-94), `setFloatMode` toggles `pop-out-arm` cue (4398-4402), `armDwell` `setTimeout(enterFloat,holdMs)` (4411-17) started 4437, `enterFloat` floatMode=true+`target={kind:'float'}` (4403-10); release armed: `onUp`→`finish(true,ev)` (4447)→`commitDrop` (4484)→kind float→`floatAtCursor` (4303-04)→`detachToFloat(win)` (4299). Move-again cancel: `onMove` re-arm past DWELL_MOVE (4438-43)→`armDwell` does `setFloatMode(false)` (4413) disarm+restart; Escape `onKey` (4457-59)→`finish(false,null)` stays tiled; (right-click→float at cursor onCtx 4448-56). "Float this window" menu (14253-54)→`detachToFloat(win)` immediate skip (inverse "Tile this window"→attachToStrip 14256). Hold-delay shared `snapHoldMsFor(win)` (4114, F037), 0 disables (guard 4415). No gap, no code change.

**Next to pick:** F037 — Drag hold delay setting (index.html): per-host dwell in ms (0 disables both gestures; 250–20000, default 3000). FRONTEND inspect-only. (This IS the setting F035/F036 read via `snapHoldMsFor`→`getSettings().snapHoldMs`; control `#set-snap-hold` 1797/14518, syncs via /state.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. MODEL: `col.rows[]`(row.height) each `row.keys[]`, `row.mode` single/tabbed/split; split→`row.cells[]`+`row.widths`; tabs `cell.keys[]`+activeTab. Float-snap ~12404+ (`armDwell`/`enterSnap`/`#snap-cancel`/`attachToStrip`); tiled-drag pop-out `startTiledDrag` 4374+ (`armDwell`/`enterFloat`/`detachToFloat`); hold-delay `snapHoldMsFor`(F037). Gutters: col `.strip-col-gutter`, split `.strip-hgutter`, row `.strip-gutter`.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
