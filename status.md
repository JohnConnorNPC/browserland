# Loop status — handoff to next iteration

**Just handled:** F050 — Launch (+) menu → **done** (FRONTEND inspection-verified, index.html not edited). + button `<button id="btn-launch">+</button>` (idx:1663-64, title "new terminal — right-click for profiles"), `launchBtn` bound 13185. Left-click (13445-47)→`launchProfile(localHost(), hostDefaultProfile(localHost()))`→`launchProfile` (13203) POST `/launch` (13216-20), profile only when named (13214, F019), btn disable/re-enable (13211/13242). Right-click `contextmenu` (13448-80): single host→`profileMenuItems(host,d)` (13458-59); multi→per-host disabled header `{label:host.label,enabled:false}` (13469)+`profileMenuItems` (13475)+"profiles unavailable" fallback (13472). Profiles from `fetchProfiles` (13250)→GET `/profiles` (13254) cached per host (F078); `profileMenuItems` (13342-67)→`launchProfile(host,name)` (13346)+"Open in folder…" (13353-65, F051). App launchers `appMenuItems()` (13428-39): 📝Sticky→launchStickyNote (13371), 📄Editor→launchTextEditor (13375), 🗂FileMgr→launchFileManager (13382), 🧰TaskMgr→launchTaskManager (13390); appended both single (13459) + multi (13478). 🎛Control panel→`launchControlPanel`→`openSettings()` (13435/13393-98, F060); ❓Help→`launchHelp`→`focusOrOpenHelp()` (13436/15167-81, F058). No gap, no code change.

**Next to pick:** F051 — Open in folder… (index.html): per-host directory picker that starts the host's default profile rooted at the chosen folder. FRONTEND inspect-only. (Already spotted in profileMenuItems 13353-65; uses /launch cwd param F019 + /file/list dir picker F022. Codex flagged: /file/* usage, allowed roots, hidden files, cancellation, cwd validation, errors.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Launch: `launchProfile(host,name)` 13203 POST /launch (cwd→F019 validated), `profileMenuItems` 13342, `fetchProfiles`/GET /profiles 13250 (F078), launchers 13371-91, `openSettings`(F060)/`focusOrOpenHelp`(F058). Dir picker likely reuses /file/list (F022).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
