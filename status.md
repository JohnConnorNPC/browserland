# Loop status — handoff to next iteration

**Just handled:** F034 — Drop-zone overlay cheat sheet → **done** (FRONTEND inspection-verified, index.html not edited). Overlay nodes: `#drop-vbar` (new-column accent bar, HTML 1674/CSS 1307-15), `#drop-col` (consume/tab/split rect, 1675/1316-24), `#drop-float` (bottom dashed float band, 1676/1325-41), `#snap-cancel` (1677/1345-61); `dropEls()` 4128-34, `showDrop(target)` 4244-90, `hideDropEls()` 4135-40. Live update: tiled `onMove` (4428-46) rAF `frame()` recompute `computeDropTarget(lastX,lastY)`+`showDrop` each frame (4418-27); float-snap `onMove` 12464-65 + initial 12423-24. All 5 kinds w/ distinct geometry: float bottom band (4250-54), new-column vbar at col edge (4283-88), tab top TAB_BAND (4262-69), split L/R half (4270-75), stack T/B half (4276-77) + consume full-col "new bottom row" (4255-61) — matches commitDrop (4301-14). Clear: `hideDropEls` (4135-40) at showDrop top (4248), finish() commit/cancel/blur (4475), Escape onKey→finish (4457-59); snap teardown→hideDropEls+hideSnapCancel (4443-44) on Escape (12543-46)/blur (12563-65)/mouseup. No gap, no code change.

**Next to pick:** F035 — Snap floating into grid (index.html): hold-to-snap dwell gesture arms drop zones; Escape/return-zone cancels; "Tile this window" menu skip. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. MODEL: `col.rows[]`(row.height) each `row.keys[]`, `row.mode` single/tabbed/split; split→`row.cells[]`+`row.widths`; tabs `cell.keys[]`+activeTab. Drop: `computeDropTarget`/`windowDropAt` L/R/T/B→kind split/consume/tab/column/float; `commitDrop`/`splitAtWindow`; overlay `showDrop`/`hideDropEls` (#drop-vbar/#drop-col/#drop-float/#snap-cancel). Gutters: col `.strip-col-gutter`, split `.strip-hgutter`, row `.strip-gutter`.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
