# Loop status — handoff to next iteration

**Just handled:** F072 — read_screen wait modes → **done** (BACKEND pytest-verified: 51 passed / 17 wait-targeted, no .py change). Arch: MCP tool→client pass-through, BROKER validates (exclusivity/timeout cap/surfaces matched), AGENT runs re-snapshot poll loop + predicates. Three modes + wait_absent: declared `server.read_screen` (server.py:320-366)→`client.read_screen` (client.py:143-181)→broker `_mcp_read` parse (app.py:1155-85)→producer `_on_screen_request` (agent.py:444-51); predicates in agent loop: `wait_for_text` substr (476), `wait_for_regex` regex.search (478), `wait_absent` inverts `_predicate_met` (474-81), `wait_for_change` hash-differs (551); poll loop `_run()` (530-79) re-renders `_render()` (483-93) parking on output-waiter futures until predicate/deadline. Exclusivity: `n_wait=sum(bool(x) for x in (change,text,regex))` >1→400 `conflicting_wait` (app.py:1178-84); wait_absent = modifier flag NOT counted. Timeout cap: clamp `max(0,min(timeout_ms,MAX_MCP_WAIT_MS=15000))` (app.py:1197/91), agent re-clamp MAX_WAIT_MS=15000 (532/41); matched True met (550)/False timeout (556), surfaced broker for predicate reads (1224-25). Tests: `pytest tests/test_protocol.py tests/test_integration.py -q`→**51 passed**; `-k "wait or matched or screen_text"`→17 passed; coverage test_integration.py:272-407/499-506 (change return/timeout/huge-timeout-clamp 307/reset-wake; text appear/timeout/already-present; regex match/invalid-safe; wait_absent disappears) + test_protocol.py:170-232 (frame shape). RECURRING NOTE: `conflicting_wait` exclusivity 400 + direct broker timeout-cap have NO test (agent clamp IS tested) — impl sound, coverage gap only. No gap.

**Next to pick:** F073 — read_screen delta mode (webterm/broker/app.py, webterm/agent). BACKEND pytest-verifiable. Verify: `since=<prior content_hash>` → if screen unchanged-ish returns `delta=true` + `changed_rows` (ONLY the rows that differ from the prior snapshot, each with its row index), ELSE (or no since / big change) returns a FULL grid with `delta=false`. Confirm: since param→row-diff against prior, changed_rows carries row idx+content, delta flag true/false, full-grid fallback when since absent or mismatch. Run tests (grep `delta`, `since`, `changed_rows`). Codex flags: row-diff correctness, delta=false fallback conditions (no since / hash baseline gone / too many changes), changed_rows indexing, hash continuity with F070.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/*, protocol.py). Sub-agent CAN read+patch .py AND run pytest. read_screen suite (F070-F073) HAS real committed tests: `tests/test_{pyte_snap,textgrid,snapshot_raw,protocol,altscreen,integration,mcptool}.py` — RUN relevant subset. broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). app.py: `/mcp/read` 1129 (view/lines/since/wait params), agent `_render_screen_text` agent.py:57, `_run` poll 530.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
