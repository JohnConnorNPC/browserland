# Loop status — handoff to next iteration

**Just handled:** F042 — Arrange-all + undo → **done** (FRONTEND inspection-verified, index.html not edited). Menu `buildCtxMenu(x,y)` (idx:14339) wired `#desktop` contextmenu (14446-54, bails `e.target.closest('.term-window')`), taskbar bg 14471, `renderMenu` 13778. 5 commands (in `if(!tiling)` branch 14347): Cascade `doCascade` (13629, CASCADE_DX/DY stagger 13644-45, item 14350); Tile H `doTileHorizontal` (13657, full-width stacked rows, 14351); Tile V `doTileVertical` (13684, full-height side-by-side, 14352); Tile H+V `doTileGrid` (13711, ceil(sqrt(N)) cols × ceil(N/cols) rows, 14353); Minimize All `doMinimizeAll` (13750, item 14358-60 enabled if some float non-min). Single-level undo: var `lastLayoutSnapshot` (13542, "prior snapshots discarded"), `snapshotAllWindows(label)` overwrites before each arrange (13544, called 13632/13661/13688/13716/13755), `doUndoLayout` restores geom+minimized once then `=null` (13760-63), item "Undo <label>" only when snapshot exists (14379-86). Scope floating-only: `floatingWindowsOrdered` skips tiled (13608-10), `snapshotAllWindows` skips win.tiled (13547), `applyGeomToWindow` early-return tiled (13560); tiling-mode menu→workspace switcher (14361-76). No gap, no code change.

**Next to pick:** F043 — Lock Size (global) (index.html): snap all floating windows to default size and hide resize handles; unlock restores. FRONTEND inspect-only. (Last Window-Management item; F044+ = Window Types.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Per-window prefs `getPref(id)`/`savePrefs`/`saveAppWindow`; resize handles `.rh` (F023), `isResizable()` 12145; floating geom `win.geom` left/top/w/h, `applyGeomToWindow`. Menu `buildCtxMenu` 14339 / `buildWindowMenu` 14151.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
