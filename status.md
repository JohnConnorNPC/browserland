# Loop status — handoff to next iteration

**Just handled:** F013 — Task-manager backend contract → **done**. `agent.py` wire handlers delegate to `backends/base.py`. `enumerate_procs` (base.py:195): flat list `{pid,ppid,name,cmdline,cpu,mem_mb,status,create_time}`, rooted at shell+children(recursive), `_MAX_PROCS=500`, cmdline trunc 512, every field try/excepted. Identity guard: `note_session_started()` pins `_root_ctime`=shell create_time; `_session_root` matches within `_CTIME_EPS=0.5s` (rejects recycled root); `kill_proc` builds member create_time map → `not_in_session` if non-member, then re-instantiates+re-verifies target create_time (`identity_mismatch`/`already_gone`) closing enumerate→kill TOCTOU. Destroy = `kill_proc(shell_pid)` (children-first terminate→wait 1.5s→kill, cascades shell, no leak). Linux SID/pgid `/proc` fallback (linux_pty.py:309); Windows psutil path → `psutil_unavailable` if psutil missing (no crash). Wire: `procs_please→procs{procs[]}`, `kill{pid}→killed{ok,error?}` (executor thread, ordered out-queue). No code change. Verification: py_compile PASS; `pytest -k "proc or task or kill or destroy or enumerate or backend"` → 15 passed/6 skipped(POSIX)/0 failed; behavioral 15/15 PASS (enumerate incl spawned child; guard refuses self_pid/high-pid/recycled→not_in_session/session_gone; kill own in-session child→gone no stray; dead-pid→session_gone no raise).

**Next to pick:** F014 — Broker producer registration (webterm/broker/app.py, registry.py): `WS /browserland` accepts producers, requires hello first, same window_id replaces. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps.
