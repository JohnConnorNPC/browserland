# Loop status — handoff to next iteration

**Just handled:** F020 — Serve desktop UI + login overlay → **done**. Server: `_index` (app.py:289-290 `return html(INDEX_HTML)`) does NO token check; route ungated `app.add_route(_index,"/",GET)` (app.py:1462); only `_cors_headers` middleware (non-gating). GET `/` → 200, `text/html; charset=utf-8`, body `<!DOCTYPE html>`, serves even WITH token configured = UNGATED. No separate asset route (single self-contained inlined index.html). Load: CACHED-AT-IMPORT — `INDEX_HTML = Path(...,"index.html").read_text()` at ui.py:23-24 module scope → frontend edits need broker RESTART (matches known stale-index behavior). index.html (inspected read-only, NOT edited — pre-existing change preserved): localStorage key `webterm:prefs:v1` (STORAGE_KEY idx:1920; token in per-host `.token` via `savePrefs`→`setItem` idx:1990); /sessions 3-way probe `pollHost` (idx:12746-12778: 401→authNeeded+overlay, 200/array→ok authed, catch TypeError→host down); `?token=`/`?auth=` adopt+scrub `adoptUrlToken()` (idx:6211-6223: set token+savePrefs, `searchParams.delete`, `history.replaceState(null,'',u)` at idx:6221). No code change. Verification: py_compile PASS; `pytest -k "ui or index or serve or html or login or root or overlay"` → 18 passed/0 failed; server-side `create_app({auth_token:'SEKRET'})`+`_index` → 200 html ungated.

**Next to pick:** F021 — Single active-browser lease (webterm/broker/app.py, index.html): exactly one browser is the layout writer; "Become active" takeover prompt; terminals keep running regardless. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; do NOT edit index.html for a feature (inspect read-only); only commit every_feature.md + status.md (+ any real code fix in a .py file a feature requires). Frontend (index.html) features = verify by inspection only.
