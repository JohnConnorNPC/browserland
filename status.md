# Loop status — handoff to next iteration

**Just handled:** F070 — read_screen rendering → **done** (BACKEND pytest-verified: 294 passed, REAL committed coverage incl broker /mcp/read e2e, no .py change). Bounded grid+pyte/fallback: `_mcp_read` (app.py:1129) correlated RPC `screen_text_please`→`screen_text` (1201-07, forward 1211-35); agent `_render_screen_text` (agent.py:57-114) pyte `pyte.Screen(cols,rows)` (85-95)→textgrid `render_screen` on import/parse fail (99-106)→raw decode (107-114); bounds: broker `lines=max(0,min(lines,1000))` (1149)+timeout (1197), textgrid clamp cols/rows `_MAX_DIM=1000` (textgrid.py:47/165-66)+history `_MAX_HISTORY_LINES=1000`/`_MAX_HISTORY_CELLS=100_000` (58-59/170), raw tail-cap `max(cols*rows*4,4096)` (agent.py:111-12). Fields: produced agent dict (93-95/104-06/113-14)→`protocol.screen_text_frame` (522-28)→broker `alt_screen` (app.py:1216), `cursor` (1221), `content_hash` (1220, `_content_hash` blake2b digest_size=16 agent.py:49-54/537), `view` (1218); determinism `test_content_hash_is_stable_and_sensitive` (test_integration.py:252-56). degraded only raw: False pyte (93)/textgrid (104), True only raw (113, view:"raw",cursor:None) + error catch-all (585-89); broker emits only when truthy (1233-34); tests `test_render_screen_text_degraded_cursor_null` (test_textgrid.py:259-70), `test_agent_uses_bounded_grid_without_pyte`/`test_agent_pyte_path_renders_grid` (226-46). Tests: `pytest tests/test_pyte_snap.py tests/test_textgrid.py tests/test_snapshot_raw.py tests/test_protocol.py tests/test_altscreen.py tests/test_integration.py tests/test_mcptool.py -q`→**294 passed**; e2e /mcp/read via test_integration.py `_read_screen` (live agent+broker RPC). NOTE: wire name `screen_text_please`/`screen_text` (not brief's `snapshot_please`) — mechanism matches. No gap.

**Next to pick:** F071 — read_screen scrollback view (webterm/broker/app.py, webterm/agent). BACKEND pytest-verifiable. Verify: `view="scrollback"` with `lines=N` PREPENDS N history lines ABOVE the current grid, reports `history_lines`; ALT-SCREEN FORCES `view="screen"` (scrollback not meaningful in alt-screen TUIs). Confirm scrollback assembly (history+grid), lines param bound, history_lines count reported, alt-screen override forces screen view. Run snapshot/integration tests (the F070 suite covers scrollback too — grep `scrollback`, `history_lines`, `view`). Codex flags: history line cap, alt-screen force, history_lines accuracy, empty-history, ordering (history above grid).

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, launcher.py, mcptool/server.py+client.py, agent/snapshot/{raw,pyte_snap,textgrid}.py, agent/agent.py, protocol.py). Sub-agent CAN read+patch .py AND run pytest. read_screen suite (F070-F073) HAS real committed tests: `tests/test_{pyte_snap,textgrid,snapshot_raw,protocol,altscreen,integration,mcptool}.py` (294 passed) — RUN the relevant subset per feature. broker behavior also via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). app.py: `/mcp/read` 1129 (view/lines/since params), agent `_render_screen_text` agent.py:57.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief (snapshot_please) ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
