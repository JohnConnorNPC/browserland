# Loop status — handoff to next iteration

**Just handled:** F064 — Host status chips → **done** (FRONTEND inspection-verified, index.html not edited). Chip-per-host+states: `renderHostStatus()` (idx:12796-838) iterate `allHosts()` append one `.host-chip`/host; `hostChipState()` (12785-95) returns `auth|down|lease|ok`; CSS `.host-chip.ok/.down/.auth/.lease`(+`.off` hidden) 103-110; container `#host-status` 1667. Status source (split by design): `pollHost()` GET /sessions (12746-78) `st.ok=true` success / `st.ok=false`+`consecFailures++` fail / `st.authNeeded=true`+`showAuthOverlay` on 401; `lease` from /control WS (F021) `onControlStatus`→`onRemoteControlStatus`→`applyRemoteLease()` `st.leaseInactive` (16147-63)→hostChipState 'lease' (12793). Click: auth→`showAuthOverlay(host,true)` token overlay (12814-15, impl 6261-80); lease→`sendBecomeActive(host)` `{type:'become_active'}` on /control to take over (12823-24, impl 6638-44); ok/down→`toggleHostHidden(host.id)` (12833-34) flips `h.hidden`+save+`applyHostVisibilityAll()` (2922-29) toggles `.broker-hidden` (display:none) on host's `.term-window`+`.taskbar-item` via `hostHidden(id)` (2892-920, CSS 112-13). No gap, no code change.

**MULTI-HOST DONE (F063-F064). Entering MCP / AI AGENTS — BACKEND (.py), pytest-verifiable.**

**Next to pick:** F065 — MCP HTTP core + token gating (webterm/broker/app.py). BACKEND — sub-agent CAN patch + run pytest. Verify: `GET /mcp/info`, `GET /mcp/terminals`, `GET /mcp/profiles`; MASTER ENABLE gate (mcp disabled→all /mcp/* blocked); MCP token DISTINCT from auth_token (bearer `Authorization: Bearer <tok>` OR `?token=` query); error table (401 missing/bad token, 403/404 disabled, proper JSON errors). Run the existing MCP test(s) (likely webterm/test_mcp*.py or tests/ — sub-agent greps for them) + confirm token≠auth_token separation. Codex flags: token precedence (env WEBTERM_MCP_TOKEN > sidecar > config), compare_digest, disabled-returns-consistent-error, info leak in errors, query-vs-header parity.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** these are BACKEND `.py` (webterm/broker/app.py, mcptool/server.py+client.py, agent/snapshot/*). Sub-agent CAN read+patch .py AND run pytest (find tests via grep: `test_mcp`, `mcp` in webterm/*test*.py or conftest). Verify by EXECUTING the relevant test(s) and/or reading app.py route handlers. real gap in .py → may FIX + re-test (then I commit the .py too). If a feature is purely index.html (e.g. F067 robot button UI), inspect-only + don't edit index.html. Only commit every_feature.md + status.md (+ any .py fix). MCP endpoints in app.py: `/mcp/info|terminals|profiles|read|input|launch|config`, `/session/mcp`; token via env`WEBTERM_MCP_TOKEN`>sidecar`webterm_mcp.json`>config; master `enabled`, `default_mode`, `allow_launch`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
