# Loop status — handoff to next iteration

**Just handled:** F045 — Sticky note + Closed notes → **done** (FRONTEND inspection-verified FULLY incl. closed-notes reopen, index.html not edited). Sticky note: `launchStickyNote()` (idx:13371-73, `appKind:'sticky-note',content:''`); `openAppWindow` factory isNote (8460-62), editable `<textarea>` (8558-63, spellcheck=isNote, placeholder 'Note…'); persist debounced autosave `onInput→saveAppWindow(win)` (9165-79) + on color (9073); `saveAppWindow` writes `appStore[id].content` (2435-65), `saveAppStore` flush (2403). Off-taskbar: chip creation `if(!isNote)` skip (10353-67, notes always-on-top reachable from +menu). Stash-on-close: `closeWindow` saveAppWindow first (12222), then trim (12259-70): `sticky-note && content`→`rec.content=content;rec.open=false` (stash) else `delete appStore[id]` (empty+other kinds discarded)+`saveAppStore`. Reopen: `closedAppMenuItems()` (13402-27) filters `open===false && !windows.has(id) && sticky-note && trimmed content` (13408-12), disabled "Closed notes" header (13415) + per-note preview item `action:openAppWindow(a)` (13422-24); wired into +menu `appMenuItems()` spread `...closedAppMenuItems()` (13437). No gap, no code change.

**Next to pick:** F046 — CodeMirror text editor (index.html): syntax-highlighted file editor opened at active terminal's cwd/host; save-prompt on close; backed by /file/* (F022). FRONTEND inspect-only. (Codex flagged F046 facets: host/cwd resolution, file open/save, syntax mode, dirty tracking, save-prompt, /file/* integration, close semantics.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. App windows: sticky 8470, editor 10396, filemgr 11227; `appStore`/`saveAppStore`/`saveAppWindow`/`openAppWindow`; `/file/*` host file API (F022) backs editor+filemgr — read `{content}`/`{content_b64}`, write atomic, errors not_found/not_a_file/bad_path. Active cwd/host from focused terminal's session.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully (don't skip on dep-order). Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F058 help-MCP-status dep F065 (MCP status row) — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
