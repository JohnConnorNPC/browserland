# Loop status — handoff to next iteration

**Just handled:** F035 — Snap floating into grid → **done** (FRONTEND inspection-verified, index.html not edited). Hold-to-snap dwell: `armDwell()` (idx:12426-31) `holdTimer=setTimeout(enterSnap,holdMs)` (12430), started at grab 12583; `enterSnap()` (12408-25) flips `snapMode=true`+showSnapCancel+showDrop; before armed `onMove` (12448) box follows cursor, only `if(snapMode)` branch (12459-66) computes/shows grid target → no snap pre-dwell; move>`DWELL_MOVE`=6px re-arms (12488-89); `HOLD_MS=3000` default (4102), armDwell no-ops when holdMs<=0 (12429). Escape/return cancel: `onKey` (12541-52) `snapMode && Escape`→`teardown()`+`applyGeomToWindow(win,originalGeom)` (stays floating), capture+stopPropagation 12580; `#snap-cancel` (HTML 1677/CSS 1345-61) `snapCancelRect`/`inSnapCancel` (4144-64), onUp (12503) `inSnapCancel||!snapTarget`→restore geom; blur cancel (12563-77). "Tile this window" menu (14256-57)→`attachToStrip(win)` immediate skip (help 14640). Dwell=`snapHoldMsFor(win)` (4114-27) reads `getSettings().snapHoldMs`/host-cache (F037 link, `#set-snap-hold` 1797/14518, 0=disabled). No gap, no code change.

**Next to pick:** F036 — Pop tiled out to float (index.html): hold-to-arm "release to float"; move-again or Escape cancels; "Float this window" menu skip. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. MODEL: `col.rows[]`(row.height) each `row.keys[]`, `row.mode` single/tabbed/split; split→`row.cells[]`+`row.widths`; tabs `cell.keys[]`+activeTab. Drop overlay `showDrop`/`hideDropEls`; float-snap path ~12404-12585 (`armDwell`/`enterSnap`/`snapMode`/`#snap-cancel`/`attachToStrip`/`detachToFloat`); hold-delay `snapHoldMsFor`(F037). Gutters: col `.strip-col-gutter`, split `.strip-hgutter`, row `.strip-gutter`.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
