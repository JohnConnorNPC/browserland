# Loop status — handoff to next iteration

**Just handled:** F048 — Task manager UI → **done** (FRONTEND inspection-verified, index.html not edited). `openTaskManagerWindow(appData)` (idx:11222, appKind 'task-manager', class `app-tm`, opened 13391). Cross-host session list: `render()` (11587) from `liveSessions()` (11326-27) reading global `sessions` map = merge of ALL hosts (`for host of hosts`…`merged.set(host.id+':'+id,{…hostId,hostLabel})` 12877-904, written 12958-59), host label per row when multiHost (11580/11645-49). Live: `setInterval(..,2500)` re-render + re-pull expanded (11765-77) + main /sessions poll. Expand: `toggle` (11675-85)→`fetchProcs(sess)` (11352) POST `/session/procs` (11360)→`res.json.procs[]` (11375-76); `buildProcTree` (11456)/`renderProcs` (11491) fields pid(11527)/name(11531)/cmdline(11534)/mem_mb+cpu%+status(11538-44). End process: per-proc "End" (11545-70)→`endProcess(sess,pid)` (11428) POST `/session/kill {pid}` (11436) + confirm (11565-67), re-fetch on ok. Destroy: per-session "✕ destroy" (11657-73) confirm (11669)→`destroySession` (11391) POST `/session/kill {pid:sess.pid}` (11398), 409 session_gone=success+teardown (11404-21); root "kill shell" 11548-64. Uses broker `/session/procs`+`/session/kill` host-aware HTTP (`sessionPost` 11332) — allowed alt to raw producer WS. No gap, no code change.

**WINDOW TYPES SECTION COMPLETE** (F044-F048, 5 types: terminal/sticky+notes/editor/filemgr/taskmgr).

**Next to pick:** F049 — Taskbar window buttons (index.html): one button per window (notes excluded); click focus/raise/minimize-toggle; right-click Focus/Minimize/Close/Terminate. First Desktop Shell item. FRONTEND inspect-only. (Overlaps F038/F039 window-action semantics — F049 = taskbar entry points.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Taskbar: `onTaskbarClick` 12728, `updateTaskbarActive` 7383, chip build `if(!isNote)` skip notes (10353-67); window actions `closeWindow`/`minimizeWindow`/`restoreWindow`/`bringToFront`/`terminateWindow`(14110). Menu `buildWindowMenu` 14151.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
