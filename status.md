# Loop status — handoff to next iteration

**Just handled:** F079 — Per-host default profile + start path → **done** (inspection-verified, index.html not edited; read app.py/launcher.py too). Default-profile: `<select id="set-default-profile">` (idx:1828-37, per-host — no `.set-browser-global`), `renderDefaultProfile()` (15373-99) populates from target host `/profiles` reading `t.s.defaultProfile` (15376), change handler writes `t.s.defaultProfile`+`t.save()` (15697-704), seed `s.defaultProfile=''` (2749-51); `hostDefaultProfile(host)` (13189-99) local live getSettings / remote hostStateCache, returns defaultProfile or **null when ''/missing**→`launchProfile` omits payload.profile→`launcher.py:108` `name=profile_name or self._default_profile` fallback; feeds + button (13446/13361/13970). Start-path: `<input id="set-start-path">` (1755-63, per-host comment 1753-54), `startPathForDisplay(s,tgtHost)` render (15342-62), `commitStartPath` debounced writes `t.s.startPath`+del legacy startPaths+`t.save()` (15676-91), normalize 2757-66. Roots new shells: `resolveStartPath(host)` in **index.html JS** (13279-312), `launchProfile` `if(!cwd) cwd=await resolveStartPath(host)` (13210)→POST /launch payload.cwd (13215); server validates `app.py:573-590` `_parse_launch_body` realpath+isdir (blank→None→agent default, non-dir→cwd_not_dir), `launcher.py:100-130` passes validated --cwd. Per-host+/state: both bind `settingsTarget` t.s (local getSettings / remote hostStateCache 2257), `t.save()`=savePrefs or remote /state PUT `saveHostSettings` (2306-54), comments #17/#10 (1753/13270-78/15342-44/15670-74), isolated per host. NOTE: no ~/env expansion (realpath+isdir only) — but F079 line doesn't promise expansion (UI hint "Must be an existing folder"), so NOT a gap (F047/F059 principle). No code change.

**DESKTOP-SHELL + PER-HOST-SETTINGS DONE (F023-F062, F078-F079). Entering MULTI-HOST.**

**Next to pick:** F063 — Add remote host (index.html, app.py): Control Panel → Hosts: add a broker by label/URL/password; browser connects DIRECTLY cross-origin to that broker; per-host settings tab appears. Mostly FRONTEND (host list in `prefs._hosts`, `getHosts`, add/remove host UI in Browser pane ~1869-88, direct cross-origin fetch/WS to host URL, auth via that host's token); app.py side = the CORS `*` + token endpoints that allow cross-origin (F018, already verified). Inspect-only for index.html; app.py CAN be pytest-checked if a host/CORS test exists. Codex flags: URL normalize/validation, dup hosts, cross-origin auth failure UX, removing the local host, persistence of host list (browser-local `_hosts`, NOT /state — it's the list OF brokers).

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE:** index.html parts = FRONTEND inspect-only (read-only grep/read, file:line evidence, do NOT edit index.html — pre-existing change). `.py` parts (app.py/launcher.py/registry.py/relay.py for F063-F076) = BACKEND, CAN patch + pytest-verify (run the named test; tests live in webterm/ test files). real gap → report finding; index.html gap→split to ## Blocked (don't fix); .py gap→may fix+test. Only commit every_feature.md + status.md (+ .py if a backend feature needed a fix). Hosts: `prefs._hosts` (browser-local, 2061-62), `getHosts`, host add/remove UI Browser pane 1869-88, `hostHttpUrl`/`hostWsUrl`, per-host auth token, `renderSettingsTabs` per-host tabs.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; run the named test. Pre-existing index.html change — leave alone.
