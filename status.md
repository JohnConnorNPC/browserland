# Loop status — handoff to next iteration

**Just handled:** F058 — In-app help guide → **done** (FRONTEND inspection-verified, full system incl MCP-status row, index.html not edited). ? chip `#help-chip` (idx:1670-71), `wireHelpChip()` (15190-97) click+Enter/Space→`focusOrOpenHelp()` (15167-81)→`findHelpWindow()` (14975-80, match appKind 'help') / `openHelpWindow({})` (15001, class app-help, newAppId('help')); openAppWindow delegates 'help' (8399-400); single-instance (14013-18/15013-18). Live search: `<input class="help-search">` in `buildHelpBody` (14925-29), `onInput=()=>renderHelpInto(win,search.value)` bound 'input' (14950-51); `renderHelpInto` filters cached `_hay` case-insensitive (14803-08), re-renders rail/grid+count; clear btn (14952-53)+scoped Escape (14959-65). One-entry-per-shortcut: `buildHelpEntries()` (14704-46) loop `KEY_ACTIONS` (14708-16) push 'Keyboard shortcuts' entry per action title=act.label body='Bound to '+combo (or Unbound), combo=`map[act.id]` from `getSettings().keybindings`; KEY_ACTIONS (13945-88)=every DEFAULT_KEYBINDINGS id (2564-82)+toggle-help, map seeded from DEFAULT_KEYBINDINGS (2721-23); per-card `.help-kbd` badge (14879-84). MCP status row: `buildHelpEntries` (14731-40) reads `mcpConfigCache.get(localHost().id)` (F065 cache decl 14568) push 'MCP status (this host)' enabled/default_mode/allow_launch; refresh `fetchMcpConfig(localHost()).then(()=>refreshHelpCorpus(win))` (15158, refreshHelpCorpus 14983-87); static MCP prose (14691-94). No gap, no code change.

**Next to pick:** F059 — Keyboard shortcuts + rebinding (index.html): full default bindings table; per-host rebinding in Control Panel; Toggle help action unbound by default. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Keybindings: `DEFAULT_KEYBINDINGS` 2564-82, `KEY_ACTIONS` 13945-88 (incl `toggle-help` UNBOUND default), live map `getSettings().keybindings` seeded 2721-23; capture dispatcher matches combo→`act.run()` 14058; Control Panel rebinding UI (per-host) — a "Keyboard" settings section with per-action capture inputs, reset-to-default, conflict detect. PER-HOST: keybindings in per-host settings (vs browser-global). Remaining shell: F060 Control Panel, F061 appearance, F062 settings persistence (/state F077).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
