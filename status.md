# Loop status — handoff to next iteration

**Just handled:** F021 — Single active-browser lease → **done**. app.py `_init` (~L350-357): single GLOBAL holder `app.ctx.active_client_id` (one clientId, broker-wide NOT per-host) + `control_clients` (cid→set of `/control` sockets) + `lease_lock` (asyncio.Lock); in-memory, resets None on restart; no seq counter — stale-writer rejection via re-reading `active_client_id` live under lock on each `/state` PUT (L1428-1442) + each `/ws` input (relay.py:68). `_control_ws` (L414-500): connect `/control?clientId=` auto-claims iff holder None; `{type:become_active}` → under lock set holder=me, then OUTSIDE lock `registry.close_clients_terminals(cid,4409)` on others + push `control_status_frame(active=False,owner=new)` to losers / `active=True` to winner (old holder's terminal-VIEW sockets closed 4409, page tears down). Terminals NOT lease-gated (relay L60-99, registry L300-317): producer never touched; output broadcast never gated (passive viewers keep stream); only WRITES gated — input/resize dropped for non-active (relay L68-95), `PUT /state`→409 `not_active` (L1436); MCP bypasses lease (L1252-1254). Disconnect frees (L480-500 finally): if holder & no other `/control` sock → holder=None, notify remaining (no auto-promote); reconnect auto-claims. index.html takeover UI present (inspected read-only, not edited). No code change. Verification: py_compile PASS (app/registry/relay/protocol); `pytest -k "lease or active or control or takeover or writer or become"` → 9 passed/1 skipped/0 failed; live-broker e2e `test_control_lease` RAN a(first auto-active)/b(2nd become_active→1st demoted 4409+notified)/c(terminals independent: passive still gets output, input dropped, producer runs)/d(disconnect frees, no auto-promote, fresh client claims) all PASS + /state not_active gate.

**Next to pick:** F022 — Sandboxed file API (webterm/broker/app.py): `/file/*` read/write/list under the host (backs editor + file manager), auth_token-or-loopback gated. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; do NOT edit index.html for a feature (inspect read-only); only commit every_feature.md + status.md (+ any real .py code fix a feature requires). Frontend (index.html) features = verify by inspection only.
