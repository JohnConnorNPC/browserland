# Loop status — handoff to next iteration

**Just handled:** F044 — Terminal window → **done** (FRONTEND inspection-verified, index.html not edited). xterm: `new Terminal({...})` (idx:7516-23), `new FitAddon.FitAddon()` (7524), `loadAddon` (7525), `term.open(body)` mount into `.term-body` (7526). Output: `ws.onmessage`→binary `win.term.write(new Uint8Array(event.data))` (11930), JSON `{type:output}`→`win.term.write(data.data)` (12013), non-JSON fallback (11935), connect banner (11923). Input: `term.onData(d=>sendChunked('input',d))` (7966)→`win.ws.send(JSON.stringify({type,data}))` chunked (7881), r-click paste `sendChunked('paste',text)` (7899). Fit/resize: `fitAddon.fit()` (6868), `sendResize` cols/rows from cell dims (12163-64)→`ws.send({type:'resize',cols,rows})` (12171) debounced `scheduleResize` (12175-80), handles 7864-66; incoming `{type:resized}`→`term.resize(cols,rows)` (11956). No gap, no code change.

**Next to pick:** F045 — Sticky note + Closed notes (index.html): off-taskbar scratch pad; non-empty closed notes reopen from the + menu's "Closed notes". FRONTEND inspect-only. **DEP NOTE: F045 closed-notes reopen references F050's + menu. Since browserland is ALREADY BUILT, F050's menu code EXISTS in index.html now — so VERIFY F045 FULLY (incl. closed-notes reopen from the + menu); do NOT skip. The skip-and-return rule was a build-time hedge, moot for verification.**

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. App windows: sticky-note 8470, editor 10396, filemgr 11227; `appStore`/`saveAppStore`/`saveAppWindow`; `/file/*` backs editor+filemgr (F022). Launch (+) menu = F050; closed-notes reopen from it.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully (don't skip on dep-order). Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F058 help-MCP-status dep F065 (MCP status row) — that part needs F065's /mcp; verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
