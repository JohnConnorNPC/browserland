# Loop status — handoff to next iteration

**Just handled:** F063 — Add remote host → **done** (FRONTEND inspection-verified, index.html not edited; app.py CORS confirmed). Add-host UI: Hosts section `#set-pane-browser` label/URL/password + `#set-host-add` btn + error div (idx:1869-88), bindings 15770-75, `commitHostForm` (15819-65, wired hostAddBtn 15865); `normalizeHostUrl` (15791-801→`u.origin` or null); validation reject invalid URL (15824)/same-origin "this broker" (15828)/dup `hosts.some(h=>h.url===url)` (15834)/no-password new remote (15848); append `hosts.push({id:mintHostId(),label,url,token:pass})` (15855-57) into `prefs._hosts` (getHosts 2856-77); edit path reuses form keeping id (15838-46). Direct cross-origin: `hostHttpUrl` (6229-32, base=host.url remote origin or '' local)+token, `hostWsUrl` (6233-43, http→ws), `appendHostToken` ?token=host.token (6244-48); direct fetch/WS — pollHost /sessions (12751), profiles (13254), launch (13216), control WS (6555); NOT proxied; CORS `*` app.py:384. Per-host tab+sessions: `renderSettingsTabs` (15221-38) iterate getHosts (15232) one tab/host, `selectSettingsTab` awaits remote /state; sessions per host pollHost hostHttpUrl/sessions (12751), profiles 13254. Persistence+remove: browser-local `savePrefs`→`savePrefsLocal` localStorage (1989-90/1998), `_stateBlob` pushes ONLY settings+layout (2094-101), `_hosts` excluded (1996-97/2061-62) — never on /state; `removeHost` (15867-928) `if(id==='local')return` guard (15868), local always unshift idx0 (getHosts 2870-75), edit/remove btns only non-local (renderHostsList 15967-72). No gap, no code change.

**Next to pick:** F064 — Host status chips (index.html): per-host taskbar chips showing ok / down / auth-needed (click to log in) / lease (click to take over); clicking toggles that host's windows visibility. FRONTEND inspect-only. (Per-host poll `pollHost` 12751 sets status; chip render in taskbar; states from /sessions success / fail / 4401-auth / lease-held; auth modal; `/control` lease F021.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE:** index.html parts = FRONTEND inspect-only (read-only grep/read, file:line evidence, do NOT edit index.html — pre-existing change). `.py` parts (app.py/launcher/registry/relay for F065-F076) = BACKEND, CAN patch + pytest-verify (run named test). real gap → report finding; index.html gap→split to ## Blocked (don't fix); .py gap→may fix+test. Only commit every_feature.md + status.md (+ .py if backend fix). Hosts: `prefs._hosts` browser-local, `getHosts` 2856, `pollHost` 12751 (status), `hostHttpUrl`/`hostWsUrl` 6229-43, host status chips taskbar, `/control` lease F021, 4401 auth.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; run named test. Pre-existing index.html change — leave alone.
