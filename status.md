# Loop status — handoff to next iteration

**Just handled:** F019 — Profiles-only launch → **done** (LAUNCH/RCE surface, rigorous; no injection gap). `app.py` `_parse_launch_body`/`_launch` (L540-618) + `launcher.py` `Launcher.launch` (L94-176). (a) NO injection: body parser reads ONLY `profile,cols,rows,title,cwd`; `command/cmd/argv/env/exe` never read (silently ignored); argv built server-side `python -m webterm.agent ... -- <profile["command"]>`, command solely from `self._profiles.get(name)["command"]`, env from `spawn_env()`+pinned broker URL+token, never client. (b) cwd: `os.path.realpath` normalize + `os.path.isdir` check; non-str/realpath-fail→400 `bad_cwd`, resolved-not-dir→400 `cwd_not_dir`, empty→None. (c) auth: token set+missing/wrong→401 `auth_required`; no-token+non-loopback→403 `launch_disabled_no_token`; no-token+loopback→ok. (d) codes: 200 registered/202 accepted-on-register-timeout · 400 unknown_profile/bad_json/bad_request/bad_dims/bad_cwd/cwd_not_dir · 401 · 403 · 429 `too_many_pending_launches` (concurrency cap MAX_PENDING=8, NOT time-based) · 500 spawn_failed/agent_exited_early. Profiles from config `agent.profiles`/`default_profile` else `default_profiles()` (cmd/powershell nt, bash/sh posix); /profiles names-only. No code change. Verification: py_compile PASS; `pytest -k "launch or profile or spawn or cwd or rate or 429"` → 25 passed/0 failed; RAN test_launch_cwd_validation (bad_cwd/cwd_not_dir), test_no_token_configured_remote_launch_403_loopback_ok (403), test_launch_real_agent (real spawn, cleaned up); injection-ignore inspected + corroborated by tests.

**Next to pick:** F020 — Serve desktop UI + login overlay (webterm/broker/ui.py, app.py, index.html): serve index.html ungated; login overlay probes /sessions, stores token in localStorage, `?token=` deep-link adopted then scrubbed via replaceState. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
