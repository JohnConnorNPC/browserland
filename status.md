# Loop status — handoff to next iteration

**Just handled:** F049 — Taskbar window buttons → **done** (FRONTEND inspection-verified, index.html not edited). One-btn-per-window: `buildTaskbarItem(s)` (idx:12675-95) one per merged session, idempotent dedupe `refreshTaskbarInner` (12958-79); notes excluded `if(!isNote)` around chip append (10349-67, synthetic session kept, chip skipped); app paths reuse `.taskbar-item[data-session-id]` dedupe (11163-66/11751-54). Left-click `onTaskbarClick(id)` (12697-732, wired 12693): minimized→`restoreWindow` (12728); minimize-toggle only `!switched && !revealed && frontId===id`→`minimizeWindow` (12730); else `bringToFront` (12731). Right-click `buildTaskbarItemMenu(key,x,y)` (14395-441, dispatched 14456-68): Focus (14409-22), Minimize/Restore (14423-26), Close (14428-30), Terminate non-app (14433-39)→restore/bringToFront/minimize/close/requestClose/terminate. Styling `updateTaskbarActive()` (7378-85) toggle `.active`(front non-min)/`.minimized`; CSS `.taskbar-item.active` accent (172-76), `.minimized opacity:.65` (177). No gap, no code change.

**Next to pick:** F050 — Launch (+) menu (index.html): left-click local default terminal; right-click profiles (per-host grouped) + app windows + Control panel + Help. FRONTEND inspect-only. (Backs F045 closed-notes, F051 open-in-folder, F079 default-profile; reads /profiles F078.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Launch (+) button/menu: `appMenuItems` 13428, `closedAppMenuItems` 13402, profiles from /profiles (F078); launchers `launchTextEditor`/`launchFileManager`/`launchTaskManager`/`launchStickyNote` ~13371-91; terminal launch POST /launch (F019 profiles-only).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
