# Loop status — handoff to next iteration

**Just handled:** F046 — CodeMirror text editor → **done** (FRONTEND inspection-verified, all 4 codex facets, index.html not edited). CodeMirror **6** (esm.sh, EditorView/EditorState/Compartment): lazy `loadCodeMirror()` (idx:8304), pkg map `CM_VER` (8281-302), langs{} (8319-36), ext→mode `detectLanguage()` (8348-70); `mountCodeMirror()` (10115)→`makeEditorState` (10174) w/ `syntaxHighlighting(defaultHighlightStyle)` (10183)+`langComp.of(langExtFor(doc))` (10192)+oneDark (10208), `new EditorView` (10232) into bodyWrap (10240); plain-`<textarea>` fallback on load fail (10308-12). Open at active cwd/host: `launchTextEditor()` (13375-81)→`activeTerminalStart()` (8089-95, focused `lastTermId`→cwd+hostId) passes `startDir:s.cwd,fileHostId:s.host`; `doOpen()` (9583-607) `fileApiPost('/file/read',{path},h)` (9591), dialog rooted `dirOf(win.filePath)||win.startDir` (9363-66), host `fileHost()` (9586). Dirty+save: `onInput` dirty (9170-73, badge dirtyEl 9338), `writeTo()` `fileApiPost('/file/write',{path,content},h)` (9468) clears dirty (9493/503), `doSave` `_saveToServer` (9576-82) Ctrl+S `Mod-s` keymap (10196-200), `_saveAllDirty` (9521-51). Save-prompt: `requestCloseAppWindow` (12292-326) `anyDirty` (12305-07)→text-editor `confirm('Save changes to … before closing?')` (12316)→`_saveAllDirty||_saveToServer` (12318), abort on fail (12322); keyboard/close-all route dirty editors here (13941/14302). No gap, no code change.

**Next to pick:** F047 — File manager (index.html): dual-pane browser at active cwd/host; open files into the editor; backed by /file/* (F022). FRONTEND inspect-only. (Codex flagged: directory nav, pane state, open-in-editor, host/cwd selection, file ops, refresh.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. App windows: sticky 8470, editor 10396, filemgr 11227; `appStore`/`saveAppWindow`/`openAppWindow`; `/file/*` (F022): list `{root,cwd,parent,entries[{name,type,size}]}`, read `{content}`/`{content_b64}`, write atomic; `fileApiPost`/`fileHost`/`activeTerminalStart` (8089). Editor open via `launchTextEditor`/`doOpen`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
