# Loop status — handoff to next iteration

**Just handled:** F052 — Start button label → **done** (FRONTEND inspection-verified, index.html not edited). Setting `startLabel` (browser-global); control `#set-start-label` text maxlength 24 placeholder `+` in "Start button" `.set-browser-global` (idx:1746-52), normalize default 2752-56. Apply: `applyStartButton()` (6824-32) `const lbl=(getSettings().startLabel||'+')` (6828-29)→btn-launch textContent+title; blank coerced `.trim().slice(0,24)||'+'` (15663) + normalize 2755. Persist: `commitStartLabel` `getSettings().startLabel=v;savePrefs()` (15662-69). Restore: `applyThemeSettings`→`applyStartButton` (6845/6889), control rehydrate `setStartLabelEl.value=(ls.startLabel==='+'?'':ls.startLabel)` (15341), default normalized load 2752-56. Behavior unchanged: launch handlers (13445-47 click→launchProfile, 13448+ contextmenu) never read startLabel; applyStartButton only mutates textContent/title. No gap, no code change.

**Next to pick:** F053 — Workspaces + pager (index.html): virtual desktops; pager dots switch; prev/next + Go-to-1–5 shortcuts; add via + dot. FRONTEND inspect-only. (Layout model `_layout.workspaces[]` from F024.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Layout `_layout.workspaces[]` (each {focusedCol, columns[]}), `getLayout()`/`renderWorkspaces`/`requestRelayout`; settings `getSettings()`/`savePrefs`/`applyThemeSettings`/`applyDisplaySettings`. Keybindings `DEFAULT_KEYBINDINGS` 2564+. Pager dots in taskbar.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
