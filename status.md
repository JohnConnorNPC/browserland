# Loop status — handoff to next iteration

**Just handled:** F057 — Clock chip → **done** (FRONTEND inspection-verified, index.html not edited). Chip `<div id="clock-chip" title="date & time">` (idx:1669) inside `#taskbar` (1662) after fullscreen btn; CSS `#clock-chip` (803-817) "bottom-right", `margin-left:2px` right-aligned. Off by default: CSS `display:none` (804) unless `.on` (817); normalize `if(typeof s.clock!=='boolean') s.clock=false` (2743); no `clock:true` anywhere. Setting key `clock`: checkbox `#set-clock` "Show date & time (bottom-right)" (1733-35), cached 14529, change handler (15652-56) `getSettings().clock`+save+`applyClock`, restore 15339, boot `applyThemeSettings`→`applyClock(s.clock)` (6843); `applyClock(on)` (6794-808) add/remove `.on`. Live: `setInterval(_renderClock,1000)` in applyClock(true) (6801, guard `_clockTimer`, `clearInterval` 6804 when off); `_renderClock` (6785-93) `new Date()`→`toLocaleDateString()+'  '+toLocaleTimeString()`. No gap, no code change.

**Next to pick:** F058 — In-app help guide (index.html): "?" chip opens a live-search interface guide injecting one entry per shortcut + an MCP-status row. FRONTEND inspect-only. NOTE the MCP-status row reads F065 (MCP server info) — app is already-built so that code exists; verify the row reads MCP status, but the help guide proper (open, live search, one-entry-per-shortcut from DEFAULT_KEYBINDINGS) is the core. Help already seen at `focusOrOpenHelp` 15167, help text 14647-66.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.** (F082 itself is the help-text vs untab-gesture gap at 14647-48 — when verifying F058's help text, that known gap is already split out; don't re-flag, just confirm the help SYSTEM works.)

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Help: `?`/help chip in taskbar→`focusOrOpenHelp` 15167, help window app-kind 'help', live search filter input, entries built from `DEFAULT_KEYBINDINGS` (one per shortcut) + sections; MCP-status row from MCP info (F065). Remaining shell: F059 keybindings+rebind, F060 Control Panel, F061 appearance, F062 settings persistence (/state F077).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
