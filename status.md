# Loop status — handoff to next iteration

**Just handled:** F074 — send_input + newline→CR mapping → **done** (BACKEND verified: 140 passed / 14 input-targeted, no .py change). Broker verbatim+readwrite+cap: `_mcp_input` (app.py:1237-60) requires readwrite else `{"error":"read_only"}` 403 (1244-45); cap `MAX_MCP_INPUT_BYTES=256*1024` (=256KiB, line 85), oversize→`{"error":"too_large"}` 413 (1250-51, on `data.encode("utf-8","replace")` bytes); verbatim forward `await entry.send_to_producer(protocol.input_frame(data))` (1255) NO transform; `bad_data` 400 guard (1248-49). Lease bypass: NO lease/active_client_id check in input path, explicit comment 1252-54 ("Deliberately bypasses the single-active-browser lease... gated by MCP token + readwrite, not a browser"); contrast /state DOES gate lease (1429-32) → omission intentional. Tool newline→CR (tool-side): `send_input` (server.py:370-89)→`client.send_input(int_id, _newlines_to_enter(data))` (389); `_newlines_to_enter` (134-51) `return data.replace("\r\n","\r").replace("\n","\r")` (151); raw endpoint+low-level client stay verbatim (docstring 147-50); `test_tool_normalizes_but_client_is_verbatim` (test_mcptool.py:202). Tests: `pytest tests/test_mcptool.py -k "input or newlines or verbatim or read_only or normalizes"`→14 passed; full file→140 passed; covers send_input post (150), `test_newlines_to_enter` param incl "ls\r\n"→"ls\r"/mixed (188-99), read_only translation (419), multi-host "hi\r" (617-24). RECURRING NOTE: broker /mcp/input endpoint (verbatim/403/256KiB/lease-bypass) confirmed by CODE READING only — no broker-side test (tests cover tool→client). No gap.

**Next to pick:** F075 — send_keys control/escape + cursor keys (webterm/broker/app.py, webterm/mcptool/server.py). BACKEND pytest-verifiable. Verify: `send_keys` tool accepts NAMED keys (Enter/Tab/Escape/Backspace/Home/End/PageUp/Delete etc.) + control chords `C-<x>` (Ctrl) + meta `M-<x>` (Alt/ESC prefix), converts each to the right BYTES; ARROW keys emit SS3 (`ESC O A`) vs CSI (`ESC [ A`) depending on the terminal's CACHED DECCKM (application-cursor-keys mode) reported in list_terminals. Confirm key-name→bytes table, C-/M- chord encoding, arrows SS3-vs-CSI from cached DECCKM, routes through /mcp/input (readwrite). Run tests (grep `send_keys`, `DECCKM`, `cursor_app`, `SS3`, `C-`, `\\x1b`). Codex flags: control-char math (C-a=0x01), M- = ESC prefix, named-key table completeness, DECCKM source (list_terminals cache), unknown-key error, SS3 vs CSI correctness.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/*, protocol.py). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140 client — send_keys/DECCKM likely here) + integration/protocol. broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). mcptool/server.py: `send_keys` key-table/chords/arrows→bytes→client `/mcp/input`; list_terminals carries cursor-key mode (DECCKM); broker `_mcp_input` 1237.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
