# Loop status — handoff to next iteration

**Just handled:** F066 — Access modes → **done** (BACKEND verified: 140 client tests + ASGI enforcement probe, no .py change). Effective-mode resolver: `_mcp_effective_mode(entry)` (app.py:1055-58) `return entry.mcp_mode or app.ctx.mcp_cfg["default_mode"]` (override wins, else global default); mirrored `WindowEntry.summary` (registry.py:114-130, line 129 `self.mcp_mode or mcp_default`); override field `mcp_mode` (registry.py:93-99, None=inherit, in-memory only). off invisible+rejected: `/mcp/terminals` `if mode=="off": continue` (app.py:1096-98); read/input via `_mcp_entry` (1072-75)→404 `unknown_or_off` (no leak). read vs readwrite: read endpoints `_mcp_read` via `_mcp_entry` (1133, off-only reject→read+rw pass); input `_mcp_input` `if mode!="readwrite": 403 read_only` (1244-45), `_mcp_reset` same (1275-76); `send_keys` not a separate route — converts to bytes→POST /mcp/input (mcptool/server.py:393), same rw gate. Tests: `python -m pytest tests/test_mcptool.py -q`→**140 passed** (client-side; broker enforcement NOT unit-tested); sub-agent drove create_app via Sanic ASGI client (throwaway, NOT committed) proving off→404+invisible, read→read-ok/input-403, readwrite→both, override raise/lower vs default, live default-flip. NOTE (non-blocking, informational): broker-side F066 enforcement has NO committed test — correct but a regression wouldn't be caught. No gap.

**Next to pick:** F067 — Per-window MCP override + robot button (index.html, app.py). MIXED: BACKEND `POST /session/mcp` (app.py — pytest/ASGI verify: sets in-memory per-window `mcp_mode` off/read/read-write, validates mode value, auth) + FRONTEND robot button (index.html INSPECT-ONLY — title-bar/right-click 🤖 sets mode via POST /session/mcp; icon FLASHES on read/type activity). Verify both: backend route mutates registry entry.mcp_mode (in-memory, not persisted) + validation/errors; frontend robot UI calls it + flash animation on MCP read/input. Don't edit index.html; app.py CAN be checked/fixed. Codex flags: mode validation (reject bad), which token gates /session/mcp (web auth vs MCP?), in-memory not /state, flash debounce, override clears on session end.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/snapshot/*). Sub-agent CAN read+patch .py AND run pytest. MCP tests=`tests/test_mcptool.py` (140 client-side) + `tests/test_broker_e2e.py` (no mode cases); broker-gate behavior via Sanic ASGI test client (throwaway, do NOT commit scratch scripts). Verify by EXECUTING tests +/or close-read app.py. real .py gap → may FIX+re-test (I commit .py too). Purely-index.html parts (robot UI) = inspect-only, don't edit index.html. Only commit every_feature.md + status.md (+ real .py fix). app.py MCP: `/mcp/info|terminals|profiles|read|input|launch|config`, `/session/mcp` (per-window override), `_mcp_auth_error` 1046, `_mcp_effective_mode` 1055, `_mcp_entry` 1072.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
