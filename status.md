# Loop status — handoff to next iteration

**Just handled:** F015 — Broker browser relay + snapshot heal → **done**. `relay.py:handle_browser_ws` + `app.py:_browser_ws` on `WS /ws?session=<id>`. Attach order (relay.py ~L49-57): `entry.add_subscriber` → send `protocol.resized_frame(cols,rows)` BROWSER-ward (xterm sizes first) → `entry.request_snapshot()` sends `snapshot_please` PRODUCER-ward (correct design; order resized→snapshot_please preserved). Browser→producer (L60-99): bare bytes + `input`/`paste`→input_frame, `resize`→resize_frame, mouse ignored (input gated on active-client lease; output never gated). Fan-out: `broadcast_binary`/`broadcast_text` iterate all subscribers; `remove_subscriber` in `finally` drops only that ws (entry + other subs + producer untouched). Lookup (L26-40): `?session`→int→`registry.get()`; missing/non-int→`error missing_session`, unknown→`error unknown_session`, no crash. No code change. Verification: py_compile PASS; `pytest -k "relay or ws or subscriber or attach or snapshot or broadcast or session"` → 46 passed/3 skipped/0 failed (incl live-broker test_fake_producer_relay pinning order); behavioral a(resized→snapshot_please order)/b(fan-out 2 subs)/c(input json+binary reaches producer)/d(detach 1of2 keeps entry+other+producer)/e(bad session graceful) all PASS.

**Next to pick:** F016 — Session list endpoint (webterm/broker/app.py, registry.py): `GET /sessions` enumerates live sessions (id, title, cwd, agent, dims). First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
