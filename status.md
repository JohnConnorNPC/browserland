# Loop status — handoff to next iteration

**Just handled:** F078 — Browser-facing `GET /profiles` → **done**. `_profiles` (app.py:524-538) returns `{default, profiles:[names], os}` — NAMES ONLY (`launcher.profiles.keys()` + `default_profile` name + `"windows"/"posix"`). Leak test: profile w/ `command:["SECRET_CMD"]`/`cwd:/secret/dir`/`env:{SECRET_ENV_VAR}`/`title:"Visible Name"` → body contained NONE of those (no command/argv/cwd/env/exe/title leaves broker). Auth = `/sessions` pattern (`if token and not auth.request_token_ok` → 401 `{ok:false,error:auth_required}`; open when no token, NOT loopback-gated). Distinct from `/mcp/profiles` (`_mcp_profiles` L1287, `_mcp_auth_error` MCP-token gate, body `{error:auth_required}`). OPTIONS preflight L1494. No code change. Verification: py_compile PASS; `pytest -k "profile or profiles or launch or sessions"` → 13 passed/0 failed (incl test_profiles_endpoint); live: GET→200 names-only no-leak, no-token→401, /mcp/profiles separate.

**CORE/INFRASTRUCTURE TIER COMPLETE** (F001-F022 + F077 + F078 = 24 features, all done).

**Next to pick:** F023 — Floating windows (index.html): free move via title-bar drag + resize via edges/corners; overlapping z-order. First unchecked, no unmet deps. **This is a FRONTEND (index.html) feature — verify by INSPECTION ONLY (read-only grep/read of index.html); do NOT edit index.html (pre-existing uncommitted change). Sub-agent: confirm the documented behavior exists in the JS; if a real gap, report as finding (don't fix). No pytest will cover pure-frontend behavior — rely on code inspection + any existing UI test.**

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018/F022) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real .py code fix a feature requires). Frontend (index.html) features = verify by inspection only.
