# Loop status — handoff to next iteration

**Just handled:** F033 — Stacks (rows) → **done** (FRONTEND inspection-verified, index.html not edited). (1) drop-to-stack: `computeDropTarget` (idx:4185) T/B quadrants (4218-23)→splitAtWindow top/bottom→`dragDropSplitRow(dragKey,targetKey,bottom)` (3942-61): `tgtCol.rows.splice(insertRow,0,newRow([key],1))` (3955)+`normalizeRowHeights` (3956)+`savePrefs()`+`requestRelayout()` (3958-59); empty col-body→`kind:'consume'` new bottom row (4230-32). (2) row-height gutter `.strip-gutter` `buildGutter(col.id,repLiveKey(above),repLiveKey(below))` loop (4714-17, 4928); `onGutterDown` (4946-93) delta→fraction, write `aboveRow.height=na;belowRow.height=nb` (4976), live flex (4980-81), onUp `savePrefs()`+`requestRelayout()` (4986-89). `normalizeRowHeights(rows)` sum-to-1 (3167-78, called 3235/3260/3294/3330/3593). **3 distinct gutters confirmed:** row-height `.strip-gutter`/`buildGutter`/`onGutterDown` (F033, writes row.height), split-width `.strip-hgutter`/`buildHGutter`/`onHGutterDown` (F032, cell widths), column-width `.strip-col-gutter`/`buildColGutter`/`onColGutterDown` (F027, col widths). No gap, no code change.

**Next to pick:** F034 — Drop-zone overlay cheat sheet (index.html): live highlighted preview of new-column / tab / split / split-rows / float landing zones while dragging. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. MODEL: `col.rows[]`(row.height) each `row.keys[]`, `row.mode` single/tabbed/split; split→`row.cells[]`+`row.widths`; tabs `cell.keys[]`+activeTab. Drop: `computeDropTarget`/`windowDropAt` L/R/T/B→kind split/consume; `commitDrop`/`splitAtWindow`. Gutters: col `.strip-col-gutter`, split `.strip-hgutter`, row `.strip-gutter`.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
