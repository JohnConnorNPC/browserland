# Loop status — handoff to next iteration

**Just handled:** F022 — Host file API → **done** (containment model clarified; F022 line corrected from "Sandboxed"). `/file/*` is intentional **FULL-HOST-AUTHED** (#35), NOT a rooted sandbox: `_resolve_host_path` (app.py:184) resolves client paths host-wide, NO `editor_root` confinement (`editor_root` app.py:332 = empty-path default only). Traversal moot — only gate is auth. Gate `_gated_auth_error` (app.py:625, == /launch): token set → missing/invalid → 401 `auth_required` (loopback or not); no token → non-loopback → 403 `disabled_no_token`, loopback ok (loopback by listener sockname → LAN/Tailscale = non-loopback). Hardening: NTFS ADS `file:ads` rejected, half-absolute Win `C:foo`/`\foo` rejected, resolve() failure→`bad_path` (never 500). Schemas: list→`{ok,root(anchor),cwd,parent,entries:[{name,type:dir|file,size}]}` dirs-first; read→`{content}` UTF-8 or `{content_b64}` (non-utf8→`not_utf8` 400); write→UTF-8 atomic mkstemp+os.replace. Cap `MAX_FILE_BYTES=5MiB` read+write→`too_large` 400. Errors: `not_found` 404, `not_a_file`/`not_a_directory`/`permission_denied`/`bad_path`/`bad_content`/`parent_missing` 400, upload `exists` 409. No code change. Verification: py_compile PASS; `pytest -k "file or sandbox or path or traversal or read or write or list or editor or filemanager"` → 42 passed/2 skipped/0 failed; live-broker harness 16/16 PASS (auth 401/403, write→read roundtrip, list schema, `..` host-wide by design, error codes, 5MiB too_large, ADS bad_path).

**Next to pick:** F077 — Synced UI-state API (webterm/broker/app.py): `GET/PUT /state` shared per-broker settings+layout blob with optimistic-concurrency `rev` (409 conflict inlines live state; lease-gated PUT returns not_active for non-active browser); durable self-healing `webterm_state.json` (atomic write, rev persisted across restarts); same token-or-loopback gate as /file/*. First unchecked, no unmet deps (F021 lease + F022 gate done).

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018/F022) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; do NOT edit index.html for a feature (inspect read-only); only commit every_feature.md + status.md (+ any real .py code fix a feature requires). Frontend (index.html) features = verify by inspection only.
