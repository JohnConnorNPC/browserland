# Loop status — handoff to next iteration

**Just handled:** F043 — Lock Size (global) → **done** (FRONTEND inspection-verified, index.html not edited). Global flag `prefs._lockSize` via `isSizeLocked()` (idx:6407)/`setSizeLocked()` (6427-34); control = desktop r-click menu "Lock Size"/"Unlock Size"→`setSizeLocked(!locked)` (14355-56); single global key (not per-window). Persist: savePrefs (6429)→localStorage STORAGE_KEY (1990)+`schedulePush()`/state (2000); startup re-apply `if(isSizeLocked()) body.classList.add('size-locked')` (16166). Snap-all-default: `applyLockedSizeToAll()` (6409-26) loops windows.values(), skip disposed/tiled, force w/h=`lockedSize()`=`defaultPixelSize()` (6408/6390), defaults DEFAULT_W=720/DEFAULT_H=480 (1954-55); called toggle-on (6431), new-window (7421-25), applyGeom guard (13561-64), local-term (15527/15538). Hide handles: CSS `body.size-locked .term-window .rh{display:none}` (1027), class toggled 6430/16166; JS `wireResize` onDown `if(isSizeLocked())return` (12593), peer-size refused (11963). Unlock: remove class→handles return, `wireResize` no early-return; `applyLockedSizeToAll` only `if(on)` (6431) so no re-snap; leaves windows current size (spec-permitted re-enable variant, comment 6432-33). No gap, no code change.

**WINDOW MANAGEMENT SECTION COMPLETE** (F023-F043, 21 features done).

**Next to pick:** F044 — Terminal window (index.html): xterm.js render + keystroke input over /ws (snapshot-heal on attach is broker's F015). First Window Types item. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Terminal window build ~7440+, xterm.js Terminal/FitAddon, WS `/ws?session=` attach; app windows 8470(sticky)/10396(editor)/11227(filemgr). `/file/*` backs editor+filemgr (F022 host file API).

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
