# Loop status — handoff to next iteration

**Just handled:** F039 — Terminate / delete → **done** (FRONTEND inspection-verified, index.html not edited). Terminate: `terminateWindow(key)` (idx:14110-49) POSTs host `/session/kill` `{id:sess.id,pid:sess.pid}` (14119-22, real backend session/process-tree kill = F013 contract), optimistic cleanup (`closeWindow`+`sessions.delete`) only after ok (14142-43); wired title-bar menu 14308-14, taskbar/ctx 14433-39 (terminal/agent-only `!isApp`); confirm `'Terminate this session? The shell process tree will be killed.'` (14310-11/14435-36). Delete: `destroyAppWindow(id)` (12335-43)→`closeWindow`+`deleteAppWindow` (2530-34 `delete appStore[id];saveAppStore()` permanent discard); menu "Delete file/note" app-only excl ephemeral (filemgr/taskmgr/ctrlpanel/help 14319-23, item 14326-34); confirm `'Delete this <what> permanently? Its saved content is removed.'` (14330-31). Distinction: comment "Terminate=hard kill (close is soft: shell keeps running)" (14304), "× Close keeps them, Delete discards" (14315-18); separate Close entry (14300-03)→requestClose/closeWindow; soft sticky-note `rec.open=false` stash (12263-66) vs Delete `delete appStore`. No gap, no code change.

**Next to pick:** F040 — Pin / lock to screen (index.html): "Lock to screen (pin)" keeps a floating window from scrolling with the strip; unlock reverses. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Window actions: soft `closeWindow`/`closeFrontWindow`/`minimizeWindow`/`restoreWindow`/`requestCloseAppWindow`; hard `terminateWindow` (14110, /session/kill)/`destroyAppWindow` (12335)/`deleteAppWindow` (2530). Menu builder `buildWindowMenu` 14151+. Keybindings `DEFAULT_KEYBINDINGS` 2564+.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
