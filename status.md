# Loop status — handoff to next iteration

**Just handled:** F076 — Shipped MCP server + multi-host routing → **done** (BACKEND pytest-verified: 140 passed, no .py change; feature line corrected 7→8 tools). Tools (server.py @mcp.tool): **8** (not 7) — `mcp_info` (260), `list_terminals` (272), `list_profiles` (308), `read_screen` (320), `send_input` (369), `send_keys` (392), `reset_terminal` (434, #27 — POST /mcp/reset clears render buffer), `launch_terminal` (452); `test_tools_registered` (test_mcptool.py:1009-16) asserts exactly these 8. CORRECTION (code wins, F018/F022/F069 pattern): my line said "7 tools" but code+test pin 8 (the 7 + reset_terminal, a real #27 tool) — over-delivery, not a gap; line updated. Config flag>env (__main__.py): `_resolve_token` (44-55) `--token`>$BROWSERLAND_MCP_TOKEN>$WEB_TERMINAL_MCP_TOKEN>--token-file (flag first 47-48); `--broker-url` default `os.environ.get(URL_ENV,DEFAULT_URL='http://127.0.0.1:4445')` (114-17); `--hosts` env default (127-34), `_resolve_hosts` (95-105) flag>single-host. --hosts namespaced `<host>:<int>`: `_parse_hosts` (58-92) ordered (name,url,token), rejects `:` in name (84-87); namespace out `list_terminals` `t["id"]=f"{name}:{t['id']}"` (server.py:300), `launch_terminal` (466); route back `_route(id)` (78-96) partition `<host>:<int>`, validate int ASCII. BrowserlandError: `BrowserlandError(RuntimeError)` (client.py:47-57, status+code), raised `_raise_for_status` (90-105) translates broker `{"error":code}`, `_request` (115-19) httpx.RequestError→`BrowserlandError(0,"connection_error")`; server `_route`/`_named_client`/`_launch_target` raise for bad/unknown ids. Tests: `pytest tests/test_mcptool.py -q`→**140 passed** (covers --hosts/namespace routing 612/645/692, flag>env 469/517/581, BrowserlandError, mcp_info, 8-tool assertion). No gap.

**MCP / AI AGENTS SECTION COMPLETE (F065-F076). 2 actionable items remain: F080, F081.**

**Next to pick:** F080 — Build-version / stale-deploy reporting (webterm/broker/app.py, webterm/__init__.py). BACKEND pytest/ASGI-verifiable. Verify: `/mcp/info` returns broker `version` (from `build_version()`); each `/mcp/terminals` entry carries `version`, `app_cursor` (cached DECCKM, the F075 source), and `machine_host`; agent entries add a `stale` flag = true when the agent's build_version DIFFERS from the broker's. Confirm build_version() source (webterm/__init__.py, e.g. git describe / VERSION+commit), /mcp/info version field (already seen F065 `_mcp_info` returns version), per-terminal version/app_cursor/machine_host in `_mcp_terminals` summary, stale = agent_version != broker_version. Run tests (grep `build_version`, `stale`, `machine_host`, `app_cursor`, `version`). Codex flags: version source/format, stale comparison correctness, app_cursor cache origin (registry from agent), machine_host meaning (which host the terminal runs on), missing-version handling.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.** After F080→F081, then ONLY F082/F083 (Blocked) remain → trigger FINISH (run full build+test suite once, write final status summary).

**VERIFICATION MODE — MCP (F065-F081 backend):** BACKEND `.py` (app.py, mcptool/server.py+client.py+__main__.py, webterm/__init__.py, registry.py, agent/*). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140) + `tests/test_integration.py`/`test_protocol.py`/`test_broker_e2e.py` + grep per feature. broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). F080: app.py `_mcp_info`/`_mcp_terminals` version+stale, `webterm/__init__.py build_version()`, registry version/app_cursor/machine_host. F081: __main__.py `--token-file` 4th tier, server.py `malformed_id`/`unknown_host` + per-host error aggregation (`errors` list).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history show intent (F018/F022/F069/F076 7→8 tools); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation/count CONTRADICTING code-intent+tests is wrong not a gap (F069/F076); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
