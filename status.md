# Loop status — handoff to next iteration

**Just handled:** F071 — read_screen scrollback view → **done** (BACKEND pytest-verified: 242 passed / 8 scrollback-targeted, no .py change). Scrollback prepends history (ordering): `render_screen` (textgrid.py:381-407), assembly 401-05 `hist=list(history)[-eff:]` then `text="\n".join(hist)+("\n"+screen if screen else "")` — history TOP, grid below; bounded `eff=min(int(lines),_MAX_HISTORY_LINES,budget)` (403), `budget=_MAX_HISTORY_CELLS//cols_c`; replays full ring `trim=not scrollback` (396) so pre-clear history survives; tests `test_render_screen_scrollback_captures_primary_history` (283), `..._survives_clear` (309). history_lines reported+accurate: `len(hist)` (406, else 0 at 407)→`_render_screen_text` (agent.py:106)→`screen_text_frame` (526/protocol.py:275)→broker `app.py:1219` `"history_lines":int(payload.get("history_lines",0) or 0)`; cap tests `test_render_screen_scrollback_bounded` (≤1000, 325), `..._history_cell_bounded_for_wide_grid` (332), wire `test_read_screen_scrollback_params_and_fields` (test_mcptool.py:131, history_lines==7). Alt-screen forces screen: `_render_screen_text` (agent.py:79-80) `want_scrollback = view=="scrollback" and not alt_screen`, `eff_view="scrollback" if want_scrollback else "screen"`; alt→pyte/screen path `view="screen",history_lines=0` (93-95); tests `test_render_screen_text_alt_forces_screen_view` (textgrid.py:249), `..._excludes_alt_but_keeps_primary_history` (294, textgrid.py:189 `if top==0 and not in_alt`). Tests: `pytest tests/test_textgrid.py tests/test_mcptool.py tests/test_integration.py -q`→**242 passed**; `-k "scrollback or history or alt_forces"`→8 passed. No gap.

**Next to pick:** F072 — read_screen wait modes (webterm/broker/app.py, webterm/mcptool/server.py). BACKEND pytest-verifiable. Verify: EXCLUSIVE wait modes — `wait_for_change` (poll until content_hash differs from a baseline) / `wait_for_text` (substring appears) / `wait_for_regex` (pattern matches), plus `wait_absent` variant (wait until text/regex GONE); bounded by `timeout_ms` capped ≤15000; returns `matched` (bool/what matched) on success or times out. Confirm the three wait modes are MUTUALLY EXCLUSIVE (reject >1), timeout cap enforced, matched reported, poll loop re-snapshots. Run tests (grep `wait_for`, `wait_absent`, `timeout_ms`, `matched`). Codex flags: exclusivity validation, timeout cap ≤15000, regex compile errors, wait_absent semantics, poll interval, matched payload, hash baseline for wait_for_change.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/*, protocol.py). Sub-agent CAN read+patch .py AND run pytest. read_screen suite (F070-F073) HAS real committed tests: `tests/test_{pyte_snap,textgrid,snapshot_raw,protocol,altscreen,integration,mcptool}.py` — RUN relevant subset. F072 wait modes may be in mcptool/server.py (client-side waits) + app.py (broker). broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). app.py: `/mcp/read` 1129 wait params; mcptool/server.py wait_for_* tools.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
