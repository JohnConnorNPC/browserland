# Loop status — handoff to next iteration

**Just handled:** F060 — Control Panel → **done (functional core) + SPLIT a doc gap to F083 (Blocked)** (FRONTEND inspection-verified, index.html not edited). DONE part: full-screen backdrop `#settings-overlay` (position:fixed inset:0, z150000, `.open`→flex, idx:1434-48); panel `#settings-modal` header "Control Panel" (1472-89/1696-98); `openSettings()` (14596-608)/`closeSettings()` (14609-17), backdrop-click (14618-20)+close btn (14621-22)+Escape/`open-control-panel` Ctrl+Alt+p (13980-83). Tab per broker: `renderSettingsTabs()` (15221-38) iterate `getHosts()` one tab/host ('this broker'+remotes) + trailing "Browser" tab (strip `#set-tabs` 1702, CSS 1557-87); click→`selectSettingsTab(id)` (15258-298) sets `currentSettingsTab`+rebinds `settingsTarget`=`makeLocalTarget()` (15271) / `makeRemoteTarget(tabId,entry)` after `/state` fetch (15296). Sections present: theme 1715, bg pattern 1719, font 1724, clock 1732, help 1737, start-label 1745, start-path 1754, win-size 1764, labels 1778, win-mode 1787, drag-delay 1794, ws-scrollbar 1806, restore 1813, taskbar-ws-filter 1821, default-profile 1828, MCP 1838, keybindings 1860, Hosts (Browser pane 1869-88). Routing: `settingsTarget {isLocal,s,save}` local `makeLocalTarget` getSettings+savePrefs (14581-88) / remote `makeRemoteTarget` entry.settings+`putHostState`→/state (14589-94, putHostState 2313-25); `.set-browser-global` tagged (1715/19/24/32/37/45/1813/1821) hidden on remote tabs `applyBrowserGlobalVisibility` (15250-53). SPLIT→F083 (Blocked): wiki Getting-Started.md:42 promises "full-screen panel" but `#settings-modal` is centered ≤480×80vh; code comment 13394 "floating Control Panel window was retired" = deliberate redesign; needs human decision (enlarge panel vs fix wiki). NOT like F047/F059 (those extras weren't documented; this IS in the wiki = real doc-vs-code gap like F082). No code change.

**Next to pick:** F061 — Appearance settings (index.html): theme/color scheme, background pattern, terminal font, clock + help toggles — browser-global, live-applied, persisted. FRONTEND inspect-only. (Sections already located 1715-41; `applyThemeSettings`/`applyDisplaySettings` 6843; verify each control applies live + persists.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Appearance: `.set-browser-global` theme `#set-theme` 1715, bg pattern 1719, terminal font 1724, clock `#set-clock` 1732, help toggle 1737; `applyThemeSettings()` 6843 (theme/pattern/font/clock/help/start-label), `applyDisplaySettings` 6889; CSS vars `--accent-*`, change handlers 15634-69. Remaining shell: F062 settings persistence (browser-global vs per-host, restore-on-refresh, /state F077 sync).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins when code+tests+issue-history show intent (F018/F022); but a USER-FACING WIKI/HELP promise the code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059). Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
