# Loop status — handoff to next iteration

**Just handled:** F055 — Workspace manage → **done** (FRONTEND inspection-verified, index.html not edited). Pager-dot manage menu: `renderWsDots` attaches `contextmenu` on each `.ws-dot`→`buildWorkspaceMenu(i,...)` (handler idx:5690-94, builder 5708-29 via renderMenu 5728; dot tooltip "right-click: rename / remove" 5688). Rename: "Rename…" (5716-17)→`renameWorkspace(i)` (5735-52): prompt→`ws.name=t.slice(0,40)` (5744), label mode 'name' (5745) or `delete ws.name` if blank (5747)→`savePrefs()` (5749)+re-render; loader caps name 40ch (3455-58). Remove: "Remove workspace" `enabled: L.workspaces.length>1` (5718-19, greyed at one); `removeWorkspace(i)` (5753-89) re-guard `length<=1` (5755-57)→tiled columns pushed to neighbor (5765-68), floats reassigned `reassignFloatingWs` (5771), victim tiled parked (5774-76)→`splice(i,1)` (5777), activeWs fixed (5778-82)+`savePrefs()` (5783). Show names/numbers: "Show names"/"Show numbers" (5721-24)→`setWsLabelMode('name'|'number')` (5730-34) sets `getSettings().wsLabelMode`+savePrefs+render, drives `wsLabel` (5660-65)/render sig (5669-73). New workspace: (5726)→`addWorkspace` (6118-24, same as + dot 5703). Help 14665-66. No gap, no code change.

**Next to pick:** F056 — Fullscreen toggle (index.html): taskbar ⛶ button + Ctrl+Alt+f → browser Fullscreen API. FRONTEND inspect-only.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. Fullscreen: taskbar ⛶ btn, `requestFullscreen`/`exitFullscreen`/`document.fullscreenElement`, `fullscreenchange` icon sync; keybinding `fullscreen`/`toggle-fullscreen` in DEFAULT_KEYBINDINGS 2564+ (Ctrl+Alt+f). Remaining shell: F057 clock, F058 help, F059 keybindings+rebind, F060 Control Panel, F061 appearance, F062 settings persistence (/state F077).

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap (in-app help promises unbuilt behavior) = real finding→Blocked (F082). F058 help-MCP-status dep F065 — verify rest. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
