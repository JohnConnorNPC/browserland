# Loop status — handoff to next iteration

**Just handled:** F075 — send_keys control/escape + cursor keys → **done** (BACKEND pytest-verified: 58 passed, no .py change). Named keys+chords (mcptool/server.py): `_NAMED_KEYS` (157-71) Enter→`\r`/Tab→`\t`/Esc→`\x1b`/Backspace→`\x7f`/Delete→`\x1b[3~`/F1→`\x1bOP`/F5→`\x1b[15~`; `_ctrl_byte` (190-98) `chr(ord(ch.upper())&0x1F)`→C-c→`\x03`/C-a→`\x01`/C-z→`\x1a`/C-@→`\x00`/C-[→`\x1b`/C-_→`\x1f`, `?`→`\x7f` special; Meta (242-44) `M-<x>`→`"\x1b"+rest`, C-Space→`\x00` (237-38); dispatch `_token_to_text` (222-48). Arrows SS3 vs CSI: `_CURSOR_KEYS` final-byte map (177-78, up=A/down=B/right=C/left=D/home=H/end=F); `_token_to_text:230` `("\x1bO" if app_cursor else "\x1b[")+final` — SS3 when DECCKM set else CSI; mode `_terminal_app_cursor` (209-19) reads `t.get("app_cursor")` from `client.list_terminals()` cache (no render, default False on error); wired send_keys 429-30 (re-encode only when `_keys_have_cursor` AND app_cursor). Routes /mcp/input readwrite: send_keys:431→`client.send_input(int_id,text)`→/mcp/input (client.py:190), broker readwrite gate 403 read_only (app.py:1244-45) before forward (1255). Tests: `pytest tests/test_mcptool.py -k "keys_to_text or send_keys or token_to_text or keys_have_cursor"`→**58 passed**; `test_keys_to_text` full table (231-58), DECCKM `test_token_to_text_cursor_mode` (312, up,True→\x1bOA), `test_send_keys_arrows_csi_in_normal_mode` (350)/`..._ss3_under_decckm` (356)/`..._fall_back_to_csi_on_lookup_error` (367), multi-host DECCKM routing (658), verbatim post (270). No gap.

**Next to pick:** F076 — Shipped MCP server + multi-host routing (webterm/mcptool/client.py, server.py, __main__.py). BACKEND pytest-verifiable. Verify: `webterm.mcptool` is a stdio FastMCP server exposing **7 tools** (mcp_info, list_profiles, list_terminals, read_screen, send_input, send_keys, launch_terminal — confirm the count+names); config precedence flag>env (CLI flag over env var); `--hosts` adds extra brokers with NAMESPACED ids `<host>:<int>` (so terminal ids are host-qualified across multiple brokers); `BrowserlandError` surfaced cleanly to the MCP client (error translation). Confirm 7 tools registered, multi-host id namespacing `<host>:<n>`, config flag>env precedence, BrowserlandError→MCP error mapping. Run tests (grep `--hosts`, `namespace`, `FastMCP`, `BrowserlandError`, tool count). Codex flags: tool count/names exact, host id parse `<host>:<int>`, flag>env>default precedence, error surfacing, default host unnamespaced vs namespaced.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.** AFTER F076: remaining items F077 (/state store — likely already a dep, verify broker app.py) + F080/F081 (check every_feature.md tail for exact ids/scope) — read the file to confirm next pick after F076.

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py+__main__.py, agent/*, protocol.py). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140 client, multi-host routing tests here e.g. 617/658) + integration/protocol. broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). mcptool: `server.py` 7 @mcp.tool defs, `__main__.py` argparse `--hosts`/flag>env, `client.py` namespaced `<host>:<int>` ids + `BrowserlandError`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
