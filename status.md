# Loop status — handoff to next iteration

**Just handled:** F037 — Drag hold delay setting → **done** (FRONTEND inspection-verified, matches spec exactly, index.html not edited). `snapHoldMs` default 3000 (`HOLD_MS` idx:4102; `normalizeSettings` default 3000 2706-12; field defaults 15324/15575). Control `#set-snap-hold` (`type=number min=0 max=20000 step=50 placeholder=3000` 1797-99), `getElementById` 14518, change handler 15569-85: `v<=0→v=0` (explicit disable, NOT clamped up), else `Math.max(250,Math.min(20000,v))`, blank/NaN→3000, `t.s.snapHoldMs=v`+`t.save()` persist (15582/84). `snapHoldMsFor(win)` (4114-27): local `getSettings().snapHoldMs` (4119-20), remote `hostStateCache.get(hid).settings.snapHoldMs` (4122-23), test override `window.__HOLD_MS__` (4115-16), returns 0 for disable (4125); guards `holdMs<=0` no-arm (pop-out 4415, snap 12429). Per-host, syncs via /state (2701/15564). Actual range/default = 250-20000 / 3000 / 0=disabled — no mismatch. No gap, no code change.

**Next to pick:** F038 — Minimize / restore / close (index.html): soft close with type-specific semantics; minimize-to-taskbar and restore (Ctrl+Alt+m / Ctrl+Alt+w). FRONTEND inspect-only. (NOTE: F038/F039/F049 overlap on window-action semantics per setup-refinement — F038 = minimize/restore/soft-close, F039 = terminate/delete hard, F049 = taskbar entry points.)

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out gap) — skip, awaiting user decision.**

**FRONTEND VERIFICATION MODE (F023-F062 = index.html):** verify by INSPECTION ONLY — read-only grep/read of `webterm/broker/index.html`; confirm documented behavior has real implementing JS with file:line evidence; do NOT edit index.html (pre-existing change) or run pytest for pure-frontend; real gap → report finding, split working part [x] + gap to ## Blocked (don't fix). Only commit every_feature.md + status.md. MODEL: `col.rows[]`(row.height) each `row.keys[]`, `row.mode` single/tabbed/split; split→`row.cells[]`+`row.widths`; tabs `cell.keys[]`+activeTab. Keybindings `DEFAULT_KEYBINDINGS` 2564+, dispatcher 14044+, `KEY_ACTIONS`/`KEY_ACTION_BY_ID`.

**Reminder:** already-built — VERIFY, don't rebuild. Docs/spec vs code disagree → code usually wins (F018/F022); code-vs-DOC+HELP gap = real finding→Blocked (F082). F045 closed-notes dep F050's + menu; F058 help-MCP-status dep F065 — skip-and-return if before deps. Backend (.py) CAN be patched + pytest-verified. Pre-existing index.html change — leave alone.
