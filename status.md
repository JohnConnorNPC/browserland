# Loop status — handoff to next iteration

**Just handled:** F067 — Per-window MCP override + robot button → **done** (MIXED: BACKEND ASGI-proven + FRONTEND inspect-only, no .py change, index.html not edited). Backend: `_session_mcp` (app.py:1022-38, route 1477) auth `_gated_auth_error(request,"/session/mcp")` (1027)→browser `auth_token` (625-638, NOT MCP token); validate `if mode not in MCP_MODES:400 bad_mode` (1035-36), `MCP_MODES=("off","read","readwrite")` (80); set in-memory `entry.mcp_mode=mode` (1037, registry.py:99, surfaced `mcp` in summary 129) — NOT /state. Robot UI (index.html inspect): 🤖 SVG+popover `attachMcpButton` (7163-272), title bar insert 7600-01, option click `setWindowMcpMode(win,val)` (7239-44) POST `/session/mcp {id,mode}` (14067-96, fetch 14080-84), re-assert tick 13012; frontend always sends canonical `readwrite`. Flash read-vs-write: backend `_flash_mcp_activity(entry,"read")` (1136)/`"write"` (1259) via `protocol.mcp_activity_frame` (protocol.py:312); frontend `mcp_activity` handler (12016-21)→`flashMcpBtn(win,write|read)` (7278-91), distinct `@keyframes mcpReadPulse` blue 280ms (322-29/335) vs `mcpWriteFlash` orange 200ms (330-33/336). Tests: no dedicated /session/mcp test; throwaway broker proof ALL PASS (no-token→401, set read→200+/sessions mcp==read, readwrite→200, garbage→400 bad_mode unchanged, 'read-write'→400, /state no per-window mode). NOTE (non-blocking): backend rejects hyphenated 'read-write' (only 'readwrite') but frontend always sends canonical → no real mismatch (display label only). No gap.

**Next to pick:** F068 — allow_launch + /mcp/launch (webterm/broker/app.py, launcher.py). BACKEND pytest/ASGI verify: SEPARATE `allow_launch` gate (distinct from enabled/default_mode — launch can be off even when MCP read enabled); `POST /mcp/launch` reuses the SAME server-validated launch shape as F019 (profile/cols/rows/title + validated cwd realpath+isdir), returns 200/202 (200 immediate vs 202 accepted?), error cases (allow_launch off→403, bad profile/cwd→4xx, MCP-token gated). Confirm allow_launch independent of default_mode, /mcp/launch shares `_parse_launch_body`/launcher path with F019. Run MCP tests. Codex flags: allow_launch default false, launch gate separate from read/write modes, cwd validation reuse, profile validation, 200-vs-202 semantics, token (MCP not web).

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, launcher.py, mcptool/server.py+client.py, agent/snapshot/*). Sub-agent CAN read+patch .py AND run pytest. MCP tests=`tests/test_mcptool.py` (140 client-side) + `tests/test_broker_e2e.py` (procs/kill/git, no mcp-mode/launch cases); broker behavior via Sanic ASGI test client OR broker subprocess (throwaway, do NOT commit scratch scripts). Verify by EXECUTING tests +/or close-read app.py. real .py gap → may FIX+re-test (I commit .py too). Purely-index.html parts = inspect-only, don't edit index.html. Only commit every_feature.md + status.md (+ real .py fix). app.py MCP: `/mcp/info|terminals|profiles|read|input|launch|config`, `/session/mcp`; `_mcp_auth_error` 1046, `_mcp_effective_mode` 1055, `_mcp_entry` 1072, `_parse_launch_body` ~573 (F019 shape), `allow_launch`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
