# Loop status — handoff to next iteration

**Just handled:** F017 — Auth model + token gating → **done** (security feature, rigorous). `auth.py`: `resolve_token(config)` = `os.environ.get("WEB_TERMINAL_TOKEN") or config["auth_token"] or None` (env>config; MCP separate `WEB_TERMINAL_MCP_TOKEN`/`mcp_token`). Only token compare is `auth.token_matches` → `hmac.compare_digest` on UTF-8 bytes (grep: no raw `==`). Per-surface (app.py): `/browserland` exempts loopback else token, close 4401 (refuses non-loopback when no token) L502-512; `/launch` token-when-set→401 else loopback-only 403 `launch_disabled_no_token` L594-607; `/ws` close **4401** on bad/missing token when configured L405-411 (same `/control` L419-423); `/sessions` 401 when token set+missing, open when none L514-518. Loopback `is_loopback_request` (auth.py:66-89) from `transport.get_extra_info("sockname")` (local listener addr) falling back peer `request.ip` — NEVER reads X-Forwarded-For/X-Real-IP → NOT spoofable (verified 8.8.8.8+spoofed headers→False). No-token ≠ fully open: `/launch`, `/file/*`, `/state`, `/session/*`, `/browserland`, `/mcp/*` stay loopback-only/disabled. No code change. Verification: py_compile PASS; `pytest -k "auth or token or loopback or 4401 or unauthorized or gate"` → 34 passed/0 failed; real-helper precedence(env wins/config/none)+compare_digest+loopback(127.0.0.1/::1 True, public/LAN False, sockname overrides peer, header can't spoof)+4401 all PASS.

**Next to pick:** F018 — Token-gated CORS (webterm/broker/app.py): emit CORS headers (incl. on errors + OPTIONS preflights) only when a token is configured; AUTO_EXTEND=False neutralizes sanic-ext. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
