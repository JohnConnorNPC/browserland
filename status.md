# Loop status — handoff to next iteration

**Just handled:** F065 — MCP HTTP core + token gating → **done** (BACKEND pytest-verified, no .py change). Endpoints: `_mcp_info` (app.py:1078-86, route GET /mcp/info 1481, returns `{ok,allow_launch,default_mode,version}` — `enabled` omitted BY DESIGN, implicit behind 403 gate, explicit on /mcp/config `_mcp_cfg_public` 1324-29); `_mcp_terminals` (1088-116, route 1482, terminals w/ effective mode≠off); `_mcp_profiles` (1287-94, route 1486, `{default,profiles}`). Master-enable gate: `_mcp_auth_error` (1046-53) `if not cfg.get("enabled") or not cfg.get("token"): return {"error":"mcp_disabled"},403` (1048-49), called top of EVERY /mcp/* handler (1079/1089/1130/1238/1269/1288/1297); default `mcp_enabled=False` (121/`_load_mcp_cfg`). MCP token: `resolve_mcp_token` (auth.py:33-39) env `WEB_TERMINAL_MCP_TOKEN`>config`mcp_token`, sidecar `webterm_mcp.json` overlay (app.py:104-139, env pins 116/129), DISTINCT from web `resolve_token`/`WEB_TERMINAL_TOKEN` (auth.py:24-30); `_mcp_auth_error` checks cfg["token"] never auth_token (1050); `provided_token` (auth.py:51-59) `?token=`/`?auth=` query OR Bearer; `token_matches` (auth.py:42-48) `hmac.compare_digest`. Error table: 401 `auth_required` (1052) missing/bad, 403 `mcp_disabled` (1049) disabled/no-token, clean `{"error":...}` JSON, no token leak. Tests: `python -m pytest tests/test_mcptool.py -q`→**140 passed** (client-side contract); sub-agent ALSO drove real create_app via Sanic test client (throwaway, NOT committed) proving gate live (disabled→403, no-token→401, Bearer/query→200, wrong→401, web-token rejected, tokenless-enabled→403). No gap.

**Next to pick:** F066 — Access modes (webterm/broker/app.py). BACKEND pytest-verifiable. Verify: EFFECTIVE per-window mode `off|read|readwrite` computed over global `default_mode`; `off`→window HIDDEN from /mcp/terminals (not listed/observable); `read`→observable (read_screen) but no input; `readwrite`→input allowed. Per-window override (F067 sets it) layered over default_mode. Confirm the effective-mode resolver fn + that /mcp/terminals filters off + read-vs-readwrite enforcement points (read endpoints need ≥read, input needs readwrite). Run MCP tests. Codex flags: default_mode fallback, override precedence, off truly invisible (no leak via other endpoints), readwrite⊃read.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/snapshot/*). Sub-agent CAN read+patch .py AND run pytest. MCP tests = `tests/test_mcptool.py` (140, client-side contract) + `tests/test_broker_e2e.py` (broker e2e — check for /mcp coverage); for broker-gate behavior the sub-agent can drive `create_app` via Sanic test client (throwaway script, do NOT commit scratch scripts). Verify by EXECUTING tests and/or close-reading app.py handlers. real .py gap → may FIX+re-test (I commit the .py too). Purely-index.html parts (F067 robot UI) = inspect-only, don't edit index.html. Only commit every_feature.md + status.md (+ any real .py fix). app.py MCP: `/mcp/info|terminals|profiles|read|input|launch|config`, `/session/mcp`; `_mcp_auth_error` 1046, effective-mode resolver, `default_mode`/`allow_launch`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
