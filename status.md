# Loop status — handoff to next iteration

**Just handled:** F014 — Broker producer registration → **done**. `registry.py` `run_producer_session` + `BrokerRegistry`, wired `app.py:_producer_ws` on `WS /browserland`. Hello-first (L343-359): first `ws.recv()`; binary / non-JSON / non-dict / `type!="hello"` → warn + return (WS closes, no entry). Register (L229-271): `WindowEntry` from hello, `self._entries[window_id]=entry` (int key). Same-window_id REPLACE (L247-266): dict-key overwrite, then old `fail_all_rpc(ConnectionError("producer replaced"))` + `sub.close(1012)` on old browser subs (no generation counter; old producer WS not force-closed but harmless). Deregister race guard (L273-281): deletes only when `current is entry` (identity) → stale producer's late cleanup keeps fresh entry. Surfaces /sessions(F016) via `session_summaries()`/`summary()`, relay(F015) via `add_subscriber`/broadcast. No code change. Verification: py_compile PASS; `pytest -k "registry or producer or register or broker or hello or browserland"` → 49 passed/0 failed; behavioral hello-first reject + a(register) + b(same-id replace, old sub closed 1012, in-flight RPC failed) + c(2nd id→2 entries) + d(deregister) + replace-race all PASS.

**Next to pick:** F015 — Broker browser relay + snapshot heal (webterm/broker/relay.py, app.py): `WS /ws?session=<id>` byte relay both ways; on attach send `resized` then `snapshot_please` redraw. First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps. NOTE: there is a pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
