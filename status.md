# Loop status — handoff to next iteration

**Just handled:** F069 — MCP admin + sidecar → **done** (BACKEND verified: ASGI proof, no .py change). POST mutate+generate+persist: `_mcp_config_post` (app.py:1337-88) set token/default_mode/allow_launch/enabled (1361-69), validate-before-lock (1347-51), `generate`→`secrets.token_urlsafe(32)` (1358-60), persist explicit schema via `_write_state_atomic` to sidecar (1375-84); proof round-tripped + generate returned fresh 43-char token. Token precedence+env-pin: `_load_mcp_cfg` (104-139) seed `resolve_mcp_token` (auth.py:33-39, env>config), sidecar overlays token only when env unset (116/129)→env>sidecar>config; `_mcp_token_env_pinned` (1318-22) blocks token edits incl generate (1352/1357), writes `token:null` (1376); proof WEB_TERMINAL_MCP_TOKEN pinned→token_env_pinned=true, stayed pinned after override+generate, sidecar null. Sidecar self-heal+auth: atomic `_write_state_atomic` (164-81, mkstemp+os.replace); `_load_mcp_cfg` try/except FileNotFound/OSError/JSONDecode/ValueError (123-27)→defaults (128-39); gated `_gated_auth_error` (625-38) on BROWSER `auth_token` (resolve_token) NOT MCP token; proof no-auth GET→401, browser-auth→200, corrupt+missing sidecar→200 defaults (no crash). GET `_mcp_config_get` (1331-35)→`_mcp_cfg_public` (1324-29). 

**CORRECTION (code wins over my brief's "no token leak"):** GET /mcp/config returns the ACTUAL token (`"token": cfg["token"] or ""` 1326), not a redacted boolean — but this is BY DESIGN, NOT a gap: (a) F069 feature LINE explicitly lists `token` as a GET/POST field; (b) documented rationale (1315-17, secret only travels to already-authenticated browser); (c) UI depends on it (index.html:15455 `setMcpToken.value=cfg.token` to copy into MCP client config); (d) browser-auth gated — reader already holds web auth_token = already controls broker, no escalation. Same pattern as F018/F022. My "no-leak" overlay was wrong. RECURRING NOTE: no committed broker-side /mcp/config test (proven via throwaway ASGI). No gap.

**Next to pick:** F070 — read_screen rendering (webterm/broker/app.py, webterm/agent/snapshot/*). BACKEND pytest-verifiable. Verify: `POST /mcp/read` returns a BOUNDED grid (pyte renderer or fallback) + `alt_screen`/`cursor`/`content_hash`/`view` fields; `degraded` flag RESERVED for raw decode path. Confirm read RPC to producer → snapshot, grid bounding (rows×cols cap), pyte vs fallback (textgrid) selection, content_hash stable, cursor+alt_screen reported, degraded only on raw. Snapshot code in agent/snapshot/{pyte_snap,textgrid,raw}.py. Run snapshot tests (grep `test_*snap*`, `pyte`, `read_screen`, `content_hash`). Codex flags: grid bounds enforcement, hash determinism, pyte-absent fallback, cursor clamp, alt-screen detection, degraded semantics.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, launcher.py, mcptool/server.py+client.py, agent/snapshot/{raw,pyte_snap,textgrid}.py). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140 client) + `tests/test_broker_e2e.py` + snapshot/agent tests (grep). broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). Verify by EXECUTING tests +/or close-read. real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). app.py: `/mcp/read` (read_screen), snapshot RPC `snapshot_please`→`snapshot` (protocol.py), agent/snapshot renderers.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); and an expectation in MY brief that CONTRADICTS the feature line/code-intent is wrong, not a gap (F069). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
