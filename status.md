# Loop status — handoff to next iteration

**Just handled:** F062 — Settings persistence → **done** (FRONTEND inspection-verified, index.html not edited). LAST F023-F062 desktop-shell item. (1) Browser-global vs per-host split: `loadPrefs` 1977/`savePrefsLocal` (localStorage-only) 1989/`savePrefs` (local+push) 1998; synced slice `_stateBlob()={settings:prefs._settings,layout:prefs._layout}` (2094-101); browser-local-ONLY (never pushed): `prefs._hosts`+pixel geom (2061-62), `_floatWs` (5905-09/savePrefsLocal 5925), recentColors (2009-14), appStore (2391-404), CLIENT_ID (1922-37); `.set-browser-global` handlers always bind LOCAL getSettings regardless of tab (1708-14, 15619/25/37-60), per-host (start-path 1753-63/profile/cols/rows/tiling) via `settingsTarget`→`putHostState` (makeRemoteTarget 14593). (2) /state sync: local GET `pullState` fetch '/state' (2212-13)→`_applyServerState` (2172-99); local PUT `pushState` (2123-32) debounced 600ms `schedulePush` (2104-13), savePrefs→schedulePush (1998-2000); remote GET `fetchHostState` (2279)/PUT `putHostState` (2313-28)→`_putHostStateOnce` (2346-55); comment "/state source of truth, localStorage cache" (2054-57). (3) Restore-on-refresh: `#set-restore-refresh` (1815, gbl 1813-19), default `restoreOnRefresh=true` (2695), handler 15618-21; open-terms on synced layout `getOpenTerms`/`addOpenTerm` (3613-41); boot `seedRestoreQueue` gated (16020), `bootActiveView` await `_stateReadyPromise`→`restoreAppWindows()` (16038-49). (4) Taskbar ws filter: `#set-taskbar-hide-other-ws` "Hide windows on other workspaces" (1823, gbl 1821-26), `setHideOtherWs` (15624-28), default normalize ~2713; `applyTaskbarWorkspace` (5989-6013) reads `hideTaskbarOtherWs` (5992) toggles `ws-hidden` non-active (6011), re-applied on /state adopt (2192), CSS 1282-83. No gap, no code change.

**Next to pick:** F079 — Per-host default profile + start path (index.html). NOTE file order puts F079 (line 78) BEFORE the Multi-host section (F063 line 82) — pick F079 next per dependency order. Verify: per-host "Default terminal profile" control (`#set-default-profile`-type ~1828, feeds the + button `hostDefaultProfile` F050 + Open-in-folder F051) and "Default start path" (~1754, roots new shells' cwd via `resolveStartPath`); BOTH per-host (settingsTarget, NOT browser-global), persisted per-host + synced via /state (#17/#10). FRONTEND inspect-only — but resolveStartPath/cwd validation may touch launcher.py (check if it's index.html-only or also backend; default-profile + start-path UI is index.html). Codex flagged: profile fallback when default missing, start-path validation/expansion (~, env), empty/blank handling, per-host isolation.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE:** F079 + multi-host UI (F063/F064 index.html parts) = FRONTEND inspect-only (read-only grep/read of `webterm/broker/index.html`, file:line evidence, do NOT edit index.html — pre-existing change). But F063/F064/F065+ also tag `.py` (app.py/launcher.py) — those BACKEND parts CAN be patched + pytest-verified (executed tests resume). real gap → report finding, split working [x] + gap to ## Blocked (don't fix index.html; .py fixable). Only commit every_feature.md + status.md (+ any .py change for backend features). Per-host settings: `settingsTarget` 14571, default-profile 1828, start-path 1754, `resolveStartPath`, `hostDefaultProfile` 13189.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code already exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059). Backend (.py) F063+ → pytest-verifiable; run the named test. Pre-existing index.html change — leave alone.
