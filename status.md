# Loop status — handoff to next iteration

**Just handled:** F073 — read_screen delta mode → **done** (BACKEND pytest-verified: 4 delta tests pass, no .py change). since→row diff: `Agent._reply` (agent.py:495-528), `base=self._frame_cache.get(since)` (506), per-row diff `[{"row":i,"text":rows_list[i]} ... if rows_list[i]!=base[i]]` (508-10); prior remembered `_remember_frame(content_hash,rows_list)` (521, def 595-607) bounded LRU OrderedDict keyed by handed-out content_hash (`_frame_cache` init 195, cap `_FRAME_CACHE_MAX=16` line 45); plumbing broker `since` (app.py:1190-92)→`screen_text_please_frame(...,since=since)` (1206/protocol.py:165/196)→agent param (451). changed_rows shape+delta=true: each `{"row":int,"text":str}` enforced `protocol.screen_text_frame` (protocol.py:293-96), `delta=true` (agent.py:518-19, serialized protocol.py:292), broker `out["delta"]`+`out["changed_rows"]` (app.py:1230-32), text emptied on delta `"" if delta else r["text"]` (523). delta=false fallback: default False (499); full grid when no/empty since (broker normalize 1191-92, guard 505), degraded/non-screen view (505), unknown/stale hash base None (507), size mismatch `len(base)!=len(rows_list)` (507), too-many-changed `len(diff)<=int(len*_DELTA_MAX_CHANGED_RATIO=0.6)` else fallback (516-17/46). Tests: `pytest tests/test_protocol.py::test_screen_text_frame_delta tests/test_integration.py::test_delta_returns_only_changed_rows ::test_delta_full_fallback_on_unknown_since ::test_delta_unchanged_screen_is_empty_diff -q`→**4 passed**; covers changed-rows+reconstruction (test_integration.py:412-38), unknown-since fallback (441-49), unchanged empty diff (452-62), frame shape (test_protocol.py:203-12). No gap.

**READ_SCREEN COMPLETE (F070-F073: render/scrollback/wait/delta). Entering INPUT-side MCP.**

**Next to pick:** F074 — send_input + newline→CR mapping (webterm/broker/app.py, webterm/mcptool/server.py). BACKEND pytest-verifiable. Verify: `POST /mcp/input` writes bytes VERBATIM to the pty (READWRITE mode only — 403 read_only otherwise, F066), size cap ≤256KiB (over→413/400), BYPASSES the browser control lease (MCP input not lease-gated); the MCP TOOL `send_input` (mcptool/server.py) maps `\n`/`\r\n`→`\r` (CR) so typed commands actually SUBMIT (esp. PowerShell/Windows). Confirm: broker verbatim write + readwrite gate + size cap + lease-bypass; tool-side newline→CR transform (server.py). Run tests (grep `send_input`, `/mcp/input`, `\\r`, `256`, newline). Codex flags: verbatim (no broker mangling — CR mapping is TOOL side only), readwrite enforcement, size limit, lease bypass rationale, \n vs \r\n both→CR, binary safety.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, mcptool/server.py+client.py, agent/*, protocol.py). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140 client) + `tests/test_integration.py`/`test_protocol.py` (read_screen e2e) + grep per feature. broker behavior via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). app.py: `/mcp/input` `_mcp_input` 1238 (readwrite 1244-45, size cap), lease bypass; mcptool/server.py `send_input` newline map, `send_keys` F075.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history+UI-dependency show intent (F018/F022/F069); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation CONTRADICTING feature-line/code-intent is wrong not a gap (F069); approximate names in MY brief ≠ gap if mechanism matches (F070). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
