# Loop status — handoff to next iteration

**Just handled:** F077 — Synced UI-state API → **done**. `GET /state` (`_state_get` ~app.py:1396, after `_gated_auth_error(req,"/state")`): `{rev, settings, layout}`. `PUT /state` (`_state_put` ~L1404): whole lease-check→rev-compare→write→bump under `app.ctx.state_lock`. Lease gate: `active is not None and clientId != active` → **409** `{ok:False,error:"not_active",rev,settings,layout}` (None lease after restart doesn't block). Optimistic concurrency: `baseRev==current rev` → `new_state` rev+1, atomic write, `{ok:True,rev}`; mismatch → **409** `{ok:False,error:"conflict",rev:<live>,settings:<live>,layout:<live>}` (live state inlined for rebase). Durable: `_write_state_atomic` = `tempfile.mkstemp(dir=parent)`+`json.dump`+`os.replace` (atomic same-dir swap, tmp cleaned on fail); startup ~L343 `state=_load_state(path)` restores rev from disk; `_load_state` catches FileNotFound/OSError/JSONDecodeError/ValueError→`_empty_state()`, rejects non-dict, coerces (rev int≥0 else 0, settings/layout dict else {}) → self-heals no crash. Auth = same `_gated_auth_error` as /file/*. No code change. Verification: py_compile PASS; `pytest -k "state or rev or conflict or persist or sync or webterm_state"` → 43 passed/4 skipped/0 failed (incl live `test_state_roundtrip_and_conflict`: 409 body inlines live `{theme:dark}` not rejected value; `test_control_lease`: non-active/id-less PUT→409 not_active, active→200); direct persistence/self-heal: missing→rev0, no leftover tmp, reload restores rev7, corrupt JSON→fresh, bad types coerce — ALL PASS.

**Next to pick:** F078 — Browser-facing `GET /profiles` (webterm/broker/app.py): names-only launchable profiles (default + list; command/cwd never leave the broker), same token-or-loopback gate as /sessions; backs launch (+) menu, per-host remote profile groups, default-profile picker, "Open in folder" (distinct from /mcp/profiles). First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, code+tests usually win (see F018/F022) — verify, correct the feature line, don't "fix" working code. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; do NOT edit index.html for a feature (inspect read-only); only commit every_feature.md + status.md (+ any real .py code fix a feature requires). Frontend (index.html) features = verify by inspection only.
