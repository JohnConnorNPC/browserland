# Loop status — handoff to next iteration

**Just handled:** F018 — CORS → **done** (SPEC-vs-CODE divergence resolved in favor of code; F018 line description corrected). `app.py`: `app.config.AUTO_EXTEND=False` (L312, sanic-ext neutralized → /docs+/openapi.json 404); `_cors_headers` response middleware (L378-399, registered `register_middleware(..,"response")`) sets `Access-Control-Allow-Origin: *` on EVERY response (Sanic runs response mw on error paths → rides 401/404/405). Origin `*` (NOT echo, no Vary); methods `GET, POST, PUT, OPTIONS`; headers `Authorization, Content-Type`; max-age 86400; `Access-Control-Allow-Private-Network: true` echoed on preflight. Explicit OPTIONS routes (L1490-1514, 204) for /sessions /profiles /launch /file/* /session/* /state /mcp/*. **KEY:** CORS is NOT token-gated — module docstring (L11-34) + inline comment (L379-383) document token-gating was the actual BUG (left tokenless LAN/Tailscale brokers unable to answer multi-host cross-origin /sessions); tests `test_cors_without_token`/`test_cors_on_nonloopback_without_token` assert ACAO `*` present WITHOUT token. Security = F017 token+loopback gating on every data/mutation endpoint, not CORS (which only governs browser reads). No code change (re-gating would break tests + re-introduce regression). Corrected the stale F018 description in every_feature.md. Verification: py_compile PASS; `pytest -k "cors or origin or preflight or options or auto_extend or header"` → 8 passed/0 failed (with-token ACAO on 200+401, preflight 204+methods+max-age, error-path 404/405, AUTO_EXTEND off, without-token ACAO `*` present).

**Next to pick:** F019 — Profiles-only launch (webterm/broker/launcher.py, app.py): browser `POST /launch` spawns from pre-approved profiles only (client never supplies raw command/env); a client `cwd` honored only after server validation (realpath + must be existing dir, else 400 bad_cwd/cwd_not_dir) to back "Open in folder"; 200/202/400/401/403/429/500 (non-loopback no token → 403 launch_disabled_no_token). First unchecked, no unmet deps.

**In-progress / failed-attempt markers:** none.

**Reminder for implementers:** browserland is an already-built product — VERIFY documented behavior (and patch real gaps), don't rebuild. Where docs/spec disagree with code+tests, the code+tests usually win (see F018) — verify, correct the feature line, don't "fix" working code to match a stale spec. F045 closed-notes depends on F050's + menu; F058 help-MCP-status depends on F065 — skip-and-return if reached before their deps. NOTE: pre-existing uncommitted change to webterm/broker/index.html (not from this loop) — leave it alone; only commit every_feature.md + status.md (+ any real code fix a feature requires).
