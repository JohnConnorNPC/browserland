# Loop status — handoff to next iteration

**Just handled:** F080 — Build-version / stale-deploy reporting → **done** (BACKEND pytest-verified: 183 passed + ASGI proof, no .py change). build_version()+/mcp/info: `build_version()` (webterm/__init__.py:8-45) `__version__="0.1.0"` (3) + git `rev-parse --short HEAD` on pkg dir (36-41, gated `.git` exists 35), cached `_BUILD_VERSION` (24-26/44), format "0.1.0+<hash>" checkout / bare "0.1.0" no-git; broker caches `app.ctx.version=build_version()` (app.py:317); `_mcp_info` returns `"version":app.ctx.version` (1086). Per-terminal: `_mcp_terminals` (1088-116) entry `version` (1103), `app_cursor` `bool(s["app_cursor"])` (1106), `host` (1100); origins `WindowEntry.summary()` emits version+app_cursor (registry.py:127-28), version seeded from agent hello (241-45), app_cursor cached from agent mode frames (395), host=producer machine hostname (235). machine_host NUANCE (not a gap): broker calls it `host`; MCP TOOL layer `server.py:296-98` renames broker host→`machine_host` + host=config name — so MCP client output has machine_host per spec. stale: `app.py:1113-14` `if s["kind"]=="agent": entry_out["stale"]=version!=broker_version` (broker_version=app.ctx.version 1093); true mismatch, absent non-agent. Tests: `pytest tests/test_protocol.py tests/test_mcptool.py tests/test_registry_agent.py tests/test_altscreen.py -q`→**183 passed** (test_build_version_* protocol.py:39-69, test_info_surfaces_broker_version mcptool:77, test_list_terminals_surfaces_version_and_stale 88, test_hello_version_field_seeds_summary registry_agent:145, machine_host preserve mcptool:845, app_cursor cache registry_agent:90); broker `_mcp_terminals` stale computed proven via throwaway ASGI (3 fake producers: agent match→stale:false, mismatch 0.1.0+DEADBEEF→stale:true+app_cursor, non-agent→no stale key). No gap.

**Next to pick (LAST ACTIONABLE):** F081 — mcptool token-file + routing errors (webterm/mcptool/__main__.py, server.py). BACKEND pytest-verifiable. Verify: `--token-file` sidecar = 4th token tier (precedence `--token` > `$BROWSERLAND_MCP_TOKEN` > `$WEB_TERMINAL_MCP_TOKEN` > `--token-file`); routing errors `malformed_id` (bad `<host>:<int>` id) + `unknown_host` (host name not in --hosts); PER-HOST error aggregation — a down/failing host lands in an `errors` list/field WITHOUT sinking the rest (list_terminals from N hosts returns the healthy ones + errors for the dead one). Confirm token-file is lowest tier (already partly seen F076 `_resolve_token` 44-55), malformed_id/unknown_host raised in `_route`/`_named_client`, list_terminals aggregates per-host errors (try/except per host → errors[]). Run tests (grep `token-file`, `token_file`, `malformed_id`, `unknown_host`, `errors`). Codex flags: token-file precedence LAST, file-missing/unreadable handling, malformed_id vs unknown_host distinction, partial-failure aggregation (one host down ≠ whole call fails), errors shape.

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.** ⚑ AFTER F081: NO unchecked unblocked items remain → TRIGGER FINISH (feature_loop.md FINISH): run the FULL build+test suite ONCE myself (`cd /x/Data/browserland && python -m pytest -q` — the whole tests/ dir), uncheck+re-loop any feature that now fails, then write a FINAL status.md summary (count done / blocked F082+F083 + reasons) and STOP. Note the pip-installed stale `webterm` at X:\Data\web_terminal — run pytest with repo on sys.path (cd into repo; conftest/pytest.ini likely handles it — verify the suite picks up the checkout not the installed copy).

**VERIFICATION MODE — MCP backend:** BACKEND `.py` (app.py, mcptool/server.py+client.py+__main__.py, webterm/__init__.py, registry.py, agent/*). Sub-agent CAN read+patch .py AND run pytest. Tests: `tests/test_mcptool.py` (140) + integration/protocol/registry_agent/broker_e2e. broker via Sanic ASGI / subprocess (throwaway, do NOT commit scratch). real .py gap → may FIX+re-test (I commit .py too). index.html parts = inspect-only. Only commit every_feature.md + status.md (+ real .py fix). F081: __main__.py `_resolve_token` token-file tier + `_parse_hosts`/`_route` errors; server.py `list_terminals` per-host try/except → `errors`.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → CODE WINS when code+tests+issue-history show intent (F018/F022/F069/F076); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079); MY brief's expectation/count CONTRADICTING code-intent+tests is wrong not a gap (F069/F076); approximate field-names in MY brief ≠ gap if the deliverable matches (F070/F080 machine_host). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
