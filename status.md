# Loop status — handoff to next iteration

**Just handled:** F068 — allow_launch + /mcp/launch → **done** (BACKEND verified: client tests + 11/11 ASGI proof, no .py change). allow_launch SEPARATE gate default False: seed `bool(config.get("mcp_allow_launch",False))` (app.py:120, normalize 135-36/1328/1366-67/1378); `_mcp_launch` checks `if not app.ctx.mcp_cfg["allow_launch"]: 403 launch_disabled` (1300-01) INDEPENDENT of default_mode (proved: default_mode=readwrite + allow_launch off→403). Reuses F019 shape: `_mcp_launch` (1296, route 1487) parses `_parse_launch_body` (540) at 1302 — SAME as /launch (609); profile/cols/rows/title/cwd only, NO raw command (cmd from named profile `launcher.launch` 94-111, unknown_profile 400). Response: returns launcher.launch `(status,payload)` verbatim (1305-11), 200 registered (launcher.py:168) / 202 register-timeout (160); cwd realpath+isdir in _parse_launch_body (580-88, cwd_not_dir 400). Tests: client-side only (tests/test_mcptool.py); `pytest tests/test_mcptool.py -k "launch or info"`→11 passed; throwaway ASGI proof 11/11 (allow_launch off→403, on+valid→200, 202 variant, bad profile→400, bad cwd→400, no/wrong token→401, web auth_token rejected, MCP disabled→403 mcp_disabled). No gap.

**RECURRING NOTE (F066/F067/F068):** broker-side /mcp/* + /session/mcp endpoints are CORRECT but have NO committed broker-side tests (coverage is client-side in test_mcptool.py); proven each time via throwaway ASGI scripts. Not a feature gap — a test-hardening opportunity. Surfaced to user.

**Next to pick:** F069 — MCP admin + sidecar (webterm/broker/app.py). BACKEND pytest/ASGI verify: `GET/POST /mcp/config` (read/write enabled/default_mode/allow_launch/token; `generate` to mint a token); TOKEN PRECEDENCE env `WEB_TERMINAL_MCP_TOKEN` > sidecar `webterm_mcp.json` > config; durable sidecar self-heal (writes/repairs webterm_mcp.json). Confirm /mcp/config GET returns public cfg (no token leak) + POST mutates+persists, which token/auth gates /mcp/config (browser auth — it's admin UI from Control Panel MCP section F060), generate mints+returns token once, sidecar precedence + self-heal on corrupt/missing file. Run tests. Codex flags: token never leaked on GET, generate idempotency, env-pin overrides POST, sidecar atomic write/repair, who can admin (web auth vs MCP token).

**In-progress / failed-attempt markers:** none. **BLOCKED: F082 (untab drag-tab-out), F083 (Control Panel full-screen vs modal) — both skip, awaiting user decision.**

**VERIFICATION MODE — MCP (F065-F076):** BACKEND `.py` (app.py, launcher.py, mcptool/server.py+client.py, agent/snapshot/*). Sub-agent CAN read+patch .py AND run pytest. MCP tests=`tests/test_mcptool.py` (140 client-side) + `tests/test_broker_e2e.py` (procs/kill/git); broker behavior via Sanic ASGI test client / subprocess (throwaway, do NOT commit scratch scripts). Verify by EXECUTING tests +/or close-read app.py. real .py gap → may FIX+re-test (I commit .py too). Purely-index.html parts = inspect-only, don't edit index.html. Only commit every_feature.md + status.md (+ real .py fix). app.py MCP: `/mcp/info|terminals|profiles|read|input|launch|config`, `/session/mcp`; `_mcp_auth_error` 1046, `_mcp_effective_mode` 1055, `_mcp_entry` 1072, `_parse_launch_body` 540, `_load_mcp_cfg`/sidecar 104-139, `_mcp_cfg_public` 1324.

**Reminder:** already-built — VERIFY, don't rebuild; ALL deps' code exists so verify fully. Docs/spec vs code disagree → code wins when code+tests+issue-history show intent (F018/F022); USER-FACING WIKI/HELP promise code doesn't meet = real gap→Blocked (F082/F083). Feature LINE source of truth — unstated extras NOT in line/docs aren't gaps (F047/F059/F079). Backend (.py) → pytest-verifiable; RUN the named test. Pre-existing index.html change — leave alone.
