# Screenshots

Drop screenshot images here to populate the **Screenshots** section of the
top-level [`README.md`](../../README.md). The README currently references:

| File | Caption |
|---|---|
| `desktop.png` | The desktop with tiled and floating terminals |
| `agent-fleet.png` | A fleet of AI coding agents, each in its own window |
| `mcp-menu.png` | The per-window MCP access menu |

PNG is assumed, but any web image format works — just match the filenames (or
update the links in the README).
