# Screenshots

This folder holds in-repo screenshot images. The **Screenshots** section of the
top-level [`README.md`](../../README.md) currently embeds hosted images (GitHub
user-attachments URLs), so images dropped here are not picked up automatically —
add them, then update the links in the README to point at them.

PNG is assumed, but any web image format works.
