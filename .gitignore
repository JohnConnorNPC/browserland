__pycache__/
.pytest_cache/
*.pyc

# Editable-install / build metadata
*.egg-info/
.playwright-mcp/
mcps/
broker_config.json

# Shared UI state store (/state) + its atomic-write temp files
webterm_state.json
.webterm-state-*.tmp

# Generic per-mod server store (/mod-store, #124) — sibling of the state store
# (its atomic-write temp files reuse the .webterm-state-*.tmp glob above)
webterm_modstore.json

# Stable broker identity (/info, #64) — sibling of the state store
webterm_identity.json

# Playwright verification screenshots (test artifacts)
/*.png

# Broker runtime logs
*.log
*.log.err

# Scratch (Codex review prompts/output, test upload artifact)
.codex_*.txt
.webterm_upload_test.bin


webterm_mcp.json

# Launch profiles sidecar (#70) — sibling of the state store; the Control Panel
# profile editor persists here, seeded from broker_config's agent.profiles.
webterm_profiles.json

# Claude Code project MCP config (embeds the MCP bearer token)
.mcp.json

# Agent Files
CLAUDE.md
AGENTS.md
.claude/

# Loop/feature tracking scratch
every_feature.md
feature_loop.md
status.md